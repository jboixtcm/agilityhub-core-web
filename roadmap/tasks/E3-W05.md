---
id: E3-W05
title: Gate E3 audit run (web) — real-core e2e, complete e2e, fresh E3 screenshots against the mockups and the spec-test traceability; no product changes
stage: E3
repo: agilityhub-core-web
thread: B
order: 50
status: verified
depends_on: [E3-W04]
branch:
pr:
updated: 2026-09-24
---

# E3-W05 · Gate E3 audit run (web)

## Objective
Jordi asked for an exhaustive review of gate E3 before he closes the stage. Codex and the organizer audit the code separately. This task gives them fresh evidence on the current `main`, after E3-W04:
- the E3 flow against the real core;
- the complete e2e;
- fresh screenshots of every E3 screen, next to its mockup;
- which spec tests exist and pass.

**Change no product code, test or fixture.**
- If something fails, report it exactly: the command, the exit code, and the failing test with its message. Then finish the report anyway.
- Nothing here blocks: set `awaiting_verification` at the end.

## Spec references
- S04 §2 (screens 16, 17, 18, 19, «enviada», 13 → 17/19, D2) and §11 (T-04-29 … T-04-34).
- S14 §11: T-14-25 (D1).
- `roadmap/ROADMAP.md`, section «Gate E3 (front)».

## Steps
1. **Commit and image.** Record `git rev-parse HEAD` and the core image revision that `pnpm e2e:core` pulls. Every result below refers to them.
2. **Real core.** Run `pnpm e2e:core E3-W05`, which covers the E1, E2 and E3 stages. Commit the log and `oauth-token-calls.log` (E3-W04).
3. **Complete e2e.** Run `pnpm e2e:docker`, the three projects in the Playwright image.
4. **Workspace checks.** Run `pnpm turbo run lint typecheck test build` and `pnpm i18n:check`.
5. **Screenshots against the real core**, at 375 px for the PWA and 1280 px for the back office:
   - screens 16, 17, 18, 19 and «enviada»;
   - screen 03 after the welcome link;
   - add-dog 17/19;
   - D1 before and after the validation;
   - D2;
   - the D11 signup toggle.
   
   Reuse the captures of `e2e/core/e3-signup.spec.ts` where they exist. Add only the missing ones, inside the e2e and without changing any assertion.
   
   Then write `roadmap/evidence/E3-W05/screens.md`: one row per screen with the screenshot, its mockup under `docs/pantalles/…`, and every visible difference. Each difference is marked **accepted** (with its decision id: A22, R-03-27, …) or **new**.
6. **Traceability.** Write `roadmap/evidence/E3-W05/trace-tests.md`: every Vitest and Playwright test whose name contains `T-04-` or `T-14-`, with its file and its result from the runs above. Then list, for T-04-29 … T-04-34 and T-14-25, the tests found or «missing».
7. **Gate lines.** For each line of «Gate E3 (front)» in `roadmap/ROADMAP.md`, give one line of evidence from this run.

## Fixed conventions
- No product change of any kind.
- Evidence under `roadmap/evidence/E3-W05/` (logs un-ignored).
- Fictional data only.

## Verification (evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E3-W05/*.log`)
- The commands of steps 2, 3 and 4.

## Definition of Done
- Every step has its evidence.
- `screens.md` covers every E3 screen.
- `trace-tests.md` lists every spec id in scope.
- Any failure is reported with the failing test and its message.

## Evidence to return
The report sections below.

---

## Executor report

### Status
- **Session:** 2026-09-24, claude-opus-5-5.
- **Dependency:** E3-W04 is `awaiting_verification`, which the protocol allows. Nothing here builds on E3-W04 code beyond running it.
- **Results:**
  - **Real core, final run (the Verification command): red.** `pnpm e2e:core E3-W05` → exit 1.
    - The E1/E2 stage passed 10/10.
    - The E3 stage passed 3/4. `T-04-34 signup.enabled=false shows only the configured closed text` failed.
  - **Root cause of the red test: most likely the core.** A temporary diagnostic caught the core serving a stale `GET /signup` (`enabled:true`) 0.5 s after a successful `PUT /parameters/signup.enabled` (`value:false`). See «Questions» #1 and `trace-tests.md` §3.
  - **Earlier green runs:** `01a` (before the extra captures) and `01d` (final code plus the failure-only diagnostic hook, which never fired) passed all 10 + 4. `01d` includes the N-37 check.
  - **Complete e2e:** `pnpm e2e:docker` passed (clubs 25/25, id 1/1, clubs-admin 29/29).
  - **Workspace checks:** `pnpm turbo run lint typecheck test build` passed (exit 0, 34/34 tasks). `pnpm i18n:check` passed (exit 0).
- **What was written:**
  - `screens.md`: 13 screens, every difference marked accepted or new.
  - `trace-tests.md`: 25 named tests; every id in scope has at least one test, none missing. It lists the partial gaps.
- **Scope:** no product code, fixture or assertion was changed.

### Files changed
- `scripts/e2e-core.sh`: the E3 staging test `== "E3-W03" || == "E3-W04"` became `=~ ^E3-W0[345]$`. Without it, `pnpm e2e:core E3-W05` would run E1 + E2 + E3 on one seed, not the two stages the task asks for. This is test infrastructure, not product code.
- `e2e/core/e3-signup.spec.ts`: 3 added captures and **no assertion changed** (`git diff` shows 5 added lines, 0 removed):
  - `13-my-dogs-loaded-core-375.png`: the existing `13-my-dogs-core-375.png` is taken while «Carregant els teus gossos» is on screen;
  - `17-add-dog-core-375.png`: the add-dog step 17 had no capture;
  - `D11-signup-toggle-viewport-core-1280.png`: the full D11 page is about 6700 px high and unreadable.
- **Temporary, removed:** a failure-only diagnostic hook (page URL, text, HTML and screenshot on failure), a record of the `PUT /parameters/signup.enabled` body, and a record of the `GET /signup` responses in the closed-form test. They were used in runs 01d–01g and removed before the final runs 01h and 01, and `git diff` confirms they are gone.
- `roadmap/evidence/E3-W05/`:
  - `screens.md`, `trace-tests.md`;
  - logs `01-e2e-core.log` (final), `01a…01h-*.log`, `02-playwright-all.log`, `03-turbo.log`, `04-i18n.log`;
  - `oauth-token-calls.log` (final) and `01a/01b/01c/01e/01h-oauth-token-calls-*.log`;
  - diagnostics `01e-diag-*`, `01f-diag-*`, `01g-diag-*-pass.txt`;
  - every real-core PNG and JSON the three core specs write, with `n37-notification.json` from run 01d.
- `CHANGELOG.md` (Unreleased → Changed): one entry.
- This task file.

### Rules and tests implemented
- No rule is implemented; this is an audit run.
- Tests in scope: `T-04-29`, `T-04-30`, `T-04-31`, `T-04-32`, `T-04-33`, `T-04-34` and `T-14-25`, mapped test by test in `roadmap/evidence/E3-W05/trace-tests.md` (plus `T-14-26` and `T-14-27`, whose names contain `T-14-`).
- **Missing:** none.
- **Partial:**
  - `T-04-32`: the «Total a pagar al club» line is neither implemented nor asserted.
  - `T-04-34`: the closed-form case is red in 5 of the 8 runs in which it ran.
  - `T-14-25`: the chart shows TER and PENDENT on the real core (E35 code pending).

**Gate E3 (front) lines, with this run's evidence:**
1. *`pnpm e2e:core` green with the E3 scenarios* — **not green in the final run.** It covered public signup → D1 → D2 → welcome mail → 03, rejection → N-03, and add-dog → validation, all green (`01-e2e-core.log`, E3 tests 1–3). The closed form failed on a stale `GET /signup` served by the core. The N-37 row was found in the last green full run (`01d`, `n37-notification.json`). All the runs are listed in «Evidence».
2. *Screenshots of 16–19, enviada, D1, D2 against the mockups; `signup`/`admin-dashboard`/`admin-census` keys complete in ca/es/en; vocabulary lint green* — `screens.md` covers all 13 captures. i18n passed: 2018 keys × 3 locales, 0 unused, 250 error codes, 0 missing. The new differences include two **raw placeholders visible to the public**: `{twoDogsMonthlyFee}` on 18 and `{deadlineDay}` on 19 and on the add-dog 19.
3. *No S04/S14 operation left in `pending.json`; CI green* — `pending.json` paths are `['/members/{id}/notification-preferences']` only, so no S04/S14 path is left (checked with python `json.load`). CI: `gh run list` was denied in this session, so CI was not checked here. The local equivalents are `03-turbo.log` (exit 0) and `02-playwright-all.log` (exit 0).

### Assumptions
- **Core image:** «the core image revision that `pnpm e2e:core` pulls» is the local `ghcr.io/jboixtcm/agilityhub-core-api:main`. The script does not pull, and `docker pull` was denied here. It is `sha256:a3154276c550…`, created 2026-09-24T13:51:51Z, `org.opencontainers.image.revision=29603299a614ae63988fad3c50661ac10edebbf3`.
- **Web commit:** `git rev-parse HEAD` = `17864bbe8901a5d7ae47e0ac109220d0173703eb`, plus this session's working tree.
- **Screenshots:** the captures reused from `e3-signup.spec.ts` are the task's «screenshots against the real core». I added only the missing ones (step 5). `16-signup-closed-core-375.png` is from run `01g`, because the final run failed before that capture.
- **Screen 13:** included in `screens.md` because the task lists «13 → 17/19» in S04 §2.
- **Accepted vs new:** «accepted» cites the decision, rule or task found in `docs/` or in the task files. When no source was found, the difference is marked «new», not guessed.
- **Retries:** I did not retry the final run to get a green result. The task says to report failures, and the failure repeats (5 of 8 runs of this test).

### Questions / catalog proposals
1. **@organizer → api: stale `GET /signup` after `ParameterChanged`.**
   - **Observed** (run 01f, `01f-diag-*.txt`): `PUT /parameters/signup.enabled {"value":false,"version":0}` → `200 value:false version:1` at 14:42:57.016Z. Then two anonymous `GET /api/v1/signup` at .537Z and .543Z → `200`, `Cache-Control: no-cache, no-store`, **`enabled:true`**.
   - **Passing run 01g:** the same PUT, then `enabled:false`.
   - **Rule:** S04 §8 (l.230) says `ParameterChanged` invalidates the `GET /signup` cache (TTL 60 s).
   - **Effect:** it makes `T-04-34 … closed text` red in 5 of 8 runs. It could also let a real club keep taking signups for up to 60 s after closing them.
   - I could not read the api code, because access outside the repo was denied.
   - Blocking: no.
2. **@organizer: unresolved placeholders in the public form texts.**
   - `signup.text.familyGroupIntro` (`{twoDogsMonthlyFee}`) and `signup.text.paymentDay` (`{deadlineDay}`) reach screens 18/19 verbatim from the real core. `SignupPage.tsx:1188/1496` render `config.texts.*` as delivered.
   - The MSW fixtures carry already-resolved texts, so Vitest/Playwright stay green.
   - **Who resolves them?** The api, in `GET /signup.texts` (my assumption: the api owns the variables `twoDogsMonthlyFee` and `deadlineDay`), or the front, with ICU and values from the config (none are exposed today)? Either way, the fixtures should carry the placeholders.
   - Blocking: no.
3. **@organizer: screen 19.** «Total a pagar al club» (T-04-32) is missing. With «Domiciliació», the upfront card ends with the `MANUAL` method's `instructions`, which the seed fills with the cash text. T-04-32 says `cashConditions` only under «Efectiu», and the mockup shows the transfer/Bizum instructions there. Proposal: a follow-up front task, plus a seed check on the api side.
4. **@organizer: branding field proposal `club.taxId`.** It would carry «G63189617» in the public footer, as the mockups show. No branding field carries it today (screens.md S2).
5. **@organizer: D2 identity document.** D2 shows the full DNI; the mockup masks it («47·····2K»), and S04 §2 D2 does not say. Should it follow the masking of the other staff views?
6. **Denied commands, for the record:**
   - `docker pull ghcr.io/jboixtcm/agilityhub-core-api:main`;
   - `gh run list --branch main --limit 5`;
   - `pnpm turbo run lint typecheck test build --force` (I wanted a run without the cache);
   - `sips -c 1800 1280 --cropOffset 0 0 D11-signup-toggle-core-1280.png --out …` (to crop the tall D11 capture; replaced by the viewport capture in the e2e);
   - `ls /Users/jordib/dev/agilityhub/agilityhub-core-api` (to read the api's cache code).
7. **INC-07, still observed:** the final `oauth-token-calls.log`, line 26, has T-01-22 `refresh_token` with `cookie=sent` → `400 {"code":"REFRESH_EXPIRED"}`, 0.3 s after a successful rotation. This is the same as E3-W04's question.

No catalog entries were added: no parameters, events, notifications or error codes.

**Cross-check with the consolidated gate-E3 report** (`roadmap/reviews/gate-E3/consolidated.md`, published during this session; MESSAGES says to finish this task as the pre-fix baseline):
- Questions 2 and 3 are already **M6** (placeholders, located in the api `SignupService`), **M3** and **M7**.
- Question 1 (stale `GET /signup` after `ParameterChanged`) and questions 4–5 do not appear there.

### Evidence

**Earlier attempts of step 2** (one line each + log; the only change between them is noted):
- `01a`: `pnpm e2e:core E3-W05`, before the extra captures → **exit 0**, 10/10 + 4/4. Logs: `01a-e2e-core-before-extra-captures.log`, `01a-oauth-token-calls-before-extra-captures.log`.
- `01b`: with the 3 captures → exit 1. E3 test 1 failed at its first step: `getByText(/Pas 1 de 4/u)` not visible in 15 s, the same symptom as E3-W04 attempt 1. Logs: `01b-e2e-core-failed.log`, `01b-oauth-token-calls-failed.log`.
- `01c`: unchanged → exit 1. The closed-text test failed. Logs: `01c-e2e-core-failed.log`, `01c-oauth-token-calls-failed.log`.
- `01d`: plus the temporary failure-only hook → **exit 0**, 10/10 + 4/4, N-37 row written. Log: `01d-e2e-core-diagnostic.log`.
- `01e`: unchanged → exit 1. The closed-text test failed; the page dump shows the open form. Files: `01e-e2e-core-diagnostic.log`, `01e-diag-closed-form-page.txt`/`-375.png`, `01e-oauth-token-calls-failed.log`.
- `01f`: plus PUT/GET recording, E3 stage only (`pnpm e2e:core E3-W05 e3-signup.spec.ts`) → exit 1. The PUT sent `false` and the GET returned `enabled:true`. Files: `01f-e2e-core-e3-diagnostic.log`, `01f-diag-*.txt`, `01f-diag-closed-form-page-375.png`.
- `01g`: the same, E3 only → **exit 0**; the GET returned `enabled:false`. Files: `01g-e2e-core-e3-diagnostic.log`, `01g-diag-*-pass.txt`.
- `01h`: diagnostics removed, final code → exit 1. The closed-text test failed. Logs: `01h-e2e-core-failed.log`, `01h-oauth-token-calls-failed.log`.

**1. `git rev-parse HEAD`** → exit 0
```
17864bbe8901a5d7ae47e0ac109220d0173703eb
```
`docker image inspect ghcr.io/jboixtcm/agilityhub-core-api:main --format '{{.Id}} {{.Created}} {{json .Config.Labels}} {{json .RepoDigests}}'` → exit 0:
```
sha256:a3154276c550aa47e1c8c4943d14e3c27794c83d2d46d34ea1146b1b8499e4ce 2026-09-24T13:51:51.122418218Z {… "org.opencontainers.image.revision":"29603299a614ae63988fad3c50661ac10edebbf3", … "org.opencontainers.image.version":"main"} ["ghcr.io/jboixtcm/agilityhub-core-api@sha256:a3154276c550aa47e1c8c4943d14e3c27794c83d2d46d34ea1146b1b8499e4ce"]
```

**2. `pnpm e2e:core E3-W05`** (final run) → **exit 1**.
- Full log: `roadmap/evidence/E3-W05/01-e2e-core.log`; token calls: `oauth-token-calls.log`.
- E1/E2 stage (log l.133–143): `[1/10] … T-01-18` … `[10/10] … T-05-CP-07`, then `10 passed (20.3s)`.
- Last 40 lines:
```

    Locator: getByText('Les inscripcions estan tancades temporalment. Torna-ho a provar més endavant o posa\'t en contacte amb el club.')
    Expected: visible
    Timeout: 15000ms
    Error: element(s) not found

    Call log:
      - Expect "toBeVisible" getByText('Les inscripcions estan tancades temporalment. Torna-ho a provar més endavant o posa\'t en contacte amb el club.') with timeout 15000ms
      - waiting for getByText('Les inscripcions estan tancades temporalment. Torna-ho a provar més endavant o posa\'t en contacte amb el club.')


      598 |       "Les inscripcions estan tancades temporalment. Torna-ho a provar més endavant o posa't en contacte amb el club.",
      599 |     ),
    > 600 |   ).toBeVisible();
          |     ^
      601 |   await expect(page.getByRole("button", { name: "CONTINUA" })).toHaveCount(0);
      602 |   await screenshot(page, "16-signup-closed-core-375.png");
      603 |   await publicContext.close();
        at /work/e2e/core/e3-signup.spec.ts:600:5

    attachment #1: oauth-token-calls (text/plain) ──────────────────────────────────────────────────
    2026-09-24T14:52:30.970Z grant_type=refresh_token status=400 cookie=none test="T-04-34 signup.enabled=false shows only the configured closed text" body={"code":"REFRESH_EXPIRED"}
    ────────────────────────────────────────────────────────────────────────────────────────────────

    Error Context: test-results/core-e1/e3-signup-T-04-34-signup-e-f5335--the-configured-closed-text/error-context.md

    attachment #3: trace (application/zip) ─────────────────────────────────────────────────────────
    test-results/core-e1/e3-signup-T-04-34-signup-e-f5335--the-configured-closed-text/trace.zip
    Usage:

        pnpm exec playwright show-trace test-results/core-e1/e3-signup-T-04-34-signup-e-f5335--the-configured-closed-text/trace.zip

    ────────────────────────────────────────────────────────────────────────────────────────────────


  1 failed
    e2e/core/e3-signup.spec.ts:587:5 › T-04-34 signup.enabled=false shows only the configured closed text 
  3 passed (28.9s)

 ELIFECYCLE  Command failed with exit code 1.
```
**Last green full run** (`01d`, final code plus the failure-only hook) → exit 0:
- `10 passed (19.9s)` (l.142) and `4 passed (12.6s)` (l.274);
- then `{"action":"OPEN_DOG","channel":"APP","code":"N-37","dogName":"Neret E3","status":"SENT"}` → `n37-notification.json`.

**3. `pnpm e2e:docker`** (`mcr.microsoft.com/playwright:v1.63.0-noble`; run before the three capture lines were added, which touch only `e2e/core`, a directory this suite does not run) → **exit 0**. Full log: `02-playwright-all.log`. Last 40 lines:
```
clubs-admin:build: [INEFFECTIVE_DYNAMIC_IMPORT] ../../packages/i18n/src/locales/es/shell.json is dynamically imported by ../../packages/i18n/src/resources.ts but also statically imported by ../../packages/i18n/src/catalog.ts, dynamic import will not move module into another chunk.
clubs-admin:build: 
clubs-admin:build: ✓ built in 237ms
clubs:e2e: cache bypass, force executing c34e9f9cbc3ab9fa
clubs:e2e: 
clubs:e2e: > clubs@0.0.0 e2e /work/apps/clubs
clubs:e2e: > playwright test
clubs:e2e: 
clubs:e2e: [WebServer] (node:662) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs:e2e: 
clubs:e2e: Running 25 tests using 6 workers
clubs:e2e: ·························
clubs:e2e:   25 passed (11.8s)
id:e2e: cache bypass, force executing b0af58c6a58b6b3e
id:e2e: 
id:e2e: > id@0.0.0 e2e /work/apps/id
id:e2e: > playwright test
id:e2e: 
id:e2e: 
id:e2e: Running 1 test using 1 worker
id:e2e: ·
id:e2e:   1 passed (2.8s)
clubs-admin:e2e: cache bypass, force executing afe4cc50daff7c8f
clubs-admin:e2e: 
clubs-admin:e2e: > clubs-admin@0.0.0 e2e /work/apps/clubs-admin
clubs-admin:e2e: > playwright test
clubs-admin:e2e: 
clubs-admin:e2e: [WebServer] (node:2182) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs-admin:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs-admin:e2e: 
clubs-admin:e2e: Running 29 tests using 9 workers
clubs-admin:e2e: ·····························
clubs-admin:e2e:   29 passed (16.1s)

 Tasks:    8 successful, 8 total
Cached:    0 cached, 8 total
  Time:    43.471s 

exit=0
```

**4a. `pnpm turbo run lint typecheck test build`** → **exit 0**.
- Full log (ANSI codes kept): `03-turbo.log`.
- 20 of the 34 tasks were replayed from the Turbo cache. The inputs were the same, and `--force` was denied. The `clubs`/`clubs-admin`/`id`/`api-client`/`auth` test and typecheck tasks were cache misses and really ran.
- `e2e/core` is not a Turbo package. After the edits, `pnpm exec eslint --max-warnings 0 e2e/core/e3-signup.spec.ts` and `pnpm exec tsc -p e2e/core/tsconfig.json --noEmit` → exit 0.
- Last 40 lines (ANSI stripped):
```
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > R-06-11 lists the RING_BLOCK_CONFLICT conflicts inside the block drawer
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/TemplatesPage.test.tsx > T-06-26 D3 weekly templates > R-06-15 offers instructor chips with maxInstructorsPerClass = 2
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/TemplatesPage.test.tsx > T-06-26 D3 weekly templates > R-06-15 hides levels and coverage with levels.enabled = false and requires a manual description
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test:  ✓ src/planning/TemplatesPage.test.tsx (13 tests) 3494ms
clubs-admin:test:    ✓ T-06-26 D3 weekly templates (13)
clubs-admin:test:      ✓ prefills band and day from an empty cell, fills the description by levels, keeps the manual text and asks for another class in the band 1044ms
clubs-admin:test:      ✓ saves every change of a full cell at once with the template version 554ms
clubs-admin:test:      ✓ queues two quick changes of a full cell and sends each PATCH with the latest version 311ms
clubs-admin:test:      ✓ reloads the template and remounts the card on STALE_VERSION 342ms
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > INSTRUCTOR reads the calendar without actions and with inert chips (A22 c)
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > shows the week warnings and selects the class of a warning; day headers open the day view
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test:  ✓ src/planning/CalendarPage.test.tsx (19 tests) 3644ms
clubs-admin:test:    ✓ T-06-28 D4 / D4b / D4c class calendar (front half, MSW) (19)
clubs-admin:test:      ✓ opens «Esborrany» on the first week with drafts, with the validation card and its count 796ms
clubs-admin:test:      ✓ cancels an active class with registrants through D4c: four rows, text required, «ANUL·LA I AVISA ELS 4 ALUMNES» 323ms
clubs-admin:test:      ✓ R-06-09 saves the diff with the version; STALE_VERSION keeps its message after the refetch remounts the card 381ms
clubs-admin:test: 
clubs-admin:test:  Test Files  15 passed (15)
clubs-admin:test:       Tests  101 passed (101)
clubs-admin:test:    Start at  16:27:17
clubs-admin:test:    Duration  6.81s (environment 40%, tests 31%, transform 15%, setup 8%, import 5%)
clubs-admin:test: 

 Tasks:    34 successful, 34 total
Cached:    20 cached, 34 total
  Time:    11.958s 

exit=0
```

**4b. `pnpm i18n:check`** → **exit 0** (`04-i18n.log`, complete):
```

> agilityhub-core-web@0.0.0 i18n:check /Users/jordib/dev/agilityhub/agilityhub-core-web
> node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs

i18n check passed
ca: 2018 keys
es: 2018 keys
en: 2018 keys
1460 statically used keys, 0 unused
250 codes, 0 missing
exit=0
```

**5–6.** Screenshots: `roadmap/evidence/E3-W05/screens.md`. Traceability: `roadmap/evidence/E3-W05/trace-tests.md`.

### Not run / notes
- CI on `main` was not checked (`gh` denied).
- The Playwright traces of the failed core runs stay inside the discarded container. The compose file does not copy `test-results/` back. The page dumps `01e`/`01f-diag-*` replace them for the closed-form failure.
- The first-step failure of `01b` («Pas 1 de 4» not visible) was not caught by the diagnostic, so its cause is unknown. It is the same symptom as E3-W04 attempt 1.

## Organizer verification
Result: verified · 2026-09-24 · organizer (Claude) — accepted as the **pre-fix baseline** of the gate E3 audit

Every step has its evidence.
- `screens.md` covers the 13 captures.
- `trace-tests.md` has no missing id; the partial ones are T-04-32, T-04-34 and T-14-25.
- The organizer read the report, `screens.md`, and the diagnostics of runs `01f`/`01g`.
- The harness change (`^E3-W0[345]$`) is fine. E3-W06 step 0 extends it to W06–W09.

**The red closed-form test is an api defect, confirmed in the code.** The configuration cache of `GET /signup` is evicted only by the outbox consumers (`SignupConfigurationEvents`; `SignupService.java:30-32, 210-212`), so a read 0.5 s after the `PUT` can still say `enabled: true`. → **api E3-T09 step 5**, which evicts after commit and decides `SIGNUP_CLOSED` on the committed parameter.

**Rulings on your questions:**
1. The stale `GET /signup`: api E3-T09 step 5 (above).
2. and 3. They were already in the gate audit: M6 (api E3-T08), M3 and M7 (web E3-W08 and E3-W06, api E3-T08).
4. **Accepted.** `/branding` gets `legalName` and `taxId`, which are public identifiers of the club that LSSI art. 10 requires on the web. S02 R-02-02 is amended. → api E3-T10 step 12, web E3-W08 step 5.
5. **D2 shows the DNI/NIE in full, as D10 does.** R-03-27 masks it only on 28, and the mockup's «47·····2K» is illustrative. S04 §2 D2 is amended.
6. Noted.
7. INC-07: your two runs gave the body. It is in the register (`docs/INCIDENCIES_OBERTES.md`).

**The new differences of `screens.md`:**
- 16 and 17 (the plan cards, no zero entry line, «Població» as a selector, the ⓘ icon), and the shell (footer, globe icon) → **E3-W08 step 5**.
- D1 (the risk card capped at 6 rows, «7:30», titles, greeting, badge) → **E3-W07 step 9**.
- D2 ([REBUTJA] outline, «Mascle», the WhatsApp chip; the DNI in full) → **E3-W07 step 8**.
- S4, the labels above the fields: **accepted**. Accessibility (AGENTS §6) wins over the mockup's in-field labels.
- S6, the primary button at 3.32:1: a brand decision → question **A32** to Jordi. The recommendation is dark text on the same orange (6.3:1).

**Independent review** `roadmap/reviews/E3-W05-20260924-1702-codex.md` (it arrived after this verification). Its four points are about the accuracy of the evidence, not about the product:
- `trace-tests.md` rates T-04-33 and T-04-29 higher than the tests prove;
- rows are collapsed;
- some `screens.md` descriptions do not match the final PNGs (D1 shows 15 alerts and 0/58, not 16 and 0/75);
- «accepted» is used for defects that already have a fix task.

The gaps themselves are already in the fix plan: M2, M13 and M15, E35, and E4-W06 for the D11 keys. The baseline is not reopened; the rules go into the re-run **E3-W09** (steps 5 and 6).
