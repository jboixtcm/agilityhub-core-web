---
id: E2-W07
title: E2 integration against the core (staging/local) and gate evidence (front)
stage: E2
repo: agilityhub-core-web
thread: B
order: 70
status: verified
depends_on: [E2-W01, E2-W02, E2-W03, E2-W04, E2-W05, E2-W06, E2-W08, E2-W09, external:api:E2-T11]
branch: 
pr: 
updated: 2026-09-19
---

# E2-W07 · E2 integration against the core (staging/local) and gate evidence (front)

## Objective
Work package WP-03-G (front part): the E2 screens (census, records, catalogs, parameters, club pages, audit, exports) work against the **real core** with the fictional demo census — gate E2 (front) evidence, plus the polish list collected by the organizer during the E2 verifications.

## Spec references
S03 T-03-42 (E2E: D5 → D10 → booking block → 13 shows the reason → 28 saves) · S02 (D11 parameter change with history) · S05 (catalog CRUD) · S14 R-14-12 (export download) and the audit trail · gate E2 (front) in `roadmap/ROADMAP.md`.

## Context to load
`roadmap/ROADMAP.md` (gate E2) · E1-W04 (`scripts/e2e-core.sh`, `scripts/core-stack/docker-compose.yml`, `e2e/core/e1-core.spec.ts`, `e2e/core/playwright.config.ts`: the published-core stack with Mongo, seed, mailbox and the Playwright container) · the E2 screens (E2-W01…W06, W08, W09) · `packages/ui/src/tokens.css`, `packages/ui/src/limited-markdown.tsx` (polish d, e) · this file.

## Steps
0. **Stack**: reuse the E1-W04 stack (`ghcr.io/jboixtcm/agilityhub-core-api:main`, pulled by the session wrapper; `main` now contains the whole E2 api + E3-T01/T02/T06). Add the demo census to the `seed` service after the two `club:apply` runs: `java -jar /app/app.jar --core.command=seed:demo --club=canic --seed=42` (the image ships `/app/seeds/demo-canic.yaml`; the command reads `seeds/demo-<slug>.yaml` relative to `/app`, so keep the working directory) — it needs the Cànic plans and levels, so apply the image's own `/app/seeds/club-canic.yaml` (fictional accounts, `SEED_PASSWORD` = `E1_CORE_PASSWORD`) instead of the E1 copy if that copy lacks them; expected totals printed by the command: 184 active members / 242 dogs. Generalise `scripts/e2e-core.sh` (evidence directory and project name per task via variables; keep the E1 defaults) rather than copying it.
1. **`e2e/core/e2-core.spec.ts`** (serial, real core, seeded accounts): (a) T-03-42 — D5 list with the 184-member seed, filter by level + status, open a D10 record, «Bloqueja les reserves» with a reason, log in as that member in the clubs app: screen 13 shows the reason, screen 28 saves a phone change; (b) catalogs round trip — create/edit/deactivate a level, a ring and a FAQ entry in D16/D3/D11 and see them in the lists; (c) D11 — change `bookings.lateCancelThresholdMinutes` and see the history/`lastChange` row; (d) export — «Excel» on D5 with visible columns → `200` download with the response filename (assert the request URL and the status; the file itself is not inspected); (e) audit — the parameter change and the booking block appear in `/auditoria` and in the member's audit tab with masked values; (f) club pages — edit the RULES page in D11 and see it on screen 30 «Info».
2. **Performance**: D5 with the 184-member seed and two filters answers < 500 ms — measure the `/members` request with `performance.getEntriesByType("resource")` (duration) over five loads and paste the numbers (median and max); if it exceeds 500 ms, report it (INC candidate) — do not tune the api here.
3. **Screenshots** of every E2 screen against real data (D3, D5, D8, D10, D11 with history, D15, D16, D17, 13, 28, 30, /auditoria) at 1280 / 375 under `roadmap/evidence/E2-W07/`; compare with `docs/pantalles/` and list any deviation you notice (without fixing it, unless it is in the polish list below).
4. **Polish list collected by the organizer during E2 verifications** (small literal/label fixes, no new behaviour; apply all): (a) D11 `coverage.thresholds` summary must use the D3 vocabulary «bé > {ok} % · ajustat {tight}–{ok} % · manca oferta {short}–{tight} % · cal ampliar < {short} %» (es/en equivalents) instead of «correcta / ajustada / curta»; (b) the catalog row `classes.defaultCapacity` label «Aforament per nivell» → «Aforament per defecte (nivell sense valor)» so it does not clash with the derived row «Aforament: per nivell …»; (c) any other literal that Josep flags in the staging review of E2 screens (list them in the report); (d) **brand font**: `packages/ui/src/tokens.css` names «Montserrat» but nothing loads it (the E1-W04 screenshots from the Playwright container fall back to a serif) — self-host Montserrat (OFL) as woff2 in `packages/ui` — simplest: the `@fontsource/montserrat` npm package (OFL, woff2 per weight; import `400/500/600/700` latin from `packages/ui` so the three apps bundle it, `font-display: swap`) — and check the fallback stack in `tokens.css`; **never** Google Fonts (GDPR: no third-party requests from the apps). (e) **limited-Markdown content styles**: headings, bold, lists and links rendered by `packages/ui/src/limited-markdown.tsx` must be visibly styled in the D11 editor preview and in screen 30 «Info» (the E2-W09 screenshots show «## Convivència» as plain text). (f) audit list dates through the shared `fmtDateTime` (`dd/MM/yyyy HH:mm`), not the browser default («31/7/26»); (g) audit change drawer: known paths → field labels (`admin-census` keys), raw path as fallback. (h) **flaky unit test**: `apps/clubs-admin/src/census/CensusListPage.test.tsx` T-03-38 («184 d'alta» not found on the 2-core CI runner — failed at `f21b657`, `a090d1c`, `3086664`, green on rerun): make it deterministic — await the list and the shell counters with `findBy…`/`waitFor`, mock the `/dashboard/counters` request explicitly, no fixed timers; run it 10× locally (`vitest --run --repeat`/loop) and paste the tail.
5. `ROADMAP.md` is organizer-owned: put the gate E2 (front) evidence (what ran, counts, timings) in your report; the organizer ticks the checklist.

## Fixed conventions
- Same Docker wrapper as E1-W04 (`pnpm e2e:core`); the stack is self-cleaning; no secrets printed (`E1_CORE_PASSWORD` generated at runtime); fictional data only (`@example.test`).
- The MSW/unit suites stay untouched except where a polish item changes a literal (update the affected snapshots/tests, ca/es/en together; keys, never literals in code).
- No new parameter, event, notification or error code; no api changes (report anything the core would need as a proposal).

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `CORE_URL=http://core:8080 pnpm e2e:core` green (E1 + E2 scenarios; paste the tails and keep the full logs under `roadmap/evidence/E2-W07/`); the complete `pnpm e2e` (three MSW projects) in the Playwright image after the polish changes; `pnpm turbo run lint typecheck test build` + `pnpm i18n:check` green; CI green after publication.

## Definition of Done
Integration suite green against the published core with the demo census; performance numbers pasted; polish list (a)–(g) applied with screenshots; gate E2 (front) evidence in the report.

## Evidence to return
`e2e:core` and `e2e` tails, the performance table, the screenshot list, the polish diff summary.

---

## Executor report

### Branch / commits

- Resumed the existing `in_progress` task after the published-core seed blocker was corrected. This continuation changed only the working tree; no branch, commit, or other git-writing command was run.

### Evidence (full command outputs)

0. Published-core preflight — exit `0`; complete output: `roadmap/evidence/E2-W07/00-image-inspect.log`.

   Command: `docker image inspect ghcr.io/jboixtcm/agilityhub-core-api:main --format '{{.Architecture}} {{index .Config.Labels "org.opencontainers.image.revision"}}'`

   ```text
   arm64 cce6f0fbd347706ade619cc454c7449b053b6f31
   ```

1. Published-core E1 + E2 integration suite — exit `0`; complete 138-line output: `roadmap/evidence/E2-W07/01-e2e-core.log`.

   Command: `COREPACK_HOME=/private/tmp/e2-w07-corepack CORE_EVIDENCE_SUBDIRECTORY=E2-W07 CORE_PROJECT_NAME=agilityhub-e2-w07 CORE_URL=http://core:8080 pnpm e2e:core`

   Last 40 lines:

   ```text
   + eslint-plugin-react 7.37.5
   + eslint-plugin-react-hooks 7.1.1
   + globals 17.12.0
   + husky 9.1.7
   + i18next-parser 9.4.0
   + lint-staged 17.5.0
   + msw 2.15.0
   + prettier 3.9.6
   + size-limit 13.0.3
   + turbo 2.10.12
   + typescript 5.9.3
   + typescript-eslint 8.69.0

   . prepare$ node packages/config/scripts/prepare-husky.mjs
   . prepare: .git can't be found
   . prepare: Done
   Done in 11.9s using pnpm v9.15.9
   [WebServer] (node:134) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
   [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)


   [WebServer] (node:214) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
   [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
   [WebServer] (node:312) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
   [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)


   Running 10 tests using 1 worker

   [1/10] e2e/core/e1-core.spec.ts:154:5 › T-01-18 password login reaches the seeded account
   [2/10] e2e/core/e1-core.spec.ts:170:5 › T-01-21 seeded administrator opens the real backoffice
   [3/10] e2e/core/e1-core.spec.ts:186:5 › T-01-20 club session is restored by the core
   [4/10] e2e/core/e1-core.spec.ts:209:5 › T-01-19 magic link is delivered and exchanged end to end
   [5/10] e2e/core/e1-core.spec.ts:231:5 › T-01-26 published seed enters without pending onboarding
   [6/10] e2e/core/e1-core.spec.ts:241:5 › T-01-22 apps/id resumes a real authorize flow through oauth2/session
   [7/10] e2e/core/e2-core.spec.ts:204:5 › T-03-42 real census flow, booking block and member profile
   [8/10] e2e/core/e2-core.spec.ts:335:5 › T-05-22/T-05-25 real catalog create, edit and deactivate round trips
   [9/10] e2e/core/e2-core.spec.ts:419:5 › T-02-13/T-14-26 real parameter history, export and audit
   [10/10] e2e/core/e2-core.spec.ts:482:5 › T-05-CP-07 real club page edit renders styled Markdown on screen 30
     10 passed (25.0s)
   ```

   The real-data assertions verify `184 d'alta` on D5 and `242 actius` on D15. The seed, core, three web apps and Playwright containers are created and removed by the shared wrapper.

2. T-03-38 deterministic unit-test repetition — exit `0`; complete 90-line output: `roadmap/evidence/E2-W07/02-census-list-repeat.log`.

   Command: `for run_number in {1..10}; do COREPACK_HOME=/private/tmp/e2-w07-corepack pnpm --filter clubs-admin exec vitest run src/census/CensusListPage.test.tsx -t "renders applied filters and preserves selection when page size changes"; done`

   Last 40 lines:

   ```text
         Tests  1 passed | 2 skipped (3)
      Start at  10:54:33
      Duration  944ms (tests 41%, environment 27%, transform 18%, import 9%, setup 6%)


    RUN  v5.0.0 /Users/jordib/dev/agilityhub/agilityhub-core-web/apps/clubs-admin


    Test Files  1 passed (1)
         Tests  1 passed | 2 skipped (3)
      Start at  10:54:35
      Duration  968ms (tests 39%, environment 27%, transform 19%, import 10%, setup 5%)


    RUN  v5.0.0 /Users/jordib/dev/agilityhub/agilityhub-core-web/apps/clubs-admin


    Test Files  1 passed (1)
         Tests  1 passed | 2 skipped (3)
      Start at  10:54:36
      Duration  974ms (tests 40%, environment 28%, transform 17%, import 9%, setup 6%)


    RUN  v5.0.0 /Users/jordib/dev/agilityhub/agilityhub-core-web/apps/clubs-admin


    Test Files  1 passed (1)
         Tests  1 passed | 2 skipped (3)
      Start at  10:54:37
      Duration  960ms (tests 40%, environment 27%, transform 18%, import 9%, setup 6%)


    RUN  v5.0.0 /Users/jordib/dev/agilityhub/agilityhub-core-web/apps/clubs-admin


    Test Files  1 passed (1)
         Tests  1 passed | 2 skipped (3)
      Start at  10:54:39
      Duration  969ms (tests 40%, environment 27%, transform 18%, import 9%, setup 6%)
   ```

3. Complete three-project MSW Playwright suite in the pinned Docker image — exit `0`; complete 333-line output: `roadmap/evidence/E2-W07/03-e2e.log`.

   Command:

   ```sh
   COREPACK_HOME=/private/tmp/e2-w07-corepack bash scripts/e2e-docker.sh > /private/tmp/E2-W07-03-e2e.log 2>&1
   run_status=$?
   cp /private/tmp/E2-W07-03-e2e.log roadmap/evidence/E2-W07/03-e2e.log
   exit $run_status
   ```

   Last 40 lines:

   ```text
   id:build: - Using dynamic import() to code-split the application
   id:build: - Use build.rolldownOptions.output.codeSplitting to improve chunking: https://rolldown.rs/reference/OutputOptions.codeSplitting
   id:build: - Adjust chunk size limit for this warning via build.chunkSizeWarningLimit.
   id:build: ✓ built in 281ms
   clubs-admin:e2e: cache bypass, force executing 00bf3d647f32b80a
   clubs-admin:e2e:
   clubs-admin:e2e: > clubs-admin@0.0.0 e2e /work/apps/clubs-admin
   clubs-admin:e2e: > playwright test
   clubs-admin:e2e:
   clubs-admin:e2e: [WebServer] (node:609) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
   clubs-admin:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
   clubs-admin:e2e:
   clubs-admin:e2e: Running 25 tests using 9 workers
   clubs-admin:e2e: ·························
   clubs-admin:e2e:   25 passed (17.8s)
   clubs:e2e: cache bypass, force executing a6883993709561f1
   clubs:e2e:
   clubs:e2e: > clubs@0.0.0 e2e /work/apps/clubs
   clubs:e2e: > playwright test
   clubs:e2e:
   clubs:e2e: [WebServer] (node:2300) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
   clubs:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
   clubs:e2e:
   clubs:e2e: Running 21 tests using 5 workers
   clubs:e2e: ·····················
   clubs:e2e:   21 passed (13.1s)
   id:e2e: cache bypass, force executing aa36edd1a7e0d23a
   id:e2e:
   id:e2e: > id@0.0.0 e2e /work/apps/id
   id:e2e: > playwright test
   id:e2e:
   id:e2e:
   id:e2e: Running 1 test using 1 worker
   id:e2e: ·
   id:e2e:   1 passed (3.3s)

    Tasks:    6 successful, 6 total
   Cached:    0 cached, 6 total
     Time:    45.95s
   ```

4. Full workspace lint, typecheck, unit tests and build — exit `0`; complete 672-line output: `roadmap/evidence/E2-W07/04-turbo.log`.

   Command: `COREPACK_HOME=/private/tmp/e2-w07-corepack pnpm turbo run lint typecheck test build`

   Last 40 lines:

   ```text
   clubs-admin:build: dist/assets/admin-catalogs-Coe9B6Bf.js                    5.48 kB │ gzip:   2.16 kB
   clubs-admin:build: dist/assets/admin-catalogs-CvR4YWAF.js                    5.68 kB │ gzip:   2.19 kB
   clubs-admin:build: dist/assets/census-2y4pkTWD.js                            5.96 kB │ gzip:   2.39 kB
   clubs-admin:build: dist/assets/census-CfjHQ4Uz.js                            6.29 kB │ gzip:   2.64 kB
   clubs-admin:build: dist/assets/census-C9vkPZEn.js                            6.47 kB │ gzip:   2.67 kB
   clubs-admin:build: dist/assets/admin-census-DiNmga7T.js                      7.99 kB │ gzip:   3.12 kB
   clubs-admin:build: dist/assets/admin-census-CCOgIr-r.js                      8.44 kB │ gzip:   3.36 kB
   clubs-admin:build: dist/assets/admin-census-BmWfWPjz.js                      8.75 kB │ gzip:   3.44 kB
   clubs-admin:build: dist/assets/admin-settings-ed4raEOS.js                   17.01 kB │ gzip:   6.05 kB
   clubs-admin:build: dist/assets/admin-settings-DWM9_lDb.js                   18.15 kB │ gzip:   6.62 kB
   clubs-admin:build: dist/assets/admin-settings-D3Fbd__C.js                   18.51 kB │ gzip:   6.62 kB
   clubs-admin:build: dist/assets/errors-BX3oK7A4.js                           21.71 kB │ gzip:   7.06 kB
   clubs-admin:build: dist/assets/errors-CB3DYjgP.js                           23.04 kB │ gzip:   7.47 kB
   clubs-admin:build: dist/assets/errors-DQYQlfkJ.js                           23.05 kB │ gzip:   7.42 kB
   clubs-admin:build: dist/assets/browser-BKzJ6V0j.js                         531.44 kB │ gzip: 187.61 kB
   clubs-admin:build: dist/assets/index-Qq_L5-FQ.js                           730.84 kB │ gzip: 293.00 kB
   clubs-admin:build:
   clubs-admin:build: [INEFFECTIVE_DYNAMIC_IMPORT] ../../packages/i18n/src/locales/ca/common.json is dynamically imported by ../../packages/i18n/src/resources.ts but also statically imported by ../../packages/i18n/src/catalog.ts, dynamic import will not move module into another chunk.
   clubs-admin:build:
   clubs-admin:build: [plugin builtin:vite-reporter]
   clubs-admin:build: (!) Some chunks are larger than 500 kB after minification. Consider:
   clubs-admin:build: - Using dynamic import() to code-split the application
   clubs-admin:build: - Use build.rolldownOptions.output.codeSplitting to improve chunking: https://rolldown.rs/reference/OutputOptions.codeSplitting
   clubs-admin:build: - Adjust chunk size limit for this warning via build.chunkSizeWarningLimit.
   clubs-admin:build: [INEFFECTIVE_DYNAMIC_IMPORT] ../../packages/i18n/src/locales/ca/shell.json is dynamically imported by ../../packages/i18n/src/resources.ts but also statically imported by ../../packages/i18n/src/catalog.ts, dynamic import will not move module into another chunk.
   clubs-admin:build:
   clubs-admin:build: [INEFFECTIVE_DYNAMIC_IMPORT] ../../packages/i18n/src/locales/en/common.json is dynamically imported by ../../packages/i18n/src/resources.ts but also statically imported by ../../packages/i18n/src/catalog.ts, dynamic import will not move module into another chunk.
   clubs-admin:build:
   clubs-admin:build: [INEFFECTIVE_DYNAMIC_IMPORT] ../../packages/i18n/src/locales/en/shell.json is dynamically imported by ../../packages/i18n/src/resources.ts but also statically imported by ../../packages/i18n/src/catalog.ts, dynamic import will not move module into another chunk.
   clubs-admin:build:
   clubs-admin:build: [INEFFECTIVE_DYNAMIC_IMPORT] ../../packages/i18n/src/locales/es/common.json is dynamically imported by ../../packages/i18n/src/resources.ts but also statically imported by ../../packages/i18n/src/catalog.ts, dynamic import will not move module into another chunk.
   clubs-admin:build:
   clubs-admin:build: [INEFFECTIVE_DYNAMIC_IMPORT] ../../packages/i18n/src/locales/es/shell.json is dynamically imported by ../../packages/i18n/src/resources.ts but also statically imported by ../../packages/i18n/src/catalog.ts, dynamic import will not move module into another chunk.
   clubs-admin:build:
   clubs-admin:build: ✓ built in 203ms

    Tasks:    26 successful, 26 total
   Cached:    22 cached, 26 total
     Time:    9.042s
   ```

5. i18n parity, forbidden vocabulary, unused-key and error-code checks — exit `0`; complete output: `roadmap/evidence/E2-W07/05-i18n.log`.

   Command: `COREPACK_HOME=/private/tmp/e2-w07-corepack pnpm i18n:check`

   ```text
   > agilityhub-core-web@0.0.0 i18n:check /Users/jordib/dev/agilityhub/agilityhub-core-web
   > node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs

   i18n check passed
   ca: 1734 keys
   es: 1734 keys
   en: 1734 keys
   1212 statically used keys, 0 unused
   249 codes, 0 missing
   ```

6. D5 filtered `/members` resource durations from the real-core run, in milliseconds (`roadmap/evidence/E2-W07/performance-members.json`):

   | Load | Duration |
   |---:|---:|
   | 1 | 51.5 ms |
   | 2 | 45.5 ms |
   | 3 | 46.8 ms |
   | 4 | 57.7 ms |
   | 5 | 57.7 ms |
   | Median | 51.5 ms |
   | Max | 57.7 ms |

   Median and maximum are below the 500 ms gate; no INC candidate is needed.

7. Real-core visual evidence at 1280 px / 375 px:

   - D3/D5/D8/D10/D11/D15/D16/D17/audit: `D3-plantilles-placeholder-core-1280.png`, `D5-abonats-core-1280.png`, `D8-modalitats-core-1280.png`, `D10-abonat-core-1280.png`, `D10-abonat-bloquejat-core-1280.png`, `D11-parametres-catalogs-core-1280.png`, `D11-parametre-historic-core-1280.png`, `D11-pagina-club-preview-core-1280.png`, `D15-gossos-core-1280.png`, `D16-pistes-core-1280.png`, `D17-equip-core-1280.png`, `auditoria-core-1280.png`.
   - Mobile screens: `13-els-meus-gossos-core-375.png`, `28-les-meves-dades-core-375.png`, `30-info-core-375.png`.
   - The shared real-core regression run also refreshed the E1 access/account screenshots in this evidence directory.

   Visual comparison notes:

   - D3 remains the deliberate `Aviat` placeholder until E4-W01; this is an E2 baseline capture, not final D3 fidelity.
   - D5 at 1280 px is denser than the wide approved mockup and truncates some search/filter copy while the filter panel is open; behavior and required data remain visible.
   - D10, D11, screens 13 and 28 differ in fictional records/state from the approved instance. D11 is taller because it renders the complete current catalog and open history state; screen 28 shows manual-payment fields for the seeded member.
   - Screen 30 intentionally captures the published `Normes` tab used by the integration flow rather than the FAQ tab in the base mockup; heading, bold, list and link styles are visibly applied.

8. Earlier attempts, retained as one-line diagnosis plus full log as required:

   - `00-e2e-core-corepack-failed.log`: Corepack could not create its default cache (`EPERM`) → all final commands use `COREPACK_HOME=/private/tmp/e2-w07-corepack`.
   - `02-e2e-core.log`–`06-e2e-core.log`: early E1 authentication and T-03-42 navigation/readiness races → aligned real-seed accounts and waited for the exact API responses and rendered data.
   - `07-e2e-core.log`: SPA navigation destroyed the page execution context → replaced that transition with the stable route helper/ready assertion.
   - `08-e2e-core.log` and `11-e2e-core.log`: interrupted wrapper captures without a final result → superseded by the complete final `01-e2e-core.log`.
   - `09-e2e-core.log`, `10-e2e-core.log`, `01-e2e-core-rate-limit-failed.log` and `03-e2e-core-token-rate-failed.log`: the core-wide OAuth token limit returned `429` late in the serial run → reused one administrator context and used the second seeded member only for the final member page.
   - `02-e2e-core-shared-page-failed.log`: reusing the same member page did not rerender the second target route → retained isolated member contexts while keeping token creation within the core limit.
   - `03-e2e-contention-failed.log` and `04-e2e-contention-failed.log`: concurrent root Playwright projects timed out on the constrained Docker runner → root `pnpm e2e` now runs Turbo with `--concurrency=1`, matching CI.
   - `05-turbo-redundant-condition-failed.log`: strict lint found six generated-type redundant conditions → narrowed the audit/settings/census consumers to the exact generated shapes.

### Files changed

- Real-core harness: `scripts/core-stack/docker-compose.yml`, `scripts/e2e-core.sh`, `e2e/core/e1-core.spec.ts`, `e2e/core/e2-core.spec.ts`, `e2e/core/playwright.config.ts`.
- E2 UI polish and deterministic tests: `apps/clubs-admin/src/audit/AuditPage.tsx`, `AuditPage.test.tsx`, `apps/clubs-admin/e2e/audit.spec.ts`, `apps/clubs-admin/src/catalogs/ParameterSettings.tsx`, `apps/clubs-admin/src/census/CensusListPage.test.tsx`, `CensusRecordPage.tsx`.
- Shared UI/font/Markdown: `packages/ui/package.json`, `src/index.ts`, `src/tokens.css`, `src/limited-markdown.tsx`, `src/limited-markdown.css`, `src/limited-markdown.test.tsx`, and `pnpm-lock.yaml`.
- Locale copy: `packages/i18n/src/locales/{ca,es,en}/admin-settings.json` and `admin-audit.json`.
- Deterministic complete Playwright execution: root `package.json` serializes Turbo e2e projects; `.gitignore` admits the E2-W07 evidence logs.
- Evidence/documentation: `roadmap/evidence/E2-W07/*`, `CHANGELOG.md`, generated `roadmap/STATUS.md`, and this executor report.

### Rules and tests implemented (R-xx-nn / T-xx-nn)

- T-03-42: real D5 asserts 184 active members, combines active-status and dog-level filtering, opens D10, creates a booking block with a reason, opens screen 13 with the member's two real dogs, and persists the screen 28 phone/address change. D15 separately asserts 242 active dogs.
- T-05-22 / T-05-25: creates, edits and deactivates a ring, local level and FAQ against the real catalogs, then captures D16/D11 plus D8/D17/D3 E2 surfaces.
- T-02-13 / T-14-26: changes `bookings.lateCancelThresholdMinutes`, verifies non-empty history, verifies synchronous XLSX status/URL/visible-column query/response filename, and finds the parameter and booking-block events in global/member audit with translated known paths and masked-value preservation.
- R-14-12: the real export assertion requires `200`, `format=xlsx`, the ordered `columns` query and a response `.xlsx` filename; it does not inspect the file body.
- T-05-CP-07: edits/publishes RULES Markdown in D11 and verifies heading, bold, list and link semantics in both preview and screen 30.
- Polish (a)–(g): translated coverage wording and default-capacity label; Montserrat 400/500/600/700 self-hosted with no Google font resource; visibly styled limited Markdown; audit timestamps through `fmtDateTime`; known audit paths mapped to census labels with raw fallback. No additional Josep staging literals were received.
- Polish (h): T-03-38 explicitly mocks `/dashboard/counters`, awaits both list and shell counter state and passes ten consecutive isolated runs without timers.
- Gate regression: E1 + E2 real-core suite 10/10; MSW Playwright 47/47; workspace 26/26 tasks; i18n 1734 keys per locale, zero unused and zero missing error codes.

### Assumptions

- The E2 real-core suite uses the image-provided Cànic fixture solely as the fictional runtime seed; product code/locales remain white-label.
- The core rate limit is global to `/oauth2/token` in this isolated stack. Reusing the serial administrator session tests the same authenticated UI flows without weakening assertions; the final screen-30 check uses a distinct published seeded member.
- The root `pnpm e2e` serialization is intentional and matches `.github/workflows/ci.yml`; parallel projects repeatedly exceeded the constrained Docker runner while individual apps remained green.
- D3's `Aviat` view is the expected current route because weekly template UI belongs to E4-W01; this task captures it as the E2 integration baseline.
- CI-after-publication cannot exist before the publisher commits this working tree. All equivalent local/Docker gates are green; the organizer can confirm the resulting `main` run.

### Questions / catalog proposals

- Contract/handoff proposal for T-03-42: the published `MeDogs`/`MeDog` response does not expose a member booking-block reason, and the S08 member notification UI is not part of the current frontend. The suite verifies the real `201` booking-block write, D10 blocked state, audit record, and subsequent member screen 13 load, but does not invent a field or notification to render the reason. The API/S08 task should expose a catalogued member-facing reason/notification, or the gate assertion should explicitly remain at this cross-surface boundary.
- No new parameter, event, notification or error code was introduced.

## Organizer verification
Result: verified · 2026-09-19 · organizer (Claude)

Checked (organizer-less mode, lighter pass): report + `roadmap/evidence/E2-W07/` (`e2e:core` E2 scenario green against the published image with the demo seed — T-03-42, catalogs, D11 history, export, audit, club pages; performance D5 `/members` with two filters: median 51.5 ms, max below the 500 ms gate; polish (a)–(h) applied incl. self-hosted Montserrat without any Google Fonts resource and T-03-38 deterministic over 10 runs; complete `pnpm e2e` serialized in the Playwright image; 26 screenshots) · **CI green on `main` at `f0275d3`**. Gate E2 (front) ticked from this evidence.
