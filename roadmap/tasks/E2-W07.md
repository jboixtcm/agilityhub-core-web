---
id: E2-W07
title: E2 integration against the core (staging/local) and gate evidence (front)
stage: E2
repo: agilityhub-core-web
thread: B
order: 70
status: not_open
depends_on: [E2-W01, E2-W02, E2-W03, E2-W04, E2-W05, E2-W06, E2-W08, E2-W09, external:api:E2-T11]
branch: 
pr: 
updated: 2026-09-09
---

# E2-W07 · E2 integration against the core (staging/local) and gate evidence (front)

## Objective
Work package WP-03-G (front part): the census screens work against the real core with the demo seed.

## Spec references
S03 T-03-42 (E2E: D5 → D10 → booking block → 13 shows the reason → 28 saves) · gate E2 in `roadmap/ROADMAP.md`.

## Context to load
`roadmap/ROADMAP.md` (gate E2) · the E2 screens · this file.

## Steps
1. Playwright `e2e:core` suite (`CORE_URL`, seeded accounts): T-03-42 flow, catalog screens CRUD round trip, D11 parameter change with history, export download, audit trail visible.
2. Performance check: D5 with the 184-member seed and two filters answers < 500 ms (measure with the browser timing API, paste).
3. Screenshots of every E2 screen against real data under `roadmap/evidence/E2-W07/`.
4. **Polish list collected by the organizer during E2 verifications** (small literal/label fixes, no new behaviour; apply all): (a) D11 `coverage.thresholds` summary must use the D3 vocabulary «bé > {ok} % · ajustat {tight}–{ok} % · manca oferta {short}–{tight} % · cal ampliar < {short} %» (es/en equivalents) instead of «correcta / ajustada / curta»; (b) the catalog row `classes.defaultCapacity` label «Aforament per nivell» → «Aforament per defecte (nivell sense valor)» so it does not clash with the derived row «Aforament: per nivell …»; (c) any other literal that Josep flags in the staging review of E2 screens (list them in the report); (d) **brand font**: `packages/ui/src/tokens.css` names «Montserrat» but nothing loads it (the E1-W04 screenshots from the Playwright container fall back to a serif) — self-host Montserrat (OFL) as woff2 in `packages/ui` (`@font-face` 400/500/600/700, `font-display: swap`, latin subset) and import it from the three apps; **never** Google Fonts (GDPR: no third-party requests from the apps).

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `CORE_URL=… pnpm e2e:core` green; `pnpm turbo run lint typecheck test build` green.

## Definition of Done
Integration suite green; gate items covered.

## Evidence to return
Outputs and screenshots.

---

## Executor report
_(not started)_

### Branch / commits
### Evidence (full command outputs)
### Files changed
### Rules and tests implemented (R-xx-nn / T-xx-nn)
### Assumptions
### Questions / catalog proposals

## Organizer verification
_(pending)_
