---
id: E2-W07
title: E2 integration against the core (staging/local) and gate evidence (front)
stage: E2
repo: agilityhub-core-web
thread: B
order: 70
status: blocked
depends_on: [E2-W01, E2-W02, E2-W03, E2-W04, E2-W05, E2-W06, E2-W08, E2-W09, external:api:E2-T11]
branch: 
pr: 
updated: 2026-09-10
---

# E2-W07 · E2 integration against the core (staging/local) and gate evidence (front)

## Objective
Work package WP-03-G (front part): the E2 screens (census, records, catalogs, parameters, club pages, audit, exports) work against the **real core** with the fictional demo census — gate E2 (front) evidence, plus the polish list collected by the organizer during the E2 verifications.

## Spec references
S03 T-03-42 (E2E: D5 → D10 → booking block → 13 shows the reason → 28 saves) · S02 (D11 parameter change with history) · S05 (catalog CRUD) · S14 R-14-12 (export download) and the audit trail · gate E2 (front) in `roadmap/ROADMAP.md`.

## Context to load
`roadmap/ROADMAP.md` (gate E2) · E1-W04 (`scripts/e2e-core.sh`, `scripts/core-stack/docker-compose.yml`, `e2e/core/e1-core.spec.ts`, `e2e/core/playwright.config.ts`: the published-core stack with Mongo, seed, mailbox and the Playwright container) · the E2 screens (E2-W01…W06, W08, W09) · `packages/ui/src/tokens.css`, `packages/ui/src/limited-markdown.tsx` (polish d, e) · this file.

## Steps
0. **Stack**: reuse the E1-W04 stack (`ghcr.io/jboixtcm/agilityhub-core-api:main`, pulled by the session wrapper; `main` now contains the whole E2 api + E3-T01/T02/T06). Add the demo census to the `seed` service after the two `club:apply` runs: `java -jar /app/app.jar --core.command=seed:demo --club=canic --seed=42` (the image ships `/app/seeds/demo-canic.yaml`; the command reads `seeds/demo-<slug>.yaml` relative to `/app`, so keep the working directory) — it needs the Cànic plans and levels, so apply the image's own `/app/seeds/club-canic.yaml` (fictional accounts, `SEED_PASSWORD` = `E1_CORE_PASSWORD`) instead of the E1 copy if that copy lacks them; expected totals printed by the command: 184 active members / 242 dogs. Generalise `scripts/e2e-core.sh` (evidence directory and project name per task via variables; keep the E1 defaults) rather than copying it.
1. **`e2e/core/e2-core.spec.ts`** (serial, real core, seeded accounts): (a) T-03-42 — D5 list with the 184-member seed, filter by level + status, open a D10 record, «Bloqueja les reserves» with a reason, log in as that member in the clubs app: screen 13 shows the reason, screen 28 saves a phone change; (b) catalogs round trip — create/edit/deactivate a level, a ring and a FAQ entry in D16/D3/D11 and see them in the lists; (c) D11 — change `bookings.lateCancelThresholdMinutes` and see the history/`lastChange` row; (d) export — «Excel» on D5 with visible columns → `200` download with the response filename (assert the request URL and the status; the file itself is not inspected); (e) audit — the parameter change and the booking block appear in `/auditoria` and in the member's audit tab with masked values; (f) club pages — edit the RULES page in D11 and see it on screen 30 «Info».
2. **Performance**: D5 with the 184-member seed and two filters answers < 500 ms — measure the `/members` request with `performance.getEntriesByType("resource")` (duration) over five loads and paste the numbers (median and max); if it exceeds 500 ms, report it (INC candidate) — do not tune the api here.
3. **Screenshots** of every E2 screen against real data (D3, D5, D8, D10, D11 with history, D15, D16, D17, 13, 28, 30, /auditoria) at 1280 / 375 under `roadmap/evidence/E2-W07/`; compare with `docs/pantalles/` and list any deviation you notice (without fixing it, unless it is in the polish list below).
4. **Polish list collected by the organizer during E2 verifications** (small literal/label fixes, no new behaviour; apply all): (a) D11 `coverage.thresholds` summary must use the D3 vocabulary «bé > {ok} % · ajustat {tight}–{ok} % · manca oferta {short}–{tight} % · cal ampliar < {short} %» (es/en equivalents) instead of «correcta / ajustada / curta»; (b) the catalog row `classes.defaultCapacity` label «Aforament per nivell» → «Aforament per defecte (nivell sense valor)» so it does not clash with the derived row «Aforament: per nivell …»; (c) any other literal that Josep flags in the staging review of E2 screens (list them in the report); (d) **brand font**: `packages/ui/src/tokens.css` names «Montserrat» but nothing loads it (the E1-W04 screenshots from the Playwright container fall back to a serif) — self-host Montserrat (OFL) as woff2 in `packages/ui` — simplest: the `@fontsource/montserrat` npm package (OFL, woff2 per weight; import `400/500/600/700` latin from `packages/ui` so the three apps bundle it, `font-display: swap`) — and check the fallback stack in `tokens.css`; **never** Google Fonts (GDPR: no third-party requests from the apps). (e) **limited-Markdown content styles**: headings, bold, lists and links rendered by `packages/ui/src/limited-markdown.tsx` must be visibly styled in the D11 editor preview and in screen 30 «Info» (the E2-W09 screenshots show «## Convivència» as plain text). (f) audit list dates through the shared `fmtDateTime` (`dd/MM/yyyy HH:mm`), not the browser default («31/7/26»); (g) audit change drawer: known paths → field labels (`admin-census` keys), raw path as fallback.
5. `ROADMAP.md` is organizer-owned: put the gate E2 (front) evidence (what ran, counts, timings) in your report; the organizer ticks the checklist.

## Fixed conventions
- Same Docker wrapper as E1-W04 (`pnpm e2e:core`); the stack is self-cleaning; no secrets printed (`E1_CORE_PASSWORD` generated at runtime); fictional data only (`@example.test`).
- The MSW/unit suites stay untouched except where a polish item changes a literal (update the affected snapshots/tests, ca/es/en together; keys, never literals in code).
- No new parameter, event, notification or error code; no api changes (report anything the core would need as a proposal).

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `CORE_URL=http://core:8080 pnpm e2e:core` green (E1 + E2 scenarios; paste the tails and keep the full logs under `roadmap/evidence/E2-W07/`); the complete `pnpm e2e` (three MSW projects) in the Playwright image after the polish changes; `pnpm turbo run lint typecheck test build` + `pnpm i18n:check` green; CI green after publication.

## Definition of Done
Integration suite green against the published core with the demo census; performance numbers pasted; polish list (a)–(g) applied with screenshots; gate E2 (front) evidence in the report.

## Evidence to return
`e2e:core` and `e2e` tails, the performance table, the screenshot list, the polish diff summary.

---

## Executor report

### Branch / commits

- Working tree only; no branch, commit, or other git-writing command was run, per the sandbox protocol.

### Evidence (full command outputs)

- Blocked before implementation. `docker image inspect ghcr.io/jboixtcm/agilityhub-core-api:main --format '{{.Architecture}} {{index .Config.Labels "org.opencontainers.image.revision"}}'` exited `0`: `arm64 476761a5c3d88c7040c01cdaa4a91ec88d7751f6`.
- `docker pull ghcr.io/jboixtcm/agilityhub-core-api:main` exited `0` and reported `Status: Image is up to date` with digest `sha256:ce3b6eb72381bbc91659ed1c1a3bc818226e0dd7c6cb2eb7001a68d600859b8f`.
- `docker run --rm --entrypoint /bin/sh ghcr.io/jboixtcm/agilityhub-core-api:main -c 'find /app -maxdepth 3 -type f -print | sort'` exited `0`; `/app/seeds/` contains `club-canic.yaml`, `club-minim.yaml`, and page Markdown files, but not the task-required `/app/seeds/demo-canic.yaml`.
- `docker run --rm --entrypoint /bin/sh ghcr.io/jboixtcm/agilityhub-core-api:main -c 'sed -n "1,260p" /app/seeds/club-canic.yaml; sed -n "1,360p" /app/seeds/demo-canic.yaml'` exited `2` with `sed: can't read /app/seeds/demo-canic.yaml: No such file or directory`.

### Files changed

- `roadmap/tasks/E2-W07.md`; `roadmap/MESSAGES.md`; generated `roadmap/STATUS.md` — blocker report and task state only.

### Rules and tests implemented (R-xx-nn / T-xx-nn)

- None; the mandatory real-core demo census cannot be seeded from the published image.

### Assumptions

- Once the corrected image is published, use its `/app/seeds/demo-canic.yaml` unchanged and require the prescribed `184 active members / 242 dogs` totals before implementing or running T-03-42.

### Questions / catalog proposals

- See the `@organizer` blocker in `roadmap/MESSAGES.md`. No catalog proposal.

## Organizer verification
_(pending)_
