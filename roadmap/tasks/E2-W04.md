---
id: E2-W04
title: D16 rings · D17 team · D11 cards (levels, FAQ) · D8 plans and prices
stage: E2
repo: agilityhub-core-web
thread: B
order: 40
status: ready
depends_on: [E2-W01, external:api:E2-T01 (contract)]
branch: 
pr: 
updated: 2026-09-06
---

# E2-W04 · D16 rings · D17 team · D11 cards (levels, FAQ) · D8 plans and prices

## Objective
Work packages WP-05-D and WP-05-E: the catalog maintenance screens.

## Spec references
S05 §2 (D16, D17, D11 cards «Nivells» and «FAQ», D8), §4 (ordering, deactivation warnings, `usage`), tests **T-05-22, 23, 24, 25** · `docs/pantalles/escriptori/D16-*.html|png`, `D17-*.html|png`, `D8-*.html|png`, `D11-*.html|png` (cards).

## Context to load
S05 §2, §11 · the four mockups · this file.

## Steps
1. `/pistes` (D16: list with order drag, capacity per slot, active switch, geometry link placeholder for E9), `/equip` (D17: instructors with short name and color, administrators with «since», add from member search, `LAST_ADMIN` error), `/parametres#nivells` (card pattern of D16: level rows with color, `grantsFreeTraining`, `agilityhubLevel`, order), `/parametres#faq` (categories, entries per locale, order).
2. `/modalitats` (D8): plan cards with modal (type, dogs included, sessions/validity for packs, texts per locale), prices section with validity dates and «current/scheduled/superseded» chips, preview of the signup text.
3. Module gating: packs/single-class UI hidden when the modules are off; prices hidden without `BILLING`.

## Verification (run all; paste full output in the report)
- `pnpm turbo run lint typecheck test build` + `pnpm i18n:check`; Playwright T-05-22…25; screenshots at 1280 px next to the PNGs under `roadmap/evidence/E2-W04/`.

## Definition of Done
Four screens faithful to the mockups with module gating and tests.

## Evidence to return
Outputs and screenshots.

---

## Executor report
_(not started)_

### Branch / commits
### Evidence (full command outputs)
### Files changed
### Rules and tests implemented (R-xx-nn / T-xx-nn)
### Assumptions
### Questions / catalog proposals

## Organizer verification
_(pending)_
