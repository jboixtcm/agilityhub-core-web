---
id: E7-W03
title: E7 integration (front): adopt the api E7 snapshot, run T-11-39 end to end against the published core with the E7 demo seed (cancel a class at D4c → N-08a in the feed with «i per SMS» and in the mailbox → [CANVIA DE CLASSE] opens 04 with the dog; the same in es), prove the channel × preference matrix and the edited template from the UI, send a mass announcement to ten fictional members with its log, register a push subscription, screenshots with real data, gate E7 (front) evidence
stage: E7
repo: agilityhub-core-web
thread: B
order: 30
status: ready
depends_on: [E7-W01, E7-W02, E6-W04, external:api:E7-T04]
branch:
pr:
updated: 2026-09-19
---

# E7-W03 · E7 integration (front) — S11 WP-11-F, web half

## Objective
Work package S11 WP-11-F (front half): the E7 screens (11, 12, 30, D9, D10 and the notification log) run in the browser against the **real published core** with the E7 demo seed of api E7-T04 — members with different preference profiles, ten fictional active members for the mass announcement, a `CUSTOM` `CLUB_NEWS` template and the `ca`/`es`/`en` seed of every catalog template. A class cancelled from D4c really produces N-08a for each registrant, in each recipient's language, with an app card, an e-mail in the private mailbox and an SMS delivery through the fake or real sender; a template edited in D9 changes the **next** notice and not the history; the preference matrix is visible from the UI (an e-mail that does not arrive because the member turned that category off); «Enviar comunicat» reaches ten members and lands in the log; and the push subscription is registered. The S11 contract is adopted from the real snapshot (no E7 entry left in `pending.json`) and the gate E7 (front) evidence is produced for the organizer.

## Spec references
S11 §11 **T-11-39** («E2E Playwright (seed demo): anul·lar una classe a D4c → Laura veu N-08a a 11 amb “i per SMS” (doble de Twilio) → [CANVIA DE CLASSE] obre 04 amb la Duna; i el mateix en `es`») re-run against real data, plus the real-data half of **T-11-34**, **T-11-35**, **T-11-37**, **T-11-38** · **R-11-01** (the recipient's language: a member with `Account.locale = es` gets the `es` text, a club with only `ca` falls back) · **R-11-02** (one notification per dog and per audience; the admin and the instructor of the cancelled class also receive theirs) · **R-11-03** (the truth table made visible: `emailByCategory` off → no e-mail in the mailbox and `SKIPPED_BY_PREFERENCE` in the log; SMS is never removed by a preference; `SMS`/`PUSH` off → `SKIPPED_MODULE_OFF`) · **R-11-04** (preferences edited from 12 and from D10 are the same block) · **R-11-09** (reprocessing an event creates no duplicate) · **R-11-10** (the feed, «i per SMS», `read-all`, `unreadCount` agreeing with `GET /me/home`, the admin log) · **R-11-11** (`CHANGE_CLASS` opens 04 with `subject.dogId`; `CLAIM_SEAT.enabled` computed on read) · **R-11-12** (an edited template changes the next notice only — the history is frozen; `reset` restores the seed) · **R-11-13** (the mass announcement: `dryRun` count, one notification per member, each member's own preferences, audited) · **R-11-15** (changing the language re-renders later notifications only) · **R-11-16** / S15 **R-15-14** (the P4 `reminders` job and N-13 with APP + PUSH and EMAIL only when `OPERATIONAL` is on) · S11 §8 (the seeded `ca` bodies the screens must show) · api **`E7-T04`** (the seed's preference profiles, the ten fictional members, the `CUSTOM` template, `bin/e7-smoke`, the jobs and clock endpoints) and `E7-T01`…`E7-T03` (the published contract) · the backlog **gate E7**: «proves de matriu verdes; SMS i push reals rebuts; comunicat massiu a 10 abonats ficticis amb el log; plantilla editada a D9 es reflecteix al següent avís en l'idioma del destinatari» · `docs/DECISIONS_PENDENTS.md` **A12**, **A21d**, **B18**, **B21**, part C S11 · `messaging.reminderOptionsMinutes = [60,120,240,360,720,1440]`, `messaging.sms.monthlyCap = 1000`, `bookings.lateCancelThresholdMinutes = 240`, `waitlist.notifyThresholdMinutes = 30` (`CATALEG_PARAMETRES.md`) · `AGENTS.md` rule 4 (contract files), `roadmap/README.md` (evidence rule).

## Context to load
`roadmap/tasks/E2-W07.md`, `E4-W05.md`, `E5-W04.md` and **`E6-W04.md`** (the real-core stack and its wrappers: `scripts/e2e-core.sh` with a per-task evidence directory and project name, `scripts/core-stack/docker-compose.yml` — note the **private mailbox volume**, `MAIL_LOCAL_DIRECTORY: /app/mailbox` on the core and `E1_MAILBOX_DIRECTORY: /mailbox` read-only in the Playwright container —, `scripts/core-stack/club-canic.yaml`, `e2e/core/playwright.config.ts`, the helpers `login`, `mobileContext`/`desktopContext`, `E1_CORE_PASSWORD`, the `POST /test/clock` detection and the `@organizer` blocker precedent when the image or the snapshot is missing) · `e2e/core/{e1-core,e2-core,e3-signup}.spec.ts` (and the E4/E5/E6 specs those stages added) — every earlier core spec must still pass in the same run · the E7 screen tasks **`E7-W01`** and **`E7-W02`** (routes, literals, data attributes, test ids, the MSW scenarios you must **not** use here) · `roadmap/tasks/E4-W02.md` (D4c: the cancellation modal with the admin text that produces `ClassCancelledByClub`) · `roadmap/tasks/E5-W03.md` (the D11 card «Processos automàtics» with `[Simula]` and `[Executa ara]` — the only UI way to run the `reminders` job) · `roadmap/tasks/E5-W01.md` (screen 03's bell and screen 04's `?dogId=` chip) · api `E7-T04` (read-only in the api clone or pasted by the organizer into `MESSAGES.md`): the seeded members and their preference profiles, the ten announcement recipients, the `CUSTOM` template's name, and what `bin/e7-smoke` already proves api-side · the staged `packages/api-client/openapi/openapi.next.json`, `pending.json`, `scripts/generate.mjs`, `src/mocks/fixtures.contract.test.ts` · `docs/pantalles/mobil/{11,12,30}-*.png`, `docs/pantalles/escriptori/{D9,D10}-*.png` · this file.

## Steps
0. **Contract adoption**: swap the staged `openapi.next.json` (api E7-T04 snapshot = the S11 contract as implemented) into `openapi.json` byte for byte (`cmp`; record both `sha256`), `pnpm api:generate`, **prune every S11 path and schema from `pending.json`** (whatever E7-W01/E7-W02 declared mocks-first; list what legitimately stays), align the MSW fixtures and consumers with the published names (Ajv suite green), `pnpm turbo run lint typecheck test build`. Preflight, and **block with an `@organizer` entry in `roadmap/MESSAGES.md` (E2-W07 precedent) if any of these fails**: the staged file exists; `docker image inspect ghcr.io/jboixtcm/agilityhub-core-api:main` resolves and its revision is recorded; the image lists `/app/seeds/demo-canic.yaml`; the image answers `GET /api/v1/message-templates` and `GET /api/v1/me/notifications` with something other than 501.
1. **Stack** (`scripts/core-stack/docker-compose.yml`, generalised by E2-W07): the `seed` service runs `club:apply` for the Cànic club file and then `seed:demo --club=canic --seed=42 --week-start=<Monday of the club-local current week>` (computed in `scripts/e2e-core.sh` with `TZ=Europe/Madrid date`, exported as `E7_WEEK_START`); `SEED_PASSWORD` = `E1_CORE_PASSWORD` (generated at runtime, never printed); evidence directory `roadmap/evidence/E7-W03`, project name `agilityhub-e7-w03`; the club file keeps `SMS` and `PUSH` **on**. Read the seeded accounts and their preference profiles from `scripts/core-stack/club-canic.yaml` and the demo-seed README — never invent one. `pnpm e2e:core` runs all of `e2e/core/*.spec.ts`.
2. **Clock control**: `POST /test/clock` (E5-T01/E6-T04) only exists under the `test`/`local` profiles. Detect it once at the start of the spec. If the published image does not expose it, run every clock-independent assertion, mark the reminder block `test.skip` with a recorded reason and cite the api evidence (`bin/e7-smoke`'s reminder block from the api clone's `roadmap/evidence/E7-T04/` or pasted in `MESSAGES.md`), saying exactly which gate lines were proven that way. Never fake time in the browser for a core run.
3. **`e2e/core/e7-core.spec.ts`** (serial, real core; select rows by data attributes such as `data-notification-id`, `data-code`, `data-template-id`, `data-delivery-status`, `data-class-id`; assert API statuses with Playwright response hooks; read the mailbox from `E1_MAILBOX_DIRECTORY` with the helper E1-W04 wrote — never through a public mail service):
   (a) **T-11-39, `ca`**: as ADMIN at 1280 open `/calendari`, pick a future seeded class with registrants, `[Anul·la la classe]` → the D4c modal, type the fictional admin text «Classe anul·lada per la pluja — {runId}» and confirm → 2xx. As the seeded **member** of that class at 375, `/notificacions` shows the new card first: code `N-08a`, the `x` icon in the `ERROR` tone with its left border, the body carrying the class date, time, description, ring, dog **and the quoted admin text**, and the meta line ending in « · i per SMS»; the mailbox holds one message to that member whose subject is the card's title; `[CANVIA DE CLASSE]` navigates to `/reservar` with her dog preselected (assert the chip, not just the query). The bell on `/inici` was animated before the visit and is quiet after it, and `unreadCount` from `GET /me/home` is 0.
   (b) **T-11-39, `es`**: a second seeded member of the same class whose `Account.locale` is `es` sees the **same** notification in Spanish (title and body), while the first member's card stays in `ca`; the mailbox message for her is the Spanish one. (If the seed has no `es` member, set her language from screen 12 **before** the cancellation and say so — R-11-15 renders the notification at creation time.)
   (c) **Edited template reflected in the next notice** (gate line): as ADMIN at `/comunicats` open N-08a, edit the `ca` **and** the `es` body adding the marker « ({runId})», `[DESA]`; then cancel a second seeded class with the same two members → the two new cards carry the marker, each in its own language, and the cards of step (a)/(b) are **unchanged** (frozen history, R-11-12). Finish with `[Restaura el text per defecte]` and assert the template is no longer marked customized.
   (d) **Matrix visible from the UI** (gate line «proves de matriu», front half): as the member of step (a), screen 12 → turn «Canvis en reserves que has fet (fets pel club)» **off** under «Correu»; cancel a third class from D4c → her feed shows the card and « · i per SMS» (SMS is never removed by a preference, R-11-04) while the mailbox receives **no** new message for her; the admin log `/notificacions?filter=code:eq:N-08a` shows her row with the EMAIL delivery `SKIPPED_BY_PREFERENCE` and the APP one `DELIVERED`. Then, from **D10** on her record, turn it back on and assert screen 12 reflects it (same block, two screens). The exhaustive 53-row matrix is the api's parametrised test — cite E7-T04's counts, never re-run it here.
   (e) **Mass announcement** (gate line): as ADMIN open `/abonats`, select the ten seeded fictional members, `[Enviar comunicat]` → the dialog with the seeded `CUSTOM` `CLUB_NEWS` template, the `dryRun` line «S'enviarà a 10 abonats», tick the confirmation, `[ENVIA]` → `202`; `/notificacions?filter=code:eq:N-24` (or the `CUSTOM` template's filter) lists ten rows sharing one batch; one of those members sees the announcement in her feed with the club's text; a member who is **not** in the selection does not. Repeat once with the **filter** shape instead of the selection and assert the `dryRun` count matches what D5 shows. Announcement idempotency is proven api-side — cite it.
   (f) **Push subscription** (gate line, partial): in a Chromium context with `grantPermissions(["notifications"])` and `addInitScript` stubbing `PushManager.subscribe` to return a fake endpoint (`https://push.example.test/{runId}`), turn the push toggle on at screen 12 → `POST /push-subscriptions` → `201`, and the api reports it (assert through a subsequent `GET`/the admin log, or cite `bin/e7-smoke`). A **real** push delivery needs a real push service and a device (installed iOS PWA / Android): assert the permission is requested **only** on interaction, never at boot, and leave the real check to staging with a `roadmap/MESSAGES.md` entry addressed to `@jordi`. Do not mark the task `blocked` for it.
   (g) **Real SMS** (gate line, conditional): if `TWILIO_ACCOUNT_SID`/`TWILIO_AUTH_TOKEN` and `SMOKE_SMS_TO` are present in the environment, the SMS delivery of step (a) is `SENT` with a provider reference (truncate it in evidence); when they are absent, assert the fake/log sender produced `SENT` or `QUEUED`, print «real SMS skipped: no Twilio credentials», keep exit 0 and record it as a staging item for `@jordi`. Never print a phone number or a message body.
   (h) **Reminder N-13** (only when step 2 found the clock): at screen 12 set «Recordatori de classe» to «2 h abans» for a member with a future booking, advance the clock to `startsAt − 120 min`, run `reminders` from the D11 card — `[Simula]` lists her booking and changes nothing, `[Executa ara]` creates exactly one N-13 card in her feed with the clock icon and **no** e-mail in the mailbox (`OPERATIONAL` is off by default, R-11-04); a second `[Executa ara]` creates nothing.
   (i) **Screen 30 and the `es` pass**: `/info` shows the FAQ groups in the R-05-22 order with the exclusive accordion and the «Normes» tab of E2-W09; repeat (a) and (d)'s feed assertions with `localStorage` `agilityhub.locale = "es"`, asserting the Spanish literals of the feed, the action button and the preferences rows.
   (j) **Cleanup for idempotence**: the edited template is reset (step c), the preference of step (d) is restored, the push subscription of step (f) is deleted, every free-text field carries the `runId` suffix, and a second `pnpm e2e:core` passes unchanged.
4. **Screenshots with real data** under `roadmap/evidence/E7-W03/`: 375 — `11-notificacions-core-375.png` (the N-08a card with «i per SMS» and its button), `11-notificacions-es-core-375.png`, `12-avisos-core-375.png`, `30-info-core-375.png`; 1280 — `D9-comunicats-core-1280.png`, `D9-enviar-comunicat-core-1280.png` (the dialog with «S'enviarà a 10 abonats»), `log-notificacions-core-1280.png` (the rows with their delivery statuses), `D10-preferencies-core-1280.png`. Compare each with `docs/pantalles/` and list every deviation (fix only one-line literal mistakes; list the rest).
5. **Contract discrepancies**: any difference between what the published core returns and the generated types (a missing required field, a `DeliveryStatus` value the enum lacks — the three `SKIPPED_*` proposals of E7-T01 are the known candidates —, a status that contradicts `CATALEG_ERRORS.md` such as the S11 codes moved from 400/409 to **422** under rule 0, an ordering the front had to redo, a `pushPublicKey` missing from `GET /branding`) is reported as an `@organizer` proposal in the report and in `MESSAGES.md` (E1-W04 precedent: INC-06); never patch `openapi.json` by hand.
6. **Gate E7 (front) evidence** in the report, one line per gate item paired with the spec step, screenshot or api citation that proves it: «proves de matriu verdes» (step d for the UI half + the parametrised-test counts cited from api E7-T04); «SMS i push reals rebuts» (step f and g: what the fake path proved, what waits for Twilio credentials, the VAPID keys and a device at staging, with the `@jordi` message); «comunicat massiu a 10 abonats ficticis amb el log» (step e + the log screenshot); «plantilla editada a D9 es reflecteix al següent avís en l'idioma del destinatari» (step c + the two screenshots). `ROADMAP.md` is organizer-owned — do not edit it.

## Fixed conventions
- Same Docker wrapper as E1-W04/E2-W07/E4-W05/E5-W04/E6-W04 (`pnpm e2e:core` with `CORE_URL=http://core:8080`); the stack is self-cleaning; no secrets printed (`E1_CORE_PASSWORD` generated at runtime, tokens, provider references and ids truncated); fictional data only (seed accounts `@example.test`, fake phones); `docker login` is never run.
- The MSW/unit suites stay untouched except for the contract alignment of step 0 (keys never literals; ca/es/en together; vocabulary lint green — never «parella/parelles»).
- No new parameter, event, notification or error code and no api change — proposals only. Never reimplement what `bin/e7-smoke` already proves api-side (the 53-row matrix, the SMS monthly cap, the bounce path, the announcement idempotency, the outbox rows): cite it with the command and the line.
- Time moves only through `POST /test/clock` and jobs run only through D11's `[Simula]`/`[Executa ara]` (or a cited api trigger); never `waitForTimeout` to cross a business threshold.
- E-mails are read from the private mailbox volume only; no external mail service, no real address, and a message body is never pasted in full (subject + recipient + the marker are enough).
- Evidence rule: report = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E7-W03/NN-<name>.log`; task file ≤ 120 KB; complete `pnpm e2e` (three MSW projects) in the Playwright image after the contract alignment; never edit other tasks, `ROADMAP.md` or the Organizer verification section.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `docker image inspect ghcr.io/jboixtcm/agilityhub-core-api:main --format '{{.Architecture}} {{index .Config.Labels "org.opencontainers.image.revision"}}'` (record the revision); `pnpm api:generate` + `shasum -a 256` of `openapi.json` vs the staged file + `jq -r '.paths | keys[]' packages/api-client/openapi/pending.json` (no S11 path left); the Ajv suite; `CORE_URL=http://core:8080 pnpm e2e:core` green **twice** (E1 + E2 + E3 + E4 + E5 + E6 + E7 specs; paste the tails, keep the full logs); `pnpm turbo run lint typecheck test build` + `pnpm i18n:check`; complete `pnpm e2e` in the Playwright image; the eight screenshots.

## Definition of Done
T-11-39 passes end to end against the published core with the E7 demo seed, twice and idempotently, in `ca` and in `es`; the S11 contract is the real snapshot with no pending entry; a cancelled class produces the app card, the mailbox message and the SMS delivery for each registrant in their own language; an edited template changes the next notice and not the history; the preference matrix is demonstrated from screen 12 and from D10 with the log showing `SKIPPED_BY_PREFERENCE`; a mass announcement reaches ten fictional members and is listed in the log; a push subscription is registered in context; screenshots with real data are committed and compared; gate E7 (front) evidence is in the report with the real-SMS and real-push checks clearly assigned to staging, and contract discrepancies, if any, are filed as proposals.

## Evidence to return
Image revision, both snapshot hashes and the `pending.json` path list, both `e2e:core` tails, the `e2e` tail, the eight screenshots with deviations, the gate E7 lines each paired with the output (or api citation) that proves it, whether `POST /test/clock` and the Twilio/VAPID credentials were available, the mailbox assertions (recipient + subject + marker only), and the discrepancy list as written in `MESSAGES.md`.

---

## Executor report
_(not started)_

### Branch / commits

### Evidence (full command outputs)

### Files changed

### Rules and tests implemented (R-xx-nn / T-xx-nn)

### Assumptions

### Questions / catalog proposals

## Organizer verification
_(pending)_
