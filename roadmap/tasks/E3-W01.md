---
id: E3-W01
title: Public signup stepper 16–19 (/apuntat-hi/*) and «add a dog» (/gossos/nou*) — mocks-first on the S04 contract
stage: E3
repo: agilityhub-core-web
thread: B
order: 10
status: in_progress
depends_on: [E2-W08, E1-W07, external:api:E3-T01 (contract snapshot or pending.json)]
branch:
pr:
updated: 2026-09-09
---

# E3-W01 · Public signup stepper 16–19 (/apuntat-hi/*) and «add a dog» (/gossos/nou*) — mocks-first on the S04 contract

## Objective
Work package WP-04-4: the public form of the club (screens 16–19) and the «add a dog» variant of screen 13 work against MSW with the published S04 contract, pixel-faithful to the V8 mockups.

## Spec references
S04 §2 (rows 16, 17, 18, 19, `/apuntat-hi/enviada`, `13 → 17/19`), §4 R-04-01…R-04-04 (field rules), R-04-08 (uploader), R-04-09/10 (plans, payment methods), R-04-11/12 (family step), R-04-14/15 (initial payment card and the two first-month options), R-04-17 (consents), R-04-20 (honeypot `website`, rate-limit `429` handling), R-04-26 (Checkout redirect), §9 modules matrix (`BILLING`/`PACKS`/`FAMILY_GROUP`/Stripe off) · tests **T-04-29, T-04-30, T-04-31, T-04-32** · mockups `docs/pantalles/mobil/16-alta-pas-1-la-persona.*`, `17-alta-pas-2-el-gos-i-la-modalitat.*`, `18-alta-segon-gos-i-grup-familiar.*`, `19-alta-pagament-consentiments-i-enviament.*`, `13-els-meus-gossos.*` · `docs/legal/AUTORITZACIO_IMATGE.md` (the image-consent «què vol dir?» text comes from the API `legal.imageConsentText`, never from the front) · `docs/specs/00-transversal/CONVENCIONS_I18N.md`.

## Context to load
S04 §2, §4, §6, §9, §11 (front part) · `packages/api-client` conventions (`openapi.json` + `pending.json` mocks-first, Ajv fixture test) · `packages/ui` form primitives, `Uploader` if one exists from E2 (dog documents of D10/13), `BrandingProvider` (public route uses `GET /branding` by host) · `apps/clubs` routing and the 13 «Els meus gossos» screen (E2-W03) · this file.

## Steps
0. **Contract**: if the organizer staged `openapi.next.json` with the S04 routes (api E3-T01), swap it in; otherwise add the S04 §6 routes and schemas to `pending.json` from the spec (mocks-first) with Ajv-checked fixtures: `GET /signup` (config with 4 steps, 3 plans of the Cànic seed, `SEPA_DD`/`MANUAL` methods, texts, legal), identity checks (`NEW`, `VERIFICATION_SENT` with masked email, `SIGNUP_ALREADY_PENDING`), towns (`08349` → one town; a code with three), upload URL, family lookup (`FOUND` «Marta R.» / `NOT_FOUND`), `POST /signup` (`201` with `upfront` lines; `400` with `fieldErrors`; `409 SIGNUP_CLOSED`; `422 CONSENT_VERSION_OUTDATED`; `429` with `Retry-After`), `POST /me/dogs/signup`, `POST /checkout-sessions`.
1. **Routes and state** (`apps/clubs`): `/apuntat-hi` (16), `/apuntat-hi/gos` (17), `/apuntat-hi/familia` (18, only with `FAMILY_GROUP`), `/apuntat-hi/pagament` (19), `/apuntat-hi/enviada`; draft in `sessionStorage` key `signup.draft.v1` (discarded after 24 h or on submit, survives reload); header «Apunta-t'hi» + language selector (anonymous → `localStorage`, refetch `GET /signup` with `Accept-Language`, draft kept); progress «Pas n de N» (N = `steps.length`); `signup.enabled=false` or club not `ACTIVE` → only `closedText`.
2. **Screen 16** fields per `countryProfile` (ES: «DNI / NIE» + «Passaport — si no tens DNI/NIE» enabled only when DNI/NIE is empty; GENERIC: type + free value), names, birth date, gender chips (Masculí · Femení · Altres / No binari), emails (2), phones with prefix + «Descripció», address with «CP» → towns (0 → free required, 1 → prefilled, >1 → list), the fixed grey note «Ja ets soci i vols afegir un altre gos? …»; [CONTINUA] → live validation (400 `fieldErrors` mapped to fields) + `POST /signup/identity-checks`: `NEW` → 17 · `VERIFICATION_SENT` → state «Revisa el correu» with `maskedEmail` and «Torna-m'ho a enviar» (rate-limited) · `SIGNUP_ALREADY_PENDING`/`CONTACT_CLUB` → inline message.
3. **Screen 17**: «Pas 2 de 4 · El teu gos (n'hi podràs afegir més després)», dog fields, chip, `Uploader` («Cartilla de vacunes»: `POST /signup/upload-urls` → PUT, proposed names `cartilla_{Nom}_{n}.{ext}`, «＋ Afegir un altre full», ≤ 10 files), the note about the card only when `requireDogDocumentAtSignup=false` (from config), texts `freeTrainingConditions`/`therapyIntro` when non-empty, plan cards from `config.plans` (price, «Entrada n €», conditions, «Ofertes si es porta més d'un gos per família» only with `FAMILY_GROUP`; no amounts without `BILLING`); [CONTINUA] → 18 or 19.
4. **Screen 18** (`FAMILY_GROUP` only, never in add-dog mode): intro text, «Quota familiar» card with the entry fee per new dog, holder name + one of the holder's dogs; both empty → continue without claim; lookup → «Grup trobat: {holderDisplayName}. El gos nou quedarà vinculat al seu grup familiar.» or the inline error with «Deixa-ho pendent i continua ›» only when `allowFamilyGroupPending`; footer «La vostra tarifa s'ajustarà en el moment de la validació de l'alta.»; «‹» keeps the draft.
5. **Screen 19**: «Pas 4 de 4 · Pagament i consentiments»; payment block only with `BILLING`: `monthlyPaymentIntro`, method segment from `config.paymentMethods` («Domiciliació» → IBAN + «Titular del compte» prefilled + mandate text + `paymentDay`; «Targeta» → secure-payment text + `paymentDay`; «Efectiu» → `cashConditions`); «Pagament inicial» card (lines, the two first-month radio options with the dates from the API — the client never computes amounts, it re-asks `GET /signup`/dry data as the contract provides — «Total a pagar al club», MANUAL instructions or «El pagament es fa en enviar» with Stripe); consents («Accepto la política de privacitat» + «Pots consultar-la aquí» opens `privacyPolicyUrl` in a new tab; «Autoritzo l'ús de la meva imatge» + «què vol dir?» expands `imageConsentText`); [ENVIA LA SOL·LICITUD] disabled until privacy is accepted → `POST /signup` (or `/me/dogs/signup`) with `website` honeypot empty; `checkout.required` → `POST /checkout-sessions` + redirect; `CONSENT_VERSION_OUTDATED` → reload config and re-ask; `429` → message with the retry time.
6. **`/apuntat-hi/enviada`**: «Sol·licitud enviada» + the footer of 19 (+ «Rebràs la confirmació del pagament per correu» with checkout); clears the draft; `?cs=cancel` returns to 19 with the warning of S04 §2.
7. **Add-dog mode** (13 → `/gossos/nou`, `/gossos/nou/pagament`): authenticated `GET /signup` returns `member`; 17 with the current plan preselected; no 18; 19 shows the current payment method read-only («Domiciliació · ···· 2231»), the new dog's initial payment (entry + `ADDITIONAL_DOG_FEE` or the «1st of next month» option as the API returns) and consents only when `legalTextsVersion` changed; submits `POST /me/dogs/signup`.
8. i18n namespace `signup` in ca/es/en (every literal of the mockups; vocabulary lint: never «parell*»); Vitest T-04-29…32; Playwright of the four screens in Docker; screenshots at 375 px of 16, 17, 18, 19, «enviada» and the add-dog 19 under `roadmap/evidence/E3-W01/`, compared against the mockups.

## Fixed conventions
- White-label: club name/branding from `GET /branding`; no Cànic literal in code (fixtures only).
- Public routes never carry the access token; no cookies needed; the honeypot field is visually hidden and not `display:none` on the input itself (bots) — follow S04 R-04-20.
- No amounts computed client-side; the API is the source of every € shown.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `pnpm turbo run lint typecheck test build` + `pnpm i18n:check`; Vitest T-04-29…32; Playwright (Docker) for 16→19→enviada with MSW; screenshots 375 px.

## Definition of Done
A fictional person completes 16–19 against MSW in ca/es/en with all module variants (`BILLING`/`FAMILY_GROUP`/`PACKS`/Stripe off); the add-dog path works from 13.

## Evidence to return
Outputs, screenshots, list of `signup` keys.

---

## Executor report
_(not started)_

## Organizer verification
_(pending)_
