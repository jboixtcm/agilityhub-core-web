---
id: E3-W02
title: D2 «Validació d'una preinscripció» (/preinscripcions/:id) + D1 «Tauler de control» (/tauler) with KPIs, risk card, pending card, dogs-by-level chart and menu counters
stage: E3
repo: agilityhub-core-web
thread: B
order: 20
status: awaiting_verification
depends_on: [E2-W01, E2-W06, external:api:E3-T01 (contract), external:api:E2-T01 (dashboard contract)]
branch:
pr:
updated: 2026-09-10
---

# E3-W02 · D2 «Validació d'una preinscripció» (/preinscripcions/:id) + D1 «Tauler de control» (/tauler) with KPIs, risk card, pending card, dogs-by-level chart and menu counters

## Objective
Work packages WP-04-5 and WP-14-F: the administrator validates or rejects a pending signup from D2 and lands on a real D1 (the E1 placeholder «Aviat» disappears).

## Spec references
S04 §2 rows D2 and D1 (card), R-04-16 (amount effectively collected), R-04-18 (warnings with gender ICU), R-04-19 (edit in `PENDING`), R-04-23 (rejection modal), test **T-04-33** · S14 §2 row D1, R-14-01…R-14-08, §6 JSON, tests **T-14-25** (D1 with the §6 JSON), T-14-27 (i18n snapshots, vocabulary lint) · mockups `docs/pantalles/escriptori/D2-validacio-d-una-preinscripcio.*`, `D1-tauler-de-control.*` · `docs/DECISIONS_PENDENTS.md` A17b (`pendingSignupAgeWarnDays = 2`), A17c.

## Context to load
S04 §2, §6 (`GET /members/{id}/signup`, validation `dryRun`, rejection), S14 §2, §6 · the `Dashboard*` schemas of the api snapshot (E2-T01) and the S04 schemas (E3-T01 / `pending.json` of E3-W01) · `UniversalList` (E2-W01), the D10 record page (E2-W02) for the person/dog cards style, `LastChange` (E2-W06) · `packages/ui` (add `BarChart` if none exists) · the clubs-admin shell (menu counters) · this file.

## Steps
0. **Contract**: the organizer staged `packages/api-client/openapi/openapi.next.json` = api `83069c1` (E3-T01: the 11 S04 operations + 55 schemas + `MemberListItem.signupPending/pendingDogs/warnings/signup`). First step: swap it into `openapi.json` byte for byte, **prune every S04 operation from `pending.json`** (E3-W01 added them mocks-first), regenerate, align E3-W01's fixtures/consumers with the published names, run the Ajv suite; the dashboard contract is already published (`Dashboard`, `DashboardCounters`, `PendingSignups`…): MSW fixture = the exact S14 §6 JSON (mockup values 184/+3, 87 % 142/163, 56/38, 3 pending with 1 older than the warn days, 4 risk rows, 8 levels/242 dogs), plus the `null` variants (`trainingBookings = null`, `dogsByLevel = null`). **Snapshot refresh (10-09)**: api E3-T03 amends the S04 contract with optional add-dog fields — `AddDogSignupRequest.additionalDogOption` (`TODAY · ALTERNATIVE`, default `TODAY`), `SignupConfig.upfront.additionalDogOptions[]` (add-dog mode), `SignupUpfront.additionalDog {option, startDate, amountDue}`, `AddDogSignupResult.checkout {required, memberId}`; the organizer re-stages `openapi.next.json` when E3-T03 publishes. Adopt whichever staged snapshot is present when you start (the file header says which api commit); if E3-T03 publishes while you work, the organizer tells you in MESSAGES — do not wait for it.
1. **D2** `/preinscripcions/:memberId` (ADMIN): header «Preinscripció #{number|short id} — {nom} + {gossos}» + «pendent des de fa n dies» badge (warning style when `pendingDays ≥ warnDays`); Person card (name, id document, contact with `wa.me`, «Grup familiar» with «tarifa familiar en validar» badge, «Pagament» with masked IBAN); yellow image warning with gender ICU («…on surti ella/ell»); red «Compte no informat»; per dog «Gos (i de n)» card (data, chip, «Documents» signed links, «Notes als instructors», «Nivell inicial» select only with `levels.enabled`, «Data del proper rebut» with «obligatori» badge only with `BILLING` + `MONTHLY`); «Modalitat i tarifa» card (plan/price selector → `dryRun` recalculates) and «Pagament inicial (anticipat)» with «Import efectivament cobrat:» (read-only + «cobrat» badge when Stripe paid; «No s'ha cobrat res: queda pendent» confirmation for 0); buttons [EDITA LES DADES] (form with the 16/17/19 fields → `PATCH /members/{id}` and `PATCH /dogs/{id}` with `version`), [REBUTJA (amb motiu)] (modal, reason required 3–500), [VALIDA L'ALTA] (`POST /members/{id}/validation`; 422 shown on the field; `STALE_VERSION`/`INVALID_STATE` messages); after validation → D1 with a toast; «Readmissió» badge when `signup.readmission`; add-dog case: «(nou gos)» and only the pending dogs editable.
2. **D1** `/tauler` (ADMIN): one `GET /dashboard`; greeting «Bon dia! {Dilluns 10 d'agost}» (club-local date; time-of-day greeting per S14 §13 assumption); 4 KPIs (3 when `trainingBookings = null`) with the exact mockup literals («184 · Abonats d'alta · +3 aquest mes», «87% · Ocupació classes · aquesta setmana · 142/163 places» («—» when `percent = null`), «56 · Entrenaments reservats · setmana · 38 persones diferents», «3 · Preinscripcions pendents · 1 de fa més de {warnDays} dies»); card «Revisió de classes en risc — {7:30}, avui i {2} dies vista» with «{n} avisos» and one row per item («{descripció} · {avui|demà|dc} {9:30} · {pista}» · «{n} inscrits» · status chip; ICU «anul·lada · avisada Laura + Duna» / «en risc · avisats Pau + Blat» / «s'anul·larà {dc} a les {7:30}» / «en risc · el club decidirà»); card «Preinscripcions pendents» rows «{Marta R.} + {Kiwi} ({whippet})» · «{Abonat} · {domiciliació}» · red «Compte no informat» badge · [VALIDA] → D2 (all rows, no «Veure totes»); chart «Gossos per nivell — {242} actius» with legend «amb reserva (setm. en curs o anterior) · total actius», one column per level `{code}` «{total} · {n} res.» (`packages/ui` `BarChart`, accessible table fallback), hidden when `dogsByLevel = null`; states: skeletons of the 4 cards, empty («Cap classe en risc» / «Cap preinscripció pendent»), error toast by `code` + [Torna-ho a provar]; refetch on window focus.
3. **Menu counters** in the clubs-admin shell: `GET /dashboard/counters` on load and on every route change → «Preinscripcions {n}», «Inactivitats i baixes {n}», «Seguiment alumnes {n}» (hide the badge at 0).
4. i18n namespaces `admin-dashboard` and `admin-census` (D2) in ca/es/en; Vitest T-04-33, T-14-25 (JSON of §6, the four risk states, `null` blocks, focus refetch); Playwright in Docker (D1 → [VALIDA] → D2 → validate → back to D1 with «2 · Preinscripcions pendents»); screenshots at 1280 px of D1 and D2 (both states) under `roadmap/evidence/E3-W02/`, compared against the mockups.

## Fixed conventions
- Every literal from the mockups through i18n keys; vocabulary lint (`parell*`) green; dates/times formatted with the club time zone and locale helpers of `packages/i18n`.
- No client-side computation of KPIs or amounts; the API JSON is rendered as is.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `pnpm turbo run lint typecheck test build` + `pnpm i18n:check`; Vitest T-04-33, T-14-25; Playwright D1/D2 in Docker; screenshots 1280 px.

## Definition of Done
D1 and D2 are pixel-faithful to the mockups on MSW data, with all states; the shell shows real counters.

## Evidence to return
Outputs and screenshots.

---

## Executor report

### Files changed

- `.gitignore`, `CHANGELOG.md`: task evidence allowlist and Unreleased entries for the S04 contract adoption, D1, and D2.
- `packages/api-client/openapi/{openapi.json,pending.json}`, `src/generated/schema.d.ts`: byte-identical staged snapshot adoption, removal of the consumed staging file, pruning of every published S04 path/schema, and regenerated merged types. The unrelated notification-preferences pending contract remains.
- `packages/api-client/src/mocks/{handlers.ts,scenarios.ts,fixtures.contract.test.ts,fixtures/dashboard.json,fixtures/member-signup-review.json,fixtures/signup-config-canic.json,fixtures/signup.ts}`, `src/index.test.ts`: exact dashboard/signup aggregates, nullable dashboard cases, Stripe/manual/rejection/validation behaviors, mutable counters, Ajv mappings, and handler-count coverage.
- `apps/clubs/src/{SignupPage.tsx,SignupPage.test.tsx}`: E3-W01 consumer and test alignment with the published S04 names, amount objects, plan validity, family-group step, idempotency, and add-dog fields.
- `apps/clubs-admin/src/dashboard/{DashboardPage.tsx,DashboardPage.test.tsx,SignupReviewPage.tsx,SignupReviewPage.test.tsx}`: D1 and D2 pages and T-14-25/T-04-33 coverage.
- `apps/clubs-admin/src/{App.tsx,App.test.tsx,main.tsx,styles.css}`, `e2e/{dashboard-signup.spec.ts,shell.spec.ts}`: protected routes, same-origin client, shell counters/navigation, layouts, complete D1→D2→D1 flow, and prior shell assertion updates.
- `packages/ui/src/{components.tsx,components.css}`: accessible themed `BarChart` with a tabular fallback.
- `packages/i18n/src/{format.ts,resources.ts,types.ts}`, `locales/{ca,es,en}/{admin-dashboard.json,admin-census.json,signup.json}`, `scripts/i18n-check.mjs`: D1/D2 translations, date formats, resource registration, and dynamic-prefix checks.
- `roadmap/evidence/E3-W02/*`: seven command logs and three visually reviewed 1280 px screenshots.

### Rules and tests implemented

- R-04-16/18: renders the API's effectively collected amount without recalculation, Stripe-paid and zero/manual states, masked person/payment details, image-consent gender ICU, missing-account warning, family/readmission/new-dog badges, signed documents, enabled levels, and conditional next-invoice date.
- R-04-19/23: pending-only member/dog editing with published versioned PATCH shapes; dry-run plan recalculation; reason validation from 3–500; rejection; validation with field-level 422, `STALE_VERSION`, and `INVALID_STATE` handling; success navigation and toast.
- R-14-01…08: one dashboard aggregate request, server-value KPIs, all four risk phrases, complete pending list, accessible eight-level/242-dog chart, nullable/empty/error/skeleton states, retry, and focus refetch.
- Shell counters request on load and every SPA route transition; zero badges are hidden and the pending counter changes from 3 to 2 after validation.
- T-04-33 and T-14-25: 7 focused Vitest cases cover aggregate rendering, accessibility fallback, four risk states, null blocks, focus refetch, D2 details, editing, rejection, validation errors, paid/manual-zero paths, and successful validation.
- The Docker Playwright flow captures D1 and D2, validates Marta's signup, returns to D1 with a toast and count 2, and the complete three-app suite passes (47 tests).

### Assumptions

- A17b and this task explicitly set `pendingSignupAgeWarnDays = 2`; that task-local decision overrides the older S14 example value 7. D2 reads the threshold from `/dashboard` because the published signup aggregate does not carry it.
- Greeting and relative risk labels use the API's `generatedAt` interpreted in the branding contract's club time zone; KPI values and monetary amounts are rendered from API fields without recomputation.
- The published signup aggregate exposes the selected plan/price proposal but no alternative catalog. D2 therefore shows the returned selection and calls `dryRun` for any contract-provided selection change without inventing plans or prices.
- The edit drawer exposes the person/dog fields representable by the task-mandated published `MemberPatch` and `DogPatch` operations. Payment data is read-only because it is absent from those patch schemas.
- `openapi.next.json` matched API commit `83069c1`; no E3-T03 restage message arrived while this task was active.

### Questions / catalog proposals

- Contract proposal: add `notesToInstructors` to `DogPatch` if D2 must edit that screen-17 field; it is currently readable from `MemberSignupDogView` but not writable through the published dog patch.
- Contract proposal: expose eligible plan/price choices on `MemberSignupView` (or a linked catalog operation) if the D2 selector must offer alternatives instead of only the selected proposal.
- No error-code, event, or notification was invented.

### Evidence

Earlier CI-equivalent attempt: the published handlers increased the expected MSW handler count from 121 to 126; the assertion was aligned and the final run passed. Complete failure output: `roadmap/evidence/E3-W02/01-turbo-run1.log`.

#### Contract adoption and generated types

Command:

```sh
COREPACK_HOME=/tmp/agilityhub-corepack pnpm api:generate > roadmap/evidence/E3-W02/00-api-generate.log 2>&1
```

Exit code: `0`. Complete output: `roadmap/evidence/E3-W02/00-api-generate.log`.

```text
> agilityhub-core-web@0.0.0 api:generate /Users/jordib/dev/agilityhub/agilityhub-core-web
> pnpm --filter @agilityhub/api-client api:generate

> @agilityhub/api-client@0.0.0 api:generate /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client
> node scripts/generate.mjs

Generated /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client/src/generated/schema.d.ts from /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client/openapi/openapi.json merged with /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client/openapi/pending.json
```

Read-only byte/pruning check:

```sh
shasum -a 256 packages/api-client/openapi/openapi.json && git show HEAD:packages/api-client/openapi/openapi.next.json | shasum -a 256 && jq -r '.paths | keys[]' packages/api-client/openapi/pending.json && jq -r '.components.schemas | keys[]' packages/api-client/openapi/pending.json
```

Exit code: `0`.

```text
a9d69a9f0fce3e2e845bbe3e81e10e7e3eb80d426072f8c07a80742fc17e3c3e  packages/api-client/openapi/openapi.json
a9d69a9f0fce3e2e845bbe3e81e10e7e3eb80d426072f8c07a80742fc17e3c3e  -
/members/{id}/notification-preferences
DogPatchPendingFields
DogPendingFields
LicensePendingFields
LicenseWithPendingFields
NotificationPreferences
NotificationPreferencesPatch
PlanBillingMode
```

#### Ajv fixture contract suite

Command:

```sh
COREPACK_HOME=/tmp/agilityhub-corepack pnpm --filter @agilityhub/api-client test -- src/mocks/fixtures.contract.test.ts > roadmap/evidence/E3-W02/01-ajv.log 2>&1
```

Exit code: `0`. Complete output: `roadmap/evidence/E3-W02/01-ajv.log`.

```text
> @agilityhub/api-client@0.0.0 test /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client
> vitest run src/mocks/fixtures.contract.test.ts

 RUN  v5.0.0 /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client

 Test Files  1 passed (1)
      Tests  15 passed (15)
   Start at  10:46:59
   Duration  322ms (tests 57%, transform 28%, import 15%, worker 1%)
```

#### CI-equivalent workspace checks

Command:

```sh
COREPACK_HOME=/tmp/agilityhub-corepack pnpm turbo run lint typecheck test build > roadmap/evidence/E3-W02/02-turbo.log 2>&1
```

Exit code: `0`. Complete output: `roadmap/evidence/E3-W02/02-turbo.log` (644 lines). Last 40 lines:

```text
clubs:test:  ✓ src/App.test.tsx (17 tests) 1108ms
clubs:test:
clubs:test:  Test Files  3 passed (3)
clubs:test:       Tests  29 passed (29)
clubs:test:    Start at  10:44:15
clubs:test:    Duration  2.88s (tests 35%, environment 34%, transform 19%, import 8%, setup 4%, worker 1%)
clubs:test:
clubs-admin:test: stderr | src/audit/AuditPage.test.tsx > T-14-26 member audit and exports > shows the catalogued EXPORT_LIMIT explanation in the exports drawer
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test:
clubs-admin:test:  ✓ src/census/CensusListPage.test.tsx (3 tests) 1886ms
clubs-admin:test:    ✓ T-03-38 D5 universal member list (2)
clubs-admin:test:      ✓ renders applied filters and preserves selection when page size changes 657ms
clubs-admin:test:      ✓ toggles visible columns and synchronizes the ordered fields to the URL 347ms
clubs-admin:test:    ✓ T-03-42 D15 translated dog list (1)
clubs-admin:test:      ✓ renders the approved columns and dog record links 879ms
clubs-admin:test: stderr | src/audit/AuditPage.test.tsx > T-14-26 member audit and exports > routes a queued members list export into the global drawer
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test:
clubs-admin:test: stderr | src/audit/AuditPage.test.tsx > T-14-26 member audit and exports > routes a queued dogs list export into the global drawer
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test:
clubs-admin:test: stderr | src/audit/AuditPage.test.tsx > T-14-26 member audit and exports > links LastChange to the entity-scoped audit trail
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test:
clubs-admin:test:  ✓ src/audit/AuditPage.test.tsx (6 tests) 2014ms
clubs-admin:test:    ✓ T-14-26 member audit and exports (6)
clubs-admin:test:      ✓ filters by action and opens the masked before/after change drawer 402ms
clubs-admin:test:      ✓ opens the exports drawer for a queued job and exposes its READY download 1296ms
clubs-admin:test:
clubs-admin:test:  Test Files  8 passed (8)
clubs-admin:test:       Tests  33 passed (33)
clubs-admin:test:    Start at  10:44:14
clubs-admin:test:    Duration  4.01s (environment 38%, tests 33%, transform 16%, setup 7%, import 6%)
clubs-admin:test:

 Tasks:    26 successful, 26 total
Cached:    7 cached, 26 total
  Time:    10.511s
```

#### i18n and error catalog

Command:

```sh
COREPACK_HOME=/tmp/agilityhub-corepack pnpm i18n:check > roadmap/evidence/E3-W02/03-i18n.log 2>&1
```

Exit code: `0`. Complete output: `roadmap/evidence/E3-W02/03-i18n.log`.

```text
> agilityhub-core-web@0.0.0 i18n:check /Users/jordib/dev/agilityhub/agilityhub-core-web
> node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs

i18n check passed
ca: 1732 keys
es: 1732 keys
en: 1732 keys
1212 statically used keys, 0 unused
249 codes, 0 missing
```

#### Focused T-04-33 and T-14-25 Vitest

Command:

```sh
COREPACK_HOME=/tmp/agilityhub-corepack pnpm --filter clubs-admin test -- src/dashboard/DashboardPage.test.tsx src/dashboard/SignupReviewPage.test.tsx > roadmap/evidence/E3-W02/04-vitest-dashboard-signup.log 2>&1
```

Exit code: `0`. Complete output: `roadmap/evidence/E3-W02/04-vitest-dashboard-signup.log`.

```text
> clubs-admin@0.0.0 test /Users/jordib/dev/agilityhub/agilityhub-core-web/apps/clubs-admin
> vitest run src/dashboard/DashboardPage.test.tsx src/dashboard/SignupReviewPage.test.tsx

 RUN  v5.0.0 /Users/jordib/dev/agilityhub/agilityhub-core-web/apps/clubs-admin

 Test Files  2 passed (2)
      Tests  7 passed (7)
   Start at  10:46:46
   Duration  1.10s (tests 34%, environment 30%, transform 18%, import 10%, setup 7%)
```

#### Complete Playwright suite in Docker

Command (Turbo serialized and each project limited to two workers to keep the complete suite within the Docker test-server resource limit):

```sh
docker run --rm --env CI=1 --volume "$PWD:/src:ro" --volume "$PWD/roadmap/evidence:/evidence" --workdir /work mcr.microsoft.com/playwright:v1.63.0-noble sh -c 'set -eu; tar -C /src --exclude=.git --exclude=node_modules --exclude=.turbo -cf - . | tar -xf -; corepack enable; pnpm install --frozen-lockfile; e2e_status=0; pnpm e2e --concurrency=1 -- --workers=2 > /work/roadmap/evidence/E3-W02/05-playwright-all-final.log 2>&1 || e2e_status=$?; tail -40 /work/roadmap/evidence/E3-W02/05-playwright-all-final.log; cp -R /work/roadmap/evidence/. /evidence/; exit "$e2e_status"'
```

Exit code: `0`. Complete output: `roadmap/evidence/E3-W02/05-playwright-all-final.log` (247 lines). Last 40 lines:

```text
clubs:build: precache  36 entries (1402.02 KiB)
clubs:build: files generated
clubs:build:   dist/sw.js
clubs:build:   dist/workbox-9c191d2f.js
id:e2e: cache bypass, force executing 79357d6823c5e9fd
id:e2e:
id:e2e: > id@0.0.0 e2e /work/apps/id
id:e2e: > playwright test --workers\=2
id:e2e:
id:e2e:
id:e2e: Running 1 test using 1 worker
id:e2e: ·
id:e2e:   1 passed (2.8s)
clubs-admin:e2e: cache bypass, force executing db3138133160c513
clubs-admin:e2e:
clubs-admin:e2e: > clubs-admin@0.0.0 e2e /work/apps/clubs-admin
clubs-admin:e2e: > playwright test --workers\=2
clubs-admin:e2e:
clubs-admin:e2e: [WebServer] (node:838) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs-admin:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs-admin:e2e:
clubs-admin:e2e: Running 25 tests using 2 workers
clubs-admin:e2e: ·························
clubs-admin:e2e:   25 passed (28.0s)
clubs:e2e: cache bypass, force executing c556fc469fa721d9
clubs:e2e:
clubs:e2e: > clubs@0.0.0 e2e /work/apps/clubs
clubs:e2e: > playwright test --workers\=2
clubs:e2e:
clubs:e2e: [WebServer] (node:1970) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs:e2e:
clubs:e2e: Running 21 tests using 2 workers
clubs:e2e: ·····················
clubs:e2e:   21 passed (13.1s)

 Tasks:    6 successful, 6 total
Cached:    0 cached, 6 total
  Time:    52.388s
```

Screenshots, all visually compared with the approved mockups:

- `roadmap/evidence/E3-W02/D1-dashboard-1280.png` — 1280×1129.
- `roadmap/evidence/E3-W02/D2-signup-validation-1280.png` — 1280×1115.
- `roadmap/evidence/E3-W02/D1-dashboard-after-validation-1280.png` — 1280×1199; success toast and both pending counters show 2.

## Organizer verification
_(pending)_
