---
id: E2-W01
title: UniversalList component and screens D5 (members) / D15 (dogs)
stage: E2
repo: agilityhub-core-web
thread: B
order: 10
status: ready
depends_on: [E0-W06, external:api:E2-T01 (contract)]
branch: 
pr: 
updated: 2026-09-06
---

# E2-W01 · UniversalList component and screens D5 (members) / D15 (dogs)

## Objective
Work package WP-03-D: the backoffice list pattern used by every later screen, built once, pixel-faithful to D5/D15.

## Spec references
S03 §2 (D5, D15 rows), R-03-22…R-03-24 (filter-values, saved views, export), `docs/specs/00-transversal/CONVENCIONS_API.md` §3–5 (list contract) · tests **T-03-31…T-03-35** · `docs/pantalles/escriptori/D5-*.html|png`, `D15-*.html|png` · `docs/DECISIONS_PENDENTS.md` (universal column filter with active-filter indicator replaces specific filters).

## Context to load
S03 §2, §11 (front tests) · CONVENCIONS_API §3–5 · the two mockups · generated types (`pnpm api:generate` from the E2-T01 snapshot) · this file.

## Steps
1. `packages/ui` `UniversalList`: search box, status chips, **universal column filter** (per column: operators by type from `x-filterable`, values from `filter-values`, active-filter indicator with count and «Neteja»), draggable/toggleable columns from `x-columns`, sort, saved views (create/rename/delete, default), rows per page, export menu (xlsx/pdf with the visible columns), row selection + bulk actions slot, empty/loading/error states; URL-synced state.
2. `/abonats` (D5) and `/gossos` (D15) with the exact columns, chips and actions of the mockups; row click → D10 / dog record.
3. MSW handlers with realistic fixtures (≥ 60 members, 80 dogs) generated in `packages/api-client/src/mocks/fixtures/census.ts`.

## Verification (run all; paste full output in the report)
- `pnpm turbo run lint typecheck test build` + `pnpm i18n:check` green; Vitest for the filter builder (operators, URL sync); Playwright T-03-31…35; screenshots at 1280 px next to D5/D15 PNGs under `roadmap/evidence/E2-W01/`.

## Definition of Done
Component reusable + two screens + tests + screenshots + three locales.

## Evidence to return
Outputs and screenshots.

---

## Executor report
_(not started)_

### Branch / commits
### Evidence (full command outputs)
### Files changed
### Rules and tests implemented (R-xx-nn / T-xx-nn)
### Assumptions
### Questions / catalog proposals

## Organizer verification
_(pending)_
