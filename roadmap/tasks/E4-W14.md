---
id: E4-W14
title: Follow-ups of the E4-W11, E4-W06 and E4-W05 reviews (web) — the D7 publication preview's errors, disabled chips that look disabled and the exemption chip, 04's start–end, the 23 drawer's instructors, null logos, a generic «Progressió» help, the D11 and D3 queues, the mock levels and coverage, catalog row names, and a stronger E4 real-core spec
stage: E4
repo: agilityhub-core-web
thread: B
order: 57
status: verified
depends_on: []
branch:
pr:
updated: 2026-09-26
---

# E4-W14 · Follow-ups of the E4-W11, E4-W06 and E4-W05 reviews

## Objective
The independent reviews of E4-W11 (`roadmap/reviews/E4-W11-20260926-0232-claude.md`), E4-W06 (`roadmap/reviews/E4-W06-20260926-0225-claude.md`) and E4-W05 (`roadmap/reviews/E4-W05-20260926-0125-claude.md`) found minors and nits that the organizer confirmed and decided. The three tasks were verified with these points routed here. The api points of the same reviews are api E5-T20.

**Each fix comes with a test that fails before the fix.**

## Spec references
- S07 §6 (`GET /activities/{id}/ring-conflicts`: `422 OUTSIDE_OPENING_HOURS`, `400 INVALID_TIME_RANGE`; amended 26-09), R-07-05, R-07-13.
- S06 §3 (the D4 card on a closed day), R-06-06 (coverage: progression levels only).
- S05 §2 (the «Nivells» card, and its generic help, amended 26-09), E29.
- AGENTS rules 2 (no club literal in locales), 5 (test names) and 6 (labels).

## Context to load
- The three reviews, and the organizer verifications of E4-W11, E4-W06 and E4-W05.
- `apps/clubs-admin/src/activities/ActivityPage.tsx` (`startPublish`, near `:687`), `packages/api-client/src/mocks/activity-handlers.ts` (near `:818`).
- `apps/clubs-admin/src/planning/SelectedClassCard.tsx` (near `:379`–`:516`), `planning/planning.css` (near `:306`), `planning/TemplatesPage.tsx` (`queueRemoval`, near `:1210`).
- `apps/clubs/src/activities/ActivitiesBlock.tsx` (near `:52`), `apps/clubs/src/today/OverviewPage.tsx` (near `:100`).
- `packages/ui/src/branding.tsx`, `branding.test.tsx`, `mocks/fixtures/branding-minim.json`.
- `apps/clubs-admin/src/catalogs/SettingsPage.tsx` (near `:471`–`:528`), `SettingsPage.test.tsx`, `mocks/fixtures/catalogs.ts` (near `:113`), the `/coverage` mock.
- `scripts/e2e-core.sh`, `e2e/core/e4-core.spec.ts`.

## Steps
1. **D7: the publication preview's errors (E4-W11 review #1).** The mock `GET /activities/{id}/ring-conflicts` answers like the api: `422 OUTSIDE_OPENING_HOURS` and `400 INVALID_TIME_RANGE` for a window the publication would refuse. `startPublish` places `OUTSIDE_OPENING_HOURS` on the date and times, as the save does, never as a general toast. Mock test and Vitest.
2. **D4 on a closed day (E4-W11 reviews #2 and #3).**
   - The disabled chip editors look disabled: a token-based muted look and a `not-allowed` cursor.
   - The chip «Exempta de la revisió de les 7:30» stays enabled: the exemption is its own action, and the api accepts it on a closed day.
   - Vitest.
3. **04 prints start–end (E4-W11 review #4, R-07-13).** A row prints `startTime`–`endTime` when both are present, and only `startTime` without an end. Vitest.
4. **The 23 drawer's instructors (E4-W11 review #5, question 1).** The label follows the count: ca «Instructor» / «Instructors», es «Instructor» / «Instructores», en «Instructor» / «Instructors». The names use `formats.formatList`. Vitest.
5. **Null logos (E4-W11 review #7).** A `branding.test.tsx` case passes `null` logos. `branding-minim.json` sends `null`, as `/branding` does since api E5-T16.
6. **A generic «Progressió» help (E4-W06 review #1, S05 §2).** `admin-catalogs:levels.progressionHelp` names no club level:
   - ca «Els nivells fora de la progressió no compten per a les descripcions «… i sup.».»;
   - es «Los niveles fuera de la progresión no cuentan para las descripciones «… y sup.».»;
   - en «Levels outside the progression don't count for «… and up» descriptions.».
7. **The «Progressió» switches and the removal queue (E4-W06 reviews #2–#4).**
   - The switches keep a set of pending level ids: a second toggle never re-enables a switch whose PATCH is still pending, and each row keeps its own error.
   - `queueRemoval` bumps the generation only after a class DELETE succeeds, and never for a band removal. A send it drops rejects, so the form puts the chips back.
   - Vitest for both, and one for «Treu la franja» (`removeBand`) through the queue.
8. **D11 and D3 details (E4-W06 reviews #7–#9, question 4).**
   - The «Nivells» describe cites T-02-13.
   - The mock levels include `PENDENT` («Pendent», outside the progression). The mock `/coverage` lists only progression levels (R-06-06).
   - The «Nou nivell» button keeps its icon inline at 1280, and the help text sits inside the card.
   - The catalog tables' row buttons are named after the row's name, not its id («Edita Nivell A», not «Edita level-t»). This follows AGENTS rule 6.
   - Vitest.
9. **A stronger E4 real-core spec (E4-W05 reviews #3 and #7).**
   - `scripts/e2e-core.sh` saves the tail of the seed container's log into the task's evidence folder, which proves `--week-start`.
   - `e2e/core/e4-core.spec.ts`:
     - (a) asserts that the generation body carries the Saturday template;
     - (d) counts the Torneig cells on the Saturday;
     - (e) asserts «(no dog)» on the 03 row;
     - the es repeat checks the admin chip «en lista de espera (n)», and that the row goes after the member's own cancellation.

## Fixed conventions
- Design system only.
- Keys are never literals; ca, es and en are added together.
- Mocks behave like the api.
- Heavy runs go through the host lock.
- Fictional data only.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E4-W14/*.log`)
- `pnpm turbo run lint typecheck test build`.
- `pnpm i18n:check`.
- `pnpm e2e:core E4-W14`.
- `pnpm e2e:docker`.

## Definition of Done
- Steps 1–9 are done. Steps 1–8 each have a test that fails before the fix.

## Evidence to return
- The command outputs.
- A step → test table.

---

## Executor report

**Sessions.** A first session (published in `d98a0a8`, 26-09 04:23) wrote the code, the tests and the before-fix logs `01-*`. It also ran the Verification commands on intermediate trees (logs `02`–`13`, all `exit 0`), but it ended before it wrote this report. This session (26-09, 09:00):
- reviewed that diff against steps 1–9 and the step 4b checklist;
- found nothing to change in the code;
- re-ran the four Verification commands on the final tree, which is `d98a0a8` plus the two organizer commits that only stage `openapi.next.json` (`67a4797`, `b8530b5`). Logs `14`–`17`.

### Summary
- **Step 1, D7:** the mock `GET /activities/{id}/ring-conflicts` checks the same ring-block window as the publication. It answers `422 OUTSIDE_OPENING_HOURS` or `400 INVALID_TIME_RANGE`, with `details: {}` and no field, as S07 §6 (amended 26-09) says. The 400 also covers a window shorter than `training.slotMinutes` (R-07-05). `startPublish` sends the preview's error through the save's `saveErrorTargets`:
  - `OUTSIDE_OPENING_HOURS` goes on the date, the start and the end;
  - `INVALID_TIME_RANGE` goes on the end time;
  - only an error without a field target becomes the page message.
- **Step 2, D4 on a closed day:**
  - Each disabled chip editor gets `calendar-chip--disabled`: `--ah-color-text-muted`, a dashed border, `opacity: 0.6`, and `cursor: not-allowed` on the chip and on its control. Only tokens are used.
  - «Exempta de la revisió de les 7:30» is disabled only by `busy || changed`: it is its own action (`POST …/risk-exemption`).
- **Step 3, 04:** `ActivityBlockRow` passes `row.endTime` to `formatActivityDate`, so the row reads «9:00–13:00» when both times are present and «9:00» without an end.
- **Step 4, 23's drawer:** `instructor:overview.class.instructors` is an ICU plural:
  - ca «Instructor»/«Instructors»;
  - es «Instructor»/«Instructores»;
  - en «Instructor»/«Instructors».
  The names go through `formats.formatList` («Marc i Núria», «Marc y Núria», «Marc and Núria»).
- **Step 5:** `branding-minim.json` sends `logoUrl`, `logoDarkUrl` and `markUrl` as `null`. A new `branding.test.tsx` case resolves `null` logos in both placements.
- **Step 6:** `admin-catalogs:levels.progressionHelp` uses the literals of the step in the three locales. They are also the S05 §2 literal.
- **Step 7:**
  - **«Progressió»:** the switches keep a `Set` of pending level ids and a per-level error map. The error shows under its own switch and is added to that switch's `aria-describedby`.
  - **D3 queue:** `queueRemoval(remove, { dropsQueuedChanges })` bumps the generation only after a class DELETE succeeds. A band removal never bumps it. It reads the template again, so a class change queued after the removal is sent with the new version. A change the queue drops rejects with `DroppedChangeError`: `ClassForm` puts its fields back and keeps the message of the change that caused the drop.
- **Step 8:**
  - The «Nivells» describe cites T-02-13.
  - The mock levels include `PENDENT` «Pendent» (order 90, capacity 5, outside the progression).
  - The mock `/coverage` lists the active progression levels in catalog order, under their current names.
  - The help sits inside the table card (`CatalogTable.footer`), and `.catalog-page .ah-button__content` keeps «+ Nou nivell» on one line.
  - `CatalogTable` takes a required `rowName`: levels and plans by name, FAQ by question, rings by name, team rows by member label. This gives «Edita Teràpia» and «Elimina Laura Serra Vidal (Duna)» instead of «Edita level-t».
- **Step 9:**
  - `scripts/e2e-core.sh` writes the last 200 lines of the seed container's log to `seed-<stage>.log`, with the run's password replaced by `[redacted]`. It does so even when `up --wait` fails.
  - `e4-core.spec.ts`:
    - (a) the generation body must equal `{weekdayTemplateId: «Setmana A», saturdayTemplateId: «Dissabtes»}`;
    - (d) the Torneig cells are counted per column: exactly one, under the Saturday header;
    - (e) the whole 03 row is matched: title, «inscrita», «{day} 18:30–20:30 · Central» and nothing else, so no dog;
    - es: 03 no longer lists the row after the member's own cancellation, and D7 shows the member's chip «en lista de espera (n)» in es.

### Step → test (the "failed before" column: logs `01-vitest-before-fix-*.log`, run on the unfixed files)
| Step | Test (file › describe › it) | Failed before the fix |
|---|---|---|
| 1 | `packages/api-client/src/mocks/activity-handlers.test.ts` › «E4-W14 S07 §6 the ring-conflicts preview refuses the window the publication would: 422 OUTSIDE_OPENING_HOURS and 400 INVALID_TIME_RANGE, without a field» | yes (`01-…-api-client.log`) |
| 1 | `apps/clubs-admin/src/activities/ActivitiesPage.test.tsx` › «T-07-29 E4-W14 D7 follow-ups of the E4-W11 review» › «R-07-05 S07 §6 a draft saved at 6:00 against a 7:00 opening: the preview's 422 … lands on the date and both times, not in a toast» and «… with the opening hours unread, a closed Sunday reaches the preview: its 422 lands on the fields; a 400 INVALID_TIME_RANGE of the window on the end time» | yes, both (`01-…-clubs-admin.log`) |
| 1 | e2e `apps/clubs-admin/e2e/activities.spec.ts` › «T-07-29 E4-W14 D7 the publication preview's errors (S07 §6)» (the preview answers 422 in the browser; `D7-publica-6h-camps-1280.png`) | — (e2e) |
| 2 | `apps/clubs-admin/src/planning/CalendarPage.test.tsx` › «T-06-28 E4-W14 D4 follow-ups of the E4-W11 review» › «S06 §3 R-02-09 on a closed Sunday the disabled chip editors look disabled (muted, not-allowed); an open day's do not» and «S06 §3 R-06-09 … the «Exempta de la revisió de les 7:30» chip stays enabled and is saved through its own action» | yes, both |
| 2 | e2e `planning-calendar.spec.ts` › «T-06-28 E4-W14 D4 closed-day chips (E4-W11 review #2 and #3)» (`D4-diumenge-tancat-xips-1280.png`) | — (e2e) |
| 3 | `apps/clubs/src/activities/activities.test.tsx` › «T-07-30 E4-W14 R-07-13 the 04 row prints the activity's hours» (and three older 04 expectations moved to start–end) | yes (`01-…-clubs.log`, 4 tests) |
| 3 | e2e `apps/clubs/e2e/activities.spec.ts` › «T-07-30 E4-W14 R-07-13 the 04 rows print the activity's hours» (`04-activitats-hores-375.png`) | — (e2e) |
| 4 | `apps/clubs/src/today/OverviewPage.test.tsx` › «T-06-29 E4-W14 screen 23's drawer: the instructors' label follows the count, the names are a list» (ca/es/en, one and two names; the E4-W11 case now reads «Marc i Núria») | yes, both |
| 4 | e2e `apps/clubs/e2e/day-grid.spec.ts` › «T-06-29 E4-W14 screen 23's class drawer names its instructors» (`23-calaix-instructor-375.png`) | — (e2e) |
| 5 | `packages/ui/src/branding.test.tsx` › «T-02-05 BrandingProvider» › «E4-W14 a club without assets: `/branding` sends null logos (api E5-T16), read as absent in both placements» | yes (`01-…-ui.log`) |
| 6 | `SettingsPage.test.tsx` › «T-02-13 E4-W14 S05 §2 D11 «Nivells» follow-ups of the E4-W06 review» › «S05 §2 the «Progressió» help names no club level, in ca, es and en» | yes |
| 7 | same describe › «a second toggle never re-enables a switch whose PATCH is still pending; each row keeps its own error» | yes |
| 7 | `TemplatesPage.test.tsx` › «T-06-26 E4-W14 D3 the removal queue (E4-W06 review #3 and #4)» › «a band removal never drops a class change queued after it», «a failed «Treu de la plantilla» keeps the change queued after it», «#7 a change dropped after a failed PATCH is never left on screen: its chip goes back and the first error stays» | yes, all three |
| 7 | same describe › «R-06-02 «Esborra la franja» (removeBand) waits in the queue for the level PATCH before it; then the drawer closes» | no: coverage the review asked for (#4); the old code already queued the band DELETE |
| 8 | `SettingsPage.test.tsx` › describe renamed «T-02-13 E4-W06 …» (the T-id; no failing test possible for a name) | — |
| 8 | `SettingsPage.test.tsx` › «R-06-06 S05 §12 B32 the mock levels include «Pendent»…»; `fixtures.contract.test.ts` › «validates the E29 level catalog» (Teràpia + Pendent outside) | yes, both |
| 8 | `TemplatesPage.test.tsx` › «T-06-26 E4-W14 D3 coverage of the progression levels» › «R-06-06 the coverage table lists only the progression levels: Teràpia and Pendent never, Cadells not once it leaves the progression» | yes |
| 8 | `SettingsPage.test.tsx` › «S05 §2 the help sits inside the «Nivells» card, and «Nou nivell» keeps its icon inline»; e2e `settings.spec.ts` › «T-02-13 E4-W14 D11 «Nivells» card follow-ups…» (icon left of the label, one line, centred at 1280; `D11-nivells-1280.png`) | yes (Vitest) |
| 8 | `SettingsPage.test.tsx` › «AGENTS rule 6: the catalog row buttons are named after the row's name, not its id»; e2e `catalogs.spec.ts` now clicks «Elimina Laura Serra Vidal (Duna)» and «Edita Abonat» | yes (Vitest) |
| 9 | `scripts/e2e-core.sh` `save_seed_log`: `seed-e4.log` line 105 «213 changes (demo planning, week start 2026-09-21)»; `e4-core.spec.ts` (a), (d), (e) and the es run: `e4-core-run.json` → `generation.body`, `tournamentSaturday.cellsByColumn {"ds 10": 1}`, `row03Ca`, `waitlistChipEs «en lista de espera (4)»` | — (real-core e2e, log `16`) |

The before-fix logs sum to 24 failing tests: api-client 2 of 73, ui 1 of 5, clubs 6 of 37, clubs-admin 15 of 116. Each log's first line is the command. The first session produced the unfixed files by reading the `HEAD` versions of the touched sources, with no git writes.

### Evidence (final runs, this session)

**1. `pnpm turbo run lint typecheck test build --force`**: `--force` bypasses the turbo cache, so nothing is replayed. Exit **0**. Full log: `roadmap/evidence/E4-W14/14-turbo.log`, with ANSI colours removed from the tail below. Vitest per package:
- course-core 342;
- ui 68;
- api-client 189;
- shared-types 21;
- i18n 63;
- auth 17;
- id 9;
- clubs 178;
- clubs-admin 312.
```
clubs-admin:test:      ✓ «Nova activitat» creates a draft with title and type and opens its maintenance 910ms
clubs-admin:test:      ✓ shows the mockup maintenance of «Torneig d'Estiu 2026» with the ring, level and waitlist chips 478ms
clubs-admin:test:      ✓ disables the ring chips of an activity outside the club («— (fora del club)») 350ms
clubs-admin:test:      ✓ R-07-14 hides «Nivells» with levels.enabled=false and «Llista d'espera» without WAITLIST 462ms
clubs-admin:test:      ✓ R-07-03 [DESA] sends only the allow-listed rich text of the editor, with the version 312ms
clubs-admin:test:      ✓ R-07-05 [PUBLICA] with conflicts opens the dialog with the conflicts and the required notice text 376ms
clubs-admin:test:      ✓ R-07-04 a draft without the registration period answers ACTIVITY_INCOMPLETE on the fields 302ms
clubs-admin:test:      ✓ R-07-06 [CANCEL·LA L'ACTIVITAT] opens the modal with «CANCEL·LA I AVISA ELS 22 INSCRITS» disabled without text 353ms
clubs-admin:test:      ✓ R-07-04 after an upload [DESA] sends only the admin's own edits with the fresh version 306ms
clubs-admin:test:    ✓ T-07-29 E4-W08 D7 follow-ups of the E4-W04 round-2 review (6)
clubs-admin:test:      ✓ R-07-05 away from the club the hours cover the whole day: 6:00 when the club opens at 7:00 540ms
clubs-admin:test:    ✓ T-07-29 E4-W10 D7 follow-ups of the E4-W08 and E4-W09 reviews (3)
clubs-admin:test:      ✓ S07 §6 CONVENCIONS_API §4 the member filter reads every page: with 1,001 distinct registrants the member on the second page can be selected 796ms
clubs-admin:test:      ✓ R-07-05 R-02-09 at the club with linked rings, a closed day shows «El club està tancat aquest dia» and offers no times; an open day or away from the club offers them 317ms
clubs-admin:test:  ✓ src/planning/CalendarPage.test.tsx (41 tests) 11413ms
clubs-admin:test:    ✓ E3-W07 step 9 a D1 risk row opens D4 on its class (1)
clubs-admin:test:      ✓ selects the class of `?classe=` in the week of `?setmana=` 755ms
clubs-admin:test:    ✓ T-06-28 D4 / D4b / D4c class calendar (front half, MSW) (24)
clubs-admin:test:      ✓ opens «Esborrany» on the first week with drafts, with the validation card and its count 1097ms
clubs-admin:test:      ✓ [VALIDAR LA SETMANA] turns every draft of the week active at once 621ms
clubs-admin:test:      ✓ cancels an active class with registrants through D4c: four rows, text required, «ANUL·LA I AVISA ELS 4 ALUMNES» 657ms
clubs-admin:test:      ✓ [ELIMINA] of an active class goes through the same D4c modal 517ms
clubs-admin:test:      ✓ R-06-09 saves the diff with the version; STALE_VERSION keeps its message after the refetch remounts the card 637ms
clubs-admin:test:      ✓ R-06-09 [Crear classe]: a new time or ring after RING_HAS_BOOKINGS drops the confirmation 356ms
clubs-admin:test:      ✓ R-06-09 [Crear classe]: a RING_HAS_BOOKINGS answer that arrives after a ring change is dropped 335ms
clubs-admin:test:      ✓ R-06-11 lists the RING_BLOCK_CONFLICT conflicts inside the block drawer 316ms
clubs-admin:test:    ✓ T-06-28 E4-W09 D4 follow-ups of the E4-W02 round-4 review (7)
clubs-admin:test:      ✓ S06 §3 R-06-09 an opening at 07:05 with 10-minute slots: «Hora» and [Crear classe] offer 07:10 first, and the class is sent on slot boundaries 333ms
clubs-admin:test:      ✓ R-06-09 after STALE_VERSION every editor stays locked until the refetched version is shown, so no edit is lost 356ms
clubs-admin:test: 
clubs-admin:test:  Test Files  20 passed (20)
clubs-admin:test:       Tests  312 passed (312)
clubs-admin:test:    Start at  09:05:31
clubs-admin:test:    Duration  17.09s (tests 45%, environment 26%, transform 18%, import 6%, setup 5%)
clubs-admin:test: 

 Tasks:    34 successful, 34 total
Cached:    0 cached, 34 total
  Time:    30.543s
```

**2. `pnpm i18n:check`**: exit **0**. Complete output below, also in `15-i18n-check.log`.
```
> agilityhub-core-web@0.0.0 i18n:check /Users/jordib/dev/agilityhub/agilityhub-core-web
> node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs && node scripts/check-parameter-keys.mjs

i18n check passed
ca: 2368 keys
es: 2368 keys
en: 2368 keys
1761 statically used keys, 0 unused
250 codes, 0 missing
151 catalog parameters (122 in D11, 29 system-only), 10 blocks, 0 missing
```

**3. `…/roadmap-kit/mac/heavy.sh pnpm e2e:core E4-W14 > roadmap/evidence/E4-W14/16-e2e-core.log 2>&1`**: exit **0**. Full log: `16-e2e-core.log`.
- **Host lock:** «09:05 waiting for the host lock, held by: … agilityhub-core-api: ./mvnw -q clean verify», then «09:08 got the host lock after 3 min 0 s».
- **Core image:** `ghcr.io/jboixtcm/agilityhub-core-api:main`, image `sha256:2cb5850f8faa…`, revision label `be2f4a821de1b84bb84aa4a6f09f8f93e6041ccb`, created 2026-09-26T02:04Z.
- **Results by stage:**
  - e1–e2: 12 passed;
  - e3: 5 passed;
  - e4: 7 passed.
- **Seed logs:** `seed-e1-e2.log`, `seed-e3.log` and `seed-e4.log`, 105 lines each.
```
+ eslint-import-resolver-typescript 4.4.5
+ eslint-plugin-import 2.32.0
+ eslint-plugin-jsx-a11y 6.10.2
+ eslint-plugin-react 7.37.5
+ eslint-plugin-react-hooks 7.1.1
+ globals 17.12.0
+ husky 9.1.7
+ i18next-parser 9.4.0
+ lint-staged 17.5.0
+ msw 2.15.0
+ prettier 3.9.6
+ size-limit 13.0.3
+ turbo 2.10.12
+ typescript 5.9.3
+ typescript-eslint 8.69.0

. prepare$ node packages/config/scripts/prepare-husky.mjs
. prepare: .git can't be found
. prepare: Done
Done in 4s using pnpm v9.15.9
[WebServer] (node:134) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)

[WebServer] (node:214) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)


[WebServer] (node:312) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)

Running 7 tests using 1 worker

[1/7] e2e/core/e4-core.spec.ts:427:5 › T-06-28 E2E (a) D3: the three seeded templates, «Setmana B» blocked by its inconsistency, «Setmana A» generates the proposed week, D3b per ring
[2/7] e2e/core/e4-core.spec.ts:562:5 › T-06-28 E2E (b) D4: «Esborrany» opens the seeded draft week, [VALIDAR LA SETMANA], the D4c modal with the 4 registrants (200), «Anul·lades», ring blocks (409 then 201)
[3/7] e2e/core/e4-core.spec.ts:712:5 › T-06-28 E2E (c) screens 10 and 23: the member never sees the cancelled class nor counts, the validated week is visible; the instructor sees the block, the dimmed class and n/n
[4/7] e2e/core/e4-core.spec.ts:767:5 › T-07-32 E2E (d) D7: the four seeded activities, the Torneig blocks every ring on D4, a new seminar published over a class with the conflict dialog; D7 exports against the core
[5/7] e2e/core/e4-core.spec.ts:908:5 › T-07-32 E2E (e) ca: 04 → detail → register → 03 → cancel in time; the waitlist of the full «Seminari de handling» and the FIFO promotion when the admin adds a place
[6/7] e2e/core/e4-core.spec.ts:1064:5 › T-07-32 E2E (e) es: the same member flow with the es literals, a second FIFO promotion, then the admin cancels the seminar (200) and the registrant reads «cancel·lada pel club»
[7/7] e2e/core/e4-core.spec.ts:1230:5 › E4-W05 step 7 · the «Excel · PDF» menus of D5 and D7 with EXPORT_TOO_LARGE and while an export is pending
  7 passed (17.4s)
```
What `e4-core-run.json` recorded in this run:
- `generation.body` = `{"saturdayTemplateId": "1b3d05c0-…", "weekdayTemplateId": "e8a058ae-…"}`: the ids the spec read for «Dissabtes» and «Setmana A». Status 200, 38 classes, week 2026-10-12.
- `tournamentSaturday` = `{"cellsByColumn": {"ds 10": 1}, "date": "2026-10-10"}`.
- `row03Ca` = «Seminari E4 i1yjpzinscritaDissabte 17 d’octubre · 18:30–20:30 · Central».
- `waitlistChipEs` = «en lista de espera (4)».

**4. `…/roadmap-kit/mac/heavy.sh pnpm e2e:docker E4-W14 > roadmap/evidence/E4-W14/17-e2e-docker.log 2>&1`**: exit **0**. Full log: `17-e2e-docker.log`.
- **Suite:** all three Playwright projects in the Playwright image: clubs 33 passed, clubs-admin 47 passed, id 1 passed.
- **Screenshots:** the five E4-W14 screenshots listed at the end of the tail.
```
clubs:e2e: 
clubs:e2e: > clubs@0.0.0 e2e /work/apps/clubs
clubs:e2e: > playwright test
clubs:e2e: 
clubs:e2e: [WebServer] (node:664) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs:e2e: 
clubs:e2e: Running 33 tests using 7 workers
clubs:e2e: ·································
clubs:e2e:   33 passed (13.7s)
clubs-admin:e2e: cache bypass, force executing efd0d9db95f64795
clubs-admin:e2e: 
clubs-admin:e2e: > clubs-admin@0.0.0 e2e /work/apps/clubs-admin
clubs-admin:e2e: > playwright test
clubs-admin:e2e: 
clubs-admin:e2e: [WebServer] (node:2230) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs-admin:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs-admin:e2e: 
clubs-admin:e2e: Running 47 tests using 9 workers
clubs-admin:e2e: ···············································
clubs-admin:e2e:   47 passed (29.0s)
id:e2e: cache bypass, force executing 9abffdafdb4d4944
id:e2e: 
id:e2e: > id@0.0.0 e2e /work/apps/id
id:e2e: > playwright test
id:e2e: 
id:e2e: 
id:e2e: Running 1 test using 1 worker
id:e2e: ·
id:e2e:   1 passed (3.1s)

 Tasks:    8 successful, 8 total
Cached:    0 cached, 8 total
  Time:    58.405s 

evidence: E4-W14/23-calaix-instructor-375.png
evidence: E4-W14/D4-diumenge-tancat-xips-1280.png
evidence: E4-W14/04-activitats-hores-375.png
evidence: E4-W14/D7-publica-6h-camps-1280.png
evidence: E4-W14/D11-nivells-1280.png
```

**Earlier runs (first session, intermediate trees, all `exit 0`, superseded by `14`–`17`):**
- Turbo: `02-turbo.log`, `06-turbo-final.log`, `10-turbo.log`, each 34/34.
- `i18n:check`: `03-`, `07-` and `11-i18n-check.log`.
- `e2e:core`: `04-e2e-core.log`, `08-e2e-core-final.log`, `13-e2e-core.log`, each 12 + 5 + 7.
- `e2e:docker`: `05-e2e-docker.log` (31 + 44 + 1: before the new e2e tests were added), then `09-e2e-docker-final.log` and `12-e2e-docker.log` (33 + 47 + 1).

None failed. They were re-run because the first session kept editing the tree between runs.

### Screenshots (`roadmap/evidence/E4-W14/`)
- `D7-publica-6h-camps-1280.png`: the 422 message under Data, Hora d'inici and Hora de final, with no toast.
- `D4-diumenge-tancat-xips-1280.png`: the muted, dashed chip editors on the closed Sunday; «Exempta…» enabled; «El club està tancat aquest dia».
- `04-activitats-hores-375.png`: «ds 22 · 10:00–12:00», «ds 12/09 · 9:00–13:00».
- `23-calaix-instructor-375.png`: «Instructor · Marc».
- `D11-nivells-1280.png`: «Pendent» off, the help inside the card, «+ Nou nivell» on one line.
- Refreshed by the real-core run: every `*-core-*.png`, `seed-*.log` and `*.json`. Examples: `D7-inscrits-llista-espera-core-es-1280.png`, `03-inscrita-core-375.png`, `D4-activitat-totes-les-pistes-core-1280.png`.

### Files changed (code, `d98a0a8`)
- **D7:** `apps/clubs-admin/src/activities/ActivityPage.tsx`, `packages/api-client/src/mocks/activity-handlers.ts` (+ test), `ActivitiesPage.test.tsx`, `e2e/activities.spec.ts`.
- **D4:** `planning/SelectedClassCard.tsx`, `planning/calendar.css`, `CalendarPage.test.tsx`, `e2e/planning-calendar.spec.ts`.
- **D3:** `planning/TemplatesPage.tsx`, `planning/ClassForm.tsx`, `planning/shared.ts` (`DroppedChangeError`), `TemplatesPage.test.tsx`, `mocks/planning-handlers.ts` (`/coverage`).
- **D11 and catalogs:** `catalogs/SettingsPage.tsx`, `catalogs/shared.tsx` (`rowName`, `footer`), `PlansPage.tsx`, `RingsPage.tsx`, `TeamPage.tsx`, `styles.css`, `SettingsPage.test.tsx`, `e2e/settings.spec.ts`, `e2e/catalogs.spec.ts`, `mocks/fixtures/catalogs.ts`, `fixtures.contract.test.ts`.
- **App:** `apps/clubs/src/activities/ActivitiesBlock.tsx` (+ test, e2e), `apps/clubs/src/today/OverviewPage.tsx` (+ test), `apps/clubs/e2e/day-grid.spec.ts`.
- **Branding:** `packages/ui/src/branding.test.tsx`, `mocks/fixtures/branding-minim.json`.
- **Locales:** `admin-catalogs.json` and `instructor.json` in ca, es and en.
- **Real core:** `scripts/e2e-core.sh`, `e2e/core/e4-core.spec.ts`.
- **Changelog:** `CHANGELOG.md`.

This session changed only this report, `roadmap/MESSAGES.md`, `STATUS.md` (rendered), and the evidence files named above.

### Rules and tests
- **Rules:** R-07-05 (step 1), R-07-13 (step 3), R-02-09 and R-06-09 (step 2), R-06-02 (step 7), R-06-06 (step 8).
- **Test ids:** T-07-29 (D7), T-06-28 (D4), T-06-26 (D3), T-07-30 (04), T-06-29 (23), T-02-13 (D11), T-02-05 (branding). T-06-28 and T-07-32 are covered by the real-core spec.

### Assumptions
1. **Step 8, `/coverage`:** the mock has figures only for the D3 mockup's levels. A level moved into the progression later, such as Teràpia after a D11 toggle, gets no row in the mock, because inventing figures would be worse. The api would compute one. Leaving the progression, like Cadells in the test, removes the row, as R-06-06 says.
2. **Step 7, the per-row error test:** it uses `403 FORBIDDEN` with `details: {}`. That is a real api shape which does not reload the list. The `STALE_VERSION` reload path was already covered by E4-W06.
3. **Step 7, the band button:** «Treu la franja» in the step is the band drawer's «Esborra la franja» button (`admin-scheduling:…remove`). Its queue test also passed on the old code, which already queued the band DELETE: it is the coverage review #4 asked for.
4. **Step 3, the MSW 04 row:** it now reads «ds 12/09 · 9:00–13:00», where the mockup shows «9:00», because the fixture's Seminari has `endTime` 13:00 and step 3 (R-07-13) asks for start–end.
5. **The «Pendent» colour:** the mock uses the mock palette like every other mock level (`ringColor(2)`), not S05 §12's colour. None of the mock level colours match S05 §12.
6. **Turbo `--force`:** I added `--force` to the turbo command so that no task was replayed from the cache. The command is otherwise identical.

### Questions / catalog proposals
- None. No new parameter, event, notification or error code.
- **Denied commands:** some were denied (shell loops with `$f`, `ls` outside the repo, `cd … && git …`, `sed …; grep …` chains). Each had an allowed alternative, and none changed the result.

### Round 2 report

This session, 26-09 from 15:55, fixed points 1–3 and nits #4–#8 of the organizer's round-2 list. Nit #9 needed no action. The tree is HEAD `11c3035` plus this session's changes. The organizer edited other files in the same working tree during the session (`docs/`, `roadmap/MESSAGES.md`, and the E4-W13, E5-W01 and E5-W04 task files); I did not touch them.

#### What changed
1. **Review #1 (major): the preview's 400, as the api answers it (R-07-04, R-07-05, S07 §6, T-07-04).**
   - **Mock `ringBlockWindowProblem`** mirrors `RingBlockWindow.of`. With rings, a missing date, start or end is `400 INVALID_TIME_RANGE` with `details: {}`. So is a `ringBlockWindow` that starts after `startTime` or ends before `endTime`. The old test at `activity-handlers.test.ts:800` now expects that 400 for `{10:00, 10:30}` and for `{10:30, 12:00}`, and a 200 for a window around the activity (`{09:30, 12:30}`).
   - **Mock `PATCH`:** a `PUBLISHED` activity is validated as a publication. A `PATCH` that clears a time (with rings) or the registration period is `422 ACTIVITY_INCOMPLETE {fieldErrors}`, and the activity is unchanged.
   - **Mock `ACTIVITY_INCOMPLETE`:** each missing hour is its own field error; before, only one of the two was listed.
   - **The mock's create now leaves `date: null`**, as the core's create does (proven below). `ACTIVITY_INCOMPLETE` then names `date` first, in the core's order: date, registration period, hours. The fixture helpers read the date only for dated activities: `Activity.startsAt` and `endsAt` are `null` without a date, as the schema allows.
   - **`startPublish`:** a draft at the club with rings but without a date or both hours skips the preview. It saves first when the form is dirty, and uses the saved activity rather than the stale closure. Then it sends `POST …/publication` with one `Idempotency-Key`, while [PUBLICA] shows its pending state. The `422 ACTIVITY_INCOMPLETE` marks every empty field «Cal per publicar» and shows «Falten dades per publicar l'activitat: revisa els camps marcats.». No «L'interval horari no és vàlid.» appears.
   - **An existing test that relied on the mock gap:** «R-07-04/05 save mode…» changed «Hora de final» before the opening hours were read, so the select had no «20:00» and it sent `endTime: null`. The old mock accepted that on the published Torneig. The test now waits for the option.
2. **Review #2 (minor): the D3 queue drops only what it must.**
   - **Per-class tracking:** a map keyed by class id replaces the page-wide counter. A class's failed `PATCH`, or its successful `DELETE`, drops only that class's changes queued after it.
   - **Other classes' changes are sent:** after a class or band removal, `rereadTemplate` reads the template's new version before the next queued change goes out, as `removeBand` already did.
   - **A message for every drop:**
     - A dropped change whose form is gone (another class opened, a remount, the class removed) shows its cause as the page toast. A removed class reads `errors:NOT_FOUND` («No s'ha trobat l'element sol·licitat.»), what the api would answer to a change sent to it.
     - A refused change whose form is gone shows its own error the same way.
     - Inside a mounted form, a drop shows the refusal only when no message is shown (`DroppedChangeError.refusal`).
   - **The form key is read synchronously:** `setCardMode` now updates `cardRef` before React renders. A temporary debug log showed the effect-based ref was still stale when the queue checked it.
3. **Review #3 (minor): the 03 checks can fail.** `expectGoneFrom03` waits for the `GET /me/activities` that 03 renders from. It asserts that `mine[]` has no `ACTIVE` or `WAITLISTED` entry for the seminar, then that no row is shown. The ca check (after the 03 cancellation) and the es check both use it. The core listed no entry at all for the seminar: `mine03AfterCancelCa` and `mine03AfterCancelEs` are `[]`.
4. **Nits:**
   - **#4:** the «Pendent» test is renamed «S05 §12 B32 the mock levels include «Pendent»…», without R-06-06.
   - **#5:** the 04 e2e is renamed «04: «{hh:mm}–{hh:mm}» for the activities with both hours», with a comment pointing to the Vitest that covers the start alone. No open-registration mock row lacks an end, and adding one would change every D7 list count.
   - **#6:** the failed «Treu de la plantilla» test now asserts the `DELETE`'s error in the card («S'ha produït un error inesperat…»).
   - **#7:** the rule `.ah-button__content { display: inline-flex; align-items: center; gap: var(--ah-space-1) }` is in `packages/ui/src/components.css`. The four copies are gone (census and catalog in `styles.css`, `planning.css`, `activities.css`). The D11 Vitest reads the design-system rule and checks that no app CSS redefines it. The settings e2e still checks the icon to the left of «Nou nivell» at 1280.
   - **#8:** the mock `/coverage` gives every active progression level a row. A level without fixture figures gets the zero row: `dogsTotal`/`dogsActive`/`maxSeats`/`propSeats` 0, `maxRatioPct`/`propRatioPct` `null`, `status: NO_DOGS` («sense gossos»).
5. **The real-core probe (spec (d), `e4-core-run.json`):**
   - **`windowlessDraft`:**
     - the seminar is created with `date`, `startTime` and `endTime` all `null`;
     - after [PUBLICA] with Central and nothing else, no `ring-conflicts` request is made;
     - the core answers `422 ACTIVITY_INCOMPLETE` with `fieldErrors` `[date, registrationFrom, registrationTo, startTime, endTime]`, each `REQUIRED`;
     - D7 shows «Cal per publicar» under the five fields (`D7-publica-sense-hores-core-1280.png`);
     - the preview itself, asked with the admin's own bearer (read from the `PATCH` request, never written), answers `400 INVALID_TIME_RANGE` with `details: {}`.
   - **`narrowWindow`:** on the saved 18:30–20:30 draft, a `PATCH` of `ringBlockWindow {18:30, 19:30}` is accepted (200, the draft never blocks). The preview then answers `400 INVALID_TIME_RANGE`. The window is set back to `null` and D7 is reopened on the new version.

#### Step → test (round 2; "failed before" = logs `25` and `26`, the final tests run on the HEAD sources)
| Point | Test (file › describe › it) | Failed before the fix |
|---|---|---|
| 1 mock | `activity-handlers.test.ts` › «E4-W11 T-07-04 R-07-05…» › «E4-W14 S07 §6 the ring-conflicts preview refuses the window…» (the `:800` case now expects 400) | yes |
| 1 mock | same describe › «E4-W14 round 2 R-07-04 R-07-05 T-07-04 a draft with rings but no hours: the preview is 400 INVALID_TIME_RANGE (RingBlockWindow.of), the publication 422 ACTIVITY_INCOMPLETE on every empty field» | yes |
| 1 mock | same › «E4-W14 round 2 R-07-01 R-07-04 a new draft has no date nor hours, as the core's create answers…» | yes (`RangeError`: the old fixtures could not read a `null` date) |
| 1 mock | same › «E4-W14 round 2 R-07-04 a published activity with rings stays publishable: a PATCH that clears a time or the registration period is 422 ACTIVITY_INCOMPLETE and changes nothing» | yes |
| 1 UI | `ActivitiesPage.test.tsx` › «T-07-29 E4-W14 D7 follow-ups of the E4-W11 review» › «E4-W14 round 2 R-07-04 R-07-05 T-07-04 a draft with rings but no hours skips the preview…» (5 fields marked, no preview request, one POST with a key) | yes |
| 1 UI | same › «E4-W14 round 2 R-07-04 the rings the admin just picked count: [PUBLICA] saves them, then asks the publication, not the preview» | yes |
| 1 e2e | `e2e/core/e4-core.spec.ts` (d): `windowlessDraft` and `narrowWindow` against the core | — (real-core e2e, log `29`) |
| 2 | `TemplatesPage.test.tsx` › «T-06-26 E4-W14 round 2 D3 the queue drops only the removed or refused class's changes (review #2)» › «class B's chip change, queued behind class A's removal, is saved with the template's new version» | yes |
| 2 | same › «class B's change queued behind class A's refused PATCH is sent; A's error goes to the page, since A's form is gone» | yes |
| 2 | same › «a change of class A queued after A's own removal is dropped with a message: the class no longer exists» | yes |
| 3 | `e4-core.spec.ts` `expectGoneFrom03` (ca and es) | — (real-core e2e: `mine03AfterCancelCa/Es = []`) |
| #4 | `SettingsPage.test.tsx` renamed «S05 §12 B32 the mock levels include «Pendent»…» | — (a name) |
| #5 | `apps/clubs/e2e/activities.spec.ts` renamed «04: «{hh:mm}–{hh:mm}» for the activities with both hours» | — (a name) |
| #6 | `TemplatesPage.test.tsx` › «R-06-02 a failed «Treu de la plantilla» keeps the change queued after it…» + `findByText` of the DELETE's error | no: the card already showed it; the assertion was missing |
| #7 | `SettingsPage.test.tsx` › «S05 §2 the help sits inside the «Nivells» card, and «Nou nivell» keeps its icon inline» (the rule in `components.css`, no app copy) | yes |
| #8 | `TemplatesPage.test.tsx` › «T-06-26 E4-W14 D3 coverage…» › «R-06-06 T-06-05 E4-W14 round 2 (review nit #8) a level put into the progression gets its row: no dogs yet, so «sense gossos» and no ratios» | yes |

Log `25`: api-client, 4 of 4 selected failed. Log `26`: clubs-admin, 7 of 8 selected failed; the passing one is #6. Each log's first line is its command.

#### Evidence (final runs on the final tree)

**1. `pnpm turbo run lint typecheck test build --force > roadmap/evidence/E4-W14/27-turbo-r2-final.log 2>&1`**: exit **0**.
- 34/34 tasks, 0 cached.
- Vitest per package: course-core 342, ui 69, api-client 221, i18n 80, shared-types 21, auth 17, id 9, clubs 225, clubs-admin 331.
- Last 40 lines, ANSI colours removed:
```
clubs-admin:test:      ✓ R-06-02 «Esborra la franja» (removeBand) waits in the queue for the level PATCH before it; then the drawer closes 329ms
clubs-admin:test:      ✓ R-06-02 a band removal never drops a class change queued after it: the PATCH is sent once the DELETE answers 348ms
clubs-admin:test:    ✓ T-06-26 E4-W14 round 2 D3 the queue drops only the removed or refused class's changes (review #2) (3)
clubs-admin:test:      ✓ R-06-02 class B's chip change, queued behind class A's removal, is saved with the template's new version 373ms
clubs-admin:test:      ✓ R-06-02 class B's change queued behind class A's refused PATCH is sent; A's error goes to the page, since A's form is gone 354ms
clubs-admin:test:    ✓ T-06-26 E4-W10 D3 closed days (S06 R-06-01, S02 R-02-09) (2)
clubs-admin:test:      ✓ R-06-01 R-02-09 a template whose kind has a closed day takes no band: the drawer says which day and sends nothing; the Saturday template still takes one 349ms
clubs-admin:test:  ✓ src/activities/ActivitiesPage.test.tsx (46 tests) 9008ms
clubs-admin:test:    ✓ T-07-29 D7 activities (list, maintenance, publication, cancellation) (21)
clubs-admin:test:      ✓ draws the four mockup rows with the R-07-13 dates, rings, registrations and state chips 681ms
clubs-admin:test:      ✓ «Nova activitat» creates a draft with title and type and opens its maintenance 704ms
clubs-admin:test:      ✓ shows the mockup maintenance of «Torneig d'Estiu 2026» with the ring, level and waitlist chips 354ms
clubs-admin:test:      ✓ R-07-14 hides «Nivells» with levels.enabled=false and «Llista d'espera» without WAITLIST 353ms
clubs-admin:test:      ✓ R-07-05 [PUBLICA] with conflicts opens the dialog with the conflicts and the required notice text 315ms
clubs-admin:test:      ✓ R-07-04/05 save mode: a resync conflict opens «DESA I APLICA»; STALE_VERSION closes it, refetches and says so 303ms
clubs-admin:test:    ✓ T-07-29 E4-W08 D7 follow-ups of the E4-W04 round-2 review (6)
clubs-admin:test:      ✓ R-07-05 away from the club the hours cover the whole day: 6:00 when the club opens at 7:00 425ms
clubs-admin:test:    ✓ T-07-29 E4-W10 D7 follow-ups of the E4-W08 and E4-W09 reviews (3)
clubs-admin:test:      ✓ S07 §6 CONVENCIONS_API §4 the member filter reads every page: with 1,001 distinct registrants the member on the second page can be selected 677ms
clubs-admin:test:  ✓ src/planning/CalendarPage.test.tsx (41 tests) 9390ms
clubs-admin:test:    ✓ E3-W07 step 9 a D1 risk row opens D4 on its class (1)
clubs-admin:test:      ✓ selects the class of `?classe=` in the week of `?setmana=` 645ms
clubs-admin:test:    ✓ T-06-28 D4 / D4b / D4c class calendar (front half, MSW) (24)
clubs-admin:test:      ✓ opens «Esborrany» on the first week with drafts, with the validation card and its count 817ms
clubs-admin:test:      ✓ [VALIDAR LA SETMANA] turns every draft of the week active at once 477ms
clubs-admin:test:      ✓ cancels an active class with registrants through D4c: four rows, text required, «ANUL·LA I AVISA ELS 4 ALUMNES» 477ms
clubs-admin:test:      ✓ [ELIMINA] of an active class goes through the same D4c modal 394ms
clubs-admin:test:      ✓ R-06-09 saves the diff with the version; STALE_VERSION keeps its message after the refetch remounts the card 548ms
clubs-admin:test:      ✓ R-06-09 [Crear classe]: a new time or ring after RING_HAS_BOOKINGS drops the confirmation 306ms
clubs-admin:test: 
clubs-admin:test:  Test Files  20 passed (20)
clubs-admin:test:       Tests  331 passed (331)
clubs-admin:test:    Start at  16:34:50
clubs-admin:test:    Duration  14.52s (tests 43%, environment 28%, transform 17%, setup 6%, import 6%)
clubs-admin:test: 

 Tasks:    34 successful, 34 total
Cached:    0 cached, 34 total
  Time:    24.489s
```

**2. `pnpm i18n:check > roadmap/evidence/E4-W14/28-i18n-check-r2-final.log 2>&1`**: exit **0**. Complete output (no locale change this round):
```
> agilityhub-core-web@0.0.0 i18n:check /Users/jordib/dev/agilityhub/agilityhub-core-web
> node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs && node scripts/check-parameter-keys.mjs

i18n check passed
ca: 2495 keys
es: 2495 keys
en: 2495 keys
1873 statically used keys, 0 unused
250 codes, 0 missing
151 catalog parameters (119 in D11, 32 system-only), 10 blocks, 0 missing
```

**3. `…/roadmap-kit/mac/heavy.sh pnpm e2e:core E4-W14 > roadmap/evidence/E4-W14/29-e2e-core-r2-final.log 2>&1`**: exit **0**.
- **Host lock:** the log has no waiting line.
- **Core image:** `ghcr.io/jboixtcm/agilityhub-core-api:main`, `sha256:eff1e850ae16…`, revision label `e9a58c15a035f6acc7f029d388f3537cdccf1761`, created 2026-09-26T13:47Z (`docker image inspect`).
- **Results:** e1–e2 12 passed, e3 5 passed, e4 7 passed.
- **`E4_WEEK_START`:** 2026-09-21.
- Last 40 lines, lines cut at 260 characters:
```
+ eslint-import-resolver-typescript 4.4.5
+ eslint-plugin-import 2.32.0
+ eslint-plugin-jsx-a11y 6.10.2
+ eslint-plugin-react 7.37.5
+ eslint-plugin-react-hooks 7.1.1
+ globals 17.12.0
+ husky 9.1.7
+ i18next-parser 9.4.0
+ lint-staged 17.5.0
+ msw 2.15.0
+ prettier 3.9.6
+ size-limit 13.0.3
+ turbo 2.10.12
+ typescript 5.9.3
+ typescript-eslint 8.69.0

. prepare$ node packages/config/scripts/prepare-husky.mjs
. prepare: .git can't be found
. prepare: Done
Done in 4s using pnpm v9.15.9
[WebServer] (node:134) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)


[WebServer] (node:214) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
[WebServer] (node:312) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)


Running 7 tests using 1 worker

[1/7] e2e/core/e4-core.spec.ts:482:5 › T-06-28 E2E (a) D3: the three seeded templates, «Setmana B» blocked by its inconsistency, «Setmana A» generates the proposed week, D3b per ring
[2/7] e2e/core/e4-core.spec.ts:617:5 › T-06-28 E2E (b) D4: «Esborrany» opens the seeded draft week, [VALIDAR LA SETMANA], the D4c modal with the 4 registrants (200), «Anul·lades», ring blocks (409 then 201)
[3/7] e2e/core/e4-core.spec.ts:767:5 › T-06-28 E2E (c) screens 10 and 23: the member never sees the cancelled class nor counts, the validated week is visible; the instructor sees the block, the dimmed class and n/n
[4/7] e2e/core/e4-core.spec.ts:822:5 › T-07-32 E2E (d) D7: the four seeded activities, the Torneig blocks every ring on D4, a new seminar published over a class with the conflict dialog; D7 exports against the core
[5/7] e2e/core/e4-core.spec.ts:1067:5 › T-07-32 E2E (e) ca: 04 → detail → register → 03 → cancel in time; the waitlist of the full «Seminari de handling» and the FIFO promotion when the admin adds a place
[6/7] e2e/core/e4-core.spec.ts:1229:5 › T-07-32 E2E (e) es: the same member flow with the es literals, a second FIFO promotion, then the admin cancels the seminar (200) and the registrant reads «cancel·lada pel club»
[7/7] e2e/core/e4-core.spec.ts:1399:5 › E4-W05 step 7 · the «Excel · PDF» menus of D5 and D7 with EXPORT_TOO_LARGE and while an export is pending
  7 passed (17.5s)
```
What `e4-core-run.json` recorded in this run:
- `windowlessDraft.created` = `{date: null, endTime: null, startTime: null}`.
- `windowlessDraft.preview` = `400 {code: INVALID_TIME_RANGE, details: {}}`.
- `windowlessDraft.publication` = `422 ACTIVITY_INCOMPLETE`, with `fieldErrors` `[date, registrationFrom, registrationTo, startTime, endTime]`, each `REQUIRED`.
- `narrowWindow` = `{patch: 200, preview: 400 INVALID_TIME_RANGE}`.
- `mine03AfterCancelCa` = `[]`, `mine03AfterCancelEs` = `[]`.
- `generation.body` carries «Dissabtes» and «Setmana A».
- `tournamentSaturday.cellsByColumn` = `{"ds 10": 1}`.
- `waitlistChipEs` = «en lista de espera (3)».

**4. `…/roadmap-kit/mac/heavy.sh pnpm e2e:docker E4-W14 > roadmap/evidence/E4-W14/30-e2e-docker-r2-final.log 2>&1`**: exit **0**.
- The three projects in the Playwright image: clubs-admin 48 passed, id 1 passed, clubs 42 passed.
- The log has no host-lock waiting line.
- Last 40 lines, ANSI colours removed:
```
clubs-admin:e2e: 
clubs-admin:e2e: > clubs-admin@0.0.0 e2e /work/apps/clubs-admin
clubs-admin:e2e: > playwright test
clubs-admin:e2e: 
clubs-admin:e2e: [WebServer] (node:664) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs-admin:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs-admin:e2e: 
clubs-admin:e2e: Running 48 tests using 9 workers
clubs-admin:e2e: ················································
clubs-admin:e2e:   48 passed (29.3s)
id:e2e: cache bypass, force executing 21e2ca9eab1f12d3
id:e2e: 
id:e2e: > id@0.0.0 e2e /work/apps/id
id:e2e: > playwright test
id:e2e: 
id:e2e: 
id:e2e: Running 1 test using 1 worker
id:e2e: ·
id:e2e:   1 passed (3.2s)
clubs:e2e: cache bypass, force executing 632b68caf4f54017
clubs:e2e: 
clubs:e2e: > clubs@0.0.0 e2e /work/apps/clubs
clubs:e2e: > playwright test
clubs:e2e: 
clubs:e2e: [WebServer] (node:3168) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs:e2e: 
clubs:e2e: Running 42 tests using 8 workers
clubs:e2e: ··········································
clubs:e2e:   42 passed (18.4s)

 Tasks:    8 successful, 8 total
Cached:    0 cached, 8 total
  Time:    1m3.346s 

evidence: E4-W14/23-calaix-instructor-375.png
evidence: E4-W14/D4-diumenge-tancat-xips-1280.png
evidence: E4-W14/04-activitats-hores-375.png
evidence: E4-W14/D7-publica-6h-camps-1280.png
evidence: E4-W14/D11-nivells-1280.png
```

**Earlier runs this round (one line each):**
- `18-vitest-before-fix-r2-api-client.log`, `19-vitest-before-fix-r2-clubs-admin.log` and `21-…-api-client-final.log`: the before-fix runs of earlier versions of the round-2 tests. They were superseded by `25` and `26` once the null-date tests were added.
- `20-turbo-r2-typecheck-fail.log`: exit 2. The new mock test read `details.fieldErrors` from an `unknown`. Rewritten as `toEqual` on the whole error.
- `22-turbo-r2.log` (exit 0, 34/34) and `23-i18n-check-r2.log` (exit 0): superseded by `27` and `28`, because the null-date change came after them.
- `24-e2e-core-r2.log`: exit 0, 12 + 5 + 7. It ran before the spec asserted the five fields and the `null` date. Its `e4-core-run.json`, overwritten by `29`, recorded the same core answers.

#### Screenshots
- `D7-publica-sense-hores-core-1280.png` (new, real core): «Falten dades…» and «Cal per publicar» under Data, Hora d'inici, Hora de final and both registration dates; the undated draft reads «—» in the list.
- The five E4-W14 MSW screenshots and every `*-core-*.png` were refreshed by runs `29` and `30`. I checked `D11-nivells-1280.png` («+ Nou nivell» on one line, now from the design-system rule) and `19-payment-core-375.png` (the buttons at 375 after the global rule).

#### Files changed (round 2)
- **D7:**
  - `apps/clubs-admin/src/activities/ActivityPage.tsx` (`save` returns the saved activity; `startPublish`) and `ActivitiesPage.test.tsx`;
  - the mocks `packages/api-client/src/mocks/activity-handlers.ts` and `fixtures/activities.ts`, and `activity-handlers.test.ts`.
- **D3:** `apps/clubs-admin/src/planning/TemplatesPage.tsx`, `ClassForm.tsx`, `shared.ts` (`DroppedChangeError.refusal`), `TemplatesPage.test.tsx`; the mock `planning-handlers.ts` (`/coverage`).
- **Design system:** `packages/ui/src/components.css`; the copies removed from `apps/clubs-admin/src/styles.css`, `planning/planning.css` and `activities/activities.css`; `catalogs/SettingsPage.test.tsx`.
- **E2E:** `apps/clubs/e2e/activities.spec.ts` (renamed test), `e2e/core/e4-core.spec.ts`.
- **Other:** `CHANGELOG.md`, this report, evidence `18`–`30` plus the refreshed screenshots and JSON.

#### Assumptions
1. **Order of the field errors:** the core's order is date, `registrationFrom`, `registrationTo`, `startTime`, `endTime`. `title` stays first in the mock; the core cannot be asked, because its create requires a title.
2. **The removed class's message:** a change dropped behind its class's removal shows `errors:NOT_FOUND`, an existing catalog message. No new copy was added.
3. **Stale version:** a stale-version refusal keeps the existing warning tone, and the recovery remounts the card. The page toast is then the same message, not a second one.

#### Questions / catalog proposals
- **@organizer, the «Lliga social» mock fixture:** it is `PUBLISHED`, has every ring, and has no `endTime`. It copies the D7 mockup row «ds 19/09 · 9:00». The api cannot produce that state (R-07-04 requires hours with rings; the core's seed gives it 9:00–14:00). With this round's `PATCH` rule, the mock refuses any edit of it with `ACTIVITY_INCOMPLETE` (endTime). No test edits it. **Assumption meanwhile:** kept as the mockup shows it. **Proposal:** `endTime: "14:00"`, as the core seed has it, if api validity weighs more than the mockup's «9:00».
- No new parameter, event, notification, error code or literal.
- **Denied commands this session (all had alternatives):**
  - reading `../agilityhub-core-api` and `heavy.sh` (outside the repo): I used the organizer's description of `RingBlockWindow.of` and the core's own answers instead;
  - `sed -i`, and `for`/`case` loops;
  - `cd … && … > log`: replaced by `pnpm -C <dir> …`;
  - writing to `/tmp`: replaced by `tmp.local/`;
  - a heredoc with braces.

## Organizer verification
Result: changes_requested (26-09-2026, organizer).

- **Review:** the automatic review did not run, because the session limit cut it after the publish. The organizer had a separate reviewer session do it: `roadmap/reviews/E4-W14-20260926-1550-claude.md` (commits `d98a0a8` and `8192323`). It found needs changes: one major, two minors and six nits.
- **CI:** `8192323` is green (run 36227976412).
- **Evidence:** it matches the report:
  - turbo 34/34 tasks, 0 cached;
  - `i18n:check` passes;
  - the real core 12 + 5 + 7 passed, and Docker 33 + 47 + 1;
  - the before-fix logs fail as the report says.
- **Confirmed by the organizer against the api (review #1):**
  - `RingBlockWindow.of` (api `activities/domain/RingBlockWindow.java:11`) answers `400 INVALID_TIME_RANGE` for an activity with rings and no date, start or end. It answers the same when the window does not contain the activity's hours.
  - The mock's `ringBlockWindowProblem` answers nothing in both cases, and the new step-1 test pins a 200 for the 10:00–10:30 window over a 10:00–12:00 activity.
  - On the real core, a draft with rings and no hours therefore never reaches `ACTIVITY_INCOMPLETE`.

### Round 2: fix these (each with a test that fails before the fix)
1. **Review #1 (major; step 1, R-07-05, S07 §6, T-07-04): the preview's 400, as the api answers it.**
   - The mock `ringBlockWindowProblem` mirrors `RingBlockWindow.of`. With rings, a missing date, start or end answers 400 `INVALID_TIME_RANGE`. So does a `ringBlockWindow` that starts after `startTime` or ends before `endTime`.
   - The test at `activity-handlers.test.ts:800` expects that 400.
   - `startPublish`: a draft with rings but without a date or hours skips the preview and goes straight to `POST …/publication`. Then `422 ACTIVITY_INCOMPLETE` marks every empty field («Cal per publicar»), as R-07-04 asks. Vitest.
   - Like the api (`ActivityService` validates a published activity as a publication), the mock refuses a `PATCH` that clears the date or a time of a `PUBLISHED` activity with rings: `422 ACTIVITY_INCOMPLETE`.
2. **Review #2 (minor; step 7): the D3 queue drops only what it must.**
   - A class `DELETE` drops only the queued changes of that class, or of a class whose change was refused.
   - The other queued changes are sent, after the template version is read again, as `removeBand` does.
   - A dropped change that has no message on screen shows one.
   - Vitest: class B's chip change, queued behind class A's removal, is saved.
3. **Review #3 (minor; step 9): the 03 checks must be able to fail.** Fix both checks: es (`e4-core.spec.ts:1108`) and ca (`:963`). Either:
   - assert on the answer of `GET /me/activities`: no `ACTIVE` or `WAITLISTED` entry for the seminar; or
   - wait for 03's list to render before counting 0.
4. **Nits #4–#8:**
   - #4: the «Pendent» test name drops R-06-06 (AGENTS rule 5).
   - #5: the 04 e2e asserts a row without an end («· 9:00», with a mock activity without `endTime`), or it is renamed.
   - #6: the D3 removal test asserts the `DELETE`'s error it mentions.
   - #7: the inline-icon rule of `.ah-button__content` moves into the design system (`packages/ui/src/components.css`), and the four per-page copies go.
   - #8: the mock `/coverage` gives every active progression level a row: `NO_DOGS` when it has no figures, as R-06-06 and T-06-05 say.
- **Nit #9: no action.** The api's `seed:demo` refuses unknown options (`DemoSeedCommand.java:29`), so a `--week-start` that is passed is used.
- **Verification:** as in the task. Round 2's report goes under «### Round 2 report».

### Round 2 verification
Result: verified (26-09-2026, organizer).

- **Review:** `roadmap/reviews/E4-W14-20260926-1726-claude.md` (commit `60129ee`): minor issues, no major. It found two minors and two nits.
  - The four round-2 points and nits #4–#8 are fixed, and each has a test.
  - The before-fix logs `25` and `26` fail as reported: 4 of 4, and 7 of 8. The one that passes is #6, a test-only item.
- **CI:** `60129ee` is green (run on 26-09 15:22 UTC).
- **Evidence:** turbo 34/34, `i18n:check`, the real core 12 + 5 + 7, and Docker 48 + 1 + 42.
- **The real core confirms the major:** `D7-publica-sense-hores-core-1280.png` and `e4-core-run.json` (`windowlessDraft`, `narrowWindow`). An undated draft with rings now reaches `ACTIVITY_INCOMPLETE`, as the mock does.
- **Routing:** the review's points go to the new web task **E4-W16**, together with the round-2 review of E4-W13.
  - #1 (minor): [DESA] on a published activity marks only the first `ACTIVITY_INCOMPLETE` field.
  - #2 (minor): the mock «Lliga social» is published without `endTime`. Decision: `endTime: "14:00"`, as the core's seed has.
  - #3 (nit): a `{date: null}` case.
  - #4 (nit): the windowless publish path never publishes without the confirmation.
