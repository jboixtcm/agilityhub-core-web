---
id: E2-W05
title: D11 parameters generated from the catalog: blocks, typed drawers, history, modules, opening hours, holidays, club pages
stage: E2
repo: agilityhub-core-web
thread: B
order: 50
status: not_open
depends_on: [E2-W04, external:api:E2-T02]
branch: 
pr: 
updated: 2026-09-06
---

# E2-W05 · D11 parameters generated from the catalog: blocks, typed drawers, history, modules, opening hours, holidays, club pages

## Objective
Work package WP-02-D: the settings screen is generated from `ParameterCatalog`, so a new parameter never needs UI work.

## Spec references
S02 §2 (D11), R-02-04, R-02-07, R-02-09, R-02-13 · tests **T-02-13, T-02-17** · `docs/pantalles/escriptori/D11-*.html|png` · `docs/DECISIONS_PENDENTS.md` A18a (jobs card placeholder), A23 (self-service modules), Part C S02.

## Context to load
S02 §2, §4, §11 · the D11 mockup · `GET /parameters` shape (E2-T02) · this file.

## Steps
1. `/parametres`: blocks and rows generated from the catalog (labels/help via i18n keys `admin-settings:param.{key}.label/help` — keys for every catalog key in three locales, checked by `i18n:check`), row → drawer with the control for its type (int, duration, time, bool, enum, money, localizedText with per-locale tabs, json with validation), «Restableix» to default, history list, «S'aplica a partir d'ara» help, rows of inactive modules hidden, header «últim canvi» from `lastChange`.
2. Modules card (self-service switches only; dependency errors shown), opening hours editor, holidays list (date + label), «Pàgines del club» (normes/FAQ links to their editors), «Processos automàtics» card placeholder (E5).
3. Duration formatting («2 h abans», «2 h antes», «2 h before») per T-02-17.

## Verification (run all; paste full output in the report)
- `pnpm turbo run lint typecheck test build` + `pnpm i18n:check` (every parameter key has label/help in ca/es/en); Playwright T-02-13, 17; screenshots at 1280 px next to D11.

## Definition of Done
Generated screen; adding a catalog key needs only i18n labels.

## Evidence to return
Outputs and screenshots.

---

## Executor report
_(not started)_

### Branch / commits
### Evidence (full command outputs)
### Files changed
### Rules and tests implemented (R-xx-nn / T-xx-nn)
### Assumptions
### Questions / catalog proposals

## Organizer verification
_(pending)_
