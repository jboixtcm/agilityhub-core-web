---
id: E3-W08
title: Gate E3 audit fixes (web, 3/3) — the payment card of 19 from the per-plan quote (total, first-month labels, packs), signed uploads in signup and add-dog, pending dogs on screen 13 (E36), the signup flags, the 16/17 details, and the footer with the tax id
stage: E3
repo: agilityhub-core-web
thread: B
order: 80
status: verified
depends_on: [E3-W07, external:api:E3-T08, external:api:E3-T09, external:api:E3-T10, external:api:E3-T12]
branch:
pr:
updated: 2026-09-25
---

# E3-W08 · Gate E3 audit fixes (web, 3/3)

## Objective
The last web fix task of the gate E3 audit (`roadmap/reviews/gate-E3/consolidated.md`). It covers the public-app findings that need the api contract of E3-T08 and E3-T09:
- the per-plan quote of `GET /signup`;
- the upload `headers`;
- the `status` of `/me/dogs`;
- the two signup flags.

E3-W07 has already adopted that snapshot. The organizer opens this task after E3-W07.

**Each fix comes with a test that fails before the fix.** Give it the spec id where one applies (T-04-30…32).

## Spec references
- S04:
  - §2, rows 17, 19 and 13;
  - R-04-08, R-04-12, R-04-14/15 and R-04-25;
  - §10: `signup:step4.total` and `signup:step4.firstMonth.todayFull|todayHalf|splitDayHalf|nextMonthFull`;
  - T-04-30…32.
- S03: row 13 and R-03-02 (amended by **E36**).
- Mockups `docs/pantalles/mobil/13`, `17` and `19`.

## Context to load
- `roadmap/reviews/gate-E3/consolidated.md` (M3, M4, M5, M16 and M20), and the source reports.
- `apps/clubs/src/SignupPage.tsx` (the «Pagament inicial» card at `:1517-1573`; the upload at `:879-883`) and `SelfServicePages.tsx` (screen 13).

## Steps
0. **A newer snapshot.** If `packages/api-client/openapi/openapi.next.json` is staged (api E3-T09 round 2 or E3-T10 published after E3-W07 adopted its snapshot), adopt it first, as E3-W07 step 0 did, and list the differences in the report. What api E3-T10 changes:
   - D1: `ClassOccupancyKpi.percent` is an integer, `waitingTotal` is nullable, and `RiskNotified.gender` is nullable. Nullable enums now carry `null` in their `enum`, across the snapshot (E3-T10 round 2);
   - `ClubSummary.legalName` and `taxId` are new, for the public footer of step 5;
   - for an add-dog, D2's `signup` is the pending dog's own submission (source `APP_ADD_DOG`, its date and locale);
   - the S04 error lists are complete.
   And api E3-T12 publishes `SignupUpfrontReview.firstMonth {option, portion, startDate, amountDue}`: prune it from `pending.json`, and check E3-W07's D2 first-month line («… (mitja quota)») against the real core, which E3-W07 left deferred.
1. **M3, M4, M5 · the «Pagament inicial» card from the selected plan's quote (R-04-14/15).** Today the card shows the options of the first monthly plan for every plan, labels that are right only from day 16 on, «Entrada 0,00 €», and no total.
   - Render the quote of the **selected** plan (api E3-T08), never a quote computed in the web:
     - `MONTHLY`: the entry fee only if > 0, plus the two first-month options;
     - `PACK`: the pack line, plus the entry fee only if > 0;
     - `SINGLE_CLASS`: the entry fee only if > 0;
     - zero lines are hidden.
   - **The labels** come from each option's `option` and `portion`:

     | `option` | `portion` | Key |
     |---|---|---|
     | TODAY | FULL | `todayFull` |
     | TODAY | HALF | `todayHalf` |
     | ALTERNATIVE | HALF | `splitDayHalf` |
     | ALTERNATIVE | FULL | `nextMonthFull` |

     Use ca, es and en. Add-dog: TODAY (this month, with the additional fee) and ALTERNATIVE («alta l'1 de {mes següent}», the entry fee only). Use the §10 keys; if §10 has none for add-dog, propose them in the report.
   - **«Total a pagar al club»** = the `totalDue` of the selected option, or of the plan. It is display-only and never sent (T-04-32).
   
   Vitest, with the club clock:
   - 05-08-2026: «Alta avui … (mes complet)» 60 € and «Alta el dia 16 … (mig mes)» 30 €;
   - 17-08-2026: 30 € / 60 €, and a total of 130 € with the 100 € entry fee;
   - 31-12-2026: «Alta l'1 de gener (mes complet)»;
   - Pack 6: 135 € and no options;
   - Teràpia: 50 €;
   - add-dog on day 26: TODAY only.
2. **M16, web side · signed uploads (R-04-08).** The signup and add-dog uploads send only `Content-Type` (`SignupPage.tsx:879-883`). Forward every header of the upload-URL response (api E3-T09 returns `headers` with `If-None-Match: *`), as the other uploaders already do. Vitest: the PUT carries the returned headers. The real-core e2e keeps passing, because local storage ignores the header.
3. **M20, web side · pending dogs on screen 13 (E36).**
   - Screen 13 renders the `PENDING` dogs of `GET /me/dogs` with the chip «pendent de validació». They get no actions: no notes, tasks, documents or photo.
   - Add the key in ca, es and en.
   
   Vitest, and a real-core e2e: after an add-dog, screen 13 shows the dog as pending; after the validation, as active.
4. **The signup flags (R-04-08, R-04-12; T-04-30/31).**
   - «Deixa-ho pendent» is offered only when `allowFamilyGroupPending` is true.
   - With `requireDogDocumentAtSignup`, the vaccination card is required on 17, with an actionable error.
   
   Vitest for both values of each flag.
5. **Screens 16 and 17, and the public shell** (E3-W05 `screens.md`: S2, S3, 16 b–c, 17 a–d):
   - 17: each plan card shows the name, `conditions` on one line, and the price. It does not repeat the long `description`, which the step's intro texts already carry. A plan without an entry fee shows no entry line.
   - 16: when the postal code has several towns, «Població» is a selector (R-04-03; T-04-03 has zero, one and many). The «Ja ets soci…» card carries its ⓘ icon.
   - The public footer reads «{name} · {taxId} · {city}», from `/branding` (api E3-T10 step 12), as in the mockups.
   - The language selector has the globe icon.
   - **Dark text on the primary buttons (decision A32, Jordi 24-09).** The api seed sets `onPrimary: #0B0B0B` (api E3-T10 step 14). Set the same in `packages/api-client/src/mocks/fixtures/branding-canic.json`. Check that no component hard-codes white on the primary colour: every primary button reads `--color-on-primary`. The `BrandingProvider` contrast warning must be gone from the Vitest output.
6. **Readmission on D2 (E38; moved from E3-W07).** D2 shows the badge «Readmissió» and, for each changed field, the old value and the new one, from the api's current and submitted blocks (api E3-T09). While a readmission is pending, the DNI is read-only (the api answers 409 `INVALID_STATE` with `details.reason = READMISSION_PENDING`, S04 R-04-06): say «per corregir-lo, rebutja la readmissió» (propose the literal). Vitest. If the demo seed has a fictional `LEFT` member, add a real-core e2e: readmit them, reject the readmission, and check that D10 shows the original data. If the seed has none, say so; Vitest is then enough.
7. **From the E3-W06 round-2 review** (`roadmap/reviews/E3-W06-20260924-1936-codex.md`, confirmed by the organizer):
   - **The routed error survives a slow navigation.** `navigateTo()` sets React's path before `location.assign()`, so the departing document can render the destination step and consume `pendingError` (`SignupPage.tsx:2481`, `:332`). Do not render or consume the destination's error in the departing document. Add a regression test with a delayed navigation.
   - **A cancelled checkout is consumed once.** Every reload of `?cs=cancel` deletes the checkout key, including the key of a later retry (`:2394`). Tie the cancellation to the attempt it cancelled, or consume the marker once. Test across remounts: cancel → retry → lost answer → reload → retry with the same key.
   - **A passport-only applicant's `INVALID_ID_DOCUMENT`** goes to the passport field, with its `aria-describedby`, not to the empty DNI/NIE field (`:913`, `:931`). Test it.
8. **Re-shoot** at 375 with `pnpm e2e:core E3-W08`, and compare each screen with its mockup in the report:
   - 19: monthly on a day before 16 and on a day from 16 on, and Pack 6;
   - add-dog 19;
   - 17 with the document required;
   - 13 with a pending dog;
   - 16 and 17, with the footer;
   - D2 with a readmission, at 1280.

## Fixed conventions
- Design system only.
- Keys are never literals; ca, es and en are added together.
- Evidence logs are committed, including one log per failed attempt.
- Fictional data only.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E3-W08/*.log`)
- `pnpm turbo run lint typecheck test build`.
- `pnpm i18n:check`.
- `pnpm e2e:core E3-W08`.
- The complete `pnpm e2e` in the Playwright Docker image.

## Definition of Done
- Screen 19 shows the selected plan's lines, the right labels on every day of the month, and the total.
- Uploads forward the signed headers.
- Screen 13 shows pending dogs.
- Both flags are enforced.
- 16, 17 and the public footer match their mockups.
- D2 shows a readmission's old and new values.

## Evidence to return
- The command outputs.
- A step → test table.
- The screenshots, with the per-screen comparison lines.

---

## Executor report

**Session 25-09 (claude-opus-5-5).** All steps 0–8 done; the four Verification commands pass on the final tree. The previous session had only adopted the snapshot (commit `1a6b15d`) and left no report; this session continued from there.

### Summary
- **Step 0 (snapshot, adopted by the previous session in `1a6b15d`; checked here).** Staged snapshot `9597de2` → `openapi.json`, `pending.json` pruned of `SignupUpfrontReview.firstMonth`, client regenerated. Differences against the E3-W07 snapshot (`e569f15`):
  - new `SignupFirstMonth {amountDue, option, portion: FULL|HALF|null, startDate}`, referenced by `SignupUpfrontReview.firstMonth` (api E3-T12). `portion` is **nullable** in the published contract (mocks-first had it required);
  - `null` added to the `enum` of every nullable enum (E3-T10 round 2): `ActivityRegistrationListItem.cancelReason`, `gender` (4 schemas), `notBookableReason`, `skipReason` (2), `dayOfWeek`, `module`, `reason`, `TrainingBooking.cancelReason/cancelledBy`, the class booking `cancelReason`;
  - descriptions only: `PATCH /members/{id}` (E38 readmission: `409 INVALID_STATE` `READMISSION_PENDING`), `POST /members/{id}/validation` (a readmitted member keeps its account), `POST /signup/identity-checks` (recipient cap parameter, a pending readmission matched on its address);
  - error lists: `POST /me/dogs/{id}/documents` 400/422, `PATCH /members/{id}` 400/409/422.
  - The D2 first-month line handles `portion: null` (ICU `other` → no «(mitja quota)»; Vitest), and on the real core D2 reads «setembre 30,00 € (mitja quota)» for `{TODAY, HALF, 2026-09-25}` (`d2-first-month-core.json`; asserted in `e3-signup.spec.ts`).
- **Step 1 (M3, M4, M5).** 19 renders `config.upfront.planQuotes` of the selected plan (never a web computation): lines above zero (ENTRY_FEE «Entrada (1 gos)», PACK with the plan name), the start options with labels from `option`/`portion` (§10 keys `signup:step4.firstMonth.todayFull|todayHalf|splitDayHalf|nextMonthFull`; add-dog: proposed `signup:step4.additionalDog.today|nextMonth`), and «Total a pagar al club» (`signup:step4.total`) = the selected option's `totalDue`, else the plan's; display-only, never sent. An option the quote no longer offers (add-dog after day 25) falls back to the first, and the option sent is the one shown. On the real core the quotes match: Abonat `ENTRY_FEE 100` + `TODAY HALF 30 → 130` / `ALTERNATIVE FULL 01-10 60 → 160`; Pack 6 `PACK 135`; Teràpia `ENTRY_FEE 50` (`signup-quote-*-core.json`).
- **Step 2 (M16).** `SignupPage` uploads send `response.data.headers` unchanged (as `uploadFile` in 13 and the D2 drawer do). On the real core the PUT carried `If-None-Match: *` and `Content-Type: image/jpeg`, and the signup with that `fileKey` was accepted (`signup-upload-headers-core.json`).
- **Step 3 (M20, E36).** 13 renders `PENDING` dogs as a reduced card: paw placeholder, name, «{raça} · {sexe} · {n} anys», chip «pendent de validació» (`census:myDogs.pendingValidation`), no photo input, notes, tasks, documents, pack or licences.
- **Step 4.** «Deixa-ho pendent i continua ›» only with `allowFamilyGroupPending === true`; with `requireDogDocumentAtSignup === true` 17 hides the optional note and, without a file, stays with «Cal adjuntar la cartilla de vacunes per continuar: toca «Cartilla de vacunes» i puja'n una foto o un PDF.» on the file control (focus, `aria-invalid`, `aria-describedby`); the error clears after an upload.
- **Step 5.** Plan cards: name, `conditions` (one element; the long `description` is no longer rendered), price; no entry line when `entryFee` is absent or 0. 16: ⓘ icon in «Ja ets soci…»; globe icon in the language selector. Footer «{legalName} · {taxId} · {city}» from `/branding` (per the organizer's 25-09 note; falls back to «{name} · {city}» without a tax id). `branding-canic.json` `onPrimary` = `#0B0B0B` (A32): no `[BrandingProvider]` warning in the final turbo log (0 matches), and `packages/ui/src/on-primary.test.ts` fails if any CSS rule paints `--ah-color-primary` behind text without `color: var(--ah-color-primary-fg)` (the design system's onPrimary token; there is no `--color-on-primary` variable). The town selector for several towns already existed; T-04-03 (0/1/many) now has a test.
- **Step 6 (E38).** D2: badge «Readmissió» (existing) and, for each `readmission.changedFields` entry, «Abans: {LEFT record}» / «Ara: {submitted}»; the drawer's DNI/NIE is read-only with the hint «El DNI/NIE no es pot canviar durant una readmissió: per corregir-lo, rebutja la readmissió.», and a `409 INVALID_STATE {reason: READMISSION_PENDING}` shows the same text inside the drawer. **Real core:** the LEFT member the lookup found was «Pau Rebuig E3», rejected earlier in the same run (R-04-23 lets a rejected applicant apply again): readmitted with a new phone and e-mail, D2 showed both, the readmission was rejected, and `GET /members/{id}` + D10 show `LEFT` with the original phones and e-mails (`readmission-core.json`, `D2-readmission-core-1280.png`, `D10-after-rejected-readmission-core-1280.png`). Whether the demo seed itself has LEFT members was not established (the list returned this one).
- **Step 7.** (1) `navigateTo` sets the step only when the navigator moved the address (a same-document router); with the production `location.assign` the departing document shows «Carregant el formulari» and never renders or consumes the destination's routed error. (2) `?cs=cancel` is read once into state and removed from the address with `history.replaceState`, so a reload never deletes the key of a later checkout attempt. (3) A passport-only applicant's document error lands on the passport field (`aria-invalid`, `aria-describedby`, focus).
- **Mocks look like the api:** `GET /signup` computes `planQuotes` (+ deprecated `firstMonthOptions`, add-dog `additionalDogOptions`) for a club date (`setSignupMockToday`, scenario `signupEarlyMonth`), with the same shape the real core returned; both flags sent (`allowFamilyGroupPending` only with FAMILY_GROUP); upload URLs return `headers`; an add-dog adds a `PENDING` dog without documents/licences to `GET /me/dogs`; `SignupResult.upfront` follows the chosen option; `adminSignupReviewReadmission` models E38 with `READMISSION_PENDING`. Computed configs and results are validated against `SignupConfig`/`SignupUpfront` (`fixtures.contract.test.ts`).

### Step → test
| Step | Tests (Vitest unless noted) | Pre-fix |
|---|---|---|
| 0 | `SignupReviewPage.test.tsx` «E3-W08 step 0: a first month frozen without its portion…»; real-core first-month assertion | passes (guard for the nullable type) |
| 1 | `SignupPage.test.tsx` «E3-W08 step 1…»: 05-08, 17-08 (+ total never sent), 31-12, Pack 6/Teràpia, zero line hidden, add-dog day 26/17; `signup-handlers.test.ts` quote table; real-core `expectQuoteCard` (monthly, Pack 6, add-dog) | 5/6 fail; 31-12 passes (the old d ≥ 16 labels were right) |
| 2 | «E3-W08 step 2…» public + add-dog PUT headers; real-core upload | 2/2 fail |
| 3 | `App.test.tsx` «E36 (R-04-25)…»; real-core 13 pending → validated | fails |
| 4 | «T-04-31 allowFamilyGroupPending=false/true», «T-04-30 requireDogDocumentAtSignup=true/false»; real-core 17 | false/true: 2/4 fail (the two default-value cases are guards) |
| 5 | «17: each plan card…», «16 (R-04-02, T-04-03)…», «16: ⓘ … globe», «footer…», «A32…»; `on-primary.test.ts` | 4/5 fail (town selector already existed); CSS guard passes (no violation existed) |
| 6 | `SignupReviewPage.test.tsx` «E3-W08 step 6…» (5); mocked e2e `dashboard-signup.spec.ts`; real-core readmission | 4/5 fail (the non-readmission case is a guard) |
| 7 | «E3-W08 step 7…» #1 slow navigation, #2 cancel once across remounts, #3 passport field | 3/3 fail |

Pre-fix proof: the product files (`SignupPage.tsx`, `SelfServicePages.tsx`, `SignupReviewPage.tsx`, `SignupEditDrawer.tsx`, the locales, `branding-canic.json`) were temporarily replaced by their `HEAD` versions (new tests and api-like mocks kept), then restored: clubs 17 of 21 new tests fail (`01-vitest-clubs-before-fix.log`), clubs-admin 4 of 6 (`02-vitest-clubs-admin-before-fix.log`).

### Evidence
1. `pnpm turbo run lint typecheck test build --force` → **exit 0** (`09-turbo-final.log`; `--force`: 0 cached). Per package: api-client 155, ui 63, i18n 51, auth 17, course-core 342, shared-types 21, id 9, clubs 155, clubs-admin 237 tests passed. Last 40 lines (ANSI colours stripped):
```
clubs:test:    Start at  19:17:55
clubs:test:    Duration  9.26s (tests 37%, environment 35%, transform 14%, setup 8%, import 6%)
clubs:test: 
clubs-admin:test:  ✓ src/audit/list-exports.test.tsx (28 tests) 5325ms
clubs-admin:test:    ✓ T-14-26 R-14-12 E4-W07 D5 members «Excel · PDF» → Excel (4)
clubs-admin:test:      ✓ 200: saves the api's file byte for byte, with its Content-Type and the Content-Disposition name, and sends the list's parameters 527ms
clubs-admin:test:      ✓ 202: opens the exports drawer with the queued job and saves nothing 361ms
clubs-admin:test:    ✓ T-14-26 R-14-12 E4-W07 D5 members «Exportar selecció» (4)
clubs-admin:test:      ✓ 200: saves the api's file byte for byte, with its Content-Type and the Content-Disposition name, and sends the list's parameters 302ms
clubs-admin:test:      ✓ 422 EXPORT_LIMIT: shows the drawer's notice 303ms
clubs-admin:test:    ✓ T-14-26 R-14-12 E4-W07 one export at a time (4)
clubs-admin:test:      ✓ R-03-24 «Exportar selecció» still exports the selected rows after a new search hides them 561ms
clubs-admin:test:  ✓ src/activities/ActivitiesPage.test.tsx (27 tests) 5645ms
clubs-admin:test:    ✓ T-07-29 D7 activities (list, maintenance, publication, cancellation) (21)
clubs-admin:test:      ✓ draws the four mockup rows with the R-07-13 dates, rings, registrations and state chips 439ms
clubs-admin:test:      ✓ «Nova activitat» creates a draft with title and type and opens its maintenance 539ms
clubs-admin:test:    ✓ E4-W08 D7 follow-ups of the E4-W04 round-2 review (6)
clubs-admin:test:      ✓ R-07-05 away from the club the hours cover the whole day: 6:00 when the club opens at 7:00 406ms
clubs-admin:test:  ✓ src/planning/CalendarPage.test.tsx (25 tests) 6014ms
clubs-admin:test:    ✓ E3-W07 step 9 a D1 risk row opens D4 on its class (1)
clubs-admin:test:      ✓ selects the class of `?classe=` in the week of `?setmana=` 455ms
clubs-admin:test:    ✓ T-06-28 D4 / D4b / D4c class calendar (front half, MSW) (24)
clubs-admin:test:      ✓ opens «Esborrany» on the first week with drafts, with the validation card and its count 606ms
clubs-admin:test:      ✓ [VALIDAR LA SETMANA] turns every draft of the week active at once 388ms
clubs-admin:test:      ✓ cancels an active class with registrants through D4c: four rows, text required, «ANUL·LA I AVISA ELS 4 ALUMNES» 394ms
clubs-admin:test:      ✓ [ELIMINA] of an active class goes through the same D4c modal 341ms
clubs-admin:test:      ✓ R-06-09 saves the diff with the version; STALE_VERSION keeps its message after the refetch remounts the card 440ms
clubs-admin:test:      ✓ R-06-09 [Crear classe]: a new time or ring after RING_HAS_BOOKINGS drops the confirmation 328ms
clubs-admin:test:      ✓ R-06-09 [Crear classe]: a RING_HAS_BOOKINGS answer that arrives after a ring change is dropped 315ms
clubs-admin:test: 
clubs-admin:test:  Test Files  19 passed (19)
clubs-admin:test:       Tests  237 passed (237)
clubs-admin:test:    Start at  19:17:55
clubs-admin:test:    Duration  10.00s (tests 39%, environment 33%, transform 15%, setup 7%, import 6%)
clubs-admin:test: 

 Tasks:    34 successful, 34 total
Cached:    0 cached, 34 total
  Time:    21.996s
```
   Earlier attempts: `03-turbo-attempt1-lint.log` (exit 1: `clubs#lint` on my new test — a redundant `??` and two colour literals; the `id`/`clubs-admin` «Test failed» lines there are turbo aborting the parallel tasks → fixed the test); `04-turbo.log` (exit 0, before the last CSS fix).
2. `pnpm i18n:check` → **exit 0** (`05-i18n-check.log`, complete output):
```
> agilityhub-core-web@0.0.0 i18n:check /Users/jordib/dev/agilityhub/agilityhub-core-web
> node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs

i18n check passed
ca: 2328 keys
es: 2328 keys
en: 2328 keys
1752 statically used keys, 0 unused
250 codes, 0 missing
```
3. `…/roadmap-kit/mac/heavy.sh pnpm e2e:core E3-W08` → **exit 0** (`07-e2e-core.log`; lock obtained after 2 min 45 s). E1/E2 stage `10 passed (18.6s)` (line 229), E3 stage `4 passed (18.6s)`, N-37 row found. Core image `ghcr.io/jboixtcm/agilityhub-core-api:main` local `sha256:d17942623118…`, created 2026-09-25T09:37:40Z, revision `8d037e62dc2dd3f569d3e20184740a0fb0051ba5` (no pull). Last 40 lines:
```
+ @types/node 24.13.3
+ eslint 9.39.5
+ eslint-import-resolver-typescript 4.4.5
+ eslint-plugin-import 2.32.0
+ eslint-plugin-jsx-a11y 6.10.2
+ eslint-plugin-react 7.37.5
+ eslint-plugin-react-hooks 7.1.1
+ globals 17.12.0
+ husky 9.1.7
+ i18next-parser 9.4.0
+ lint-staged 17.5.0
+ msw 2.15.0
+ prettier 3.9.6
+ size-limit 13.0.3
+ turbo 2.10.12
+ typescript 5.9.3
+ typescript-eslint 8.69.0

. prepare$ node packages/config/scripts/prepare-husky.mjs
. prepare: .git can't be found
. prepare: Done
Done in 1m 5s using pnpm v9.15.9
[WebServer] (node:134) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` v…
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)

[WebServer] (node:214) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` v…
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)

[WebServer] (node:312) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` v…
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)


Running 4 tests using 1 worker

[1/4] e2e/core/e3-signup.spec.ts:496:5 › T-04-34 public signup is validated and enters through the N-02 welcome link
[2/4] e2e/core/e3-signup.spec.ts:1079:5 › T-04-34 rejected signup delivers N-03
[3/4] e2e/core/e3-signup.spec.ts:1086:5 › T-04-34 active member adds a dog and the club validates it
[4/4] e2e/core/e3-signup.spec.ts:1090:5 › T-04-34 signup.enabled=false shows only the configured closed text
  4 passed (18.6s)
{"action":"OPEN_DOG","channel":"APP","code":"N-37","dogName":"Neret E3","status":"SENT"}
```
   Earlier attempt: `06-e2e-core-attempt1-left-lookup-400.log` (exit 0, but `GET /members?filter=status:eq:LEFT&size=5` answered 400, so the readmission block was skipped; its screenshots also showed the globe icon over «CAT» and a small «Total» row → the lookup now encodes the filter and asks `size=20`, which the core accepts, and the two CSS rules were fixed).
4. `…/roadmap-kit/mac/heavy.sh pnpm e2e:docker E3-W08` (the complete `pnpm e2e` in `mcr.microsoft.com/playwright:v1.63.0-noble`) → **exit 0** (`08-e2e-docker.log`): clubs-admin 38, clubs 29, id 1. Last 40 lines:
```
id:build: - Use build.rolldownOptions.output.codeSplitting to improve chunking: https://rolldown.rs/reference/OutputOptions.codeSplitting
id:build: - Adjust chunk size limit for this warning via build.chunkSizeWarningLimit.
clubs-admin:e2e: cache bypass, force executing 1ae256cc79197ac6
clubs-admin:e2e: 
clubs-admin:e2e: > clubs-admin@0.0.0 e2e /work/apps/clubs-admin
clubs-admin:e2e: > playwright test
clubs-admin:e2e: 
clubs-admin:e2e: [WebServer] (node:665) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued fo…
clubs-admin:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs-admin:e2e: 
clubs-admin:e2e: Running 38 tests using 9 workers
clubs-admin:e2e: ······································
clubs-admin:e2e:   38 passed (21.0s)
clubs:e2e: cache bypass, force executing 1526bda9bf6c0b0e
clubs:e2e: 
clubs:e2e: > clubs@0.0.0 e2e /work/apps/clubs
clubs:e2e: > playwright test
clubs:e2e: 
clubs:e2e: [WebServer] (node:2677) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `ur…
clubs:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs:e2e: 
clubs:e2e: Running 29 tests using 7 workers
clubs:e2e: ·····························
clubs:e2e:   29 passed (12.0s)
id:e2e: cache bypass, force executing fdd2740353c82043
id:e2e: 
id:e2e: > id@0.0.0 e2e /work/apps/id
id:e2e: > playwright test
id:e2e: 
id:e2e: 
id:e2e: Running 1 test using 1 worker
id:e2e: ·
id:e2e:   1 passed (3.1s)

 Tasks:    8 successful, 8 total
Cached:    0 cached, 8 total
  Time:    47.75s 

evidence: E3-W08/D2-readmission-mock-1280.png
evidence: E3-W08/19-payment-early-month-mock-375.png
```
   (Long lines are cut at 220 characters in these tails; the logs have them whole.)

### Screenshots (`roadmap/evidence/E3-W08/`) against the mockups
Shell-wide on the real core: brand initial «C» (E19, no logo in the seed); **footer «Club Agility Cànic · Cabrera de Mar» without the tax id**, because the core's `/branding` has no `taxId` yet (api E3-T16 adds it; with the fixture the footer reads «Club Agility Cànic · G00000000 · Cabrera de Mar», `19-payment-early-month-mock-375.png`); dark text on the primary buttons (A32, accepted); visible labels above fields (E3-W05 S4).
- `19-payment-core-375.png` (monthly, day 25 ≥ 16) vs `mobil/19`: «Entrada (1 gos) 100,00 €», «Alta avui, 25 de setembre (mig mes) 30,00 €», «Alta l’1 d’octubre (mes complet) 60,00 €», «Total a pagar al club 130,00 €» — same structure and amounts as the mockup's 17-08 example. Differences: «,00» decimals (accepted, E3-W01 review 4); the closing text under the card is the club's MANUAL/Stripe text as before.
- `19-payment-early-month-mock-375.png` (monthly, MSW club date 05-08-2026; the core's clock cannot be moved, see Assumptions): «Alta avui, 5 d’agost (mes complet) 60,00 €» / «Alta el dia 16 d’agost (mig mes) 30,00 €», total 160,00 €.
- `19-payment-pack6-core-375.png`: «Pack 6 135,00 €», no start options, total 135,00 € (no mockup for a pack; R-04-14).
- `19-add-dog-core-375.png`: «Alta avui, 25 de setembre (quota addicional d'aquest mes) 45,00 €» / «Alta l’1 d’octubre (ara només l'entrada) 0,00 €», total 145,00 € (no mockup; literals proposed below).
- `17-document-required-core-375.png`: the error under «Cartilla de vacunes» with the control outlined in red; no «És imprescindible…» note. Plan cards: Abonat «60,00 €/mes · Entrada 100,00 € · fins a dues classes per setmana · Ofertes…», Pack 6 «135,00 € · 3 mesos · Només un cop» (no «Entrada 0,00 €»), Teràpia with the seed's `conditions` beside the title and the maintenance line — the long descriptions are gone (E3-W05 17a–d). The seed's Teràpia `conditions` is longer than the mockup's «condicions i cost segons cada cas» (data).
- `16-person-core-375.png`, `17-dog-core-375.png`: ⓘ before «Ja ets soci…», globe + «CAT ▾», footer as above.
- `13-pending-dog-core-375.png`: «Neret E3 · Mestís · mascle · 4 anys» with «pendent de validació», no note, tasks, documents or photo input (no mockup: S04 §13-8); `13-validated-dog-core-375.png`: after D2, an ordinary card with «＋ DOC.».
- `D2-readmission-core-1280.png` / `D2-readmission-mock-1280.png` vs `escriptori/D2`: the badge «Readmissió» and «Canvis respecte de la fitxa de baixa» with «Abans / Ara» rows under the person card (no mockup for a readmission; literals proposed). On the real core the person card itself shows the LEFT record's values (the api's `member`), the block shows the submitted ones.

### Files changed
- `apps/clubs/src/SignupPage.tsx` (steps 1, 2, 4, 5, 7), `SelfServicePages.tsx` (step 3), `styles.css`; `SignupPage.test.tsx` (harness navigator moves the address; `stubFullPageLoads` changes it when the next document loads; 21 new tests), `App.test.tsx` (E36), `e2e/signup.spec.ts` (19 before the split day).
- `apps/clubs-admin/src/dashboard/SignupReviewPage.tsx`, `SignupEditDrawer.tsx`, `styles.css`; `SignupReviewPage.test.tsx` (6 new tests), `e2e/dashboard-signup.spec.ts` (readmission capture).
- `packages/api-client/src/mocks/fixtures/signup.ts`, `signup-config-canic.json`, `signup-review.ts`, `member-self-service.ts`, `branding-canic.json`, `handlers.ts`, `scenarios.ts`; tests `signup-handlers.test.ts`, `fixtures.contract.test.ts`.
- `packages/ui/src/branding.tsx` (`club.legalName`, `club.taxId`), `packages/ui/src/on-primary.test.ts`.
- `packages/i18n/src/locales/{ca,es,en}/signup.json` (`step4.*`, `common.footerWithTaxId*`, `dog.documentRequired`; removed `payment.startToday|startAlternative`), `census.json` (`myDogs.pendingValidation`), `admin-census.json` (`signupReview.readmission.*`, `fields.contactEmails|phones|address`).
- `e2e/core/e3-signup.spec.ts`; `CHANGELOG.md`; `roadmap/evidence/E3-W08/*`.
- From the previous session (commit `1a6b15d`): `openapi.json`, `pending.json`, `src/generated/schema.d.ts`.

### Rules and tests implemented
R-04-08 (flag + signed headers), R-04-12, R-04-14, R-04-15, R-04-25/E36, R-04-06/E38, R-04-02; T-04-03 (web part), T-04-30, T-04-31, T-04-32 (payment card and total); S03 row 13 / R-03-11 (pending dogs on 13).

### Assumptions
- Footer: «{legalName} · {taxId} · {city}» as the organizer wrote on 25-09 (the task text said `{name}`); without a tax id the E1-W05 «{name} · {city}» stays. The second line (registered office) is E3-W12 step 5.
- «`conditions` on one line» = one element with the conditions, no clamping: the mockup's Pack 10 wraps its conditions on two lines.
- The core's clock cannot be moved (no `POST /test/clock` in this image), so «19 on a day before 16» is captured against MSW with the club date 05-08-2026; on the real core the day was 25.
- Add-dog options: the web labels them by `option` only (`portion` is not meaningful for the additional-dog fee); the mock sends `FULL` for both.
- While a full page load is on its way the departing page shows «Carregant el formulari» (existing key) instead of the next step; a checkout redirect keeps 19 visible.
- The readmission e2e uses the first LEFT member with a document; in this run it was the applicant rejected earlier in the same run.

### Questions / catalog proposals
1. **New UI literals (no mockup), ca/es/en in the locales:** `signup:step4.additionalDog.today` «Alta avui, {date} (quota addicional d'aquest mes)», `signup:step4.additionalDog.nextMonth` «Alta l’{date} (ara només l'entrada)» (§10 has no add-dog keys); `signup:dog.documentRequired`; `census:myDogs.pendingValidation` «pendent de validació» (es «pendiente de validación», en «pending approval»); `admin-census:signupReview.readmission.title|previous|submitted|documentLocked` («Canvis respecte de la fitxa de baixa», «Abans: …», «Ara: …», «El DNI/NIE no es pot canviar durant una readmissió: per corregir-lo, rebutja la readmissió.»).
2. **api (for E3-T16 / the organizer):** `GET /members?filter=status:eq:LEFT&size=5` answered 400 in attempt 1 (body not captured); `size=20` with the filter encoded works. If a page size below 20 is invalid, the OpenAPI `size` parameter should say so (it only has `minimum: 1`).
3. Commands denied in this sandbox (worked around, nothing blocked): `git -C … diff`, `cd … && git …`, `npx eslint …`, heredocs containing braces and shell `for` loops. The pre-fix swap therefore used a small Python helper under the git-ignored `node_modules/.e3-w08-backup/` (it only read `git show HEAD:…`; removed afterwards), and ESLint ran as `pnpm exec eslint`. No `docker pull` was attempted: the e2e used the local core image above.

### Round 2 (session 25-09, claude-opus-5-5)
All seven points of the organizer's list are fixed; the four Verification commands pass on the final tree. The staged `openapi.next.json` (`c9eff8f`, api E3-T14 round 2) was **not** adopted: this round fixes exactly the listed points, and E3-W11 step 0 adopts that snapshot.

**What changed, per point**
1. **D2 edits the readmission's submitted values (review #1).**
   - `apps/clubs-admin/src/dashboard/readmission.ts` `signupPerson(signup)`: `member` (the LEFT record) overlaid with `readmission.submitted` (names, gender, birth date, contacts, address, payment method, «Compte no informat» derived from a SEPA method without an account). With no readmission block it returns `member` unchanged.
   - D2's person card (name, contact, WhatsApp, payment) and the edit drawer (form and PATCH arrays) read it. The PATCH still carries `member.version`. With phone 2 edited, the body is `phones: [{Mòbil 655123123}, {Feina 655444333}]`: the submitted phone 1 is kept.
   - The core writes absent blocks as `null`. The real core sent `submitted.paymentMethod.maskedAccount: null` (`d2-readmission-view-core.json`). So `maskedAccount: null` counts as «no account», and `readmission: null` (an ordinary signup) keeps the DNI/NIE editable: the drawer now checks `!= null`, where round 1 checked `!== undefined`.
   - Mock: the `readmission` variant keeps `member` as the LEFT record (marta.antic@…, 655000111, Carrer del Mar 7, Efectiu) and `submitted` = the applicant's values. `PATCH /members/{id}` applies the edits to `readmission.submitted`, recomputes `changedFields`, bumps the member's version and returns the LEFT record (`withReadmissionValues` in `fixtures/signup-review.ts`).
   - Real core (`e3-signup.spec.ts`): the card reads «retorn.e3@example.test · +34 699000905», WhatsApp `wa.me/34699000905`, and the drawer opens with the submitted e-mail and phone and a read-only DNI/NIE (`D2-readmission-core-1280.png`, `D2-readmission-drawer-core-1280.png`). `GET /members/{id}/signup` keeps `member.phones` = the LEFT phones and `submitted.phones` = `["699000905"]` (asserted). An ordinary signup's drawer has an editable DNI/NIE (asserted).
2. **bfcache (review #2).** `SignupPage` listens to `pageshow`. With `event.persisted` it clears `leaving`, re-reads the path from `location`, and re-reads the session draft. That last part goes beyond the ask: the restored document's in-memory draft is older than what the next page saved, and the next «CONTINUA» would otherwise write the old draft over it. A non-persisted `pageshow` changes nothing.
3. **Frozen amounts (review #3, R-04-14, T-04-32).** On a committed signup, `SignupSubmission.frozen = {upfront: SignupResult.upfront (or AddDogSignupResult.upfront), start: {option, portion, startDate} of the option shown and sent}`. That is amounts and dates only, never personal data. While committed, the card renders:
   - the frozen lines (ENTRY_FEE, PACK);
   - one start row: add-dog from the api's `upfront.additionalDog`; public from the FIRST_MONTH line's amount, labelled by the stored start (`SignupUpfront` has no first-month block);
   - the frozen `totalDue`.
   There are no radio buttons any more: the start is already committed (R-04-26). The existing E3-W06 test «#2 after a cancelled checkout…» asserted disabled radios, so it now asserts the frozen row. `additionalDog: null` (the core's shape on a public signup) and `upfront: null` are read as absent.
4. **Line concepts (review #4).** `conceptLabel`: `ENTRY_FEE` → «Entrada (1 gos)», `PACK` → the plan name. `FIRST_MONTH` and `ADDITIONAL_DOG_FEE` are not shown as a second line: the live start options, or the frozen start row, show them.
5. **Lookup assertion (review #5).** `expect(leftLookup.listStatus).toBe(200)`, `expect(leftMember).not.toBeNull()`, `expect(leftMember?.idDocument).toBeTruthy()`; the silent `else` branch is gone. As the review says, this covers an applicant rejected earlier in the same run, not a seed member with a real `leftAt` history.
6. **Nits.**
   - (a) `signup:dog.maintenance` is now an ICU `select` on `withEntry`: with an entry fee of 0, or none, Teràpia reads «Quota mínima durant el tractament: 10,00 €/mes» (es/en too). The line used to be hidden when there was no entry fee at all.
   - (b) MSW enforces both flags, as the api does. With `requireDogDocumentAtSignup`, a submission without a file gets a bare `422 DOG_DOCUMENT_REQUIRED` (`details: {}`), for both `POST /signup` (`dog.documents`) and `POST /me/dogs/signup` (`documents`). `leavePending: true` without `allowFamilyGroupPending` gets `400 VALIDATION_ERROR` `fieldErrors: [{code: INVALID, field: familyGroupClaim.leavePending}]`. Both flags come from the new scenario fields `signupRequireDogDocument` / `signupFamilyPendingAllowed` (scenarios `signupDocumentRequired`, `signupNoFamilyPending`), and `GET /signup` sends the same values.
   - (c) The onPrimary guard is wider. It now reads:
     - the solid colour as the first value of a shorthand, with `!important`, or as a custom-property fallback;
     - `color-mix` with ≥ 50 % primary (the `:hover` of the primary button, 86 %). Lighter tints, with the primary as the text colour, are exempt;
     - state rules (`:hover`, `:not(…)`), which inherit the text colour of their base rule;
     - TSX inline `background(Color)` with the primary, which is forbidden.
     It found no violation. Its claim is now «CSS rules and TSX inline styles».
7. **Evidence.** `signup-quote-add-dog-core.json` (real core, 25-09): the member plan's add-dog quote `ENTRY_FEE 10000` + `TODAY FULL 2026-09-25 4500 → 14500` / `ALTERNATIVE 2026-10-01 0 → 10000`, and the submitted `AddDogSignupResult.upfront`: lines `ENTRY_FEE 10000` + `ADDITIONAL_DOG_FEE 4500` (both `DUE`, `paidAmount 0`), `totalDue 14500`, `additionalDog {TODAY, 2026-09-25, 4500}`. The e2e asserts that the submitted total equals the total the card showed. The MSW result lines now carry `paidAmount: 0`, as the core's do.

**Step → test (round 2).** Pre-fix = the product files swapped to `HEAD` while the new tests and mocks are kept. For the null-shape rows, only that one line was reverted.

| Point | Test | Pre-fix |
|---|---|---|
| 1 | `SignupReviewPage.test.tsx` «round 2 #1: the person card, its WhatsApp and the drawer carry the submitted values…», «…editing phone 2 keeps the submitted phone 1 in the PATCH» | 2/2 fail (`11-…log`: phone 1 sent as `655000111`) |
| 1 | «round 2 #1: a submitted SEPA method without an account (the core's `maskedAccount: null`)…», «round 2: `readmission: null` … keeps the DNI/NIE editable» | fail (`17-…log`, `19-…log`) |
| 1 | real-core D2 card/drawer/view assertions; mocked `dashboard-signup.spec.ts` | pass (final run) |
| 2 | `SignupPage.test.tsx` «round 2 #2: a page restored from the back-forward cache…»; «…a pageshow that is not a bfcache restore changes nothing» | first fails; second is a guard and passes (`10-…log`) |
| 3 | «#3 submitted on 15-08 with a full month, retried on 16-08…», «#3 a committed add-dog shows the api's frozen additional-dog choice», «#3 the core's result shape (`additionalDog: null`, paid amounts)…» | 2/2 fail (`10-…log`); null shape fails with `TypeError … reading 'amountDue'` (`18-…log`) |
| 4 | «#4 every concept of a quote line has its label…» | fails (`10-…log`) |
| 6a | «round 2 #6: a maintenance plan without an entry fee…» | fails (`10-…log`) |
| 6b | `signup-handlers.test.ts` «T-04-13 requireDogDocumentAtSignup=true…», «T-04-16 leavePending with allowFamilyGroupPending=false…» (+ the `false` guard) | 2/2 fail against `HEAD` `handlers.ts` (`12-…log`) |
| 6c | `on-primary.test.ts` (3 tests) | pass: there is no violation to find (a guard) |
| 5, 7 | real-core `e3-signup.spec.ts` | pass (final run) |

**Evidence (final tree)**
1. `pnpm turbo run lint typecheck test build --force` → **exit 0** (`20-turbo-round2-final.log`). Counts: api-client 160, ui 65, i18n 51, auth 17, course-core 342, shared-types 21, id 9, clubs 162, clubs-admin 251. There are 0 `[BrandingProvider]` lines. Last 40 lines (ANSI stripped, cut at 220 characters):
```
clubs-admin:test:      ✓ shows the mockup: DNI and phone in full, signed documents, consent warning, level, invoice and Stripe payment 434ms
clubs-admin:test:    ✓ E3-W07 step 4 · M15 the plan and family decisions (R-04-13, T-04-33) (4)
clubs-admin:test:      ✓ a NOT_FOUND_PENDING claim shows what the applicant typed and is resolved by attaching the holder's group 544ms
clubs:test:  ✓ src/SignupPage.test.tsx (89 tests) 5668ms
clubs:test:    ✓ T-04-29 signup person and draft (7)
clubs:test:      ✓ persists a current draft, expires it after 24 hours and maps identity states 839ms
clubs:test:    ✓ E3-W08 step 7: the three narrow cases of the E3-W06 round-2 review (5)
clubs:test:      ✓ #1 a slow full page load: the departing page never renders nor consumes the destination's error 390ms
clubs:test: 
clubs:test:  Test Files  7 passed (7)
clubs:test:       Tests  162 passed (162)
clubs:test:    Start at  21:12:30
clubs:test:    Duration  9.42s (tests 35%, environment 34%, transform 17%, setup 7%, import 7%)
clubs:test: 
clubs-admin:test:  ✓ src/activities/ActivitiesPage.test.tsx (27 tests) 5369ms
clubs-admin:test:    ✓ T-07-29 D7 activities (list, maintenance, publication, cancellation) (21)
clubs-admin:test:      ✓ draws the four mockup rows with the R-07-13 dates, rings, registrations and state chips 448ms
clubs-admin:test:      ✓ «Nova activitat» creates a draft with title and type and opens its maintenance 490ms
clubs-admin:test:    ✓ E4-W08 D7 follow-ups of the E4-W04 round-2 review (6)
clubs-admin:test:      ✓ R-07-05 away from the club the hours cover the whole day: 6:00 when the club opens at 7:00 418ms
clubs-admin:test:  ✓ src/planning/CalendarPage.test.tsx (32 tests) 6994ms
clubs-admin:test:    ✓ E3-W07 step 9 a D1 risk row opens D4 on its class (1)
clubs-admin:test:      ✓ selects the class of `?classe=` in the week of `?setmana=` 449ms
clubs-admin:test:    ✓ T-06-28 D4 / D4b / D4c class calendar (front half, MSW) (24)
clubs-admin:test:      ✓ opens «Esborrany» on the first week with drafts, with the validation card and its count 609ms
clubs-admin:test:      ✓ [VALIDAR LA SETMANA] turns every draft of the week active at once 379ms
clubs-admin:test:      ✓ cancels an active class with registrants through D4c: four rows, text required, «ANUL·LA I AVISA ELS 4 ALUMNES» 359ms
clubs-admin:test:      ✓ [ELIMINA] of an active class goes through the same D4c modal 347ms
clubs-admin:test:      ✓ R-06-09 saves the diff with the version; STALE_VERSION keeps its message after the refetch remounts the card 451ms
clubs-admin:test: 
clubs-admin:test:  Test Files  19 passed (19)
clubs-admin:test:       Tests  251 passed (251)
clubs-admin:test:    Start at  21:12:30
clubs-admin:test:    Duration  11.03s (tests 38%, environment 33%, transform 16%, setup 7%, import 6%)
clubs-admin:test: 

 Tasks:    34 successful, 34 total
Cached:    0 cached, 34 total
  Time:    21.172s
```
2. `pnpm i18n:check` → **exit 0** (`21-i18n-check-round2-final.log`, complete output):
```
> agilityhub-core-web@0.0.0 i18n:check /Users/jordib/dev/agilityhub/agilityhub-core-web
> node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs

i18n check passed
ca: 2329 keys
es: 2329 keys
en: 2329 keys
1753 statically used keys, 0 unused
250 codes, 0 missing
```
3. `…/roadmap-kit/mac/heavy.sh pnpm e2e:core E3-W08` → **exit 0** (`22-e2e-core-round2-final.log`; the lock was free). The E1/E2 stage has `10 passed (18.2s)` (line 142); the E3 stage has `4 passed (20.1s)`; the N-37 row was found. The core image is the local `ghcr.io/jboixtcm/agilityhub-core-api:main` (`sha256:6041df2971d9…`, created 2026-09-25T17:59:21Z, revision `c9eff8f99ff4540ed52fa0e2f9b2bed4e8a5b379`; no pull). Last 40 lines:
```
+ @types/node 24.13.3
+ eslint 9.39.5
+ eslint-import-resolver-typescript 4.4.5
+ eslint-plugin-import 2.32.0
+ eslint-plugin-jsx-a11y 6.10.2
+ eslint-plugin-react 7.37.5
+ eslint-plugin-react-hooks 7.1.1
+ globals 17.12.0
+ husky 9.1.7
+ i18next-parser 9.4.0
+ lint-staged 17.5.0
+ msw 2.15.0
+ prettier 3.9.6
+ size-limit 13.0.3
+ turbo 2.10.12
+ typescript 5.9.3
+ typescript-eslint 8.69.0

. prepare$ node packages/config/scripts/prepare-husky.mjs
. prepare: .git can't be found
. prepare: Done
Done in 4.4s using pnpm v9.15.9

[WebServer] (node:134) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` v
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
[WebServer] (node:214) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` v
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)


[WebServer] (node:312) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` v
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)

Running 4 tests using 1 worker

[1/4] e2e/core/e3-signup.spec.ts:496:5 › T-04-34 public signup is validated and enters through the N-02 welcome link
[2/4] e2e/core/e3-signup.spec.ts:1154:5 › T-04-34 rejected signup delivers N-03
[3/4] e2e/core/e3-signup.spec.ts:1161:5 › T-04-34 active member adds a dog and the club validates it
[4/4] e2e/core/e3-signup.spec.ts:1165:5 › T-04-34 signup.enabled=false shows only the configured closed text
  4 passed (20.1s)
{"action":"OPEN_DOG","channel":"APP","code":"N-37","dogName":"Neret E3","status":"SENT"}
```
4. `…/roadmap-kit/mac/heavy.sh pnpm e2e:docker E3-W08` (the complete `pnpm e2e` in the Playwright image) → **exit 0** (`23-e2e-docker-round2-final.log`): clubs-admin 39, clubs 29, id 1. Last 40 lines:
```
clubs:build:   dist/sw.js
clubs:build:   dist/workbox-9c191d2f.js
id:e2e: cache bypass, force executing 983c19b80edf36aa
id:e2e: 
id:e2e: > id@0.0.0 e2e /work/apps/id
id:e2e: > playwright test
id:e2e: 
id:e2e: 
id:e2e: Running 1 test using 1 worker
id:e2e: ·
id:e2e:   1 passed (3.2s)
clubs-admin:e2e: cache bypass, force executing b9aa3bca72195d3a
clubs-admin:e2e: 
clubs-admin:e2e: > clubs-admin@0.0.0 e2e /work/apps/clubs-admin
clubs-admin:e2e: > playwright test
clubs-admin:e2e: 
clubs-admin:e2e: [WebServer] (node:893) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued fo
clubs-admin:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs-admin:e2e: 
clubs-admin:e2e: Running 39 tests using 9 workers
clubs-admin:e2e: ·······································
clubs-admin:e2e:   39 passed (23.9s)
clubs:e2e: cache bypass, force executing e6e5028f50f48732
clubs:e2e: 
clubs:e2e: > clubs@0.0.0 e2e /work/apps/clubs
clubs:e2e: > playwright test
clubs:e2e: 
clubs:e2e: [WebServer] (node:2950) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `ur
clubs:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs:e2e: 
clubs:e2e: Running 29 tests using 7 workers
clubs:e2e: ·····························
clubs:e2e:   29 passed (12.5s)

 Tasks:    8 successful, 8 total
Cached:    0 cached, 8 total
  Time:    51.73s 

evidence: E3-W08/D2-readmission-mock-1280.png
evidence: E3-W08/19-payment-early-month-mock-375.png
```

Earlier runs in this round (one line each):
- `13-turbo-round2.log` and `14-i18n-check-round2.log`: both exit 0, but on an earlier tree. After them I added the null handling, `paidAmount` and one more test.
- `15-e2e-core-round2.log`: **exit 1**. `response.json: Protocol error (Network.getResponseBody)`: the add-dog result was read after the full page load to «enviada» had discarded it. The spec now reads it through `page.route` + `route.fetch()`.
- `16-e2e-core-round2-attempt2.log`: exit 0 (10 + 4). Its screenshot showed «Compte no informat» as a header badge, which revealed `maskedAccount: null` in the submitted block. That led to the null handling in point 1 and to the `d2-readmission-view-core.json` capture.

**Screenshots against the mockups** (`roadmap/evidence/E3-W08/`, re-shot by the final runs):
- `D2-readmission-core-1280.png` vs `escriptori/D2`: the PERSONA card now shows the submitted «retorn.e3@example.test · +34 699000905» with WhatsApp, and the inline «Compte no informat» (the submitted SEPA method has no account). The «Canvis respecte de la fitxa de baixa» block still shows «Abans / Ara». There is no mockup for a readmission.
- `D2-readmission-drawer-core-1280.png`: the drawer with the submitted e-mail and phone, and the DNI/NIE read-only with its hint.
- The other captures (19, add-dog 19, 17, 13, 16, footer, D2 mock) are unchanged from round 1. The committed-card layout (frozen rows, no radios) only shows after a cancelled checkout, which the real-core run does not reach (the core seed has no Stripe); it is covered by Vitest.

**Files changed (round 2)**
- `apps/clubs-admin/src/dashboard/readmission.ts` (new), `SignupEditDrawer.tsx`, `SignupReviewPage.tsx`, `SignupReviewPage.test.tsx` (+5 tests).
- `apps/clubs/src/SignupPage.tsx` (bfcache, frozen card, concept labels, maintenance line), `SignupPage.test.tsx`: +8 tests; `stubFullPageLoads({aborted})`; the E3-W06 «#2 after a cancelled checkout» test asserts the frozen row.
- `packages/api-client/src/mocks/fixtures/signup-review.ts` (LEFT-record `member`, `withReadmissionValues`), `fixtures/signup.ts` (`paidAmount: 0`), `handlers.ts` (readmission PATCH, flag enforcement), `scenarios.ts` (two flag scenarios), `signup-handlers.test.ts` (+3 tests).
- `packages/i18n/src/locales/{ca,es,en}/signup.json` (`dog.maintenance` ICU `select`).
- `packages/ui/src/on-primary.test.ts` (wider guard, 3 tests).
- `e2e/core/e3-signup.spec.ts`; `CHANGELOG.md`; `roadmap/evidence/E3-W08/10-…23-*.log`, `signup-quote-add-dog-core.json`, `d2-readmission-view-core.json`, `D2-readmission-drawer-core-1280.png`, plus the re-shot captures.

**Assumptions (round 2)**
- `leavePending` refused → `400 VALIDATION_ERROR` with `fieldErrors[{code: "INVALID", field: "familyGroupClaim.leavePending"}]`. T-04-16 says only «400». `INVALID` is the field code the mocks already use; the web never sends this body (the link is hidden), so no UI depends on it.
- A readmission's `submitted` block is the whole submitted person: a missing (or `null`) `lastName2` means none; a missing `address`/`birthDate`/`gender`/`paymentMethod` falls back to the record's value.
- After a PATCH during a readmission, the mock bumps `member.version`: the edit is written on the member (`MemberService.edit`), so the view's `version` moves.
- The frozen card keeps the start option shown, because `SignupUpfront` has no first-month block (proposal 1 below).

**Questions / catalog proposals (round 2)**
1. **api (contract):** `SignupResult.upfront` (`SignupUpfront`) has no first-month block, so the web keeps the option it showed in order to label a committed FIRST_MONTH line. Proposal: add `firstMonth: SignupFirstMonth` to `SignupUpfront`, as `SignupUpfrontReview` already has (api E3-T12). The web would then label the frozen line from the api alone.
2. **api (contract):** the core serializes absent optional fields as `null`: `submitted.paymentMethod.maskedAccount`/`channel` (seen in `d2-readmission-view-core.json`), and `member.plan`/`planId`/`maskedAccount` (`d2-signup-view-core.json`). The OpenAPI marks them optional but not nullable. The web now reads them as absent; the snapshot should say `nullable`, as E3-T10 did for the enums. I did not capture whether an ordinary signup's `readmission` arrives as `null` or absent; the web accepts both.
3. Which field code does the core put in `fieldErrors` for a refused `leavePending`? (assumption above)
4. Commands denied this session (worked around): a Python heredoc containing braces and quotes (edits made with the file tools instead); `ls /Users/jordib/dev/agilityhub/` (reading outside the repo; not needed); a compound `pnpm exec vitest …; echo; grep …` from a sub-directory (split into separate commands). The pre-fix swap used a Python helper under the git-ignored `node_modules/.e3-w08-r2/` (it read `git show HEAD:…` only), removed afterwards.

## Organizer verification
Result: changes_requested · 2026-09-25 · organizer (Claude)

Steps 1–8 are in place:
- the payment card of 19 from the per-plan quote;
- the signed uploads;
- the pending dogs on screen 13;
- the switches;
- 16/17 and the public shell;
- the D2 readmission block;
- the three narrow cases of E3-W06.

17 of the 21 new tests failed before the fix (log `01`).

Evidence:
- turbo: 34/34;
- the complete e2e: 38 + 29 + 1;
- the real-core e2e: 10 + 4;
- CI green at `f7f5bf1`.

The independent review (`roadmap/reviews/E3-W08-20260925-1929-claude.md`) found two majors, three minors and three nits. Codex could not answer, so a fresh Claude session reviewed. The organizer confirmed major #1 in the code and in the api:
- `MemberSignupView.readmission` says «The LEFT record keeps its values until validation»;
- `MemberService.edit` writes a readmission's person edits to the submitted values.

Answers to the report's questions:
1. The new literals are accepted.
2. The api accepts only `size` ∈ {20, 50, 200, 1000}; anything else is `400 INVALID_FILTER`. CONVENCIONS_API §4 now says so, and api E3-T16 documents it in the OpenAPI.

Round 2 — fix exactly these points:
1. **D2 edits the readmission's submitted values (review #1, R-04-06, E38).** During a readmission, `member` is the LEFT record.
   - Build the person form, the card's contact and WhatsApp from `readmission.submitted`.
   - The mock keeps `member` as the LEFT record and applies the PATCH edits to `readmission.submitted`, as the api does.
   - Vitest: during a readmission, editing phone 2 keeps the submitted phone 1.
   - The real-core e2e checks that the drawer opens with the submitted values.
2. **A page restored from the back-forward cache is usable (review #2).** On `pageshow` with `event.persisted`, clear `leaving` and re-read the path from `location`. Test with a persisted `pageshow`.
3. **A committed signup shows its frozen amounts (review #3, R-04-14, T-04-32).** Once the signup is committed, the card renders `SignupResult.upfront`, kept in the draft's submission, and not the live quote. Test: submitted on day 15 with a full month and retried on day 16; the card still shows the full month and the frozen total.
4. **Every line concept has its label (review #4).** Map `ENTRY_FEE` explicitly. Label `FIRST_MONTH` and `ADDITIONAL_DOG_FEE`, or leave them out where another row already shows them.
5. **The readmission e2e asserts its lookup (review #5).** Use `expect(leftMember).not.toBeNull()` instead of a log.
6. **Nits:**
   - no «Entrada a compte: 0,00 €» for a plan without an entry fee (review #6);
   - the mocks enforce `requireDogDocumentAtSignup` (`422 DOG_DOCUMENT_REQUIRED`) and `allowFamilyGroupPending` (400), as the api does (review #7);
   - widen the `onPrimary` guard, or narrow its claim (review #8).
7. **Evidence:** commit the real-core add-dog quote, the JSON with the 45 € additional fee and the 145 € total.

On the real core, the footer lacks the tax id until api E3-T16 publishes it, and E3-W12 adds the second line. That is expected, and not part of this round.

### Round 2 verification
Result: verified · 2026-09-25 · organizer (Claude)

- **The seven points are fixed, with tests that failed before the fix** (logs `10`, `11`, `12`, `17`, `18` and `19`):
  - D2 shows and edits the readmission's submitted values (`signupPerson`); a PATCH that edits phone 2 keeps the submitted phone 1; the mock keeps `member` as the LEFT record;
  - a page restored from the back-forward cache is usable again;
  - a committed signup shows its frozen amounts;
  - every line concept has its label, or is shown by its start row;
  - the readmission e2e asserts its lookup;
  - the nits: no 0 € entry line, the mocks enforce both flags, and a wider `onPrimary` guard;
  - the real-core add-dog quote and its result are committed.
- **Evidence:**
  - turbo `--force`: 34/34;
  - `i18n:check`: green;
  - the real-core e2e: 10 + 4, with the D2 readmission card, drawer and view asserted;
  - the complete e2e: 39 + 29 + 1;
  - CI green at `098c240`.
- **No independent review of round 2 was produced.** The review started while the loop was being restarted, and no file came back. The organizer read the code of points 1, 2 and 4 and the round-2 evidence. The gate re-run E3-W09 audits all of it again.
- **Answers to the report's questions:**
  1. **`SignupUpfront.firstMonth`:** accepted as a later improvement, after the gate. Until then, the web keeps the option it showed.
  2. **Nulls the snapshot does not declare:** api E3-T16 round 2, point 5. The D2 view and the signup results declare as nullable every field the core sends as `null`.
  3. **The refused `leavePending`:** the core answers `400 VALIDATION_ERROR` with `fieldErrors: [{field: familyGroupClaim.leavePending, code: NOT_ALLOWED}]`. The mock's `INVALID` becomes `NOT_ALLOWED` in E3-W12 step 6.
