---
id: E0-W09
title: Playbook: build a screen from its mockup (docs/playbooks/screen-from-mockup.md) + checklist
stage: E0
repo: agilityhub-core-web
thread: B
order: 90
status: awaiting_verification
depends_on: [E0-W06, E0-W07]
branch: 
pr: 
updated: 2026-09-06
---

# E0-W09 · Playbook: build a screen from its mockup (docs/playbooks/screen-from-mockup.md) + checklist

## Objective
The pattern every later front task follows, written from the code you actually built in E0.

## Context to load
The code of `apps/clubs` shell, `packages/ui`, `packages/i18n`, `packages/api-client/src/mocks` · `AGENTS.md` · `docs/METODOLOGIA_VIBE_CODING.md` §4 · this file.

## Steps
Write `docs/playbooks/` (English, ≤ 150 lines each, with real code excerpts):
1. `screen-from-mockup.md` — open the mockup HTML + PNG, list the copy, map to i18n keys (three locales), find the spec §2 row (route, role, API), add MSW handlers and fixtures first, build with `packages/ui` components only, gate by role/module through `modules.ts`, write the Vitest + Playwright tests named `T-xx-nn`, take screenshots at 375/1280 next to the PNG, run `pnpm i18n:check`.
2. `api-client-usage.md` — hooks, error mapping to messages, idempotent POSTs, optimistic updates policy.
3. `checklist.md` — the DoD as a checklist to paste into every task report.

## Verification
- `ls docs/playbooks` → the three files; excerpts reference real files.

## Definition of Done
Three playbooks reviewed against the code.

## Evidence to return
`wc -w docs/playbooks/*.md`.

---

## Executor report

### Branch / commits
- Branch: shared `main` working tree (no git write commands run, per session protocol).
- Commits: none; the publish script commits after the session.

### Evidence (full command outputs)
`ls docs/playbooks`

```text
api-client-usage.md
checklist.md
screen-from-mockup.md
```

`wc -w docs/playbooks/*.md`

```text
     677 docs/playbooks/api-client-usage.md
     526 docs/playbooks/checklist.md
     722 docs/playbooks/screen-from-mockup.md
    1925 total
```

`wc -l docs/playbooks/*.md`

```text
     108 docs/playbooks/api-client-usage.md
      61 docs/playbooks/checklist.md
     110 docs/playbooks/screen-from-mockup.md
     279 total
```

Excerpt source checks:

```text
$ rg -n 'createApiClient|refreshBranding' apps/clubs/src/main.tsx
1:import { createApiClient, normalizeBranding, refreshBranding } from "@agilityhub/api-client";
25:  const source = await refreshBranding(
26:    createApiClient({ baseUrl: apiBaseUrl }),
$ rg -n 'currentMockScenario\(\)\.branding|currentMockScenario\(\)\.me' packages/api-client/src/mocks/handlers.ts
7:    HttpResponse.json(currentMockScenario().branding, {
12:    const branding = currentMockScenario().branding;
20:  http.get("*/api/v1/me", () => HttpResponse.json(currentMockScenario().me)),
$ rg -n 'useTranslation\("shell"\)|requiredModulesForUiItem\("routes"' apps/clubs/src/App.tsx
98:  const { t } = useTranslation("shell");
109:  const { i18n, t } = useTranslation("shell");
146:  const { t } = useTranslation("shell");
182:  const { t } = useTranslation("shell");
224:  const requiredModules = requiredModulesForUiItem("routes", route.path);
239:  const { t } = useTranslation("shell");
$ rg -n 'loadNamespaces\("errors"\)|errors:INVALID_CREDENTIALS' packages/i18n/src/index.test.ts
21:    await instance.loadNamespaces("errors");
22:    expect(instance.t("errors:INVALID_CREDENTIALS")).toBe(
```

`node_modules/.bin/prettier --check docs/playbooks/*.md CHANGELOG.md roadmap/tasks/E0-W09.md`

```text
Checking formatting...
All matched files use Prettier code style!
```

### Files changed
- `docs/playbooks/screen-from-mockup.md` — ordered mockup-to-screen workflow with real
  shell, i18n, module-gating, MSW, testing, and screenshot precedents.
- `docs/playbooks/api-client-usage.md` — generated client/hook, error mapping,
  idempotency, optimistic update, MSW, and test policies.
- `docs/playbooks/checklist.md` — reusable frontend Definition of Done checklist.
- `CHANGELOG.md` — Unreleased playbook entry.
- `roadmap/tasks/E0-W09.md` — status and executor report.
- `roadmap/STATUS.md` — generated status board.

### Rules and tests implemented (R-xx-nn / T-xx-nn)
- Documentation-only task; no feature `R-xx-nn` or `T-xx-nn` was assigned or added.
- The playbooks require future tests to carry their spec IDs and cite the existing
  `T-02-14` shell test as the real repository precedent.

### Assumptions
- Dependency `E0-W07` is `awaiting_verification`; continued under the dependency rule
  in `roadmap/README.md`. Revisit E0-W09 if that dependency receives changes.
- The hook example is explicitly conceptual because E0 contains infrastructure hooks,
  not screen-specific generated operations; placeholders must be replaced from the
  OpenAPI generator in each feature task.
- “English, ≤ 150 lines each” is interpreted as prose language and physical file lines;
  the measured files are 108, 61, and 110 lines.

### Questions / catalog proposals
- None. No parameters, events, notifications, or error codes were added.

## Organizer verification
_(pending)_
