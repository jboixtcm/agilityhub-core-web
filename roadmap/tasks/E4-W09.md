---
id: E4-W09
title: D4 — follow-ups of the E4-W02 round-4 review: time options on slot boundaries, the editor locked through conflict recovery, and closed days (absent from club.openingHours)
stage: E4
repo: agilityhub-core-web
thread: B
order: 47
status: verified
depends_on: [E4-W02]
branch:
pr:
updated: 2026-09-25
---

# E4-W09 · D4: follow-ups of the round-4 review

## Objective
The independent review of E4-W02 round 4 (`roadmap/reviews/E4-W02-20260924-2106-codex.md`) found two majors and one minor in the class calendar. The organizer confirmed them against S02 R-02-09 and S06. E4-W02 was verified with these routed here. All three must be fixed before the E4 gate (E4-W05 depends on this task).

**Each fix comes with a test that fails before the fix.** Give it the spec id.

## Spec references
- S02 R-02-09: `club.openingHours` per day, **a closed day is absent**, and times have a 5-minute granularity.
- S06:
  - §3, the `date`/`startTime`/`endTime` rows (multiples of `classes.slotMinutes`, inside the day's opening hours) and the ring-block `from`/`to` rows;
  - R-06-09 (the class card and its version);
  - R-06-11 (ring blocks).
- `CATALEG_ERRORS.md`: `INVALID_SLOT_GRANULARITY`, `OUTSIDE_OPENING_HOURS`, `STALE_VERSION`, `INVALID_STATE`.

## Context to load
- The review.
- `apps/clubs-admin/src/planning/calendar-shared.tsx` (the time options near `:55`, and the opening-hours fallback near `:244`).
- `SelectedClassCard.tsx` (near `:286`) and `CalendarPage.tsx` (near `:768`).
- `CreateClassDrawer.tsx` and `RingBlockDrawer.tsx`.
- `packages/api-client/src/mocks/calendar-handlers.ts` (near `:259`).

## Steps
1. **Time options on slot boundaries (major #1).** Today the options start at the opening minute. With opening hours `07:05–22:00` and `classes.slotMinutes = 10`, every offered time ends in `:05`, `:15`…, and the api refuses them with `INVALID_SLOT_GRANULARITY`.
   - The first option is the opening time rounded **up** to a slot boundary, and the last one ends at or before the closing time.
   - Apply this to every time picker of D4: create, edit and ring block. For ring blocks, follow what the api checks on `from`/`to` (S06 §3).
   - Vitest: an opening at `07:05` with 10-minute slots offers `07:10` first, and a save sends aligned times.
2. **The editor stays locked through conflict recovery (major #2, R-06-09).** After `STALE_VERSION` or `INVALID_STATE`, the card unlocks while the calendar refetch is still pending. An edit made in that window is lost when the refreshed version remounts the card. Success already waits for the new version (`awaitingVersion`); do the same for these two errors, `toggleExempt` included. Vitest with a delayed refetch after a 409: an edit attempted before the refetch lands is refused, and no edit is lost.
3. **Closed days (minor #3, R-02-09).** A weekday missing from `club.openingHours` is closed, not open 7:00–22:00.
   - Represent closed days explicitly, and offer no times on them: a clear message, and submission blocked.
   - Make the mock enforce the same rule: `OUTSIDE_OPENING_HOURS` on a closed day, per the catalog.
   - Vitest with Sunday absent from `club.openingHours`.

## Fixed conventions
- Design system only.
- Keys are never literals; ca, es and en are added together.
- Mocks behave like the api.
- Heavy runs go through the host lock.
- Fictional data only.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E4-W09/*.log`)
- `pnpm turbo run lint typecheck test build`.
- `pnpm i18n:check`.
- `pnpm e2e:docker`.

## Definition of Done
- Steps 1–3 are done, each with a test that fails before the fix.
- The mock follows the same rules.

## Evidence to return
- The command outputs.
- A step → test table, with the tests you saw fail before the fix.

---

## Executor report

Session of 25-09 (claude:claude-opus-5-5). All three steps are done, each with tests named after the spec rules that fail on the pre-task code; the mock follows the same rules. Every Verification command is green on the final tree.

### Summary per step
1. **Time options on slot boundaries (major #1).**
   - `calendar-shared.tsx` changes:
     - `timeOptions` now returns multiples of `step` from midnight: the first option is the opening rounded **up**.
     - The new `rangeOptions(opening, step, minimum)` gives the start/end options of a range. The shortest range is `minimum` rounded up to whole slots, and the last end is at or before the closing time.
   - All three D4 pickers use it:
     - the card's «Hora»: starts that keep the class length inside the day;
     - [Crear classe]: at least one slot;
     - [Bloqueja pista]: at least `training.slotMinutes`, with `from`/`to` on `classes.slotMinutes` boundaries. That is what the api checks (S06 §3 `to − from` a multiple of `classes.slotMinutes`; R-09-11 / T-09-13 `from/to` aligned).
   - With 07:05–21:55 and 10-minute slots:
     - [Crear classe] offers starts 07:10…21:40 and ends 07:20…21:50;
     - the block drawer offers from 07:10…21:20 and to 07:40…21:50;
     - a new class/block is sent as 07:10–08:10 and 07:10–07:40 (`05:10Z`/`05:40Z`).
2. **The editor stays locked through conflict recovery (major #2, R-06-09).**
   - `SelectedClassCard` now locks after `STALE_VERSION`/`INVALID_STATE` from [ACCEPTA] and from «Exempta…» (`toggleExempt`), as it already did after a save. The lock (`awaitingVersion`) holds until the calendar refetch settles.
   - `onSaved`/`onConflict` now return that refetch. It comes from the new `useResource(...).refetch()`, which drops loads already on their way (sequence bump) and resolves when the load it started settles.
   - A refetch with a new version remounts the card (key `id:version`) with fresh values. A refetch that fails, or returns the same version, leaves the card in place and unlocks it with the admin's values kept, so it is never locked for good. A later save then gets `STALE_VERSION` again: nothing is overwritten silently.
   - My first attempt followed `calendar.loading` with a render-time state machine. It failed the guard test: a fast refetch flips `loading` true→false inside one batch, so the card never saw it and stayed locked. It was replaced by the promise (see Evidence).
3. **Closed days (minor #3, R-02-09).**
   - `openingOf` returns `null` for a weekday absent from `club.openingHours`.
   - `loadOpeningHours` falls back to the product default (CATALEG_PARAMETRES: dl–dg 07:00–22:00) only when the value cannot be read, the same effective behaviour as before.
   - On a closed day (typed date), [Crear classe] and [Bloqueja pista]:
     - show «El club està tancat aquest dia» as the date field's error (`role="alert"`, `aria-invalid`, `aria-describedby`);
     - offer no times (empty, disabled selects);
     - disable their submit button, and `create()`/`save()` return early.
   - The card of a class on a closed day offers only its own time and shows the same text as a note.
   - D4 opens the drawers only once the opening hours are loaded (buttons disabled until then). While they load, the card offers only the class's time.
   - Mock (`calendar-handlers.ts`): `openingWindow` returns `undefined` for an absent weekday, and `timeProblem` answers `422 OUTSIDE_OPENING_HOURS` for classes (create/patch) and ring blocks (create/patch) there. The product default applies only when the parameter does not exist.
   - Copy: `admin-scheduling:calendar.closedDay` in ca «El club està tancat aquest dia» (literal of S09 §1, screen 08 closed-day state), es «El club está cerrado este día», en «The club is closed on this day».

### Step → test table
Pre-fix = the new tests run on the pre-task product code, before any fix. Logs `01-prefix-clubs-admin.log` (`Tests 9 failed | 1 passed | 30 skipped (40)`) and `02-prefix-mock.log` (`Tests 1 failed | 1 passed | 6 skipped (8)`).

| Step | Test (file › name) | Pre-fix |
|---|---|---|
| 1 | `calendar-shared.test.ts` › R-06-09 an opening at 07:05 with 10-minute slots starts at 07:10 and never passes the closing time | **failed** (`expected '07:05' to be '07:10'`) |
| 1 | `calendar-shared.test.ts` › R-06-09 R-06-11 a range keeps at least its minimum length on slot boundaries | failed (`rangeOptions` did not exist: TypeError, not an assertion) |
| 1 | `CalendarPage.test.tsx` › S06 §3 R-06-09 an opening at 07:05 with 10-minute slots: «Hora» and [Crear classe] offer 07:10 first, and the class is sent on slot boundaries | **failed** (`expected '18:50' to be '07:10'`: 18:50 was not an option and was prepended) |
| 1 | `CalendarPage.test.tsx` › R-06-11 T-06-32 an opening at 07:05: [Bloqueja pista] offers 07:10–07:40 first and saves on slot boundaries | **failed** (`De` was 07:05) |
| 1 | `calendar-handlers.test.ts` › S06 §3 with an opening at 07:05 and 10-minute slots: 07:10 is the first valid start | passed (guard: the mock already refused 07:05 with `INVALID_SLOT_GRANULARITY`) |
| 1, 3 | e2e `planning-calendar.spec.ts` › E4-W09 … a 07:05 opening offers slot boundaries, and a closed Sunday offers no times | not run pre-fix (browsers only run in Docker); green in `12` |
| 2 | `CalendarPage.test.tsx` › R-06-09 after STALE_VERSION every editor stays locked until the refetched version is shown, so no edit is lost | **failed** (editors enabled while the refetch was held) |
| 2 | `CalendarPage.test.tsx` › R-06-09 after INVALID_STATE on «Exempta…» every editor stays locked until the refetched class is shown | **failed** (same) |
| 2 | `CalendarPage.test.tsx` › R-06-09 a conflict refetch that fails unlocks the card with the admin's edit kept | passed (guard against a permanent lock; it **failed** on my first implementation, see Evidence) |
| 3 | `calendar-shared.test.ts` › R-02-09 a weekday absent from club.openingHours is closed: no window, no options | **failed** (`expected { close: '22:00', open: '07:00' } to be null`) |
| 3 | `CalendarPage.test.tsx` › R-02-09 Sunday absent from club.openingHours is closed: the card and [Crear classe] offer no times there, say so and do not submit | **failed** («Hora» offered the whole day) |
| 3 | `CalendarPage.test.tsx` › R-02-09 R-06-11 Sunday absent from club.openingHours: [Bloqueja pista] offers no times there, says so and does not submit | **failed** (no alert) |
| 3 | `calendar-handlers.test.ts` › R-02-09 a weekday absent from club.openingHours is closed: classes and ring blocks there get OUTSIDE_OPENING_HOURS | **failed** (`promise resolved … instead of rejecting`: 201) |

Every earlier D4 test still passes, including the E4-W02 round-3 lock test «keeps every editor disabled from [ACCEPTA] until the saved version is shown».

### Evidence
Logs in `roadmap/evidence/E4-W09/`. ANSI colour codes are stripped from the tails pasted here.

**Earlier attempts (one line each):**
- `01-prefix-clubs-admin.log`: `pnpm --filter clubs-admin exec vitest run src/planning/CalendarPage.test.tsx src/planning/calendar-shared.test.ts -t "E4-W09|slot boundaries"` → exit 1 (pre-fix proof, see the table).
- `02-prefix-mock.log`: `pnpm --filter @agilityhub/api-client exec vitest run src/mocks/calendar-handlers.test.ts -t "E4-W09"` → exit 1 (pre-fix proof).
- First post-fix attempt: the same clubs-admin command without `-t` → exit 1, `Tests 1 failed | 39 passed (40)`. The guard «a conflict refetch that fails unlocks…» failed because the card stayed locked (render-time state machine on `calendar.loading`). **My mistake: the passing re-run was written to the same file name, so this attempt's log was overwritten.** I replaced the state machine with `useResource.refetch()`, and the re-run is `03-postfix-clubs-admin.log`: exit 0, `Tests 40 passed (40)`.
- `04-postfix-mock.log`: `pnpm --filter @agilityhub/api-client exec vitest run src/mocks/calendar-handlers.test.ts` → exit 0, `Tests 8 passed (8)`.
- `05-lint-typecheck-targeted.log`: `pnpm turbo run lint typecheck --filter=clubs-admin --filter=@agilityhub/api-client` → exit 1, a `type` alias in the mock test (`consistent-type-definitions`). Changed to an `interface`. The run took 15 min: host load average 45 from the other lane.
- `06-turbo.log`: `pnpm turbo run lint typecheck test build --force` → exit 1, clubs-admin lint: two `as HTMLFormElement` casts (`non-nullable-type-assertion-style`). Replaced by a `formOf()` helper. `07-lint-clubs-admin.log` is the re-lint: exit 0.
- `10-e2e-docker.log`: `heavy.sh pnpm e2e:docker E4-W09` → exit 1, only my new e2e failed: `page.evaluate: SyntaxError: Unexpected token '<'`. The mock-api `fetch` ran right after sign-in's full reload, before the MSW worker served the new document. The sidebar is a plain `<a>`, so a reload would also reset the in-page mock state. Fix: set the hours once D4 is drawn, then re-enter D4 through the day view and back (client-side navigation). `11-lint-clubs-admin-e2e-spec.log`: re-lint, exit 0.

**1. `pnpm turbo run lint typecheck test build --force`** (the Verification command plus `--force`, 0 cached) → **exit 0**, full log `08-turbo.log`. Last 40 lines (vitest prints only the tests slower than 300 ms; the E4-W09 describe shows `(7)`):
```
clubs-admin:test:      ✓ shows the mockup maintenance of «Torneig d'Estiu 2026» with the ring, level and waitlist chips 362ms
clubs-admin:test:      ✓ R-07-03 [DESA] sends only the allow-listed rich text of the editor, with the version 304ms
clubs-admin:test:      ✓ R-07-05 [PUBLICA] with conflicts opens the dialog with the conflicts and the required notice text 370ms
clubs-admin:test:      ✓ R-07-06 [CANCEL·LA L'ACTIVITAT] opens the modal with «CANCEL·LA I AVISA ELS 22 INSCRITS» disabled without text 384ms
clubs-admin:test:      ✓ R-07-04 after an upload [DESA] sends only the admin's own edits with the fresh version 332ms
clubs-admin:test:      ✓ R-07-05 a RING_BLOCK conflict is not forceable: no option for it and [PUBLICA I APLICA] disabled with the reason 328ms
clubs-admin:test:      ✓ R-07-04/05 save mode: a resync conflict opens «DESA I APLICA»; STALE_VERSION closes it, refetches and says so 326ms
clubs-admin:test:      ✓ the plain publish confirm is pending once, keeps one key per payload and turns ADMIN_TEXT_REQUIRED into the text 302ms
clubs-admin:test:      ✓ [DESPUBLICA] answers ACTIVITY_HAS_REGISTRATIONS with registrants and unpublishes without them 324ms
clubs-admin:test:    ✓ E4-W08 D7 follow-ups of the E4-W04 round-2 review (6)
clubs-admin:test:      ✓ R-07-04 the rich text is read-only while [DESA] is pending, and nothing typed is lost 314ms
clubs-admin:test:      ✓ R-07-05 away from the club the hours cover the whole day: 6:00 when the club opens at 7:00 522ms
clubs-admin:test:  ✓ src/planning/CalendarPage.test.tsx (32 tests) 9528ms
clubs-admin:test:    ✓ E3-W07 step 9 a D1 risk row opens D4 on its class (1)
clubs-admin:test:      ✓ selects the class of `?classe=` in the week of `?setmana=` 637ms
clubs-admin:test:    ✓ T-06-28 D4 / D4b / D4c class calendar (front half, MSW) (24)
clubs-admin:test:      ✓ opens «Esborrany» on the first week with drafts, with the validation card and its count 971ms
clubs-admin:test:      ✓ [VALIDAR LA SETMANA] turns every draft of the week active at once 499ms
clubs-admin:test:      ✓ cancels an active class with registrants through D4c: four rows, text required, «ANUL·LA I AVISA ELS 4 ALUMNES» 493ms
clubs-admin:test:      ✓ [ELIMINA] of an active class goes through the same D4c modal 520ms
clubs-admin:test:      ✓ R-06-09 saves the diff with the version; STALE_VERSION keeps its message after the refetch remounts the card 642ms
clubs-admin:test:      ✓ R-06-05 moving a class onto training bookings asks first and resends with cancelBookings 317ms
clubs-admin:test:      ✓ R-06-11 a new ring after RING_HAS_BOOKINGS drops the confirmation: no cancelBookings for a ring not shown 380ms
clubs-admin:test:      ✓ R-06-09 [Crear classe]: a new time or ring after RING_HAS_BOOKINGS drops the confirmation 460ms
clubs-admin:test:      ✓ R-06-09 [Crear classe]: a RING_HAS_BOOKINGS answer that arrives after a ring change is dropped 402ms
clubs-admin:test:      ✓ R-06-11 the block drawer locks ring, date and times until a delayed RING_HAS_BOOKINGS arrives 326ms
clubs-admin:test:      ✓ R-06-09 keeps every editor disabled from [ACCEPTA] until the saved version is shown 332ms
clubs-admin:test:    ✓ E4-W09 D4 follow-ups of the E4-W02 round-4 review (7)
clubs-admin:test:      ✓ R-06-09 after STALE_VERSION every editor stays locked until the refetched version is shown, so no edit is lost 306ms
clubs-admin:test: 
clubs-admin:test:  Test Files  19 passed (19)
clubs-admin:test:       Tests  247 passed (247)
clubs-admin:test:    Start at  20:26:05
clubs-admin:test:    Duration  16.19s (tests 35%, environment 29%, transform 20%, setup 8%, import 8%)
clubs-admin:test: 

 Tasks:    34 successful, 34 total
Cached:    0 cached, 34 total
  Time:    31.699s
```

**2. `pnpm i18n:check`** → **exit 0** (`09-i18n-check.log`, complete output):
```
> agilityhub-core-web@0.0.0 i18n:check /Users/jordib/dev/agilityhub/agilityhub-core-web
> node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs

i18n check passed
ca: 2329 keys
es: 2329 keys
en: 2329 keys
1753 statically used keys, 0 unused
250 codes, 0 missing
```

**3. `/Users/jordib/Dropbox/Documents/SOFTWARE_CANIC/05-desenvolupament/roadmap-kit/mac/heavy.sh pnpm e2e:docker E4-W09 > roadmap/evidence/E4-W09/12-e2e-docker.log 2>&1`** (the complete Playwright suite in the Playwright Docker image, all three projects) → **exit 0**: clubs-admin 39 passed, id 1 passed, clubs 29 passed. Full log `12-e2e-docker.log`. Last 40 lines:
```
clubs:build:   dist/workbox-9c191d2f.js
clubs-admin:e2e: cache bypass, force executing 7e253318ad1189ba
clubs-admin:e2e: 
clubs-admin:e2e: > clubs-admin@0.0.0 e2e /work/apps/clubs-admin
clubs-admin:e2e: > playwright test
clubs-admin:e2e: 
clubs-admin:e2e: [WebServer] (node:664) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs-admin:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs-admin:e2e: 
clubs-admin:e2e: Running 39 tests using 9 workers
clubs-admin:e2e: ·······································
clubs-admin:e2e:   39 passed (30.9s)
id:e2e: cache bypass, force executing c1fec78083a21c5c
id:e2e: 
id:e2e: > id@0.0.0 e2e /work/apps/id
id:e2e: > playwright test
id:e2e: 
id:e2e: 
id:e2e: Running 1 test using 1 worker
id:e2e: ·
id:e2e:   1 passed (3.6s)
clubs:e2e: cache bypass, force executing 10fff20c952e603e
clubs:e2e: 
clubs:e2e: > clubs@0.0.0 e2e /work/apps/clubs
clubs:e2e: > playwright test
clubs:e2e: 
clubs:e2e: [WebServer] (node:2947) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs:e2e: 
clubs:e2e: Running 29 tests using 7 workers
clubs:e2e: ·····························
clubs:e2e:   29 passed (15.2s)

 Tasks:    8 successful, 8 total
Cached:    0 cached, 8 total
  Time:    1m4.591s 

evidence: E4-W09/D4-crear-classe-obertura-0705-1280.png
evidence: E4-W09/D4-bloqueja-pista-diumenge-tancat-1280.png
evidence: E4-W09/D4-crear-classe-diumenge-tancat-1280.png
```

**Screenshots (1280, from the Docker run; states without a mockup):**
- `D4-crear-classe-obertura-0705-1280.png`: [Crear classe] on dj 13/08 with a 07:05 opening shows «Inici 7:10 · Final 8:10».
- `D4-crear-classe-diumenge-tancat-1280.png`: dg 16/08 with Sunday absent shows «El club està tancat aquest dia» under «Data», empty disabled «Inici»/«Final», and [CREA LA CLASSE] disabled.
- `D4-bloqueja-pista-diumenge-tancat-1280.png`: the same in [Bloqueja pista], with [DESA EL BLOQUEIG] disabled.

### Files changed
- `apps/clubs-admin/src/planning/calendar-shared.tsx`:
  - `timeOptions` on slot boundaries, and the new `rangeOptions` and `OpeningWindow`;
  - `openingOf` returns `null` for a closed day;
  - `defaultOpeningHours` is used only when `club.openingHours` cannot be read.
- `apps/clubs-admin/src/planning/SelectedClassCard.tsx`:
  - the lock through `STALE_VERSION`/`INVALID_STATE` (save and «Exempta…») until the refetch settles;
  - `onSaved`/`onConflict` return the refetch promise;
  - «Hora» on slot boundaries, plus the closed-day note.
- `apps/clubs-admin/src/planning/CreateClassDrawer.tsx`, `RingBlockDrawer.tsx`: `rangeOptions`, plus the closed-day date error, no times and the disabled submit.
- `apps/clubs-admin/src/planning/CalendarPage.tsx`:
  - the card callbacks return `calendar.refetch()`;
  - the drawers open only once the opening hours are loaded;
  - the card receives `openingHours` (or `undefined` while it loads).
- `apps/clubs-admin/src/planning/shared.ts`: `useResource` gains `refetch()`.
- `apps/clubs-admin/src/activities/ActivityPage.tsx`: keeps its previous fallback on a closed day (explicit `?? 07:00–22:00`), because `openingOf` now returns `null`.
- `packages/api-client/src/mocks/calendar-handlers.ts`: a closed day gets `422 OUTSIDE_OPENING_HOURS`.
- `packages/i18n/src/locales/{ca,es,en}/admin-scheduling.json`: `calendar.closedDay`.
- Tests:
  - `apps/clubs-admin/src/planning/CalendarPage.test.tsx`: new describe «E4-W09 …» (7 tests), plus helpers; settings are reset between tests;
  - `apps/clubs-admin/src/planning/calendar-shared.test.ts`: 3 tests;
  - `packages/api-client/src/mocks/calendar-handlers.test.ts`: 2 tests, and settings are reset;
  - `apps/clubs-admin/e2e/planning-calendar.spec.ts`: 1 e2e test, with 3 screenshots into `roadmap/evidence/E4-W09/`.
- `CHANGELOG.md` (Unreleased › Changed, «E4-W09»), and `roadmap/evidence/E4-W09/*`.
- I did not touch the concurrent working-tree edits to `docs/`, `roadmap/MESSAGES.md`, `roadmap/reviews/gate-E3/…`, or the E3-W08/E3-W11 task files; they are not part of this task.

### Rules and tests implemented
- S06 §3 (`startTime`/`endTime` multiples of `classes.slotMinutes` inside the day's opening hours; ring-block `to − from` a multiple of `classes.slotMinutes` and ≥ `training.slotMinutes`) and R-09-11 / T-09-13 (`from/to` aligned): step 1 tests.
- T-06-32 (block from D4 on slot boundaries): the block test of step 1.
- R-06-09 (the card and its `version`: no edit lost until the refreshed version is applied, `toggleExempt` included): step 2 tests.
- R-02-09 (a closed day is absent): step 3 tests, UI and mock.
- CATALEG_ERRORS:
  - `OUTSIDE_OPENING_HOURS` (422) on a closed day and outside the window;
  - `INVALID_SLOT_GRANULARITY` (400) on unaligned times;
  - `STALE_VERSION` / `INVALID_STATE` (409) drive the lock.
- No new error code, parameter, event or notification.

### Assumptions
1. Slot boundaries are counted from local midnight, i.e. `minuteOfDay % classes.slotMinutes === 0`, as the mock and the D3 bands already check. For ring blocks, S06 §3 asks only `to − from` to be a multiple of `classes.slotMinutes`, but R-09-11 / T-09-13 say `from/to` aligned, so I align both; the duration is then a multiple as well.
2. «El club està tancat aquest dia» is the spec literal of S09 §1 (screen 08). The es/en texts are my translations.
3. The card of a class on a closed day offers only its own time and shows the note. [ACCEPTA] stays available for the other fields, because I do not know whether the api re-checks the opening hours on a PATCH that does not touch the times. The mock (E4-W02 behaviour, unchanged) re-validates the whole class, so it answers `OUTSIDE_OPENING_HOURS` there, shown in the card.
4. When `GET /club/opening-hours` fails, the product default applies (as before E4-W09), and the api still decides. While the value loads, [Crear classe] / [Bloqueja pista] are disabled.
5. A refetch that fails or brings the same version unlocks the card with the admin's values. Keeping it locked would block the card for good. With the stale `version` still in place, the next save is refused (`STALE_VERSION`), so nothing is overwritten.
6. `useResource.refetch()` drops loads already on their way. `reload()` is unchanged for its other callers.

### Questions / catalog proposals
1. **D7 (R-07-05), follow-up proposal.** An activity at the club with linked rings on a closed day still offers 07:00–22:00; the api refuses its ring block there. D7 was out of this task's scope, so it keeps its previous behaviour explicitly (comment in `ActivityPage.tsx`). The same treatment as D4 (message, no times) would need a small D7 task.
2. **D3 templates (R-06-01).** `TemplatesPage` and the template mock still treat a weekday absent from `club.openingHours` as 07:00–22:00 when they compute a kind's common window. Should a closed day be left out of the kind (a WEEKDAY kind with a closed Monday)? Or does it make every band `OUTSIDE_OPENING_HOURS`? Not changed here; I need the api's rule to align it.
3. **api:** does `PATCH /class-sessions/{id}` re-check `OUTSIDE_OPENING_HOURS` when the patch does not touch `startTime`/`endTime` (assumption 3)?
4. **Commands denied by the session's permissions (alternatives used):**
   - `ls` in `/Users/jordib/dev/agilityhub` (looking for the api source; not needed);
   - `cd /Users/jordib/dev/agilityhub/agilityhub-core-web/apps/clubs-admin && pnpm exec vitest run …` (used `pnpm --filter clubs-admin exec vitest run …` instead);
   - a `for l in ca es en; do grep … "$l" …; done` loop (used explicit paths);
   - one long inline `python3` heredoc that wrote this report (the command parser refused its length; written with the editor instead).

## Organizer verification
Result: verified · 2026-09-25 · organizer (Claude)

- **Steps 1–3 are done, with tests that failed before the fix** (log `01`: 9 of 10; log `02`: 1 of 2):
  - D4's time options start on a slot boundary in create, edit and ring blocks;
  - the editor stays locked until the refetch after `STALE_VERSION` or `INVALID_STATE` lands;
  - a weekday missing from `club.openingHours` is closed: it has no times and shows a message, and the mock answers `OUTSIDE_OPENING_HOURS`.
- **Evidence:**
  - turbo: 34/34, uncached;
  - `i18n:check`: green;
  - the complete e2e: 39 + 1 + 29;
  - CI green at `ffd9c55`.
- **The independent review** (`roadmap/reviews/E4-W09-20260925-2039-claude.md`) has one minor and six nits. Claude reviewed, because Codex could not answer. The findings go to **E4-W10 steps 4–6**, and E4-W05 already depends on E4-W10.
- **Answers to the report's questions:**
  1. **D7:** activities at the club with linked rings get D4's closed-day treatment (E4-W10 step 6).
  2. **D3:** the api refuses a band when any day of its kind is closed or does not contain it (`WeekTemplateRules`). So a closed day in the kind refuses every band. The web follows that rule (E4-W10 step 6). Whether a kind should skip its closed days is a spec question for later. It does not affect the Cànic, whose WEEKDAYS kind (Mon–Fri) has no closed day.
  3. **Class edits on a closed day:** yes. `ClassSessionService.patch` re-validates the whole class on any patch that is not only `notes`, `OUTSIDE_OPENING_HOURS` included, even with unchanged times. So on a closed day the card allows only `notes` (E4-W10 step 4). The mock is already right.
