---
id: E4-W06
title: D11 «Nivells» card — «Progressió» switch (Level.progression, ruling E29), the D3 description preview on the real field, D11 labels for the new parameter blocks, and the D3 edit-mode leftovers of E4-W01
stage: E4
repo: agilityhub-core-web
thread: B
order: 60
status: awaiting_verification
depends_on: [E4-W01, external:api:E5-T11]
branch:
pr:
updated: 2026-09-26
---

# E4-W06 · D11 «Nivells» — «Progressió» switch (E29)

## Objective
This task implements the organizer ruling E29 of 2026-09-24 (`docs/DECISIONS_PENDENTS.md`):
- a level can be outside the progression of levels (the Cànic's «Teràpia» is);
- only progression levels count for the automatic description «{first} i sup.».

The admin sets this per level in the D11 «Nivells» card.

## Spec references
- S05 §2, row «D11 (targeta Nivells)».
- S05 §3 `Level.progression` (new 24-09) and the §12 seed.
- S06 R-06-03 and T-06-02 (amended 24-09).
- `docs/DECISIONS_PENDENTS.md`: E29.

## Context to load
- This file.
- `apps/clubs-admin/src/catalogs/SettingsPage.tsx`: the level form, with its `grantsFreeTraining` switch as the pattern to follow.
- `apps/clubs-admin/src/planning/description.ts` and the MSW planning resolver (E4-W01).
- The `pending.json` overlay that E4-W01 round 2 adds.

## Steps
0. **Contract.**
   - If the staged api snapshot already has `Level.progression` (api E5-T11), adopt it byte for byte and prune the `pending.json` overlay that E4-W01 round 2 added.
   - Otherwise, keep the overlay (mocks-first) and say so in the report.
1. **D11 «Nivells» card.**
   - Add a «Progressió» switch per level, with the help text «Els nivells fora de la progressió, com Teràpia, no compten per a «D i sup.»».
   - Save it with the same `PATCH` + `version` pattern as the other fields.
   - Add the keys in ca/es/en.
2. **Preview and mock.** The D3 preview helper (`description.ts`) and the MSW resolver read `progression`. No level code is hard-coded.
3. **Tests.**
   - Vitest for the switch: the `PATCH` body, and the error shown by code.
   - The T-06-02 helper cases with Teràpia.
   - A Playwright screenshot `D11-nivells-1280.png`.

4. **D11 labels for the new parameter blocks (added 24-09 from E3-W03 round 2).** Against core image `9e3a9c6`, D11 showed raw keys: `admin-settings:block.training.title`, `admin-settings:block.messaging.title` and the `param.learn.*.label` rows. On the core of 24-09 (web E3-W05, `D11-signup-toggle-core-1280.png`), the privacy and jobs blocks and the `param.jobs.*` labels were raw as well: cover every block and key of the current catalog, not only these.
   - Add ca/es/en entries for every block and parameter of `docs/specs/00-transversal/CATALEG_PARAMETRES.md` that D11 renders.
   - Add `scripts/check-parameter-keys.mjs`, chained in `pnpm i18n:check` like `check-error-codes.mjs`. It fails when a catalog key or block has no label in a locale.
   - System-only parameters that D11 never shows go in an explicit allowlist in the script, with a comment.

5. **D3 edit-mode leftovers (added 24-09 from the E4-W01 round-2 review `roadmap/reviews/E4-W01-20260924-1148-claude.md`).**
   - #3: `removeClass` and `removeBand` go through `patchQueue`, so a DELETE never overtakes a queued PATCH.
   - #4: no invented opening hours. Without `GET /club/opening-hours` data, place `OUTSIDE_OPENING_HOURS` by comparing the times the admin sent.
   - #7: a failed chip PATCH reverts only the chips. Text still being typed in «Descripció» or «Límit» stays.
   - One Vitest for each.

## Fixed conventions
- Design system only.
- Keys are never literals; ca/es/en are added together.
- Evidence logs are committed.
- Fictional data only.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E4-W06/*.log`)
- `pnpm api:generate` and the Ajv suite.
- `pnpm turbo run lint typecheck test build`.
- `pnpm i18n:check`.
- The complete `pnpm e2e` in the Playwright Docker image (`scripts/e2e-docker.sh`).

## Definition of Done
- The admin can mark a level as outside the progression in D11.
- The D3 preview follows R-06-03 with the real field, or with the mock field until api E5-T11 publishes it.
- D11 shows no raw keys against the real core, and `pnpm i18n:check` fails on a parameter without a label.

## Evidence to return
Command outputs, the screenshot, and the adoption proof (or the `pending.json` diff).

---

## Executor report

**Outcome.** Steps 0–5 are done. Dependencies: E4-W01 is `verified`, and api E5-T11's `Level.progression` is in the adopted snapshot.

The admin can now take a level out of the progression in D11. There is a «Progressió» switch per row, and one in the level form.

D11 on the published core shows no raw key. The new real-core spec proves it:
- first run (log `01`): red, with 14 raw parameter labels;
- final run (log `09`): green.

`pnpm i18n:check` now fails on a catalog parameter without a label: on the HEAD locales it reports 102 missing items (log `07`).

The 7 new Vitests fail on the HEAD product code (log `06`). All Verification commands are green.

### Step 0 — contract
`Level.progression` has been in `openapi.json` since the E3-W07 adoption (`fbc00cd`), which also pruned its `pending.json` overlay. No snapshot is staged now: the only `openapi.next.json`, `3f0381e`, was adopted by E4-W11 in this same session. So there is nothing to adopt or prune. `12-contract-progression.log`:
```
Level.progression {"description": "S05 §3 (E29): only progression levels count for the automatic «{first} i sup.» (S06 R-06-03)", "type": "boolean"} required
pending.json x-schema-overlays: None | schemas: ['DogPatchPendingFields', 'DogPendingFields', 'LicensePendingFields', 'LicenseWithPendingFields', 'NotificationPreferences', 'NotificationPreferencesPatch', 'PlanBillingMode']
```

### Evidence

**1. `pnpm api:generate` and the Ajv suite.**
- `pnpm api:generate` → exit 0 (`10-api-generate.log`). The generated file is byte-identical before and after: `8c2daf7bf74a7adc72766eac63b3def6fc55178e71053d90cf01db34f1472052` both times.
  ```
  > node scripts/generate.mjs

  Generated /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client/src/generated/schema.d.ts from /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client/openapi/openapi.json merged with /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client/openapi/pending.json
  exit 0
  8c2daf7bf74a7adc72766eac63b3def6fc55178e71053d90cf01db34f1472052  packages/api-client/src/generated/schema.d.ts
  ```
- Ajv: `pnpm --filter @agilityhub/api-client exec vitest run src/mocks/fixtures.contract.test.ts src/mocks/activity-handlers.test.ts src/mocks/calendar-handlers.test.ts src/mocks/club-settings-handlers.test.ts` → exit 0 (`11-ajv-suite.log`):
  ```
   RUN  v5.0.0 /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client

   Test Files  4 passed (4)
        Tests  83 passed (83)
     Start at  02:17:05
     Duration  989ms (import 40%, transform 30%, tests 30%)

  exit 0
  ```

**2. `pnpm turbo run lint typecheck test build --force`** → exit 0: 34/34, 0 cached. The complete output is `13-turbo.log`. The last 40 lines (ANSI colours stripped):
```
clubs:test:      ✓ #1 a slow full page load: the departing page never renders nor consumes the destination's error 400ms
clubs:test: 
clubs:test:  Test Files  7 passed (7)
clubs:test:       Tests  171 passed (171)
clubs:test:    Start at  02:17:25
clubs:test:    Duration  10.39s (tests 39%, environment 36%, transform 14%, setup 6%, import 5%)
clubs:test: 
clubs-admin:test:  ✓ src/activities/ActivitiesPage.test.tsx (36 tests) 7828ms
clubs-admin:test:    ✓ T-07-29 D7 activities (list, maintenance, publication, cancellation) (21)
clubs-admin:test:      ✓ draws the four mockup rows with the R-07-13 dates, rings, registrations and state chips 567ms
clubs-admin:test:      ✓ «Nova activitat» creates a draft with title and type and opens its maintenance 667ms
clubs-admin:test:      ✓ shows the mockup maintenance of «Torneig d'Estiu 2026» with the ring, level and waitlist chips 344ms
clubs-admin:test:      ✓ R-07-14 hides «Nivells» with levels.enabled=false and «Llista d'espera» without WAITLIST 310ms
clubs-admin:test:    ✓ T-07-29 E4-W08 D7 follow-ups of the E4-W04 round-2 review (6)
clubs-admin:test:      ✓ R-07-05 away from the club the hours cover the whole day: 6:00 when the club opens at 7:00 529ms
clubs-admin:test:    ✓ T-07-29 E4-W10 D7 follow-ups of the E4-W08 and E4-W09 reviews (3)
clubs-admin:test:      ✓ S07 §6 CONVENCIONS_API §4 the member filter reads every page: with 1,001 distinct registrants the member on the second page can be selected 682ms
clubs-admin:test:  ✓ src/planning/CalendarPage.test.tsx (37 tests) 8757ms
clubs-admin:test:    ✓ E3-W07 step 9 a D1 risk row opens D4 on its class (1)
clubs-admin:test:      ✓ selects the class of `?classe=` in the week of `?setmana=` 565ms
clubs-admin:test:    ✓ T-06-28 D4 / D4b / D4c class calendar (front half, MSW) (24)
clubs-admin:test:      ✓ opens «Esborrany» on the first week with drafts, with the validation card and its count 816ms
clubs-admin:test:      ✓ [VALIDAR LA SETMANA] turns every draft of the week active at once 434ms
clubs-admin:test:      ✓ cancels an active class with registrants through D4c: four rows, text required, «ANUL·LA I AVISA ELS 4 ALUMNES» 465ms
clubs-admin:test:      ✓ [ELIMINA] of an active class goes through the same D4c modal 408ms
clubs-admin:test:      ✓ R-06-09 saves the diff with the version; STALE_VERSION keeps its message after the refetch remounts the card 435ms
clubs-admin:test:      ✓ R-06-09 [Crear classe]: a new time or ring after RING_HAS_BOOKINGS drops the confirmation 336ms
clubs-admin:test:      ✓ R-06-09 [Crear classe]: a RING_HAS_BOOKINGS answer that arrives after a ring change is dropped 327ms
clubs-admin:test: 
clubs-admin:test:  Test Files  20 passed (20)
clubs-admin:test:       Tests  283 passed (283)
clubs-admin:test:    Start at  02:17:25
clubs-admin:test:    Duration  12.72s (tests 44%, environment 33%, transform 12%, setup 6%, import 5%)
clubs-admin:test: 

 Tasks:    34 successful, 34 total
Cached:    0 cached, 34 total
  Time:    24.636s 

exit 0
```

**3. `pnpm i18n:check`** → exit 0. The check now runs three scripts; the last one is new. The complete output is `14-i18n-check.log`:
```
> agilityhub-core-web@0.0.0 i18n:check /Users/jordib/dev/agilityhub/agilityhub-core-web
> node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs && node scripts/check-parameter-keys.mjs

i18n check passed
ca: 2365 keys
es: 2365 keys
en: 2365 keys
1758 statically used keys, 0 unused
250 codes, 0 missing
151 catalog parameters (122 in D11, 29 system-only), 10 blocks, 0 missing
exit 0
```

**4. Complete e2e in the Playwright image.** `/Users/jordib/Dropbox/Documents/SOFTWARE_CANIC/05-desenvolupament/roadmap-kit/mac/heavy.sh pnpm e2e:docker E4-W06 > roadmap/evidence/E4-W06/15-e2e-docker.log 2>&1` → exit 0: 44 + 1 + 29, with the new `D11-nivells-1280.png`. The last 40 lines:
```
clubs:build:   dist/sw.js
clubs:build:   dist/workbox-9c191d2f.js
clubs-admin:e2e: cache bypass, force executing 53d95f96861f1de8
clubs-admin:e2e: 
clubs-admin:e2e: > clubs-admin@0.0.0 e2e /work/apps/clubs-admin
clubs-admin:e2e: > playwright test
clubs-admin:e2e: 
clubs-admin:e2e: [WebServer] (node:664) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs-admin:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs-admin:e2e: 
clubs-admin:e2e: Running 44 tests using 9 workers
clubs-admin:e2e: ············································
clubs-admin:e2e:   44 passed (28.0s)
id:e2e: cache bypass, force executing 7eba446329caea81
id:e2e: 
id:e2e: > id@0.0.0 e2e /work/apps/id
id:e2e: > playwright test
id:e2e: 
id:e2e: 
id:e2e: Running 1 test using 1 worker
id:e2e: ·
id:e2e:   1 passed (3.1s)
clubs:e2e: cache bypass, force executing df7312588cde9541
clubs:e2e: 
clubs:e2e: > clubs@0.0.0 e2e /work/apps/clubs
clubs:e2e: > playwright test
clubs:e2e: 
clubs:e2e: [WebServer] (node:3058) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs:e2e: 
clubs:e2e: Running 29 tests using 7 workers
clubs:e2e: ·····························
clubs:e2e:   29 passed (12.8s)

 Tasks:    8 successful, 8 total
Cached:    0 cached, 8 total
  Time:    56.268s 

evidence: E4-W06/D11-nivells-1280.png
exit 0
```
The `exit` lines of logs `09`–`15` are appended from the shell status.

**Real core (for the DoD «no raw keys against the real core»).** `/Users/jordib/Dropbox/Documents/SOFTWARE_CANIC/05-desenvolupament/roadmap-kit/mac/heavy.sh pnpm e2e:core E4-W06 d11-core.spec.ts > roadmap/evidence/E4-W06/09-e2e-core-d11.log 2>&1`. The image is `ghcr.io/jboixtcm/agilityhub-core-api:main`, revision `94a7050`. Result: exit 0.
```
Running 1 test using 1 worker

[1/1] e2e/core/d11-core.spec.ts:60:5 › T-02-13 E4-W06 D11 on the published core names every block and parameter (no raw keys)
  1 passed (3.8s)
exit 0 (shell status of the command, appended by the executor)
```
The capture is `D11-parametres-core-1280.png`. It shows the four new block titles and, on the core, the «Progressió» column with Teràpia and Pendent off.

The spec also records the structure of `GET /parameters` in `d11-core-parameter-blocks.json`: block, key, module and `editableBy`, without any value.

Earlier attempts, one line each:
- `01-e2e-core-d11-before.log`: before the labels. 14 raw rows: `param.learn.recommendationsTtlMinutes.label`, `param.learn.baseUrl.label` and the twelve `param.jobs.*`. This run also produced the JSON. Its block-title check missed the CSS-uppercased titles; the spec now reads `textContent`.
- `08-e2e-core-d11.log`: red because the spec read the grid while only the loading card was on screen (0 texts). It now waits for a parameter row.

**Pre-fix proof.** The product files were set temporarily to their HEAD content, with the tests kept: `tmp.local/e4w06/swap.py`, read-only `git show`, then restored.
- `06-prefix-clubs-admin.log`: `vitest run src/catalogs/SettingsPage.test.tsx src/planning/TemplatesPage.test.tsx -t "E4-W06"` gives `Tests 7 failed | 17 skipped`. All four «Progressió» tests and the three D3 tests fail.
- `07-prefix-check-parameter-keys.log`: `node scripts/check-parameter-keys.mjs` on the HEAD locales gives exit 1, «102 missing». The missing items are the `activities.cancelDeadline` enum values, `jobs.*`, `learn.*` and the four blocks.

### Step → test table

| Step | Test | Failed before |
|---|---|---|
| 1 | `SettingsPage.test.tsx` › «E4-W06 S05 §2 D11 «Nivells»: the «Progressió» switch (S05 §3 Level.progression, E29)» › four tests: the switch per level with the help text; the toggle's `PATCH {progression: true, version: 1}`, pending state and next version; the refusal shown by code (`FORBIDDEN` text) plus `STALE_VERSION` → message and reload; the form's `POST {progression: false}` and the edit's `PATCH {progression: false, version: 1}` | yes (`06`) |
| 1 e2e | `apps/clubs-admin/e2e/settings.spec.ts` › «E4-W06 D11 «Nivells» card» (Teràpia off, G on, help, the toggle's PATCH body, one «Processos automàtics» heading) → `D11-nivells-1280.png` | new (green in `15`) |
| 2 | Already in place since E4-W01. `description.ts` and `mockDisplayDescription` read `progression`, with no level code hard-coded. T-06-02 with Teràpia is `description.test.ts` › «E29: a level outside the progression joins with «+» and never ends a «… i sup.» run». Only a comment in `fixtures/catalogs.ts` changed | n/a |
| 4 | `scripts/check-parameter-keys.mjs` (in `pnpm i18n:check`), plus `e2e/core/d11-core.spec.ts` «T-02-13 E4-W06 D11 on the published core names every block and parameter (no raw keys)» | yes (`07`; core run `01`) |
| 5 #3 | `TemplatesPage.test.tsx` › «T-06-26 E4-W06 D3 edit-mode leftovers…» › «R-06-02 #3 «Treu de la plantilla» waits for the level PATCH queued before it: the DELETE never overtakes it» | yes (`06`) |
| 5 #4 | same describe › «S06 §3 #4 without the opening hours, OUTSIDE_OPENING_HOURS lands on the time the admin moved; a new band gets it without a field» | yes (`06`) |
| 5 #7 | same describe › «R-06-02 #7 a failed chip PATCH puts back only the chips: the text still being typed in «Descripció» and «Límit» stays» | yes (`06`) |

### What changed

- **Step 1 — `SettingsPage.tsx`.**
  - A new «Progressió» column (`ProgressionSwitch`): `aria-label` «Progressió: {name}», described by the help text under the table.
  - `toggleProgression` sends `PATCH /levels/{id} {progression: !current, version}`. Only that row's switch is disabled while the PATCH is pending. The row takes the saved `Level` through a functional update. A refusal shows `useCatalogError`'s message by code in a `role="alert"` under the table; `STALE_VERSION` also reloads the list.
  - `LevelForm` gets a «Progressió» switch with the help text, sent in `POST` (default `true`) and `PATCH`.
  - Keys `admin-catalogs:levels.columns.progression`, `fields.progression`, `progressionHelp` and `progressionSwitch`, in ca, es and en.
- **Step 4.**
  - Labels:
    - `admin-settings:blocks.training`, `messaging`, `privacy` and `jobs`;
    - label and help for the twelve `jobs.*` parameters, `learn.recommendationsTtlMinutes` and `learn.baseUrl`;
    - the `enum.activities.cancelDeadline` values (the core showed a raw `EVENT_START`).

    The mock-only `blocks.freeTraining`, `communications` and `privacyAudit` are removed.
  - Mock `fixtures/settings.ts`:
    - uses the core's block keys (`training`, `messaging`, `privacy`) and title strings (`admin-settings:block.<key>.title`);
    - gains the core's `jobs` block.
  - `derived-settings.ts`: the free-training row goes in `training`. On the core it never showed, because the block key was `freeTraining`; it shows now.
  - `ParameterSettings.tsx`: the «Processos automàtics» placeholder shows only when there is no `jobs` block.
  - `scripts/check-parameter-keys.mjs`:
    - It parses `CATALEG_PARAMETRES.md`: `a.b · c` shorthands, the `jobs.<nom>` list, Annex A's «Bloc D11» column, and `(CLUB…)` rows skipped.
    - It requires label and help for every catalog key that is not in the explicit, commented `systemOnly` allowlist (29 keys, the core's `system` block). It also requires every D11 block to map to a core key (`blockKeys`, which fails on an unknown block name) and every listed enum value to have a label.
    - `--list` prints the parsed keys. They match the core's 151 keys exactly (`d11-core-parameter-blocks.json`).
    - It is chained in `package.json` `i18n:check`.
- **Step 5.**
  - `TemplatesPage.tsx`:
    - `queueRemoval` chains `removeClass` and `removeBand` on `patchQueue` and bumps `patchGeneration` when the DELETE runs.
    - `bandErrorField` takes the edited band: without opening hours it blames the only time that moved, and returns `general` otherwise.
  - `ClassForm.tsx`: a failed PATCH restores only the keys of that patch.
- **Files.**
  - Product: `apps/clubs-admin/src/catalogs/{SettingsPage.tsx, ParameterSettings.tsx, derived-settings.ts}` and `apps/clubs-admin/src/planning/{TemplatesPage.tsx, ClassForm.tsx}`.
  - Mocks: `packages/api-client/src/mocks/fixtures/{settings.ts, catalogs.ts}`.
  - Locales: `packages/i18n/src/locales/*/admin-catalogs.json` and `*/admin-settings.json`.
  - Scripts: `scripts/check-parameter-keys.mjs` (new) and `package.json`.
  - Tests: `SettingsPage.test.tsx` (new), `TemplatesPage.test.tsx`, `apps/clubs-admin/e2e/settings.spec.ts` and `e2e/core/d11-core.spec.ts` (new).
  - Also `CHANGELOG.md`.

### Assumptions

1. **Where the switch lives.** The inline switch saves at once, like the spec's «interruptor» column. The level form carries the same field for creation and editing.
2. **The jobs block and the future S15 card.** The core's `jobs` block is rendered as the «Processos automàtics» card with generic rows; the S15 card with [Simula]/[Executa ara] is E5-W03. The label of each `jobs.<name>.enabled` row is the S15 process name (S15 §11: «Obertura d'inscripcions»…).
3. **Hours unknown on D3.** Without opening hours, a new band's `OUTSIDE_OPENING_HOURS` (both times sent) is shown without a field, so no hours are assumed.
4. **Test names.** The Nivells card has no T-id in S05, so its describe cites «S05 §2 D11 «Nivells»» and E29. The D3 describe carries T-06-26, and the core spec T-02-13, the D11 test id.

### Questions / catalog proposals

1. **Proposed literals** (no spec text), each in ca, es and en: `jobs.dailyTime`, `jobs.alertAdminsOnFailure`, `learn.recommendationsTtlMinutes`, `learn.baseUrl`, the helps of the ten process switches, the `activities.cancelDeadline` values («Fins a l'inici de l'activitat», «Fins al tancament de les inscripcions»), and the switch name «Progressió: {name}». Please confirm or send the literals.
2. **White label.** The help text from the spec names «Teràpia» and «D», which are the Cànic's levels, in the locales. A generic wording, or one built from the club's own levels, would avoid a club literal. It is kept as the spec gives it until you decide.
3. **`CATALEG_PARAMETRES.md` against the core** (the key sets are identical, 151 keys):
   - `learn.baseUrl` and `learn.recommendationsTtlMinutes` are «sistema» in the catalog, but the core sends them in `club`, editable by the club. They are labelled here.
   - `signup.onboardingFields` and `legal.maxPostpones` are «Alta i consentiments» in the catalog, but `system` on the core.
   - «Històric i tauler» has no «bloc» (core: `system`).

   The script's allowlist follows the core. The catalog may want updating.
4. **Accessible names of catalog row buttons.** The catalog table's row buttons are named after the row id («Edita level-t»), not its name. This is a pre-existing gap and is not changed here.

## Organizer verification
_(pending)_
