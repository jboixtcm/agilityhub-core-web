---
id: E3-W12
title: Screen 13 without /parameters — the document types from GET /me/dogs (api E3-T16), «Nivell» only when the dog carries its level, and mocks that refuse /parameters to members and impersonation like the api (from the E4-W08 report); the public footer's registered-office line (api E3-T16)
stage: E3
repo: agilityhub-core-web
thread: B
order: 87
status: awaiting_verification
depends_on: [external:api:E3-T16]
branch:
pr:
updated: 2026-09-25
---

# E3-W12 · Screen 13 without `/parameters`

## Objective
Screen 13 (`MyDogsPage` in `apps/clubs/src/SelfServicePages.tsx`) reads `census.dogDocumentTypes` and `levels.enabled` from `GET /parameters/{key}` as the member. The api allows `/parameters/*` only to a non-impersonated ADMIN (`SettingsController`, MATRIU_PERMISOS). On the real core both reads get 403, and `optionalParameter` hides the error:
- «＋ DOC.» offers only the vaccination card, so a member cannot upload the insurance or any other type (R-03-15);
- «Nivell {codi}» follows a default instead of the club's `levels.enabled` (R-03-30).

The mocks still answer members, so no test sees it. The E4-W08 executor reported it. S03 was amended on 25-09, and api E3-T16 adds `documentTypes` to `GET /me/dogs`. The organizer opens this task once E3-T16 is published. It must be done before the gate re-run E3-W09, which depends on this task.

Jordi also chose the public footer on 25-09: «{legalName} · {taxId} · {city}», as E3-W08 builds it, with the registered office below. Api E3-T16 gives `/branding` a `city` from the new `displayCity` («Cabrera de Mar») and a `legalAddress` (step 5).

**Each fix comes with a test that fails before the fix.**

## Spec references
- S03: row 13 of §2, R-03-15, R-03-30, R-03-32, §6 `GET /me/dogs`, and the change log entry of 25-09.
- `MATRIU_PERMISOS.md` (MEMBER: no `/parameters`).
- S02 R-02-02 (the public footer) and its change log entry of 25-09.

## Context to load
- `apps/clubs/src/SelfServicePages.tsx`: `optionalParameter` (near `:178`), `MyDogsPage` (near `:622`), and the document dialog (near `:536`).
- The `/parameters/{key}` and `/me/dogs` handlers in `packages/api-client/src/mocks/handlers.ts`.

## Steps
0. **Adopt the staged snapshot** (api E3-T16: `MeDogs.documentTypes`, `ClubSummary.legalAddress`), and prune the matching `pending.json` entries, as usual.
1. **The document types come from `GET /me/dogs` (R-03-15, R-03-32).** The «＋ DOC.» dialog offers `documentTypes` in their order, with their `label`. Remove the member's reads of `/parameters` and the hard-coded fallback type. Vitest: the dialog offers the club's three types with the reader's labels, and choosing «Assegurança» sends `INSURANCE`.
2. **«Nivell {codi}» only when the dog carries `level` (R-03-30).** The api leaves out `level` when `levels.enabled=false`. Vitest: a dog without `level` shows no «Nivell».
3. **The mocks refuse `/parameters/*` as the api does.**
   - MEMBER, INSTRUCTOR and impersonation tokens get 403; only a non-impersonated ADMIN reads it.
   - The mock `GET /me/dogs` returns `documentTypes`.
   - Test the mock's statuses.
   - Then search `apps/clubs` for other reads of endpoints that MATRIU_PERMISOS keeps from MEMBER. List them in the report, and fix them only when the fix stays in the web.
4. **The real core.** `scripts/e2e-core.sh` runs the E3 stage on a fresh seed only for `E3-W03`…`E3-W09` (`^E3-W0[3-9]$`). If E3-W11 has not changed that yet, stage E3 on a fresh seed for every task id (E3-W11 step 0). Then extend the real-core e2e where screen 13 is already covered: it lists the seed's document types.
5. **The footer's second line (S02 R-02-02, Jordi 25-09).** Under «{legalName} · {taxId} · {city}», the public footer shows the registered office from `/branding` `legalAddress`: «{street} · {postalCode} {city}», when it is not `null`.
   - Everywhere the public footer shows: 01, 16–19 and the other public screens.
   - The mocks carry the Cànic's values.
   - Vitest. The real-core screenshots of 01 and 16 show «Cabrera de Mar» and the office line.
6. **The mock's refused `leavePending` (E3-W08 round-2 question 3).** The core answers `400 VALIDATION_ERROR` with `fieldErrors: [{field: familyGroupClaim.leavePending, code: NOT_ALLOWED}]`. The mock answers `INVALID`. Align it, and its test.

## Fixed conventions
- Design system only.
- Keys are never literals; ca, es and en are added together.
- Mocks behave like the api.
- Heavy runs go through the host lock.
- Fictional data only.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E3-W12/*.log`)
- `pnpm turbo run lint typecheck test build`.
- `pnpm i18n:check`.
- `pnpm e2e:core E3-W12`.
- `pnpm e2e:docker`.

## Definition of Done
- Steps 1–6 are done. Steps 1–3 and 5 each have a test that fails before the fix.

## Evidence to return
- The command outputs.
- A step → test table.

---

## Executor report

**Session 2026-09-25 (claude:claude-opus-5-5).**
- Steps 0–6 are done. Steps 1, 2, 3, 5 and 6 each have tests that fail before the fix (logs `01`, `03`).
- Verifications 1, 2 and 4 pass on the final tree.
- **Verification 3 (`pnpm e2e:core E3-W12`) exits 1 in this sandbox, and only on api E3-T16's two new fields:**
  - The only core images here are `ghcr.io/jboixtcm/agilityhub-core-api:main` (revision `c9eff8f`, created 25-09 17:59Z) and an older `:local` (09-09).
  - Neither sends `legalAddress` in `/branding` or `documentTypes` in `GET /me/dogs`.
  - `docker pull` of the published image (`5b158ae`, per the organizer's 25-09 note) was denied (see Questions).
  - All 14 pre-existing real-core tests pass. The three new E3-W12 real-core tests fail, each only on its soft contract check; their other checks pass.
  - **Please re-run `pnpm e2e:core E3-W12` with the published image.**

### Step 0: the snapshot
- The staged snapshot `c9eff8f` was already adopted by E3-W11 step 0 in this same session, and it already carries E3-T16: `MeDogs.documentTypes` (required) and `ClubSummary.legalAddress`/`city`. No `openapi.next.json` is staged now, and no `pending.json` entry matches.
- The organizer's 25-09 note says E3-T16's round 2 (`5b158ae`) only touches the tax-id validation and makes `displayCity` nullable. That is the next adoption; nothing in this task depends on it.

### What changed
1. **Screen 13's document types come from `GET /me/dogs`** (R-03-15, R-03-32). `SelfServicePages.tsx`:
   - `optionalParameter`, the `Parameter` type, the `documentTypeOptions` parser and the hard-coded «Cartilla de vacunes» fallback are removed, together with its now unused key `census:myDogs.vaccinationCard` in ca/es/en.
   - The «＋ DOC.» dialog lists `data.documentTypes` by `key`/`label`, in the api's order.
   - The selected type is derived: the member's pick, else the first type.
   - [PUJA EL DOCUMENT] stays disabled without a type.
2. **«Nivell {codi}» shows when the dog carries `level`** (R-03-30). The `levelsEnabled` state and prop are gone. The chip renders on `dog.level != null`, so a `null` sent by the core also hides it.
3. **Mocks as the api** (`handlers.ts`, `fixtures/member-self-service.ts`, `scenarios.ts`):
   - `parametersForbidden` is applied to `GET /parameters`, `GET /parameters/{key}`, `…/history`, `PUT` and `DELETE`. The answer is `403 FORBIDDEN` unless the account's roles include ADMIN and the session is not an impersonation (`me.impersonation`, or the `mock-impersonation-token`). This is MATRIU_PERMISOS: «the api authorizes by the union of the JWT's roles»; an impersonation acts as MEMBER, rule 3. It replaces E4-W08's INSTRUCTOR-only rule.
   - `GET /me/dogs`:
     - lists the three types of the catalog's default (`VACCINATION_CARD`, `INSURANCE`, `OTHER`, per CATALEG_PARAMETRES), labelled in the reader's language when the club offers it, else in `defaultLocale` (R-03-32);
     - leaves `level` out of ACTIVE dogs under `levels.enabled = false` (new `memberNoLevels` scenario).
   - `POST /me/dogs/{id}/documents` accepts any catalog key (before: only two), with a `typeLabel` in the reader's language.
   - The ADMIN's `census.dogDocumentTypes` value is now the catalog's `[{key, label: {ca, es, en}, required}]`. Before, it was `{code, label}`, a shape the api never sends.
   - **Audit of `apps/clubs`:** every endpoint it calls, against the MATRIU's MEMBER column.
     - Called by member screens, and allowed to MEMBER: `/me/*`, `/signup*`, `/attachments/upload-url`, `/checkout-sessions`, `/activity-registrations*`, `/day-grid`, `/faq-entries`, `/club-pages`, `/country-profile/postal-codes/{code}`.
     - `/class-sessions/{id}`, `/ring-blocks/{id}` and `POST /ring-blocks/{id}/cancellation` are called only by screen 23 (`OverviewPage`, route `/instructor/avui`, `RequireRole ["INSTRUCTOR", "ADMIN"]`).
     - After step 1, no MEMBER-profile screen calls an endpoint the MATRIU keeps from MEMBER, so nothing else to fix.
4. **Real core** (`e2e/core`, `scripts/e2e-core.sh`):
   - E3-W11 step 0, in this session, already stages E1/E2 and then E3 on a fresh seed for every task id.
   - This task makes the script run both stages even when the first one fails, and exit with the failure. Before, a failing stage 1 hid stage 2.
   - The checks that depend on api E3-T16 are separate tests at the end of each serial file, so that a core without E3-T16 fails only them:
     - `e2-core` «E3-W12 step 4 · screen 13 reads GET /me/dogs only»: the dialog's options equal `documentTypes` (key and label, in order); `.dog-card__level` count equals the dogs carrying `level`; no `/api/v1/parameters` request; the core's answer to the member's `GET /parameters/levels.enabled` is recorded (`me-dogs-core.json`: `403 FORBIDDEN`, the same as the mock);
     - `e1-core` «E3-W12 step 5 · the public footer of 01»;
     - `e3-signup` «E3-W12 step 5 · the signup's public footer».
   - The contract checks (`toHaveProperty("legalAddress")`, `toHaveProperty("documentTypes")`) are `expect.soft`, so the rest of the test still runs.
5. **The footer's second line** (S02 R-02-02, Jordi 25-09):
   - A new `apps/clubs/src/PublicFooter.tsx` renders the access screen (01, `App.tsx`) and the signup layout (16–19 and their other pages, `SignupPage.tsx`).
   - First line: «{legalName} · {taxId} · {city}», or «{name} · {city}» without a tax id, E3-W08's rule.
   - Second line: «{street} · {postalCode} {city}», or «{street} · {postalCode}» when the office has no town, and only when `legalAddress` is not `null`.
   - Keys: new `common:publicFooter.{name, nameWithCity, withTaxId, withTaxIdAndCity, office, officeWithCity}` in ca/es/en. The old `signup:common.footer*` and `auth:access.footer*` are removed.
   - `packages/ui` `Branding.club` gains `legalAddress`. The CSS class `.public-footer__line` shows one line per block.
   - The mock `branding-canic.json` has a fictional office on Jordi's town, «Carrer de la Riera, 1 · 08392 Sant Andreu de Llavaneres». E3-W11 added it.
   - **Visible change on 01:** the access footer now follows R-02-02, «Club Agility Cànic · G00000000 · Cabrera de Mar» plus the office line in the mock. Mockup 01 shows «{club} · {city}». With a club without a tax id, 01 still reads «{name} · {city}».
6. **Refused `leavePending`:** the mock answers `fieldErrors: [{field: familyGroupClaim.leavePending, code: NOT_ALLOWED}]`, the core's code. The signup UI maps it by field, so nothing changes there.

### Step → test

| Step | Test | Before the fix |
|---|---|---|
| 1 | `apps/clubs/src/App.test.tsx` «E3-W12 … step 1: «＋ DOC.» offers the club's three types of GET /me/dogs in order, «Assegurança» sends INSURANCE, and /parameters is never read» | fails: `expected [ 'Cartilla de vacunes' ] to deeply equal [ … 3 ]` (`01`) |
| 1 | «step 1: the labels are the reader's (es), as GET /me/dogs resolves them» | fails: `[ 'Cartilla de vacunas' ]` (`01`) |
| 2 | «step 2: «Nivell {codi}» only when the dog carries its level (none without levels.enabled), without reading /parameters» | fails: 4 `/parameters` reads (`01`). The chips themselves render the same before the fix under api-like mocks: the old default was `true`, and the api omits `level` without `levels.enabled`. The defect was the member's forbidden reads. |
| 3 | `packages/api-client/src/mocks/signup-handlers.test.ts` «E3-W12 step 3: /parameters/* is for a non-impersonated ADMIN only» (member, instructor, impersonated and `dayGridEmpty` × 6 reads/writes; ADMIN, multi-profile and the impersonation token; the ADMIN's catalog map) and «E3-W12 step 3: GET /me/dogs as the api answers it» (types and language fallback; `level` only with `levels.enabled`) | 8/8 fail on the HEAD mocks (`03`) |
| 4 | real core: `e2-core` «E3-W12 step 4 …» | on `c9eff8f`: fails only on the E3-T16 contract (`documentTypes` absent); its other checks pass (`08`) |
| 5 | `App.test.tsx` «uses the exact access actions, focuses an empty email and gates signup» (01: both lines); `SignupPage.test.tsx` «E3-W12 step 5 … the registered office from /branding legalAddress, below the first line» (two lines, the office without a town, `null`) | both fail (`01`) |
| 5 | real core: `e1-core` and `e3-signup` «E3-W12 step 5 …»; mock e2e: `apps/clubs/e2e/access.spec.ts` (both lines) → `01-footer-375.png` | on `c9eff8f`: fail only on `legalAddress` absent, and the footer equals `/branding` (`08`); mock e2e green (`09`) |
| 6 | `signup-handlers.test.ts` «T-04-16 leavePending … → 400 VALIDATION_ERROR on the claim» now asserts `NOT_ALLOWED` | fails on HEAD: `INVALID` (`03`) |

- **How `03` was produced:** the HEAD versions of `handlers.ts`, `scenarios.ts`, `fixtures/member-self-service.ts`, `signup-review.ts`, `signup.ts` and `member-signup-review.json` were put in place temporarily (`git show HEAD:…`), then restored and compared with `cmp`.
- **`02-vitest-clubs-after-fix.log`:** the step-1 upload test submits its form with `fireEvent.submit`, because jsdom's constraint validation does not see a file set by `fireEvent.change`. After that change, 115/115. Log `02` was rewritten by that rerun; it is an intermediate run, not a Verification command.

### Screenshots
**Mock** (Docker e2e, 375):
- `roadmap/evidence/E3-W12/01-footer-375.png`: 01 with «Club Agility Cànic · G00000000 · Cabrera de Mar» and «Carrer de la Riera, 1 · 08392 Sant Andreu de Llavaneres» below.
- `13-document-dialog-375.png`: the «＋ DOC.» dialog with «Assegurança» chosen; the e2e checks the three options.

**Real core** (`c9eff8f`):
- `01-footer-core-375.png`, `16-footer-core-375.png` and `16-person-core-375.png` show «Club Agility Cànic · Cabrera de Mar» only. That core's `/branding` has no tax id and no `legalAddress` (`branding-club-core.json`).
- `13-document-types-core-375.png` shows an empty «Tipus», because that core sends no `documentTypes`.
- These captures need the published core. See the request at the top.

### Evidence

Earlier attempts (one line each):
- `06-e2e-core.log`: the E3-T16 checks were inside T-01-18 and T-03-42, as hard `expect`s. They crashed on the absent fields (`TypeError … reading 'city'` / `'length'`), and stage 2 never ran.
  → I made them `expect.soft`, and made the script run both stages (`07-e2e-core.log`).
- `07-e2e-core.log`: the soft failures still failed T-01-18, T-03-42 and the T-04-34 flow, and the serial files skipped the rest («8 did not run», «3 did not run»).
  → I moved the E3-T16 checks into their own last tests (`08`).
- `04-turbo.log` / `05-i18n-check.log`: green runs before the e2e restructure. Only `e2e/core` and the script changed after them, and turbo does not lint `e2e/core`, so both were run again (`10`, `11`), with `e2e/core` checked separately.

**1. `pnpm turbo run lint typecheck test build --force`**: exit 0. Log: `roadmap/evidence/E3-W12/10-turbo-final.log`. Tests: api-client 172, clubs 166, clubs-admin 258, course-core 342, ui 65, i18n 51, shared-types 21, auth 17, id 9. Separately, on the final `e2e/core`: `pnpm exec tsc -p e2e/core/tsconfig.json --noEmit` → exit 0, and `pnpm exec eslint e2e/core --max-warnings 0` → exit 0. Last 40 lines (ANSI stripped):
```
clubs:test: 
clubs:test:  Test Files  7 passed (7)
clubs:test:       Tests  166 passed (166)
clubs:test:    Start at  22:29:14
clubs:test:    Duration  10.65s (tests 38%, environment 35%, transform 16%, setup 5%, import 5%)
clubs:test: 
clubs-admin:test:  ✓ src/activities/ActivitiesPage.test.tsx (27 tests) 5893ms
clubs-admin:test:    ✓ T-07-29 D7 activities (list, maintenance, publication, cancellation) (21)
clubs-admin:test:      ✓ draws the four mockup rows with the R-07-13 dates, rings, registrations and state chips 505ms
clubs-admin:test:      ✓ «Nova activitat» creates a draft with title and type and opens its maintenance 472ms
clubs-admin:test:      ✓ shows the mockup maintenance of «Torneig d'Estiu 2026» with the ring, level and waitlist chips 324ms
clubs-admin:test:    ✓ E4-W08 D7 follow-ups of the E4-W04 round-2 review (6)
clubs-admin:test:      ✓ R-07-05 away from the club the hours cover the whole day: 6:00 when the club opens at 7:00 466ms
clubs-admin:test:  ✓ src/dashboard/SignupReviewPage.test.tsx (72 tests) 6047ms
clubs-admin:test:    ✓ T-04-33 D2 signup validation (12)
clubs-admin:test:      ✓ shows the mockup: DNI and phone in full, signed documents, consent warning, level, invoice and Stripe payment 369ms
clubs-admin:test:    ✓ E3-W07 step 4 · M15 the plan and family decisions (R-04-13, T-04-33) (4)
clubs-admin:test:      ✓ a NOT_FOUND_PENDING claim shows what the applicant typed and is resolved by attaching the holder's group 555ms
clubs-admin:test:  ✓ src/planning/CalendarPage.test.tsx (32 tests) 7638ms
clubs-admin:test:    ✓ E3-W07 step 9 a D1 risk row opens D4 on its class (1)
clubs-admin:test:      ✓ selects the class of `?classe=` in the week of `?setmana=` 537ms
clubs-admin:test:    ✓ T-06-28 D4 / D4b / D4c class calendar (front half, MSW) (24)
clubs-admin:test:      ✓ opens «Esborrany» on the first week with drafts, with the validation card and its count 604ms
clubs-admin:test:      ✓ [VALIDAR LA SETMANA] turns every draft of the week active at once 461ms
clubs-admin:test:      ✓ cancels an active class with registrants through D4c: four rows, text required, «ANUL·LA I AVISA ELS 4 ALUMNES» 367ms
clubs-admin:test:      ✓ [ELIMINA] of an active class goes through the same D4c modal 318ms
clubs-admin:test:      ✓ R-06-09 saves the diff with the version; STALE_VERSION keeps its message after the refetch remounts the card 436ms
clubs-admin:test:      ✓ R-06-09 [Crear classe]: a new time or ring after RING_HAS_BOOKINGS drops the confirmation 332ms
clubs-admin:test:      ✓ R-06-09 [Crear classe]: a RING_HAS_BOOKINGS answer that arrives after a ring change is dropped 341ms
clubs-admin:test: 
clubs-admin:test:  Test Files  19 passed (19)
clubs-admin:test:       Tests  258 passed (258)
clubs-admin:test:    Start at  22:29:14
clubs-admin:test:    Duration  12.30s (tests 38%, environment 34%, transform 17%, import 6%, setup 6%)
clubs-admin:test: 

 Tasks:    34 successful, 34 total
Cached:    0 cached, 34 total
  Time:    23.759s
```

**2. `pnpm i18n:check`**: exit 0. Complete output (`11-i18n-check-final.log`); one key fewer than E3-W11's run: the 6 new ones, less the 7 removed.
```
> agilityhub-core-web@0.0.0 i18n:check /Users/jordib/dev/agilityhub/agilityhub-core-web
> node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs

i18n check passed
ca: 2328 keys
es: 2328 keys
en: 2328 keys
1752 statically used keys, 0 unused
250 codes, 0 missing
```

**3. `/Users/jordib/Dropbox/Documents/SOFTWARE_CANIC/05-desenvolupament/roadmap-kit/mac/heavy.sh pnpm e2e:core E3-W12 > roadmap/evidence/E3-W12/08-e2e-core.log 2>&1`**: **exit 1**, on the local core `c9eff8f` (no E3-T16).
- The lock waited 1 min 0 s for the api's `bin/e3-smoke`.
- Stage 1: 10 passed, and 2 failed: the new `e1-core` and `e2-core` E3-W12 tests. Their only errors are the soft «/branding club.legalAddress (api E3-T16)», «GET /me/dogs.documentTypes (api E3-T16)» and «the seed's census.dogDocumentTypes» (log lines 132–239).
- Stage 2 (fresh seed): 4 passed, and 1 failed: the new `e3-signup` footer test, on `legalAddress` absent. The N-37 check passed.
- Data: `branding-club-core.json` has no `legalAddress`, with `taxId: null`; `me-dogs-core.json` has `documentTypes: "absent"`, levels `CAD`/`E`, and the member's `/parameters` → `403 FORBIDDEN`.
- Last 40 lines:
```
    expect(received).toHaveProperty(path)

    Expected path: "legalAddress"
    Received path: []

    Received value: {"city": "Cabrera de Mar", "legalName": null, "name": "Club Agility Cànic", "slug": "canic", "taxId": null}

       at public-footer.ts:41

      39 | export async function expectPublicFooter(footer: Locator, club: BrandingClub): Promise<void> {
      40 |   // Soft: a core older than api E3-T16 fails here, and the rest of the flow still runs.
    > 41 |   expect.soft(club, "/branding club.legalAddress (api E3-T16)").toHaveProperty("legalAddress");
         |                                                                 ^
      42 |   await expect(footer.locator(".public-footer__line")).toHaveText(publicFooterLines(club));
      43 | }
      44 |
        at expectPublicFooter (/work/e2e/core/public-footer.ts:41:65)
        at /work/e2e/core/e3-signup.spec.ts:1279:27

    attachment #1: oauth-token-calls (text/plain) ──────────────────────────────────────────────────
    2026-09-25T20:27:29.976Z grant_type=refresh_token status=400 cookie=none test="E3-W12 step 5 · the signup's public footer shows /branding's identity and registered office (S02 R-02-02)" body={"code":"REFRESH_EXPIRED"}
    ────────────────────────────────────────────────────────────────────────────────────────────────

    Error Context: test-results/core-e1/e3-signup-E3-W12-step-5-·--cf9f2-istered-office-S02-R-02-02-/error-context.md

    attachment #3: trace (application/zip) ─────────────────────────────────────────────────────────
    test-results/core-e1/e3-signup-E3-W12-step-5-·--cf9f2-istered-office-S02-R-02-02-/trace.zip
    Usage:

        pnpm exec playwright show-trace test-results/core-e1/e3-signup-E3-W12-step-5-·--cf9f2-istered-office-S02-R-02-02-/trace.zip

    ────────────────────────────────────────────────────────────────────────────────────────────────


  1 failed
    e2e/core/e3-signup.spec.ts:1271:5 › E3-W12 step 5 · the signup's public footer shows /branding's identity and registered office (S02 R-02-02) 
  4 passed (20.9s)

{"action":"OPEN_DOG","channel":"APP","code":"N-37","dogName":"Neret E3","status":"SENT"}
 ELIFECYCLE  Command failed with exit code 1.
```

**4. `/Users/jordib/Dropbox/Documents/SOFTWARE_CANIC/05-desenvolupament/roadmap-kit/mac/heavy.sh pnpm e2e:docker > roadmap/evidence/E3-W12/09-e2e-docker.log 2>&1`**: exit 0. The complete suite in `mcr.microsoft.com/playwright:v1.63.0-noble`: id 1, clubs-admin 39, clubs 29, with 0 cached and no flaky test. The evidence was copied back for E3-W12. Last 40 lines (ANSI stripped):
```
clubs:build:   dist/sw.js
clubs:build:   dist/workbox-9c191d2f.js
id:e2e: cache bypass, force executing 0f53052c835867aa
id:e2e: 
id:e2e: > id@0.0.0 e2e /work/apps/id
id:e2e: > playwright test
id:e2e: 
id:e2e: 
id:e2e: Running 1 test using 1 worker
id:e2e: ·
id:e2e:   1 passed (3.0s)
clubs-admin:e2e: cache bypass, force executing 04ad6e433b3f7ed0
clubs-admin:e2e: 
clubs-admin:e2e: > clubs-admin@0.0.0 e2e /work/apps/clubs-admin
clubs-admin:e2e: > playwright test
clubs-admin:e2e: 
clubs-admin:e2e: [WebServer] (node:897) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs-admin:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs-admin:e2e: 
clubs-admin:e2e: Running 39 tests using 9 workers
clubs-admin:e2e: ·······································
clubs-admin:e2e:   39 passed (22.9s)
clubs:e2e: cache bypass, force executing 7a21cf86c6b84d4c
clubs:e2e: 
clubs:e2e: > clubs@0.0.0 e2e /work/apps/clubs
clubs:e2e: > playwright test
clubs:e2e: 
clubs:e2e: [WebServer] (node:2943) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs:e2e: 
clubs:e2e: Running 29 tests using 7 workers
clubs:e2e: ·····························
clubs:e2e:   29 passed (12.8s)

 Tasks:    8 successful, 8 total
Cached:    0 cached, 8 total
  Time:    50.829s 

evidence: E3-W12/01-footer-375.png
evidence: E3-W12/13-document-dialog-375.png
```

### Assumptions
1. **`/parameters` rule in the mocks:** the roles, not the active profile, decide (MATRIU line 3). A multi-profile account with ADMIN reads `/parameters` even while it acts as a member. The code of the 403 is `FORBIDDEN` for a MEMBER, the same as the real core's answer (`me-dogs-core.json`). For an impersonation the mock also answers `FORBIDDEN`, not `IMPERSONATION_DENIED`, because the api treats the token as MEMBER (rule 3). This case was not checked on the real core.
2. **Mock e2e evidence:** the mock e2e adds two captures into `roadmap/evidence/E3-W12/`. The real-core captures of 01 and 16 were taken, but on a core without E3-T16.
3. **The document types key:** screen 13 keeps no fallback type. A core without `documentTypes` shows an empty «Tipus» and a disabled upload, rather than inventing a type.

### Questions / proposals
1. **Denied command:** `docker pull ghcr.io/jboixtcm/agilityhub-core-api:main`. It was needed so that `pnpm e2e:core E3-W12` runs against api E3-T16 (`5b158ae`). **@organizer:** please re-run `pnpm e2e:core E3-W12` on the published image. On a core with E3-T16, the three E3-W12 real-core tests check the seed's document types, the tax id, the office line on 01 and 16, and the member's `403`.
2. **api (from E3-W11):** the snapshot's nullable references (`{$ref, type: [object, null]}`) refuse `null` under JSON Schema 2020-12. openapi-typescript drops that `null` too, so the ui `Branding` type declares `legalAddress` by hand.
3. No new parameter, event, notification or error code. `NOT_ALLOWED` is a `fieldErrors` code the core already sends.

### Notes
- Concurrent edits in the working tree (MESSAGES, E3-W08, gate-E3, S02) were left untouched.
- E3-W11 ran earlier in this session and is `awaiting_verification`. Both tasks share `scripts/e2e-core.sh`, `handlers.ts` and `signup-handlers.test.ts`, and this report states which change belongs to which task.

## Organizer verification
_(pending)_
