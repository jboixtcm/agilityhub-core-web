---
id: E4-W15
title: Follow-ups of the E4-W12 review and adoption of api E5-T22 (web) — 17's price label and add-dog plan line on the real core, a member without a plan chooses one, «DNI/NIE» in English, no therapy intro in add-dog mode, «manteniment» whole at 375 px, «/classe» prices, an ICU key for the all-rings cell, a generic overlay description
stage: E4
repo: agilityhub-core-web
thread: B
order: 58
status: not_open
depends_on: [external:api:E5-T22]
branch:
pr:
updated: 2026-09-26
---

# E4-W15 · Follow-ups of the E4-W12 review, and api E5-T22

## Objective
E4-W12 fixed screen 17 in MSW, but the real core did not send the data of its steps 3 and 4. Api E5-T22 adds `priceLabel` and the member's own plan to `GET /signup`, and makes a member without a plan choose one. The independent review of E4-W12 (`roadmap/reviews/E4-W12-20260926-0333-claude.md`) also found minors that the organizer confirmed. The organizer opens this task once api E5-T22 is verified and its snapshot is staged.

**Each fix comes with a test that fails before the fix.**

## Spec references
- S04: §6 `GET /signup` (`plans[].priceLabel`, `plans[].current`; amended 26-09), R-04-09 (the add-dog offer; amended 26-09), T-04-32.
- S05 R-05-19 (the price line: «{price}/mes», «{price}/classe», `priceLabel` without a price).
- AGENTS rules 1 (ICU keys), 2 (no club literal outside branding and fixtures) and 6 (nothing clipped at the design widths).

## Context to load
- The E4-W12 review, report and organizer verification; the api E5-T22 report.
- `apps/clubs/src/SignupPage.tsx` (the plan cards, `planPrice` near `:1302`, the add-dog branch near `:2613`, `therapyIntro`).
- `packages/i18n/src/locales/en/admin-census.json` (near `:251`), `SignupReviewPage.test.tsx`.
- `packages/ui/src/day-grid.css` (near `:63`–`:71`), `apps/clubs-admin/src/planning/CalendarPage.tsx` (near `:577`).
- `packages/api-client/openapi/pending.json` (near `:55`).

## Steps
0. **Adopt the staged snapshot** (api E5-T22: `plans[].priceLabel`, `plans[].current`, and the lists' sparse `fields`). Prune the `SignupPlan.priceLabel` overlay from `pending.json`.
1. **17 on the real core (E4-W12 steps 3 and 4).**
   - The Teràpia card shows the core's `priceLabel`.
   - In add-dog mode, the plan line reads the plan marked `current`.
   - Extend the real-core e2e with both.
2. **A member without a plan (E4-W12 review #5, R-04-09).** In add-dog mode, when no plan is `current`, 17 shows the offered cards, and the member must pick one: [CONTINUA] stays disabled until then, and `planIdRequested` is sent. Vitest.
3. **English «DNI/NIE» (review #1).** In English, D2 reads «DNI/NIE» for a DNI or NIE, as screen 16 does. First check that no other D2 label reuses the key with a generic meaning. Fix the test row.
4. **No therapy intro in add-dog mode (review #2).** `texts.therapyIntro` is hidden in add-dog mode, where the plan cannot change. Vitest.
5. **«manteniment» whole (review #3).** In the 375 px cells of 10 and 23, «manteniment» fits whole, as the mockups show. Use the design tokens (a smaller subtitle size, or tighter spacing), not an ellipsis. Captures at 375.
6. **`SINGLE_CLASS` prices (review #4, R-05-19).** A `SINGLE_CLASS` plan reads «{price}/classe» (new key `signup:dog.singleClassPrice`, in ca, es and en), on the cards and on the add-dog line. Vitest.
7. **Keys and literals (reviews #7 and #8).**
   - The accessible name of the all-rings D4 cell comes from one ICU key (`calendar.cell.activityAllRings`, «Activitat · {title} · totes les pistes»), in ca, es and en.
   - The `pending.json` overlay description names no club text.

## Fixed conventions
- Design system only.
- Keys are never literals; ca, es and en are added together.
- Mocks behave like the api.
- Heavy runs go through the host lock.
- Fictional data only.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E4-W15/*.log`)
- `pnpm turbo run lint typecheck test build`.
- `pnpm i18n:check`.
- `pnpm e2e:core E4-W15`.
- `pnpm e2e:docker`.

## Definition of Done
- Steps 0–7 are done. Steps 1–7 each have a test that fails before the fix (step 5: a capture and a test on the cell's class).

## Evidence to return
- The command outputs.
- A step → test table.
- The captures of 17 (Teràpia, add-dog) on the real core, and of 10 and 23 at 375.

---

## Executor report
_(pending)_

## Organizer verification
_(pending)_
