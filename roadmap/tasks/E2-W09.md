---
id: E2-W09
title: D11 «Pàgines del club» editor (rules, privacy, image consent, welcome guide) + screen 30 «Info» tabs (ClubPage contract of api E2-T12)
stage: E2
repo: agilityhub-core-web
thread: B
order: 65
status: verified
depends_on: [E2-W05, external:api:E2-T12]
branch:
pr:
updated: 2026-09-09
---

# E2-W09 · D11 «Pàgines del club» editor (rules, privacy, image consent, welcome guide) + screen 30 «Info» tabs (ClubPage contract of api E2-T12)

## Objective
The club maintains its own texts from D11 and the students read them in the app «Info» page; the public signup links the rules page. Replaces the disabled «Normes del club» link of E2-W05.

## Spec references
S05 §6 rows `/club-pages*` and `/public/{clubSlug}/pages/{key}` · S05 §11 **T-05-CP-07** (front) · S05 change log 05-09 (`ClubPage`, «editor per idioma + vista prèvia») · R-05-21b (provisional marker text «[Text pendent — el club l'omplirà des de Paràmetres]» stays visible until the club edits it) · S11 change log 05-09 (screen 30 «Info» tabs FAQ · Normes · other active pages) · S04 change log 05-09 (screen 19 link «Normes del club: consulta-les aquí», no checkbox — the signup screen itself is E3) · mockup `docs/pantalles/escriptori/D11-*.png` (card «Pàgines del club» next to the FAQ card), `docs/pantalles/mobil/30-*.png` («Info»).

## Context to load
S05 §6 (the three `club-pages` rows) · the FAQ card/editor of E2-W04 (`apps/clubs-admin/src/catalogs/*`) and the D11 card of E2-W05 · `apps/clubs` screen 30 «Info» (FAQ accordion) · the api snapshot with `ClubPage` (`GET/POST/PATCH /api/v1/club-pages`, `GET /api/v1/public/{clubSlug}/pages/{key}`) · this file.

## Steps
0. **Contract (organizer 09-09 21:10)**: the staged `packages/api-client/openapi/openapi.next.json` is the api snapshot of `4e3657e` (E2-T06 census: `Dog.handlerName`, `License {category, division}`, `PlanReference.billingMode`, `DogTasks`, `POST /attachments*`; E2-T08: `POST /{members,dogs}/export`, `GET /exports/{id}/download`; E2-T12: `GET/POST/PATCH /club-pages*`, `GET /public/{clubSlug}/pages/{key}` — 10 new operations, none redefined by `pending.json`). First step: swap it into `openapi.json` byte for byte, prune `pending.json` of anything now published, regenerate, fix consumers/fixtures, run the Ajv suite — then build the editor. `ClubPage {key, title: LocalizedText, body: LocalizedText (limited Markdown), version, publishedAt, active, lastChange, history?[] (ADMIN only)}`. MSW fixtures: the three Cànic seed pages (`RULES` active with the marker + a short fictional body, `IMAGE_CONSENT` active, `PRIVACY` inactive draft), `STALE_VERSION` on a stale `PATCH`.
1. **D11 card «Pàgines del club»** (clubs-admin, `/parametres`, ADMIN): list of pages with title (current locale), status chip («publicada» · «esborrany»), `versió n · dd/mm` and a «+ Pàgina» action for a free key (`[a-z0-9-]{2,40}`, unique; the four fixed keys are always listed even when they do not exist yet, as «sense contingut»). Row → **editor drawer/page** with per-locale tabs (club locales), Markdown textarea with a rendered **preview** (same limited Markdown rules as the API: headings, paragraphs, bold/italic, lists, links — the preview strips anything else), title per locale, [Desa l'esborrany] (`PATCH` without activating), [Publica] (`active: true`; confirmation «Es publicarà la versió n+1 — els consentiments futurs hi faran referència» when `key ∈ {RULES, PRIVACY, IMAGE_CONSENT}`), [Desactiva] for free pages only (the four fixed keys cannot be deactivated from the UI once published, only edited). Errors: `STALE_VERSION` → «Un altre administrador ha editat aquesta pàgina — recarrega», `VALIDATION_ERROR` field errors under the textarea. Pages whose body still contains the R-05-21b marker show an inline warning «Text provisional — pendent d'omplir» in the list and in the editor.
2. **Screen 30 «Info» (clubs app)**: tabs FAQ · Normes (`RULES`) · one tab per other **active** page in `GET /club-pages?active=true` (title in the member's locale, fallback to the club default), rendered Markdown, «Actualitzat el dd/mm/yyyy» from `publishedAt`; FAQ tab unchanged. The «Normes» tab is hidden when `RULES` is not active.
3. **Links**: the E2-W05 disabled «Normes del club» link becomes the row action of the card; `apps/clubs-admin` sidebar/search entries as for FAQ. The public route (`/public/{clubSlug}/pages/{key}`) is consumed by the signup screen in E3 — expose a typed client function now, no screen.
4. i18n keys ca/es/en for every literal (no literal in code; `i18n:check` green); the marker text itself comes from the API body (never from the front).

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `pnpm turbo run lint typecheck test build` + `pnpm i18n:check`; Vitest for the versioning confirmation, the marker warning, the tabs of screen 30 (active pages only) and the `STALE_VERSION` mapping; Playwright T-05-CP-07 (D11 card → editor → publish → the «Normes» tab appears in the app); screenshots at 1280 px (D11 card, editor with preview) and 390 px (screen 30 «Info») under `roadmap/evidence/E2-W09/`.

## Definition of Done
Pages editable per locale with preview and versioned publication from D11; students see FAQ · Normes · other active pages in «Info»; contract adopted from the api snapshot (or mocks-first with Ajv fixtures).

## Evidence to return
Outputs and screenshots.

---

## Executor report

### Summary

- Adopted the task-pinned `openapi.next.json` byte-for-byte as `openapi.json` (both SHA-256 `072a26cb227823605c3958559b84679e57d97d528efe6d3947bcb6010324991f`), pruned the now-published attachment upload overlay, regenerated the client, and aligned consumers and Ajv fixtures with the published E2 contract. A newer snapshot staged concurrently for E2-W06 remains untouched as `openapi.next.json`.
- Replaced the disabled D11 club-rules placeholder with an ADMIN ClubPage card/editor: four fixed rows, unique free keys, locale tabs, limited-Markdown preview, draft/publish/deactivate actions, consent-version confirmation, provisional-copy warning, and specific validation/concurrency errors.
- Added the authenticated mobile Info screen with the unchanged FAQ accordion plus `RULES` and all other active ClubPages, localized fallback, safe Markdown, and publication dates. Info remains available when FAQ is disabled; only its FAQ tab/request is module-gated.
- Added the typed public ClubPage reader (explicit API-key and locale headers), versioned/validated MSW handlers and three seed pages, three-locale copy, component tests, and cross-app Playwright evidence.

### Files changed

- Contract/client: `packages/api-client/openapi/openapi.json`, pruned `pending.json`, regenerated `src/generated/schema.d.ts`, added `src/club-pages.ts` and its test, and preserved an explicit public `Accept-Language` header in `src/client.ts`.
- API mocks/contract adoption: `packages/api-client/src/mocks/fixtures/club-pages.json`, `fixtures/catalogs.ts`, `fixtures/member-self-service.ts`, `fixtures.contract.test.ts`, `mocks/handlers.ts`, `src/index.ts`, and `src/index.test.ts`.
- Contract-drift consumers: `apps/clubs/src/SelfServicePages.tsx`, `apps/clubs-admin/src/census/CensusRecordPage.tsx`, and `apps/clubs-admin/src/catalogs/PlansPage.tsx`.
- D11: `apps/clubs-admin/src/catalogs/ClubPagesCard.tsx`, `ClubPagesCard.test.tsx`, `ParameterSettings.tsx`, `src/styles.css`, `e2e/club-pages.spec.ts`, and the superseded assertion in `e2e/settings.spec.ts`.
- Screen 30: `apps/clubs/src/InfoPage.tsx`, `InfoPage.test.tsx`, `App.tsx`, `src/styles.css`, and `packages/ui/src/modules.ts`.
- Shared renderer/i18n: `packages/ui/src/limited-markdown.tsx`, its test and export; `packages/i18n/src/locales/{ca,es,en}/{admin-settings,shell}.json`.
- Delivery: `CHANGELOG.md` and `roadmap/evidence/E2-W09/*`.

### Rules and tests implemented

- R-05-21b: API-supplied provisional marker is rendered unchanged and detected for the inline D11 warning; no frontend locale defines the marker.
- T-05-CP-01 mock semantics: inactive content edits keep their version; activation or active-body edits increment it, update `publishedAt`, and retain up to ten published snapshots.
- T-05-CP-03/04: typed public projection with locale header, active-only public lookup, limited Markdown validation, field errors, and optimistic-lock `STALE_VERSION` behavior.
- T-05-CP-07: Vitest covers provisional warnings, safe preview, consent version `n+1`, specific stale-version copy, active-only Info tabs, FAQ module gating, and the FAQ accordion. Playwright covers D11 card → editor/preview → publish → mobile Normes tab.
- Accessibility: labeled fields, tabs and dialogs; a single expanded FAQ item; status/alert semantics; keyboard-native row buttons; horizontal tab scrolling at 390 px.

### Evidence

Earlier combined-gate attempt: `env COREPACK_HOME=/private/tmp/e2w09-corepack pnpm turbo run lint typecheck test build` exited 1 on the new public-client test's local import order; reordered the imports. Complete output: `roadmap/evidence/E2-W09/02-turbo-failed.log`.

#### Contract generation

Command: `env COREPACK_HOME=/private/tmp/e2w09-corepack pnpm api:generate`

Exit code: `0`

Complete output: `roadmap/evidence/E2-W09/07-api-generate.log`.

```text

> agilityhub-core-web@0.0.0 api:generate /Users/jordib/dev/agilityhub/agilityhub-core-web
> pnpm --filter @agilityhub/api-client api:generate


> @agilityhub/api-client@0.0.0 api:generate /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client
> node scripts/generate.mjs

Generated /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client/src/generated/schema.d.ts from /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client/openapi/openapi.json merged with /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client/openapi/pending.json
```

Command: `shasum -a 256 packages/api-client/openapi/openapi.json && git show 059c818:packages/api-client/openapi/openapi.next.json | shasum -a 256 && shasum -a 256 packages/api-client/openapi/openapi.next.json`

Exit code: `0`

```text
072a26cb227823605c3958559b84679e57d97d528efe6d3947bcb6010324991f  packages/api-client/openapi/openapi.json
072a26cb227823605c3958559b84679e57d97d528efe6d3947bcb6010324991f  -
7fd5dbdef253805ee96aed32a2b0bd230dc5daed71caee0d3186d80a53d71d78  packages/api-client/openapi/openapi.next.json
```

#### Repository gate

Command: `env COREPACK_HOME=/private/tmp/e2w09-corepack pnpm turbo run lint typecheck test build`

Exit code: `0`

Complete output: `roadmap/evidence/E2-W09/03-turbo.log` (518 lines). Last 40 lines:

```text
clubs-admin:test: stderr | src/census/CensusRecordPage.test.tsx > T-03-38 dog record > deactivates and reactivates a dog without deleting its record
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test:
clubs:test: stderr | src/App.test.tsx > T-03-40 mobile own dogs > renders own dogs, saves the note, shows task totals and gates TASKS
clubs:test: [BrandingProvider] Club Mínim: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs:test:
clubs:test: stderr | src/App.test.tsx > T-03-41 mobile own data > keeps identity read-only, resolves towns, maps READ_ONLY and gates billing
clubs:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs:test:
clubs-admin:test: stderr | src/census/CensusListPage.test.tsx > T-03-42 D15 translated dog list > renders the approved columns and dog record links
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test:
clubs:test: stderr | src/App.test.tsx > T-03-41 mobile own data > keeps identity read-only, resolves towns, maps READ_ONLY and gates billing
clubs:test: [BrandingProvider] Club Mínim: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs:test:
clubs:test:  ✓ src/App.test.tsx (17 tests) 1075ms
clubs:test:
clubs:test:  Test Files  2 passed (2)
clubs:test:       Tests  19 passed (19)
clubs:test:    Start at  21:50:54
clubs:test:    Duration  2.65s (tests 31%, environment 27%, transform 25%, import 9%, setup 8%)
clubs:test:
clubs-admin:test:  ✓ src/census/CensusRecordPage.test.tsx (5 tests) 1005ms
clubs-admin:test:  ✓ src/census/CensusListPage.test.tsx (3 tests) 1703ms
clubs-admin:test:    ✓ T-03-38 D5 universal member list (2)
clubs-admin:test:      ✓ renders applied filters and preserves selection when page size changes 529ms
clubs-admin:test:      ✓ toggles visible columns and synchronizes the ordered fields to the URL 319ms
clubs-admin:test:    ✓ T-03-42 D15 translated dog list (1)
clubs-admin:test:      ✓ renders the approved columns and dog record links 852ms
clubs-admin:test:
clubs-admin:test:  Test Files  5 passed (5)
clubs-admin:test:       Tests  20 passed (20)
clubs-admin:test:    Start at  21:50:55
clubs-admin:test:    Duration  3.19s (tests 35%, environment 31%, transform 19%, setup 7%, import 7%, worker 1%)
clubs-admin:test:

 Tasks:    26 successful, 26 total
Cached:    9 cached, 26 total
  Time:    9.339s
```

#### i18n

Command: `env COREPACK_HOME=/private/tmp/e2w09-corepack pnpm i18n:check`

Exit code: `0`

Complete output: `roadmap/evidence/E2-W09/04-i18n.log`.

```text

> agilityhub-core-web@0.0.0 i18n:check /Users/jordib/dev/agilityhub/agilityhub-core-web
> node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs

i18n check passed
ca: 1437 keys
es: 1437 keys
en: 1437 keys
944 statically used keys, 0 unused
245 codes, 0 missing
```

#### Targeted Vitest

Command: `env COREPACK_HOME=/private/tmp/e2w09-corepack pnpm --filter clubs-admin exec vitest run src/catalogs/ClubPagesCard.test.tsx`

Exit code: `0`

Complete output: `roadmap/evidence/E2-W09/05-admin-club-pages-vitest.log`.

```text

 RUN  v5.0.0 /Users/jordib/dev/agilityhub/agilityhub-core-web/apps/clubs-admin


 Test Files  1 passed (1)
      Tests  2 passed (2)
   Start at  21:51:34
   Duration  761ms (environment 35%, tests 27%, transform 20%, import 11%, setup 7%)
```

Command: `env COREPACK_HOME=/private/tmp/e2w09-corepack pnpm --filter clubs exec vitest run src/InfoPage.test.tsx`

Exit code: `0`

Complete output: `roadmap/evidence/E2-W09/06-info-vitest.log`.

```text

 RUN  v5.0.0 /Users/jordib/dev/agilityhub/agilityhub-core-web/apps/clubs


 Test Files  1 passed (1)
      Tests  2 passed (2)
   Start at  21:51:35
   Duration  679ms (environment 39%, transform 21%, tests 19%, import 12%, setup 8%)
```

#### Playwright T-05-CP-07

Command (inside the Docker recipe from `scripts/e2e-docker.sh`, Playwright image `mcr.microsoft.com/playwright:v1.63.0-noble`): `pnpm --filter clubs-admin exec playwright test e2e/club-pages.spec.ts --workers=1`

Exit code: `0`

Complete output: `roadmap/evidence/E2-W09/01-playwright.log` (67 lines). Last 40 lines:

```text
.../esbuild@0.25.12/node_modules/esbuild postinstall: Done

devDependencies:
+ @commitlint/cli 21.2.2
+ @commitlint/config-conventional 21.2.2
+ @eslint/css 2.0.0
+ @eslint/js 9.39.5
+ @playwright/test 1.63.0
+ @size-limit/file 13.0.3
+ @types/node 24.13.3
+ eslint 9.39.5
+ eslint-import-resolver-typescript 4.4.5
+ eslint-plugin-import 2.32.0
+ eslint-plugin-jsx-a11y 6.10.2
+ eslint-plugin-react 7.37.5
+ eslint-plugin-react-hooks 7.1.1
+ globals 17.12.0
+ husky 9.1.7
+ lint-staged 17.5.0
+ msw 2.15.0
+ prettier 3.9.6
+ size-limit 13.0.3
+ turbo 2.10.12
+ typescript 5.9.3
+ typescript-eslint 8.69.0

. prepare$ node packages/config/scripts/prepare-husky.mjs
. prepare: .git can't be found
. prepare: Done
Done in 12.7s using pnpm v9.15.9
.                                        |  WARN  Unsupported engine: wanted: {"node":">=22 <23"} (current: {"node":"v24.20.0","pnpm":"9.15.9"})
(node:104) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
(Use `node --trace-deprecation ...` to show where the warning was created)
[WebServer] (node:214) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)

Running 1 test using 1 worker
·
  1 passed (4.0s)
```

Screenshots (visually checked against the D11/screen-30 references; correct runtime branding, no clipping or horizontal overflow):

- `roadmap/evidence/E2-W09/D11-club-pages-1280.png` — 1280 × 3159.
- `roadmap/evidence/E2-W09/D11-club-page-editor-preview-1280.png` — 1280 × 3159.
- `roadmap/evidence/E2-W09/30-info-390.png` — 390 × 844.

### Assumptions

- D11 currently has one shell navigation/search destination for `/parametres`; FAQ is an anchored card rather than a separate sidebar item. Club pages follow that established pattern at `#club-pages`, and the disabled rules row is replaced by the interactive card.
- `/info` is not gated by `FAQ`, because ClubPages have no module; only the FAQ tab and request are gated. This keeps active rules/pages reachable when FAQ is off.
- The public helper takes the signup surface's public API key as input; E3 owns the public signup screen and its runtime key source.
- During verification, organizer commit `a21894b` staged a newer API snapshot (`a794c7f`) for E2-W06. It does not replace E2-W09's task-pinned `4e3657e` contract; it is preserved unmodified in the final tree as `openapi.next.json`.
- The standard Docker helper's first broad all-app run hit unrelated parallel Playwright timeouts while T-05-CP-07 passed. The required scenario was rerun alone with one worker and passed; the repository unit/build gate is fully green.

### Questions and catalog proposals

- None. No frontend parameter, event, notification, or error code was introduced.

## Organizer verification
Result: verified · 2026-09-09 · organizer (Claude)

Checked: report + `roadmap/evidence/E2-W09/` (repository gate, i18n 1437 keys, targeted Vitest for the card and the Info page, Playwright T-05-CP-07 in Docker, screenshots) · `git show 415cb2c` (42 files: snapshot `4e3657e` adopted byte for byte with `pending.json` pruned of the published attachment overlay; `packages/api-client/src/club-pages.ts` typed public reader; `packages/ui/src/limited-markdown.tsx` shared renderer; D11 `ClubPagesCard` with the four fixed keys + free keys, locale tabs, preview, draft/publish/deactivate, consent-version confirmation, provisional-copy warning, `STALE_VERSION`/`VALIDATION_ERROR` mapping; screen 30 `InfoPage` with FAQ · Normes · other active pages, FAQ tab module-gated only) · screenshots inspected: `D11-club-pages-1280.png`/`D11-club-page-editor-preview-1280.png` (editor with CA/ES tabs, textarea + «Vista prèvia», «Text provisional — pendent d'omplir», [Desa l'esborrany]/[Publica]) and `30-info-390.png` (tabs FAQ · Normes · Consentiment d'imatge, marker text, «Actualitzat el 09/09/2026») · independent run of `415cb2c` in the organizer sandbox: 26/26 tasks, i18n 0 missing · **CI on `main` green at `415cb2c`**.

Accepted assumptions: club pages anchored in `/parametres#club-pages` like the FAQ card; `/info` not gated by `FAQ` (only its tab); public helper takes the API key from the E3 signup surface; the newer staged snapshot (`a794c7f`) left untouched for E2-W06.

Polish carried to E2-W07 step 4 (e): the shared limited-Markdown renderer emits headings/bold but the content styles do not visibly differentiate them in the editor preview nor in «Info» (screenshots show «Convivència» as plain text) — style `h1–h3`, `strong`, lists inside `.ah-markdown` in both apps.
