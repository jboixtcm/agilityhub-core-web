---
id: E4-W13
title: D2 for the reused dog of a readmission (api E3-T17 and E5-T19) — the dog's old and new values, its submitted documents edited through PATCH /dogs/{id} by type and by file, no calls to the frozen routes (409 READMISSION_PENDING), and file removal on public-signup dogs
stage: E4
repo: agilityhub-core-web
thread: B
order: 56
status: awaiting_verification
depends_on: [external:api:E5-T19]
branch:
pr:
updated: 2026-09-26
---

# E4-W13 · D2 for the reused dog of a readmission

## Objective
Api E3-T17 extended E38 to the dog of a readmission. The reused dog keeps its record until validation, and the submitted values and documents wait in the request. While the readmission is pending, the routes that would write the dog's record answer `409 INVALID_STATE` with `details.reason = READMISSION_PENDING`. D2's edit drawer still uses some of those routes for documents. Api E5-T19 adds each file's `fileKey` to D2's view and a plain id for signup files, so D2 can add or remove one file. This task adopts both. The organizer opens it once api E5-T19 is verified and its snapshot is staged.

**Each fix comes with a test that fails before the fix.**

## Spec references
- S04: R-04-06 (the reused dog, amended 25-09 and 26-09; (b) the chip), R-04-19 (D2 edits), R-04-23 (rejection), T-04-12, T-04-19.
- `DECISIONS_PENDENTS.md` E38.

## Context to load
- The api E3-T17 report and organizer verification (both rounds), and the api E5-T19 report.
- `apps/clubs-admin/src/dashboard/SignupReviewPage.tsx` (the readmission block «Canvis respecte de la fitxa de baixa»), `SignupEditDrawer.tsx` (documents near `:214`–`:300`, the dog PATCH near `:474`).
- The D2 and dog handlers in `packages/api-client/src/mocks/`.

## Steps
0. **Adopt the staged snapshot:** E3-T17 (`SignupDogView.readmission`, `SignupDogReadmission`, `SignupDogValues`, the documented 409s) and E5-T19 (`files[].fileKey`, the plain file ids). Prune the matching `pending.json` entries. The kit stages the latest api snapshot at session start, so it also carries api E5-T20, and maybe E5-T21. Adopt all of it:
   - D7's two lists are sparse with `fields`: only the row id is required. The D7 activity mock answers sparse items, as the api does (E4-W12 question 3).
   - D7 and the app show `waitlistRank` («En llista d'espera · 1r») where they showed `position` (E4-W05 question 2). Vitest after a promotion.
   - `fields` sends only keys from `x-fields`.
   - D11 shows `signup.onboardingFields` and `legal.maxPostpones` under «Alta i consentiments», and no longer the learn keys.
   
   If the staged snapshot still lacks E5-T19 (`SignupDocumentFile.fileKey`), stop and say so, as before.
1. **D2 shows the reused dog's changes (R-04-06).** For a dog with `readmission`, the readmission block lists each field of `changedFields` with «Abans» (`readmission.current`) and «Ara» (the view), as it does for the member. `documents` shows the old and the new document rows. Vitest.
2. **The edit drawer for a reused dog (R-04-19).**
   - Its values and documents change only through `PATCH /dogs/{id}`: replace a type, add one file (the upload plus the kept `fileKey`s), or remove one file (send the kept ones).
   - The drawer never calls the frozen routes for that dog: `POST /dogs/{id}/documents`, `DELETE …/files/{fileId}`, the photo route, and `handlerName`/`licenses`.
   - If a `409 READMISSION_PENDING` still arrives, the drawer shows the new literal:
     - ca «La readmissió està pendent: la fitxa del gos no es pot canviar fins que es validi o es rebutgi.»;
     - es «La readmisión está pendiente: la ficha del perro no se puede cambiar hasta que se valide o se rechace.»;
     - en «The readmission is pending: the dog's record can't change until it is validated or rejected.».
   - After a `PATCH` or a file removal, D2 re-reads `GET /members/{id}/signup`, because the `PATCH` answers the record.
   - A kept file keeps its stored name: the drawer offers no rename. A removed file's key answers `400 FILE_NOT_FOUND` once api E5-T21 is in: the drawer shows the error and re-reads the view.
   - Vitest.
3. **The reused dog's chip is read-only on D2** while the readmission is pending (R-04-06 b), with the same literal as its hint. Vitest.
4. **File removal on the dogs of public signups (api E5-T19 step 2).** The drawer's file removal works on a pending public-signup dog. Real-core e2e.
5. **Mocks behave like the api:**
   - the frozen routes answer 409 for a reused dog;
   - `PATCH /dogs/{id}` with `documents` merges by type;
   - the D2 view carries `readmission` and the `fileKey`s.
   
   Mock tests.
6. **The real core.** Extend the E3 stage of the real-core e2e:
   - a readmission reuses the seed's dog, with a new name and a new card;
   - D2 shows «Abans / Ara» for the dog;
   - a rejection leaves the dog as it was on D10.

## Fixed conventions
- Design system only.
- Keys are never literals; ca, es and en are added together.
- Mocks behave like the api.
- Heavy runs go through the host lock.
- Fictional data only.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E4-W13/*.log`)
- `pnpm turbo run lint typecheck test build`.
- `pnpm i18n:check`.
- `pnpm e2e:core E4-W13`.
- `pnpm e2e:docker`.

## Definition of Done
- Steps 0–6 are done. Steps 1–5 each have a test that fails before the fix.

## Evidence to return
- The command outputs.
- A step → test table.
- Captures of D2 with a reused dog (the changes, the drawer) at 1280.

---

## Executor report

**Sessions.** A first session (published in `8192323`, no report) adopted the snapshot and wrote steps 0–6. This session (26-09) reviewed that diff against the spec and the checklist. It fixed what the real core and the reviews turned up (below), and re-ran every Verification command on the final tree. The snapshot adopted is the one staged at `b8530b5` (api `d7b1d63`, which carries E5-T19's `SignupDocumentFile.fileKey`). The real-core runs used image `ghcr.io/jboixtcm/agilityhub-core-api:main`, revision `d2952d5` (built 26-09 07:38 UTC). No `openapi.next.json` was staged at the start of this session.

### What was done

- **Step 0: adopt the snapshot.** `openapi.json` ← the staged snapshot; `schema.d.ts` regenerated (`00-api-generate.log`; the diff of schemas and operations is in `00-snapshot-diff.txt`). `pnpm api:generate` on the final tree leaves no diff. `pending.json` has no entry for E3-T17, E5-T19 or E5-T20, so nothing was pruned.
  - D7's two lists read sparse items: an absent key means «not asked for», never a value. `fields` sends only keys from `x-fields`, and the D7 mock answers sparse items (400 `INVALID_FILTER` outside `x-fields`).
  - D7 and the app show `waitlistRank` («en llista d'espera (1)») instead of `position`. The registrants' `appliedFilters` no longer filter out the path's `activityId`, which the core does not echo.
  - D11 «Alta i consentiments» shows `signup.onboardingFields` (with the «Completa el teu perfil» labels) and `legal.maxPostpones`. The learn keys and their labels are gone.
- **Step 1 (R-04-06): «Abans / Ara» for the reused dog.** The reused dog's card has its own region «Canvis respecte de la fitxa de baixa {dog}» (`SignupReviewPage.tsx`). Each field of `readmission.changedFields` gets «Abans» (`readmission.current`) and «Ara» (the view), documents included (file names joined by « · »). A dog with `readmission: null`, as the core writes an ordinary dog, has no block.
- **Step 2 (R-04-19): the edit drawer for a reused dog.** `ReadmissionDogDocuments` in `SignupEditDrawer.tsx`:
  - the submitted documents change only through `PATCH /dogs/{id}`, one type at a time: add = the kept `fileKey`s plus the upload; remove = the kept ones; the last file withdraws the type with `files: []`;
  - a second change before the view reload lands carries the version the PATCH answered;
  - D2 re-reads `GET /members/{id}/signup` after each change;
  - no frozen route is called for that dog, and a kept file offers no rename;
  - `409 INVALID_STATE {reason: READMISSION_PENDING}` shows the new literal inside the drawer (ca/es/en, `admin-census:signupReview.readmission.dogLocked`);
  - `400 FILE_NOT_FOUND` shows «No s'ha trobat el fitxer.» in the drawer and re-reads the view;
  - the whole dog fieldset and [DESA ELS CANVIS] are disabled while a document change is pending.
- **Step 3 (R-04-06 b):** the reused dog's chip is read-only, with the same literal as its hint (`aria-describedby`), and is never sent.
- **Step 4:** on the real core, the drawer removes a public-signup dog's file with `DELETE /dogs/{id}/documents/{docId}/files/{fileId}`: a plain id, 204 (`d2-signup-file-removal-core.json`, `D2-signup-file-removed-core-1280.png`).
- **Step 5 (mocks):**
  - the D2 view carries `readmission` (with `changedFields` recomputed after each PATCH) and every file's `fileKey`;
  - the frozen routes answer `409 INVALID_STATE {reason: READMISSION_PENDING}` for the reused dog (documents, file DELETE, photo, reminder, level, free training, transfer, and chip, `handlerName` or `licenses` in the PATCH);
  - `PATCH /dogs/{id}` merges `documents` by type: a kept key keeps its stored name; a type without files is withdrawn for the reused dog and left pending for an ordinary dog;
  - a key removed through `DELETE …/files/{fileId}` answers `400 FILE_NOT_FOUND`;
  - `DOG_DOCUMENT_REQUIRED` applies unless the reused dog has its own card;
  - the response is the record (`Dog`), with the record's own name.
- **Step 6 (real core, `e2e/core/e3-signup.spec.ts`):**
  - a readmission reuses a LEFT member's INACTIVE dog, with a new name («Retorn E3») and a new card;
  - D2 shows «Abans: Brisa E3 / Ara: Retorn E3» and «Abans: — / Ara: cartilla_Retorn_E3_1.jpg» (`D2-readmission-core-1280.png`);
  - the chip is read-only with the hint;
  - the drawer's document PATCHes call no frozen route;
  - after the rejection the dog is exactly as before: name, breed, chip, `INACTIVE`, `deactivationReason`, `deactivatedAt`, documents (`readmission-core.json`); D10 shows «Brisa E3», never «Retorn E3» (`D10-after-rejected-readmission-core-1280.png`).

**Fixed in this session:**
1. **Focus bug in `@agilityhub/ui` `useOverlay`, shared by `Modal` and `Drawer`.** Its effect depended on `onClose`, and D2's drawer passes a new closure on every render. So every change inside the drawer (a keystroke, a file removal) moved the focus to the close button and scrolled the drawer back to its top. It showed in the first mock capture. The latest `onClose` is now kept in a ref, and open/close behave as before. A new Vitest failed first (`07-vitest-before-fix-overlay-focus.log`). The mock e2e now asserts that the documents stay in the viewport and that the close button is not focused.
2. **Mock e2e `dashboard-signup.spec.ts`.** The E3-W08 test looked up the region «Canvis respecte de la fitxa de baixa» by substring, which now also matches the dog's region (strict-mode violation). It uses `exact: true` now. The new E4-W13 mock e2e produces the 1280 captures `D2-reused-dog-mock-1280.png` and `D2-reused-dog-drawer-mock-1280.png`.
3. **Real-core e2e, found by running it:**
   - the file-removal check read the body of a 204;
   - the LEFT-member lookup required a member number, but the seed has no INACTIVE dog, so the only reusable dog is the one this test's own rejection leaves (see question 3);
   - `GET /dogs/{id}` answers the `DogDetail` wrapper, not a flat `Dog`;
   - the add-a-file flow needed the two api gaps of questions 1 and 2 to be handled (below).
4. **Drawer file rows:** the document icon now sits beside the name (it was stacked above it). CSS only.

### Step → test table
| Step | Test (file › name) | Fails before the fix |
|---|---|---|
| 0 | `packages/api-client/src/mocks/activity-handlers.test.ts` › «E4-W13 step 0 S07 §6 CONVENCIONS_API §4 sparse fields and waitlistRank (api E5-T20)» (3) | yes, 3/3 on the pre-E4-W13 mocks (`22-vitest-before-fix-step0-mocks.log`) |
| 0 | `apps/clubs-admin/src/activities/ActivitiesPage.test.tsx` › «T-07-29 E4-W13 step 0 …» (3); `apps/clubs-admin/src/catalogs/ParameterSettings.test.tsx` › «E4-W13 step 0 (api E5-T20): «Alta i consentiments» …» | 3 of 4 on the pre-E4-W13 screens (`23-…-admin.log`); the fourth, «a view with only the date column reads sparse rows», passes on the old code (E4-W12 already asked for `title`): coverage |
| 0 | `apps/clubs/src/activities/activities.test.tsx` › «T-07-30 E4-W13 step 0 R-07-08 the app shows the live waitlist rank (api E5-T20)» | yes (`24-…-app.log`) |
| 0 | `e2e/core/e4-core.spec.ts` › T-07-32 (e) ca/es: after a promotion, `waitlistRank` < `position` and the chips «en llista d'espera (2)» / «en lista de espera (n)» read the rank | real core, green in `20-e2e-core.log` (E4 stage, 7 passed) |
| 1 | `SignupReviewPage.test.tsx` › «T-04-12 T-04-19 E4-W13 …» › «step 1: the reused dog's card lists each changed field with «Abans» … «Ara» …, its documents too» (+ the control «an ordinary signup's dog, or `readmission: null` … has no block») | yes (`01-vitest-before-fix-d2.log`: 7 failed, the 2 controls pass) |
| 2 | same describe › «step 2: removing one …», «step 2: adding a file …», «step 2: the last file of a submitted type withdraws it …», «step 2: a 409 READMISSION_PENDING …», «step 2: a removed file's key (400 FILE_NOT_FOUND …)» | yes (`01`) |
| 2 | `packages/ui/src/components.test.tsx` › «E4-W13 a Drawer keeps the focus where the user is …»; `apps/clubs-admin/e2e/dashboard-signup.spec.ts` › «T-04-12 E4-W13 (R-04-06, R-04-19, E38) …» | Vitest yes (`07`); the e2e was written with the fix |
| 3 | same describe › «step 3: the reused dog's chip is read-only …» (+ control «an ordinary signup's chip stays editable») | yes (`01`) |
| 4 | `e2e/core/e3-signup.spec.ts` › «T-04-34 public signup is validated …» (the DELETE of the passport dog's card) | the fix is api E5-T19 step 2; no older core image is available here to show the failure |
| 5 | `packages/api-client/src/mocks/signup-handlers.test.ts` › «T-04-12 T-04-19 E4-W13 step 5 …» (4) | yes, 4/4 (`02-vitest-before-fix-mocks.log`) |
| 6 | `e2e/core/e3-signup.spec.ts` › «T-04-34 public signup is validated …» (readmission section) | new assertions; green on `d2952d5` |

Rules: R-04-06 (a, b), R-04-19, R-04-23; tests T-04-12 and T-04-19 (describe names), plus T-07-29 and T-07-30 for step 0.

### Evidence (final runs on the final tree; ANSI colour codes stripped from the tails)

**1. `pnpm turbo run lint typecheck test build --force`**: exit 0, 34/34 tasks, 0 cached. Full log: `roadmap/evidence/E4-W13/18-turbo.log`. `--force` was added so nothing was replayed from cache; the command without it also passed earlier (`04-turbo.log`, exit 0, 30 cached). Vitest counts: course-core 342, ui 69, api-client 196, shared-types 21, i18n 63, auth 17, id 9, clubs 179, clubs-admin 325. Earlier failures:
- `03-turbo.log`: exit 1. One D2 test lost its element when the view reload replaced the `<span>` with a link. The first session fixed it with `waitFor`.
- `17-turbo.log`: exit 1. Lint `no-unsafe-return` in the new UI test. Fixed with a block-bodied `onClose`.
```
clubs-admin:test:      ✓ R-07-05 [PUBLICA] with conflicts opens the dialog with the conflicts and the required notice text 347ms
clubs-admin:test:      ✓ R-07-04 a draft without the registration period answers ACTIVITY_INCOMPLETE on the fields 344ms
clubs-admin:test:      ✓ R-07-04 after an upload [DESA] sends only the admin's own edits with the fresh version 326ms
clubs-admin:test:      ✓ R-07-05 a RING_BLOCK conflict is not forceable: no option for it and [PUBLICA I APLICA] disabled with the reason 312ms
clubs-admin:test:      ✓ R-07-04/05 save mode: a resync conflict opens «DESA I APLICA»; STALE_VERSION closes it, refetches and says so 303ms
clubs-admin:test:    ✓ T-07-29 E4-W08 D7 follow-ups of the E4-W04 round-2 review (6)
clubs-admin:test:      ✓ R-07-05 away from the club the hours cover the whole day: 6:00 when the club opens at 7:00 651ms
clubs-admin:test:    ✓ T-07-29 E4-W10 D7 follow-ups of the E4-W08 and E4-W09 reviews (3)
clubs-admin:test:      ✓ S07 §6 CONVENCIONS_API §4 the member filter reads every page: with 1,001 distinct registrants the member on the second page can be selected 912ms
clubs-admin:test:      ✓ R-07-05 R-02-09 at the club with linked rings, a closed day shows «El club està tancat aquest dia» and offers no times; an open day or away from the club offers them 310ms
clubs-admin:test:  ✓ src/planning/CalendarPage.test.tsx (41 tests) 12122ms
clubs-admin:test:    ✓ E3-W07 step 9 a D1 risk row opens D4 on its class (1)
clubs-admin:test:      ✓ selects the class of `?classe=` in the week of `?setmana=` 772ms
clubs-admin:test:    ✓ T-06-28 D4 / D4b / D4c class calendar (front half, MSW) (24)
clubs-admin:test:      ✓ opens «Esborrany» on the first week with drafts, with the validation card and its count 1133ms
clubs-admin:test:      ✓ [VALIDAR LA SETMANA] turns every draft of the week active at once 544ms
clubs-admin:test:      ✓ cancels an active class with registrants through D4c: four rows, text required, «ANUL·LA I AVISA ELS 4 ALUMNES» 632ms
clubs-admin:test:      ✓ [ELIMINA] of an active class goes through the same D4c modal 592ms
clubs-admin:test:      ✓ R-06-09 saves the diff with the version; STALE_VERSION keeps its message after the refetch remounts the card 589ms
clubs-admin:test:      ✓ R-06-05 moving a class onto training bookings asks first and resends with cancelBookings 327ms
clubs-admin:test:      ✓ R-06-11 a new ring after RING_HAS_BOOKINGS drops the confirmation: no cancelBookings for a ring not shown 312ms
clubs-admin:test:      ✓ R-06-09 [Crear classe]: a new time or ring after RING_HAS_BOOKINGS drops the confirmation 401ms
clubs-admin:test:      ✓ R-06-09 [Crear classe]: a RING_HAS_BOOKINGS answer that arrives after a ring change is dropped 385ms
clubs-admin:test:      ✓ R-06-11 the block drawer locks ring, date and times until a delayed RING_HAS_BOOKINGS arrives 326ms
clubs-admin:test:      ✓ R-06-09 keeps every editor disabled from [ACCEPTA] until the saved version is shown 328ms
clubs-admin:test:      ✓ R-06-11 lists the RING_BLOCK_CONFLICT conflicts inside the block drawer 366ms
clubs-admin:test:    ✓ T-06-28 E4-W09 D4 follow-ups of the E4-W02 round-4 review (7)
clubs-admin:test:      ✓ S06 §3 R-06-09 an opening at 07:05 with 10-minute slots: «Hora» and [Crear classe] offer 07:10 first, and the class is sent on slot boundaries 377ms
clubs-admin:test:      ✓ R-06-09 after STALE_VERSION every editor stays locked until the refetched version is shown, so no edit is lost 370ms
clubs-admin:test: 
clubs-admin:test:  Test Files  20 passed (20)
clubs-admin:test:       Tests  325 passed (325)
clubs-admin:test:    Start at  14:34:11
clubs-admin:test:    Duration  17.85s (tests 46%, environment 28%, transform 15%, setup 6%, import 5%)
clubs-admin:test: 

 Tasks:    34 successful, 34 total
Cached:    0 cached, 34 total
  Time:    42.424s
```

**2. `pnpm i18n:check`**: exit 0. Full output below (`19-i18n-check.log`).
```
> agilityhub-core-web@0.0.0 i18n:check /Users/jordib/dev/agilityhub/agilityhub-core-web
> node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs && node scripts/check-parameter-keys.mjs

i18n check passed
ca: 2363 keys
es: 2363 keys
en: 2363 keys
1762 statically used keys, 0 unused
250 codes, 0 missing
151 catalog parameters (119 in D11, 32 system-only), 10 blocks, 0 missing
```

**3. `/Users/jordib/Dropbox/Documents/SOFTWARE_CANIC/05-desenvolupament/roadmap-kit/mac/heavy.sh pnpm e2e:core E4-W13`**: exit 0. Stages: E1/E2 12 passed, E3 5 passed, E4 7 passed. The lock waited 3 min 45 s for the api lane (log lines 1–2). Full log: `20-e2e-core.log`; seed logs `seed-e1-e2.log`, `seed-e3.log`, `seed-e4.log`; N-37 in `n37-notification.json`. Earlier attempts, one line each:
- `09`: the E3 stage read `text()` of the 204 → reads the body only when the status is not 204.
- `10`: no LEFT member with a number has an INACTIVE dog (the seed has none) → the lookup takes any LEFT member with an INACTIVE chipped dog.
- `11`: `GET /dogs/{id}` is `DogDetail` → the spec unwraps `dog`.
- `12`: 8-minute timeout, no PATCH was sent → diagnostics.
- `13`: the storage PUT answered 401 → question 2, and a route on that path only.
- `14`: `400 FILE_NOT_FOUND` for the new key.
- `15`: probe (`d2-readmission-add-file-probe-core.json`) → question 1; the spec now proves removal and records the add outcome.
- `16`: E3 spec green (5 passed).
```
+ eslint-import-resolver-typescript 4.4.5
+ eslint-plugin-import 2.32.0
+ eslint-plugin-jsx-a11y 6.10.2
+ eslint-plugin-react 7.37.5
+ eslint-plugin-react-hooks 7.1.1
+ globals 17.12.0
+ husky 9.1.7
+ i18next-parser 9.4.0
+ lint-staged 17.5.0
+ msw 2.15.0
+ prettier 3.9.6
+ size-limit 13.0.3
+ turbo 2.10.12
+ typescript 5.9.3
+ typescript-eslint 8.69.0

. prepare$ node packages/config/scripts/prepare-husky.mjs
. prepare: .git can't be found
. prepare: Done
Done in 4s using pnpm v9.15.9

[WebServer] (node:134) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
[WebServer] (node:214) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)

[WebServer] (node:312) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)


Running 7 tests using 1 worker

[1/7] e2e/core/e4-core.spec.ts:431:5 › T-06-28 E2E (a) D3: the three seeded templates, «Setmana B» blocked by its inconsistency, «Setmana A» generates the proposed week, D3b per ring
[2/7] e2e/core/e4-core.spec.ts:566:5 › T-06-28 E2E (b) D4: «Esborrany» opens the seeded draft week, [VALIDAR LA SETMANA], the D4c modal with the 4 registrants (200), «Anul·lades», ring blocks (409 then 201)
[3/7] e2e/core/e4-core.spec.ts:716:5 › T-06-28 E2E (c) screens 10 and 23: the member never sees the cancelled class nor counts, the validated week is visible; the instructor sees the block, the dimmed class and n/n
[4/7] e2e/core/e4-core.spec.ts:771:5 › T-07-32 E2E (d) D7: the four seeded activities, the Torneig blocks every ring on D4, a new seminar published over a class with the conflict dialog; D7 exports against the core
[5/7] e2e/core/e4-core.spec.ts:912:5 › T-07-32 E2E (e) ca: 04 → detail → register → 03 → cancel in time; the waitlist of the full «Seminari de handling» and the FIFO promotion when the admin adds a place
[6/7] e2e/core/e4-core.spec.ts:1077:5 › T-07-32 E2E (e) es: the same member flow with the es literals, a second FIFO promotion, then the admin cancels the seminar (200) and the registrant reads «cancel·lada pel club»
[7/7] e2e/core/e4-core.spec.ts:1252:5 › E4-W05 step 7 · the «Excel · PDF» menus of D5 and D7 with EXPORT_TOO_LARGE and while an export is pending
  7 passed (18.8s)
```

**4. `/Users/jordib/Dropbox/Documents/SOFTWARE_CANIC/05-desenvolupament/roadmap-kit/mac/heavy.sh pnpm e2e:docker E4-W13`**: exit 0. Complete suite in `mcr.microsoft.com/playwright:v1.63.0-noble`: clubs-admin 48 passed, id 1 passed, clubs 33 passed. Full log: `21-e2e-docker.log`. A targeted run before it (`06-e2e-docker-d2-targeted.log`, exit 0) ran only the two D2 readmission specs.
```
clubs:build:   dist/sw.js
clubs:build:   dist/workbox-9c191d2f.js
clubs-admin:e2e: cache bypass, force executing 587094037253e162
clubs-admin:e2e: 
clubs-admin:e2e: > clubs-admin@0.0.0 e2e /work/apps/clubs-admin
clubs-admin:e2e: > playwright test
clubs-admin:e2e: 
clubs-admin:e2e: [WebServer] (node:664) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs-admin:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs-admin:e2e: 
clubs-admin:e2e: Running 48 tests using 9 workers
clubs-admin:e2e: ················································
clubs-admin:e2e:   48 passed (40.8s)
id:e2e: cache bypass, force executing 8b3eca39710fe578
id:e2e: 
id:e2e: > id@0.0.0 e2e /work/apps/id
id:e2e: > playwright test
id:e2e: 
id:e2e: 
id:e2e: Running 1 test using 1 worker
id:e2e: ·
id:e2e:   1 passed (3.1s)
clubs:e2e: cache bypass, force executing 2b934578783328d0
clubs:e2e: 
clubs:e2e: > clubs@0.0.0 e2e /work/apps/clubs
clubs:e2e: > playwright test
clubs:e2e: 
clubs:e2e: [WebServer] (node:3168) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs:e2e: 
clubs:e2e: Running 33 tests using 7 workers
clubs:e2e: ·································
clubs:e2e:   33 passed (16.3s)

 Tasks:    8 successful, 8 total
Cached:    0 cached, 8 total
  Time:    1m14.704s 

evidence: E4-W13/D2-reused-dog-drawer-mock-1280.png
evidence: E4-W13/D2-reused-dog-mock-1280.png
```

**Captures (1280).**
- Mocks: `D2-reused-dog-mock-1280.png` (the person's and the dog's «Abans / Ara» blocks), `D2-reused-dog-drawer-mock-1280.png` (the drawer after a withdrawal: read-only chip with its hint, documents in view).
- Real core: `D2-readmission-core-1280.png`, `D2-readmission-dog-documents-core-1280.png` (the refused add, with the error inside the drawer), `D2-readmission-drawer-core-1280.png` (after the withdrawal), `D2-signup-file-removed-core-1280.png` and `D10-after-rejected-readmission-core-1280.png`. The rest of the real-core suite's captures are refreshed in the same folder.

### Files changed (E4-W13, both sessions)
- `packages/api-client/openapi/openapi.json`, `src/generated/schema.d.ts`; `openapi.next.json` removed.
- `apps/clubs-admin/src/dashboard/SignupReviewPage.tsx`, `SignupEditDrawer.tsx`, `SignupReviewPage.test.tsx`; `apps/clubs-admin/src/styles.css`.
- `apps/clubs-admin/src/activities/ActivitiesPage.tsx` (+ test), `ActivityRegistrantsPage.tsx`; `apps/clubs-admin/src/catalogs/ParameterSettings.tsx` (+ test).
- `apps/clubs/src/activities/ActivityDetailPage.tsx`, `HistoryRowsPreview.tsx`, `activities.test.tsx`.
- `packages/api-client/src/mocks/handlers.ts`, `activity-handlers.ts` (+ test), `signup-handlers.test.ts`, `list-exports.test.ts`, `fixtures/signup-review.ts`, `fixtures/member-signup-review.json`, `fixtures/activities.ts`, `fixtures/settings.ts`.
- `packages/ui/src/components.tsx`, `components.test.tsx`.
- `packages/i18n/src/locales/{ca,es,en}/admin-census.json` (`signupReview.readmission.dogLocked`), `…/admin-settings.json` (learn keys removed).
- `apps/clubs-admin/e2e/dashboard-signup.spec.ts`, `e2e/core/e3-signup.spec.ts`, `e2e/core/e4-core.spec.ts`; `CHANGELOG.md`.

### Assumptions
1. «Ara» for `documents` lists the view's files (what the validation writes), names joined by « · », as D2's card does. `SignupDocumentView` has no type label, so there is no per-type label.
2. The type selector of a reused dog's upload lists `census.dogDocumentTypes` (`GET /parameters/{key}`, ADMIN), because the frozen record's own rows are not the club's list.
3. Step 6 reuses the dog of the applicant this same test rejects (Pau Rebuig E3, LEFT; Brisa E3, `INACTIVE{SIGNUP_REJECTED}`). The demo seed has no INACTIVE dog (`seed-e3.log`: 245 dogs = 242 active + 3 pending), and no route turns an ACTIVE member LEFT (question 3).
4. The `useOverlay` fix changes every `Modal`/`Drawer`, only in that a new `onClose` no longer re-runs the focus effect. Escape calls the latest `onClose` (tested).

### Questions / catalog proposals
1. **@organizer, api (blocks «add a file» on the real core, not this task):** `PATCH /dogs/{id}` refuses a new file key from the admin's upload route. On `d2952d5`:
   - the drawer's PATCH `{documents: [{type: VACCINATION_CARD, files: [kept signup key, new key from POST /attachments/upload-url (DOG_DOCUMENT)]}]}` answers `400 FILE_NOT_FOUND` (`d2-readmission-add-file-core.json`);
   - the same PATCH with the kept key only answers 200;
   - the admin gets `403` on `POST /signup/upload-urls` (probe: `d2-readmission-add-file-probe-core.json`).

   So no ADMIN route yields a key that the PATCH takes as «a new signup upload». Proposal: `PATCH /dogs/{id}` accepts a `DOG_DOCUMENT` upload of the same club as a new file, or the contract names the upload route D2 must use. The web sends what the contract describes; the mocks accept the admin key. The real-core e2e proves removal and records the add outcome: it takes the 200 branch when the api accepts the key.
2. **@organizer, api (local stack):** the local core's signed URL for the admin, `/api/v1/attachments/uploads/{id}`, answers `401` to a PUT carrying only `Upload.headers` (`13-e2e-core-e3-targeted.log`). A presigned S3 URL needs no bearer, and the web never sends one to the storage. This affects every admin/member upload on the local stack (D2, D7 image and documents, D10 documents and photo, 13). The real-core e2e adds the admin's bearer on that path only. Proposal: the local profile's URL authorises itself (a signed token in the URL), like S3.
3. **@organizer, api seed:** a LEFT member with an INACTIVE chipped dog (with a card) in `seed:demo` would let step 6 reuse a seed dog, as the step says.
4. Observations against the core, outside this task:
   - D10's dog row shows the raw key `VACCINATION_CARD` as its pending-document chip, and an empty «núm.» badge for a member without a number (`D10-after-rejected-readmission-core-1280.png`);
   - the audit list's «Entitat» and «Qui» columns fall back to `entityType`/`actorRole`, which they do not request; with sparse `fields` those keys are absent.

   Candidates for a later task.

### Next steps
None for this task. It is `awaiting_verification`.

### Round 2 report

This session (26-09, after E4-W14 round 2) fixed round-2 points 1–5.
- **Tree:** HEAD `11c3035` plus this session's changes.
- **Core image:** `ghcr.io/jboixtcm/agilityhub-core-api:main`, `sha256:eff1e850ae16…`, revision `e9a58c15a035f6acc7f029d388f3537cdccf1761`, created 2026-09-26T13:47Z.
- **E5-T24:** the organizer's message «api E5-T24 verified (`dd3c246`)» arrived while this session was running. The local image `e9a58c1` does not carry it:
  - its seed has no INACTIVE dog (`seed-e3.log`: `dogs=245, activeDogs=242, pendingDogs=3`, so no «Demo Boira»);
  - its `PATCH /dogs/{id}` still refuses the admin's upload key (`400 FILE_NOT_FOUND`).

  So points 2 and 3 take their «older image» branches, as that message allows. Pulling a newer image would change Docker state outside this repository, so I did not.

#### What changed
1. **Review #1 (major): a withdrawn type falls back to the record's own row (R-04-06, R-04-19).**
   - **Mock `patchSignupDogDocuments`:** a type sent with `files: []` for the reused dog puts back the row of `readmission.current.documents` for that type. It removes the type only when the record has none.
   - **`dogReadmissionChanges`:** `documents` compares only the types sent with files, against the record's row of the same type (the api's `withFiles`).
   - **Mock `PATCH /dogs/{id}`:** documents alone that change no file key write nothing and keep the version (E5-T21). That check comes before `DOG_DOCUMENT_REQUIRED`, like E5-T23's «sent back as shown».
   - **Drawer (`ReadmissionDogDocuments`):**
     - it receives the record's own documents (`own`);
     - a file of a row equal to the record's own row has no [Retira];
     - until the view is read again, a withdrawal shows the record's row.
2. **Review #3 (minor): the rejection check proves something (T-04-19, R-04-23).**
   - Pau's first public signup attaches the card (`attachCard: true`). Brisa E3 keeps `cartilla_Brisa_E3_1.jpg` after the rejection, and the spec requires the record to have a card file (`oldCardFiles`).
   - The readmission sends a new name, a new card and a new breed («Pastor belga»).
   - D2 shows:
     - «Abans: Brisa E3 / Ara: Retorn E3»;
     - «Abans: Mestís / Ara: Pastor belga»;
     - under «Cartilla de vacunes», «Abans: cartilla_Brisa_E3_1.jpg / Ara: cartilla_Retorn_E3_1.jpg» (`D2-readmission-core-1280.png`).
   - Withdrawing the submitted card brings the record's card back in the drawer, with no [Retira] (`D2-readmission-drawer-core-1280.png`). The core's view then shows `VACCINATION_CARD: cartilla_Brisa_E3_1.jpg` with `changedFields: ["name", "breed"]`. This is #1 proven on the core (`d2-readmission-view-core.json` → `dogAfterCardWithdrawn`).
   - After the rejection, the record keeps «Mestís» and `VACCINATION_CARD: cartilla_Brisa_E3_1.jpg` (`readmission-core.json`). D10 shows neither «Retorn E3» nor «Pastor belga».
3. **Review #2 (minor): add a file on the core. Not possible yet.** The image does not carry E5-T24, so the add still answers `400 FILE_NOT_FOUND` (`d2-readmission-add-file-core.json`). As point 3 says, both workarounds stay: the `400` fallback and the admin's bearer on the local upload path. The code for the 200 branch is ready: it removes the added file, then withdraws the card. **Step 2's add is therefore still unverified on the core**; it is proven only in Vitest and MSW.
4. **Review #4 (minor):** `T-04-19` is dropped from the Vitest describe «T-04-12 E4-W13 D2 for the reused dog…», and from the mock describe «T-04-12 E4-W13 step 5…», which asserts no rejection either. `T-04-19` stays in the real-core spec, which asserts the rejection.
5. **Review #5 (nit): «Abans / Ara» per type.**
   - The reused dog's block has one row per changed document type (a type sent with files that differs from the record's row), named after `census.dogDocumentTypes` («Cartilla de vacunes», «Assegurança»).
   - The loader is shared with the drawer: `loadDogDocumentTypes` in `dashboard/readmission.ts`.
   - While the labels load, the row reads «Documents»; a type missing from the list shows its key.
   - A row without files reads «pendent» (the existing `admin-census:dog.documents.pending`), and a type absent from one side reads «—».

#### Step → test (round 2; "failed before" = logs `25` and `28`, the final tests run on the HEAD sources)
| Point | Test | Failed before the fix |
|---|---|---|
| 1 mock | `signup-handlers.test.ts` › «T-04-12 E4-W13 step 5 …» › «E4-W13 round 2 R-04-06 a withdrawn type of the reused dog shows the record's own row again and is no change; a PATCH that changes no key writes nothing (api E5-T21)» | yes (`25`) |
| 1 UI | `SignupReviewPage.test.tsx` › «T-04-12 E4-W13 D2 …» › «round 2 R-04-06 R-04-19: withdrawing Kiwi's submitted card shows the record's card again, which offers no [Retira] and is no change; the insurance withdrawn too leaves no documents change» | yes (`28`) |
| 1 e2e | `apps/clubs-admin/e2e/dashboard-signup.spec.ts` › «T-04-12 E4-W13 …» (both card files withdrawn: the 2025 card with no [Retira], no card line; `D2-reused-dog-drawer-mock-1280.png`) | — (e2e, log `33`) |
| 5 | same describe › «round 2 (review nit #5): a document type without files reads as pending, not as absent»; «step 1: …» now expects the terms «Nom, Raça, Cartilla de vacunes, Assegurança» and one line per type | yes, both (`28`) |
| 5 | «step 2: removing one …», «step 2: adding a file …»: the per-type «Ara» lines | yes (`28`; the old single-line format) |
| 2, 3 | `e2e/core/e3-signup.spec.ts` › «T-04-34 …» (readmission section) | — (real-core e2e, log `32`) |
| 4 | describe names | — (a name) |

#### Evidence (final runs)
**1. `pnpm turbo run lint typecheck test build --force > roadmap/evidence/E4-W13/30-turbo-r2-final.log 2>&1`**: exit **0**.
- 34/34 tasks, 0 cached.
- Vitest: course-core 342, ui 69, api-client 222, i18n 80, shared-types 21, auth 17, id 9, clubs 225, clubs-admin 333.
- **After it:** `readmission.ts` was formatted with prettier (it was clean at HEAD). `pnpm turbo run lint typecheck test build --filter=clubs-admin --force` then gave exit 0, 8/8 tasks, 333 tests (`34-turbo-clubs-admin-after-format.log`). The mock describe was also renamed, and its file re-ran green: 28 passed.
- Last 40 lines:
```
clubs-admin:test:      ✓ R-07-05 a RING_BLOCK conflict is not forceable: no option for it and [PUBLICA I APLICA] disabled with the reason 448ms
clubs-admin:test:      ✓ R-07-04/05 save mode: a resync conflict opens «DESA I APLICA»; STALE_VERSION closes it, refetches and says so 508ms
clubs-admin:test:      ✓ the plain publish confirm is pending once, keeps one key per payload and turns ADMIN_TEXT_REQUIRED into the text 337ms
clubs-admin:test:      ✓ [DESPUBLICA] answers ACTIVITY_HAS_REGISTRATIONS with registrants and unpublishes without them 361ms
clubs-admin:test:    ✓ T-07-29 E4-W08 D7 follow-ups of the E4-W04 round-2 review (6)
clubs-admin:test:      ✓ R-07-04 the rich text is read-only while [DESA] is pending, and nothing typed is lost 343ms
clubs-admin:test:      ✓ R-07-05 away from the club the hours cover the whole day: 6:00 when the club opens at 7:00 619ms
clubs-admin:test:    ✓ T-07-29 E4-W10 D7 follow-ups of the E4-W08 and E4-W09 reviews (3)
clubs-admin:test:      ✓ S07 §6 CONVENCIONS_API §4 the member filter reads every page: with 1,001 distinct registrants the member on the second page can be selected 865ms
clubs-admin:test:  ✓ src/planning/CalendarPage.test.tsx (41 tests) 14709ms
clubs-admin:test:    ✓ E3-W07 step 9 a D1 risk row opens D4 on its class (1)
clubs-admin:test:      ✓ selects the class of `?classe=` in the week of `?setmana=` 1405ms
clubs-admin:test:    ✓ T-06-28 D4 / D4b / D4c class calendar (front half, MSW) (24)
clubs-admin:test:      ✓ opens «Esborrany» on the first week with drafts, with the validation card and its count 1543ms
clubs-admin:test:      ✓ [VALIDAR LA SETMANA] turns every draft of the week active at once 919ms
clubs-admin:test:      ✓ cancels an active class with registrants through D4c: four rows, text required, «ANUL·LA I AVISA ELS 4 ALUMNES» 989ms
clubs-admin:test:      ✓ [ELIMINA] of an active class goes through the same D4c modal 796ms
clubs-admin:test:      ✓ R-06-09 saves the diff with the version; STALE_VERSION keeps its message after the refetch remounts the card 949ms
clubs-admin:test:      ✓ R-06-09 keeps the INVALID_STATE message of the «Exempta…» toggle after the refetch 331ms
clubs-admin:test:      ✓ R-06-05 moving a class onto training bookings asks first and resends with cancelBookings 539ms
clubs-admin:test:      ✓ R-06-11 a new ring after RING_HAS_BOOKINGS drops the confirmation: no cancelBookings for a ring not shown 379ms
clubs-admin:test:      ✓ R-06-09 [Crear classe]: a new time or ring after RING_HAS_BOOKINGS drops the confirmation 481ms
clubs-admin:test:      ✓ R-06-09 [Crear classe]: a RING_HAS_BOOKINGS answer that arrives after a ring change is dropped 454ms
clubs-admin:test:      ✓ R-06-11 the block drawer locks ring, date and times until a delayed RING_HAS_BOOKINGS arrives 396ms
clubs-admin:test:      ✓ R-06-09 keeps every editor disabled from [ACCEPTA] until the saved version is shown 368ms
clubs-admin:test:      ✓ R-06-11 lists the RING_BLOCK_CONFLICT conflicts inside the block drawer 319ms
clubs-admin:test:    ✓ T-06-28 E4-W09 D4 follow-ups of the E4-W02 round-4 review (7)
clubs-admin:test:      ✓ S06 §3 R-06-09 an opening at 07:05 with 10-minute slots: «Hora» and [Crear classe] offer 07:10 first, and the class is sent on slot boundaries 323ms
clubs-admin:test:      ✓ R-06-09 after STALE_VERSION every editor stays locked until the refetched version is shown, so no edit is lost 313ms
clubs-admin:test: 
clubs-admin:test:  Test Files  20 passed (20)
clubs-admin:test:       Tests  333 passed (333)
clubs-admin:test:    Start at  16:57:42
clubs-admin:test:    Duration  23.33s (tests 43%, environment 28%, transform 17%, setup 6%, import 6%)
clubs-admin:test: 

 Tasks:    34 successful, 34 total
Cached:    0 cached, 34 total
  Time:    39.113s
```

**2. `pnpm i18n:check > roadmap/evidence/E4-W13/31-i18n-check-r2.log 2>&1`**: exit **0**. Complete output (no new key this round):
```
> agilityhub-core-web@0.0.0 i18n:check /Users/jordib/dev/agilityhub/agilityhub-core-web
> node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs && node scripts/check-parameter-keys.mjs

i18n check passed
ca: 2495 keys
es: 2495 keys
en: 2495 keys
1873 statically used keys, 0 unused
250 codes, 0 missing
151 catalog parameters (119 in D11, 32 system-only), 10 blocks, 0 missing
```

**3. `…/roadmap-kit/mac/heavy.sh pnpm e2e:core E4-W13 > roadmap/evidence/E4-W13/32-e2e-core-r2.log 2>&1`**: exit **0**.
- **Host lock:** «16:58 waiting for the host lock, held by: … agilityhub-core-api: bin/e5-smoke», then «17:02 got the host lock after 4 min 16 s».
- **Results:** e1–e2 12 passed, e3 5 passed (the readmission is in e3), e4 7 passed.
- Last 40 lines, lines cut at 240 characters:
```
+ eslint-import-resolver-typescript 4.4.5
+ eslint-plugin-import 2.32.0
+ eslint-plugin-jsx-a11y 6.10.2
+ eslint-plugin-react 7.37.5
+ eslint-plugin-react-hooks 7.1.1
+ globals 17.12.0
+ husky 9.1.7
+ i18next-parser 9.4.0
+ lint-staged 17.5.0
+ msw 2.15.0
+ prettier 3.9.6
+ size-limit 13.0.3
+ turbo 2.10.12
+ typescript 5.9.3
+ typescript-eslint 8.69.0

. prepare$ node packages/config/scripts/prepare-husky.mjs
. prepare: .git can't be found
. prepare: Done
Done in 3.9s using pnpm v9.15.9
[WebServer] (node:134) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)

[WebServer] (node:214) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)

[WebServer] (node:312) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)


Running 7 tests using 1 worker

[1/7] e2e/core/e4-core.spec.ts:482:5 › T-06-28 E2E (a) D3: the three seeded templates, «Setmana B» blocked by its inconsistency, «Setmana A» generates the proposed week, D3b per ring
[2/7] e2e/core/e4-core.spec.ts:617:5 › T-06-28 E2E (b) D4: «Esborrany» opens the seeded draft week, [VALIDAR LA SETMANA], the D4c modal with the 4 registrants (200), «Anul·lades», ring blocks (409 then 201)
[3/7] e2e/core/e4-core.spec.ts:767:5 › T-06-28 E2E (c) screens 10 and 23: the member never sees the cancelled class nor counts, the validated week is visible; the instructor sees the block, the dimmed class and n/n
[4/7] e2e/core/e4-core.spec.ts:822:5 › T-07-32 E2E (d) D7: the four seeded activities, the Torneig blocks every ring on D4, a new seminar published over a class with the conflict dialog; D7 exports against the core
[5/7] e2e/core/e4-core.spec.ts:1067:5 › T-07-32 E2E (e) ca: 04 → detail → register → 03 → cancel in time; the waitlist of the full «Seminari de handling» and the FIFO promotion when the admin adds a place
[6/7] e2e/core/e4-core.spec.ts:1229:5 › T-07-32 E2E (e) es: the same member flow with the es literals, a second FIFO promotion, then the admin cancels the seminar (200) and the registrant reads «cancel·lada pel club»
[7/7] e2e/core/e4-core.spec.ts:1399:5 › E4-W05 step 7 · the «Excel · PDF» menus of D5 and D7 with EXPORT_TOO_LARGE and while an export is pending
  7 passed (17.1s)
```
The readmission's own evidence from this run:
- `readmission-core.json`: `dogBefore` and `dogAfterRejection` are both `{breed: "Mestís", name: "Brisa E3", status: INACTIVE, deactivationReason: SIGNUP_REJECTED, documents: ["VACCINATION_CARD: cartilla_Brisa_E3_1.jpg"]}`.
- `d2-readmission-view-core.json` → `dogAfterCardWithdrawn` = `{changedFields: ["name", "breed"], documents: ["VACCINATION_CARD: cartilla_Brisa_E3_1.jpg"]}`.
- `d2-readmission-add-file-core.json`: `400 FILE_NOT_FOUND`.

**4. `…/roadmap-kit/mac/heavy.sh pnpm e2e:docker E4-W13 > roadmap/evidence/E4-W13/33-e2e-docker-r2.log 2>&1`**: exit **0**.
- **Host lock:** «17:06 waiting for the host lock, held by: … agilityhub-core-api: ./mvnw -q clean verify», then «17:17 got the host lock after 10 min 46 s».
- **Results:** clubs 42 passed, clubs-admin 48 passed, id 1 passed.
- Last 40 lines, lines cut at 240 characters:
```
id:build: - Adjust chunk size limit for this warning via build.chunkSizeWarningLimit.
id:build: ✓ built in 175ms
clubs:e2e: cache bypass, force executing d99d61541bf5881d
clubs:e2e: 
clubs:e2e: > clubs@0.0.0 e2e /work/apps/clubs
clubs:e2e: > playwright test
clubs:e2e: 
clubs:e2e: [WebServer] (node:665) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabil
clubs:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs:e2e: 
clubs:e2e: Running 42 tests using 8 workers
clubs:e2e: ··········································
clubs:e2e:   42 passed (19.2s)
clubs-admin:e2e: cache bypass, force executing 320b5a705231716a
clubs-admin:e2e: 
clubs-admin:e2e: > clubs-admin@0.0.0 e2e /work/apps/clubs-admin
clubs-admin:e2e: > playwright test
clubs-admin:e2e: 
clubs-admin:e2e: [WebServer] (node:2564) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vul
clubs-admin:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs-admin:e2e: 
clubs-admin:e2e: Running 48 tests using 9 workers
clubs-admin:e2e: ················································
clubs-admin:e2e:   48 passed (35.6s)
id:e2e: cache bypass, force executing 647de7a266a7f998
id:e2e: 
id:e2e: > id@0.0.0 e2e /work/apps/id
id:e2e: > playwright test
id:e2e: 
id:e2e: 
id:e2e: Running 1 test using 1 worker
id:e2e: ·
id:e2e:   1 passed (3.3s)

 Tasks:    8 successful, 8 total
Cached:    0 cached, 8 total
  Time:    1m11.175s 

evidence: E4-W13/D2-reused-dog-drawer-mock-1280.png
evidence: E4-W13/D2-reused-dog-mock-1280.png
```

**Earlier attempts this round (one line each):**
- `26-vitest-before-fix-r2-d2.log`: the D2 before-fix run with an earlier text of the nit-#5 test, which did not typecheck. It is superseded by `28` (same failures).
- `27-turbo-r2-build-fail.log`: exit 2. The clubs-admin build type-checks the tests, and the nit-#5 test read `readmission.current` from the test's loose `SignupBody` type. Fixed with a cast.
- `29-turbo-r2-budget-timing-fail.log`: exit 1. The E3-W10 import-time budget test failed under turbo's parallel load: «20.83 × the reference work; budget 17 ×». No code changed after it. Re-run alone it passed (15/15, and api-client 222/222), and the next full run `30` was green.

#### Captures (1280)
- **Real core:**
  - `D2-readmission-core-1280.png`: name, breed and card lines, the card with the old file under «Abans»;
  - `D2-readmission-drawer-core-1280.png`: after the card is withdrawn, the record's `cartilla_Brisa_E3_1.jpg`, with no [Retira];
  - `D2-readmission-dog-documents-core-1280.png`: the refused add;
  - `D10-after-rejected-readmission-core-1280.png`.
- **Mocks:**
  - `D2-reused-dog-mock-1280.png`: the per-type lines «Cartilla de vacunes» and «Assegurança»;
  - `D2-reused-dog-drawer-mock-1280.png`: the 2025 card with no [Retira].

#### Files changed (round 2)
- **Mocks:** `packages/api-client/src/mocks/fixtures/signup-review.ts`, `handlers.ts` and `signup-handlers.test.ts`.
- **D2:** `apps/clubs-admin/src/dashboard/SignupEditDrawer.tsx`, `SignupReviewPage.tsx`, `readmission.ts` (`loadDogDocumentTypes`) and `SignupReviewPage.test.tsx`.
- **E2E:** `apps/clubs-admin/e2e/dashboard-signup.spec.ts` and `e2e/core/e3-signup.spec.ts` (`breed` for `fillDog`/`completePublicSignup`, `readmissionBreed`).
- **Other:** `CHANGELOG.md`, this report, evidence `25`–`34` plus the refreshed captures and JSON.

#### Assumptions
1. **A row «is the record's own»** when its file keys equal the record's row for that type. Only such rows lose [Retira]. A submitted row keeps its [Retira], including the last file, which withdraws the type.
2. **The no-op rule applies only to a `PATCH` that carries documents and nothing else.** A `PATCH` with other fields writes and bumps the version as before; the api's behaviour for that mix was not observed.
3. **Labels while loading:** until `census.dogDocumentTypes` is read, a type row reads «Documents»; after a failed read it shows the type's key. No new copy was added.

#### Questions / catalog proposals
- No new parameter, event, notification, error code or literal.
- **@organizer, E5-T24:** it is verified at `dd3c246`, but the local image is `e9a58c1`, without it. With an image at `dd3c246` or later, a later session:
  - makes the real-core spec's 200 branch of the add mandatory;
  - drops the 400 fallback and the upload-route bearer;
  - reuses «Demo Boira» of LEFT member 192 instead of the test's own rejected applicant.

  The spec's lookup already takes the first LEFT member with an INACTIVE chipped dog, and requires a card file. The 200 branch is already written: remove the added file, then withdraw the card.

## Organizer verification
Result: changes_requested (26-09-2026, organizer).

- **Review:** `roadmap/reviews/E4-W13-20260926-1556-claude.md` (commit `23db4c4`): needs changes. It found one major, three minors and one nit.
- **CI:** `23db4c4` is green (run 36246547812).
- **Evidence:** it matches the report:
  - turbo `--force` 34/34;
  - `i18n:check` passes;
  - the real core 12 + 5 + 7 on `d2952d5`, and Docker 48 + 1 + 33;
  - the before-fix logs fail as the report says.
- **Confirmed by the organizer against the api (review #1):**
  - For a reused dog, the api keeps only the types sent with files (`SignupService.java:476`, `withFiles`).
  - D2's view shows the dog's own rows, with each submitted type replaced (`documentRows` and `replaced`, near `:854`–`:861`).
  - So a withdrawn type shows the record's own row again, as R-04-06 says: «un tipus … enviat sense fitxers conserva el del gos». The mock drops the type instead.
- **The report's questions:** questions 1–3 go to api E5-T24, which is in progress. Question 4 goes to E4-W15 step 8.

### Round 2: fix these (each with a test that fails before the fix)
1. **Review #1 (major; step 5, R-04-06, R-04-19): a withdrawn type falls back to the record's own row.**
   - **Mock:**
     - a type sent with `files: []` for the reused dog leaves the submission, and the view shows the row of `readmission.current.documents` for that type again;
     - `dogReadmissionChanges` compares only the types sent with files, as the api does;
     - as in E5-T21, a `PATCH` that changes no file key writes nothing and does not bump the version.
   - **Drawer:** a file of the record's own row (not a submitted one) offers no [Retira]. The api cannot withdraw it, so the drawer shows no control that does nothing.
   - **Vitest:** Kiwi's record has `cartilla_Kivi_2025.pdf`. Withdrawing Kiwi's submitted `VACCINATION_CARD` shows the record's card again, and no «documents» change.
2. **Review #3 (minor; step 6, T-04-19, R-04-23): the rejection check must prove something.**
   - The reused dog has a card with a file, and the readmission changes the breed.
   - D2 then shows the old card's file under «Abans».
   - After the rejection, the record keeps its old card, files included, and its old breed.
   - Which dog to use:
     - once the core image carries api E5-T24 (step 3), use the demo seed's LEFT member with an INACTIVE dog;
     - until then, use `attachCard: true` on the first signup.
   - This also exercises #1 on the core.
3. **Review #2 (minor; step 2): add a file on the core.** When the core image carries api E5-T24:
   - the drawer's add must answer 200 (the admin's `DOG_DOCUMENT` upload, then the kept keys plus the new one), so drop the `400 FILE_NOT_FOUND` fallback;
   - drop the bearer that the spec adds on the local upload path (E5-T24 step 2).

   If the image does not carry E5-T24 yet, keep both workarounds and say so in the report. The organizer's MESSAGES entry names the api commit that verifies E5-T24; compare it with the image's revision label.
4. **Review #4 (minor; AGENTS rule 5):** the Vitest describe drops `T-04-19`, or gains a D2 rejection case for the reused dog.
5. **Review #5 (nit; step 1): «Abans / Ara» per type.**
   - One line per document type, with the type's label from `census.dogDocumentTypes` («Cartilla de vacunes: …»).
   - A pending type without files reads as pending, not as absent.
- **Verification:** as in the task. Round 2's report goes under «### Round 2 report».
