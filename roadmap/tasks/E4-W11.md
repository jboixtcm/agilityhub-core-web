---
id: E4-W11
title: D7, D4 and D3 — follow-ups of the E4-W10 review: D7 publication on a closed day with rings (and a mock that answers OUTSIDE_OPENING_HOURS), the D4 card's editors on a closed day, one error helper for details.field and fieldErrors, the closed-days literal, spec-id test names; the api E5-T15 fields on screen 23's drawer and the app rows
stage: E4
repo: agilityhub-core-web
thread: B
order: 49
status: verified
depends_on: [E4-W10]
branch:
pr:
updated: 2026-09-26
---

# E4-W11 · D7, D4 and D3: follow-ups of the E4-W10 review

## Objective
The independent review of E4-W10 (`roadmap/reviews/E4-W10-20260925-2326-claude.md`) found two minors and seven nits. The organizer confirmed them and decided the open points. They must be done before the E4 gate closes: the organizer verifies E4-W05 only once this task is verified.

**Each fix comes with a test that fails before the fix.**

## Spec references
- S07:
  - §6 (`POST /activities/{id}/publication` → `422 OUTSIDE_OPENING_HOURS`);
  - R-07-05 (ring blocks inside `club.openingHours`), T-07-04.
- S06 §3 (times inside the day's opening hours); S02 R-02-09 (closed days).
- `CONVENCIONS_API.md` §5: `details.fieldErrors[{field, code}]`, or `details.field` for one field (amended 25-09).
- AGENTS rule 5 (test names).
- Step 6: S07 R-07-13 (the app rows' hours) and S06 §6 `GET /class-sessions/{id}` (the staff projection, api E5-T15 step 10).

## Context to load
- The review.
- `apps/clubs-admin/src/activities/ActivityPage.tsx` (near `:414` and `:871`), `activities/shared.tsx` (near `:76`).
- `packages/api-client/src/mocks/activity-handlers.ts`.
- `apps/clubs-admin/src/planning/SelectedClassCard.tsx` (near `:276`), `planning/shared.ts` (near `:106`).
- `apps/clubs/src/SelfServicePages.tsx` (near `:138`), `SignupPage.tsx` (near `:506`), `ActivityDetailPage.tsx`.
- Step 6: the E4-W03 report (question 3 and its organizer answer), screen 23's class drawer, and the app rows of `RegisteredActivity` and `ActivityRow`.

## Steps
1. **D7 on a closed day (review #2).** This applies to an activity at the club with rings, on a day missing from `club.openingHours`.
   - [PUBLICA] is disabled, with the closed-day note.
   - Saving a published activity whose ring window falls on a closed day shows the api's `422 OUTSIDE_OPENING_HOURS` on the date and time fields.
   - The activity mock checks the ring-block window against `club.openingHours` on publication and on the PATCH of a published activity. It answers `422 OUTSIDE_OPENING_HOURS`, as the api does.
   - A mock test and a Vitest.
2. **The D4 card on a closed day (review #6).** Only «Notes» stays editable: the chip editors are disabled, so [ACCEPTA] never disables silently. Vitest.
3. **One error helper (review #8).** A shared helper in `packages/api-client` reads a field error from both `details.fieldErrors[]` and `details.field`.
   - Use it in D7 (`activities/shared.tsx`), in planning (`planning/shared.ts`), in `SelfServicePages.tsx`, in `SignupPage.tsx` and in `ActivityDetailPage`.
   - Test: a single-field `VALIDATION_ERROR` lands on its field in D7.
4. **The closed-days literal (review #7).** `admin-scheduling:templates.bandForm.closedDays` becomes a label, so it reads well with one day or several, and with «dissabte» too:
   - ca «Dies de tancament: {days}. Aquesta plantilla no admet franges.»;
   - es «Días de cierre: {days}. Esta plantilla no admite franjas.»;
   - en «Closed days: {days}. This template cannot have time bands.».
   
   `{days}` is the long weekday names joined as a list in the reader's language, for example «dilluns i dimarts», «lunes y martes», «Monday and Tuesday». Vitest on the ca text with two days.
5. **Tests (review #1, #3, #4 and #5).**
   - T-07-29 and T-07-30 on the remaining D7 and app suites: the two e2e describes, the app Vitest describe, and the new «E4-W10 D7 on a day the club is closed» describe.
   - T-06-28 and T-06-26 on the new D4 and D3 describes.
   - The failed-opening-hours test asserts the error message and, after the retry, the closed-day note again.
   - An INSTRUCTOR on D4 and D3 never calls `GET /club/opening-hours` (record the requests). D7's non-editable views stop reading it too (`ActivityPage.tsx:414`).
6. **The api E5-T15 fields in the screens.** The api snapshot staged from `d791361` carries them. If E4-W05 has not adopted it yet, adopt it here first and prune the matching `pending.json` entries.
   - The staged snapshot may be later (api E5-T16, `22a8ea9`). E5-T16 declares the nulls the api sends: `ClubSettings` and the records it reaches (`Theme` logos, also on `/branding`; `LastChange.actorName`), the D1 blocks as `X | null`, and `TrainingBookingRequest.override`. `paymentProviders`, `iconUrls` and `imageConsentTextI18n` become required, and `PUT /club` returns `taxId` normalized (`""` clears it to `null`). Fix what the new types raise, and read `null` as absent.
   - **Screen 23's class drawer** reads `instructorNames[]` and `ring {id, name, color}` from the staff `GET /class-sessions/{id}`, no longer from the tapped cell (E4-W03 question 3). `ring` is `null` for a class without a ring. Vitest.
   - **The app rows** (`RegisteredActivity`, `ActivityRow`) show `startTime`–`endTime` when present, and no time when they are `null` (R-07-13): a date-only activity never reads «0:00». Vitest.
   - The mocks send the new fields like the api.

## Fixed conventions
- Design system only.
- Keys are never literals; ca, es and en are added together.
- Mocks behave like the api.
- Heavy runs go through the host lock.
- Fictional data only.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E4-W11/*.log`)
- `pnpm turbo run lint typecheck test build`.
- `pnpm i18n:check`.
- `pnpm e2e:docker`.

## Definition of Done
- Steps 1–6 are done. Steps 1–3 and 6 each have a test that fails before the fix.

## Evidence to return
- The command outputs.
- A step → test table.

---

## Executor report

**Outcome.** Steps 1–6 are done. The dependency E4-W10 is `verified`. The staged snapshot was `3f0381e`, which is api E5-T16 plus E3-T17. It is adopted byte for byte: `sha256 1895d0c2…a72f255` for the staged file and for `openapi.json`. `openapi.next.json` is removed. `pending.json` is unchanged, because none of its entries is published.

Every new or rewritten test was run against the HEAD product code first. 21 of them failed there (logs `08`–`11`), and they all pass on the fixed tree. The two INSTRUCTOR guards of D4/D3 already passed on HEAD: E4-W10 had gated them. A mutation check (log `12`) shows they fail when that gating is removed.

The final tree passes all three Verification commands:
- turbo `--force`: 34/34, 0 cached;
- `i18n:check`;
- the Docker e2e: 43 + 29 + 1.

### Evidence

**1. `pnpm turbo run lint typecheck test build --force`** → exit 0. The complete output is `roadmap/evidence/E4-W11/14-turbo.log`. The last 40 lines (ANSI colours stripped):
```
clubs-admin:test:    ✓ E3-W07 step 4 · M15 the plan and family decisions (R-04-13, T-04-33) (4)
clubs-admin:test:      ✓ a NOT_FOUND_PENDING claim shows what the applicant typed and is resolved by attaching the holder's group 536ms
clubs:test:  ✓ src/SignupPage.test.tsx (90 tests) 5961ms
clubs:test:    ✓ T-04-29 signup person and draft (7)
clubs:test:      ✓ persists a current draft, expires it after 24 hours and maps identity states 938ms
clubs:test:    ✓ E3-W08 step 7: the three narrow cases of the E3-W06 round-2 review (5)
clubs:test:      ✓ #1 a slow full page load: the departing page never renders nor consumes the destination's error 396ms
clubs:test: 
clubs:test:  Test Files  7 passed (7)
clubs:test:       Tests  171 passed (171)
clubs:test:    Start at  01:51:42
clubs:test:    Duration  9.69s (tests 36%, environment 35%, transform 16%, setup 7%, import 6%)
clubs:test: 
clubs-admin:test:  ✓ src/activities/ActivitiesPage.test.tsx (36 tests) 6796ms
clubs-admin:test:    ✓ T-07-29 D7 activities (list, maintenance, publication, cancellation) (21)
clubs-admin:test:      ✓ draws the four mockup rows with the R-07-13 dates, rings, registrations and state chips 458ms
clubs-admin:test:      ✓ «Nova activitat» creates a draft with title and type and opens its maintenance 498ms
clubs-admin:test:    ✓ T-07-29 E4-W08 D7 follow-ups of the E4-W04 round-2 review (6)
clubs-admin:test:      ✓ R-07-05 away from the club the hours cover the whole day: 6:00 when the club opens at 7:00 477ms
clubs-admin:test:    ✓ T-07-29 E4-W10 D7 follow-ups of the E4-W08 and E4-W09 reviews (3)
clubs-admin:test:      ✓ S07 §6 CONVENCIONS_API §4 the member filter reads every page: with 1,001 distinct registrants the member on the second page can be selected 652ms
clubs-admin:test:  ✓ src/planning/CalendarPage.test.tsx (37 tests) 7596ms
clubs-admin:test:    ✓ E3-W07 step 9 a D1 risk row opens D4 on its class (1)
clubs-admin:test:      ✓ selects the class of `?classe=` in the week of `?setmana=` 454ms
clubs-admin:test:    ✓ T-06-28 D4 / D4b / D4c class calendar (front half, MSW) (24)
clubs-admin:test:      ✓ opens «Esborrany» on the first week with drafts, with the validation card and its count 582ms
clubs-admin:test:      ✓ [VALIDAR LA SETMANA] turns every draft of the week active at once 377ms
clubs-admin:test:      ✓ cancels an active class with registrants through D4c: four rows, text required, «ANUL·LA I AVISA ELS 4 ALUMNES» 335ms
clubs-admin:test:      ✓ R-06-09 saves the diff with the version; STALE_VERSION keeps its message after the refetch remounts the card 399ms
clubs-admin:test: 
clubs-admin:test:  Test Files  19 passed (19)
clubs-admin:test:       Tests  276 passed (276)
clubs-admin:test:    Start at  01:51:42
clubs-admin:test:    Duration  11.65s (tests 40%, environment 31%, transform 16%, setup 7%, import 6%)
clubs-admin:test: 

 Tasks:    34 successful, 34 total
Cached:    0 cached, 34 total
  Time:    22.848s
```
Test totals from the same log:

| Package | Tests passed |
|---|---|
| api-client | 188 |
| i18n | 58 |
| ui | 65 |
| clubs | 171 |
| clubs-admin | 276 |
| course-core | 342 |
| shared-types | 21 |
| auth | 17 |
| id | 9 |

**2. `pnpm i18n:check`** → exit 0. The complete output is `13-i18n-check.log`:
```
> agilityhub-core-web@0.0.0 i18n:check /Users/jordib/dev/agilityhub/agilityhub-core-web
> node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs

i18n check passed
ca: 2330 keys
es: 2330 keys
en: 2330 keys
1754 statically used keys, 0 unused
250 codes, 0 missing
```

**3. `/Users/jordib/Dropbox/Documents/SOFTWARE_CANIC/05-desenvolupament/roadmap-kit/mac/heavy.sh pnpm e2e:docker E4-W11 > roadmap/evidence/E4-W11/15-e2e-docker.log 2>&1`** → exit 0. It ran through the host lock, on `mcr.microsoft.com/playwright:v1.63.0-noble`, with the three projects. The last 40 lines:
```
id:e2e: 
id:e2e: > id@0.0.0 e2e /work/apps/id
id:e2e: > playwright test
id:e2e: 
id:e2e: 
id:e2e: Running 1 test using 1 worker
id:e2e: ·
id:e2e:   1 passed (3.3s)
clubs-admin:e2e: cache bypass, force executing d779ea81d6aac340
clubs-admin:e2e: 
clubs-admin:e2e: > clubs-admin@0.0.0 e2e /work/apps/clubs-admin
clubs-admin:e2e: > playwright test
clubs-admin:e2e: 
clubs-admin:e2e: [WebServer] (node:892) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs-admin:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs-admin:e2e: 
clubs-admin:e2e: Running 43 tests using 9 workers
clubs-admin:e2e: ···········································
clubs-admin:e2e:   43 passed (27.2s)
clubs:e2e: cache bypass, force executing 4587af74a690bc25
clubs:e2e: 
clubs:e2e: > clubs@0.0.0 e2e /work/apps/clubs
clubs:e2e: > playwright test
clubs:e2e: 
clubs:e2e: [WebServer] (node:3050) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs:e2e: 
clubs:e2e: Running 29 tests using 7 workers
clubs:e2e: ·····························
clubs:e2e:   29 passed (12.6s)

 Tasks:    8 successful, 8 total
Cached:    0 cached, 8 total
  Time:    55.433s 

evidence: E4-W11/D7-desa-divendres-tancat-1280.png
evidence: E4-W11/D7-publica-diumenge-tancat-1280.png
evidence: E4-W11/D4-classe-diumenge-tancat-xips-desactivats-1280.png
evidence: E4-W11/D3-franja-dies-de-tancament-1280.png
exit 0 (shell status of the command, appended by the executor)
```
The `exit` line was appended by the executor from the shell status. The same line ends logs `13` and `14`.

Supporting runs, with the complete logs committed:
- `00-api-generate.log`: `pnpm api:generate`, exit 0.
- `01-typecheck-after-adoption.log`: exit 2, recorded on purpose. The adopted types break only `clubs`/`clubs-admin` `main.tsx`, where `Theme` logos became `string | null` against the ui `BrandingTheme`. That is fixed in `packages/ui/src/branding.tsx`.
- `02`, `03`, `05`, `06`, `07`: targeted Vitest runs.
- `04`: typecheck and lint, 20/20.

**Pre-fix proof.** The product files of this task were set temporarily to their HEAD content, and the tests were kept. The swap used `tmp.local/e4w11/swap.py` (git-ignored), with read-only `git show`. Then the complete Vitest suites ran, and the fixed files were restored. The runs:

| Log | Package | Failed | Total | Failing tests |
|---|---|---|---|---|
| `08` | api-client | 7 | 188 | the 4 `apiFieldErrors` tests, the 2 opening-hours mock tests, the `GET /club` nulls test |
| `09` | i18n | 3 | 58 | `formatList` ca/es/en |
| `10` | clubs-admin | 8 | 276 | D7 ×4, D4 closed-day chips, D3 ×3 (the two E4-W10 tests with the new label, and the two-days label) |
| `11` | clubs | 3 | 171 | the midnight row, the drawer ×2 |

- Nothing else failed.
- `12-mutation-instructor-hours.log`: with the `readOnly` gating of `loadOpeningHours` removed from `CalendarPage.tsx` and `TemplatesPage.tsx`, both «INSTRUCTOR never calls GET /club/opening-hours» tests fail. They pass when it is restored.

### Step → test table

| Step | Test (file › describe › test) | Failed before the fix |
|---|---|---|
| 1 mock | `packages/api-client/src/mocks/activity-handlers.test.ts` › «E4-W11 T-07-04 R-07-05 the ring-block window must fit club.openingHours…» › «publication: a closed Sunday is 422 OUTSIDE_OPENING_HOURS without a field; 06:00 with a 07:00 opening too; 10:00 publishes» and «PATCH of a published activity: a resync on a closed Friday is 422 and changes nothing; notes alone are saved; a wider ringBlockWindow is checked too» | yes (log `08`) |
| 1 UI | `ActivitiesPage.test.tsx` › «T-07-29 E4-W11 D7 follow-ups of the E4-W10 review» › «…a draft at the club with rings on a closed Sunday: [PUBLICA] is disabled…» and «R-07-05 T-07-04 saving the published Torneig on a closed Friday shows the api's 422 OUTSIDE_OPENING_HOURS on the date and both times» | yes (log `10`) |
| 1 e2e | `apps/clubs-admin/e2e/activities.spec.ts` › «T-07-29 E4-W10 D7 on a day the club is closed»: the Friday test now also saves and asserts `#activity-date-error`, `#activity-start-error` and `#activity-end-error`; a new Sunday-draft test asserts [PUBLICA] is disabled | new (green in `15`) |
| 2 | `CalendarPage.test.tsx` › «T-06-28 E4-W11 D4 follow-ups of the E4-W10 review» › «…on a closed Sunday only «Notes» is editable: every chip editor is disabled, and [ACCEPTA] saves the notes alone». Plus «an open day keeps the chip editors enabled», and the rewritten e2e «T-06-28 E4-W10 D4 on a day the club is closed» | yes (log `10`) |
| 3 | `api-error.test.ts` › «E4-W11 CONVENCIONS_API §5 apiFieldErrors reads both field shapes» (4 tests), and `ActivitiesPage.test.tsx` › «CONVENCIONS_API §5 a single-field VALIDATION_ERROR (details.field) lands on its field, not in a toast» | yes (logs `08`, `10`) |
| 4 | `TemplatesPage.test.tsx` › «T-06-26 E4-W11 D3 follow-ups of the E4-W10 review» › «…«Dies de tancament: dilluns i dimarts. …»» (it also checks «dissabte»), and `format.test.ts` › «E4-W11 joins weekday names as a list in ca/es/en» | yes (logs `09`, `10`) |
| 5 ids | T-07-29 on «T-07-29 E4-W08 D7 follow-ups» and «T-07-29 E4-W10 D7 on a day the club is closed» (e2e). T-07-30 on the app e2e «T-07-30 E4-W08 R-07-09/10 impersonated…» and on the Vitest «T-07-30 E4-W08 S07 §6 /activitats/:id is for MEMBER…». T-06-28 on the E4-W09/E4-W10 D4 Vitest and e2e describes. T-06-26 on the E4-W10 D3 Vitest and e2e describes | n/a (names) |
| 5 retry | `TemplatesPage.test.tsx` › «…a failed GET /club/opening-hours shows its error… after the retry the closed Monday is named again»: it asserts the error text, then the band drawer's «Dies de tancament: dilluns. …» and a disabled [Desa] | yes (log `10`, new literal) |
| 5 guards | «MATRIU_PERMISOS an INSTRUCTOR on D4 / on D3 never calls GET /club/opening-hours» (requests recorded), and D7 «an INSTRUCTOR and a finished activity never read GET /club/opening-hours» (then an editable one reads it once) | D7 yes (log `10`). D4/D3 passed on HEAD because E4-W10 gated them; they fail under mutation (log `12`) |
| 6 snapshot | `club-settings-handlers.test.ts` › «E4-W11 api E5-T16 GET /club answers ClubSettings with the nulls the api sends» (Ajv plus the nulls) | yes (log `08`) |
| 6 drawer | `OverviewPage.test.tsx` › «T-06-29 E4-W11 screen 23's class drawer reads the class detail (api E5-T15)» › «names the instructors (instructorNames[]) and the ring (ring.name)…» and «a class without a ring (ring: null) has no «Pista» row…» | yes (log `11`) |
| 6 rows | `activities.test.tsx` › «T-07-30 E4-W11 R-07-13 the app rows read the api's startTime/endTime (E5-T15)» › «an activity from midnight reads «0:00–2:00» / «0:00»…» and «a date-only activity… never «0:00»» | midnight: yes (log `11`). Date-only: passed on HEAD too, because the old `startsAtLocal` parser already dropped `T00:00` |

### What changed

- **Step 6 (adoption).** `openapi.json` = the staged `3f0381e`; `openapi.next.json` is removed; `schema.d.ts` is regenerated.
  - `packages/ui/src/branding.tsx`: `logoUrl`, `logoDarkUrl` and `markUrl` accept `null`, read as absent in `resolveBrandingLogo`.
  - `DashboardPage.tsx`: drops its hand-made nullable overlay and uses the published `Dashboard`.
  - `derived-settings.ts`: `paymentProviders` is now required.
  - Mock `GET /club` sends each optional `ClubSettings` block, `null` when the club has none: `address` from the fixture's registered office, `contactEmail`/`contactPhone`/`websiteUrl`/`lastChange`/`pwa` `null`, `legal` with `imageConsentTextI18n`, `displayCity`, `legalName`, `taxId`.
  - The `instructorNames`, `ring` and `startTime`/`endTime` mocks were already in place since E4-W05.
- **Step 6 (screens).**
  - `OverviewPage.tsx`: the class drawer reads `value.instructorNames` (joined) and `value.ring.name`. There is no «Pista» row for `ring: null` and no «Instructor» row without names. The risk text still comes from the cell.
  - `ActivityReservationRow.tsx` (03): prints `startTime`–`endTime`.
  - `ActivitiesBlock.tsx` (04): prints `startTime` only, as the mockup does («ds 12/09 · 9:00»).
  - `null` hours print nothing. `localHours` is removed.
- **Step 1.**
  - `ActivityPage.tsx`:
    - [PUBLICA] is `disabled={busy || closedDay}`, next to the existing closed-day note.
    - `saveErrorTargets` places `OUTSIDE_OPENING_HOURS` on `date`, `startTime` and `endTime`, by its code.
    - The `loadOpeningHours` effect runs only for an editable maintenance (`readsOpeningHours`).
  - `activity-handlers.ts` gets `openingHoursProblem()`: the window is `ringBlockWindow`, else the hours; `club.openingHours` is looked up for the date's weekday; a missing day or a window outside it → `422 OUTSIDE_OPENING_HOURS` with `details {}`. It runs on `POST …/publication` (after the incomplete and past checks) and on a re-syncing PATCH of a published activity, before the ring conflicts.
- **Step 2.** `SelectedClassCard.tsx`: `chipsEditable = editable && !closedDay`.
  - Every chip editor gets `disabled={busy || !chipsEditable}`: ring, levels, instructor(s), limit, time, description and the exemption chip.
  - On a closed day the chips show the class's own values and the diff carries only `notes`.
  - The old `closedDayLocked` path is gone.
- **Step 3.** `packages/api-client/src/api-error.ts` has the new `apiFieldErrors(error): {code, field}[]`, exported from the package. It reads `details.fieldErrors[]` (an entry without a code takes the error's) plus the single `details.field` (with the error's code), without duplicates.
  - Used by the five places the task lists: `activities/shared.tsx` `errorFields`, `planning/shared.ts` `fieldOfValidationError`, `SelfServicePages.tsx`, `SignupPage.tsx` and `ActivityDetailPage.tsx`.
  - Also used by two more readers of `fieldErrors`, so a single-field error lands everywhere: `dashboard/signup-review-errors.ts` (D2) and `catalogs/ClubPagesCard.tsx`.
- **Step 4.**
  - `admin-scheduling:templates.bandForm.closedDays` is replaced in ca, es and en with the task's literals.
  - The new `formatList` in `packages/i18n` uses `Intl.ListFormat` (conjunction, reader's locale) and is exposed on `useClubFormats()`. D3 joins the long weekday names with it.
- **Step 5.** The test ids above.
  - The D4 e2e «T-06-28 E4-W10 D4 on a day the club is closed» no longer types into the (now disabled) limit. It asserts that the editors are disabled, and its capture goes to E4-W11.
  - The D3 e2e checks the new label, and its capture goes to E4-W11.
- **Screenshots** (`roadmap/evidence/E4-W11/`, 1280 px, no mockup exists for these states):
  - `D7-desa-divendres-tancat-1280.png`: the api's message under Data, Hora d'inici and Hora de final.
  - `D7-publica-diumenge-tancat-1280.png`: [PUBLICA] disabled with «El club està tancat aquest dia».
  - `D4-classe-diumenge-tancat-xips-desactivats-1280.png`.
  - `D3-franja-dies-de-tancament-1280.png`.
- **Files.**
  - Product: `apps/clubs-admin/src/{activities/ActivityPage.tsx, activities/shared.tsx, planning/SelectedClassCard.tsx, planning/TemplatesPage.tsx, planning/shared.ts, catalogs/ClubPagesCard.tsx, catalogs/derived-settings.ts, dashboard/DashboardPage.tsx, dashboard/signup-review-errors.ts}` and `apps/clubs/src/{today/OverviewPage.tsx, activities/ActivitiesBlock.tsx, activities/ActivityReservationRow.tsx, activities/shared.ts, activities/ActivityDetailPage.tsx, SelfServicePages.tsx, SignupPage.tsx}`.
  - Packages: `packages/api-client/{openapi/openapi.json, openapi/openapi.next.json (deleted), src/api-error.ts, src/index.ts, src/generated/schema.d.ts, src/mocks/activity-handlers.ts, src/mocks/handlers.ts}`, `packages/i18n/src/{format.ts, locales/*/admin-scheduling.json}` and `packages/ui/src/branding.tsx`.
  - Tests: `api-error.test.ts` and `mocks/club-settings-handlers.test.ts` (new), `mocks/activity-handlers.test.ts`, `format.test.ts`, `ActivitiesPage.test.tsx`, `CalendarPage.test.tsx`, `TemplatesPage.test.tsx`, `OverviewPage.test.tsx`, `activities.test.tsx`, `App.test.tsx`, and the three clubs-admin e2e specs plus `apps/clubs/e2e/activities.spec.ts`.
  - Also `CHANGELOG.md`.

Rules and tests covered: R-07-05, R-07-13, R-02-09, R-06-01, R-06-09, CONVENCIONS_API §5; T-07-04 (mock), T-07-29, T-07-30, T-06-26, T-06-28, T-06-29.

### Assumptions

1. **Mock order.** On publication and on a re-syncing PATCH, the opening-hours check comes before the ring-conflict check, because a window outside the hours cannot be forced. `ACTIVITY_INCOMPLETE` and `ACTIVITY_IN_PAST` still come first on publication. A draft's PATCH never checks the hours, because a draft never blocks.
2. **The D4 exemption chip.** The chip «Exempta de la revisió de les 7:30» counts as one of the chip editors on a closed day, because «only Notes stays editable». [ANUL·LA LA CLASSE] and [ELIMINA] remain, so a class on a closed day can still be cancelled.
3. **Where the D7 message appears.** On `422 OUTSIDE_OPENING_HOURS` the same message appears under all three fields (date, start, end), as the step literally asks. There is no page toast.
4. **Screen 04.** The 04 row keeps printing only `startTime`, because the mockup's 04 row is «ds 12/09 · 9:00» for a 9:00–13:00 activity. Screen 03 prints `startTime`–`endTime`.
5. **An activity at midnight.** An activity that really starts at 00:00 (`startTime: "00:00"`) now prints «0:00». Only `null` means «no time».

### Questions / catalog proposals

1. **Plural label in the 23 drawer.** The «Instructor» label (`instructor:overview.class.instructors`, ca «Instructor») stays singular when `instructorNames` has several names. A plural form needs a literal decision; for now the names are joined with «, ».
2. **Dog readmission in the D2 mock.** E3-T17's `SignupDogView.readmission` is in the types now. The D2 mock's `readmission` variant does not send it yet, because MESSAGES 25-09 routes showing it to a later web task. No new parameter, event, notification or error code is added.
3. **Denied commands.** Blocked by the sandbox, each with an allowed alternative, and none needed:
   - a `for f in …` shell loop;
   - `npx vitest …` → `pnpm --filter … test`;
   - python and cat heredocs with `{…"…"}` → the Edit/Write tools;
   - `tar -cf tmp.local/…` → `cp` plus a git-ignored helper script.
4. **Formatting noise.** `prettier --write` on some touched files reformatted unrelated code: `handlers.ts`, `SignupPage.tsx`, `SelfServicePages.tsx`, `App.test.tsx`, `signup-review-errors.ts`. Those files were restored from HEAD and only this task's edits were re-applied, so the diff carries no reformatting.

## Organizer verification
Result: verified · 2026-09-26 · organizer (Claude)

Steps 1–6 are in place. 21 new or rewritten tests failed on the HEAD code (logs `08`–`11`), and the mutation of log `12` shows the INSTRUCTOR guards:
- D7 disables [PUBLICA] on a closed day with rings, and a published activity's save shows `422 OUTSIDE_OPENING_HOURS` on the date and times;
- the D4 card's chips are disabled on a closed day;
- one error helper reads `details.field` and `details.fieldErrors[]`;
- the closed-days label, and the T-ids;
- screen 23's drawer and the app rows read the api E5-T15 fields, from the adopted snapshot `3f0381e` (E5-T16 and E3-T17 included).

Evidence:
- turbo `--force` 34/34, `i18n:check`, and the Docker e2e 43 + 29 + 1 (logs `13`–`15`). The final tree of `c85c32f`, with E4-W06's edits, is covered by E4-W06's logs: 34/34, 2365 keys, 44 + 29 + 1;
- CI green at `c85c32f`.

The independent review (`roadmap/reviews/E4-W11-20260926-0232-claude.md`) has two minors and five nits. Codex could not answer, so a fresh Claude session reviewed. They go to the new E4-W14 (web) and E5-T20 (api).

Organizer decisions:
- **Review #1 → E4-W14 and E5-T20.** The api's preview `GET /activities/{id}/ring-conflicts` also answers `422 OUTSIDE_OPENING_HOURS` and `400 INVALID_TIME_RANGE`. The mock answers the same, and D7 places the 422 on the date and times. S07 §6 lists both errors now (26-09), and the api declares them in E5-T20.
- **Review #2 → E4-W14:** the disabled chips look disabled.
- **Review #3 → E4-W14.** Assumption 2 is overruled: the exemption is its own action, and the api accepts it on a closed day. Its chip stays enabled.
- **Review #4 → E4-W14.** Assumption 4 is overruled: 04 prints `startTime`–`endTime` when both are present (R-07-13). The mockup's «9:00» row is an activity without an end.
- **Review #5 and question 1 → E4-W14:** the label follows the count («Instructor» / «Instructors»), and the names use `formats.formatList`.
- **Review #6, accepted:** the final tree is proven by E4-W06's logs.
- **Review #7 → E4-W14:** a test with `null` logos, and the minimal branding fixture sends `null`, as the api does.
- **Question 2:** the D2 mock sends the dog's `readmission` in E4-W13.
- **Assumptions 1, 3 and 5, accepted.**
