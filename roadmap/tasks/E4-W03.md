---
id: E4-W03
title: Screens 10 «Classes del dia» (/avui) and 23 «Visió global» (/instructor/avui) — shared DayGrid in packages/ui, «Ocupada» state, risk balloon, student view without counts, instructor global view
stage: E4
repo: agilityhub-core-web
thread: B
order: 30
status: verified
depends_on: [E1-W02, E2-W03, external:api:E4-T01]
branch:
pr:
updated: 2026-09-24
---

# E4-W03 · Screens 10 «Classes del dia» (/avui) and 23 «Visió global» (/instructor/avui) — shared DayGrid in packages/ui, «Ocupada» state, risk balloon, student view without counts, instructor global view

## Objective
Work package S06 P6: the student sees the day as a ring × time grid (screen 10: description + instructor only when the api sends it, «Ocupada» cells without saying who, the at-risk class with its balloon, never a count — decision A22 e) and the instructor sees the whole club at a glance (screen 23: `n/n +e`, the planned instructor always, training bookings with names, ring blocks, cancelled classes dimmed). Both screens render `GET /day-grid` (form D) through one shared `DayGrid` component in `packages/ui`, also used by the desktop day views of E4-W01/E4-W02. Mocks-first on the S06 contract of api E4-T01; pixel-faithful to the V8 mockups at 375 px.

## Spec references
S06 §2 rows 10 and 23 · R-06-12 (what each role sees: table per element and view), R-06-13 (risk display: `atRisk` + `riskText` computed by the api, ICU texts for 0 and 1 dog), R-06-14 (the «today» date is the club's, never the device's), R-06-15 (`WAITLIST` off → no «+e»; `FREE_TRAINING` off → no training cells; `ACTIVITIES` off → no `ACTIVITY` cells; `COURSES` off → no `activeSetupId`) · §6 row `GET /day-grid?date=YYYY-MM-DD&view=member|instructor` + form D (cells `CLASS · OCCUPIED · TRAINING · BLOCK · ACTIVITY`, stacking, `rows: []` for an empty day; impersonation tokens only with `view=member`), row `GET /class-sessions/{id}` («Altres formes»: instructor projection), rows `/ring-blocks/{id}`, `POST /ring-blocks/{id}/cancellation` · §10 (`home:today.*`, `instructor:overview.*`, `enums:*`) · §13 assumption 2 (no counts for the student) · tests **T-06-29, T-06-30** · `docs/DECISIONS_PENDENTS.md` A22 (d, e) · `MATRIU_PERMISOS.md` (`/day-grid`: MEMBER as «Ocupada» without who; `view=instructor` only INSTRUCTOR/ADMIN → 403) · `CATALEG_ERRORS.md` (`RING_BLOCK_MANAGED_BY_ACTIVITY`, `INVALID_STATE` on block cancellation) · `docs/PLA_FRONTEND.md` §2 (10 → `/avui`, 23 → `/instructor/avui`) · mockups `docs/pantalles/mobil/10-avui-classes-del-dia-quadre.*`, `23-instructor-visio-global-quadre-del-dia.*`.

## Context to load
S06 §2 (10, 23), §4 (R-06-12, 13, 14, 15), §6 (form D + «Altres formes»), §10, §11 (front), §13 · api tasks `E4-T01` (schemas `DayGrid`, `DayGridColumn`, `DayGridRow`, `DayGridCell`, `ClassSession`, `RingBlock`) and `E4-T03` step 8 (`DayGridQuery`: rows, columns, «Sense», per-view fields) · `apps/clubs/src/App.tsx` (`MOBILE_ROUTES` `/avui`, `/instructor/avui`, `MobileNavigation`, `routeAfterOnboarding`, `MobileShell`), `apps/clubs/src/InfoPage.tsx` (page pattern with `client`, skeleton/empty/error), `apps/clubs/src/SelfServicePages.tsx` (drawer usage on mobile) · `packages/ui/src/{components.tsx,components.css,tokens.css}` and, if E4-W01 has landed, `packages/ui/src/schedule-grid.tsx` (`ScheduleGrid`/`ScheduleCell`: build `DayGrid` on top of it and reuse its cell; if it has not landed, create `schedule-grid.tsx` yourself with the same exported names and props described in E4-W01 step 3 so E4-W01 adopts it — say which case applied in the report) · `packages/i18n/src/{types,resources,format}.ts` (`useClubFormats`, `fmtDate(weekdayShort|dayMonth)`) · `packages/api-client` (`pending.json`, MSW `handlers.ts`, `scenarios.ts`, `fixtures.contract.test.ts`, fixtures `me-member.json`, `me-instructor.json`, `me-impersonated.json`) · `docs/playbooks/screen-from-mockup.md` · this file.

## Steps
0. **Contract.** Normal path: the organizer staged `packages/api-client/openapi/openapi.next.json` (api E4-T01; the commit message names the api commit) → swap it into `openapi.json` byte for byte, `pnpm api:generate`, prune every S06/S07 path and schema from `pending.json`, run the Ajv fixture suite — unless E4-W01 has already done it (then only verify `openapi.json` contains `/api/v1/day-grid`). If no snapshot is staged when you start: mocks-first — declare `GET /day-grid`, `GET /class-sessions/{id}`, `GET /ring-blocks/{id}`, `POST /ring-blocks/{id}/cancellation` in `pending.json` from S06 §6 with E4-T01's schema names (`DayGrid`, `DayGridCell`…) and Ajv-validated fixtures; say so in the report (the organizer swaps later).
1. **Routes and navigation** (`apps/clubs/src/App.tsx`): `/avui` (`?date=YYYY-MM-DD`) for MEMBER (and IMPERSONATED — the impersonation banner stays; only `view=member`), `/instructor/avui` for INSTRUCTOR and ADMIN (the shell's registered route for screen 23 per `docs/PLA_FRONTEND.md`; S06 §2 writes `/instructor/visio-global` — keep `/instructor/avui`, note it in the report). Fix the tab bar: the «Avui» tab (`shell:nav.today`) must open `/avui` for the MEMBER active profile (mockup 10 tab bar: Inici · Reservar · Entrenaments · Avui · Perfil · Info); for the INSTRUCTOR active profile the same slot is «Visió global» (`shell:nav.globalView`, new key) → `/instructor/avui` (mockup 23 tab bar; its other tabs «El meu dia», «Alumnes» are S10/E6 — do not add them). Both are wired in `routeContent`: `apps/clubs/src/today/TodayPage.tsx` (10) and `apps/clubs/src/today/OverviewPage.tsx` (23), sharing `apps/clubs/src/today/useDayGrid.ts` (query + date state).
2. **`DayGrid` in `packages/ui/src/day-grid.tsx`** (exported from `packages/ui/src/index.ts`, styles in `day-grid.css`): props `columns: {id, label, color?}[]`, `rows: {time, cells: DayGridCellModel[]}[]`, `view: "member" | "instructor"`, `onCellPress?(cell)`, `selectedCellId?`, `noRingLabel`, `labels` (aria strings) — a pure presenter over form D (the page maps the api response; no fetching inside). Layout per mockup: column headers = `Ring.shortName` upper-cased by CSS in the ring colour (CSS custom property from `column.color`, pattern of `RingsPage.tsx`; no literal colours), **identical column widths** (`grid-template-columns: repeat(n, minmax(0, 1fr))`; the row-label column fixed), one row per distinct start time («8:30», «17:40» — the api `time` string, leading zero stripped), cells stacked when two share a column in a row, empty positions drawn as dashed placeholders. Cell variants: `CLASS` → line 1 `description`, line 2 `instructorName` (member view: line 2 only when the api sends it — otherwise nothing, never a count; instructor view: «{booked}/{capacity}» + « +{waiting}» when `waiting > 0` + « · {instructorName}»), background tinted with the ring colour; `atRisk` → warning outline + line 2 «en risc ⚠» (`home:today.atRisk`; instructor view keeps the counts and adds the outline); `state = CANCELLED` (instructor view only) → dimmed + «anul·lada» (`enums:classState.CANCELLED`); `OCCUPIED` (member) → «Ocupada» + reason in **grey text without background** (`home:today.occupied`, reasons `enums:occupiedReason.TRAINING` «entren.», `MAINTENANCE` «manteniment», other `RingBlock` reasons through `enums:ringBlockReason.*` — `PRIVATE_CLASS` «classe particular», `THERAPY` «teràpia», `PREPARATION` «preparació», `OTHER` «altres» are proposed literals); `TRAINING` (instructor) → «Entren.» + `who[]` joined by « + » is already the api format («Pau + Blat»): one line per entry; `BLOCK` (instructor) → «Bloq.» + reason (note and `createdByName` in the detail, not in the cell); `ACTIVITY` (both) → «Activitat · {title}» (`home:today.activity`). Column «Sense» (`noRingLabel`) only when a cell has no `ringId`. Unit tests of the presenter in `packages/ui/src/day-grid.test.tsx` (stacking, «Sense», member cells never render `occupancy` even if present in the model).
3. **Screen 10** `/avui`: `AppBar` title «Classes del dia» (`home:today.title`); date header «‹ {dt 4 d'agost} ›» = `home:today.dateHeader` «{weekday} {date}» with `weekday = fmtDate(date, "weekdayShort")` and `date = fmtDate(date, "dayMonth")` (club `timeZone`), arrows ±1 day (Sunday reachable only by arrows); chips dl–ds of the ISO week of the selected date («dl 3» = `weekdayShort` + day number), selected chip filled; default date = **club-local today** (`useBranding().timeZone`: derive `YYYY-MM-DD` with `Intl.DateTimeFormat("en-CA", {timeZone})`, never `new Date().toISOString()`); `?date=` overrides. Data `GET /day-grid?date=&view=member` → `DayGrid view="member"`; tapping an `atRisk` class opens the balloon below the grid (`role="status"`, arrow pointing at the cell, one balloon at a time, tap again or elsewhere closes it) with the api `riskText` **as delivered** (already in the reader's locale, R-06-13 — do not re-implement the ICU text; the MSW fixture carries the mockup sentence). Footer `home:today.footer` «Ocupada = pista reservada per a entrenament o bloquejada». Empty (`rows: []`): «Cap classe aquest dia» (`home:today.empty`). Loading skeleton; error toast + retry. The mockup cell «Teràpia 1 g · Estel» is **not** reproduced (A22 e): the api member view never sends `occupancy`.
4. **Screen 23** `/instructor/avui`: title «Visió global» (`instructor:overview.title`), same date header and chips, `GET /day-grid?date=&view=instructor` → `DayGrid view="instructor"`; footer `instructor:overview.footer` «n/n +e = inscrits/places + llista d'espera · l'instructor previst es mostra sempre». Tap: `CLASS` → `Drawer` with `GET /class-sessions/{id}` (instructor projection: `displayDescription`, `date` + `startTime–endTime`, `ring.name`, instructors, «{booked}/{capacity} +{waiting}», state chip, `atRisk` text) — the attendance screen 21 (S10) replaces this drawer in E6; `BLOCK` → `Drawer` with reason, `fromLocal–toLocal`, `note`, `createdByName` and [Anul·la el bloqueig] (`POST /ring-blocks/{id}/cancellation {}`; 422 `RING_BLOCK_MANAGED_BY_ACTIVITY` → button hidden when `activityId ≠ null`, message otherwise; 409 `INVALID_STATE` → message + refetch; literals proposed: «Bloqueig de pista», «Motiu», «Nota», «Creat per», «Anul·la el bloqueig»); `TRAINING` and `ACTIVITY` → no action in E4 (S09/E5, S07 detail is E4-W04's member route, not for instructors). ADMIN accounts with the INSTRUCTOR profile see the same page.
5. **Roles and modules**: `/instructor/avui` behind `RequireRole(["INSTRUCTOR","ADMIN"])`; `/avui` behind `RequireAuth`; nothing module-gated (the api omits `TRAINING`/`ACTIVITY` cells when the modules are off); the «+e» suffix only when `useBranding().modules` includes `WAITLIST` (the api omits `waiting` too).
6. **i18n**: new namespaces `home` (`today.*`) and `instructor` (`overview.*`) registered in `types.ts` + the three loaders; additions to `enums` (`classState.*`, `occupiedReason.*`, `ringBlockReason.*` — create the namespace if E4-W01 has not) and `shell:nav.globalView` «Visió global»; ca = mockup literals; es/en in the same task; vocabulary lint green.
7. **MSW**: fixture `packages/api-client/src/mocks/fixtures/day-grid.ts` reproducing form D for the mockups — member view of 2026-08-04 (rows 8:30 «C i sup. · Marc»/«A+B · Estel», «Ocupada manteniment» on Carretera; 9:30 «Ocupada entren.» on Muntanya + «Cadells · Núria»; 17:40 «D+E · Marc», «Teràpia · Estel» **without** occupancy; 18:50 «A+B · Estel», «Ocupada entren.», «Cadells · Marc»; 20:00 «B+C · Estel», «D i sup.» `atRisk: true` with `riskText` «Aquesta classe només té un alumne: si ningú més s'hi apunta abans de les 7:30 de dimarts, s'anul·larà.», and a class with `instructorName: null` (24 h rule)), instructor view of 2026-08-03 (8:00 `TRAINING` «Pau + Blat» on Carretera; 8:30 «C i sup. 3/5 · Marc», «A+B 4/5 · Estel»; 9:30 «Cadells 3/4 · Núria»; 16:00 `BLOCK` `MAINTENANCE` on Carretera; 17:40 «D+E 4/5 · Marc», «Teràpia 1/1 · Estel»; 18:50 «B+C 5/5 +2 · Marc», «Cadells 2/4 · Marc»; 19:00 two `TRAINING` cells «Júlia + Kira», «Sergio + Thai»; one `CANCELLED` class), an empty day (`rows: []`), one day with an `ACTIVITY` cell and a class without ring (column «Sense»); `class-sessions/{id}` instructor projection for the tapped class; `ring-blocks/{id}` + cancellation. Names in fixtures are fictional (the mockup first names are acceptable only as fixture data). `view=instructor` by a member scenario → 403 handler. Scenarios: `member`, `instructor`, `impersonated` (existing) + `dayGridEmpty`. Update the handler-count assertion.
8. **Tests and evidence**: Vitest `apps/clubs/src/today/TodayPage.test.tsx` + `OverviewPage.test.tsx` **T-06-29**: identical column widths (assert the computed `gridTemplateColumns`), «Ocupada» rendered in the grey no-background class, the balloon appears on tap with the fixture `riskText`, 23 shows «5/5 +2 · Marc» and the training names, the «today» date is the club's (fake system time at 2026-08-03T23:30Z with `branding.timeZone = "Europe/Madrid"` → requests `date=2026-08-04`; with a device in `America/Bogota` the same), member view never shows a count even when a fixture cell carries `occupancy`; **T-06-30**: vocabulary lint over `home`, `instructor`, `enums` (ca/es/en) and the exact `ca` literals «Classes del dia», «Ocupada = pista reservada per a entrenament o bloquejada», «Visió global», «n/n +e = inscrits/places + llista d'espera · l'instructor previst es mostra sempre», «Cap classe aquest dia». Playwright `apps/clubs/e2e/day-grid.spec.ts` (375 × 812, scenarios `member`, `instructor`, `impersonated`) with screenshots under `roadmap/evidence/E4-W03/`: `10-avui-375.png` (balloon open on the 20:00 class), `10-avui-buit-375.png`, `10-avui-impersonat-375.png`, `23-visio-global-375.png`, `23-visio-global-classe-375.png` (drawer open); compare with `docs/pantalles/mobil/10-*.png`, `23-*.png`.

## Fixed conventions
- Design system only; no literal colours (ring colours through CSS custom properties); keys never literals; ca/es/en together; vocabulary rules (never «parell*», «amigable», «(paràmetre)», requirement codes; the mockup annotation «s'avisarà la parella» must not leak into any key).
- Dates through `packages/i18n` formatters with the club `timeZone`; the selected day and «today» are club-local (R-06-14).
- The api decides visibility: the front renders `instructorName`, `occupancy`, `riskText`, `atRisk`, cell kinds as delivered and never derives counts, names or risk texts (A22 e, R-06-12, R-06-13).
- Mockup fidelity checked with screenshots against `docs/pantalles/mobil/…`; expected deviations: no «1 g» count on 10 (A22 e), descriptions in the single api form (A22 d), the instructor tab bar limited to «Visió global» until E6.
- Evidence rule: command + exit code + last 40 lines; long outputs → `roadmap/evidence/E4-W03/NN-<name>.log`; task file ≤ 120 KB; complete `pnpm e2e` (three projects) in `mcr.microsoft.com/playwright:v1.63.0-noble` before `awaiting_verification`; fictional data only (`@example.test`); never edit other tasks, `ROADMAP.md` or the Organizer verification section; closed catalogs — proposals (block drawer literals, `occupiedReason`/`ringBlockReason` literals, `shell:nav.globalView`) in the report + `MESSAGES.md`.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `pnpm turbo run lint typecheck test build`; `pnpm i18n:check`; Vitest T-06-29, T-06-30 + `day-grid.test.tsx`; Ajv suite; complete `pnpm e2e` in Docker (`scripts/e2e-docker.sh`); the five 375 px screenshots.

## Definition of Done
Screens 10 and 23 render form D through the shared `DayGrid` for member, impersonated and instructor sessions in ca/es/en, with the balloon, «Ocupada», stacking, «Sense», empty and error states, the club-local date, and the tab bar fixed; `DayGrid` is exported from `packages/ui` for the desktop day views.

## Evidence to return
Command outputs, screenshots, the `home`/`instructor`/`enums` key lists, whether `ScheduleGrid` was reused or created, deviations from the mockup with their decision id.

---

## Executor report
Session 2026-09-24 (claude:claude-opus-5-5). Screens 10 and 23 are done on the shared `DayGrid`, for member, impersonated and instructor sessions, in ca/es/en. Every Verification command below passed (exit 0), including the complete `pnpm e2e` of the three projects in `mcr.microsoft.com/playwright:v1.63.0-noble`.

**Step 0, contract.** E4-W01/E4-W02 had already adopted the S06 snapshot. `packages/api-client/openapi/openapi.json` contains `/api/v1/day-grid`, `/api/v1/class-sessions/{id}`, `/api/v1/ring-blocks/{id}` and `/api/v1/ring-blocks/{id}/cancellation` (checked with a JSON diff). `pending.json` holds no S06/S07 path, and nothing was added to it.
- The staged `openapi.next.json` (api `a202870`) is a newer S10 snapshot. It adds `/attendances`, `/instructor/day`, and `attendanceStatus` on `DayGridCell`/`ClassSession`, among others.
- I did **not** swap it in: it belongs to its adoption task, and nothing in 10/23 needs it.

**`ScheduleGrid` case.** E4-W01 had landed, so its **cell was reused**. `DayGrid` renders every cell with `ScheduleCell` and draws empty positions with the `ah-schedule-cell--empty` placeholder. `ScheduleCell` gains two things, both backward compatible:
- a `plain` variant (grey text, no background) for «Ocupada», «Entren.» and «Bloq.»;
- the click event, so the page can place the balloon arrow.

The row/column frame is **not** the `ScheduleGrid` `<table>`. That table has a desktop `min-width` of 8rem per column and scrolls at 375 px, while V8 asks for identical columns that fit the phone. `DayGrid` is a CSS grid with `grid-template-columns: var(--ah-day-grid-time, 2.25rem) repeat(n, minmax(0, 1fr))` and ARIA table roles. It is exported from `@agilityhub/ui`. The D3b/D4 day views of E4-W01/E4-W02 are awaiting verification, so I did not migrate them (other tasks' code).

### Branch / commits
None. `.git` is not written in this session; the publish script commits the working tree on `main`.

Note: `git status` also lists files this session did not touch. They were probably changed by a concurrent session: `docs/*`, the `admin-census`/`admin-dashboard` locales, `roadmap/ROADMAP.md`, `roadmap/tasks/E3-W03|E4-W01|E4-W02|E4-W06.md`, the new `roadmap/tasks/E3-W04.md` and `roadmap/reviews/E0-W08-*`. I left them as they are.

### Evidence (full command outputs)
Complete logs are in `roadmap/evidence/E4-W03/` (index in `README.md`). ANSI colours are stripped in the tails below.

**1. `pnpm turbo run lint typecheck test build`** (run as `pnpm --dir <repo> exec turbo run lint typecheck test build`) → **exit 0**. Full log: `01-turbo-lint-typecheck-test-build.log` (final run, after the last CSS change). Last lines:
```
clubs-admin:test:  Test Files  14 passed (14)
clubs-admin:test:       Tests  75 passed (75)
 Tasks:    34 successful, 34 total
Cached:    18 cached, 34 total
  Time:    10.836s
```
Per-package test results in the same log: api-client 53 · course-core 342 · shared-types 21 · ui 49 · i18n 18 · auth 17 · id 9 · clubs 46 · clubs-admin 75, all passed. The 18 cached tasks replay identical-input runs from earlier in this session; every package whose inputs changed ran as a cache miss. The only stderr is the existing `[BrandingProvider] … contrast (3.32:1)` warning. The build ends with `✓ built` for clubs, clubs-admin and id.

**2. `pnpm i18n:check`** → **exit 0** (`02-i18n-check.log`):
```
i18n check passed
ca: 2014 keys
es: 2014 keys
en: 2014 keys
1456 statically used keys, 0 unused
250 codes, 0 missing
```

**3. Vitest T-06-29, T-06-30, `day-grid.test.tsx` and the Ajv suite** (`vitest run --reporter=verbose` in `apps/clubs` for `src/today src/App.test.tsx`, in `packages/ui` for `src/day-grid.test.tsx src/schedule-grid.test.tsx`, and in `packages/api-client` for `fixtures.contract.test.ts index.test.ts calendar-handlers.test.ts`) → **exit 0 / 0 / 0**. Full log: `03-vitest-e4-w03.log`.
```
clubs:       Test Files  4 passed (4)   Tests  34 passed (34)
ui:          Test Files  2 passed (2)   Tests   9 passed (9)
api-client:  Test Files  3 passed (3)   Tests  52 passed (52)
 ✓ literals.test.ts > T-06-30 … > has no forbidden term nor internal code in home, instructor and enums (ca/es/en)
 ✓ literals.test.ts > T-06-30 … > keeps the exact ca literals of mockups 10 and 23
 ✓ TodayPage.test.tsx > T-06-29 screen 10 … > draws the mockup grid with identical ring columns, the date header and the week chips
 ✓ TodayPage.test.tsx > T-06-29 screen 10 … > renders «Ocupada» + generic reason in grey without background and never a count
 ✓ TodayPage.test.tsx > T-06-29 screen 10 … > opens the risk balloon with the api riskText on tap and closes it on a second tap or elsewhere
 ✓ TodayPage.test.tsx > T-06-29 screen 10 … > uses the club's today (Europe/Madrid) whatever the device time zone
 ✓ TodayPage.test.tsx > T-06-29 screen 10 … > never shows a count in the member view even when a cell carries occupancy
 ✓ TodayPage.test.tsx > T-06-29 screen 10 … > moves by chips and arrows (Sunday only by the arrows) and keeps ?date in the address
 ✓ TodayPage.test.tsx > T-06-29 screen 10 … > shows the «Sense» column and the activity cell
 ✓ TodayPage.test.tsx > T-06-29 screen 10 … > shows the empty state (dayGridEmpty) and the error toast with retry
 ✓ OverviewPage.test.tsx > T-06-29 screen 23 … > shows n/n +e with the planned instructor, training names, the block and the cancelled class
 ✓ OverviewPage.test.tsx > T-06-29 screen 23 … > drops «+e» when the club has no WAITLIST module
 ✓ OverviewPage.test.tsx > T-06-29 screen 23 … > opens the class drawer with the instructor projection of the tapped class
 ✓ OverviewPage.test.tsx > T-06-29 screen 23 … > opens the block drawer and cancels the block (POST /ring-blocks/{id}/cancellation)
 ✓ OverviewPage.test.tsx > T-06-29 screen 23 … > maps 409 INVALID_STATE and 422 RING_BLOCK_MANAGED_BY_ACTIVITY on the block cancellation
 ✓ OverviewPage.test.tsx > T-06-29 screen 23 … > answers 403 to a member asking for the instructor view
 ✓ App.test.tsx > T-02-14 clubs shell > E4-W03 turns the «Avui» slot into «Visió global» for the instructor profile
 ✓ day-grid.test.tsx > DayGrid presenter … (7 tests: identical columns + time label, stacking + placeholders, «Sense» only without ring, member never renders occupancy, «n/n +e · instructor» and «+e» only with WAITLIST, risk/cancelled/occupied/block/activity, member hides cancelled)
 ✓ fixtures.contract.test.ts > E4-W03 day-grid fixtures follow the S06 contract … (11 tests: DayGrid Ajv for 4 dates × 2 views, the R-06-12 member projection, the screen 23 content, ClassSession + RingBlock Ajv)
```

**4. Complete `pnpm e2e` in Docker: `scripts/e2e-docker.sh`** (run as `pnpm --dir <repo> e2e:docker`) → **exit 0**. Final log: `05-e2e-docker-final.log`. Run 1 (`04-e2e-docker.log`) also passed; after comparing its screenshots with the mockups I made two changes and ran the suite again. First, the 6-tab bar overlapped «Entrenaments»/«Avui» at 375 px. Second, «Ocupada» was cut to «Ocup…»; I fixed the tab columns (auto-sized) and scaled the grid type to the mockup. Final run, last lines:
```
clubs-admin:e2e: Running 29 tests using 9 workers
clubs-admin:e2e:   29 passed (24.5s)
id:e2e: Running 1 test using 1 worker
id:e2e:   1 passed (4.0s)
clubs:e2e: Running 25 tests using 6 workers
clubs:e2e:   25 passed (13.5s)
 Tasks:    8 successful, 8 total
Cached:    0 cached, 8 total
  Time:    57.341s
evidence: E4-W03/10-avui-375.png
evidence: E4-W03/10-avui-buit-375.png
evidence: E4-W03/10-avui-impersonat-375.png
evidence: E4-W03/23-visio-global-classe-375.png
evidence: E4-W03/23-visio-global-375.png
```

**5. Screenshots, 375 × 812** (`apps/clubs/e2e/day-grid.spec.ts`, scenarios `member`, `dayGridEmpty`, `impersonated`, `instructor`), all in `roadmap/evidence/E4-W03/`:
- `10-avui-375.png`: dt 4, balloon open on the 20:00 «D i sup.».
- `10-avui-buit-375.png`: empty day.
- `10-avui-impersonat-375.png`: the banner, over the member view.
- `23-visio-global-375.png`: dl 3.
- `23-visio-global-classe-375.png`: drawer open on «B+C».

Compared with `docs/pantalles/mobil/10-*.png` and `23-*.png`: same grid (5 identical columns MUN·CEN·CAR·CAD·PET in the ring colours, the same rows and cells), same chips, header, footer and balloon. The deviations are listed under Assumptions.

### Files changed
- **ui**
  - `packages/ui/src/day-grid.tsx`, `day-grid.css`, `day-grid.test.tsx` (new).
  - `index.ts` (export + CSS).
  - `schedule-grid.tsx` / `.css`: the `plain` variant and the click event.
  - `components.css`: TabBar columns auto-sized, `nowrap` labels.
- **clubs app**
  - `apps/clubs/src/today/{useDayGrid.ts, shared.tsx, TodayPage.tsx, OverviewPage.tsx, today.css, TodayPage.test.tsx, OverviewPage.test.tsx, literals.test.ts}` (new).
  - `apps/clubs/src/App.tsx`: routes, content, detail shell, tab bar.
  - `apps/clubs/src/App.test.tsx`.
  - `apps/clubs/e2e/day-grid.spec.ts` (new).
  - `apps/clubs/e2e/shell.spec.ts`: the member now has «Avui» → `/avui`; this E0-W06 assertion said the opposite.
- **i18n**
  - `packages/i18n/src/locales/{ca,es,en}/home.json` and `instructor.json` (new).
  - `{ca,es,en}/enums.json` (`occupiedReason`) and `{ca,es,en}/shell.json` (`nav.globalView`).
  - `packages/i18n/src/types.ts` and `resources.ts` (namespaces + loaders).
  - `scripts/i18n-check.mjs`: dynamic prefix `enums:occupiedReason.`.
- **api-client mocks**
  - `packages/api-client/src/mocks/fixtures/day-grid.ts` and `day-grid-handlers.ts` (new).
  - `handlers.ts`: registered before `calendarHandlers`; other dates, classes and blocks fall through to the E4-W02 world.
  - `scenarios.ts` (`dayGridEmpty`).
  - `planning-handlers.ts`: `resetPlanningState` also resets the day-grid blocks.
  - `fixtures.contract.test.ts` (E4-W03 Ajv block).
  - `index.test.ts`: handler count 155 → 159.
- **roadmap**: `CHANGELOG.md`, `roadmap/evidence/E4-W03/*`, `roadmap/MESSAGES.md`, this file.

**Key lists.**
- `home:today.*`: `title`, `dateHeader`, `dayChip`, `previousDay`, `nextDay`, `weekDays`, `grid`, `time`, `occupied`, `atRisk`, `activity`, `noRing`, `footer`, `empty`, `loading`, `loadError`, `retry`.
- `instructor:overview.*`: `title`, `footer`, `training`, `block`, `close`.
- `instructor:overview.class.*`: `loading`, `loadError`, `when`, `ring`, `instructors`, `occupancy`, `state`, `risk`.
- `instructor:overview.blockDrawer.*`: `title`, `reason`, `note`, `createdBy`, `cancel`, `cancelling`, `loading`, `loadError`.
- `enums:occupiedReason.{TRAINING, MAINTENANCE}`. The other reasons are read from the existing `enums:ringBlockReason.*`, and «anul·lada» from the existing `enums:classState.CANCELLED`.
- `shell:nav.globalView`.

### Rules and tests implemented (R-xx-nn / T-xx-nn)
- **R-06-12**: what each view shows.
  - Member: description + `instructorName` only as delivered (`null` → no line 2), never a count even if `occupancy` arrives, «Ocupada» + generic reason, no cancelled classes.
  - Instructor: «n/n +e · name», «Entren.» + `who[]`, «Bloq.» + reason (note and author in the drawer), dimmed «anul·lada».
  - Both views: «Activitat · {title}»; «Sense» only when a cell has no `ringId`; rows = the api `time` values without the leading zero.
- **R-06-13**: warning outline + «en risc ⚠» on the cell. The balloon (`role="status"`) shows `riskText` exactly as delivered. One balloon at a time; a second tap or a tap elsewhere closes it. The instructor view keeps the counts and adds the outline.
- **R-06-14**: the default date is `Intl.DateTimeFormat("en-CA", {timeZone: branding.timeZone})`. Dates are formatted at 12:00Z with the club formatters. The test sets the system clock to 2026-08-03T23:30Z and the device zone to `America/Bogota`, and the page requests `date=2026-08-04`.
- **R-06-15**: «+e» only with `WAITLIST` (the front gates it too). `FREE_TRAINING`/`ACTIVITIES` off → the mock omits those cells; the front does not gate them (the api decides). `activeSetupId` is sent only with `COURSES`.
- **Tests**: **T-06-29** (TodayPage, OverviewPage, App tab bar, e2e `day-grid.spec.ts`) and **T-06-30** (`literals.test.ts`).
- **MATRIU_PERMISOS**: `/instructor/avui` behind `RequireRole(["INSTRUCTOR","ADMIN"])`, `/avui` behind `RequireAuth`. The mock answers `view=instructor` with 403 `FORBIDDEN` for a member and 403 `IMPERSONATION_DENIED` for an impersonation.

### Assumptions
- **Routes**: `/instructor/avui` is kept for screen 23 (shell registry and `PLA_FRONTEND.md`). S06 §2 writes `/instructor/visio-global`.
- **Tab bar**: the slot is «Visió global» when the active profile is INSTRUCTOR **or ADMIN**, and «Avui» otherwise. «El meu dia» and «Alumnes» are not added (E6). The other tabs (Inici, Reservar, Entrenaments, Perfil, Info) stay for the instructor profile, as the shell had them.
- **Mockup deviations**:
  - No «1 g» count on 10 (A22 e).
  - Descriptions in the single api form (A22 d).
  - The dates use the Intl typographic apostrophe «d’agost» (mockup «d'agost»), as the admin screens do.
  - Screen 23 has one extra cell, the cancelled «A+B · anul·lada · Estel» at 17:40 Muntanya, because the task asks for a cancelled class. I put it in an existing row to keep the mockup's rows.
  - The 10 fixture gives `instructorName: null` to the 20:00 «D i sup.» (the task's 24 h rule case). It looks the same as the mockup because line 2 is «en risc ⚠».
  - Drawers take the full width at 375 px (ui `Drawer`).
- **Error state**: the error is an inline danger `Toast` with [Torna-ho a provar]. The clubs app has no `ToastProvider`.
- **Block cancellation errors**: `RING_BLOCK_MANAGED_BY_ACTIVITY` is handled by code (422 per `CATALEG_ERRORS.md` rule 0; R-06-11 writes 409); it hides the button and shows `errors:RING_BLOCK_MANAGED_BY_ACTIVITY`. `INVALID_STATE` shows `errors:INVALID_STATE` and refetches the grid.
- **Instructor drawer data**: the api instructor projection of `ClassSession` has neither instructor names nor a `ring` object. The drawer shows the tapped cell's `instructorName` and the grid column's `name` for the ring.
- **Blocks**: `OCCUPIED` cells whose reason is not `TRAINING`, and `BLOCK` cells, get the dashed outline of the mockup's `bloq` style.
- **MSW world**: the mockup days are 2026-08-03/04 plus 05 (empty) and 06 (activity + «Sense»). Every other date comes from the E4-W02 calendar world.

### Questions / catalog proposals
Also posted in `roadmap/MESSAGES.md`.
1. **Proposed literals** (no mockup source; ca/es/en in the locale files):
   - Date navigation and states: `home:today.previousDay` «Dia anterior», `nextDay` «Dia següent», `weekDays` «Dies de la setmana», `grid` «Quadre del dia», `time` «Hora», `loading` «Carregant el quadre del dia», `loadError` «No s'ha pogut carregar el quadre del dia.», `retry` «Torna-ho a provar».
   - Cell texts: `home:today.activity` «Activitat · {title}» (from the R-06-12 table), `noRing` «Sense» (as in D3b).
   - Class drawer: `instructor:overview.close` «Tanca», and `instructor:overview.class.*` «Data i hora», «Pista», «Instructor», «Inscrits/places» (from the 23 footer), «Estat», «En risc», plus loading/error texts.
   - Block drawer: `instructor:overview.blockDrawer.*` with the task's proposals «Bloqueig de pista», «Motiu», «Nota», «Creat per», «Anul·la el bloqueig», plus «Anul·lant el bloqueig» and loading/error texts.
   - Enums and nav: `enums:occupiedReason.TRAINING` «entren.» / `MAINTENANCE` «manteniment» (mockup), `shell:nav.globalView` «Visió global» (mockup 23).
2. **«Sense» column**: `DayGridColumn.ringId` is nullable. Does the api send a «Sense» column itself, or only cells with `ringId: null`? The front ignores null-ring columns and adds «Sense» when a cell has no ring, so both shapes render.
3. **Class drawer names**: the instructor projection of `GET /class-sessions/{id}` has `instructorIds` but no names or ring name, so the drawer depends on the grid cell. Could the api add `instructorNames[]` and `ring {id, name, color}` to the staff projection (the member projection has `ring`)? The attendance screen 21 (E6) will need them too.
4. **Denied commands**. None blocked the task; I used an alternative each time:
   - `python3 - <<'EOF' … EOF` (heredoc to write the locale JSON): denied as "brace with quote character". I used the file tools.
   - `sed -i '' 's/…155…/…159…/' packages/api-client/src/index.test.ts && …`: I used Edit.
   - `mkdir -p roadmap/evidence/E4-W03 && pnpm turbo run lint typecheck test build --force > … 2>&1`: I created the folder with a file write and ran `pnpm --dir <repo> exec turbo run …` without `--force`.
   - `cd <repo> && pnpm turbo … > log`, `cd <repo> && git status --short` and `git -C <repo> status --short`: I ran `git status --short` from the working directory instead.

### Round 2 (2026-09-24, claude:claude-opus-5-5)
All four points of the Organizer verification are fixed. Every Verification command passed (exit 0), including the complete `pnpm e2e` of the three projects in Docker. Complete logs are `roadmap/evidence/E4-W03/06…09-r2-*.log` (index in `README.md`).

**1. Invalid dates.**
- `packages/i18n` now owns the check: `parsePlainDate(value)` builds the date with `Date.UTC` and keeps it only if the year, month and day round-trip, so `2026-13-01`, `2026-08-32`, `2026-02-30`, `hola` and `""` are rejected. `isPlainDate` wraps it.
- `useDayGrid.ts` has no `toISOString()` on unchecked input any more. It reads `?date=` with `parsePlainDate`. A present but invalid value falls back to the club-local today and rewrites the address with `history.replaceState` (`?date=2026-08-04`).
- Tests:
  - Vitest `TodayPage.test.tsx › falls back to the club-local today and rewrites the address for ?date=<value>`, for the four values. Each asserts the address, the requested date, the header and the pressed chip.
  - `format.test.ts › rejects … as a business date`.
  - `plain-dates.test.ts › rejects …`: the clubs-admin `isIsoDate` now delegates to `isPlainDate`.
- The mock `/day-grid` also answers 400 `VALIDATION_ERROR` to those four values (`day-grid-handlers.test.ts`).

**2. Business dates are calendar dates (R-06-14).**
- **The helper.** `packages/i18n/src/format.ts` adds `formatPlainDate(value, locale, presentation)` (alias `fmtPlainDate`) and `ClubFormats.formatPlainDate(value, presentation)`. It builds the date with `Date.UTC` and formats it with `timeZone: "UTC"`, so no club zone can shift it. It throws `RangeError` on an invalid date.
- **Safety net.** `formatDate`, `formatDateRange`, `formatMonth` and `formatWeekRange` format a plain `YYYY-MM-DD` string the same way, and instants still in the club zone. `formatWeekRange` loses its noon-UTC `toBusinessDate`.
- **Call sites moved to `formatPlainDate`** (the noon-UTC strings are gone):
  - **10/23:** `today/shared.tsx` (header and chips) and `useDayGrid.ts` (UTC arithmetic only).
  - **clubs:** `SelfServicePages.tsx` (`fullDate`, 28).
  - **planning, `shared.ts`:** `weekdayLabel` formats the reference week as plain dates, and `businessDate()` is deleted.
  - **planning, `calendar-shared.tsx`:** `dayLabel` takes `formatPlainDate`, and `isIsoDate` uses `isPlainDate`.
  - **planning pages:** their callers `CalendarPage`, `CalendarDayPage`, `ClassForm`, `TemplateDayPage` and `TemplatesPage`.
  - **dashboard:** `DashboardPage.tsx` (greeting and `relativeDay`, D1) and `SignupReviewPage.tsx` (next invoice date and dog birth month, D2).
  - **census:** `CensusRecordPage.tsx` (plain values through `fmtPlainDate`, D10).
- **Pure UTC arithmetic stays:** `addDays`, `weekChipDates`, `mondayOf`, `openingOf`, and the tomorrow calculation.
- **Tests:**
  - `TodayPage.test.tsx › R-06-14 in <zone>: requested date, header and chips name the club's calendar day`, for `Pacific/Auckland`, `Pacific/Kiritimati`, `Europe/Madrid` and `America/Bogota`. The system time is 2026-08-03T23:30Z. `/avui` requests the club's today: 08-04 in the first three zones, 08-03 in Bogota. The header and the pressed chip match that day, the chips stay dl 3…ds 8, and `?date=2026-08-04` shows «dt 4 d’agost» in every zone.
  - `format.test.ts › R-06-14 formats business dates as the calendar day they name in <zone>`: the same four zones, for day/month, weekday, short, weekday+date, month and week range.
  - `plain-dates.test.ts`: the admin `dayLabel`, `weekdayLabel` and `formatWeekRange` in the same four zones.
- Nothing changes for Madrid: every existing admin and clubs test, and the five screenshots, are unchanged. The Docker run re-shot the PNGs byte-identical, so `git status` shows no PNG change.

**3. «Sense» comes from the api.**
- `DayGrid` renders `columns` exactly as given: the `noRingLabel` prop and the `dayGridColumns` helper are removed. Cells with `ringId: null` land in the column whose id is `DAY_GRID_NO_RING`.
- `mapDayGrid` maps every api column, the `ringId: null` one included (label = api `shortName`, colour = api `color`), so no own column and no own label. `home:today.noRing` is deleted in ca/es/en.
- The class drawer's «Pista» row reads the api column `name` (`ringName` matches `null` to `null`), and it is omitted if no column matches.
- The MSW fixture sends `{ringId: null, shortName, name, color}` last, only when a class of the day has no ring. It is translated from `Accept-Language`: ca «Sense»/«Sense pista», es «Sin»/«Sin pista», en «None»/«No ring». The colour is `branding.theme.colors.border`.
- Tests:
  - `day-grid.test.tsx › renders the api's «Sense» column as delivered and never adds one of its own`;
  - `TodayPage.test.tsx › shows the api «Sense» column as delivered …` and `› never adds a «Sense» column of its own when the api does not send one`;
  - `fixtures.contract.test.ts › sends the «Sense» column last, translated, only when a class has no ring` (Ajv `DayGrid` for the 4 dates × 2 views still green).

**4. Mock fidelity (`day-grid-handlers.ts`).**
- **`GET /class-sessions/{id}`:**
  - MEMBER and impersonation get the member projection `ClassSessionMemberView` (`dayGridMemberSession`): only `ACTIVE`, instructor per R-06-12, `ring {id,name,color}`, `freeSeats`, and `waiting` only with `WAITLIST`. It has no `counters`, `instructorIds`, `atRisk`, `notes` or `version`.
  - A cancelled class answers 404 to them.
  - Staff keep the instructor projection.
- **`POST /ring-blocks/{id}/cancellation`:** 403 `FORBIDDEN` to MEMBER and 403 `IMPERSONATION_DENIED` to an impersonation. The block stays `ACTIVE`.
- **`GET /ring-blocks/{id}`:**
  - 403 `IMPERSONATION_DENIED` to an impersonation.
  - For MEMBER I followed the contract, not the literal text of point 4: MEMBER gets the **redacted** `RingBlockMemberView`, without `note` or `createdByName`. Three sources say so: S06 §6 row `GET /ring-blocks · /ring-blocks/{id}` «MEMBER (redactat) … 200 (MEMBER: sense `note` ni `createdByName`)», the OpenAPI response «Staff or redacted MEMBER projection» with `RingBlockMemberView`, and `MATRIU_PERMISOS.md` «R (com «Ocupada», sense qui)».
  - This closes the review's leak (member scenarios received notes and authors). See Question 1 if you want a 403 anyway.
- **`/day-grid?view=instructor`:** 403 `FORBIDDEN` to MEMBER and 403 `IMPERSONATION_DENIED` to an impersonation. Staff = ADMIN/INSTRUCTOR role without an impersonation.
- **Tenant:**
  - The world belongs to `DAY_GRID_TENANT = branding-canic.json club.slug`, the scenario's club standing in for the JWT tenant.
  - Another club (`minimal`, `minimalAdmin`) gets 404 `NOT_FOUND` on the fixture class, the block and the cancellation, and an empty day (`columns: []`, `rows: []`) on `/day-grid`.
  - Ids that are not in this world still fall through to the E4-W02 calendar handlers, which I did not touch.
- **Negative tests** in the new `packages/api-client/src/mocks/day-grid-handlers.test.ts` (11 tests): staff projection; member and impersonation projection + 404 cancelled; member redacted block + impersonation 403; member/impersonation cancellation 403 and the block stays active; `view=instructor` 403 ×2; tenant 404 ×3 + empty days; 400 for the four invalid dates. Plus `fixtures.contract.test.ts › validates the member projections of the class and the block` (Ajv `ClassSessionMemberView` / `RingBlockMemberView`).

#### Evidence (round 2)
**1. `pnpm turbo run lint typecheck test build --force`** → **exit 0**. Full log: `06-r2-turbo-lint-typecheck-test-build.log` (ANSI stripped).
- Per-package tests: course-core 342, api-client 68, ui 49, i18n 28, shared-types 21, auth 17, id 9, clubs 55, clubs-admin 95, all passed.
- `--force` means there are no cache hits.
- The only stderr is the existing `[BrandingProvider] … contrast (3.32:1)` warning.

Last 40 lines:
```
clubs-admin:test:  Test Files  15 passed (15)
clubs-admin:test:       Tests  95 passed (95)
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/TemplatesPage.test.tsx > T-06-26 D3 weekly templates > shows the coverage table with the D3 vocabulary and one-decimal places
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/TemplatesPage.test.tsx > T-06-26 D3 weekly templates > R-06-15 offers instructor chips with maxInstructorsPerClass = 2
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/TemplatesPage.test.tsx > T-06-26 D3 weekly templates > R-06-15 hides levels and coverage with levels.enabled = false and requires a manual description
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > INSTRUCTOR reads the calendar without actions and with inert chips (A22 c)
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test:  ✓ src/planning/TemplatesPage.test.tsx (13 tests) 4740ms
clubs-admin:test:    ✓ T-06-26 D3 weekly templates (13)
clubs-admin:test:      ✓ prefills band and day from an empty cell, fills the description by levels, keeps the manual text and asks for another class in the band 1568ms
clubs-admin:test:      ✓ saves every change of a full cell at once with the template version 733ms
clubs-admin:test:      ✓ queues two quick changes of a full cell and sends each PATCH with the latest version 419ms
clubs-admin:test:      ✓ reloads the template and remounts the card on STALE_VERSION 421ms
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > shows the week warnings and selects the class of a warning; day headers open the day view
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test:  ✓ src/planning/CalendarPage.test.tsx (19 tests) 4799ms
clubs-admin:test:    ✓ T-06-28 D4 / D4b / D4c class calendar (front half, MSW) (19)
clubs-admin:test:      ✓ opens «Esborrany» on the first week with drafts, with the validation card and its count 1057ms
clubs-admin:test:      ✓ [VALIDAR LA SETMANA] turns every draft of the week active at once 461ms
clubs-admin:test:      ✓ cancels an active class with registrants through D4c: four rows, text required, «ANUL·LA I AVISA ELS 4 ALUMNES» 388ms
clubs-admin:test:      ✓ [ELIMINA] of an active class goes through the same D4c modal 340ms
clubs-admin:test:      ✓ R-06-09 saves the diff with the version; STALE_VERSION keeps its message after the refetch remounts the card 485ms
clubs-admin:test: 
clubs-admin:test:  Test Files  15 passed (15)
clubs-admin:test:       Tests  95 passed (95)
clubs-admin:test:    Start at  15:43:20
clubs-admin:test:    Duration  9.54s (environment 39%, tests 28%, transform 19%, setup 8%, import 6%)
clubs-admin:test: 

 Tasks:    34 successful, 34 total
Cached:    0 cached, 34 total
  Time:    25.751s
```

**2. `pnpm i18n:check`** → **exit 0**. Complete output (`07-r2-i18n-check.log`):
```
> node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs

i18n check passed
ca: 2018 keys
es: 2018 keys
en: 2018 keys
1460 statically used keys, 0 unused
250 codes, 0 missing
```
Note: this round removed one key per locale (`home:today.noRing`) and added none. Round 1 counted 2014 keys; the net +4 comes from locale changes committed after round 1 by other tasks (commit `1ca2918`, E4-W02 round 2 among others).

**3. Vitest: T-06-29, T-06-30, `day-grid.test.tsx`, the Ajv suite and the new date and mock tests.** Five commands, all **exit 0**.

(a) `pnpm --filter clubs exec vitest run --reporter=verbose src/today src/App.test.tsx`: 43 passed. Log: `08a-r2-vitest-clubs.log`. Last 40 lines:
```
stderr | src/App.test.tsx > T-01-26 imported-account onboarding and policy re-consent > renders the screen and pop-up in Catalan, Spanish and English
[BrandingProvider] Club Mínim: primary and onPrimary contrast (3.32:1) is below WCAG AA.

stderr | src/today/TodayPage.test.tsx > T-06-29 screen 10 «Classes del dia» > R-06-14 in America/Bogota: requested date, header and chips name the club's calendar day
[BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.

stderr | src/App.test.tsx > T-01-26 imported-account onboarding and policy re-consent > renders the screen and pop-up in Catalan, Spanish and English
[BrandingProvider] Club Mínim: primary and onPrimary contrast (3.32:1) is below WCAG AA.

stderr | src/App.test.tsx > T-01-26 imported-account onboarding and policy re-consent > renders the screen and pop-up in Catalan, Spanish and English
[BrandingProvider] Club Mínim: primary and onPrimary contrast (3.32:1) is below WCAG AA.

 ✓ src/today/TodayPage.test.tsx > T-06-29 screen 10 «Classes del dia» > R-06-14 in Europe/Madrid: requested date, header and chips name the club's calendar day 51ms
 ✓ src/today/TodayPage.test.tsx > T-06-29 screen 10 «Classes del dia» > R-06-14 in America/Bogota: requested date, header and chips name the club's calendar day 57ms
stderr | src/today/TodayPage.test.tsx > T-06-29 screen 10 «Classes del dia» > shows the empty state (dayGridEmpty) and the error toast with retry
[BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.

stderr | src/App.test.tsx > T-01-26 imported-account onboarding and policy re-consent > renders the screen and pop-up in Catalan, Spanish and English
[BrandingProvider] Club Mínim: primary and onPrimary contrast (3.32:1) is below WCAG AA.

stderr | src/today/TodayPage.test.tsx > T-06-29 screen 10 «Classes del dia» > shows the empty state (dayGridEmpty) and the error toast with retry
[BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.

stderr | src/App.test.tsx > T-01-26 imported-account onboarding and policy re-consent > renders the screen and pop-up in Catalan, Spanish and English
[BrandingProvider] Club Mínim: primary and onPrimary contrast (3.32:1) is below WCAG AA.

 ✓ src/App.test.tsx > T-01-26 imported-account onboarding and policy re-consent > reloads an outdated policy version before accepting it 56ms
 ✓ src/today/TodayPage.test.tsx > T-06-29 screen 10 «Classes del dia» > shows the empty state (dayGridEmpty) and the error toast with retry 58ms
stderr | src/App.test.tsx > T-03-40 mobile own dogs > renders own dogs, saves the note, shows task totals and gates TASKS
[BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.

stderr | src/App.test.tsx > T-03-40 mobile own dogs > renders own dogs, saves the note, shows task totals and gates TASKS
[BrandingProvider] Club Mínim: primary and onPrimary contrast (3.32:1) is below WCAG AA.

stderr | src/App.test.tsx > T-03-41 mobile own data > keeps identity read-only, resolves towns, maps READ_ONLY and gates billing
[BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.

 ✓ src/App.test.tsx > T-01-26 imported-account onboarding and policy re-consent > renders the screen and pop-up in Catalan, Spanish and English 112ms
 ✓ src/App.test.tsx > T-03-40 mobile own dogs > renders own dogs, saves the note, shows task totals and gates TASKS 80ms
stderr | src/App.test.tsx > T-03-41 mobile own data > keeps identity read-only, resolves towns, maps READ_ONLY and gates billing
[BrandingProvider] Club Mínim: primary and onPrimary contrast (3.32:1) is below WCAG AA.

 ✓ src/App.test.tsx > T-03-41 mobile own data > keeps identity read-only, resolves towns, maps READ_ONLY and gates billing 76ms
 ✓ src/App.test.tsx > T-03-41 mobile own data > validates DNI, NIE, phone and postal code from the country profile 1ms

 Test Files  4 passed (4)
      Tests  43 passed (43)
   Start at  15:43:49
   Duration  2.28s (tests 39%, environment 27%, transform 19%, import 9%, setup 7%)
```
(the T-06-29 / T-06-30 test names of rounds 1 and 2, all ✓, are in the log, lines 4–187.)

(b) `pnpm --filter @agilityhub/ui exec vitest run --reporter=verbose src/day-grid.test.tsx src/schedule-grid.test.tsx`: 9 passed. Complete output (`08b-r2-vitest-ui.log`):
```
 RUN  v5.0.0 /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/ui

 ✓ src/day-grid.test.tsx > DayGrid presenter (screens 10 / 23) > keeps identical ring columns and strips the leading zero of each start time 87ms
 ✓ src/schedule-grid.test.tsx > ScheduleGrid > stacks cells per slot, exposes day buttons and tints cells through --schedule-color 137ms
 ✓ src/schedule-grid.test.tsx > ScheduleGrid > renders the D4 variants: counts, draft outline, cancelled and link cells 14ms
 ✓ src/day-grid.test.tsx > DayGrid presenter (screens 10 / 23) > stacks two cells of the same ring and row and draws empty placeholders 23ms
 ✓ src/day-grid.test.tsx > DayGrid presenter (screens 10 / 23) > renders the api's «Sense» column as delivered and never adds one of its own 18ms
 ✓ src/day-grid.test.tsx > DayGrid presenter (screens 10 / 23) > never renders occupancy in the member view even when the model carries it 10ms
 ✓ src/day-grid.test.tsx > DayGrid presenter (screens 10 / 23) > shows «n/n +e · instructor» in the instructor view, «+e» only with WAITLIST 11ms
 ✓ src/day-grid.test.tsx > DayGrid presenter (screens 10 / 23) > marks risk, dims cancelled classes and renders occupied, block and activity cells 26ms
 ✓ src/day-grid.test.tsx > DayGrid presenter (screens 10 / 23) > hides cancelled classes from the member view 2ms

 Test Files  2 passed (2)
      Tests  9 passed (9)
   Start at  15:43:51
   Duration  861ms (environment 57%, tests 22%, setup 13%, transform 6%, import 1%)
```

(c) `pnpm --filter @agilityhub/i18n exec vitest run --reporter=verbose src/format.test.ts`: 22 passed. Complete output (`08c-r2-vitest-i18n.log`, 31 lines):
```
 RUN  v5.0.0 /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/i18n

 ✓ src/format.test.ts > club-aware formats > formats money in ca 12ms
 ✓ src/format.test.ts > club-aware formats > formats money in es 0ms
 ✓ src/format.test.ts > club-aware formats > formats money in en 0ms
 ✓ src/format.test.ts > club-aware formats > formats durations in ca 0ms
 ✓ src/format.test.ts > club-aware formats > formats durations in es 0ms
 ✓ src/format.test.ts > club-aware formats > formats durations in en 0ms
 ✓ src/format.test.ts > club-aware formats > uses the club time zone for the same UTC instant 4ms
 ✓ src/format.test.ts > club-aware formats > formats ISO week ranges in ca (E4-W01 D3 generation card) 2ms
 ✓ src/format.test.ts > club-aware formats > formats ISO week ranges in es (E4-W01 D3 generation card) 2ms
 ✓ src/format.test.ts > club-aware formats > formats ISO week ranges in en (E4-W01 D3 generation card) 1ms
 ✓ src/format.test.ts > club-aware formats > formats numeric day and month and long weekday names 0ms
 ✓ src/format.test.ts > club-aware formats > formats date ranges and binds branding values 1ms
 ✓ src/format.test.ts > club-aware formats > R-06-14 formats business dates as the calendar day they name in Pacific/Auckland 1ms
 ✓ src/format.test.ts > club-aware formats > R-06-14 formats business dates as the calendar day they name in Pacific/Kiritimati 1ms
 ✓ src/format.test.ts > club-aware formats > R-06-14 formats business dates as the calendar day they name in Europe/Madrid 1ms
 ✓ src/format.test.ts > club-aware formats > R-06-14 formats business dates as the calendar day they name in America/Bogota 1ms
 ✓ src/format.test.ts > club-aware formats > rejects "2026-13-01" as a business date 0ms
 ✓ src/format.test.ts > club-aware formats > rejects "2026-08-32" as a business date 0ms
 ✓ src/format.test.ts > club-aware formats > rejects "2026-02-30" as a business date 0ms
 ✓ src/format.test.ts > club-aware formats > rejects "hola" as a business date 0ms
 ✓ src/format.test.ts > club-aware formats > rejects "" as a business date 0ms
 ✓ src/format.test.ts > club-aware formats > accepts real business dates, leap days included 0ms

 Test Files  1 passed (1)
      Tests  22 passed (22)
   Start at  15:43:53
   Duration  282ms (transform 62%, import 22%, tests 15%, worker 1%)
```

(d) Ajv suite + mocks: `pnpm --filter @agilityhub/api-client exec vitest run --reporter=verbose src/mocks/fixtures.contract.test.ts src/mocks/day-grid-handlers.test.ts src/index.test.ts src/mocks/calendar-handlers.test.ts`: 67 passed. Log: `08d-r2-vitest-api-client.log`. Last 40 lines:
```
 ✓ src/mocks/day-grid-handlers.test.ts > E4-W03 day-grid MSW handlers: roles, impersonation and tenant (S06 §6, MATRIU_PERMISOS) > answers 400 VALIDATION_ERROR to date=2026-08-32 19ms
 ✓ src/mocks/day-grid-handlers.test.ts > E4-W03 day-grid MSW handlers: roles, impersonation and tenant (S06 §6, MATRIU_PERMISOS) > answers 400 VALIDATION_ERROR to date=2026-02-30 18ms
 ✓ src/mocks/day-grid-handlers.test.ts > E4-W03 day-grid MSW handlers: roles, impersonation and tenant (S06 §6, MATRIU_PERMISOS) > answers 400 VALIDATION_ERROR to date=hola 19ms
 ✓ src/mocks/calendar-handlers.test.ts > E4-W02 calendar MSW handlers follow the S06 contract (forms B and D) > R-06-05 answers RING_HAS_BOOKINGS when a class or a block moves onto a training booking 36ms
 ✓ src/mocks/calendar-handlers.test.ts > E4-W02 calendar MSW handlers follow the S06 contract (forms B and D) > serves the instructor day grid (form D) of the Wednesday 24ms
 ✓ src/mocks/fixtures.contract.test.ts > T-01-25 OpenAPI mock fixture contract > validates me-member.json 18ms
 ✓ src/mocks/fixtures.contract.test.ts > T-01-25 OpenAPI mock fixture contract > validates me-multi-profile.json 19ms
 ✓ src/mocks/fixtures.contract.test.ts > T-01-25 OpenAPI mock fixture contract > validates member-signup-review.json 27ms
 ✓ src/mocks/fixtures.contract.test.ts > T-01-25 OpenAPI mock fixture contract > validates sessions.json 15ms
 ✓ src/mocks/fixtures.contract.test.ts > T-01-25 OpenAPI mock fixture contract > validates signup-config-canic.json 20ms
 ✓ src/mocks/fixtures.contract.test.ts > E4-W01 planning fixtures follow the S06 contract (WeekTemplate, Coverage, WeekListItem) > validates the Setmana A template 4ms
 ✓ src/index.test.ts > R-03-22 census list handlers > serves 242 dogs and rejects unknown filter fields 250ms
 ✓ src/mocks/fixtures.contract.test.ts > E4-W01 planning fixtures follow the S06 contract (WeekTemplate, Coverage, WeekListItem) > validates the Setmana B template 3ms
 ✓ src/mocks/fixtures.contract.test.ts > E4-W01 planning fixtures follow the S06 contract (WeekTemplate, Coverage, WeekListItem) > validates the Dissabtes template 2ms
 ✓ src/mocks/fixtures.contract.test.ts > E4-W01 planning fixtures follow the S06 contract (WeekTemplate, Coverage, WeekListItem) > validates the E29 level catalog (Level.progression overlay of pending.json) 0ms
 ✓ src/mocks/fixtures.contract.test.ts > E4-W01 planning fixtures follow the S06 contract (WeekTemplate, Coverage, WeekListItem) > keeps every fixture description equal to the mock resolver, so an edit never flips «D i sup.» 1ms
 ✓ src/mocks/fixtures.contract.test.ts > E4-W01 planning fixtures follow the S06 contract (WeekTemplate, Coverage, WeekListItem) > validates the coverage table and the week list items 3ms
 ✓ src/mocks/fixtures.contract.test.ts > E4-W02 calendar fixtures follow the S06 contract (ClassSession, RingBlock, CancellationPreview) > validates every class session of the D4, D4b and inconsistent weeks 4ms
 ✓ src/mocks/fixtures.contract.test.ts > E4-W02 calendar fixtures follow the S06 contract (ClassSession, RingBlock, CancellationPreview) > keeps every calendar fixture description equal to the mock resolver (E29: {F,G} = «F i sup.») 1ms
 ✓ src/mocks/fixtures.contract.test.ts > E4-W02 calendar fixtures follow the S06 contract (ClassSession, RingBlock, CancellationPreview) > validates the ring block and the cancellation preview of the Wednesday 18:50 class 4ms
 ✓ src/mocks/fixtures.contract.test.ts > E4-W02 calendar fixtures follow the S06 contract (ClassSession, RingBlock, CancellationPreview) > R-06-14 derives UTC instants from the club-local time across the DST change 0ms
 ✓ src/mocks/fixtures.contract.test.ts > E4-W03 day-grid fixtures follow the S06 contract (DayGrid form D, ClassSession, RingBlock) > validates the 2026-08-03 member grid 3ms
 ✓ src/mocks/fixtures.contract.test.ts > E4-W03 day-grid fixtures follow the S06 contract (DayGrid form D, ClassSession, RingBlock) > validates the 2026-08-03 instructor grid 1ms
 ✓ src/mocks/fixtures.contract.test.ts > E4-W03 day-grid fixtures follow the S06 contract (DayGrid form D, ClassSession, RingBlock) > validates the 2026-08-04 member grid 1ms
 ✓ src/mocks/fixtures.contract.test.ts > E4-W03 day-grid fixtures follow the S06 contract (DayGrid form D, ClassSession, RingBlock) > validates the 2026-08-04 instructor grid 1ms
 ✓ src/mocks/fixtures.contract.test.ts > E4-W03 day-grid fixtures follow the S06 contract (DayGrid form D, ClassSession, RingBlock) > validates the 2026-08-05 member grid 1ms
 ✓ src/mocks/fixtures.contract.test.ts > E4-W03 day-grid fixtures follow the S06 contract (DayGrid form D, ClassSession, RingBlock) > validates the 2026-08-05 instructor grid 1ms
 ✓ src/mocks/fixtures.contract.test.ts > E4-W03 day-grid fixtures follow the S06 contract (DayGrid form D, ClassSession, RingBlock) > validates the 2026-08-06 member grid 1ms
 ✓ src/mocks/fixtures.contract.test.ts > E4-W03 day-grid fixtures follow the S06 contract (DayGrid form D, ClassSession, RingBlock) > validates the 2026-08-06 instructor grid 1ms
 ✓ src/mocks/fixtures.contract.test.ts > E4-W03 day-grid fixtures follow the S06 contract (DayGrid form D, ClassSession, RingBlock) > projects the member view as the api does (R-06-12): no counts, names, notes nor staff kinds 2ms
 ✓ src/mocks/fixtures.contract.test.ts > E4-W03 day-grid fixtures follow the S06 contract (DayGrid form D, ClassSession, RingBlock) > carries the screen 23 counts, trainings, block and cancelled class in the instructor view 0ms
 ✓ src/mocks/fixtures.contract.test.ts > E4-W03 day-grid fixtures follow the S06 contract (DayGrid form D, ClassSession, RingBlock) > validates the class sessions and ring blocks behind the screen 23 drawers 7ms
 ✓ src/mocks/fixtures.contract.test.ts > E4-W03 day-grid fixtures follow the S06 contract (DayGrid form D, ClassSession, RingBlock) > sends the «Sense» column last, translated, only when a class has no ring 0ms
 ✓ src/mocks/fixtures.contract.test.ts > E4-W03 day-grid fixtures follow the S06 contract (DayGrid form D, ClassSession, RingBlock) > validates the member projections of the class and the block (S06 §6 «Altres formes») 12ms

 Test Files  4 passed (4)
      Tests  67 passed (67)
   Start at  15:43:54
   Duration  1.06s (tests 42%, import 31%, transform 26%)
```
The eight earlier `day-grid-handlers.test.ts` ✓ lines are above these 40 in the log: staff projection; member and impersonated projection; redacted block + impersonation 403; cancellation 403; `view=instructor` 403; tenant 404 + empty days; 400 for `2026-13-01`.

(e) `pnpm --filter clubs-admin exec vitest run --reporter=verbose src/planning/plain-dates.test.ts`: 9 passed. Complete output (`08e-r2-vitest-clubs-admin.log`):
```
 RUN  v5.0.0 /Users/jordib/dev/agilityhub/agilityhub-core-web/apps/clubs-admin

 ✓ src/planning/plain-dates.test.ts > R-06-14 planning business dates are calendar dates in every club zone > labels days and weekdays without shifting them in Pacific/Auckland 11ms
 ✓ src/planning/plain-dates.test.ts > R-06-14 planning business dates are calendar dates in every club zone > labels days and weekdays without shifting them in Pacific/Kiritimati 1ms
 ✓ src/planning/plain-dates.test.ts > R-06-14 planning business dates are calendar dates in every club zone > labels days and weekdays without shifting them in Europe/Madrid 0ms
 ✓ src/planning/plain-dates.test.ts > R-06-14 planning business dates are calendar dates in every club zone > labels days and weekdays without shifting them in America/Bogota 0ms
 ✓ src/planning/plain-dates.test.ts > R-06-14 planning business dates are calendar dates in every club zone > rejects 2026-13-01 0ms
 ✓ src/planning/plain-dates.test.ts > R-06-14 planning business dates are calendar dates in every club zone > rejects 2026-08-32 0ms
 ✓ src/planning/plain-dates.test.ts > R-06-14 planning business dates are calendar dates in every club zone > rejects 2026-02-30 0ms
 ✓ src/planning/plain-dates.test.ts > R-06-14 planning business dates are calendar dates in every club zone > rejects hola 0ms
 ✓ src/planning/plain-dates.test.ts > R-06-14 planning business dates are calendar dates in every club zone > parses masked dates only when they are real 1ms

 Test Files  1 passed (1)
      Tests  9 passed (9)
   Start at  15:43:55
   Duration  744ms (environment 53%, transform 25%, setup 12%, import 7%, tests 2%)
```

**4. Complete `pnpm e2e` in Docker: `pnpm e2e:docker`** (`scripts/e2e-docker.sh`, `mcr.microsoft.com/playwright:v1.63.0-noble`) → **exit 0**. Full log: `09-r2-e2e-docker.log`. Last 40 lines:
```
id:e2e: 
id:e2e: > id@0.0.0 e2e /work/apps/id
id:e2e: > playwright test
id:e2e: 
id:e2e: 
id:e2e: Running 1 test using 1 worker
id:e2e: ·
id:e2e:   1 passed (3.1s)
clubs-admin:e2e: cache bypass, force executing 5671efced6695e58
clubs-admin:e2e: 
clubs-admin:e2e: > clubs-admin@0.0.0 e2e /work/apps/clubs-admin
clubs-admin:e2e: > playwright test
clubs-admin:e2e: 
clubs-admin:e2e: [WebServer] (node:891) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs-admin:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs-admin:e2e: 
clubs-admin:e2e: Running 29 tests using 9 workers
clubs-admin:e2e: ·····························
clubs-admin:e2e:   29 passed (30.5s)
clubs:e2e: cache bypass, force executing 89f9fc0635f9c4a5
clubs:e2e: 
clubs:e2e: > clubs@0.0.0 e2e /work/apps/clubs
clubs:e2e: > playwright test
clubs:e2e: 
clubs:e2e: [WebServer] (node:2682) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs:e2e: 
clubs:e2e: Running 25 tests using 6 workers
clubs:e2e: ·························
clubs:e2e:   25 passed (14.8s)

 Tasks:    8 successful, 8 total
Cached:    0 cached, 8 total
  Time:    59.628s 

evidence: E4-W03/10-avui-375.png
evidence: E4-W03/10-avui-buit-375.png
evidence: E4-W03/10-avui-impersonat-375.png
evidence: E4-W03/23-visio-global-classe-375.png
evidence: E4-W03/23-visio-global-375.png
```
**5. Screenshots.** The run re-shot the five 375 px PNGs, and they are byte-identical to round 1 (no PNG in `git status`). I checked `10-avui-375.png` and `23-visio-global-classe-375.png` again: the «Pista» row now shows the api column `name`, «Central».

#### Files changed (round 2)
- **i18n:**
  - `packages/i18n/src/format.ts`: `parsePlainDate`, `isPlainDate`, `formatPlainDate`/`fmtPlainDate`, `ClubFormats.formatPlainDate`, plain dates formatted in UTC.
  - `format.test.ts`.
  - `locales/{ca,es,en}/home.json`: `today.noRing` removed.
- **ui:** `packages/ui/src/day-grid.tsx` (columns as delivered, `noRingLabel`/`dayGridColumns` removed), `day-grid.test.tsx`.
- **clubs:** `apps/clubs/src/today/{useDayGrid.ts, shared.tsx, TodayPage.tsx, OverviewPage.tsx, TodayPage.test.tsx}`, `apps/clubs/src/SelfServicePages.tsx`.
- **clubs-admin:**
  - `apps/clubs-admin/src/planning/{shared.ts, calendar-shared.tsx, CalendarPage.tsx, CalendarDayPage.tsx, ClassForm.tsx, TemplateDayPage.tsx, TemplatesPage.tsx}`;
  - `plain-dates.test.ts` (new);
  - `dashboard/{DashboardPage.tsx, SignupReviewPage.tsx}`, `census/CensusRecordPage.tsx`.
- **api-client:** `packages/api-client/src/mocks/{day-grid-handlers.ts, fixtures/day-grid.ts, fixtures.contract.test.ts}`, `day-grid-handlers.test.ts` (new).
- **Other:** `CHANGELOG.md`, `roadmap/evidence/E4-W03/{06…09-r2-*.log, README.md}`, `roadmap/MESSAGES.md`, this file.

#### Rules and tests (round 2)
- **R-06-14:**
  - the club-local today;
  - calendar dates formatted in UTC in every zone;
  - an invalid `?date=` falls back and is rewritten.
- **R-06-12:** «Sense» only as the api delivers it; the member projections of the class and the block.
- **MATRIU_PERMISOS / S06 §6:** the impersonation and member 403s, and the tenant 404 in the mocks.
- **Tests:** T-06-29 (the new TodayPage cases), `day-grid.test.tsx`, `format.test.ts`, `plain-dates.test.ts`, `day-grid-handlers.test.ts` and the Ajv block.

#### Assumptions (round 2)
- **`GET /ring-blocks/{id}` for MEMBER returns the redacted projection, not 403.** This follows S06 §6, the OpenAPI and `MATRIU_PERMISOS` (see point 4 and Question 1). Impersonation gets 403, as S06 §6 says: «Tokens d'impersonació: només `GET /day-grid?view=member` i `GET /class-sessions/{id}`».
- **The mock «Sense» column colour is `theme.colors.border`.** The contract requires `color`, but S06 does not say which colour the api sends for `ringId: null`. `DayGrid` renders whatever colour arrives.
- **The mock «Sense» labels are my own choice.** They are ca «Sense»/«Sense pista», es «Sin»/«Sin pista», en «None»/«No ring», the same wording as the existing `admin-scheduling` keys. The real text is the api's `scheduling.noRing`.
- **Tenant scoping covers only the day-grid world.** The E4-W02 calendar handlers, which serve other ids and dates, still ignore role and tenant. They are other tasks' code.

#### Questions / catalog proposals (round 2)
1. **`GET /ring-blocks/{id}` for MEMBER.** Round 2 point 4 says 403 `FORBIDDEN`, but S06 §6, the OpenAPI («Staff or redacted MEMBER projection») and `MATRIU_PERMISOS` («R … sense qui») say MEMBER gets the redacted `RingBlockMemberView`. I implemented the redacted projection. If you want the 403, it is a one-line change in `day-grid-handlers.ts` plus one test.
2. **Follow-up for E4-W02 (not changed here).** Now that `/day-grid` sends its own «Sense» column, `CalendarDayPage` (D4 day view) derives «Sense» itself (`NO_RING`), so it could show two «Sense» columns against the real api. Its mock world does not send the column, so nothing shows in the tests today.
3. **Denied commands (round 2).** None blocked the task; each time I used an alternative:
   - `cd packages/ui/src && sed -i '' … day-grid.test.tsx`: I used Edit.
   - A `{ …; } > log` group of the five Vitest runs: I ran them one by one.
   - `sed -i '' -E 's/\x1b…//g' roadmap/evidence/E4-W03/08*.log; grep …` and `grep -c $'\x1b' …`: not needed, because the non-TTY Vitest logs have no ANSI.

## Organizer verification
Result: changes_requested · 2026-09-24 · organizer (Claude)

Screens 10 and 23 match the mockups. The organizer compared `10-avui-375.png` and `23-visio-global-375.png` with `docs/pantalles/mobil/10-*.png` and `23-*.png`:
- identical ring columns, the same rows and cells, «Ocupada» in grey without background, and the balloon with the api text;
- on 23: «n/n +e · instructor», the trainings, the block and the cancelled class.

The deviations are accepted: A22 d and e, the extra cancelled cell, and the instructor tab bar until E6. `DayGrid` reuses `ScheduleCell` in a phone-width CSS grid. Turbo 34/34, i18n 2014 × 3, Vitest 34 + 9 + 52, e2e 29 + 1 + 25.

The independent review `roadmap/reviews/E4-W03-20260924-1506-codex.md` (Codex) found two real defects. The organizer confirmed both in the code:
- **#1: an invalid `?date=` crashes the page.** `2026-13-01` and `2026-08-32` pass the regex, and `dateAtNoon(…).toISOString()` throws `RangeError`. The address is user input: a shared link is enough.
- **#2: dates shift one day in clubs at UTC+12 or more.** A business date formatted as `${date}T12:00:00Z` in the club's time zone shows the next day in `Pacific/Auckland` or `Pacific/Kiritimati`. The same pattern is used in D1, D2, D3/D4, D10 and 28, not only here. No Cànic user is affected (`Europe/Madrid`), but R-06-14 is a platform rule.

Round 2 — fix exactly these points:
1. **Invalid dates.**
   - `isPlainDate` checks that the parsed time is a real date (`!Number.isNaN(…getTime())` and a round trip) before `toISOString()`.
   - An invalid `?date=` falls back to the club-local today and rewrites the address.
   - Vitest: `2026-13-01`, `2026-08-32`, `2026-02-30` and `hola`.
2. **Business dates are calendar dates (R-06-14).**
   - Add one helper in `packages/i18n` (e.g. `formatPlainDate(value, style)`): it builds the `YYYY-MM-DD` value with `Date.UTC` and formats it with `timeZone: "UTC"`, so no club zone can shift it.
   - Use it for every formatted `YYYY-MM-DD` value. Today's call sites are 10/23 (`today/shared.tsx`, `useDayGrid.ts`), `SelfServicePages.tsx:124`, `planning/shared.ts`, `planning/calendar-shared.tsx`, `dashboard/DashboardPage.tsx`, `dashboard/SignupReviewPage.tsx` and `census/CensusRecordPage.tsx`.
   - Pure UTC arithmetic (`getUTCDay`, `setUTCDate`) can stay.
   - Tests: the header, the chips and the requested date in `Pacific/Auckland`, `Pacific/Kiritimati`, `Europe/Madrid` and `America/Bogota`. Nothing changes for Madrid.
3. **The «Sense» column comes from the api** (your Question 2). `DayGridQuery` sends it: `{ringId: null, shortName, name}` = `scheduling.noRing` in the reader's language, last, only when a class has no ring.
   - Render the api columns as delivered, without adding your own column or your own label.
   - The MSW fixture sends the column the same way.
4. **Mock fidelity (review #3).**
   - `GET /class-sessions/{id}` returns the member projection to MEMBER.
   - `GET /ring-blocks/{id}` and its cancellation answer `403 FORBIDDEN` to MEMBER and `403 IMPERSONATION_DENIED` to an impersonation.
   - The fixtures are scoped by tenant.
   - Add negative tests.

**Rulings on the rest:**
- **Review #4** (evidence format): the log references are accepted, as for E4-W01.
- **Q1:** the proposed literals are accepted.
- **Q3:** accepted. The api adds `instructorNames[]` and `ring {id, name, color}` to the staff projection of `GET /class-sessions/{id}` (**api E5-T15** step 10). Until then the drawer keeps reading the tapped cell.
- **Routes:** S06 §2 now writes `/instructor/avui` for screen 23.
- **Tab bar:** your choice (INSTRUCTOR or ADMIN profile) is accepted, and E6 replaces it.

### Round 2 verification
Result: verified · 2026-09-24 · organizer (Claude)

The four points of round 2 are fixed, with tests:
- invalid dates fall back to the club's today and rewrite the address;
- `formatPlainDate` works in UTC at every call site, including the Kiritimati and Auckland cases;
- the «Sense» column comes from the API;
- the mocks check the role, the impersonation and the club.

The evidence matches the report: turbo `--force` 34/34 with 0 cached, and the e2e 29 + 1 + 25. CI is green at `3adad1d`.

**Your question 1: the organizer was wrong.** S06 §6 gives MEMBER a redacted `200` on `/ring-blocks/{id}` (without `note` or `createdByName`), and `MATRIU_PERMISOS` agrees. Your redacted projection stands, and round 1's point 4 on that route is withdrawn.

**Independent review** `roadmap/reviews/E4-W03-20260924-1550-claude.md`:
- **#1** is answered above.
- **#7** (and your question 2): the D4 day view derives its own «Sense» → **E4-W02 round 3, point 5**.
- **#2–#6** are small and real:
  - a cell whose ring matches no column disappears silently;
  - `formatDateRange` with a plain date at one end and a timestamp at the other;
  - `replaceState` during render;
  - the mock accepts any `view`;
  - `parseMaskedDate` checks the date twice.
  
  They go to **E4-W05 step 6**, the polish before gate E4.
