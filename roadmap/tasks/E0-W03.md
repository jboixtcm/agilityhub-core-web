---
id: E0-W03
title: packages/i18n: ICU, namespaces, club-aware formats, parity/vocabulary/unused-key checks
stage: E0
repo: agilityhub-core-web
thread: B
order: 30
status: verified
depends_on: [E0-W01]
branch: 
pr: 
updated: 2026-09-06
---

# E0-W03 · packages/i18n: ICU, namespaces, club-aware formats, parity/vocabulary/unused-key checks

## Objective
The i18n foundation: three locales from day one, ICU plural/gender, formats in the club's time zone and currency, and CI checks that keep translations complete and the vocabulary clean.

## Spec references
`docs/specs/00-transversal/CONVENCIONS_I18N.md` (namespaces, key style, formats, forbidden vocabulary) · `docs/decisions/ADR-011` · S02 T-02-17 (mechanism) · `docs/specs/00-transversal/CATALEG_ERRORS.md` (every code needs a message).

## Context to load
CONVENCIONS_I18N · ADR-011 · CATALEG_ERRORS · this file.

## Steps
1. `i18next` + `react-i18next` + `i18next-icu`; locales `ca`, `es`, `en`; namespaces `common`, `auth`, `shell`, `errors` (others per stage); lazy loading per namespace; language resolution: user (`me.account.locale`) → `branding.locales` ∩ browser languages → `branding.defaultLocale`.
2. `format.ts`: `formatDate`, `formatTime`, `formatDateRange`, `formatDuration` («2 h», «30 min», «4 h abans» / «4 h before»), `formatMoney` («45,00 €» / «€45.00») using `Intl` with the club `timeZone` and `currency` (`useClubFormats()` hook reading `useBranding()`); unit tests for `ca`, `es`, `en`.
3. `errors` namespace: a message for **every** error code in `CATALEG_ERRORS.md` in the three locales; script `scripts/check-error-codes.mjs` comparing the document's codes with the keys.
4. `pnpm i18n:extract` (i18next-parser config) and `pnpm i18n:check` (`scripts/i18n-check.mjs`): (a) key parity ca/es/en, (b) no key used in code without a translation, (c) no empty values, (d) **forbidden vocabulary** in translation files and in `.tsx` string literals: `parell`, `parella`, `amigable`, `(paràmetre)`, `\bF\d+\b`, `\bRF-`, `\bBR-`; exit code 1 on any failure with a readable list.

## Verification (run all; paste full output in the report)
- `pnpm --filter @agilityhub/i18n test && pnpm i18n:check` → green.
- Add a key only to `ca` → `pnpm i18n:check` fails naming the key; add the word «parella» to a translation → fails naming the file; revert both (paste outputs).
- `node scripts/check-error-codes.mjs` → «N codes, 0 missing».

## Definition of Done
Three locales complete for `common/auth/shell/errors` · checks proven · formats tested.

## Evidence to return
Outputs above; key counts per locale.

---

## Executor report

### Branch / commits
Working tree only. No branch, commit, or other git-writing command was run, per the sandbox protocol.

### Evidence (full command outputs)
`pnpm --filter @agilityhub/i18n test && pnpm i18n:check` (with `COREPACK_HOME` exported to a task-specific writable temporary directory because the default Corepack cache is outside the sandbox):

```text
> @agilityhub/i18n@0.0.0 test /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/i18n
> vitest run


 RUN  v5.0.0 /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/i18n


 Test Files  3 passed (3)
      Tests  14 passed (14)
   Start at  09:17:07
   Duration  162ms (transform 54%, import 26%, tests 18%, worker 2%)


> agilityhub-core-web@0.0.0 i18n:check /Users/jordib/dev/agilityhub/agilityhub-core-web
> node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs

i18n check passed
ca: 303 keys
es: 303 keys
en: 303 keys
60 statically used keys, 0 unused
244 codes, 0 missing
```

Temporary key added only to `ca`, expected failure from `pnpm i18n:check`, then reverted:

```text
> agilityhub-core-web@0.0.0 i18n:check /Users/jordib/dev/agilityhub/agilityhub-core-web
> node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs

i18n check failed with 3 issue(s):
- en: missing translation key auth:verificationOnly
- es: missing translation key auth:verificationOnly
- unused translation key auth:verificationOnly
 ELIFECYCLE  Command failed with exit code 1.
```

Temporary `parella` translation, expected failure from `pnpm i18n:check`, then reverted:

```text
> agilityhub-core-web@0.0.0 i18n:check /Users/jordib/dev/agilityhub/agilityhub-core-web
> node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs

i18n check failed with 1 issue(s):
- packages/i18n/src/locales/ca/common.json: forbidden parell/parella/parelles in common:gallery.active
 ELIFECYCLE  Command failed with exit code 1.
```

`node scripts/check-error-codes.mjs`:

```text
244 codes, 0 missing
```

### Files changed
- `packages/i18n/package.json`, `packages/i18n/src/{catalog,format,index,locale,resources,setup,types}.ts`, and unit tests.
- `packages/i18n/src/locales/{ca,es,en}/{auth,common,errors,shell}.json`.
- `scripts/i18n-check.mjs`, `scripts/check-error-codes.mjs`, and `i18next-parser.config.mjs`.
- Root `package.json`, `pnpm-workspace.yaml`, and `pnpm-lock.yaml` for commands and parser dependency.
- Existing app/gallery source and tests under `apps/{clubs,clubs-admin,id}` migrated to namespaced keys.
- `CHANGELOG.md`, this executor report, and generated `roadmap/STATUS.md`.

### Rules and tests implemented (R-xx-nn / T-xx-nn)
- ADR-011: `ca`/`es`/`en`, ICU plural/select, lazy namespace backend, typed static keys, and account → anonymous stored locale → allowed browser locale → club default resolution.
- ADR-011: all date/time output uses the club IANA time zone; money uses locale plus club ISO currency; duration examples are covered in all three locales.
- `T-02-17`: locale-resolution mechanism and club-locale filtering.
- Catalog coverage: 244 concrete codes from `CATALEG_ERRORS.md`, each with non-empty `ca`, `es`, and `en` messages.
- CI checks: locale parity, missing/empty/unused keys, forbidden vocabulary in translations and TSX, and error-catalog drift.
- Unit tests: 14 passing tests across ICU plural/gender, lazy loading, resolution/storage, dates, date ranges, times, durations, currency, and two club time zones.

### Assumptions
- Anonymous `localStorage` preference is kept between account locale and browser locale, as required by `CONVENCIONS_I18N.md`; the task's abbreviated priority omitted that documented step.
- `formatMoney` accepts major currency units because no API money value is involved in this foundation task; API money conversion remains the caller's responsibility.
- The `auth` namespace is present and parity-checked but empty until approved auth copy is introduced; no UI copy was invented.
- Concrete legacy aliases quoted in `CATALEG_ERRORS.md` are translated as catalog entries. Quoted wildcard/prefix rules such as `ALREADY_` and `FILE_*` are not treated as concrete codes.
- `IDEMPOTENCY_KEY_REUSED` was added to the shared catalog by E0-T04 during verification; its three translations were included before final evidence.

### Questions / catalog proposals
None.

## Organizer verification
Result: verified · 2026-09-06 · organizer (Claude)

Independent run in a clean Linux copy: `pnpm turbo run lint typecheck test build` → 26/26; `node scripts/check-error-codes.mjs` → 244 codes, 0 missing (the catalog updated today with `IDEMPOTENCY_KEY_REUSED` is already translated); `pnpm i18n:check` green on the clean tree and **red when it should be**: a key only in `ca` → «missing translation key» for `es`/`en` + unused; «una parella amigable» in a translation → two forbidden-vocabulary findings. Reviewed: i18next + react-i18next + ICU setup, locale resolution (account → stored preference → browser ∩ club locales → club default), `format.ts` with the club time zone/currency and the expected literals («2 h», «30 min», «4 h abans/antes/before», «45,00 €» / «€45.00»), namespaces `common/auth/shell/errors` in the three locales, i18next-parser config, the three apps now going through the real setup. Assumptions accepted (money in major units for now; empty `auth` namespace until E1).

Note for later tasks: `formatMoney` will need a `Money{amountMinor,currency}` overload when the api-client types arrive (E0-W04) — add it there.

