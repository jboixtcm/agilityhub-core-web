---
id: E5-W04
title: E5 integration (front): adopt the api E5 snapshot, run T-08-40 and T-09-40 end to end against the published core with the E5 demo seed (book → 03 → cancel in time and late → swap → waitlist join → seat released → claim → free training → ring block), P1/P6/P7 effects with the clock advanced, screenshots with real data, gate E5 (front) evidence
stage: E5
repo: agilityhub-core-web
thread: B
order: 40
status: not_open
depends_on: [E5-W01, E5-W02, E5-W03, E4-W05, external:api:E5-T06, external:api:E5-T25]
branch:
pr:
updated: 2026-09-26
---

# E5-W04 · E5 integration (front)

## Objective
Work packages S08 WP-08-G and S09 WP-09-F (front halves): the E5 screens (03, 04, 06/29, 07, the waitlist detail, 08, 24 and the back-office additions) run in the browser against the **real published core** with the E5 demo seed of api E5-T06 — bookings, a swap, a waitlist with entries, free-training bookings, a ring block and the processes P1, P6 and P7 reachable with the clock advanced; the S08/S09/S15 contract is adopted from the real snapshot (no E5 entry left in `pending.json`); and the gate E5 (front) evidence is produced for the organizer.

## Spec references
S08 §12 WP-08-G and **T-08-40** («reservar → veure a 03 → anul·lar dins termini → tornar a reservar amb intercanvi → apuntar-se a una classe plena → rebre N-15 (simulada) → agafar la plaça; i el mateix en `es`»), R-08-07 (the 30 s hold is real here: the test must confirm inside it), R-08-09, R-08-10 (the **4 h** threshold, `bookings.lateCancelThresholdMinutes = 240`), R-08-11 (`waitlist.notifyThresholdMinutes = 30`), R-08-13 (`ALL_AT_ONCE` at the Cànic: every waiting entry is notified), R-08-15 (claim), R-08-17 · S09 §12 WP-09-F and **T-09-40** (screen 24 E2E: the «Bloqueig» segment, the summary sentence, the block visible afterwards), R-09-04 (day+3 window), R-09-05 (3/week **by session date**), R-09-06, R-09-07, R-09-10 · S15 R-15-11 (P1 `week-opening`: after Sunday 20:00 local the `NOT_YET_OPEN` rows of 04 become bookable), R-15-16 (P6 `waitlist-fifo`), R-15-17 (P7 `payment-timeouts`), R-15-19 (P9 `cleanup`), R-15-08 (a dry run writes nothing), R-15-09 (manual trigger) and **T-15-32/T-15-33** re-run against real data · api `E5-T06` (the seed contents, `bin/e5-smoke`, `POST /test/clock`, `jobs:run`, the k6 scenarios — **k6 T-08-41 and the 50-simultaneous-requests concurrency proof are api-side**, do not reimplement them here) · gate E5 in the backlog: «cicle sencer reservar / anul·lar / llista d'espera (2 modes) / entrenament lliure amb els processos P1/P6/P7/P9 actius a staging i el rellotge avançat; k6 dins d'objectius; cap sobre-reserva sota concurrència» · `AGENTS.md` rule 4 (contract files), `roadmap/README.md` (evidence rule).

## Context to load
`roadmap/tasks/E2-W07.md` and `E4-W05.md` (the real-core stack and its generalisation: `scripts/e2e-core.sh` with per-task evidence directory and project name, `scripts/core-stack/docker-compose.yml`, `scripts/core-stack/club-canic.yaml`, `e2e/core/playwright.config.ts`, the existing `e2e/core/*.spec.ts` helpers `login`, `mobileContext`/`desktopContext`, mailbox reading, `E1_CORE_PASSWORD`) · `roadmap/tasks/E3-W03.md` (the blocker precedent when the image or the snapshot is missing) · the E5 screen tasks `E5-W01`, `E5-W02`, `E5-W03` (routes, literals, test ids, data attributes) · the staged `packages/api-client/openapi/openapi.next.json` (organizer: api E5-T06 snapshot; the commit message names the api commit) · `packages/api-client/openapi/pending.json`, `scripts/generate.mjs`, `src/mocks/fixtures.contract.test.ts` · api `E5-T06` task file (read-only, in the api clone or pasted by the organizer in `MESSAGES.md`): the seed's members, dogs, classes, waitlist entries, training bookings and ring block, the `--week-start` semantics inherited from E4-T05, the fictional second club that carries `waitlist.mode = FIFO` and `SINGLE_CLASS` · this file.

## Steps
0. **Contract adoption**: swap the staged `openapi.next.json` (api E5-T06 snapshot = the E5 contracts as implemented) into `openapi.json` byte for byte (`cmp`; record both `sha256`), `pnpm api:generate`, **prune every S08/S09/S15 path and schema from `pending.json`** (whatever E5-W01…W03 added mocks-first; list what legitimately stays), align the MSW fixtures and consumers with the published names (Ajv suite green), `pnpm turbo run lint typecheck test build`. Preflight, and **block with a `@organizer` entry in `MESSAGES.md` (E2-W07 precedent) if any of these fails**: the staged file exists; `docker image inspect ghcr.io/jboixtcm/agilityhub-core-api:main` resolves and its revision is recorded; `docker run --rm --entrypoint /bin/sh ghcr.io/jboixtcm/agilityhub-core-api:main -c 'ls /app/seeds'` lists `demo-canic.yaml`; the image answers `GET /api/v1/training-slots` (not 501).
   - **Api E5-T25 (organizer, 26-09).** The staged snapshot must also carry api E5-T25, which publishes what E5-W01 covered with overlays:
     - `Booking.dog`, `Booking.lateCancelThresholdMinutes` and `Booking.cancellableInTimeUntil`;
     - `WaitlistEntry.dog`;
     - `ReservationRow.ringColor`, and `activityId` on ACTIVITY rows;
     - `BookedBy.self`.

     Prune E5-W01's three overlays.
   - **Screen 07:** «Reservada per {name}» is decided with `BookedBy.self`, never by comparing names. The in-time warning uses `cancellableInTimeUntil`. 03 paints the ring dot with `ringColor`. Vitest for each.
   - The organizer opens this task when api E5-T25 is verified.
1. **Stack** (`scripts/core-stack/docker-compose.yml`, generalised by E2-W07): the `seed` service runs `club:apply` for the Cànic club file and then `seed:demo --club=canic --seed=42 --week-start=<Monday of the club-local current week>` (computed in `scripts/e2e-core.sh` with `TZ=Europe/Madrid date`, exported as `E5_WEEK_START` to compose and to Playwright); `SEED_PASSWORD` = `E1_CORE_PASSWORD` (generated at runtime, never printed); evidence directory `roadmap/evidence/E5-W04`, project name `agilityhub-e5-w04`. Keep every earlier core spec runnable in the same run (`pnpm e2e:core` executes all of `e2e/core/*.spec.ts`).
2. **Clock control**: E5-T06 exposes `POST /test/clock` **only under the `test`/`local` profiles**. Detect it once at the start of the spec (`POST /api/v1/test/clock` with the current instant → 2xx = available). If it is **not** available on the published image, run every clock-independent assertion, mark the clock-dependent ones as `test.skip` with a recorded reason, and take the P1/P6/P7 evidence from the api side (`bin/e5-smoke` output pasted by the organizer in `MESSAGES.md` or read from the api clone's `roadmap/evidence/E5-T06/`) — say exactly which gate lines were proven that way. Never fake time in the browser for a core run.
3. **`e2e/core/e5-core.spec.ts`** (serial, real core, seeded fictional accounts read from `scripts/core-stack/club-canic.yaml` / the demo seed README — never invented, never the mockups' first names as people; select rows by state, time and data attributes such as `data-class-id`, `data-class-state`, `data-bookable-state`, `data-slot-state`, `data-ring`, and assert api statuses with Playwright response hooks):
   (a) **T-08-40, book** (member, 375, `ca`): `/inici` shows the seeded reservations and the counters; `/reservar` shows the dog chips with the seeded default dog and the six row states of the seed (assert at least `BOOKABLE`, `WAITLIST_OPEN`, `WAITLIST_FULL`, `WEEKLY_LIMIT_DONE`, `NOT_YET_OPEN`); tap a `BOOKABLE` row → `/reservar/confirmar` shows the countdown running from `serverNow` → [CONFIRMAR LA RESERVA] **within the 30 s hold** → 201 → the green note with the three calendar links → `/inici` shows the new row.
   (b) **Cancel in time and late**: open the new booking on `/reserves/:id` → [ANUL·LA LA RESERVA] → the green in-time note (`late = false`) and the row gone from `/inici`. Then, on the seeded booking that starts inside the 4 h threshold, cancel → the yellow note quoting **4 hores** and the booking listed as «anul·lada tard» (the seed provides both cases; if it only provides one, advance the clock per step 2 and say so).
   (c) **Swap**: as the seeded member who has the weekly limit reached **with** a swappable booking, tap a `BOOKABLE` row → 06 shows the yellow limit note, the `swappable` cards and the inert `notSelectable` ones → select one → the button reads «ANUL·LA {dia} I CONFIRMA {dia}» → confirm → 201; `/inici` shows the new booking and not the old one. As the member with the limit reached and **no** swappable booking, the same tap lands on the 29 informative variant. Its second sentence follows `details.week` (S08 §2 row 29, amended 26-09): «Podràs reservar per a la setmana vinent a partir de …» with `CURRENT`, and «Podràs reservar aquesta classe a partir de …» with `NEXT`.
   (d) **Waitlist (`ALL_AT_ONCE`, the Cànic)**: on a `WAITLIST_OPEN` row → the dialog → [Sí] → toast «Ets a la llista d'espera» and the row disappears from 04; `/inici` shows the «en llista d'espera» row → `/espera/:id` shows the entry. Free a seat: as the admin (1280) open `/calendari`, select that class and use E5-W03's registrants panel to confirm the booked members, then have a **booked** member cancel her seat from `/reserves/:id` more than 30 minutes before the start (`waitlist.notifyThresholdMinutes`) → the waiting member's `/espera/:id` now shows the `NOTIFIED` state (**N-15 arrives through the feed of screen 11, which is E7**: assert the entry state and, if the api exposes it, the mailbox, and say the feed assertion is out of scope) → [AGAFA LA PLAÇA] → the hold page → confirm → 201 `CONSOLIDATED` and the booking on `/inici`. FIFO is only configured on the fictional second club of the seed: assert it **api-side** from `bin/e5-smoke` and say so.
   (e) **Free training (T-09-40 and R-09-04/05/07)** (member, 375): `/entrenaments` shows the eligible-dog chips, the four day chips (today + 3) and the counter «Portes 2/3 …» from the seed; book a free slot with «Qualsevol» → 201 and the counter goes to 3/3; attempt a fourth → the limit message with the cancellable-booking link; open the booking detail → cancel → the counter drops back and the slot is `FREE` again; assert that no chip exists for day+4. Instructor (375): `/instructor/pistes/{ringId}/reservar` → segment «Bloqueig» hides the reservation reasons → pick two contiguous cells → the summary sentence «Ocupa la pista {Pista} de {from} a {to}, surt al quadre global i al registre d'ús de pistes. No es vincula a cap alumne.» → [BLOQUEJA LA PISTA] → 201; the block shows up on `/instructor/avui` («Bloq. …»), on the admin `/calendari` and in E5-W03's ring-usage register; a block over the seeded live training booking answers `422 RING_HAS_BOOKINGS` and the instructor screen offers no force option.
   (f) **Processes (P1, P6, P7) with the clock advanced** — only when step 2 found `POST /test/clock`: set the clock just before the next Sunday 20:00 club-local, assert a `NOT_YET_OPEN` row on 04 with its `opensAt`; advance past it, trigger `week-opening` from E5-W03's D11 card ([Executa ara]) and assert the same row is now `BOOKABLE` (**P1**); run [Simula] on `risk-review` and assert the modal shows a plan and that **nothing changed** afterwards (R-15-08), then [Executa ara] and check the D1 risk card statuses (**T-15-33** against real data); `payment-timeouts` and `waitlist-fifo` belong to the fictional FIFO/`SINGLE_CLASS` club and are asserted api-side (**P6**, **P7**) — record their `bin/e5-smoke` lines; `cleanup` (**P9**) is asserted only through its `JobRun` row in the D11 card. Restore the clock at the end of the spec.
   (g) **`es` pass**: repeat (a) and (d) with `localStorage` `agilityhub.locale = "es"`, asserting the Spanish literals of the confirm and waitlist flows.
   (h) **Cleanup for idempotence**: every booking, waitlist entry, training booking and ring block the run creates is cancelled at the end (never deleted physically); a second `pnpm e2e:core` must pass unchanged (use a `runId` suffix wherever a free-text field exists).
4. **Screenshots with real data** under `roadmap/evidence/E5-W04/`: 375 — `03-inici-core-375.png`, `04-reservar-core-375.png`, `06-confirmar-canvi-core-375.png`, `29-limit-core-375.png`, `07-anullada-tard-core-375.png`, `07-espera-notificada-core-375.png`, `08-entrenaments-core-375.png`, `24-bloqueig-core-375.png`; 1280 — `D4-inscrits-core-1280.png`, `entrenaments-registre-core-1280.png`, `D11-processos-core-1280.png`, `D1-risc-core-1280.png`. Compare each with `docs/pantalles/` and list every deviation (fix only one-line literal mistakes; list the rest).
5. **Contract discrepancies**: any difference between what the published core returns and the generated types (a missing required field, a status different from `CATALEG_ERRORS.md` — `JOB_UNKNOWN`, `SLOT_NOT_ON_GRID`, `OVERRIDE_NOT_ALLOWED` and `DOG_ALREADY_BOOKED` are the known candidates —, a literal in `message` you had to rely on) is reported as a `@organizer` proposal in the report and `MESSAGES.md` (E1-W04 precedent: INC-06); never patch `openapi.json` by hand.
6. **Gate E5 (front) evidence** in the report, one line per gate item with the spec step or screenshot that proves it: full cycle book / cancel in time / cancel late / swap; waitlist join → seat released → notified → claim in `ALL_AT_ONCE` (FIFO api-side, cited); free training booked and cancelled with the 3/week counter by session date and the day+3 window; instructor ring block visible in the three views; P1 effect visible on 04 with the clock advanced (or the api evidence that replaces it); the D11 and D1 cards driving and reporting the processes. `ROADMAP.md` is organizer-owned — do not edit it.

## Fixed conventions
- Same Docker wrapper as E1-W04/E2-W07/E4-W05 (`pnpm e2e:core` with `CORE_URL=http://core:8080`); the stack is self-cleaning; no secrets printed (`E1_CORE_PASSWORD` generated at runtime, tokens and ids truncated in the report); fictional data only (seed accounts `@example.test`); `docker login` is never run (the wrapper pulls the image outside the sandbox).
- The MSW/unit suites stay untouched except for the contract alignment of step 0 (keys never literals; ca/es/en together).
- No new parameter, event, notification or error code; no api change (proposals only). Design system, i18n and vocabulary rules unchanged. Never reimplement k6 or the concurrency proof here — they are E5-T06's.
- Timing: the 30 s hold is real. Do the confirm step immediately after the hold and give the countdown assertions a tolerance; never `waitForTimeout` past `expiresAt` except in the deliberate expiry check (which must then re-hold).
- Evidence rule: command + exit code + last 40 lines; long outputs → `roadmap/evidence/E5-W04/NN-<name>.log`; task file ≤ 120 KB; complete `pnpm e2e` (three MSW projects) in the Playwright image after the contract alignment; never edit other tasks, `ROADMAP.md` or the Organizer verification section.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `docker image inspect ghcr.io/jboixtcm/agilityhub-core-api:main --format '{{.Architecture}} {{index .Config.Labels "org.opencontainers.image.revision"}}'` (record the revision); `pnpm api:generate` + `shasum -a 256` of `openapi.json` vs the staged file + `jq -r '.paths | keys[]' packages/api-client/openapi/pending.json` (no S08/S09/S15 path); the Ajv suite; `CORE_URL=http://core:8080 pnpm e2e:core` green **twice** (E1 + E2 + E3 + E4 + E5 specs; paste the tails, keep the full logs); `pnpm turbo run lint typecheck test build` + `pnpm i18n:check`; complete `pnpm e2e` in the Playwright image; the twelve screenshots.

## Definition of Done
T-08-40 and T-09-40 pass end to end against the published core with the E5 demo seed (twice, idempotent); the S08/S09/S15 contract is the real snapshot with no pending entry; the P1/P6/P7/P9 effects are proven from the UI with the clock advanced, or cited from the api evidence with the reason recorded; screenshots with real data are committed and compared; gate E5 (front) evidence is in the report; contract discrepancies, if any, are filed as proposals.

## Evidence to return
Image revision, snapshot hashes and the `pending.json` path list, `e2e:core` tails (both runs), `e2e` tail, the screenshot list with deviations, the gate E5 lines each paired with the output that proves it (marking the api-cited ones), whether `POST /test/clock` was available, and the discrepancy list.

---

## Executor report
_(not started)_

### Branch / commits

### Evidence (full command outputs)

### Files changed

### Rules and tests implemented (R-xx-nn / T-xx-nn)

### Assumptions

### Questions / catalog proposals

## Organizer verification
_(pending)_
