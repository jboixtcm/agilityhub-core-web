---
id: E3-W06
title: Gate E3 audit fixes (web, 1/3) — public signup: the identity document at submission (NIE/passport), stable retries, errors mapped to fields and steps, the consent version, the payment texts under the right method, and the signup minors
stage: E3
repo: agilityhub-core-web
thread: B
order: 60
status: verified
depends_on: [E3-W04]
branch:
pr:
updated: 2026-09-24
---

# E3-W06 · Gate E3 audit fixes (web, 1/3)

## Objective
Jordi asked for an exhaustive review of gate E3 before he closes the stage. Codex and the organizer audited the code separately, at web `5d371d3` and api `20552c6`. Both say the gate does not pass. The consolidated report is `roadmap/reviews/gate-E3/consolidated.md`, and the two source reports sit next to it with the file:line detail.

This task fixes the public-signup findings that need **no api change**, so it can start now. Two later tasks need the new api snapshot (api E3-T08/T09):
- **E3-W07**: D2 and D1;
- **E3-W08**: the payment card of 19 from the per-plan quote, signed uploads, screen 13 and the signup flags.

**E3-W09** re-runs the audit at the end.

Every step names the finding (the B/M-number of the consolidated report), the defect, the rule, the fix and the test. **Each fix comes with a test that fails before the fix.** Name those tests in the report, and give them the spec id where one applies (T-04-29…32).

## Spec references
- S04:
  - §2, rows 16–19 and «enviada»;
  - §3 (`Member`, `Dog`);
  - R-04-01, R-04-03, R-04-05, R-04-07, R-04-10, R-04-17 and R-04-26/27;
  - §10 (keys) and §11 (T-04-29…32).
- `CONVENCIONS_API.md` §7 (idempotency, retries from the phone), and `CATALEG_ERRORS.md` (rule 0; the fronts map by code).
- Mockups `docs/pantalles/mobil/16…19`.

## Context to load
- `roadmap/reviews/gate-E3/consolidated.md` (B1, M1, M2, M19 and «Web, signup»), and the source reports.
- `apps/clubs/src/SignupPage.tsx` and its tests.
- `packages/i18n/src/locales/*/signup.json`.
- The MSW signup handlers.
- `e2e/core/e3-signup.spec.ts`.

## Steps
0. **Harness.** `scripts/e2e-core.sh` runs the E3 stage only for the evidence folders `E3-W03`…`E3-W05` (the E3-W05 run set `^E3-W0[345]$`). Extend it to `E3-W06`…`E3-W09`, so this task and the next three run the E3 scenarios on a fresh seed.
1. **B1 · the identity document at submission (R-04-01).**
   - The defect: `POST /signup` spreads `draft.person` (`SignupPage.tsx:1359`). Its `idDocument.type` stays `"DNI"` and the passport is never sent. A NIE applicant gets `400 INVALID_ID_DOCUMENT`, and a passport-only applicant a 400.
   - The fix: build the body's `idDocument` with `identityDocument()` (`:376-389`), the builder the identity check already uses. One source for both requests.
   - Vitest, on the request body:
     - DNI;
     - NIE: `x1234567-l` → `{type: NIE, value: X1234567L}`;
     - passport only: `{type: PASSPORT, value}`.
   - Real-core e2e: a NIE applicant reaches «Sol·licitud enviada».
2. **M1 · stable retries (CONVENCIONS_API §7, R-04-26/27).** Today every tap sends a fresh `Idempotency-Key` (`:1331, 1343, 1369, 1382`). The draft is also deleted before the checkout, and `signupToken` lives only in a closure.
   - Use one key per payload:
     - generate it when the step-19 payload is first sent;
     - keep it in the draft;
     - reset it when any field of the payload changes.
   - Keep `memberId`, `signupToken` and the checkout state in the session draft until the flow ends: the success page, or a resolved checkout. Delete the draft only then.
   - `?cs=cancel` goes back to 19 with the data. Its retry reuses the signup already created (checkout only); it is not a new `POST /signup`.
   
   Vitest:
   - a lost 201 (a network error after the server committed) → the retry sends the same key and gets the same 201;
   - add-dog, the same: the member's own dog never answers `DOG_CHIP_ALREADY_REGISTERED`;
   - a cancelled checkout → 19 keeps the data.
3. **M2 · errors mapped to fields and steps (§2 «400 del back mapejat a camps», CATALEG_ERRORS).** Today step 16 maps only `details.fieldErrors`. Step 19 handles five codes, and everything else becomes «No s'ha pogut completar l'acció.» (`:451-463`, `:1389-1404`).
   - Map each code to the field and the step that owns it, navigate there, and focus the field:

     | Code | Goes to |
     |---|---|
     | `INVALID_ID_DOCUMENT`, `ID_DOCUMENT_AMBIGUOUS` | 16, document |
     | `INVALID_PHONE` | 16, phone |
     | `INVALID_IBAN` | 19, IBAN |
     | `PLAN_NOT_AVAILABLE` | 18, plan, after reloading the config |
     | `DOG_DOCUMENT_REQUIRED`, `FILE_NOT_FOUND` | 17, documents |
     | `DOG_CHIP_ALREADY_REGISTERED` | 17, chip |
     | `FAMILY_HOLDER_NOT_FOUND` | 18, family |
     | `CONSENT_VERSION_OUTDATED` | 19, consents (step 4) |
     | `MEMBER_ALREADY_EXISTS`, `SIGNUP_ALREADY_PENDING` | the inline message of 16 (R-04-05) |
     | `SIGNUP_CLOSED` | the closed screen |
     | `RATE_LIMITED` | its own message |
   - Normalise the `details.fieldErrors` paths (`emails[0]`, `person.idDocument.value`, …) to the form keys.
   - Any other code: `t('errors:'+code)`, with the generic text as the last fallback.
   - Local checks of the ES profile before sending: the postal code, the birth date (past, ≥ 1900), `birthMonth` ≤ the current month, and the IBAN mod-97.
   - MSW answers with the api's real shapes and statuses: rule 0 (422 vs 409), and a top-level `code` with empty `details` where the api does that.
   
   Vitest: one table test over the codes.
4. **M19 · the consent version (R-04-17; GDPR art. 7).**
   - The defect: the version sent at submission is the current config's `legal.legalTextsVersion` (`:1311`). Meanwhile the draft keeps `privacyAccepted` for 24 h.
   - The fix:
     - store the accepted version together with the acceptance;
     - when a config reload brings another version, clear the acceptance and ask again;
     - send the accepted version;
     - on `422 CONSENT_VERSION_OUTDATED`, go back to the consents with the new texts.
   
   Vitest: accept v1 → reload with v2 → the box is cleared, and submission is blocked until it is accepted again.
5. **The payment texts under the right method (M7, web half; T-04-32).**
   - The cash conditions (`texts.cashConditions`) appear only when the recurring method CASH is selected.
   - The day-of-month text (`texts.paymentDay`) does not appear under CARD (Codex F30).
   - The MANUAL instructions come from `paymentMethods[MANUAL].instructions`. Api E3-T08 fixes their content; the field already exists.
6. **The signup minors**, with a test wherever there is behaviour:
   - `planId` and `planIdRequested` are omitted when empty (a club without signup plans; Codex F3).
   - The honeypot is `aria-hidden` and out of the tab order.
   - Focus moves to the first error **after** it renders, and `aria-describedby` links each field to its message.
   - The async upload and the town lookup update the draft functionally, with no stale overwrite.
   - `offerLabel` shows only with `FAMILY_GROUP`.
   - The image-consent text comes from `texts.imageConsent` when the config has it, otherwise from `legal.imageConsentText`. Say which one the current snapshot has.
   - The account holder is prefilled with the group holder when the family group is FOUND (R-04-10).
   - The `notesToInstructors` field on 17 and the `holderTaxId` field on 19 (§2, §3).
   - The add-dog success page does not promise a welcome email.
   - «Laia F..» → «Laia F.».
   - The chip is validated per profile (ES: 15 digits; GENERIC: 8–15 alphanumerics) and normalised.
   - The IBAN leaves the session draft when the method changes away from SEPA.
   - GENERIC profile: an editable phone prefix, and the profile's first document type as the default. It is latent (no GENERIC club yet), so a unit test is enough.
7. **Re-shoot** 16, 17, 18, 19 (monthly), «enviada» and add-dog 19 at 375, with `pnpm e2e:core E3-W06`. Compare each screen with its mockup in the report, one line per screen. The payment card itself changes in E3-W08.

## Fixed conventions
- Design system only.
- Keys are never literals; ca, es and en are added together.
- Evidence logs are committed, **including one log per failed attempt**.
- Fictional data only.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E3-W06/*.log`)
- `pnpm turbo run lint typecheck test build`.
- `pnpm i18n:check`.
- `pnpm e2e:core E3-W06` (the E1, E2 and E3 stages).
- The complete `pnpm e2e` in the Playwright Docker image (`scripts/e2e-docker.sh`).

## Definition of Done
- A NIE and a passport-only applicant can submit, in Vitest and in the real-core e2e.
- A lost 201 is retried with the same key.
- Every api code of step 3 lands on its field.
- The consent version sent is the accepted one.

## Evidence to return
- The command outputs.
- A step → test table.
- The screenshots, with the per-screen comparison lines.

---

## Executor report

### Status
- Executor: claude-opus-5-5, session of 2026-09-24. Depends on E3-W04 (`awaiting_verification`, allowed by the protocol).
- All steps 0–7 are done. Every Vitest named below **fails on the pre-fix page** (46 of the 53 tests in `SignupPage.test.tsx` fail against `git HEAD` `SignupPage.tsx`; the 7 that pass are pre-existing tests; log `01-vitest-before-fix.log`) and passes after the fix.
- Verification: `turbo lint typecheck test build` exit 0, `i18n:check` exit 0, the complete `pnpm e2e` in Docker exit 0 (1 + 25 + 29).
- **`pnpm e2e:core E3-W06` is red (exit 1), and not because of this change.** E1/E2 passed 10/10 and E3 passed 3/4: the NIE signup, D2, the welcome link and add-dog all pass. The only failure is `T-04-34 signup.enabled=false`, the api defect E3-W05 already reported: the core still answers `GET /signup` with `enabled: true` after `PUT /parameters/signup.enabled {false}`. The new diagnostic proves it (`signup-closed-config-core.json`: `"enabled": true`, `no-store`). There were 6 runs in total (logs `04a`…`04e` plus the final `04`).

### Files changed
- `apps/clubs/src/SignupPage.tsx`: the fixes for steps 1–6 (details below).
- `apps/clubs/src/SignupPage.test.tsx`: the existing tests updated (valid chips, seeded drafts, add-dog success path) plus 43 new tests.
- `packages/ui/src/components.tsx`: `FormField` gives its error the id `{id}-error`, the target of `aria-describedby`.
- `packages/api-client/src/mocks/handlers.ts`, for the signup handlers:
  - api statuses per rule 0: `SIGNUP_CLOSED` and `SIGNUP_ALREADY_PENDING` are 422;
  - identity check: a top-level `INVALID_ID_DOCUMENT` 400 with empty `details`; an empty value gets `VALIDATION_ERROR`;
  - `POST /signup`: document checks (DNI/NIE control letter, passport), IBAN mod-97, `PLAN_NOT_AVAILABLE`;
  - idempotent replays per `Idempotency-Key` for `/signup` and `/me/dogs/signup`. A fresh key after a commit gets `SIGNUP_ALREADY_PENDING` or `DOG_CHIP_ALREADY_REGISTERED`, like the api;
  - `resetSignupMockState` is exported; add-dog `checkout.required` follows the Stripe scenario.
- `packages/i18n/src/locales/{ca,es,en}/signup.json`: 5 new keys (see Questions).
- `scripts/e2e-core.sh`: E3 staging for `^E3-W0[3-9]$` (step 0).
- `e2e/core/e3-signup.spec.ts`:
  - the second applicant is a NIE (`y7654321-g`), and each submission asserts the normalised `person.idDocument` in the request body;
  - add-dog waits for `/gossos/nou/enviada`, asserts no «benvinguda», and captures `enviada-add-dog-core-375.png`;
  - a diagnostic on the closed-signup test writes the core's `GET /signup` answer to the evidence folder (no assertion changed);
  - the signup response body is kept from the route for the status message.
- `apps/clubs/e2e/signup.spec.ts`: the MSW e2e types valid 15-digit chips.
- `CHANGELOG.md` (Unreleased → Changed).
- `roadmap/evidence/E3-W06/*`: logs, screenshots and JSON diagnostics.

### Rules and tests implemented (step → test)
All tests are in `apps/clubs/src/SignupPage.test.tsx` unless noted.

| Step / finding | Fix | Test(s) |
|---|---|---|
| 0 harness | `e2e-core.sh` stages E3 for `E3-W03`…`E3-W09` | `pnpm e2e:core E3-W06` ran two stages (log `04`: 10 tests, then 4) |
| 1 · B1 (R-04-01) | the body's `person.idDocument` = `identityDocument(draft, ES)`, the same builder as the identity check | `B1 … › submits a DNI / NIE / passport only applicant…`: `12345678z` → `{DNI, 12345678Z}`, `x1234567-l` → `{NIE, X1234567L}`, passport only → `{PASSPORT, AB1234567}`. Real core: `e3-signup.spec.ts`, where Pau `y7654321-g` → `{NIE, Y7654321G}` reaches «Sol·licitud enviada» (log `04`, test 1/4 passed) |
| 2 · M1 (CONVENCIONS_API §7, R-04-26/27) | `draft.submission {fingerprint, idempotencyKey, memberId?, signupToken?, checkoutKey?}`: one key per payload, generated on the first send, renewed when the payload changes; the created signup and the checkout attempt stay in the draft until `/…/enviada`; `?cs=cancel` → 19 with the data, only a new checkout (the cancelled attempt's key is dropped) | `M1 › retries a lost 201 with the same Idempotency-Key…`, `› changing a payload field after a failure uses a new key`, `› add-dog: a lost 201 is replayed, never DOG_CHIP_ALREADY_REGISTERED…`, `› a cancelled checkout returns to 19 with the data and retries only the checkout` |
| 3 · M2 (§2, CATALEG_ERRORS) | `CODE_ROUTES` table + `formField()` path normaliser + `t('errors:'+code)` with the generic fallback; the page navigates to the owning step, which focuses the field after render; local ES checks | `M2 api errors land on their field and step` (table, 18 rows: every code of the step table + `PAYMENT_METHOD_NOT_AVAILABLE`, an unknown code, 2 `fieldErrors` path cases); `T-04-29 › M2 checks the ES postal code and a past birth date from 1900…`; `T-04-30 › validates and normalises the chip … and the birth month`; `T-04-32 › checks the IBAN mod-97 before sending` |
| 4 · M19 (R-04-17) | `draft.consentVersion` is stored with the acceptance; a config reload with another version clears privacy + image; the submission sends `consentVersion`; `CONSENT_VERSION_OUTDATED` → cleared + reload + focus on the box; old drafts without a version lose the acceptance | `M19 › clears an acceptance of other legal texts and sends the version accepted again` (v1 accepted → reload with v2 → box cleared, submit disabled → accept → body `2027-01`); `M19 › a saved acceptance of an older version is not carried…`; table row `CONSENT_VERSION_OUTDATED` |
| 5 · M7 web half (T-04-32) | cash conditions only under MANUAL, no `texts.paymentDay` under CARD, the upfront instructions from `paymentMethods[MANUAL].instructions` | `T-04-32 › shows each payment text only under its method` |
| 6 `planId` / `planIdRequested` | omitted when empty | `T-04-32 › omits planId and planIdRequested when the club has no signup plans` |
| 6 honeypot | `aria-hidden` label, `tabIndex -1` | `T-04-32 › hides the honeypot from assistive technology…` |
| 6 focus / `aria-describedby` | `useFocusFirstError` (effect after render); `invalidProps()` on every field | `T-04-29 › focuses the first error after it renders and links each field to its message` (+ the focus assertions of the M2 table) |
| 6 functional draft updates | `onChange` is the `setDraft` dispatcher; the upload and town lookup use updaters, and a stale town lookup is ignored | `T-04-30 › keeps fields typed while an upload is in flight`; `T-04-29 › keeps fields typed while the town lookup is in flight` |
| 6 `offerLabel` | only with the `FAMILY_GROUP` module | `T-04-30 › gates offerLabel by FAMILY_GROUP even when the configuration sends it` |
| 6 image consent | `texts.imageConsent`, else `legal.imageConsentText` | `T-04-32 › reads the image-consent text from texts.imageConsent, then legal.imageConsentText` |
| 6 holder = group holder (R-04-10) | FOUND sets `payment.holderName` to the typed holder; 19 prefills an empty holder at mount | `T-04-31 › prefills the account holder with the group holder…` |
| 6 `notesToInstructors` / `holderTaxId` | a textarea on 17 under the chip (§13 #1); an optional field on 19 under SEPA, normalised | `T-04-32 › sends notesToInstructors from 17 and holderTaxId from 19` |
| 6 add-dog success | `/gossos/nou/enviada` with `signup:payment.addDogReviewFooter` (also the 19 footer in add-dog mode) | `T-04-32 › the add-dog success page does not promise a welcome email`, `› uses the member plan … posts the add-dog flow`. Real core: `enviada-add-dog-core-375.png` |
| 6 «Laia F..» | the trailing period of `holderDisplayName` is stripped before the sentence | `T-04-31 › continues empty, resolves…` (exact text «Grup trobat: Marta R. El gos nou…»). Real core: `18-family-core-375.png` |
| 6 chip | normalised (no spaces, hyphens or dots; upper case); ES 15 digits, GENERIC 8–15 alphanumerics | `T-04-30 › validates and normalises the chip per country profile…` |
| 6 IBAN leaves the draft | switching away from SEPA deletes `iban` and `holderTaxId` | `T-04-32 › drops the IBAN from the session draft…` |
| 6 GENERIC (latent) | the profile's first `PASSPORT`/`OTHER` as the default, options from the profile, an editable prefix `Input` | `T-04-29 › GENERIC profile: the first document type of the profile and an editable prefix` |
| 7 re-shoot | real-core captures at 375 | see «Screens» below |

### Screens (375 px, real core, `roadmap/evidence/E3-W06/`)
- **16** `16-person-core-375.png` vs `docs/pantalles/mobil/16-…png`: same fields and order; unchanged by this task (labels above the fields, as accepted in E3-W01/W03).
- **17** `17-dog-core-375.png` vs mockup 17: same, plus the optional «Notes als instructors (opcional)» under «Núm. de xip», as decided in S04 §13 #1 (the mockup does not show it). The packs show «Entrada 0,00 €» from the core's plan data. Not changed here: M5 (hide zero lines), E3-W08.
- **18** `18-family-core-375.png` vs mockup 18: «Grup trobat: Laia F. El gos nou quedarà vinculat…», one period as in the mockup. The intro still shows the api placeholder `{twoDogsMonthlyFee}` (M6, api E3-T08).
- **19 (monthly)** `19-payment-core-375.png` vs mockup 19:
  - «Titular del compte» is now the group holder «Laia Fictici001» (R-04-10);
  - new «NIF del titular (opcional)»;
  - the mandate and the day text show under «Domiciliació» only. The day text still carries the api placeholder `{deadlineDay}` (M6, api);
  - the upfront card shows the MANUAL `instructions` exactly as the core sends them (their content is api E3-T08);
  - «Total a pagar al club» is still missing (M3, E3-W08).
- **enviada** `enviada-core-375.png`: title + the 19 footer, unchanged.
- **add-dog 19** `19-add-dog-core-375.png`: current method read-only «Domiciliació · ···· ···· ···· ···· 1332». The footer now reads «El club revisarà la sol·licitud i el pagament del gos nou, i t'avisarem a l'app quan l'hagi validat.». The payment card itself changes in E3-W08. Its «0,00 €» ALTERNATIVE row is api data (the 1 October start pays only the entry; R-04-14).
- **add-dog enviada** `enviada-add-dog-core-375.png` (new): no welcome promise.

### Assumptions
1. **`PLAN_NOT_AVAILABLE` goes to 17, not 18.** The task table says «18, plan». The plan selector («Modalitat») lives on screen 17 (S04 §2 row 17, mockup 17), and add-dog has no 18. The spec wins; the config is reloaded first, as the task asks.
2. **The day text under CARD.** S04 §2 row 19 lists `signup.text.paymentDay` under «Targeta», but T-04-32 and the task (Codex F30) say mandate + day text only under «Domiciliació». I followed T-04-32 and the task. The §2 row should be aligned.
3. **Checkout attempts.** The checkout key is stable across retries of the same attempt (network errors). A `?cs=cancel` return resolves that attempt: the retry asks for a new session for the signup already created, with a new key. `cancelUrl` is now `/apuntat-hi/pagament?cs=cancel` (or `/gossos/nou/pagament?cs=cancel`); the old `/apuntat-hi/enviada?cs=cancel` still lands on 19. Stripe itself is E8-T04.
4. **Old drafts.** A saved draft without `consentVersion` (written by the previous build) loses its privacy and image acceptance: it cannot prove which texts were accepted.
5. **Consent version for the image box.** `imageUse.version` is the same accepted version. A new legal version clears both boxes.
6. **`MEMBER_ALREADY_EXISTS`** shows `errors:MEMBER_ALREADY_EXISTS` («Aquest abonat ja existeix.») inline on 16, and `SIGNUP_ALREADY_PENDING` shows the R-04-05 literal `signup:person.alreadyPending`. `SIGNUP_CLOSED` shows the closed screen with `closedText` (fallback `errors:SIGNUP_CLOSED`) and reloads the config.
7. **The image-consent text in the current snapshot:** `SignupTexts.imageConsent` and `SignupLegal.imageConsentText` are both present and required in `openapi.json`. The front uses `texts.imageConsent` when it is non-empty, otherwise `legal.imageConsentText`.
8. **Add-dog** gets its own success route `/gossos/nou/enviada` (inside the authenticated `/gossos/nou*` tree). The 19 footer in add-dog mode uses the same non-welcome text.
9. **The birth-date and birth-month checks** use the browser's local date. The club's time zone matters only around midnight.

### Questions / catalog proposals
- **Proposed literals** (no mockup; ca / es / en):
  - `signup:common.invalidPostalCode`: «El codi postal no és vàlid.» / «El código postal no es válido.» / «The postal code is invalid.»;
  - `signup:dog.invalidChip`: «El número de xip no és vàlid.» / «El número de chip no es válido.» / «The microchip number is invalid.»;
  - `signup:dog.notesToInstructors`: «Notes als instructors (opcional)» (D2 label + «(opcional)» as on 16) / «Notas para los instructores (opcional)» / «Notes for the instructors (optional)»;
  - `signup:payment.holderTaxId`: «NIF del titular (opcional)» / «NIF del titular (opcional)» / «Account holder tax ID (optional)»;
  - `signup:payment.addDogReviewFooter`: «El club revisarà la sol·licitud i el pagament del gos nou, i t'avisarem a l'app quan l'hagi validat.» (N-37 is APP) / es / en.
- **For the api (repeat of E3-W05):** the stale `GET /signup` after `PUT signup.enabled=false`. It reproduced in all 4 E3 stages that reached that test this session (attempts `04b`, `04c`, `04d` and the final `04`). Evidence: `signup-closed-config-core.json` and `04d-signup-closed-config-core.json` show `enabled: true` with `Cache-Control: no-cache, no-store…`.
- **For the api / E3-W07 (M11):** in run `04e`, D1 did not list the just-added dog «Neret E3» within 15 s after `/tauler` was reopened. This is the async dashboard invalidation.
- **No new error codes, parameters, events or notifications.**

### Evidence
Earlier attempts, one line each:
- turbo attempt 1 → exit 1: `clubs#lint` found 4 errors in the new test (`no-empty-function` ×2, `non-nullable-type-assertion-style` ×2); turbo then stopped the running `clubs-admin:test`. Fixed in the test file. Its log was overwritten by the final run (my mistake); the errors are quoted here.
- `04a-e2e-core-attempt1-fail.log` → my spec read the signup response body after navigation (`Network.getResponseBody`). Fixed: the body is kept from the route.
- `04b-…-attempt2-closed-text.log` and `04c-…-attempt3-closed-text.log` → E1/E2 10/10, E3 3/4; closed-text failure (stale core config). Added the config diagnostic.
- `04d-…-attempt4-closed-text.log` (+ `04d-signup-closed-config-core.json`: `enabled: true`) → same.
- `04e-…-attempt5-d1-stale.log` → E3 test 1 failed on D1 not listing the new add-dog row within 15 s (M11); 3 did not run.

**1. Pre-fix proof.** `pnpm exec vitest run src/SignupPage.test.tsx --reporter=verbose` (in `apps/clubs`, with `git show HEAD:apps/clubs/src/SignupPage.tsx` temporarily in place, then restored) → **exit 1**, `Tests 46 failed | 7 passed (53)`. Condensed log (per-test list + first assertion lines): `roadmap/evidence/E3-W06/01-vitest-before-fix.log`.

**2.** `pnpm turbo run lint typecheck test build` → **exit 0**. Full log: `02-turbo.log`. The live tests of this run: clubs 6 files / **98 passed**, clubs-admin 15 / **101 passed**. The other test tasks were cache hits from attempt 1, where they had run with the same inputs: api-client 68, ui 49, i18n 37, id 9. Last 40 lines:
```
clubs-admin:test: stderr | src/planning/TemplatesPage.test.tsx > T-06-26 D3 weekly templates > shows the coverage table with the D3 vocabulary and one-decimal places
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/TemplatesPage.test.tsx > T-06-26 D3 weekly templates > R-06-15 offers instructor chips with maxInstructorsPerClass = 2
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/TemplatesPage.test.tsx > T-06-26 D3 weekly templates > R-06-15 hides levels and coverage with levels.enabled = false and requires a manual description
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > INSTRUCTOR reads the calendar without actions and with inert chips (A22 c)
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test:  ✓ src/planning/TemplatesPage.test.tsx (13 tests) 4034ms
clubs-admin:test:    ✓ T-06-26 D3 weekly templates (13)
clubs-admin:test:      ✓ prefills band and day from an empty cell, fills the description by levels, keeps the manual text and asks for another class in the band 1055ms
clubs-admin:test:      ✓ saves every change of a full cell at once with the template version 656ms
clubs-admin:test:      ✓ queues two quick changes of a full cell and sends each PATCH with the latest version 381ms
clubs-admin:test:      ✓ reloads the template and remounts the card on STALE_VERSION 389ms
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > shows the week warnings and selects the class of a warning; day headers open the day view
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test:  ✓ src/planning/CalendarPage.test.tsx (19 tests) 4034ms
clubs-admin:test:    ✓ T-06-28 D4 / D4b / D4c class calendar (front half, MSW) (19)
clubs-admin:test:      ✓ opens «Esborrany» on the first week with drafts, with the validation card and its count 707ms
clubs-admin:test:      ✓ [VALIDAR LA SETMANA] turns every draft of the week active at once 304ms
clubs-admin:test:      ✓ cancels an active class with registrants through D4c: four rows, text required, «ANUL·LA I AVISA ELS 4 ALUMNES» 346ms
clubs-admin:test:      ✓ [ELIMINA] of an active class goes through the same D4c modal 336ms
clubs-admin:test:      ✓ R-06-09 saves the diff with the version; STALE_VERSION keeps its message after the refetch remounts the card 437ms
clubs-admin:test: 
clubs-admin:test:  Test Files  15 passed (15)
clubs-admin:test:       Tests  101 passed (101)
clubs-admin:test:    Start at  17:27:20
clubs-admin:test:    Duration  7.38s (environment 39%, tests 31%, transform 17%, setup 8%, import 5%)
clubs-admin:test: 

 Tasks:    34 successful, 34 total
Cached:    28 cached, 34 total
  Time:    9.573s 
```
The clubs test lines of the same run (log lines 1143–1144): `clubs:test: Test Files 6 passed (6)` / `clubs:test: Tests 98 passed (98)`.

**3.** `pnpm i18n:check` → **exit 0** (`03-i18n-check.log`, complete):
```
> agilityhub-core-web@0.0.0 i18n:check /Users/jordib/dev/agilityhub/agilityhub-core-web
> node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs

i18n check passed
ca: 2023 keys
es: 2023 keys
en: 2023 keys
1464 statically used keys, 0 unused
250 codes, 0 missing
```

**4.** `pnpm e2e:core E3-W06` → **exit 1**, final run `04-e2e-core.log`. Stage E1/E2: `10 passed`. Stage E3: `3 passed`, `1 failed`: `T-04-34 signup.enabled=false…`, the stale core config (`signup-closed-config-core.json` from this run: `{"cacheControl": "no-cache, no-store, max-age=0, must-revalidate", "closedText": null, "enabled": true, "status": 200}`). The N-37 mongo check of the script did not run, because the script stops at the failed stage. Last 40 lines:
```
    Locator: getByText('Les inscripcions estan tancades temporalment. Torna-ho a provar més endavant o posa\'t en contacte amb el club.')
    Expected: visible
    Timeout: 15000ms
    Error: element(s) not found

    Call log:
      - Expect "toBeVisible" getByText('Les inscripcions estan tancades temporalment. Torna-ho a provar més endavant o posa\'t en contacte amb el club.') with timeout 15000ms
      - waiting for getByText('Les inscripcions estan tancades temporalment. Torna-ho a provar més endavant o posa\'t en contacte amb el club.')


      634 |       "Les inscripcions estan tancades temporalment. Torna-ho a provar més endavant o posa't en contacte amb el club.",
      635 |     ),
    > 636 |   ).toBeVisible();
          |     ^
      637 |   await expect(page.getByRole("button", { name: "CONTINUA" })).toHaveCount(0);
      638 |   await screenshot(page, "16-signup-closed-core-375.png");
      639 |   await publicContext.close();
        at /work/e2e/core/e3-signup.spec.ts:636:5

    attachment #1: oauth-token-calls (text/plain) ──────────────────────────────────────────────────
    2026-09-24T15:45:45.990Z grant_type=refresh_token status=400 cookie=none test="T-04-34 signup.enabled=false shows only the configured closed text" body={"code":"REFRESH_EXPIRED"}
    ────────────────────────────────────────────────────────────────────────────────────────────────

    Error Context: test-results/core-e1/e3-signup-T-04-34-signup-e-f5335--the-configured-closed-text/error-context.md

    attachment #3: trace (application/zip) ─────────────────────────────────────────────────────────
    test-results/core-e1/e3-signup-T-04-34-signup-e-f5335--the-configured-closed-text/trace.zip
    Usage:

        pnpm exec playwright show-trace test-results/core-e1/e3-signup-T-04-34-signup-e-f5335--the-configured-closed-text/trace.zip

    ────────────────────────────────────────────────────────────────────────────────────────────────


  1 failed
    e2e/core/e3-signup.spec.ts:603:5 › T-04-34 signup.enabled=false shows only the configured closed text 
  3 passed (30.0s)

 ELIFECYCLE  Command failed with exit code 1.
```

**5.** `pnpm e2e:docker` (the complete `pnpm e2e` in the Playwright image, `scripts/e2e-docker.sh`) → **exit 0**. Full log: `05-e2e-docker.log`. Last 40 lines:
```
clubs-admin:build: [INEFFECTIVE_DYNAMIC_IMPORT] ../../packages/i18n/src/locales/es/shell.json is dynamically imported by ../../packages/i18n/src/resources.ts but also statically imported by ../../packages/i18n/src/catalog.ts, dynamic import will not move module into another chunk.
clubs-admin:build: 
clubs-admin:build: ✓ built in 242ms
id:e2e: cache bypass, force executing abd45939afefb42b
id:e2e: 
id:e2e: > id@0.0.0 e2e /work/apps/id
id:e2e: > playwright test
id:e2e: 
id:e2e: 
id:e2e: Running 1 test using 1 worker
id:e2e: ·
id:e2e:   1 passed (3.4s)
clubs:e2e: cache bypass, force executing d8e3ffc00de9d6d9
clubs:e2e: 
clubs:e2e: > clubs@0.0.0 e2e /work/apps/clubs
clubs:e2e: > playwright test
clubs:e2e: 
clubs:e2e: [WebServer] (node:892) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs:e2e: 
clubs:e2e: Running 25 tests using 6 workers
clubs:e2e: ·························
clubs:e2e:   25 passed (11.9s)
clubs-admin:e2e: cache bypass, force executing d7cbda041a0ae23b
clubs-admin:e2e: 
clubs-admin:e2e: > clubs-admin@0.0.0 e2e /work/apps/clubs-admin
clubs-admin:e2e: > playwright test
clubs-admin:e2e: 
clubs-admin:e2e: [WebServer] (node:2176) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs-admin:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs-admin:e2e: 
clubs-admin:e2e: Running 29 tests using 9 workers
clubs-admin:e2e: ·····························
clubs-admin:e2e:   29 passed (17.6s)

 Tasks:    8 successful, 8 total
Cached:    0 cached, 8 total
  Time:    44.699s
```

Extra checks on the edited real-core spec: `pnpm exec tsc -p e2e/core/tsconfig.json --noEmit` → exit 0; `pnpm exec eslint e2e/core/e3-signup.spec.ts apps/clubs/e2e/signup.spec.ts --max-warnings 0` → exit 0.

### Not run / notes
- **Denied commands** (the allowed alternative was used each time; none was blocking):
  - `cd …/locales/ca && grep … errors.json; ls; cat …/i18n-check.mjs` (compound command), and `cat …json; for c in …; do grep …; done` («simple_expansion») → used `Read` / a single `grep`;
  - `cd <root> && mkdir … && cp … && git show HEAD:apps/clubs/src/SignupPage.tsx > …` («cd before a version-control command») → the same steps as separate commands without `cd`;
  - `cd <root> && pnpm turbo run … > log` («cd with output redirection») → a plain `cd` first, then the command;
  - `pnpm i18n:check | tail; echo ${pipestatus[1]}` («expansion») → redirected to a log file.
- `pnpm -C <root> turbo …` is not a denial: pnpm answered `EACCES` (it tried to execute the directory).
- `apps/clubs/e2e/signup.spec.ts` (MSW) still writes its screenshots to `roadmap/evidence/E3-W01/`. `e2e-docker.sh` copies back only the `in_progress` task's evidence, so no E3-W01 file changed.
- `test-results/core-e1/*` (the Playwright traces of the real-core failures) stay inside the container and are not copied back. The JSON diagnostic replaces them for the closed-text failure.

### Round 2 (2026-09-24, claude-opus-5-5)

#### Status
- The 7 points of the organizer's list are fixed, each with a test. 8 of the 10 new or changed signup Vitest fail on the pre-fix page (log `06`). The other 2 pass there on purpose: the same-step `INVALID_IBAN` case and a regression guard. Both new mock tests fail on the pre-fix handlers (log `07`).
- All four Verification commands are green on their first run, so there is no failed attempt this round:
  - `turbo lint typecheck test build`: exit 0;
  - `i18n:check`: exit 0;
  - **`pnpm e2e:core E3-W06`: exit 0.** E1/E2 passed 10/10 and E3 passed 4/4, including the new passport-only applicant and the closed-signup test;
  - the complete `pnpm e2e` in Docker: exit 0 (clubs-admin 34, id 1, clubs 27).
- **#8** (the overwritten turbo log of round 1) was accepted by the organizer as an exception. This round keeps one log per run.

#### Files changed (round 2)
- `apps/clubs/src/SignupPage.tsx`:
  - `PendingError` in the draft, the `usePendingError` hook, and `routeApiError` now returns `{code, field?, fields?, retryAfter?, step}`;
  - `updateDraft` (a ref kept in sync) and `navigateTo` write the draft before every navigation;
  - 19 is read-only once the signup is committed, and every step path shows 19;
  - `payloadDigest` / `canonicalJson`, and `withPaymentType` (the SEPA-only fields leave the draft);
  - `holderFromGroup` / `withoutGroupHolder`;
  - the club's `today` goes to 16 and 17 (`clubToday` from `./today/useDayGrid`);
  - `readDraft` drops the plain fingerprints of older builds.
- `apps/clubs/src/SignupPage.test.tsx`: `productionNavigator` option and `stubFullPageLoads`; 9 new tests in `E3-W06 round 2`; the existing cancelled-checkout test now presses «PAGA ARA».
- `packages/api-client/src/mocks/handlers.ts`: `normalisedIdentityDocument` replaces `validSpanishDocument`. It follows the country profile, and the normalised (padded) value is the identity used for `SIGNUP_ALREADY_PENDING`.
- `packages/api-client/src/mocks/scenarios.ts`: the new `signupGeneric` scenario (GENERIC profile).
- `packages/api-client/src/mocks/signup-handlers.test.ts` (new).
- `packages/i18n/src/locales/{ca,es,en}/signup.json`: `payment.payNow`.
- `e2e/core/e3-signup.spec.ts`: a passport-only applicant. `fillPerson` gets `passport`, and `completePublicSignup` gets explicit `chip` / `phone`.
- `CHANGELOG.md` (Unreleased, the E3-W06 entry: «Round 2»).
- `roadmap/evidence/E3-W06/06…11-*.log`, plus the real-core captures re-shot by `e2e:core`.

#### Point → fix → test
All tests are in `apps/clubs/src/SignupPage.test.tsx › E3-W06 round 2` unless noted.

| Point | Fix | Test(s) (pre-fix result, log `06`/`07`) |
|---|---|---|
| 1 · the routed error survives the navigation | The error is kept in the draft as `{code, field?, fields?, retryAfter?, step}`. `handleApiError` stores it with `updateDraft`, and `navigateTo` writes the draft to `sessionStorage` synchronously before calling the navigator. The owning step's `usePendingError` shows it on mount: the field message, `aria-describedby`, focus after render. Then it removes it from the draft. The tests run with **no `onNavigate`**, so the page uses its production navigator `window.location.assign`, the same one `App.tsx` uses. `stubFullPageLoads` stubs `location` so that `assign` unmounts the page and mounts a fresh one at the new URL; only `sessionStorage` survives. | `#1 an INVALID_ID_DOCUMENT routed to 16 survives the full page load and focuses the document` (loads `["/apuntat-hi"]`, «DNI / NIE» focused with «El document d'identitat no és vàlid.», consumed from the draft): **fails pre-fix**. `#1 an INVALID_IBAN from 19 stays on 19, on the IBAN field (production navigator)` (no load; the IBAN is focused with its message): passes pre-fix too, because the step does not change. It now covers the draft path. |
| 2 · only the checkout after the signup exists (R-04-26) | Once `submission.memberId` is set:<br>• the method buttons, the start radios and both consents are disabled, and their handlers also refuse changes;<br>• IBAN / holder / tax ID are `readOnly`;<br>• the button reads «PAGA ARA» and asks only for `POST /checkout-sessions`;<br>• a config reload does not touch the draft;<br>• every step path (16/17/18) renders 19, and the errors stay on 19. | `#2 after a cancelled checkout 19 is read-only and the retry charges the committed signup`: the controls are disabled or read-only; clicks on «Efectiu» and the other start option change nothing in the draft; «PAGA ARA» → 1 `POST /signup` in total, and the checkout for `member-signup-357` / `mock-signup-token`; `/apuntat-hi` shows «PAGA ARA», not 16. **Fails pre-fix**, as does the updated `M1 › a cancelled checkout…`. |
| 3 · no personal data in the fingerprint | `fingerprint = SHA-256(canonicalJson(body))` via `crypto.subtle.digest`, in lowercase hex (sorted keys, `undefined` dropped). `withPaymentType` deletes `iban` / `holderTaxId` on a method switch **and** in the config-reload merge. `readDraft` drops a fingerprint that is not a 64-hex digest (the drafts of the previous build). | `#3 the fingerprint is a SHA-256 digest and the IBAN leaves the draft after a failed submission`: IBAN → network failure → the fingerprint matches `/^[0-9a-f]{64}$/` → «Efectiu» → the raw stored draft contains neither the IBAN nor its BBAN. `#3 a config reload that drops SEPA removes the IBAN from the draft`. Both **fail pre-fix**. |
| 4 · the holder after a cleared FOUND claim (R-04-10) | `draft.holderFromGroup` is set when FOUND (or the 19 prefill) uses the group holder. Any claim edit or clear goes through `withoutGroupHolder`: a group-prefilled holder goes back to `applicantName(person)`. Typing in «Titular del compte» sets the flag to false. | `#4 a cleared FOUND claim gives the account holder back to the applicant`: FOUND «Marta Roca» → clear both → CONTINUA → 19 holder «Nora Soler Pons». **Fails pre-fix.** `#4 a holder typed by the applicant is kept when the claim changes` (a regression guard; it passes pre-fix too). |
| 5 · the club's today | `today = config.upfront?.today ?? clubToday(branding.timeZone)`, passed to 16 (birth date < today) and 17 (`birthMonth` ≤ the club's month). | `#5 the birth date is checked against the club's today, not the browser's`: the browser is on 18/08/2026 and the club on 2026-08-17. 17/08/2026 is rejected and no identity check is sent; 16/08/2026 passes. `#5 the dog's birth month…`: the browser is on 01/09/2026 and the club on 2026-08-31. 09/2026 is rejected and 08/2026 continues. Both **fail pre-fix**. |
| 6 · passport-only on the real core | The third applicant: «Joana Passaport E3», DNI/NIE empty, passport `pa1234567`. | `e2e/core/e3-signup.spec.ts` (inside the first T-04-34 test, after the NIE applicant): the body carries `{type: PASSPORT, value: PA1234567}`, and the page reaches «Sol·licitud enviada». Log `10`, E3 stage 4/4 passed. |
| 7 · the mocks follow R-04-01 | `normalisedIdentityDocument` works per `countryProfile`:<br>• ES: a 7-digit DNI gets its leading zero; DNI/NIE control letter; passport 5–20;<br>• GENERIC: `PASSPORT` / `OTHER` with ≥ 4 characters after normalisation.<br>It serves both `/signup/identity-checks` and `POST /signup`. | `packages/api-client/src/mocks/signup-handlers.test.ts`, `T-04-01 / T-04-02 …`:<br>• ES `1234567L` → `NEW` + `201`, and `01234567L` then gives `SIGNUP_ALREADY_PENDING` (same identity); `12345678A` → 400;<br>• GENERIC `OTHER` «AB-12» → `NEW` + `201`, «AB» → 400 `INVALID_ID_DOCUMENT`, DNI → 400.<br>Both **fail pre-fix** (log `07`). |

#### Assumptions (round 2)
1. **The production navigator in Vitest.** jsdom cannot navigate, and `window.location.assign` cannot be spied on: it is non-configurable, and `vi.spyOn` throws «Cannot redefine property». `vi.stubGlobal("location", …)` works, so the test replaces `location` with an object whose `assign` behaves as a full load: it unmounts the page and mounts a new one from `sessionStorage`. The page itself runs with no `onNavigate` override.
2. **`{code, field, step}`** also carries `fields` (for `details.fieldErrors`) and `retryAfter` (for `RATE_LIMITED`), because both need to survive the load. Messages are resolved from the codes when the error is consumed, so they follow the language of the new page.
3. **The committed 19 covers every step path.** After the signup exists, `/apuntat-hi`, `/gos` and `/familia` also render 19, because edits there would be ignored in the same way (#2). The draft is still deleted only on the success page, as in round 1.
4. **SEPA-only fields** are `iban` and `holderTaxId`. `holderName` stays, because the prefill (applicant or group holder) runs only when 19 mounts.
5. **The digest and brute force.** A SHA-256 of a payload that contains an IBAN is not a keyed hash. Someone who can read the session could in theory test candidate IBANs against it. A key held only in memory would not survive the full page loads that the retry has to cover. I applied the digest the organizer specified.
6. **`today` fallback.** When the configuration has no `upfront` (no BILLING), «today» is the club's date in `branding.timeZone`, computed with the same `clubToday` as screen 10.

#### Questions / catalog proposals (round 2)
- **Proposed literal** (no mockup; the committed 19 is not in the mockups): `signup:payment.payNow` = «PAGA ARA» / «PAGA AHORA» / «PAY NOW».
- **For the api (closed signup, repeated):** the closed-signup test passed this time with core image `d9e6483` (created 2026-09-24T17:04Z). The page showed the core's own `closedText` («Les inscripcions estan tancades temporalment…», which is not one of our keys). However, the first `GET /signup` that the diagnostic captured still answered `{"enabled": true, "closedText": null}` with `no-store` (`signup-closed-config-core.json`, rewritten by this run with identical content). My inference: a later `GET /signup` of the same page (the page refetches when the locale resolves) answered closed. The first answer right after the PUT therefore still looks stale.
- No new error codes, parameters, events or notifications.

#### Evidence (round 2)
**Pre-fix proof.**
- `pnpm exec vitest run src/SignupPage.test.tsx -t "E3-W06 round 2|cancelled checkout" --reporter=verbose`, run in `apps/clubs` with `git show HEAD:./src/SignupPage.tsx` temporarily in place and then restored (`cmp` identical) → **exit 1**, `Tests 8 failed | 2 passed | 52 skipped (62)`. Full log: `06-vitest-round2-before-fix-full.log`.
- The same for the mocks: `pnpm --dir ../../packages/api-client exec vitest run src/mocks/signup-handlers.test.ts` with the HEAD `handlers.ts` → **exit 1**, `Tests 2 failed (2)` (`expected { code: 'INVALID_ID_DOCUMENT', … } to deeply equal { result: 'NEW' }`). Log: `07-vitest-mocks-before-fix.log`.

**1.** `pnpm turbo run lint typecheck test build` → **exit 0**. Full log: `08-turbo-round2.log`. The test lines of the run: clubs 7 files / **118 passed**, clubs-admin 18 / 121, api-client 7 / 78, i18n 5 / 51, ui 9 / 61, id 1 / 9. Last 40 lines (ANSI stripped):
```
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > R-06-15 without WAITLIST there is no «+e» in the cell nor in the card
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > R-06-15 without FREE_TRAINING [Bloqueja pista] offers only «Bloqueig»
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > R-06-15 with levels.enabled=false the card has no «Nivells» chip
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > R-06-15 INSTRUCTOR (no /parameters) sees «Nivells» only on a class with levels
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > R-06-11 lists the RING_BLOCK_CONFLICT conflicts inside the block drawer
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > INSTRUCTOR reads the calendar without actions and with inert chips (A22 c)
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > shows the week warnings and selects the class of a warning; day headers open the day view
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test:  ✓ src/planning/CalendarPage.test.tsx (22 tests) 5651ms
clubs-admin:test:    ✓ T-06-28 D4 / D4b / D4c class calendar (front half, MSW) (22)
clubs-admin:test:      ✓ opens «Esborrany» on the first week with drafts, with the validation card and its count 1206ms
clubs-admin:test:      ✓ [VALIDAR LA SETMANA] turns every draft of the week active at once 546ms
clubs-admin:test:      ✓ cancels an active class with registrants through D4c: four rows, text required, «ANUL·LA I AVISA ELS 4 ALUMNES» 510ms
clubs-admin:test:      ✓ [ELIMINA] of an active class goes through the same D4c modal 392ms
clubs-admin:test:      ✓ R-06-09 saves the diff with the version; STALE_VERSION keeps its message after the refetch remounts the card 507ms
clubs-admin:test:      ✓ R-06-09 [Crear classe]: a new time or ring after RING_HAS_BOOKINGS drops the confirmation 315ms
clubs-admin:test: 
clubs-admin:test:  Test Files  18 passed (18)
clubs-admin:test:       Tests  121 passed (121)
clubs-admin:test:    Start at  19:25:40
clubs-admin:test:    Duration  10.66s (environment 39%, tests 29%, transform 16%, setup 8%, import 6%)
clubs-admin:test: 

 Tasks:    34 successful, 34 total
Cached:    16 cached, 34 total
  Time:    17.92s
```

**2.** `pnpm i18n:check` → **exit 0** (`09-i18n-check-round2.log`, complete):
```
> agilityhub-core-web@0.0.0 i18n:check /Users/jordib/dev/agilityhub/agilityhub-core-web
> node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs

i18n check passed
ca: 2254 keys
es: 2254 keys
en: 2254 keys
1680 statically used keys, 0 unused
250 codes, 0 missing
```

**3.** `pnpm e2e:core E3-W06` → **exit 0** (`10-e2e-core-round2.log`). Stage E1/E2: `Running 10 tests … 10 passed (21.8s)` (log lines 130–142). Stage E3 and the N-37 mongo check are below. Last 40 lines:
```
+ @types/node 24.13.3
+ eslint 9.39.5
+ eslint-import-resolver-typescript 4.4.5
+ eslint-plugin-import 2.32.0
+ eslint-plugin-jsx-a11y 6.10.2
+ eslint-plugin-react 7.37.5
+ eslint-plugin-react-hooks 7.1.1
+ globals 17.12.0
+ husky 9.1.7
+ i18next-parser 9.4.0
+ lint-staged 17.5.0
+ msw 2.15.0
+ prettier 3.9.6
+ size-limit 13.0.3
+ turbo 2.10.12
+ typescript 5.9.3
+ typescript-eslint 8.69.0

. prepare$ node packages/config/scripts/prepare-husky.mjs
. prepare: .git can't be found
. prepare: Done
Done in 4.1s using pnpm v9.15.9
[WebServer] (node:134) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)


[WebServer] (node:214) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
[WebServer] (node:312) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)


Running 4 tests using 1 worker

[1/4] e2e/core/e3-signup.spec.ts:347:5 › T-04-34 public signup is validated and enters through the N-02 welcome link
[2/4] e2e/core/e3-signup.spec.ts:627:5 › T-04-34 rejected signup delivers N-03
[3/4] e2e/core/e3-signup.spec.ts:634:5 › T-04-34 active member adds a dog and the club validates it
[4/4] e2e/core/e3-signup.spec.ts:638:5 › T-04-34 signup.enabled=false shows only the configured closed text
  4 passed (16.5s)
{"action":"OPEN_DOG","channel":"APP","code":"N-37","dogName":"Neret E3","status":"SENT"}
```
Also run on the edited spec and mock test: `pnpm exec tsc -p e2e/core/tsconfig.json --noEmit` → exit 0; `pnpm exec eslint e2e/core/e3-signup.spec.ts packages/api-client/src/mocks/signup-handlers.test.ts --max-warnings 0` → exit 0.

**4.** `pnpm e2e:docker` (the complete `pnpm e2e` in the Playwright image) → **exit 0**. Full log: `11-e2e-docker-round2.log`. Last 40 lines:
```
clubs:build: precache  54 entries (1604.43 KiB)
clubs:build: files generated
clubs:build:   dist/sw.js
clubs:build:   dist/workbox-9c191d2f.js
clubs-admin:e2e: cache bypass, force executing 10c0e185d6208f7a
clubs-admin:e2e: 
clubs-admin:e2e: > clubs-admin@0.0.0 e2e /work/apps/clubs-admin
clubs-admin:e2e: > playwright test
clubs-admin:e2e: 
clubs-admin:e2e: [WebServer] (node:661) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs-admin:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs-admin:e2e: 
clubs-admin:e2e: Running 34 tests using 9 workers
clubs-admin:e2e: ··································
clubs-admin:e2e:   34 passed (24.4s)
id:e2e: cache bypass, force executing 80748eefe290827d
id:e2e: 
id:e2e: > id@0.0.0 e2e /work/apps/id
id:e2e: > playwright test
id:e2e: 
id:e2e: 
id:e2e: Running 1 test using 1 worker
id:e2e: ·
id:e2e:   1 passed (3.2s)
clubs:e2e: cache bypass, force executing 6ffbc7549593dc36
clubs:e2e: 
clubs:e2e: > clubs@0.0.0 e2e /work/apps/clubs
clubs:e2e: > playwright test
clubs:e2e: 
clubs:e2e: [WebServer] (node:2788) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs:e2e: 
clubs:e2e: Running 27 tests using 7 workers
clubs:e2e: ···························
clubs:e2e:   27 passed (15.9s)

 Tasks:    8 successful, 8 total
Cached:    0 cached, 8 total
  Time:    56.645s
```

#### Screens (round 2)
- `pnpm e2e:core` re-shot the real-core captures in `roadmap/evidence/E3-W06/`. 16, 17, 18, 19 (monthly), «enviada», add-dog 19 and add-dog «enviada» show the same content as in round 1. The api placeholders `{twoDogsMonthlyFee}` / `{deadlineDay}` are now resolved by the current core image, and 19's start options show today's dates (24 September / 1 October).
- The only visible change of round 2, the committed 19 with «PAGA ARA», needs a Stripe checkout. The real core does not offer one in this seed (it is E8-T04), so it is covered by Vitest only and has no screenshot.

#### Not run / notes (round 2)
- **Denied commands** (the allowed alternative was used each time):
  - `git -C <root> show HEAD:… > file` and `git -C <root> show HEAD:…` → `git show HEAD:./src/SignupPage.tsx > src/SignupPage.tsx` run from `apps/clubs`;
  - `python3 - <<'EOF' … f-string …` («brace with quote character») → the `Edit` tool;
  - `rm …; git -C ../.. status` (compound) → separate commands.
- **Files I did not touch** show as modified in the working tree during this session: `docs/specs/S07-activitats.md`, `roadmap/MESSAGES.md` and `roadmap/tasks/E4-W04.md`, apparently from a concurrent organizer session. I left them as they are.

## Organizer verification
Result: changes_requested · 2026-09-24 · organizer (Claude)

The eight steps are implemented, and 46 of the new tests fail against the pre-fix page. That is the proof that was asked for. B1 works on the real core: a NIE applicant reaches «Sol·licitud enviada», with `{NIE, Y7654321G}` in the body. Turbo, `i18n:check` and the complete e2e are green. CI is green at `0b21911`. The only red real-core test is the closed form, which is the api defect of E3-T09 step 5.

The independent review `roadmap/reviews/E3-W06-20260924-1753-codex.md` found three majors, and the organizer confirmed them in the code:
- **#1:** the production navigator is a full page load (`App.tsx:1107-1112`, `window.location.assign`). The routed error lives only in React state, so it is lost on arrival. The table test hides this with `vi.fn()`.
- **#2:** after the signup is created, a cancelled checkout leaves 19 editable, but a new submission skips the payload (`submission.memberId` is set). The edits are silently ignored, and the checkout charges what was first sent.
- **#3:** `submission.fingerprint = JSON.stringify(body)` (`:1918`) keeps the whole request in the session draft, IBAN included. The draft still holds it after the method changes to cash, which step 6 forbids.

The minors are real too, and they are cheap.

Round 2 — fix exactly these points:
1. **The routed error survives the navigation (#1).** Keep the pending error in the session draft as `{code, field, step}`. The step consumes it when it mounts: it shows it on the field and focuses the field. Test through the production navigator (a full load), not `vi.fn()`: an `INVALID_IBAN` from 19 and an `INVALID_ID_DOCUMENT` routed to 16.
2. **After the signup exists, only the checkout can be retried (#2; R-04-26).** Once `memberId` is set, 19 shows the committed choices (start option, method, consents) read-only, with only [Paga ara] / retry. Test: a cancelled checkout → the fields cannot change, and the retry charges the committed submission.
3. **No personal data in the fingerprint (#3).** The fingerprint is a SHA-256 digest (Web Crypto) of the canonical payload, never the payload itself. The SEPA-only fields leave the draft when the method changes, including after a failed submission and when a config reload changes the method. Test: IBAN → failed submission → switch to cash → the stored draft has no IBAN anywhere.
4. **The account holder after a cleared FOUND claim (#4, R-04-10).** Track whether the holder was prefilled from the group. When the claim is cleared or no longer FOUND, a prefilled holder goes back to the applicant. Test: FOUND → clear both fields → continue.
5. **The club's today in the local checks (#5).** The birth date and `birthMonth` are checked against the club's date (`upfront.today` of the configuration), not the browser's. Test with a browser date one day off the club's.
6. **A passport-only applicant on the real core (#6).** The DoD asks for NIE and passport-only in the real-core e2e. Add the passport-only flow: the body carries `{PASSPORT, …}`, and the applicant reaches «Sol·licitud enviada».
7. **The mocks follow R-04-01 (#7).** Pad a 7-digit DNI (`1234567L` → `01234567L`), and choose the validation by country profile (GENERIC accepts `OTHER` ≥ 4). Add success assertions for both.

**#8, evidence.** The first failed turbo log was overwritten and cannot be recovered. This is accepted as an exception this once: from now on, one log per failed attempt.

### Round 2 verification
Result: verified · 2026-09-24 · organizer (Claude)

The seven points are fixed, each with a test. 8 of the 10 new or changed tests fail on the pre-fix page, and the other two are guards on purpose. Every verification command is green at its first run. The real-core run is 10/10 + 4/4, with the new passport-only applicant and the closed-signup test. CI is green at `bbd15dc`.

The independent review `roadmap/reviews/E3-W06-20260924-1936-codex.md` found three narrower cases, reproduced with the component in memory. They are not worth a third round, so they move to **E3-W08 step 7**, before the gate re-run:
- a race in the full-page navigation, where the departing page consumes the routed error;
- a reload of `?cs=cancel` that drops the key of a later retry;
- the passport-only error shown on the DNI field.
