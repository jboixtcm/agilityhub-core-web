---
id: E3-W10
title: Test stability — a build-time budget for the MSW mock worlds, the flaky D3b URL test, and a lighter e2e container copy; no product change
stage: E3
repo: agilityhub-core-web
thread: B
order: 65
status: awaiting_verification
depends_on: []
branch:
pr:
updated: 2026-09-24
---

# E3-W10 · Test stability

## Objective
On the evening of 24-09, `pnpm e2e:docker` failed five runs out of five (E4-W02 round 4 and E4-W04 round 2), always in the same way:
- in clubs-admin, the first 18 of 34 tests timed out right after the login, and the dot line was `TTTTTTTTTTTTTTTTTT················`;
- runs took 2–5 minutes, and one had «Target crashed».

The E4-W04 round-2 executor found the likely cause:
- E4-W02 round 4 made the mock helper `clubInstant` (`packages/api-client/src/mocks/fixtures/calendar.ts`) sample about 55 offsets per call, each with a new `Intl.DateTimeFormat`;
- every browser page therefore built thousands of formatters when MSW started (the calendar world took 171 ms in Node);
- a per-time-zone cache (`d95b199`) brought it to 38 ms, and the complete suite passed: clubs-admin 34/34 in 22.7 s, clubs 27/27 in 16.6 s.

Two more problems showed up the same evening:
- CI went red at `d95b199` on a flaky Vitest of E4-W01;
- the e2e container copy carried about 790 MB of files it does not need.

This task makes those regressions visible and removes the noise.

**Change no product code.** Test and script changes only.

## Context to load
- `packages/api-client/src/mocks/` (the handlers, the stateful fixtures and their reset).
- `apps/clubs-admin/src/planning/TemplateDayPage.test.tsx` and `TemplateDayPage.tsx` (lines 89–96).
- `scripts/e2e-docker.sh`.
- The failures:
  - `roadmap/evidence/E4-W04/18-e2e-docker-round2-run2.log`;
  - the green run `20-e2e-docker-round2-run4.log`;
  - the CI log of `d95b199` (in `.roadmap-ci-fail.log`, or `gh run view 36043032611 --log-failed`).

## Steps
1. **A build-time budget for the mock worlds.** Add a Vitest that builds every stateful mock world, running the same code the browser runs when MSW starts (the handlers' initial fixtures and their reset), and times each one.
   - Measure first, then set the budget at about three times the measured value (for example, 150 ms per world in Node).
   - Show in the report that the test fails with the uncached `clubInstant`: put the pre-`d95b199` helper back temporarily, run the test, and restore it.
   - If E4-W02 is not verified when you run this task, do not touch `apps/clubs-admin/src/planning/calendar-shared.tsx` (it is E4-W02's code).
2. **The flaky D3b test.** `TemplateDayPage.test.tsx`, «rewrites an unknown day in the URL to the template's first day», failed on CI at `d95b199` and passed at `84437c3`. The test asserts `window.location.pathname` right after `findByRole`, before the passive effect that rewrites the URL has run.
   - Keep what the test asserts; only wait for it (`waitFor`).
   - Run the file 20 times in a row, green, and paste the summary.
3. **A lighter copy of the checkout.** `scripts/e2e-docker.sh` tars the whole checkout into the container. On 24-09 that was about 850 MB:
   - about 400 MB was the executor log `.roadmap-executor.log` (the kit now archives it outside the repo once it passes 50 MB);
   - about 390 MB was a stray `.pnpm-store/` from 06-09 that no config uses.
   
   Exclude `.roadmap-*` and `.pnpm-store` from the tar, and print the size of what is copied. Keep the evidence copy-back as it is.
4. **Evidence.** Run the complete `pnpm e2e:docker` twice in a row, through the host lock. For both runs, paste each app's dot line, totals and duration. None may contain a `T`.

## Fixed conventions
- No product code change. Do not add retries or longer test timeouts.
- Heavy runs go through the host lock (step 4a of the task prompt).
- Evidence goes under `roadmap/evidence/E3-W10/`: one log per run, failed runs included.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E3-W10/*.log`)
- `pnpm turbo run lint typecheck test build`.
- `pnpm e2e:docker`, twice.

## Definition of Done
- The budget test exists, and it fails with the uncached helper.
- The D3b test is stable across 20 runs.
- The container copy excludes the executor logs and the stray store, and prints its size.
- Two consecutive complete `pnpm e2e:docker` runs are green.

## Evidence to return
- The command outputs.
- The pre-fix proof of step 1.
- The dot lines of both e2e runs.

---

## Executor report

**Session 24-09 (claude-opus-5-5).** Every step is done, and there is no product change: only a new test, one test line, and the e2e script. The three Verification runs are green on their first real attempt: turbo, and the complete Docker e2e twice. No dot line contains a `T`.

### Summary per step
1. **Build-time budget.** `packages/api-client/src/mocks/mock-worlds.budget.test.ts` (13 tests):
   - It finds every `reset…State` that `handlers.ts` exports, by name, so a new world gets timed too. It adds `resetDayGridState`, which the planning reset calls.
   - Each world is built five times, and the fastest build must stay under its budget. The budget is 50 ms for `resetPlanningState` and 5 ms for every other world.
   - A third check proves that the timed code is the start-up code: the six exported worlds, captured at import before any reset, are `toEqual` to the worlds after a reset.
   - **Measured** (`01`, every budget set to 0 on purpose so that each failure prints its time): planning 13.8 ms; the day grid 0.44 ms; settings 0.08 ms; the others ≤ 0.05 ms. Earlier runs gave 15.4–17.3 ms for planning. The budget of 50 ms is about 3 × 16 ms. The small worlds get a 5 ms floor, because three times 0.05 ms is timer noise.
   - **Pre-fix proof** (`02`): I put the pre-`d95b199` inline `new Intl.DateTimeFormat(…)` back in `zoneOffsetMinutes`; the log starts with that diff. The test fails with `resetPlanningState: 141.72 ms (budget 50 ms)`, and the other 12 tests pass. I then restored the helper, and `git status` shows `fixtures/calendar.ts` unmodified. The green run is `03`, and the final one `10`.
   - I did not use the task's 150 ms example. The uncached helper measured 142–187 ms here, so a 150 ms budget would not fail reliably.
2. **Flaky D3b test.** In `TemplateDayPage.test.tsx`, «rewrites an unknown day…» now waits for the pathname with `waitFor`, then checks the search. The assertions are unchanged.
   - **Cause:** the URL rewrite is a passive effect.
     - When the table's commit takes longer than the scheduler's 5 ms frame, React runs the passive effects in a later `setImmediate` task.
     - On a loaded host, the `setTimeout(0)` drain of Testing Library's async wrapper is already due by then, so the test resumes first.
   - **Reproduction** (`04`, scratch source in `04-d3b-race-scratch.test.tsx.txt`, deleted from `src/` afterwards). It simulates a loaded host in two ways: `performance.now` skews by 10 ms per call, so the scheduler yields after every task, and there is 5 ms of busy work after the table mutation.
     - The old assertion fails with the exact CI message: `expected '/plantilles/template-setmana-a/dia/so…' to be '/plantilles/template-setmana-a/dia/mo…'`.
     - The `waitFor` version passes under the same conditions.
     - Without the simulation, the old assertion passed locally, even with the template answer delayed until after the catalogs, so the flake never showed up here on its own.
   - **20 runs in a row** (`05`): 20 × `Tests  7 passed (7)`, and every run `exit 0`.
3. **Lighter container copy.** `scripts/e2e-docker.sh` excludes `.roadmap-*` and `.pnpm-store` from the tar and prints `Checkout copied into the container: 67M` (both runs). On this host `.pnpm-store` is 380 MB, and the `.roadmap-*` files are small now because the kit archives the log. The evidence copy-back is unchanged.
4. **Evidence:** two complete `pnpm e2e:docker` runs in a row through the host lock (`08`, `09`), both green. The dot lines are below.

### Evidence

**1. `pnpm --dir /Users/jordib/dev/agilityhub/agilityhub-core-web exec turbo run lint typecheck test build --force > roadmap/evidence/E3-W10/07-turbo.log 2>&1` → exit 0.** This is the Verification command plus `--force`, so nothing came from cache. Full log: `roadmap/evidence/E3-W10/07-turbo.log`. The earlier attempt `06-turbo-invocation-error.log` never ran: I had used `pnpm -C <dir> turbo …`, and pnpm tried to spawn the directory itself (EACCES).
In the log: `@agilityhub/api-client:test:  ✓ src/mocks/mock-worlds.budget.test.ts (13 tests) 347ms` and `clubs-admin:test:  ✓ src/planning/TemplateDayPage.test.tsx (7 tests) 1628ms`. After this run I changed only a comment of the budget test. `10-api-client-lint-and-budget-final.log` has the final ESLint run of `@agilityhub/api-client` (clean) and the budget file run: 13/13, exit 0. Last 40 lines:
```
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > R-06-15 with levels.enabled=false the card has no «Nivells» chip
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > R-06-15 INSTRUCTOR (no /parameters) sees «Nivells» only on a class with levels
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > R-06-11 lists the RING_BLOCK_CONFLICT conflicts inside the block drawer
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > INSTRUCTOR reads the calendar without actions and with inert chips (A22 c)
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > shows the week warnings and selects the class of a warning; day headers open the day view
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test:  ✓ src/planning/CalendarPage.test.tsx (24 tests) 7298ms
clubs-admin:test:    ✓ T-06-28 D4 / D4b / D4c class calendar (front half, MSW) (24)
clubs-admin:test:      ✓ opens «Esborrany» on the first week with drafts, with the validation card and its count 1362ms
clubs-admin:test:      ✓ [VALIDAR LA SETMANA] turns every draft of the week active at once 588ms
clubs-admin:test:      ✓ cancels an active class with registrants through D4c: four rows, text required, «ANUL·LA I AVISA ELS 4 ALUMNES» 520ms
clubs-admin:test:      ✓ [ELIMINA] of an active class goes through the same D4c modal 404ms
clubs-admin:test:      ✓ R-06-09 saves the diff with the version; STALE_VERSION keeps its message after the refetch remounts the card 526ms
clubs-admin:test:      ✓ R-06-05 moving a class onto training bookings asks first and resends with cancelBookings 328ms
clubs-admin:test:      ✓ R-06-11 a new ring after RING_HAS_BOOKINGS drops the confirmation: no cancelBookings for a ring not shown 317ms
clubs-admin:test:      ✓ R-06-09 [Crear classe]: a new time or ring after RING_HAS_BOOKINGS drops the confirmation 369ms
clubs-admin:test:      ✓ R-06-09 [Crear classe]: a RING_HAS_BOOKINGS answer that arrives after a ring change is dropped 343ms
clubs-admin:test:      ✓ R-06-11 the block drawer locks ring, date and times until a delayed RING_HAS_BOOKINGS arrives 315ms
clubs-admin:test: 
clubs-admin:test:  Test Files  18 passed (18)
clubs-admin:test:       Tests  134 passed (134)
clubs-admin:test:    Start at  21:15:45
clubs-admin:test:    Duration  11.93s (tests 34%, environment 33%, transform 17%, setup 8%, import 7%)
clubs-admin:test: 

 Tasks:    34 successful, 34 total
Cached:    0 cached, 34 total
  Time:    28.411s 
```

**2. `/Users/jordib/Dropbox/Documents/SOFTWARE_CANIC/05-desenvolupament/roadmap-kit/mac/heavy.sh pnpm e2e:docker E3-W10 > roadmap/evidence/E3-W10/08-e2e-docker-run1.log 2>&1` → exit 0** (run 1; the lock wrote nothing to the log). Full log `08-e2e-docker-run1.log`; line 7: `Checkout copied into the container: 67M`.

| App | Dot line | Totals | Duration |
|---|---|---|---|
| clubs-admin | `··································` | 34 passed (9 workers) | 23.3 s |
| clubs | `···························` | 27 passed (7 workers) | 15.3 s |
| id | `·` | 1 passed | 3.3 s |

Turbo `8 successful, 8 total`, `0 cached`, 55.5 s. Last 40 lines:
```
id:build: - Using dynamic import() to code-split the application
id:build: - Use build.rolldownOptions.output.codeSplitting to improve chunking: https://rolldown.rs/reference/OutputOptions.codeSplitting
id:build: - Adjust chunk size limit for this warning via build.chunkSizeWarningLimit.
id:build: ✓ built in 172ms
clubs-admin:e2e: cache bypass, force executing a6b46b89d3311a4e
clubs-admin:e2e: 
clubs-admin:e2e: > clubs-admin@0.0.0 e2e /work/apps/clubs-admin
clubs-admin:e2e: > playwright test
clubs-admin:e2e: 
clubs-admin:e2e: [WebServer] (node:664) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs-admin:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs-admin:e2e: 
clubs-admin:e2e: Running 34 tests using 9 workers
clubs-admin:e2e: ··································
clubs-admin:e2e:   34 passed (23.3s)
clubs:e2e: cache bypass, force executing 6c5c85e8b4c8b28d
clubs:e2e: 
clubs:e2e: > clubs@0.0.0 e2e /work/apps/clubs
clubs:e2e: > playwright test
clubs:e2e: 
clubs:e2e: [WebServer] (node:2586) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs:e2e: 
clubs:e2e: Running 27 tests using 7 workers
clubs:e2e: ···························
clubs:e2e:   27 passed (15.3s)
id:e2e: cache bypass, force executing 506e5c473ca9fd2d
id:e2e: 
id:e2e: > id@0.0.0 e2e /work/apps/id
id:e2e: > playwright test
id:e2e: 
id:e2e: 
id:e2e: Running 1 test using 1 worker
id:e2e: ·
id:e2e:   1 passed (3.3s)

 Tasks:    8 successful, 8 total
Cached:    0 cached, 8 total
  Time:    55.476s 
```

**3. `/Users/jordib/Dropbox/Documents/SOFTWARE_CANIC/05-desenvolupament/roadmap-kit/mac/heavy.sh pnpm e2e:docker E3-W10 > roadmap/evidence/E3-W10/09-e2e-docker-run2.log 2>&1` → exit 0** (run 2, straight after run 1). Full log `09-e2e-docker-run2.log`; line 7: `Checkout copied into the container: 67M`.

| App | Dot line | Totals | Duration |
|---|---|---|---|
| clubs-admin | `··································` | 34 passed (9 workers) | 53.5 s |
| clubs | `···························` | 27 passed (7 workers) | 22.4 s |
| id | `·` | 1 passed | 3.4 s |

Turbo `8 successful, 8 total`, `0 cached`, 1 m 33.7 s. I cannot say why clubs-admin took longer than in run 1. There was still no `T`. Last 40 lines:
```
clubs:build: precache  54 entries (1607.02 KiB)
clubs:build: files generated
clubs:build:   dist/sw.js
clubs:build:   dist/workbox-9c191d2f.js
id:e2e: cache bypass, force executing 506e5c473ca9fd2d
id:e2e: 
id:e2e: > id@0.0.0 e2e /work/apps/id
id:e2e: > playwright test
id:e2e: 
id:e2e: 
id:e2e: Running 1 test using 1 worker
id:e2e: ·
id:e2e:   1 passed (3.4s)
clubs-admin:e2e: cache bypass, force executing a6b46b89d3311a4e
clubs-admin:e2e: 
clubs-admin:e2e: > clubs-admin@0.0.0 e2e /work/apps/clubs-admin
clubs-admin:e2e: > playwright test
clubs-admin:e2e: 
clubs-admin:e2e: [WebServer] (node:891) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs-admin:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs-admin:e2e: 
clubs-admin:e2e: Running 34 tests using 9 workers
clubs-admin:e2e: ··································
clubs-admin:e2e:   34 passed (53.5s)
clubs:e2e: cache bypass, force executing 6c5c85e8b4c8b28d
clubs:e2e: 
clubs:e2e: > clubs@0.0.0 e2e /work/apps/clubs
clubs:e2e: > playwright test
clubs:e2e: 
clubs:e2e: [WebServer] (node:2821) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs:e2e: 
clubs:e2e: Running 27 tests using 7 workers
clubs:e2e: ···························
clubs:e2e:   27 passed (22.4s)

 Tasks:    8 successful, 8 total
Cached:    0 cached, 8 total
  Time:    1m33.738s 
```

**Pre-fix proof of step 1: `pnpm --filter @agilityhub/api-client exec vitest run src/mocks/mock-worlds.budget.test.ts` with the uncached helper → exit 1** (`02-budget-prefix-uncached.log`, which starts with the temporary diff):
```
 ❯ src/mocks/mock-worlds.budget.test.ts (13 tests | 1 failed) 952ms
     × builds resetPlanningState within its budget 763ms
 FAIL  src/mocks/mock-worlds.budget.test.ts > E3-W10 build-time budget of the MSW mock worlds > builds resetPlanningState within its budget
AssertionError: resetPlanningState: 141.72 ms (budget 50 ms): expected 141.72020900000007 to be less than 50
 Test Files  1 failed (1)
      Tests  1 failed | 12 passed (13)
```
With the cache (`03`, `10`): 13/13 passed, exit 0. Between `02` and `10`, the only changes to the test file were the world-list check (`toEqual` of the sorted list → `arrayContaining`) and a comment.

**D3b, 20 runs in a row** (`05-d3b-20-runs.log`; `pnpm exec vitest run src/planning/TemplateDayPage.test.tsx`, run 20 times from `apps/clubs-admin`): every run printed `Tests  7 passed (7)` and `run N exit 0`, for N = 1…20.

### Files changed
- `packages/api-client/src/mocks/mock-worlds.budget.test.ts` (new).
- `apps/clubs-admin/src/planning/TemplateDayPage.test.tsx`: `waitFor` on the URL rewrite.
- `scripts/e2e-docker.sh`: the `.roadmap-*` and `.pnpm-store` excludes, and the size line.
- `CHANGELOG.md` (Unreleased → Changed).
- `roadmap/tasks/E3-W10.md` (this report), `roadmap/STATUS.md` (rendered by `check.py`), and `roadmap/evidence/E3-W10/*`.
- **Not mine:** during the session the working tree also changed in `docs/…`, `roadmap/MESSAGES.md`, `roadmap/tasks/E3-W07.md`, `E3-W08.md`, `E4-W02.md`, `E4-W05.md`, and the new `E4-W09.md`. These look like the organizer's concurrent edits (MESSAGES has a 24-09 entry «E4-W02 verified; new E4-W09»). I did not touch them.

### Rules and tests implemented
- T-06-27 (D3b): «rewrites an unknown day in the URL to the template's first day» is stabilised with the same assertions.
- New infrastructure test: «E3-W10 build-time budget of the MSW mock worlds», with 11 worlds, parity with the start-up worlds, and discovery of new worlds.
- No `R-xx-nn` and no product code change. `calendar-shared.tsx` is untouched: E4-W02 was `awaiting_verification` when I started. `fixtures/calendar.ts` is back to HEAD.

### Assumptions
- **What a world is:** each `reset…State` exported by `handlers.ts`, plus `resetDayGridState`. The timing goes through the reset because it calls the same builders as the module-level start-up state (`initialClassSessions`, `initialRingBlocks`, …). The parity test checks that for the six exported state objects.
- **The fastest of five builds, not the first:** the budget has to guard the cost of the code, not the load of a shared CI runner. The regression (a new `Intl.DateTimeFormat` per sample) costs the same on every build, so the fastest build still shows it: 142 ms against 50 ms.
- **The D3b fix does not fail locally without the simulation:** the committed test with the old assertion never failed on this host (it is a timing race). The failure is shown with the scratch simulation (`04`), not with the committed test.

### Questions / catalog proposals
- No catalog proposal.
- **Denied here:** shell `for` loops, `$VAR` expansions and `cd … && <write>`. So the 20 D3b runs are 20 explicit commands chained with `;` in three Bash calls, and the logs use absolute paths.
- **Not in this task:** the organizer's 20:45 entry mentioned warming up the dev servers and capping CI at 4 workers. The re-scoped task file does not ask for either, so I did neither.

## Organizer verification
_(pending)_
