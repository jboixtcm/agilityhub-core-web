---
id: E3-W07
title: Gate E3 audit fixes (web, 2/3) — adopt the api snapshot; D2: the dog's version, 422 codes on their fields, both contacts and the full edit drawer, the plan selector with dryRun, the family claim, the refund warning, D1 fresh after a decision; the D2 and D1 minors
stage: E3
repo: agilityhub-core-web
thread: B
order: 70
status: verified
depends_on: [E3-W06, external:api:E3-T08]
branch:
pr:
updated: 2026-09-25
---

# E3-W07 · Gate E3 audit fixes (web, 2/3)

## Objective
The back-office half of the gate E3 audit fixes (`roadmap/reviews/gate-E3/consolidated.md`): D2 «Validació d'una preinscripció» and D1 «Tauler de control». It needs the api contract of E3-T08: `planOptions`, `warnDays` and the dog `version` in the signup view, and the `details.reason` values. The readmission view (E38, api E3-T09) moves to **E3-W08**, so this task can start as soon as E3-T08 is published.

Every step names the finding, the defect, the rule, the fix and the test. **Each fix comes with a test that fails before the fix.** Give it the spec id where one applies (T-04-33, T-14-25).

## Spec references
- S04:
  - §2, row D2;
  - R-04-03, R-04-06 (amended by **E38**), R-04-13, R-04-16, R-04-18, R-04-19, R-04-23 and R-04-24/25;
  - §10;
  - T-04-33 and T-04-34.
- S14: R-14-01 and R-14-05…07 (R-14-07 amended by **E35**); T-14-25.
- S03: R-03-27 (masking: the phone in clear).
- `CATALEG_ERRORS.md`.
- `DECISIONS_PENDENTS.md` Part E: **E35, E38 and E39**.
- Mockups `docs/pantalles/escriptori/D1…` and `D2…`.

## Context to load
- `roadmap/reviews/gate-E3/consolidated.md` (M11–M15, «Web, D2 and D1»), and the source reports.
- `apps/clubs-admin/src/dashboard/SignupReviewPage.tsx`, `DashboardPage.tsx` and their tests.
- `e2e/core/e3-signup.spec.ts`.

## Steps
0. **Adopt the staged snapshot.** This is the adoption task for the api snapshot that contains E3-T08 and E3-T09:
   - swap `openapi.next.json` in, prune `pending.json`, run `pnpm api:generate`, and fix the type errors;
   - check that it has `planOptions`, `warnDays`, the dog `version` and the per-plan quote of `GET /signup`; if any of them is missing, stop and ask in the report;
   - render the review warnings by code (`signupReview.warning.{code}`), so that the values api E3-T08 round 2 adds (`PAID_EXCEEDS_QUOTE`, ruling E39b) need only their keys;
   - add the `enums` labels of the new values `AuditAction.SIGNUP_SUBMITTED` (a readmission submitted, E38) and `AuditOrigin.PUBLIC` (the anonymous public form) in ca, es and en (api E3-T09).
1. **M12 · the dog's own version (R-04-19).**
   - The dog `PATCH` sends `signup.version`, which is the member's version (`SignupReviewPage.tsx:150-158`). After an add-dog or a member edit, every dog edit answers 409.
   - Send each dog's own `version`.
   - After a partial save (the member saved, the dog failed), reload.
   
   Vitest.
2. **M13 · 422 codes on their fields (§2 D2).** Today only `details.fieldErrors` is mapped (`:50-59`, `:329-332`), but the api answers bare codes.

   | Code | Shown on |
   |---|---|
   | `LEVEL_REQUIRED`, `LEVEL_NOT_ACTIVE` | the dog's «Nivell inicial» |
   | `NEXT_INVOICE_DATE_REQUIRED`, and a `VALIDATION_ERROR` on `nextInvoiceDate` | the date |
   | `UPFRONT_AMOUNT_EXCEEDS_DUE` | the amount; the input also gets `max` = the amount due |
   | `PLAN_NOT_AVAILABLE` | the plan selector |
   | `INVALID_STATE` with `details.reason = CHECKOUT_PENDING` (E39) | the plan card |
   | `MEMBERSHIP_EXISTS` | a banner |
   | any other code | `errors:{code}` |

   MSW answers like the api: bare codes with empty details. Vitest: one table test over the codes.
3. **M14 · both contacts and the full edit drawer (R-04-19, R-04-03).**
   - Today the drawer rewrites `contactEmails` and `phones` as one-element arrays (`:185-212`), so fixing the first email deletes the second. Edit the element and keep the others.
   - Add the missing fields:
     - gender;
     - the second email and the second phone;
     - the notes to instructors;
     - `holderTaxId`;
     - the requested plan, read-only;
     - the dog documents: add and remove, forwarding the signed upload headers.
   - In add-dog mode (an ACTIVE member), the person block is read-only and only the pending dogs can be edited (R-04-25).
   
   Vitest: editing the first email keeps the second.
4. **M15 · the plan and family decisions (§2 D2, R-04-13, T-04-33).**
   - The plan/price selector lists `planOptions` (api E3-T08) and sends `planId` and `priceId` together.
   - Each change runs `dryRun` and renders the whole response: the upfront lines, the price, `nextInvoiceDate` and the warnings.
   - A family claim that is not FOUND:
     - shows what the applicant typed (the holder's name, the dog's name);
     - offers a member search to attach the holder's group (`familyGroupId`), and an explicit «sense grup» decision.
   - A FOUND claim shows the group, with «tarifa familiar en validar».
   - A FOUND claim whose holder has no group yet validates **without** `familyGroupId`: the api creates the group (R-04-13, T-04-16). Today D2 blocks it with the generic error (`SignupReviewPage.tsx:362-371`; Codex review of E3-W04, finding 1), and `SignupReviewPage.test.tsx:109` asserts that rejection: make it assert a successful POST without the field.
   
   Vitest:
   - a change to Pack 6 → the `dryRun` lines are updated;
   - a `NOT_FOUND_PENDING` claim is resolved both ways.
5. **M11, web side · D1 fresh after a decision (R-14-01).**
   - D2 reads `warnDays` from the signup view, and never calls `/dashboard`.
   - After VALIDA or REBUTJA, navigate to `/tauler` and refetch.
   - A member with nothing pending (`409 INVALID_STATE`, `details.reason = NOT_PENDING`) shows «Aquesta preinscripció ja s'ha resolt», with a link to D10. Add the key in ca, es and en.
   - Remove the `waitForTimeout(1_000)` and the `/abonats` detour from `e3-signup.spec.ts:400-401`. The e2e must be green without them.
6. **Readmission (E38):** moved to **E3-W08 step 6**, because it needs api E3-T09.
7. **The refund warning (R-04-23).**
   - When a row is `PAID`, the rejection modal says «Hi ha un pagament cobrat: caldrà retornar-lo des de Facturació» before confirming.
   - After the rejection, keep showing `paidPaymentRequiresRefund` (a toast on D1).
8. **The D2 minors:**
   - `STALE_VERSION` gets a reload action, and errors show inside the modal or drawer, not behind it.
   - The breakdown line comes from `firstMonth`; today it always says «(mitja quota)».
   - The price comes from the selected price, with its periodicity: «Pack 6 · 135 €», with no hard-coded «/mes».
   - Phones are shown in full (R-03-27).
   - en, gender OTHER → «them». Keep the three gender values.
   - Module gating of the plan, payment and group blocks (`BILLING`, `FAMILY_GROUP`).
   - Accessibility: a distinct accessible name for each VALIDA, and `aria-invalid` on the date.
   - The warning badge when `pendingDays > warnDays` (the specs were aligned on `>` on 24-09).
   - D5 «Nou abonat» must not navigate to `/preinscripcions/nova`, which D2 reads as an id. Route it to the S03 new-member flow, or hide it; say which, citing S03.
   - From the E3-W05 screens (`roadmap/evidence/E3-W05/screens.md`, D2 d–j):
     - [REBUTJA (amb motiu)] is a neutral outline button, as in the mockup;
     - «Mascle» and «Femella» start with a capital letter;
     - WhatsApp is an inline chip;
     - the DNI/NIE stays in full, as on D10. This is an organizer ruling of 24-09: R-03-27 masks it only on 28, and the mockup's «47·····2K» is illustrative.
9. **The D1 minors (S14 R-14-05…07, T-14-25):**
   - «(nou gos)» on add-dog rows;
   - the submission date in red when `pendingDays > warnDays`;
   - signs «+3 aquest mes», «−2 aquest mes», «0 aquest mes», never «+-2» or «+0»;
   - «1 avís», with an ICU plural;
   - times as «7:30»;
   - risk rows link to D4, on that class;
   - the chart:
     - one column per progression level (E35);
     - the resolved level name as tooltip;
     - the legend follows `coverage.activeDogWeeks` (ICU);
     - `others` is not painted;
   - `/dashboard/counters` only for ADMIN (no 403 for INSTRUCTOR), and not refetched on every navigation (on focus and after commands).
   - From the E3-W05 screens (D1 b–h):
     - the risk card shows at most 6 rows, then «+{n} més», which opens D4 on the first of them (the real seed gave 16 rows);
     - the card titles are bold sentence case, with ⚠ on the risk card, as in the mockup;
     - the greeting has no comma after the weekday;
     - the «Preinscripcions» menu badge is filled with the primary colour.
10. **Re-shoot** at 1280 with `pnpm e2e:core E3-W07`, and compare each screen with its mockup in the report:
    - D1;
    - D2: normal, add-dog, and a `NOT_FOUND_PENDING` claim.

## Fixed conventions
- Design system only.
- Keys are never literals; ca, es and en are added together.
- Evidence logs are committed, including one log per failed attempt.
- Fictional data only.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E3-W07/*.log`)
- `pnpm turbo run lint typecheck test build`.
- `pnpm i18n:check`.
- `pnpm e2e:core E3-W07`.
- The complete `pnpm e2e` in the Playwright Docker image.

## Definition of Done
- Every D2 action of §2 works against the real core:
  - a dog edit after an add-dog;
  - 422 codes on their fields;
  - both contacts kept;
  - a plan change with `dryRun`;
  - a family claim resolved;
  - the refund warning.
- D1 is fresh right after a decision, with no wait in the e2e.

## Evidence to return
- The command outputs.
- A step → test table.
- The screenshots, with the per-screen comparison lines.

---

## Executor report

### Status
- Executor: claude-opus-5-5. Two sessions: 24-09 (committed as `fbc00cd`, `in_progress`, most of the product code, no tests and no report) and 25-09 (this one: snapshot `e569f15`, mocks, the remaining fixes, every test, e2e, report). Depends on E3-W06 (`verified`) and api E3-T08 (`verified`).
- Steps 0–5 and 7–10 are done (step 6 moved to E3-W08), **except step 8's first-month line, which is deferred** (round 2 point 5): the real core does not publish `SignupUpfrontReview.firstMonth` yet, so against the core D2 reads «Primera quota 30,00 €». The field stays mocks-first in `pending.json`; api E3-T12 step 5 publishes it and E3-W08 step 0 adopts it and checks the line on the core. **56 of the new tests fail on the pre-fix code** (the pages/App/D4/D18 of `3e0c28f`, the commit before this task, run against the new api-like mocks; log `03-vitest-before-fix.log`) and all pass after the fix.
- Verification: `turbo lint typecheck test build` exit 0 (`--force`, 0 cached, 34/34); `i18n:check` exit 0; `pnpm e2e:core E3-W07` exit 0 (E1/E2 10/10, E3 4/4, N-37 row found); the complete `pnpm e2e` in the Playwright Docker image exit 0 (clubs-admin 34, id 1, clubs 27). Heavy runs went through `heavy.sh`.

### Files changed
- Contract: `packages/api-client/openapi/openapi.json` (= staged `e569f15`; `openapi.next.json` removed), `packages/api-client/openapi/pending.json` (Level overlays pruned, now published; keeps only the mocks-first `SignupUpfrontReview.firstMonth`), `packages/api-client/src/generated/schema.d.ts`.
- D2: `apps/clubs-admin/src/dashboard/SignupReviewPage.tsx`, `SignupEditDrawer.tsx` (new), `signup-review-errors.ts` (new), `counters.tsx` (new), their test `SignupReviewPage.test.tsx`.
- D1 and shell: `apps/clubs-admin/src/dashboard/DashboardPage.tsx` + `DashboardPage.test.tsx`; `apps/clubs-admin/src/App.tsx` (counters ADMIN-only, on load/focus/after commands) + `App.test.tsx`; `apps/clubs-admin/src/planning/CalendarPage.tsx` (`?classe=`) + `CalendarPage.test.tsx`; `apps/clubs-admin/src/styles.css`; `packages/ui/src/components.tsx` + `components.css` (chart `title`, filled sidebar counters).
- Step 0 labels: `apps/clubs-admin/src/audit/AuditPage.tsx` (+ `AuditPage.test.tsx`), `packages/i18n/src/locales/{ca,es,en}/admin-audit.json`.
- D5: `apps/clubs-admin/src/census/CensusListPage.tsx` («Nou abonat» hidden), `census.json` ×3 (`members.new` removed).
- i18n: `packages/i18n/src/locales/{ca,es,en}/admin-census.json`, `admin-dashboard.json`; `scripts/i18n-check.mjs` (dynamic prefixes `signupReview.warning.` / `gender.` / `sex.`).
- Contract consumers of the new snapshot: `apps/clubs/src/SelfServicePages.tsx`, `packages/api-client/src/mocks/fixtures/member-self-service.ts` (`MeDog.status`, optional documents), `packages/api-client/src/mocks/fixtures/signup.ts` (quotes of offered plans), `signup-config-canic.json`, `branding-canic.json` / `branding-minim.json` (`legalName`, `taxId`).
- Mocks: `packages/api-client/src/mocks/fixtures/signup-review.ts` (new: D2 variants, plan-aware dry run, signup-dog documents), `member-signup-review.json`, `handlers.ts`, `scenarios.ts` (`adminSignupReviewAddDog`, `adminSignupReviewFamilyPending`), `fixtures/census.ts` (Laura's family group), `fixtures.contract.test.ts`.
- E2E: `e2e/core/e3-signup.spec.ts`.
- `CHANGELOG.md`; evidence `roadmap/evidence/E3-W07/`.

### Rules and tests implemented (step → test)
Vitest names are in `SignupReviewPage.test.tsx` (D2), `DashboardPage.test.tsx` (D1), `App.test.tsx`, `CalendarPage.test.tsx`, `AuditPage.test.tsx`, `fixtures.contract.test.ts`; «pre-fix» = fails in `03-vitest-before-fix.log`.

| Step | Rule / spec | Test | Pre-fix |
|---|---|---|---|
| 0 snapshot | contract | `E3-W07 D2 signup review fixtures follow the S04 contract` (5 views + every plan's dry run, Ajv); real core: `planOptions`, `warnDays`, dog `version` asserted in `e3-signup.spec.ts` | n/a (mocks) |
| 0 warnings by code | S04 §2 D2 | `step 0 renders each review warning by its code…` | fails |
| 0 audit labels | E38 / E3-T09 | `E3-W07 step 0 labels a readmission submitted from the public form (SIGNUP_SUBMITTED, PUBLIC)` | fails |
| 1 M12 | R-04-19 | `edits the member and the dog in one save: the dog PATCH carries the dog's version…`; `add-dog: … version 0 while the member is at 7 (R-04-25)`; `after a partial save … reloads, shows the error in the drawer and retries only the dog`; real core: add-dog dog edit → `PATCH /dogs` 200 | all 3 fail |
| 2 M13 | S04 §2 D2, CATALEG_ERRORS | table `%s (%i, bare as the api sends it) is shown on its field` (9 codes: LEVEL_REQUIRED, LEVEL_NOT_ACTIVE, NEXT_INVOICE_DATE_REQUIRED, VALIDATION_ERROR on `nextInvoiceDate`, UPFRONT_AMOUNT_EXCEEDS_DUE, PLAN_NOT_AVAILABLE, INVALID_STATE+CHECKOUT_PENDING, MEMBERSHIP_EXISTS, MEMBER_ERASED); `caps the amount input at the amount due`; real core: bare `422 LEVEL_REQUIRED` on «Nivell inicial» with `aria-invalid` | all fail |
| 3 M14 | R-04-19, R-04-03 | `editing the first email keeps the second…`; `editing the second phone sends both phones…`; `offers gender (three values), the notes, holderTaxId and the requested plan read-only`; `adds a dog document forwarding the signed upload headers, and removes one`; add-dog read-only person (above); `locks the fields while the save is in flight`; real core: second email + phone added, then the first phone edited, all four kept | first 4 fail; the lock test was added after the pre-fix run (it asserts the `disabled` the fix adds) |
| 4 M15 | R-04-13, T-04-33, T-04-16 | `lists planOptions … runs dryRun on a change to Pack 6: lines, date and body updated`; `renders the dry run's warnings on the plan card (PAID_EXCEEDS_QUOTE…)`; `a NOT_FOUND_PENDING claim … resolved by attaching the holder's group`; `… resolved with an explicit «Sense grup»`; `T-04-16 validates a FOUND claim whose holder has no group yet without familyGroupId` (replaces the old rejection assertion); real core: Pack 6 dry run, NOT_FOUND_PENDING resolved with `familyGroupId` | all fail |
| 5 M11 | R-14-01, R-04-24 | `after VALIDA navigates to /tauler and refreshes the menu counters`; `R-04-24 warns … read from the signup view` (no `/dashboard` call); `a signup with nothing pending (409 INVALID_STATE NOT_PENDING) …links to D10`; `a VALIDA on a signup another admin already resolved…`; real core: `waitForTimeout` and `/abonats` detour removed, D1's first read after VALIDA has count − 1 and the menu badge too | all fail |
| 7 refund | R-04-23 | `warns before rejecting a signup with a collected payment and keeps the refund flag for D1`; `rejects a signup with nothing collected without the warning`; D1 `R-04-23 keeps the refund notice…`; real core: no warning on Pau (nothing collected) | 1st and D1 fail; the 2nd passes pre-fix (no warning existed) |
| 8 D2 minors | R-03-27, S04 §2 | `STALE_VERSION offers a reload that keeps the admin's own choices`; `shows a rejection error inside the modal…`; `names the first month and its portion from upfront.firstMonth…` (mocks-first only, **deferred** on the core: round 2 point 5); `shows the mockup: DNI and phone in full…` (DNI, phone, WhatsApp chip, «Femella», ghost REBUTJA, «Abonat · 60,00 €/mes»); plan option labels without «/mes» for packs (step 4 test); `en: gender OTHER → «them»`; `gates the plan, payment and upfront blocks on BILLING…`; `R-04-24 warns on the age badge only when pendingDays > warnDays`; distinct VALIDA names (D1) | all fail |
| 8 D5 «Nou abonat» | S03 §2 D5 | hidden (no test: removal) | — |
| 9 D1 minors | R-14-05…07 (E35), T-14-25, R-14-08 | `gives each VALIDA its own accessible name`; `marks an add-dog row «(nou gos)»`; `R-14-05 paints the submission date red…`; `signs the members delta %i…` (+3/−2/0); `counts %i risk alert(s) with an ICU plural`; `writes times as «7:30» and links each risk row to D4 on its class`; `shows at most 6 risk rows, then «+{n} més»…`; `puts the warning icon on the risk card title`; `E35 paints one column per progression level … never others`; `follows coverage.activeDogWeeks = %i in the chart legend`; D4 `selects the class of ?classe=…`; App `reads the counters once for an ADMIN, again on focus, and not on every navigation` + `never asks an INSTRUCTOR for the counters` (+ filled `.ah-sidebar__count`) | all fail except the «avís» ICU cases (the plural lives in the locale files, which the pre-fix run shares) |
| 10 re-shoot | — | `pnpm e2e:core E3-W07` captures below | — |

### Screens (1280 px, real core `5fa199e`, `roadmap/evidence/E3-W07/`) — comparison with the mockups
- `D1-dashboard-core-1280.png` / `D1-dashboard-after-validation-core-1280.png` vs `D1-tauler-de-control.png`: same layout and copy («Bon dia! Divendres 25 de setembre» without comma, «+1 aquest mes», «10 avisos», times «7:30», bold sentence-case titles with ⚠, filled menu counters, «(Mestís)» rows, «enviada el …» red past 2 days, «+4 més» after 6 risk rows). Differences: seed data (0 % occupancy, 0 bookings, the seed's «Demo … (Fictional mixed breed)» rows, 16 at-risk classes); the chart has no highlighted bars because the seed has no recent bookings.
- `D2-signup-core-1280.png` vs `D2-validacio-d-una-preinscripcio.png`: same blocks and actions. Differences: header `#a269924e` (a PENDING member has no number yet; the mockup shows #1042) and the full name; DNI and phone in full (organizer ruling 24-09 / R-03-27); first-month line «Primera quota 30,00 €» instead of «agost 30 € (mitja quota)» because the core does not send `upfront.firstMonth` yet (proposal below); the plan is the core's family-fare proposal «Abonat · 2 gossos (familiar) · 90,00 €/mes»; buttons with dark text (A32).
- `D2-signup-add-dog-core-1280.png` (add-dog after a dog edit): «NOU GOS» badge, «Quota de gos addicional 45,00 €», member number #195.
- `D2-signup-family-pending-core-1280.png` (NOT_FOUND_PENDING): «Pendent — ha indicat: Pere Inexistent + gos Tro», «Cerca el titular», «Sense grup». No mockup exists for this state.
- `D2-signup-plan-change-core-1280.png` (dry run for Pack 6: «Pack 6 · 135,00 €», «Pack 135,00 €», no date field) and `D2-signup-level-required-core-1280.png` (bare 422 on «Nivell inicial»).

### Assumptions
- **Snapshot:** adopted `e569f15` (staged after the 24-09 session had adopted `f4d6997`), as step 0 allows «a later one»; it also carries api E3-T09/T10 contract bits, so the branding fixtures gained `legalName`/`taxId` (fictional `G00000000`, `null` for Mínim). The real-core e2e ran on the local image `5fa199e` (24-09 19:20): `docker pull` was denied (below).
- **Audit labels:** the `SIGNUP_SUBMITTED` / `PUBLIC` labels live in `admin-audit:actions|origins`, where D18 reads every audit enum; there is no `enums:auditAction` namespace.
- **First-month line (deferred, not done):** `SignupUpfrontReview.firstMonth {option, portion, startDate, amountDue}` stays mocks-first in `pending.json` (S04 §3 lists `upfront.firstMonth`; `portion` is added so the web never reimplements R-04-15). Without it the line reads «Primera quota {amount}», which is what the real core shows today. Api E3-T12 step 5 publishes the field; E3-W08 step 0 adopts it and checks the line against the core.
- **Mock dry run (E39; the PARTIAL part is superseded by E39b in round 2, point 4):** PAID/PARTIAL rows are kept and their paid amount deducted; DUE rows are replaced by the new plan's lines; `PAID_EXCEEDS_QUOTE` + `paidExceedsQuote` when paid > quote. The api's exact line layout for a paid-then-changed plan is not in the contract; the tests only assert what the UI renders from the answer.
- **Add-dog view:** `dogs` holds only the pending dogs (the api description: «signup is the oldest pending dog's own submission»).
- **D5 «Nou abonat»:** hidden, not routed. S03 §2 D5 says it opens the S04 signup form **inside the back office** (S03 open point 1); that form does not exist, and the public `/apuntat-hi` is another app/host, so any link would be dead (hard rule 3).
- **Menu counters:** all three sidebar counters are filled with the primary colour: the mockup's `.nav .b` style applies to every counter, not only «Preinscripcions».
- **Refund warning against the real core:** not reachable in the e2e stack: a pending signup can only have a PAID row through Stripe (manual payments are recorded at validation). Covered by Vitest + MSW; the e2e asserts its absence when nothing was collected.

### Questions / catalog proposals
- **api proposal (contract):** publish `SignupUpfrontReview.firstMonth {option, portion, startDate, amountDue}` in `GET /members/{id}/signup` and the dry run (S04 §3 `Member.signup.upfront.firstMonth`, plus `portion`). Until then D2 cannot say «agost … (mitja quota)» against the core.
- **Proposed literals (not in the mockup), ca/es/en:** `admin-census:signupReview.` `familyPending` «Pendent — ha indicat: {holder} + gos {dog}», `familySearch` «Cerca el titular», `familyAttach` «Afegeix al grup de {name}», `familyNoGroup`, `familyNoResults` «Cap abonat amb aquest nom», `familyNone` «Sense grup», `familyChosen` «Grup de {name}», `familyChange` «Canvia», `familyDecisionRequired`, `resolvedLink` «Obre la fitxa de l'abonat», `invoiceInvalid`, `warning.CHECKOUT_PENDING`, `warning.PAID_EXCEEDS_QUOTE`, `actions.reload` «Torna a carregar», `upfrontLines.FIRST_MONTH_UNKNOWN` «Primera quota {amount}», the drawer fields (`gender`, `secondEmail`, `phonePrefix`, `secondPhone`, `phoneLabel` «Descripció», `holderTaxId` «NIF del titular», `requestedPlan` «Modalitat sol·licitada», `planName`); `admin-dashboard:signups.validateLabel` «VALIDA: {name}», `signups.submitted` «enviada el {date}»; `admin-audit:actions.SIGNUP_SUBMITTED` «Sol·licitud d'alta enviada», `origins.PUBLIC` «Formulari públic». The task gave «Aquesta preinscripció ja s'ha resolt», «Hi ha un pagament cobrat…» and «+{n} més».
- **Denied commands** (worked around, nothing skipped except the pull): `docker pull ghcr.io/jboixtcm/agilityhub-core-api:main`; `git -C <repo> status --short`; `rm -r <dir> …` (deleted file by file); `sed -e 's|…|…|' file > copy` (copied, then edited); heredocs with braces (`python3 - <<'EOF' …`).

### Evidence
Earlier failing attempts (one line each):
- `02-turbo-attempt1.log`: `clubs-admin#lint` failed on 4 errors in the new `SignupReviewPage.test.tsx` (a `#1042` string read as a colour, an unnecessary `??`, two `as HTMLElement`) → fixed in the test.
- `06-e2e-core.log`: E1/E2 10/10; E3 failed at `expect(fieldset).toBeDisabled()` (Playwright does not report a `<fieldset disabled>` itself as disabled; everything before it passed, including D1 fresh after VALIDA) → the e2e asserts the contained inputs instead.

Other logs: `01-api-generate-e569f15.log` (exit 0), `03-vitest-before-fix.log` (pre-fix proof: 56 failed | 51 passed), `04-turbo.log` and `09-turbo-final.log` (cached runs, exit 0), `05-i18n-check.log` (exit 0).

**1. `pnpm turbo run lint typecheck test build --force`** → exit 0 (full log `11-turbo-force.log`; colour codes stripped below)
```
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > R-06-15 without FREE_TRAINING [Bloqueja pista] offers only «Bloqueig»
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > R-06-15 with levels.enabled=false the card has no «Nivells» chip
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > R-06-15 INSTRUCTOR (no /parameters) sees «Nivells» only on a class with levels
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > R-06-11 lists the RING_BLOCK_CONFLICT conflicts inside the block drawer
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > INSTRUCTOR reads the calendar without actions and with inert chips (A22 c)
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > shows the week warnings and selects the class of a warning; day headers open the day view
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test:  ✓ src/planning/CalendarPage.test.tsx (25 tests) 5887ms
clubs-admin:test:    ✓ E3-W07 step 9 a D1 risk row opens D4 on its class (1)
clubs-admin:test:      ✓ selects the class of `?classe=` in the week of `?setmana=` 485ms
clubs-admin:test:    ✓ T-06-28 D4 / D4b / D4c class calendar (front half, MSW) (24)
clubs-admin:test:      ✓ opens «Esborrany» on the first week with drafts, with the validation card and its count 610ms
clubs-admin:test:      ✓ [VALIDAR LA SETMANA] turns every draft of the week active at once 335ms
clubs-admin:test:      ✓ cancels an active class with registrants through D4c: four rows, text required, «ANUL·LA I AVISA ELS 4 ALUMNES» 354ms
clubs-admin:test:      ✓ [ELIMINA] of an active class goes through the same D4c modal 313ms
clubs-admin:test:      ✓ R-06-09 saves the diff with the version; STALE_VERSION keeps its message after the refetch remounts the card 441ms
clubs-admin:test:      ✓ R-06-09 [Crear classe]: a new time or ring after RING_HAS_BOOKINGS drops the confirmation 322ms
clubs-admin:test:      ✓ R-06-09 [Crear classe]: a RING_HAS_BOOKINGS answer that arrives after a ring change is dropped 300ms
clubs-admin:test: 
clubs-admin:test:  Test Files  18 passed (18)
clubs-admin:test:       Tests  187 passed (187)
clubs-admin:test:    Start at  08:32:40
clubs-admin:test:    Duration  9.76s (environment 38%, tests 35%, transform 15%, setup 7%, import 5%)
clubs-admin:test: 

 Tasks:    34 successful, 34 total
Cached:    0 cached, 34 total
  Time:    22.337s 
```
Test counts in that log: api-client 101, ui 61, i18n 51, auth 17, course-core 342, shared-types 21, id 9, clubs 125, clubs-admin 187, all passed.

**2. `pnpm i18n:check`** → exit 0 (`10-i18n-check-final.log`, complete)
```
> agilityhub-core-web@0.0.0 i18n:check /Users/jordib/dev/agilityhub/agilityhub-core-web
> node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs

i18n check passed
ca: 2305 keys
es: 2305 keys
en: 2305 keys
1732 statically used keys, 0 unused
250 codes, 0 missing
```

**3. `heavy.sh pnpm e2e:core E3-W07`** → exit 0 (full log `07-e2e-core.log`: E1/E2 stage «10 passed (19.8s)», E3 stage below)
```
+ @types/node 24.13.3
+ eslint 9.39.5
+ eslint-import-resolver-typescript 4.4.5
+ eslint-plugin-import 2.32.0
+ eslint-plugin-jsx-a11y 6.10.2
+ eslint-plugin-react 7.37.5
+ eslint-plugin-react-hooks 7.1.1
+ globals 17.12.0
+ husky 9.1.7
+ i18next-parser 9.4.0
+ lint-staged 17.5.0
+ msw 2.15.0
+ prettier 3.9.6
+ size-limit 13.0.3
+ turbo 2.10.12
+ typescript 5.9.3
+ typescript-eslint 8.69.0

. prepare$ node packages/config/scripts/prepare-husky.mjs
. prepare: .git can't be found
. prepare: Done
Done in 4.2s using pnpm v9.15.9

[WebServer] (node:134) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)

[WebServer] (node:214) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
[WebServer] (node:312) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)


Running 4 tests using 1 worker

[1/4] e2e/core/e3-signup.spec.ts:355:5 › T-04-34 public signup is validated and enters through the N-02 welcome link
[2/4] e2e/core/e3-signup.spec.ts:745:5 › T-04-34 rejected signup delivers N-03
[3/4] e2e/core/e3-signup.spec.ts:752:5 › T-04-34 active member adds a dog and the club validates it
[4/4] e2e/core/e3-signup.spec.ts:756:5 › T-04-34 signup.enabled=false shows only the configured closed text
  4 passed (16.1s)
{"action":"OPEN_DOG","channel":"APP","code":"N-37","dogName":"Neret E3","status":"SENT"}
```

**4. The complete `pnpm e2e` in the Playwright Docker image: `heavy.sh pnpm e2e:docker E3-W07`** → exit 0 (full log `08-e2e-docker.log`; colour codes stripped)
```
clubs:build: precache  54 entries (1622.57 KiB)
clubs:build: files generated
clubs:build:   dist/sw.js
clubs:build:   dist/workbox-9c191d2f.js
clubs-admin:e2e: cache bypass, force executing 9667fdbad9c2e047
clubs-admin:e2e: 
clubs-admin:e2e: > clubs-admin@0.0.0 e2e /work/apps/clubs-admin
clubs-admin:e2e: > playwright test
clubs-admin:e2e: 
clubs-admin:e2e: [WebServer] (node:664) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs-admin:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs-admin:e2e: 
clubs-admin:e2e: Running 34 tests using 9 workers
clubs-admin:e2e: ··································
clubs-admin:e2e:   34 passed (20.4s)
id:e2e: cache bypass, force executing 26aa8925dc4e2b8f
id:e2e: 
id:e2e: > id@0.0.0 e2e /work/apps/id
id:e2e: > playwright test
id:e2e: 
id:e2e: 
id:e2e: Running 1 test using 1 worker
id:e2e: ·
id:e2e:   1 passed (3.2s)
clubs:e2e: cache bypass, force executing 13a09f7473a31e00
clubs:e2e: 
clubs:e2e: > clubs@0.0.0 e2e /work/apps/clubs
clubs:e2e: > playwright test
clubs:e2e: 
clubs:e2e: [WebServer] (node:2813) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs:e2e: 
clubs:e2e: Running 27 tests using 7 workers
clubs:e2e: ···························
clubs:e2e:   27 passed (12.6s)

 Tasks:    8 successful, 8 total
Cached:    0 cached, 8 total
  Time:    48.349s
```

### Not run / notes
- The real-core e2e ran against the local core image `5fa199e`, not a fresh pull (denied). It exercised: D1 fresh after VALIDA with no wait, both contacts, a dog edit after an add-dog, a plan change with `dryRun`, a bare `LEVEL_REQUIRED` on its field and a NOT_FOUND_PENDING claim resolved with the found holder's group. Not exercised on the core: the refund warning (see Assumptions) and the other 422 codes (Vitest table only).
- The `waitForTimeout(1_000)` before the D11 capture (`e3-signup.spec.ts`, D11 part) is unrelated to M11 and stays.
- The working tree also holds organizer edits made during this session (`roadmap/MESSAGES.md` entries of 25-09, `roadmap/tasks/E3-W08.md`, `roadmap/tasks/E3-W10.md`, `docs/MODEL_DADES_PLATAFORMA.md`, `docs/specs/00-transversal/CATALEG_ESDEVENIMENTS.md`); I did not touch them.
- **For E3-W08 step 0:** the snapshot adopted here, `e569f15`, already carries E3-T10 round 1 (integer `percent`, nullable `waitingTotal` and `RiskNotified.gender` without `null` in the enum, `ClubSummary.legalName`/`taxId`, the add-dog `signup`). D1 and the fixtures are aligned with it. The E3-T10 round-2 change («nullable enums carry `null` in their `enum`») is not in it.

### Round 2 (25-09 · the five points of «Organizer verification», review `E3-W07-20260925-0841-codex.md`)

**Status.** Session 3, executor claude-opus-5-5. All five points are fixed:
- the invoice date is canonical;
- a quote is bound to its plan, price and view;
- the drawer changes the payment method;
- the mock follows E39b;
- the report now says the first-month line is deferred.

Every point has a test that failed before the fix, except where noted below. All four Verification commands passed on the final tree:
- turbo `--force`: exit 0, 34/34, 0 cached;
- `i18n:check`: exit 0;
- `pnpm e2e:core E3-W07`: exit 0 (E1/E2 10, E3 4, N-37 row);
- the complete Docker `pnpm e2e`: exit 0 (34 + 1 + 27).

The heavy runs went through `heavy.sh`: it waited 5 min 46 s for the api's `./mvnw verify`, and 3 min for `bin/e3-smoke`. The staged `openapi.next.json` (api `19d8460`, committed after round 1) is not adopted here. Its adoption belongs to E3-W08 step 0.

**Point → fix → test** (Vitest in `SignupReviewPage.test.tsx`, «round 2» blocks; mocks in `fixtures.contract.test.ts`; «pre-fix» = the product files of `HEAD` (round 1) run against the new tests and mocks):

| # | Fix | Test | Pre-fix |
|---|---|---|---|
| 1 date (R-04-15, S04 §10) | The typed date is kept as `TypedDate {iso, locale, text}`. The ISO date is what is sent. The text is shown as typed only in the language it was typed in; otherwise the ISO date is formatted in the current locale. | `10/11/2026 typed in ca still sends 2026-11-10 after a switch to en, shown in the en format` | fails: the en field still read «10/11/2026» (log `12`) |
| 2 quote (steps 4/8, S04 §2) | A `Quote {key, view, result \| error}`. The key is plan \| price \| view `version`, and the quote also keeps the view object it was asked on. An effect asks for it (`useEffectEvent`) whenever the choice, the version, the view or a retry changes; a stale answer is dropped. It is shown only while it matches the current key and view. A failed quote shows its error on the plan card, with «Torna-ho a provar» (or «Torna a carregar» for `STALE_VERSION`), and has no upfront block. A reload gives a new view object, which drops the quote and asks again. [VALIDA] is disabled until the current quote has succeeded. | `Pack 6 is quoted, then the Pack 10 quote fails: no 135 € breakdown or max remains, and VALIDA is disabled` (then the retry shows Pack 10's 180 €); `a reload after a full payment shows no editable amount (the old quote is discarded)`; `drops a Pack 6 answer that lands after the change to Pack 10` | the first two fail. The third passes pre-fix: the old `dryRunSeq` already dropped it. It stays as a regression test (log `12`) |
| 3 method (R-04-19, R-04-10, R-04-25) | The drawer has a «Mètode de pagament» selector. Its options are the providers of `GET /club` (`SEPA_XML`→SEPA_DD, `STRIPE`→CARD, `MANUAL`→MANUAL), and the applicant's own method is always kept. SEPA shows IBAN (optional), holder (pre-filled with the full name) and holder tax id, and sends `{type: SEPA_DD, sepa}`. Cash and card send `{type}`. In add-dog mode the selector is disabled and `/club` is never read; without BILLING there is no selector. D2 shows a non-SEPA method by its label. | `moves a cash applicant to direct debit with IBAN and holder, among the club's methods`; `moves a direct-debit applicant to cash: only the type is sent, and D2 reads «Efectiu»`; `offers every provider the club has, whatever its enabled flag (the real core's GET /club for the seed)`; `offers the card too when the club has Stripe enabled`; `add-dog (R-04-25): the method stays read-only…`; `without BILLING the drawer has no payment method` | all fail except «without BILLING», which the old drawer already met (logs `12`, `24`) |
| 4 mock E39b | `quoteUpfront`: a PARTIAL line that a plan change supersedes becomes `CANCELLED` with its amounts intact. A new `PAID` line records the collected amount (same concept and provider). The quote reads the requested `priceId`. | `a 60 € quote leaves 10 € to pay: the PARTIAL line is CANCELLED and a PAID line records the 50 €` (Ajv-valid); `a 40 € quote adds no line to pay and warns PAID_EXCEEDS_QUOTE for the 10 € to refund`; `the requested plan keeps the PARTIAL line open (no plan change)` | the first two fail (60 € due instead of 10 €); the third passes pre-fix (log `13`) |
| 5 report | The first-month line is recorded as **deferred** in Status, in the step-8 row, in Assumptions and in `CHANGELOG.md`. The overlay stays in `pending.json`. | — | — |

**On the real core** (`e3-signup.spec.ts`, Joana, passport applicant, SEPA without IBAN):
- the drawer reads `GET /club` (200) and offers «Efectiu»;
- `PATCH /members/{id}` `{paymentMethod: {type: MANUAL}}` answers 200;
- the reload asks for the quote again (`dryRun` 200), and [VALIDA] is enabled afterwards;
- D2 reads «Efectiu», and the red «Compte no informat» is gone.

Capture: `D2-signup-payment-method-core-1280.png` shows the drawer with «Mètode de pagament: Efectiu» and no IBAN fields. Points 1 and 4 are Vitest only: the e2e has no language switch, and a PARTIAL payment needs Stripe, which the e2e stack does not have.

**Found on the real core.** My first draft offered only providers with `enabled: true`, and the e2e failed at the «Efectiu» option (logs `17`, `18`). The probe in `d2-payment-methods-core.json` shows why:
- the seed club's `GET /club` has `SEPA_XML` and `MANUAL` at `{configured: false, enabled: false}`;
- the core's anonymous `GET /signup` still offers «Domiciliació» and «Efectiu»;
- `GET /signup` with the ADMIN bearer answers `403 FORBIDDEN`.

So the drawer now offers every provider present, as the core's public form does. The mock mirrors the core: cash is listed with `enabled: false`. A Vitest covers exactly the core's answer.

**Files changed (round 2):**
- `apps/clubs-admin/src/dashboard/SignupReviewPage.tsx`, `SignupEditDrawer.tsx`, `SignupReviewPage.test.tsx`;
- `packages/api-client/src/mocks/fixtures/signup-review.ts` (E39b), `handlers.ts` (`GET /club` providers per scenario; the member `PATCH` applies a method change and recomputes `ACCOUNT_NOT_PROVIDED`; `PAYMENT_METHOD_NOT_AVAILABLE` for a method without provider), `fixtures.contract.test.ts`;
- `packages/i18n/src/locales/{ca,es,en}/admin-census.json` (`signupReview.fields.paymentMethod`);
- `e2e/core/e3-signup.spec.ts`;
- `CHANGELOG.md`;
- evidence `12`–`24`, `d2-payment-methods-core.json`, `D2-signup-payment-method-core-1280.png`. The e2e also refreshed the E3-W07 captures and wrote `members-export-download.json` (E4-W07's E2 check).

**Assumptions (round 2):**
- **Dry-run body.**
  - It no longer sends `upfrontAmountPaid`: an amount typed for the old plan could make a cheaper plan's quote fail with `UPFRONT_AMOUNT_EXCEEDS_DUE`.
  - Levels and the family group are sent as they are when the quote is asked. Changing them does not ask again, because they do not price the plan. `LEVEL_REQUIRED` stays a validation error on the level field, as the core e2e shows.
- **Quote key.** Plan, price and version, plus the view object. A reload with the same version, for instance after a Stripe payment, still drops the quote.
- **A failed or pending quote** hides the upfront block. `NOT_PENDING` on a quote shows the resolved state.
- **Methods.**
  - Cash and card send `{type}` only: at signup the channel is `null` (R-04-10), and a card is captured by Stripe (R-04-26).
  - A move to SEPA leaves the IBAN optional («Compte no informat» until it is given).
  - If `GET /club` fails, only the applicant's own method is offered, with the error under the selector.
- **E39b mock.**
  - The correction line has no `correctionOf`, because `UpfrontLine` does not carry it.
  - When the amount paid exceeds the quote, the correction is kept and no line to pay is added. I read «no es crea cap línia» in S04 §5 as «no line to pay».
  - A PARTIAL line is closed only when the quoted plan differs from `signup.planIdRequested`.

**Questions / proposals (round 2):**
- **api (contract).**
  - The seed club's `GET /club` reports both providers as `enabled: false, configured: false`, while `GET /signup` offers both. R-04-10 says the offered methods are the active providers, so either the flags or the seed are inconsistent.
  - Proposal: publish the assignable methods in `MemberSignupView` (for instance `paymentMethods: SignupPaymentMethod[]`, like `planOptions`), so D2 does not depend on `/club`. `GET /signup` answers 403 to an ADMIN.
- **Proposed literal:** `admin-census:signupReview.fields.paymentMethod` «Mètode de pagament» / «Método de pago» / «Payment method», the same words as D10 and 19.
- **Denied commands** (all worked around; nothing skipped):
  - ANSI stripping with `perl -pi …` and `sed $'…'`. Logs `12`, `13` and `24` keep their colour codes, and the tails below have them removed by hand.
  - `( … )` subshells, and `cd … && … > log`: I used absolute paths.
  - `ls` outside the repository.

**Evidence (round 2).** Failing and superseded attempts, one line each:
- `12-vitest-round2-before-fix.log`: pre-fix, 7 failed, 2 passed (the late-answer drop and «without BILLING»);
- `13-vitest-e39b-before-fix.log`: pre-fix, 2 failed, 1 passed;
- `24-vitest-providers-before-fix.log`: the providers test on the pre-fix drawer, 1 failed;
- `14-turbo-force.log`: exit 243. `pnpm -C <dir> turbo …` took the directory as the command (EACCES), so nothing ran → re-run from the repository root (`15`);
- `15-turbo-force.log` and `16-i18n-check.log`: exit 0, before the providers fix → superseded by `22` and `23`;
- `17-e2e-core.log`: exit 1. E1/E2 10/10; E3 failed at `option «Efectiu»` (the drawer filtered `enabled`) → probe added;
- `18-e2e-core-probe.log`: exit 1, the same failure; it wrote `d2-payment-methods-core.json` → the drawer lists every provider present;
- `19-e2e-core.log`: exit 0, but the capture missed the selector below the drawer's fold → viewport capture after `scrollIntoViewIfNeeded`.

**R2-1. `pnpm turbo run lint typecheck test build --force`** → exit 0 (`22-turbo-force-final.log`; colour codes removed). Test counts: api-client 140, ui 61, i18n 51, auth 17, course-core 342, shared-types 21, id 9, clubs 125, clubs-admin 224, all passed.
```
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > R-06-15 without FREE_TRAINING [Bloqueja pista] offers only «Bloqueig»
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > R-06-15 with levels.enabled=false the card has no «Nivells» chip
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > R-06-15 INSTRUCTOR (no /parameters) sees «Nivells» only on a class with levels
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > R-06-11 lists the RING_BLOCK_CONFLICT conflicts inside the block drawer
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > INSTRUCTOR reads the calendar without actions and with inert chips (A22 c)
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/CalendarPage.test.tsx > T-06-28 D4 / D4b / D4c class calendar (front half, MSW) > shows the week warnings and selects the class of a warning; day headers open the day view
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test:  ✓ src/planning/CalendarPage.test.tsx (25 tests) 6431ms
clubs-admin:test:    ✓ E3-W07 step 9 a D1 risk row opens D4 on its class (1)
clubs-admin:test:      ✓ selects the class of `?classe=` in the week of `?setmana=` 536ms
clubs-admin:test:    ✓ T-06-28 D4 / D4b / D4c class calendar (front half, MSW) (24)
clubs-admin:test:      ✓ opens «Esborrany» on the first week with drafts, with the validation card and its count 694ms
clubs-admin:test:      ✓ [VALIDAR LA SETMANA] turns every draft of the week active at once 424ms
clubs-admin:test:      ✓ cancels an active class with registrants through D4c: four rows, text required, «ANUL·LA I AVISA ELS 4 ALUMNES» 437ms
clubs-admin:test:      ✓ [ELIMINA] of an active class goes through the same D4c modal 368ms
clubs-admin:test:      ✓ R-06-09 saves the diff with the version; STALE_VERSION keeps its message after the refetch remounts the card 513ms
clubs-admin:test:      ✓ R-06-09 [Crear classe]: a new time or ring after RING_HAS_BOOKINGS drops the confirmation 339ms
clubs-admin:test:      ✓ R-06-09 [Crear classe]: a RING_HAS_BOOKINGS answer that arrives after a ring change is dropped 337ms
clubs-admin:test: 
clubs-admin:test:  Test Files  19 passed (19)
clubs-admin:test:       Tests  224 passed (224)
clubs-admin:test:    Start at  10:48:59
clubs-admin:test:    Duration  10.77s (tests 37%, environment 35%, transform 14%, import 7%, setup 7%)
clubs-admin:test: 

 Tasks:    34 successful, 34 total
Cached:    0 cached, 34 total
  Time:    22.656s
```

**R2-2. `pnpm i18n:check`** → exit 0 (`23-i18n-check-final.log`, complete)
```
> agilityhub-core-web@0.0.0 i18n:check /Users/jordib/dev/agilityhub/agilityhub-core-web
> node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs

i18n check passed
ca: 2307 keys
es: 2307 keys
en: 2307 keys
1734 statically used keys, 0 unused
250 codes, 0 missing
```

**R2-3. `heavy.sh pnpm e2e:core E3-W07`** → exit 0 (`20-e2e-core-final.log`; the lock waited 5 min 46 s for the api's `./mvnw -q clean verify`; E1/E2 stage «10 passed (18.0s)», E3 stage below)
```
+ @types/node 24.13.3
+ eslint 9.39.5
+ eslint-import-resolver-typescript 4.4.5
+ eslint-plugin-import 2.32.0
+ eslint-plugin-jsx-a11y 6.10.2
+ eslint-plugin-react 7.37.5
+ eslint-plugin-react-hooks 7.1.1
+ globals 17.12.0
+ husky 9.1.7
+ i18next-parser 9.4.0
+ lint-staged 17.5.0
+ msw 2.15.0
+ prettier 3.9.6
+ size-limit 13.0.3
+ turbo 2.10.12
+ typescript 5.9.3
+ typescript-eslint 8.69.0

. prepare$ node packages/config/scripts/prepare-husky.mjs
. prepare: .git can't be found
. prepare: Done
Done in 17.8s using pnpm v9.15.9

[WebServer] (node:134) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)

[WebServer] (node:214) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
[WebServer] (node:312) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)


Running 4 tests using 1 worker

[1/4] e2e/core/e3-signup.spec.ts:355:5 › T-04-34 public signup is validated and enters through the N-02 welcome link
[2/4] e2e/core/e3-signup.spec.ts:805:5 › T-04-34 rejected signup delivers N-03
[3/4] e2e/core/e3-signup.spec.ts:812:5 › T-04-34 active member adds a dog and the club validates it
[4/4] e2e/core/e3-signup.spec.ts:816:5 › T-04-34 signup.enabled=false shows only the configured closed text
  4 passed (15.5s)
{"action":"OPEN_DOG","channel":"APP","code":"N-37","dogName":"Neret E3","status":"SENT"}
```

**R2-4. The complete `pnpm e2e` in the Playwright Docker image: `heavy.sh pnpm e2e:docker E3-W07`** → exit 0 (`21-e2e-docker.log`; the lock waited 3 min for the api's `bin/e3-smoke`)
```
clubs:build: precache  54 entries (1626.20 KiB)
clubs:build: files generated
clubs:build:   dist/sw.js
clubs:build:   dist/workbox-9c191d2f.js
clubs-admin:e2e: cache bypass, force executing 9ae1cc7467a40d6f
clubs-admin:e2e: 
clubs-admin:e2e: > clubs-admin@0.0.0 e2e /work/apps/clubs-admin
clubs-admin:e2e: > playwright test
clubs-admin:e2e: 
clubs-admin:e2e: [WebServer] (node:665) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs-admin:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs-admin:e2e: 
clubs-admin:e2e: Running 34 tests using 9 workers
clubs-admin:e2e: ··································
clubs-admin:e2e:   34 passed (18.8s)
id:e2e: cache bypass, force executing dec8e350cfdc6404
id:e2e: 
id:e2e: > id@0.0.0 e2e /work/apps/id
id:e2e: > playwright test
id:e2e: 
id:e2e: 
id:e2e: Running 1 test using 1 worker
id:e2e: ·
id:e2e:   1 passed (3.0s)
clubs:e2e: cache bypass, force executing d1b53513323b480f
clubs:e2e: 
clubs:e2e: > clubs@0.0.0 e2e /work/apps/clubs
clubs:e2e: > playwright test
clubs:e2e: 
clubs:e2e: [WebServer] (node:2823) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs:e2e: 
clubs:e2e: Running 27 tests using 7 workers
clubs:e2e: ···························
clubs:e2e:   27 passed (12.3s)

 Tasks:    8 successful, 8 total
Cached:    0 cached, 8 total
  Time:    45.987s
```

**Notes (round 2):**
- After runs `20`–`23` the only changes are `CHANGELOG.md`, this report, and a byte-identical restore of `SignupEditDrawer.tsx` after the pre-fix run `24` (it was copied back from a backup inside the repository, and the backup was deleted). The D2 file re-ran 55/55 afterwards.
- The working tree also holds organizer edits made during this session (`roadmap/MESSAGES.md`, `roadmap/tasks/E4-W05.md`, `roadmap/tasks/E4-W07.md`). I did not touch them.

## Organizer verification
Result: changes_requested · 2026-09-25 · organizer (Claude)

Steps 0–10 are in place, and the verification commands are green. The CI failure at `ca84417` is E3-W10's first budget on the slower runner, which E3-W10 round 2 fixed; it is not this task.

The independent review `roadmap/reviews/E3-W07-20260925-0841-codex.md` found two majors and three minors. The majors come with reproductions, and the organizer confirmed all five in the code and the contract:
- `SignupReviewPage.tsx:219` re-parses the localized date text on every render;
- `SignupReviewPage.tsx:274` keeps the last quote after a failed quote or a reload;
- the drawer (`SignupEditDrawer.tsx:532`) offers the SEPA fields only to a SEPA applicant;
- the mock sums a PARTIAL line at its original amount;
- `SignupUpfrontReview.firstMonth` exists only in `pending.json`.

Round 2 — fix exactly these points:
1. **The invoice date is canonical (review #1, R-04-15, S04 §10).** Keep the chosen date as an ISO date, and only format it for display in the current locale. Changing the language must never change the date that is sent. Vitest: enter `10/11/2026` in ca, switch to en; the request still sends `2026-11-10`.
2. **A quote belongs to its plan, price and signup version (review #2, steps 4 and 8, S04 §2).**
   - Bind each `dryRun` result to the plan, price and signup `version` it was asked for, and drop an answer that no longer matches.
   - A failed quote clears the previous one and shows its error on the plan card.
   - A reload discards the old quote.
   - [VALIDA] stays disabled until the quote of the current selection has succeeded.
   
   Vitest:
   - Pack 6 is quoted, then the Pack 10 quote fails: no 135 € breakdown or `max` remains, and [VALIDA] is disabled;
   - a reload after a full payment shows no editable amount.
3. **The drawer can change the payment method (review #3, R-04-19, S04 §2).** Offer a selector with the club's available methods, send the matching patch (a MANUAL applicant can be moved to direct debit with IBAN and holder), and keep the add-dog restrictions (R-04-25). Vitest.
4. **The mock follows E39b (review #4).** A PARTIAL line that a plan change supersedes becomes `CANCELLED`, and a `PAID` correction line for what was collected is added. Test the E39b example: 50 € paid of 100 €, then a 60 € quote leaves 10 € to pay, not 60 €.
5. **Step 8's first-month line is deferred, not done (review #5).** Correct the report: the real core does not publish `SignupUpfrontReview.firstMonth` yet. Keep your mocks-first overlay in `pending.json`.
   - Api **E3-T12** step 5 publishes the field, with the shape you proposed.
   - E3-W08 step 0 adopts it and checks the line against the real core.

### Round 2 verification
Result: verified · 2026-09-25 · organizer (Claude)

- **The five points are fixed.** Points 1–4 have tests that failed on the round-1 code:
  - a `TypedDate` whose ISO value is what is sent, whatever the language;
  - a quote keyed by plan, price and version, with a stale answer dropped and the error on the plan card;
  - the drawer's payment-method selector, checked on the real core (`PATCH` to `MANUAL`, a new quote, «Efectiu», no «Compte no informat»);
  - the E39b mock: 60 € quoted leaves 10 €, and a `PAID` correction line.
  
  Point 5, the first-month line, is recorded as deferred; api E3-T12 publishes the field and E3-W08 adopts it.
- **Evidence:**
  - turbo `--force`: 34/34;
  - `i18n:check`: ok;
  - `pnpm e2e:core E3-W07`: E1/E2 10, E3 4;
  - the complete Docker e2e: 34 + 1 + 27;
  - every heavy run went through the host lock.
- **Your api findings are right.** The seed's `GET /club` has `SEPA_XML` and `MANUAL` disabled, while `GET /signup` offers them, and an ADMIN cannot read `GET /signup`. They go to the new api task **E3-T14**: one rule for the offer (enabled providers), the Cànic seed enabling what it uses, and `paymentMethods` in the D2 view, as you proposed.
- **The Codex round-2 review** (`roadmap/reviews/E3-W07-20260925-1058-codex.md`) has one major and one minor, both narrow. The organizer confirmed them. The api's optimistic lock refuses a decision on a stale version, so the stale window cannot validate wrong data; it can only show it. Under the rule for rounds ≥ 2, they go to the new task **E3-W11**. The organizer opens it when api E3-T14 is published, and E3-W09 depends on it:
  1. a reload must drop the quote and disable [VALIDA] at once, with a retry when the reload fails;
  2. the selector must offer the view's `paymentMethods`, not every provider key.
