---
id: E2-W06
title: Audit and exports UI: /auditoria, member audit tab, change drawer, LastChange, exports drawer
stage: E2
repo: agilityhub-core-web
thread: B
order: 60
status: verified
depends_on: [E2-W01, external:api:E2-T08, external:api:E2-T09]
branch: 
pr: 
updated: 2026-09-10
---

# E2-W06 · Audit and exports UI: /auditoria, member audit tab, change drawer, LastChange, exports drawer

## Objective
Work package WP-14-G (E2 part): the admin sees who changed what, and downloads exports.

## Spec references
S14 §2 (audit screens without mockup: use the D5 pattern), R-14-09…R-14-13 · tests **T-14-26, T-14-27** · `docs/DECISIONS_PENDENTS.md` A17c (Auditoria entry in the Configuració menu).

## Context to load
S14 §2, §6, §11 · `UniversalList` (E2-W01) · this file.

## Steps
0. **Contract refresh (organizer 09-09 21:50)**: api E2-T09 (verified, `2efbbfd`) changed the audit read model: `AuditEntry` read fields (`entityType/entityId/entityLabel`, `actorAccountId/actorName/actorRole`, `impersonatedMemberId/impersonatedName`, `origin`, `changes[] {path, before, after}` masked, optional `details`, `eventIds`), `LastChange` as object-or-null, facets `GET /audit-entries/filter-values`, `GET/POST /audit-entries/export` (xlsx/pdf, 202 above 5 000 rows), `GET /members/{id}/audit-entries` (own + impersonated). The organizer stages the api snapshot as `packages/api-client/openapi/openapi.next.json` (api `a794c7f` or newer); if the file is missing when you start, copy `../agilityhub-core-api/docs/openapi/openapi.json` from a checkout at `a794c7f` or newer. Swap byte for byte, prune `pending.json`, regenerate, fix consumers/fixtures, Ajv suite — then build.
1. `/auditoria` (UniversalList over `/audit-entries`: action, actor, target, date filters), `/abonats/:id` audit tab (member trail incl. impersonated), change drawer (before/after diff with masked sensitive fields), `LastChange` component («últim canvi: … · dd/mm · qui») used in D11 and levels.
2. Exports drawer (global): job list with status, download when READY, `EXPORT_LIMIT` message; entry points from list export menus when the API answers `202`.
3. Menu: «Auditoria» under Configuració (ADMIN only).

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `pnpm turbo run lint typecheck test build` + `pnpm i18n:check`; Playwright T-14-26, 27; screenshots under `roadmap/evidence/E2-W06/`.

## Definition of Done
Audit + exports screens with tests; literals proposed in the report (no mockup).

## Evidence to return
Outputs and screenshots.

---

## Executor report

### Branch / commits

- Working tree only; no branch or commit was created because `.git` is read-only and the publish script owns the commit.

### Evidence (full command outputs)

1. OpenAPI snapshot adoption and generation — exit `0`.

   Command: `COREPACK_HOME=/tmp/agilityhub-corepack pnpm api:generate > roadmap/evidence/E2-W06/00-api-generate.log 2>&1`

   Complete output: `roadmap/evidence/E2-W06/00-api-generate.log`

   ```text
   > agilityhub-core-web@0.0.0 api:generate /Users/jordib/dev/agilityhub/agilityhub-core-web
   > pnpm --filter @agilityhub/api-client api:generate


   > @agilityhub/api-client@0.0.0 api:generate /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client
   > node scripts/generate.mjs

   Generated /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client/src/generated/schema.d.ts from /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client/openapi/openapi.json merged with /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client/openapi/pending.json
   ```

2. Full workspace verification — exit `0`; complete 562-line output: `roadmap/evidence/E2-W06/01-turbo.log`.

   Command: `COREPACK_HOME=/tmp/agilityhub-corepack pnpm turbo run lint typecheck test build > roadmap/evidence/E2-W06/01-turbo.log 2>&1`

   Last 40 lines:

   ```text
   clubs:test:      ✓ uses the compact mark with the runtime club name 308ms
   clubs:test:
   clubs:test:  Test Files  2 passed (2)
   clubs:test:       Tests  19 passed (19)
   clubs:test:    Start at  22:50:50
   clubs:test:    Duration  6.35s (environment 37%, tests 27%, transform 23%, import 7%, setup 6%)
   clubs:test:
   clubs-admin:test: stderr | src/audit/AuditPage.test.tsx > T-14-26 member audit and exports > shows the catalogued EXPORT_LIMIT explanation in the exports drawer
   clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
   clubs-admin:test:
   clubs-admin:test: stderr | src/audit/AuditPage.test.tsx > T-14-26 member audit and exports > routes a queued members list export into the global drawer
   clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
   clubs-admin:test:
   clubs-admin:test: stderr | src/audit/AuditPage.test.tsx > T-14-26 member audit and exports > routes a queued dogs list export into the global drawer
   clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
   clubs-admin:test:
   clubs-admin:test: stderr | src/audit/AuditPage.test.tsx > T-14-26 member audit and exports > links LastChange to the entity-scoped audit trail
   clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
   clubs-admin:test:
   clubs-admin:test:  ✓ src/audit/AuditPage.test.tsx (6 tests) 2948ms
   clubs-admin:test:    ✓ T-14-26 member audit and exports (6)
   clubs-admin:test:      ✓ filters by action and opens the masked before/after change drawer 966ms
   clubs-admin:test:      ✓ opens the exports drawer for a queued job and exposes its READY download 1475ms
   clubs-admin:test:  ✓ src/census/CensusListPage.test.tsx (3 tests) 3150ms
   clubs-admin:test:    ✓ T-03-38 D5 universal member list (2)
   clubs-admin:test:      ✓ renders applied filters and preserves selection when page size changes 1283ms
   clubs-admin:test:      ✓ toggles visible columns and synchronizes the ordered fields to the URL 547ms
   clubs-admin:test:    ✓ T-03-42 D15 translated dog list (1)
   clubs-admin:test:      ✓ renders the approved columns and dog record links 1317ms
   clubs-admin:test:
   clubs-admin:test:  Test Files  6 passed (6)
   clubs-admin:test:       Tests  26 passed (26)
   clubs-admin:test:    Start at  22:50:50
   clubs-admin:test:    Duration  7.25s (environment 34%, tests 31%, transform 23%, setup 6%, import 5%)
   clubs-admin:test:

    Tasks:    26 successful, 26 total
   Cached:    9 cached, 26 total
     Time:    16.522s
   ```

   Earlier attempt: the new `UniversalList` callback shorthands failed `@typescript-eslint/no-confusing-void-expression`; they were converted to block bodies before this final run.

3. i18n parity, vocabulary, unused-key and error-code checks — exit `0`.

   Command: `COREPACK_HOME=/tmp/agilityhub-corepack pnpm i18n:check > roadmap/evidence/E2-W06/02-i18n.log 2>&1`

   Complete output: `roadmap/evidence/E2-W06/02-i18n.log`

   ```text
   > agilityhub-core-web@0.0.0 i18n:check /Users/jordib/dev/agilityhub/agilityhub-core-web
   > node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs

   i18n check passed
   ca: 1516 keys
   es: 1516 keys
   en: 1516 keys
   1015 statically used keys, 0 unused
   245 codes, 0 missing
   ```

4. Playwright T-14-26 and T-14-27 in Docker — exit `0`.

   Command: `docker run --rm --env CI=1 --volume "$PWD:/src:ro" --workdir /work mcr.microsoft.com/playwright:v1.63.0-noble sh -c 'set -eu; tar --exclude=.git --exclude=.turbo --exclude=dist --exclude=node_modules --exclude=playwright-report --exclude=test-results -cf - -C /src . | tar -xf - -C /work; corepack enable; pnpm install --frozen-lockfile >/tmp/pnpm-install.log; pnpm --filter clubs-admin exec playwright test e2e/audit.spec.ts --workers=1' > roadmap/evidence/E2-W06/03-e2e.log 2>&1`

   Complete output: `roadmap/evidence/E2-W06/03-e2e.log`

   ```text
   ! Corepack is about to download https://registry.npmjs.org/pnpm/-/pnpm-9.15.9.tgz
   (node:20) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
   (Use `node --trace-deprecation ...` to show where the warning was created)
   .                                        |  WARN  Unsupported engine: wanted: {"node":">=22 <23"} (current: {"node":"v24.20.0","pnpm":"9.15.9"})
   (node:104) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
   (Use `node --trace-deprecation ...` to show where the warning was created)
   [WebServer] (node:214) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
   [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)

   Running 2 tests using 1 worker
   ··
     2 passed (13.2s)
   ```

5. Visual evidence captured at 1280 px and inspected against the D5 layout pattern: `roadmap/evidence/E2-W06/member-audit-1280.png`, `change-drawer-1280.png`, `exports-drawer-1280.png`, `global-audit-1280.png`, `audit-ca-1280.png`, `audit-es-1280.png`, `audit-en-1280.png`.

### Files changed

- Audit and export UI: `apps/clubs-admin/src/audit/*`, `apps/clubs-admin/src/App.tsx`, `apps/clubs-admin/src/main.tsx`, `apps/clubs-admin/src/styles.css`.
- Audit links and export entry points: `apps/clubs-admin/src/census/CensusListPage.tsx`, `apps/clubs-admin/src/census/CensusRecordPage.tsx`, `apps/clubs-admin/src/catalogs/ParameterSettings.tsx`, `apps/clubs-admin/src/catalogs/SettingsPage.tsx`, `packages/ui/src/universal-list.tsx`, `packages/ui/src/universal-list.css`.
- Contract and mocks: adopted `packages/api-client/openapi/openapi.next.json` as `openapi.json`, removed the staged `openapi.next.json`, regenerated `src/generated/schema.d.ts`, and updated API fixture validation, catalogs, handlers and handler-count tests; added `audit-entries.json` and `export-jobs.json` fixtures. `pending.json` required no pruning because it does not overlap the adopted snapshot.
- Locales: added `admin-audit.json` in `ca`, `es` and `en`; registered the namespace; removed superseded audit/last-change placeholders from census/settings catalogs; taught the unused-key checker about the dynamic audit-action prefix.
- Tests and evidence: `apps/clubs-admin/src/audit/AuditPage.test.tsx`, `apps/clubs-admin/e2e/audit.spec.ts`, `apps/clubs-admin/src/App.test.tsx`, and `roadmap/evidence/E2-W06/*`.
- Documentation: `CHANGELOG.md` and this executor report.

### Rules and tests implemented (R-xx-nn / T-xx-nn)

- R-14-09 / R-14-11: ADMIN-only global and member audit routes consume the published universal-list read models, including own and impersonated records, facets, query filters, saved views, and entity-scoped links.
- R-14-10: the detail drawer renders the API-provided `changes[]` before/after values without attempting to reveal masked sensitive data.
- R-14-11: reusable `LastChange` renders the full D11 history link and a compact level-row variant; `null` produces no stale attribution.
- R-14-12: XLSX/PDF audit and member/dog list exports handle synchronous files, `202` jobs, queued/running polling, READY download links, and the catalogued `EXPORT_LIMIT` explanation in the global drawer.
- R-14-13: the published member-data export contract is present in the adopted generated types; this task's explicit UI steps and Definition of Done cover audit and list-export surfaces, so no new RGPD request entry point was added.
- T-14-26: six component cases plus one Playwright flow cover action filtering, masked diffs, D10/member navigation, entity-scoped `LastChange`, asynchronous exports, READY downloads, list entry points, and `EXPORT_LIMIT`.
- T-14-27: the Playwright locale flow captures the audit/export surface in `ca`, `es` and `en`, exercises PDF job state, and asserts that untranslated i18n keys are absent; `pnpm i18n:check` covers parity and forbidden vocabulary.

### Assumptions

- The audit screens have no approved mockup, so the implementation follows the mandated D5 `UniversalList`, existing drawer, token and shell patterns. All new Catalan, Spanish and English literals are proposals recorded below.
- The full `LastChange` sentence is used where D11 has room; level rows use the same component's compact date/actor link to avoid repeating a long sentence in every row.
- `UniversalList` keeps its direct-download default for existing consumers and accepts an optional callback where the application must route `202` responses into the shared exports drawer.
- T-14-27 is scoped here to the audit/list-export UI delivered by E2-W06. D1, mobile profile screen 12 and API-produced PDF/ZIP document rendering are not changed by this task.

### Questions / catalog proposals

- Proposed UI literals are the new `admin-audit` catalog in `ca`, `es` and `en`: audit headings/columns/filters, masked-change drawer labels, `LastChange`, export job states, progress/expiry copy, and `EXPORT_LIMIT` guidance. No new parameter, event, notification or error code is proposed.
- No blocking questions. The implementation uses the published `EXPORT_LIMIT`, `EXPORT_TOO_LARGE` and `EXPORT_EXPIRED` contracts unchanged.

### Round 2

#### Evidence

Earlier attempt: the complete unbounded `pnpm e2e` run exhausted the Docker runner while the three apps ran concurrently; T-03-35 passed, and the run was repeated serially by app. Full output: `roadmap/evidence/E2-W06/04-e2e-full-round2-run1.log`.

Earlier attempt: a follow-up browser-download event assertion timed out for the service-worker-backed blob; the requested export-response URL assertions were kept and the unsupported event assertion was removed. Full output: `roadmap/evidence/E2-W06/05-e2e-full-round2-run1.log`.

1. Full workspace verification — exit `0`; complete 619-line output: `roadmap/evidence/E2-W06/08-turbo-round2.log`.

   Command: `COREPACK_HOME=/tmp/agilityhub-corepack pnpm turbo run lint typecheck test build > roadmap/evidence/E2-W06/08-turbo-round2.log 2>&1`

   Last 40 lines:

   ```text
   clubs:test:
   clubs:test: stderr | src/SignupPage.test.tsx > T-04-31 signup family lookup > keeps the dog draft when returning from the family step
   clubs:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
   clubs:test:
   clubs:test: stderr | src/App.test.tsx > T-03-40 mobile own dogs > renders own dogs, saves the note, shows task totals and gates TASKS
   clubs:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
   clubs:test:
   clubs:test: stderr | src/SignupPage.test.tsx > T-04-32 signup payment, checkout and add-dog mode > renders API totals and payment conditions and submits only after privacy consent
   clubs:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
   clubs:test:
   clubs:test: stderr | src/App.test.tsx > T-03-40 mobile own dogs > renders own dogs, saves the note, shows task totals and gates TASKS
   clubs:test: [BrandingProvider] Club Mínim: primary and onPrimary contrast (3.32:1) is below WCAG AA.
   clubs:test:
   clubs:test: stderr | src/App.test.tsx > T-03-41 mobile own data > keeps identity read-only, resolves towns, maps READ_ONLY and gates billing
   clubs:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
   clubs:test:
   clubs:test: stderr | src/SignupPage.test.tsx > T-04-32 signup payment, checkout and add-dog mode > redirects through checkout when Stripe is enabled
   clubs:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
   clubs:test:
   clubs:test: stderr | src/SignupPage.test.tsx > T-04-32 signup payment, checkout and add-dog mode > uses the member plan and payment method and posts the add-dog flow
   clubs:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
   clubs:test:
   clubs:test: stderr | src/App.test.tsx > T-03-41 mobile own data > keeps identity read-only, resolves towns, maps READ_ONLY and gates billing
   clubs:test: [BrandingProvider] Club Mínim: primary and onPrimary contrast (3.32:1) is below WCAG AA.
   clubs:test:
   clubs:test:  ✓ src/App.test.tsx (17 tests) 1391ms
   clubs:test:  ✓ src/SignupPage.test.tsx (10 tests) 1493ms
   clubs:test:    ✓ T-04-29 signup person and draft (3)
   clubs:test:      ✓ persists a current draft, expires it after 24 hours and maps identity states 644ms
   clubs:test:
   clubs:test:  Test Files  3 passed (3)
   clubs:test:       Tests  29 passed (29)
   clubs:test:    Start at  08:50:45
   clubs:test:    Duration  3.97s (environment 40%, tests 32%, transform 15%, setup 7%, import 6%)
   clubs:test:

    Tasks:    26 successful, 26 total
   Cached:    26 cached, 26 total
     Time:    28ms >>> FULL TURBO
   ```

2. i18n parity, vocabulary, unused-key and error-code checks — exit `0`; complete output: `roadmap/evidence/E2-W06/09-i18n-round2.log`.

   Command: `COREPACK_HOME=/tmp/agilityhub-corepack pnpm i18n:check > roadmap/evidence/E2-W06/09-i18n-round2.log 2>&1`

   ```text
   > agilityhub-core-web@0.0.0 i18n:check /Users/jordib/dev/agilityhub/agilityhub-core-web
   > node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs

   i18n check passed
   ca: 1629 keys
   es: 1629 keys
   en: 1629 keys
   1128 statically used keys, 0 unused
   249 codes, 0 missing
   ```

3. Complete Playwright suite in Docker, pass 1 — exit `0`; complete 312-line output: `roadmap/evidence/E2-W06/06-e2e-full-round2-pass1.log`.

   Command: `docker run --rm --env CI=1 --volume "$PWD:/src:ro" --workdir /work mcr.microsoft.com/playwright:v1.63.0-noble sh -c 'set -eu; tar --exclude=.git --exclude=.turbo --exclude=dist --exclude=node_modules --exclude=playwright-report --exclude=test-results -cf - -C /src . | tar -xf - -C /work; corepack enable; pnpm install --frozen-lockfile; pnpm e2e --concurrency=1' > roadmap/evidence/E2-W06/06-e2e-full-round2-pass1.log 2>&1`

   Last 40 lines:

   ```text
   id:build: - Using dynamic import() to code-split the application
   id:build: - Use build.rolldownOptions.output.codeSplitting to improve chunking: https://rolldown.rs/reference/OutputOptions.codeSplitting
   id:build: - Adjust chunk size limit for this warning via build.chunkSizeWarningLimit.
   id:build: ✓ built in 140ms
   clubs:e2e: cache bypass, force executing a2381d91209c1957
   clubs:e2e:
   clubs:e2e: > clubs@0.0.0 e2e /work/apps/clubs
   clubs:e2e: > playwright test
   clubs:e2e:
   clubs:e2e: [WebServer] (node:610) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
   clubs:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
   clubs:e2e:
   clubs:e2e: Running 21 tests using 5 workers
   clubs:e2e: ·····················
   clubs:e2e:   21 passed (11.2s)
   clubs-admin:e2e: cache bypass, force executing 4afc70d28e39f9a6
   clubs-admin:e2e:
   clubs-admin:e2e: > clubs-admin@0.0.0 e2e /work/apps/clubs-admin
   clubs-admin:e2e: > playwright test
   clubs-admin:e2e:
   clubs-admin:e2e: [WebServer] (node:1714) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
   clubs-admin:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
   clubs-admin:e2e:
   clubs-admin:e2e: Running 24 tests using 8 workers
   clubs-admin:e2e: ························
   clubs-admin:e2e:   24 passed (14.0s)
   id:e2e: cache bypass, force executing 3a558131735cfd7d
   id:e2e:
   id:e2e: > id@0.0.0 e2e /work/apps/id
   id:e2e: > playwright test
   id:e2e:
   id:e2e:
   id:e2e: Running 1 test using 1 worker
   id:e2e: ·
   id:e2e:   1 passed (2.8s)

    Tasks:    6 successful, 6 total
   Cached:    0 cached, 6 total
     Time:    36.558s
   ```

4. Complete Playwright suite in Docker, pass 2 — exit `0`; complete 299-line output: `roadmap/evidence/E2-W06/07-e2e-full-round2-pass2.log`.

   Command: `docker run --rm --env CI=1 --volume "$PWD:/src:ro" --workdir /work mcr.microsoft.com/playwright:v1.63.0-noble sh -c 'set -eu; tar --exclude=.git --exclude=.turbo --exclude=dist --exclude=node_modules --exclude=playwright-report --exclude=test-results -cf - -C /src . | tar -xf - -C /work; corepack enable; pnpm install --frozen-lockfile; pnpm e2e --concurrency=1' > roadmap/evidence/E2-W06/07-e2e-full-round2-pass2.log 2>&1`

   Last 40 lines:

   ```text
   clubs-admin:build:
   clubs-admin:build: [INEFFECTIVE_DYNAMIC_IMPORT] ../../packages/i18n/src/locales/es/shell.json is dynamically imported by ../../packages/i18n/src/resources.ts but also statically imported by ../../packages/i18n/src/catalog.ts, dynamic import will not move module into another chunk.
   clubs-admin:build:
   clubs-admin:build: ✓ built in 216ms
   id:e2e: cache bypass, force executing 3a558131735cfd7d
   id:e2e:
   id:e2e: > id@0.0.0 e2e /work/apps/id
   id:e2e: > playwright test
   id:e2e:
   id:e2e:
   id:e2e: Running 1 test using 1 worker
   id:e2e: ·
   id:e2e:   1 passed (3.3s)
   clubs:e2e: cache bypass, force executing a2381d91209c1957
   clubs:e2e:
   clubs:e2e: > clubs@0.0.0 e2e /work/apps/clubs
   clubs:e2e: > playwright test
   clubs:e2e:
   clubs:e2e: [WebServer] (node:840) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
   clubs:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
   clubs:e2e:
   clubs:e2e: Running 21 tests using 5 workers
   clubs:e2e: ·····················
   clubs:e2e:   21 passed (12.3s)
   clubs-admin:e2e: cache bypass, force executing 4afc70d28e39f9a6
   clubs-admin:e2e:
   clubs-admin:e2e: > clubs-admin@0.0.0 e2e /work/apps/clubs-admin
   clubs-admin:e2e: > playwright test
   clubs-admin:e2e:
   clubs-admin:e2e: [WebServer] (node:1937) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
   clubs-admin:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
   clubs-admin:e2e:
   clubs-admin:e2e: Running 24 tests using 8 workers
   clubs-admin:e2e: ························
   clubs-admin:e2e:   24 passed (15.3s)

    Tasks:    6 successful, 6 total
   Cached:    0 cached, 6 total
     Time:    41.644s
   ```

#### Files changed

- `apps/clubs-admin/src/census/CensusListPage.tsx`: synchronous export responses now create, click and remove a temporary download link, with deferred object-URL cleanup; `202` responses continue to open the global exports drawer.
- `apps/clubs-admin/e2e/census-lists.spec.ts`: T-03-35 keeps the empty and error-recovery checks and now clicks the Excel action, waits for the API response, and asserts `format=xlsx`, the exact visible `columns`, `200`, and the attachment filename.
- `.gitignore`: allow the E2-W06 verification logs required by the roadmap evidence rule to be published.
- `CHANGELOG.md`, `roadmap/tasks/E2-W06.md`, and `roadmap/evidence/E2-W06/*.log`: round-2 handoff and verification evidence.

#### Rules and tests implemented

- R-14-12: the explicit Excel/PDF buttons call the list export API; synchronous `200` files use the response filename and asynchronous `202` jobs retain the shared drawer flow already covered for members and dogs by T-14-26.
- T-03-35: verifies the complete export request URL state after its existing empty-list and invalid-filter recovery assertions.
- Complete Docker Playwright coverage ran twice: 21 Clubs, 24 Clubs Admin (including T-03-35), and 1 ID test passed in each run.

#### Assumptions

- The three app projects are serialized with Turbo `--concurrency=1` inside Docker because the unbounded run exhausted the constrained runner. Playwright still uses each project's configured workers, and every project/test runs.
- The publish script owns the next commit and CI URL. The organizer can re-check `.roadmap-ci.json` after publication as requested.

#### Questions / catalog proposals

- No questions and no new parameter, event, notification or error-code proposal.

## Organizer verification
Result: verified · 2026-09-10 · organizer (Claude) — round 2 accepted

Round 2 checked: report + `roadmap/evidence/E2-W06/` (`06-e2e-full-round2-pass1.log` and `07-…-pass2.log`: complete three-project `pnpm e2e` in the Playwright image, twice — clubs 21, clubs-admin 24 (T-03-35 included), id 1; `08-turbo-round2.log` 26/26; `09-i18n-round2.log` green) · `git show ce27746` (`CensusListPage.tsx`: explicit «Excel»/«PDF» actions call the list export API, a `200` file is downloaded through a temporary link with the response filename and deferred object-URL cleanup, `202` opens the shared exports drawer — S14 R-14-12; `census-lists.spec.ts` T-03-35 asserts `format=xlsx`, the visible `columns`, the `200` and the attachment filename after the empty/error-recovery checks) · **CI on `main` green at `c4f62d5`** (unit + Playwright; it contains this round's code — `0108e92` only adds the report and evidence) · the organizer's sandbox run of `c4f62d5` (lint/typecheck 16/16, test/build 14/14, i18n:check) covers this code as well.

Polish (f) and (g) stay in E2-W07 step 4. E2-W07 opens now (gate E2 front).

---
_Round 1 (2026-09-09, changes_requested) — kept for the record:_


Checked: report + `roadmap/evidence/E2-W06/` (repository gate 26/26, i18n 1516 keys, `audit.spec.ts` green in Docker, seven screenshots) · `git show a090d1c` (snapshot `a794c7f` adopted; `/auditoria`, member audit tab, masked change drawer «···· 2231 → ···· 8867», `LastChange` link, global exports drawer with job states/READY download/`EXPORT_LIMIT`, «Auditoria» menu entry; `admin-audit` ca/es/en) · independent run of `a090d1c` in the organizer sandbox: 26/26, i18n 0 missing. The audit/exports UI is accepted as built (screens without mockup: D5 pattern; Josep validates literals in staging).

**Round 2 — CI on `main` is red at `a090d1c`** (run of 21:01Z, Playwright job): `apps/clubs-admin/e2e/census-lists.spec.ts:101 › E2-W01 census universal lists › T-03-35 exposes empty, error recovery, and visible-column export states` — `getByRole('link', { name: 'Excel' })` not found: this task changed the list export entry (the «Excel · PDF» menu now goes through the exports drawer / job flow) and only `e2e/audit.spec.ts` was run in Docker, so the E2-W01 expectation was not updated.

1. Keep the new behaviour (S14 R-14-12: `200` file → direct download, `202` → job in the exports drawer) but make it explicit in the UI: the «Excel»/«PDF» actions of the list menu request the export; on `200` they trigger the download with the same `format`/`columns` query (assert that URL in the test), on `202` they open the drawer with the queued job. Update `T-03-35` in `census-lists.spec.ts` accordingly (keep its empty/error-recovery assertions), plus the Vitest of `UniversalList` if it still asserts an `<a href>`.
2. Run the **whole** `pnpm e2e` (three projects) in the Playwright Docker image, twice, and paste the tails — this is now the rule for every web task (AGENTS.md updated by the organizer): a task is not `awaiting_verification` until the complete e2e suite is green locally.
3. After publication the organizer re-checks `.roadmap-ci.json`; verified with `main` green.

Polish carried to E2-W07 step 4 (not for this round): (f) shared `fmtDateTime` in the audit list («31/7/26» → «31/07/2026 18:30»); (g) change drawer path → field label mapping.
