---
id: E2-W08
title: Adopt the api E2 contract snapshot (census, catalogs, parameters, audit/exports) — replace the pending.json entries, align fixtures and consumers
stage: E2
repo: agilityhub-core-web
thread: B
order: 45
status: not_open
depends_on: [E1-W06, E2-W01, E2-W02, E2-W04, external:api:E2-T01 (verified snapshot)]
branch: 
pr: 
updated: 2026-09-06
---

# E2-W08 · Adopt the api E2 contract snapshot — replace the pending.json entries, align fixtures and consumers

## Objective
The api has published and the organizer has verified the E2 contract (api task E2-T01: 104 paths, 223 schemas — members, dogs, family groups, `/me/*`, saved views, exports, levels/rings/instructors/administrators/plans/prices/faq, parameters/club settings, audit entries). The organizer placed that snapshot at **`packages/api-client/openapi/openapi.next.json`** (staged on purpose: the generator's redefinition guard would break `main` if it replaced `openapi.json` while `pending.json` still defines the same paths). Swap it in, delete every now-published entry from `pending.json`, regenerate the types and make every E2 screen and fixture conform to the real contract.

## Spec references
S03 §6, S05 §6, S02 §6, S14 §6 (the tables the api implemented) · `docs/specs/00-transversal/CONVENCIONS_API.md` §3–5 (list contract: `ListPage {items, page, size, totalItems, totalPages, appliedFilters}`, `x-filterable`, `x-sortable`, `x-filter-operators`, `x-columns[{key, defaultVisible, module?, parameter?}]`, `x-exportable`) · E1-W06 conventions (authoritative snapshot never edited by hand; `pending.json` only for unpublished contracts; Ajv fixture test).

## Context to load
`packages/api-client/openapi/{openapi.json, openapi.next.json, pending.json}` · `packages/api-client/scripts/generate.mjs` · `packages/api-client/src/mocks/{handlers.ts, scenarios.ts, fixtures/*}` · `packages/ui/src/universal-list.tsx` · `apps/clubs-admin/src/census/*` and the E2-W02/E2-W04 screens · this file.

## Fixed conventions
- `mv openapi.next.json openapi.json` (byte for byte; never edit). `pending.json` must end up **empty of anything the snapshot defines** (paths and component schemas); the guard keeps it honest. Anything left in `pending.json` must be listed in the report with the spec row that justifies it.
- Wire names come from the snapshot — e.g. list responses `ListPage*`, `MemberListItem`/`DogListItem` column keys (`fullName`, `dogs`, `plan`, `displayStatus`, … / `name`, `breed`, `level`, `owner`, `freeTraining`, `licenses`, `displayStatus`, …), `x-filter-operators`, `SavedView {listKey, name, columns[], filters[], sort, shared, version}` (no default flag: the default view stays a browser preference), `Member` (`firstName`, `lastName1`, `lastName2`, `contactEmails[]`, `phones[]`, `paymentMethod` masked/write-only IBAN, `consents`, `bookingBlock`, `roles`, `version`), `Dog` (`levelId`, `freeTrainingOverride`, `licenses[]`, `status`, `version`), `MemberInstructorView` for the INSTRUCTOR projection.
- Spec changes of 06/08/09-09 to absorb here (the next api snapshot carries them): **A5** no `agilityhubLevel` on levels (delete the `enums:agilityhubLevel.*` keys and any column/select); **B10** `Plan.billingMode` (`MONTHLY_FEE · MAINTENANCE`) — D10 «Mode de facturació» read-only from the plan, D8 plan form select; **B19** no warning on level change; `Dog.handlerName` (D15 column «Guia», dog record field, `PATCH /dogs/{id}`), `licenses[].category/division`.
- Errors: `STALE_VERSION` (409) on versioned PATCH/PUT, `MEMBER_ERASED` (409, new in the catalog — translation already present), `FAMILY_GROUP_MEMBER_ALREADY_IN_GROUP`, `SAVED_VIEW_NAME_TAKEN`, `EXPORT_LIMIT` (422).
- Every JSON fixture used by MSW for these endpoints is validated by the Ajv contract test against the snapshot schema (extend the mapping); TypeScript fixtures (`census.ts`) must be typed with the generated types.

## Steps
1. Swap the snapshot; prune `pending.json`; `pnpm --filter @agilityhub/api-client api:generate`; commit-free check that `src/generated` changed only by the contract.
2. Fix typecheck across `packages/ui` (UniversalList metadata types), `apps/clubs-admin` (D5/D15 lists, D10 member record, dog record, D16/D17/D11/D8 screens), `apps/clubs` (screens 13/28 if E2-W03 already landed), MSW handlers/scenarios, fixtures.
3. Where the real contract differs from the mock (names, nesting, pagination, operators), the contract wins: adapt the UI without changing what the mockup shows; list every such change in the report.
4. Re-run unit + e2e suites; refresh screenshots only if something visible changed.

## Verification (run all; paste full output in the report)
- `pnpm --filter @agilityhub/api-client api:generate && git status --short packages/api-client` → generated files only, no `openapi.next.json` left.
- `pnpm turbo run lint typecheck test build`, `pnpm i18n:check`, `pnpm e2e:docker` → all green.
- `node -e` (or a short script) printing the paths that remain in `pending.json` (expected: none, or a justified short list).

## Definition of Done
Real E2 contract in `openapi.json`; `pending.json` pruned; all E2 screens typed against the generated schema; fixtures validated; suites green.

## Evidence to return
Outputs above + the list of contract-driven UI changes.

---

## Executor report
_(not started)_

### Branch / commits
### Evidence (full command outputs)
### Files changed
### Rules and tests implemented (R-xx-nn / T-xx-nn)
### Assumptions
### Questions / catalog proposals

## Organizer verification
_(pending)_
