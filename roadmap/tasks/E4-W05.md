---
id: E4-W05
title: E4 integration (front): adopt the api E4 snapshot, run T-06-28 and T-07-32 end to end against the published core with the E4 demo seed (generate → validate → cancel with registrants → 10/23; publish an activity → block on D4 → 04 → register → waitlist promotion), screenshots with real data, gate E4 (front) evidence
stage: E4
repo: agilityhub-core-web
thread: B
order: 50
status: verified
depends_on: [E4-W01, E4-W02, E4-W03, E4-W04, E4-W07, E4-W08, E4-W09, E4-W10, E2-W07, external:api:E4-T05]
branch:
pr:
updated: 2026-09-26
---

# E4-W05 · E4 integration (front): adopt the api E4 snapshot, run T-06-28 and T-07-32 end to end against the published core with the E4 demo seed (generate → validate → cancel with registrants → 10/23; publish an activity → block on D4 → 04 → register → waitlist promotion), screenshots with real data, gate E4 (front) evidence

## Objective
Work packages S06 P7 and S07 P6 (front halves): the E4 screens (D3, D3b, D4, D4b, D4c, D7, 10, 23, the activities block/detail/rows) run in the browser against the **real published core** with the E4 demo seed of api E4-T05 (templates «Setmana A»/«Setmana B»/«Dissabtes», a validated week and a draft week, one ring block, one inconsistency, the four D7 activities — one with a class conflict —, registrants and a waitlist); the S06/S07 contract is adopted from the real snapshot (no S06/S07 entry left in `pending.json`); the gate E4 (front) evidence is produced for the organizer.

## Spec references
S06 §12 P7 and **T-06-28** (E2E half: «filtre Esborrany obre la primera setmana amb esborranys; targeta de validació amb recompte; [VALIDAR LA SETMANA] → totes actives; anul·lar activa amb inscrits obre el modal amb la llista i el botó «ANUL·LA I AVISA ELS 4 ALUMNES»; [ELIMINA] passa pel mateix modal; sense text el botó queda desactivat»), R-06-07, R-06-08, R-06-10 (the seeded Wednesday 18:50 «B+C» with 4 registrants and 2 waiting), R-06-11, R-06-12 · S07 §12 P6 and **T-07-32** («admin crea, publica (amb un conflicte forçat) i veu els bloquejos a D4; l'alumne veu l'activitat a 04, s'hi inscriu, la veu a 03, anul·la dins termini; l'admin la cancel·la…; el mateix en `es`»), R-07-05, R-07-08, R-07-09, R-07-11 · api `E4-T05` (seed contents, `--week-start`, `bin/e4-smoke` sequence: the states the front asserts) · gate E4 in `roadmap/ROADMAP.md` (organizer-owned) and in the backlog: «l'admin genera i valida una setmana des de les plantilles (A/B + dissabtes); alumne i instructor veuen 10/23; D4c anul·la una classe amb inscrits ficticis; una activitat publicada bloqueja la pista i apareix a 04» · `AGENTS.md` rule 4 (contract files), `roadmap/README.md` (evidence rule).

## Context to load
`roadmap/tasks/E1-W04.md`, `E2-W07.md` (the real-core stack and its generalisation: `scripts/e2e-core.sh` with per-task evidence directory/project name variables, `scripts/core-stack/docker-compose.yml`, `scripts/core-stack/club-canic.yaml`, `e2e/core/playwright.config.ts`, `e2e/core/e1-core.spec.ts` + `e2-core.spec.ts` helpers: `login`, `mobileContext`/`desktopContext`, mailbox reading, `E1_CORE_PASSWORD`) · `roadmap/tasks/E3-W03.md` (adoption step and its blocker precedent when the image/snapshot is missing) · the E4 screen tasks E4-W01…W04 (routes, literals, test ids, data attributes) · the staged `packages/api-client/openapi/openapi.next.json` (organizer: api E4-T05 snapshot; the commit message names the api commit) · `packages/api-client/openapi/pending.json`, `scripts/generate.mjs`, `src/mocks/fixtures.contract.test.ts` · api `E4-T05` task file (read-only, in the api repo or pasted by the organizer in `MESSAGES.md`): seed option `--week-start`, expected counts, the demo bookings adapter (`counters` of the Wednesday 18:50 class), the four activities and their states · this file.

## Steps
0a. **Seed arrangement (organizer 19-09, after api E4-T05's block)**: the planning demo is anchored relative to the run date — current week untouched; **week +1** generated from «Setmana A» + «Dissabtes» and kept as DRAFT (D4b), with the Wednesday 18:50 class carrying the 4 fictional registrants; **week +2** generated and validated, holding the «Torneig d'Estiu» Saturday conflict and the maintenance block; generation candidates propose week +3. Read `seeds/README.md` of the api (shipped in the image at `/app/seeds/README.md` if present, else the E4-T05 report) for the final arrangement and select rows by **state**, never by dates or names.
0. **Contract adoption**: swap the staged `openapi.next.json` (api E4-T05 snapshot = E4-T01…T04 contracts as implemented) into `openapi.json` byte for byte (`cmp`; record both `sha256`), `pnpm api:generate`, **prune every S06/S07 path and schema from `pending.json`** (whatever E4-W01…W04 added mocks-first; only unrelated pending contracts may remain — list what stays), align the MSW fixtures and consumers with the published names (Ajv suite green), `pnpm turbo run lint typecheck test build`. If the staged file is absent or the published image lacks the E4 seed (`docker run --rm --entrypoint /bin/sh ghcr.io/jboixtcm/agilityhub-core-api:main -c 'ls /app/seeds'` must list `demo-canic.yaml`; the seed must accept `--week-start`), set the task `blocked` with a `@organizer` entry in `MESSAGES.md` (E2-W07 precedent) and stop.
1. **Stack** (`scripts/core-stack/docker-compose.yml`, generalised by E2-W07): the `seed` service runs the two `club:apply` commands and then `java -jar /app/app.jar --core.command=seed:demo --club=canic --seed=42 --week-start=<Monday of the club-local current week>` (compute the Monday in `scripts/e2e-core.sh` with `TZ=Europe/Madrid date`; pass it as `E4_WEEK_START` to the compose file and to Playwright), `SEED_PASSWORD` = `E1_CORE_PASSWORD` (generated, never printed); evidence directory `roadmap/evidence/E4-W05`, project name `agilityhub-e4-w05`. Keep the E1/E2/E3 core specs runnable in the same run (`pnpm e2e:core` executes every `e2e/core/*.spec.ts`).
2. **`e2e/core/e4-core.spec.ts`** (serial, real core, seeded fictional accounts of the club seed: one ADMIN, one INSTRUCTOR, one eligible ACTIVE MEMBER — take them from `scripts/core-stack/club-canic.yaml`/the demo seed README, never invent accounts; select rows by state, time and data attributes (`data-class-state`, `data-time`, `data-ring`, `data-activity-state`) — never by the mockup's first names):
   (a) **T-06-28 E2E — templates and generation** (admin, 1280): `/plantilles` shows three templates in the tabs; «Setmana B» renders the yellow note «Incoherència: …» and [GENERAR CLASSES] disabled with «bloquejat: 1 incoherència a la plantilla»; switch to «Setmana A» → the generation card proposes the first week after the seeded draft (`proposed` candidate) → [GENERAR CLASSES] → confirm «Es generaran com a esborrany les classes de la setmana del …» → toast with `classCount` → the SETMANES table shows the new row with «Classes generades» filled and «Validades» «—»; D3b of «Setmana A» Monday shows only the used rings.
   (b) **T-06-28 E2E — calendar, validation, cancellation** (admin): `/calendari` → segment «Esborrany» opens the **first** week with drafts (the seeded draft week: assert its `startDate` equals `E4_WEEK_START + 7 days`) → validation card with «{n} classes en esborrany · cap incoherència» → [VALIDAR LA SETMANA] → confirm → `?estat=actives` of that week shows the same classes without dashed outline; back to the current week (Actives): the warning card lists the seeded «assignat a dues pistes alhora» inconsistency; select the Wednesday 18:50 class with `data-class-state="ACTIVE"` whose cell reads «4/5 +2» (the R-06-10 seed) → [ANUL·LA LA CLASSE] → the D4c modal lists 4 rows and the button «ANUL·LA I AVISA ELS 4 ALUMNES» is **disabled** until a text is typed → [Torna enrere] → [ELIMINA] opens the same modal → type a fictional text → confirm → the cell is dimmed «anul·lada · pel club/eliminada» and «Anul·lades» lists it; assert the api answered 200 on `POST /class-sessions/{id}/cancellation` (Playwright response hook). Ring block: [Bloqueja pista] on a free slot → 201 and the cell «{Pista} bloquejada»; the same over an active class → 409 `RING_BLOCK_CONFLICT` shown in the drawer.
   (c) **Students and instructor see it** (375): member `/avui?date=<Wednesday>` no longer shows the cancelled class and never shows counts; `/avui?date=<Monday of the newly validated week>` shows the validated classes; instructor `/instructor/avui?date=<Wednesday>` shows the block cell «Bloq. …», the cancelled class dimmed and `n/n` counts on the others; screen 25 for class rows depends on S10's `/me/history` (E6) — state that it is out of scope here.
   (d) **T-07-32 E2E — activities** (admin, 1280): `/activitats` lists the four seeded activities with the expected «Estat» chips (three «publicada», one «esborrany»); the seeded «Torneig d'Estiu 2026» is already published with the class conflict resolved by the seed → its D4 Saturday shows the five «Activitat · Torneig d'Estiu 2026» cells (`ringBlocks[].activityTitle`). Then create a new activity «Seminari E4 {runId}» (`SEMINAR`) dated on the Saturday of the week generated in (a) (week +2: the seeded Torneig already blocks every ring on the Saturday of week +1), 18:30–20:30 on Central (the slot of the «Dissabtes» template class) → «Inscripció» from today to the day before, «Places: 5», «Llista d'espera: sí» → [DESA] → [PUBLICA] → the conflict dialog lists that Saturday class → check «Anul·la les classes en conflicte…» (+ text if it has registrants) → [PUBLICA I APLICA] → chip «publicada»; D4 of that week shows «Activitat · Seminari E4 {runId}» on Central and the class «anul·lada».
   (e) **Member flow** (375, `ca`): `/reservar` block «Activitats» shows «Seminari E4 {runId} · ds {d} · 18:30» with «5 places» → detail → [INSCRIU-M'HI] → `/inici` row «Seminari E4 {runId} · inscrita · Dissabte {d} · 18:30–20:30 · Central» (no dog) → detail → [ANUL·LA LA INSCRIPCIÓ] (within `cancellableUntil`) → confirm → the row disappears. Waitlist promotion on the seeded full «Seminari de handling» (12/12 + 2 waiting per E4-T05): member → detail shows [APUNTA'M A LA LLISTA D'ESPERA] → dialog → toast «Ets a la llista d'espera» → admin `/activitats/{id}/inscrits` shows the member as «en llista d'espera (3)» → admin raises «Places» 12 → 13 → [DESA] → the registrants list shows the former position-1 registrant «inscrita» (FIFO, R-07-08: one promotion, never the member who joined last) and the member still «en llista d'espera»; finally the admin cancels «Seminari E4 {runId}» through the modal with a text → 200 → the member's `/inici` has no row for it and the registrants list reads «cancel·lada pel club» (the N-32c feed assertion belongs to screen 11, E7 — say so). Repeat (e) with the `es` locale (`localStorage` `agilityhub.locale = "es"`): the same flow with the `es` literals.
   (f) **Cleanup for idempotence**: the run's activity is cancelled at the end (never deleted physically), the ring block is cancelled; a second `pnpm e2e:core` run must pass (`runId` suffix on titles).
3. **Screenshots with real data** under `roadmap/evidence/E4-W05/`: 1280 — `D3-plantilles-core-1280.png` (Setmana B with the note), `D3b-dia-core-1280.png`, `D4-actives-core-1280.png`, `D4b-esborrany-core-1280.png`, `D4c-modal-core-1280.png`, `D4-anullades-core-1280.png`, `D7-llistat-core-1280.png`, `D7-manteniment-core-1280.png`, `D7-conflictes-core-1280.png`, `D7-inscrits-core-1280.png`; 375 — `10-avui-core-375.png`, `23-visio-global-core-375.png`, `04-activitats-core-375.png`, `activitat-detall-core-375.png`, `03-inscrita-core-375.png`; compare with `docs/pantalles/` and list every deviation you notice (do not fix them here unless they are one-line literal fixes — list those in the report).
4. **Contract discrepancies**: any difference between what the published core returns and the generated types (a required field missing, a status different from `CATALEG_ERRORS.md`, a literal in `message` you had to rely on) is reported as a `@organizer` proposal in the report and `MESSAGES.md` (E1-W04 precedent: INC-06) — never patched by hand in `openapi.json`.
5. **Gate E4 (front) evidence** in the report, one line per gate item with the proving spec step/screenshot: week generated from A/B + Saturdays and validated through the UI; student and instructor grids 10/23; D4c cancellation with the fictional registrants (200 + the cell state); published activity blocking a ring on D4 and listed on 04; registration, waitlist and FIFO promotion. `ROADMAP.md` is organizer-owned — do not edit it.
6. **Follow-ups of the E4-W03 round-2 review** (`roadmap/reviews/E4-W03-20260924-1550-claude.md` #2–#6):
   - `DayGrid` never hides a cell silently. A cell whose `ringId` matches no column is logged in development builds, and the `TodayPage` test asserts the intended behaviour explicitly.
   - `formatDateRange` formats each end in its own zone, or rejects a plain date mixed with a timestamp.
   - `useDayGrid` rewrites the address in a mount effect, not in the `useState` initializer.
   - The day-grid mock answers `400 VALIDATION_ERROR` for an unknown `view`.
   - `parseMaskedDate` drops its second, noon-UTC check.

7. **Follow-ups of E4-W07 (organizer verification of 25-09):**
   - `scripts/e2e-core.sh` stages E3 on a fresh seed for **every** task id, not only `E3-W0[3-9]`, so that `pnpm e2e:core <ID>` passes as one command (E3-W11 brings this forward; if it is done, only check it). Before, E4-W07 needed split runs: T-04-34 failed with `REFRESH_EXPIRED` on a shared seed;
   - download an activities export and a registrants export from D7 against the real core with the E4 seed: the file name and the magic bytes, as E4-W07 did for the members;
   - screenshots at 1280 of the export menu of D5 and D7: with an `EXPORT_TOO_LARGE` message, and pending (a deterministic delayed answer).

## Fixed conventions
- Same Docker wrapper as E1-W04/E2-W07 (`pnpm e2e:core` with `CORE_URL=http://core:8080`); the stack is self-cleaning; no secrets printed (`E1_CORE_PASSWORD` generated at runtime, the public API key never shown); fictional data only (seed accounts `@example.test`); `docker login` is never run (the wrapper pulls the image outside the sandbox).
- The MSW/unit suites stay untouched except for the contract alignment of step 0 (keys, never literals; ca/es/en together).
- No new parameter, event, notification or error code; no api change (proposals only). Design system, i18n and vocabulary rules unchanged.
- Evidence rule: command + exit code + last 40 lines; long outputs → `roadmap/evidence/E4-W05/NN-<name>.log`; task file ≤ 120 KB; complete `pnpm e2e` (three MSW projects) in the Playwright image after the contract alignment; never edit other tasks, `ROADMAP.md` or the Organizer verification section.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `docker image inspect ghcr.io/jboixtcm/agilityhub-core-api:main --format '{{.Architecture}} {{index .Config.Labels "org.opencontainers.image.revision"}}'` (record the revision); `pnpm api:generate` + `shasum -a 256` of `openapi.json` vs the staged file + `jq -r '.paths | keys[]' packages/api-client/openapi/pending.json` (no S06/S07 path); Ajv suite; `CORE_URL=http://core:8080 pnpm e2e:core` green **twice** (E1 + E2 + E3 + E4 specs; paste the tails, keep the full logs); `pnpm turbo run lint typecheck test build` + `pnpm i18n:check`; complete `pnpm e2e` in the Playwright image; the fifteen screenshots.

## Definition of Done
T-06-28 and T-07-32 pass end to end against the published core with the E4 demo seed (twice, idempotent); the S06/S07 contract is the real snapshot with no pending entry; screenshots with real data are committed and compared; gate E4 (front) evidence is in the report; contract discrepancies, if any, are filed as proposals.

## Evidence to return
Image revision, snapshot hashes and the `pending.json` path list, `e2e:core` tails (both runs), `e2e` tail, the screenshot list with deviations, the gate E4 lines with their proving output, the discrepancy list.

---

## Executor report
Session of 2026-09-25/26 (claude:claude-opus-5-5). Dependencies: every `depends_on` task was `verified` at the end of the session, except that E4-W10 was `awaiting_verification` when the task started (the organizer verified it meanwhile, MESSAGES 25-09). The core image is the one the kit pulled at session start: `docker pull` is denied inside the session.

**Outcome.** T-06-28 and T-07-32 pass end to end against the published core `1308743` with its E4 demo seed, twice in a row (each run on a fresh seed: E1/E2 12, E3 5, E4 7). The snapshot `d791361` is adopted byte for byte, and `pending.json` has no S06/S07 entry. The integration found three web defects on the real core and fixes them, with tests that fail on the old code:
1. the D7 registrants page never loaded (`400 INVALID_FILTER`: it sent the column key `contact` in `fields`);
2. the D7 list lost the type, the hours, «totes — bloquejades» and «22/40 · fins el …» (the core returns an empty value for every key not in `fields`);
3. the registrants page showed the core's implicit `activityId` filter as a chip with a raw UUID.

### Branch / commits
No branch or commit (`.git` is read-only in this session): the publish script commits the working tree on `main`.

### Evidence (full command outputs)
Every log is in `roadmap/evidence/E4-W05/`. Earlier attempts, one line each:
- `06a-turbo-failed.log`: three hand-built `RegisteredActivity` test fixtures in `apps/clubs` lacked the new required `startTime`/`endTime` → completed them. `06b-turbo-failed.log`: `Array.prototype.findLast` is not in the TS lib → `filter(...).at(-1)`. `06c-turbo-before-view-mock.log`: green, but before the last mock change (below).
- `07a-`/`08a-e2e-core-run*-previous-spec.log`: both green (12 + 5 + 7), on an earlier revision of `e4-core.spec.ts`. The final spec only changes captures: D4c at viewport size, D5's filters cleared before its export captures, no focus ring on the D3 tab.
- `09a-e2e-docker-before-view-mock.log`: green (42 + 1 + 29) before the day-grid mock learnt the core's answer to an empty `view` (`view=` → the member view, as the core answers; seen on the dev stack).
- `06d-turbo-before-prettier.log` and `09b-e2e-docker-before-prettier.log`: green, before a last `prettier --write` of five touched files (whitespace only, including a few older lines). `06` and `09` below are the runs on the final tree.
- Development loop against a private stack (`agilityhub-e4-w05-dev`, git-ignored env file under `test-results/`), not committed: the spec was iterated there before the runs below.

**1. Image revision** — `docker image inspect ghcr.io/jboixtcm/agilityhub-core-api:main --format '{{.Architecture}} {{index .Config.Labels "org.opencontainers.image.revision"}}'` → exit 0
```
arm64 1308743f9f9eca90bca1ed8faf4de86599d31b02
```
The image lists `demo-canic.yaml` in `/app/seeds` (seen through `docker compose run` of a one-service probe file, because `docker run --entrypoint` is denied here) and `seed:demo` accepts `--week-start` (the seed log prints `218 changes (demo planning, week start 2026-09-21)`). There is no `/app/seeds/README.md`: the arrangement comes from the image's `demo-canic.yaml` (Assumption 1).

**2. Snapshot adoption** — `cp packages/api-client/openapi/openapi.next.json packages/api-client/openapi/openapi.json && cmp …` → exit 0 (`cmp: identical`), then `openapi.next.json` removed as in the earlier adoptions. `shasum -a 256` (the staged file read back from `HEAD`):
```
before (HEAD openapi.json)       6a5e9118d268e7a606caec07517ea86d7c0d248e3aa665c5c901529e26472134
staged (HEAD openapi.next.json)  2055718ea54d0350db98ae135a049dc271ea9f7afa5061c3518a3ed348833513
adopted (openapi.json)           2055718ea54d0350db98ae135a049dc271ea9f7afa5061c3518a3ed348833513
```
The staged snapshot `d791361` differs from the previous one only by api E5-T15: `ActivityRow`/`RegisteredActivity.startTime`/`endTime` (required, nullable), `ClassSession.instructorNames`/`ring` (in `GET /class-sessions/{id}` only), the `position` rule (kept after a waitlisted registration is cancelled) and two descriptions.

`pnpm api:generate` → exit 0 (`00-api-generate.log`, complete):
```

> agilityhub-core-web@0.0.0 api:generate /Users/jordib/dev/agilityhub/agilityhub-core-web
> pnpm --filter @agilityhub/api-client api:generate


> @agilityhub/api-client@0.0.0 api:generate /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client
> node scripts/generate.mjs

Generated /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client/src/generated/schema.d.ts from /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client/openapi/openapi.json merged with /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client/openapi/pending.json
```
`jq -r '.paths | keys[]' packages/api-client/openapi/pending.json` → exit 0 (no S06/S07 path; the schemas left are `DogPatchPendingFields, DogPendingFields, LicensePendingFields, LicenseWithPendingFields, NotificationPreferences, NotificationPreferencesPatch, PlanBillingMode`, all S03/S05/S13):
```
/members/{id}/notification-preferences
```

**3. Ajv suite** — `pnpm --filter @agilityhub/api-client exec vitest run src/mocks/fixtures.contract.test.ts` → exit 0 (`04-ajv.log`, complete):
```

 RUN  v5.0.0 /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client


 Test Files  1 passed (1)
      Tests  53 passed (53)
   Start at  00:48:13
   Duration  732ms (import 37%, tests 36%, transform 27%)
```

**4. Real core, run 1** — `/Users/jordib/Dropbox/Documents/SOFTWARE_CANIC/05-desenvolupament/roadmap-kit/mac/heavy.sh pnpm e2e:core E4-W05 > roadmap/evidence/E4-W05/10-e2e-core-run1.log 2>&1` → exit 0. `CORE_URL` is the script's default `http://core:8080` (a `CORE_URL=…` prefix is denied in this session). Stage summaries (`grep -n "Running\|passed"`): E1/E2 `12 passed (19.4s)`, E3 `5 passed (19.9s)` with the N-37 check `{"action":"OPEN_DOG","channel":"APP","code":"N-37","dogName":"Neret E3","status":"SENT"}`, E4 `7 passed (16.6s)`. Last 40 lines:
```
+ eslint-import-resolver-typescript 4.4.5
+ eslint-plugin-import 2.32.0
+ eslint-plugin-jsx-a11y 6.10.2
+ eslint-plugin-react 7.37.5
+ eslint-plugin-react-hooks 7.1.1
+ globals 17.12.0
+ husky 9.1.7
+ i18next-parser 9.4.0
+ lint-staged 17.5.0
+ msw 2.15.0
+ prettier 3.9.6
+ size-limit 13.0.3
+ turbo 2.10.12
+ typescript 5.9.3
+ typescript-eslint 8.69.0

. prepare$ node packages/config/scripts/prepare-husky.mjs
. prepare: .git can't be found
. prepare: Done
Done in 4.7s using pnpm v9.15.9
[WebServer] (node:134) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)

[WebServer] (node:214) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)


[WebServer] (node:312) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)

Running 7 tests using 1 worker

[1/7] e2e/core/e4-core.spec.ts:361:5 › T-06-28 E2E (a) D3: the three seeded templates, «Setmana B» blocked by its inconsistency, «Setmana A» generates the proposed week, D3b per ring
[2/7] e2e/core/e4-core.spec.ts:480:5 › T-06-28 E2E (b) D4: «Esborrany» opens the seeded draft week, [VALIDAR LA SETMANA], the D4c modal with the 4 registrants (200), «Anul·lades», ring blocks (409 then 201)
[3/7] e2e/core/e4-core.spec.ts:630:5 › T-06-28 E2E (c) screens 10 and 23: the member never sees the cancelled class nor counts, the validated week is visible; the instructor sees the block, the dimmed class and n/n
[4/7] e2e/core/e4-core.spec.ts:682:5 › T-07-32 E2E (d) D7: the four seeded activities, the Torneig blocks every ring on D4, a new seminar published over a class with the conflict dialog; D7 exports against the core
[5/7] e2e/core/e4-core.spec.ts:799:5 › T-07-32 E2E (e) ca: 04 → detail → register → 03 → cancel in time; the waitlist of the full «Seminari de handling» and the FIFO promotion when the admin adds a place
[6/7] e2e/core/e4-core.spec.ts:929:5 › T-07-32 E2E (e) es: the same member flow with the es literals, a second FIFO promotion, then the admin cancels the seminar (200) and the registrant reads «cancel·lada pel club»
[7/7] e2e/core/e4-core.spec.ts:1073:5 › E4-W05 step 7 · the «Excel · PDF» menus of D5 and D7 with EXPORT_TOO_LARGE and while an export is pending
  7 passed (16.6s)
```

**5. Real core, run 2** (idempotence) — the same command into `11-e2e-core-run2.log` → exit 0. E1/E2 `12 passed (20.5s)`, E3 `5 passed (19.4s)`, E4 `7 passed (16.0s)`. Last 40 lines:
```
+ eslint-import-resolver-typescript 4.4.5
+ eslint-plugin-import 2.32.0
+ eslint-plugin-jsx-a11y 6.10.2
+ eslint-plugin-react 7.37.5
+ eslint-plugin-react-hooks 7.1.1
+ globals 17.12.0
+ husky 9.1.7
+ i18next-parser 9.4.0
+ lint-staged 17.5.0
+ msw 2.15.0
+ prettier 3.9.6
+ size-limit 13.0.3
+ turbo 2.10.12
+ typescript 5.9.3
+ typescript-eslint 8.69.0

. prepare$ node packages/config/scripts/prepare-husky.mjs
. prepare: .git can't be found
. prepare: Done
Done in 4s using pnpm v9.15.9
[WebServer] (node:134) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)

[WebServer] (node:214) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)

[WebServer] (node:312) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)


Running 7 tests using 1 worker

[1/7] e2e/core/e4-core.spec.ts:361:5 › T-06-28 E2E (a) D3: the three seeded templates, «Setmana B» blocked by its inconsistency, «Setmana A» generates the proposed week, D3b per ring
[2/7] e2e/core/e4-core.spec.ts:480:5 › T-06-28 E2E (b) D4: «Esborrany» opens the seeded draft week, [VALIDAR LA SETMANA], the D4c modal with the 4 registrants (200), «Anul·lades», ring blocks (409 then 201)
[3/7] e2e/core/e4-core.spec.ts:630:5 › T-06-28 E2E (c) screens 10 and 23: the member never sees the cancelled class nor counts, the validated week is visible; the instructor sees the block, the dimmed class and n/n
[4/7] e2e/core/e4-core.spec.ts:682:5 › T-07-32 E2E (d) D7: the four seeded activities, the Torneig blocks every ring on D4, a new seminar published over a class with the conflict dialog; D7 exports against the core
[5/7] e2e/core/e4-core.spec.ts:799:5 › T-07-32 E2E (e) ca: 04 → detail → register → 03 → cancel in time; the waitlist of the full «Seminari de handling» and the FIFO promotion when the admin adds a place
[6/7] e2e/core/e4-core.spec.ts:929:5 › T-07-32 E2E (e) es: the same member flow with the es literals, a second FIFO promotion, then the admin cancels the seminar (200) and the registrant reads «cancel·lada pel club»
[7/7] e2e/core/e4-core.spec.ts:1073:5 › E4-W05 step 7 · the «Excel · PDF» menus of D5 and D7 with EXPORT_TOO_LARGE and while an export is pending
  7 passed (16.0s)
```
What the E4 stage recorded (`e4-core-run.json`, run 2, runId `hjrz7s`): generation `200`, `classCount 38`, `skipped []` for the proposed week `2026-10-12`; validation of `38` drafts of `2026-09-28`; class cancellation `200`, `reason DELETED`, `affectedBookings 4`, `affectedWaitlist 2`; ring block over «Obed. urbana» `409 RING_BLOCK_CONFLICT` (`details.conflicts[0].type CLASS`), then `201` on 11:00–12:00; the seminar's conflicts `[CLASS «A+B» 0 booked]`; FIFO ca: the member joined at position 3, the position-1 registrant became `ACTIVE`, the member stayed `WAITLISTED`; FIFO es: the es member joined at position 4, the next head (position 2) became `ACTIVE`; the seminar's cancellation `200`, its registrants `[MEMBER, MEMBER, ACTIVITY_CANCELLED]`; downloads `canic_activities_20260926-0043.xlsx` and `canic_activity-registrations_20260926-0043.xlsx`, both `PK\x03\x04` (`activities-export-download.json`, `registrants-export-download.json`). `oauth-token-calls.log`: 62 × `200`. Every non-2xx is `REFRESH_EXPIRED`: the cookie-less probes before each login, plus one call with the cookie in T-01-22 (the apps/id authorize flow, which passes; the same call appears in the E3-W05 and E3-W12 logs). There is no `REFRESH_REUSED`.

**6. Lint, typecheck, test, build** — `pnpm turbo run lint typecheck test build --force` → exit 0 (`06-turbo.log`, 34/34, 0 cached). Last 40 lines (colour codes removed):
```
clubs:test:    Duration  11.13s (tests 37%, environment 35%, transform 15%, setup 7%, import 5%)
clubs:test: 
clubs-admin:test:  ✓ src/dashboard/SignupReviewPage.test.tsx (72 tests) 6467ms
clubs-admin:test:    ✓ T-04-33 D2 signup validation (12)
clubs-admin:test:      ✓ shows the mockup: DNI and phone in full, signed documents, consent warning, level, invoice and Stripe payment 430ms
clubs-admin:test:    ✓ E3-W07 step 4 · M15 the plan and family decisions (R-04-13, T-04-33) (4)
clubs-admin:test:      ✓ a NOT_FOUND_PENDING claim shows what the applicant typed and is resolved by attaching the holder's group 552ms
clubs-admin:test:  ✓ src/activities/ActivitiesPage.test.tsx (32 tests) 7522ms
clubs-admin:test:    ✓ T-07-29 D7 activities (list, maintenance, publication, cancellation) (21)
clubs-admin:test:      ✓ draws the four mockup rows with the R-07-13 dates, rings, registrations and state chips 576ms
clubs-admin:test:      ✓ «Nova activitat» creates a draft with title and type and opens its maintenance 616ms
clubs-admin:test:      ✓ shows the mockup maintenance of «Torneig d'Estiu 2026» with the ring, level and waitlist chips 357ms
clubs-admin:test:      ✓ R-07-03 [DESA] sends only the allow-listed rich text of the editor, with the version 300ms
clubs-admin:test:    ✓ T-07-29 E4-W08 D7 follow-ups of the E4-W04 round-2 review (6)
clubs-admin:test:      ✓ R-07-05 away from the club the hours cover the whole day: 6:00 when the club opens at 7:00 501ms
clubs-admin:test:    ✓ T-07-29 E4-W10 D7 follow-ups of the E4-W08 and E4-W09 reviews (3)
clubs-admin:test:      ✓ S07 §6 CONVENCIONS_API §4 the member filter reads every page: with 1,001 distinct registrants the member on the second page can be selected 761ms
clubs-admin:test:  ✓ src/planning/CalendarPage.test.tsx (35 tests) 8817ms
clubs-admin:test:    ✓ E3-W07 step 9 a D1 risk row opens D4 on its class (1)
clubs-admin:test:      ✓ selects the class of `?classe=` in the week of `?setmana=` 578ms
clubs-admin:test:    ✓ T-06-28 D4 / D4b / D4c class calendar (front half, MSW) (24)
clubs-admin:test:      ✓ opens «Esborrany» on the first week with drafts, with the validation card and its count 697ms
clubs-admin:test:      ✓ [VALIDAR LA SETMANA] turns every draft of the week active at once 490ms
clubs-admin:test:      ✓ cancels an active class with registrants through D4c: four rows, text required, «ANUL·LA I AVISA ELS 4 ALUMNES» 435ms
clubs-admin:test:      ✓ [ELIMINA] of an active class goes through the same D4c modal 447ms
clubs-admin:test:      ✓ R-06-09 saves the diff with the version; STALE_VERSION keeps its message after the refetch remounts the card 420ms
clubs-admin:test:      ✓ R-06-09 [Crear classe]: a new time or ring after RING_HAS_BOOKINGS drops the confirmation 343ms
clubs-admin:test:      ✓ R-06-09 [Crear classe]: a RING_HAS_BOOKINGS answer that arrives after a ring change is dropped 332ms
clubs-admin:test:    ✓ E4-W09 D4 follow-ups of the E4-W02 round-4 review (7)
clubs-admin:test:      ✓ R-06-09 after STALE_VERSION every editor stays locked until the refetched version is shown, so no edit is lost 305ms
clubs-admin:test: 
clubs-admin:test:  Test Files  19 passed (19)
clubs-admin:test:       Tests  268 passed (268)
clubs-admin:test:    Start at  00:51:51
clubs-admin:test:    Duration  13.80s (tests 39%, environment 32%, transform 18%, setup 6%, import 5%)
clubs-admin:test: 

 Tasks:    34 successful, 34 total
Cached:    0 cached, 34 total
  Time:    26.118s 
```

**7. i18n** — `pnpm i18n:check` → exit 0 (`05-i18n-check.log`, complete):
```

> agilityhub-core-web@0.0.0 i18n:check /Users/jordib/dev/agilityhub/agilityhub-core-web
> node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs

i18n check passed
ca: 2330 keys
es: 2330 keys
en: 2330 keys
1754 statically used keys, 0 unused
250 codes, 0 missing
```

**8. Complete e2e in the Playwright image** — `/Users/jordib/Dropbox/Documents/SOFTWARE_CANIC/05-desenvolupament/roadmap-kit/mac/heavy.sh pnpm e2e:docker E4-W05 > roadmap/evidence/E4-W05/09-e2e-docker.log 2>&1` → exit 0: clubs-admin 42, id 1, clubs 29, no failure, no flaky. No other task's evidence changed (`git status --short roadmap/evidence` lists only `E4-W05/`). Last 40 lines (colour codes removed):
```
clubs:build: mode      generateSW
clubs:build: precache  54 entries (1644.23 KiB)
clubs:build: files generated
clubs:build:   dist/sw.js
clubs:build:   dist/workbox-9c191d2f.js
id:e2e: cache bypass, force executing 97605e6e536ee93a
id:e2e: 
id:e2e: > id@0.0.0 e2e /work/apps/id
id:e2e: > playwright test
id:e2e: 
id:e2e: 
id:e2e: Running 1 test using 1 worker
id:e2e: ·
id:e2e:   1 passed (3.2s)
clubs-admin:e2e: cache bypass, force executing d93e70e426ad48c3
clubs-admin:e2e: 
clubs-admin:e2e: > clubs-admin@0.0.0 e2e /work/apps/clubs-admin
clubs-admin:e2e: > playwright test
clubs-admin:e2e: 
clubs-admin:e2e: [WebServer] (node:894) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs-admin:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs-admin:e2e: 
clubs-admin:e2e: Running 42 tests using 9 workers
clubs-admin:e2e: ··········································
clubs-admin:e2e:   42 passed (24.1s)
clubs:e2e: cache bypass, force executing bcfbb9ce4699a27f
clubs:e2e: 
clubs:e2e: > clubs@0.0.0 e2e /work/apps/clubs
clubs:e2e: > playwright test
clubs:e2e: 
clubs:e2e: [WebServer] (node:3005) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs:e2e: 
clubs:e2e: Running 29 tests using 7 workers
clubs:e2e: ·····························
clubs:e2e:   29 passed (12.9s)

 Tasks:    8 successful, 8 total
Cached:    0 cached, 8 total
  Time:    52.404s 
```

**9. Tests that fail without the change** (the tests kept, the product file reverted to `HEAD`):
- `01-vitest-registrants-before-fix.log`: with the core-like mock, 4 of 5 registrants tests fail on the old page (`fields=…,contact` → `400`).
- `02-vitest-d7-list-before-fix.log`: «draws the four mockup rows» fails on the old `fields` (`'Torneig d'Estiu 2026 ·'`, the type lost).
- `03-vitest-registrants-chip-before-fix.log`: the new `fields` test fails while the `activityId` chip is shown.
- `12a-vitest-today-before-fix.log` (2 fail: the DayGrid warning, the render-time rewrite), `12b-vitest-format-before-fix.log` (4 fail: `formatDateRange` mixed ends), `12c-vitest-mocks-before-fix.log` (7 fail: registrants `fields`, E5-T15 `position`, E5-T15 `instructorNames`/`ring` ×2, `view` ×3; the `view` test was then aligned with the core's bare `400` and its empty `view`).

**10. Screenshots with real data** (all in `roadmap/evidence/E4-W05/`, from run 2):
- 1280: `D3-plantilles-core-1280.png` (Setmana B with the note), `D3b-dia-core-1280.png`, `D4-actives-core-1280.png`, `D4b-esborrany-core-1280.png`, `D4c-modal-core-1280.png`, `D4-anullades-core-1280.png`, `D7-llistat-core-1280.png`, `D7-manteniment-core-1280.png`, `D7-conflictes-core-1280.png`, `D7-inscrits-core-1280.png`; extra: `D7-inscrits-cancellada-core-1280.png`.
- 375: `10-avui-core-375.png`, `23-visio-global-core-375.png`, `04-activitats-core-375.png`, `activitat-detall-core-375.png`, `03-inscrita-core-375.png`; extra: `03-inscrita-core-es-375.png`, `activitat-llista-espera-core-es-375.png`.
- Step 7 (1280): `D5-export-massa-gran-core-1280.png`, `D5-export-pendent-core-1280.png`, `D7-export-massa-gran-core-1280.png`, `D7-export-pendent-core-1280.png`.
- The E1/E2/E3 stages of the same runs re-captured their own screens here too (as in E3-W11/E3-W12).

**Deviations from `docs/pantalles/` noticed on the real-data captures** (not fixed here):
1. Every screen: the header shows the initial «C» + «Club Agility Cànic»: the seed club has no `logoUrl`/`markUrl` (branding data).
2. D3/D4/D4c: level descriptions «A+B», «F i sup.» (the api's R-06-03), where the mockups draw «A i B».
3. D3 coverage: «Prop. s/actius» «—» and «sense gossos» on every level. The seed has no booking in the current or previous week: the E5 scenario reports 0 rows when `--week-start` is the run's Monday on a later weekday.
4. D4 (week +2): the Torneig's five ring blocks stack as five cells in the Saturday column, so the 18:30 band is very tall (the mockup has no activity). Proposal: one cell «Activitat · {title} · totes les pistes» when an activity blocks every ring.
5. D4c: no ⚠ before the title (the mockup has one); «app · correu · SMS» without «(2 telèfons)» (data).
6. D7 maintenance: «URL: · surt a l'API de la web (mai noms)» with an empty URL: the real core sends `publicUrl: null` (no public web configured). The line should hide, or show the path; this is not a one-line literal fix, so it is only listed.
7. D7 registrants (no mockup): at 1280 the table overflows sideways with the seed's long demo e-mails (`demo.<uuid>.N@example.test`), clipping the header and «Excel · PDF».
8. D7/app waitlist positions: after a promotion the others keep their stored positions, so the head of the queue reads «en llista d'espera (2)» (Question 2).
9. Screen 10: no instructor under the member's cells for a class 11 days ahead (R-06-12 visibility; the mockup's classes show it). «Ocupada · manteniment» wraps mid-word («manteni ment») in the 375 cells, also on 23 («Bloq.»).
10. Screen 03: «Dissabte 17 · 18:30–20:30 · Central» for a date in October seen in September: the «day» presentation never shows the month.
11. D5 (seen while taking the step 7 captures, E2 code): the default view carries `planId:eq:plan-member`, a mock fixture id, so a fresh `/abonats` on the real core lists nobody («Cap abonat amb aquests criteris»). The captures clear the filters first, as T-03-42 does. Proposal: a follow-up task.

**Gate E4 (front) — one line per item, with its proof:**
- **Week generated from A/B + Saturdays and validated through the UI** — test (a): «Setmana B» blocked (`bloquejat: 1 incoherència a la plantilla`, [GENERAR CLASSES] disabled); «Setmana A» + «Dissabtes» generated the proposed week `2026-10-12` (`200`, 38 classes, SETMANES row). Test (b): «Esborrany» opened `2026-09-28` (= `E4_WEEK_START` + 7), and [VALIDAR LA SETMANA] turned its 38 drafts active (`200`). Captures: `D3-plantilles-core-1280.png`, `D3b-dia-core-1280.png`, `D4b-esborrany-core-1280.png`.
- **Student and instructor grids 10/23** — test (c): the member's Wednesday has no counts and no cancelled class, and the validated Monday shows its classes. The instructor's Wednesday shows «Bloq.», the dimmed «anul·lada» class and `n/n` counts. Captures: `10-avui-core-375.png`, `23-visio-global-core-375.png`.
- **D4c cancellation with the fictional registrants** — test (b): 4 rows, «ANUL·LA I AVISA ELS 4 ALUMNES» disabled until the text; [Torna enrere], then [ELIMINA] through the same modal; `POST /class-sessions/{id}/cancellation` `200` (`DELETED`, 4 bookings, 2 waiting); the cell reads «anul·lada · eliminada», also under «Anul·lades». Captures: `D4c-modal-core-1280.png`, `D4-anullades-core-1280.png`.
- **A published activity blocks a ring on D4 and is listed on 04** — test (d): the Torneig's five «Activitat · Torneig d'Estiu 2026» cells on D4. «Seminari E4 {runId}» was published over the «Dissabtes» class (conflict dialog, «Anul·la les classes en conflicte…», `200`): D4 shows «Activitat · Seminari E4 …» on Central and the class «anul·lada». Test (e): 04 lists it with «5 places». Captures: `D7-conflictes-core-1280.png`, `04-activitats-core-375.png`.
- **Registration, waitlist and FIFO promotion** — test (e) ca: [INSCRIU-M'HI] `201` → 03 row «inscrita» → [ANUL·LA LA INSCRIPCIÓ] `200` → the row is gone; the member joins the full «Seminari de handling» at position 3 (`201`); Places 12 → 13 promotes position 1 only (FIFO, R-07-08). Test (e) es: the same flow with the es literals, and a second promotion (13 → 14) of the next head; the club's cancellation `200` → «cancel·lada pel club». Captures: `03-inscrita-core-375.png`, `activitat-detall-core-375.png`, `D7-inscrits-core-1280.png`, `D7-inscrits-cancellada-core-1280.png`.

### Files changed
- Contract: `packages/api-client/openapi/openapi.json` (= staged `d791361`), `openapi.next.json` removed, `src/generated/schema.d.ts`.
- Mocks: `src/mocks/fixtures/activities.ts` (E5-T15 `startTime`/`endTime`), `activity-handlers.ts` (`fields` as the core applies it on the activities and registrants lists; the registrants echo `activityId`; a cancelled waitlisted registration keeps its `position`), `calendar-handlers.ts` + `fixtures/day-grid.ts` + `day-grid-handlers.ts` (`instructorNames`/`ring` in `GET /class-sessions/{id}`; `view` as the core answers it), with tests in `activity-handlers.test.ts`, `calendar-handlers.test.ts`, `day-grid-handlers.test.ts`.
- D7: `apps/clubs-admin/src/activities/ActivitiesPage.tsx` (`activityFields`), `ActivityRegistrantsPage.tsx` (`registrantFields`, no `activityId` chip), `ActivitiesPage.test.tsx` (2 new tests).
- Step 6: `packages/ui/src/day-grid.tsx`, `apps/clubs/src/today/TodayPage.test.tsx`, `packages/i18n/src/format.ts` + `format.test.ts`, `apps/clubs/src/today/useDayGrid.ts`, `apps/clubs-admin/src/planning/calendar-shared.tsx`.
- App fixtures typed by the snapshot: `apps/clubs/src/activities/HistoryRowsPreview.tsx`, `activities.test.tsx`.
- Real-core harness: `scripts/e2e-core.sh` (three stages on fresh seeds, `E4_WEEK_START`, a compose project per task id), `scripts/core-stack/docker-compose.yml` (`--week-start`, `E4_WEEK_START` for Playwright), new `e2e/core/e4-core.spec.ts`.
- `CHANGELOG.md`; evidence in `roadmap/evidence/E4-W05/`.

### Rules and tests implemented (R-xx-nn / T-xx-nn)
- **T-06-28** (E2E half), `e2e/core/e4-core.spec.ts` tests (a)–(c): R-06-07 (the first week with drafts), R-06-08 (the whole week validated at once), R-06-10 (the seeded Wednesday 18:50 «B+C» with 4 registrants and 2 waiting, text required, [ELIMINA] through the same modal), R-06-11 (ring blocks: `409 RING_BLOCK_CONFLICT` shown in the drawer, then `201`), R-06-12 (the member view has no counts; the instructor's does).
- **T-07-32**, tests (d)–(e) in ca and es: R-07-05 (publication over a class), R-07-08 (FIFO, twice), R-07-09 (the member's cancellation in time), R-07-11 (04 and 03 rows). The N-32c feed assertion belongs to screen 11 (E7), and screen 25's class rows to S10's `/me/history` (E6): both out of scope here.
- Step 7: the D7 activities and registrants downloads (file name + `PK\x03\x04`), and the four export-menu captures. `pnpm e2e:core` stages E3 on a fresh seed for every id (already done by E3-W11), and now E4 too.
- Step 6 (E4-W03 review #2–#6): Vitest in `TodayPage.test.tsx` (the unplaced cell is not drawn and is logged; the address is rewritten after mounting), `format.test.ts` (R-06-14 mixed ends), `day-grid-handlers.test.ts` (`view`), the existing `plain-dates.test.ts` (`parseMaskedDate`).
- **T-07-29** Vitest: «asks `fields` in response keys (never «contact»)…» and «with only the «Contacte» column the rows keep their key and member…»; the list row test «draws the four mockup rows…» now runs against the core-like projection.

### Assumptions
1. **Seed arrangement.** It follows the image's `demo-canic.yaml`, not step 0a. Week 0 is generated and validated (E5-T06), week +1 is the draft, and week +2 (validated) holds the registrants, the maintenance block, the instructor inconsistency and the Torneig; the candidates propose week +3. So (b) finds the inconsistency and the R-06-10 class in week +2 (reached with «Setmana següent»), not in the current week. Rows are chosen by time, counts and state in their accessible names: the cells carry no `data-*` hooks, and adding them was not needed. People's names are only read back from API responses.
2. **Accounts** from `club-canic.yaml`: `admin@`, `instructor@`, `member.4@` (ca) and `member.5@` (es). In the es repeat the member side is in es and the admin stays in ca.
3. **The seminar's Saturday** is in week +3, generated in (a). Its conflicting «Dissabtes» class is a draft, which the core cancels with `DELETED` («anul·lada · eliminada»).
4. **Idempotence** comes from the harness: every stage starts on a fresh seed (the stack is removed with its volumes). The run's titles still carry `runId`, and the run cancels its ring block and seminar.
5. **Export-menu states** (step 7) are Playwright route stubs on the real-core pages: `EXPORT_TOO_LARGE` as `422` (CATALEG_ERRORS rule 0) with `details: {}`, and a held answer for «pending». The core cannot produce either on demand.
6. **App rows stay on `startsAtLocal`/`endsAtLocal`.** The published image does not send E5-T15's `startTime`/`endTime` yet, and E4-W11 step 6 owns the switch (organizer 25-09).
7. **Scope of the product fixes.** The D7 and registrants `fields` fixes and the chip are in this task: the T-07-32 flow cannot pass against the real core without them, and the list showed wrong figures. The other visual deviations are only listed.

### Questions / catalog proposals
No new parameter, event, notification or error code. For `@organizer` (also in `MESSAGES.md`):
1. **Contract, `fields`.** `GET /activities` and `GET /activities/{id}/registrations` send an empty value for every key not in `fields`: `null`, and `false` for a flag (an all-rings activity read `allRings: false`). That includes `registrationId`, which the snapshot declares as a required string. `GET /members` omits such keys instead. Proposal: omit them (a sparse fieldset, as `/members` does), or always send the id and the required keys; and publish the accepted `fields` keys per list (e.g. `x-fields`), because a column key such as `contact` is `400 INVALID_FILTER`.
2. **Waitlist display.** After a promotion, the others keep their stored `position`, so the head of the queue reads «(2)». Should D7 and the app show the rank (1, 2, …) instead? R-07-08 (FIFO) holds either way.
3. **Contract, `appliedFilters`.** The registrants list echoes the path's `activityId` among its `appliedFilters`. Proposal: leave it out, or mark it as implicit. The web hides it now.
4. **Image lag.** The published image `1308743` predates the adopted snapshot. `GET /me/activities` rows lack `startTime`/`endTime`, and `GET /class-sessions/{id}` lacks `instructorNames`/`ring`. Nothing breaks: the consumers do not read them yet.
5. **Error details.** `GET /day-grid?view=foo` answers `400 VALIDATION_ERROR` with `details: {}`, while a bad `date` carries `fieldErrors[date]`. `RING_BLOCK_CONFLICT` over a class says «L'horari coincideix amb un bloqueig de pista.». The web maps both by code; noted for the api's catalog.
6. **Follow-ups proposed** (not done here): deviations 4, 6, 7, 9 and 10 above, and D5's fixture default filter (11).
7. **Denied in this session** (the alternatives used are above):
   - `docker run --rm --entrypoint /bin/sh ghcr.io/jboixtcm/agilityhub-core-api:main -c 'ls /app/seeds'`;
   - `docker pull ghcr.io/jboixtcm/agilityhub-core-api:main`;
   - any read outside the repository (the api repo's E4-T05 file).

## Organizer verification
Result: verified · 2026-09-26 · organizer (Claude)

T-06-28 and T-07-32 pass end to end against the published core `1308743` with its E4 demo seed, twice, each run on a fresh seed (E1/E2 12, E3 5, E4 7). The snapshot `d791361` is adopted byte for byte, and `pending.json` has no S06/S07 entry. The three web defects that the integration found are fixed, with tests that failed on the old code (logs `01`–`03`): the registrants page's `fields`, the D7 list's missing columns, and the raw `activityId` chip. CI green at `6cf867e`.

The organizer verified E4-W11 first, as planned.

The independent review (`roadmap/reviews/E4-W05-20260926-0125-claude.md`) has one major that predates this task, three minors and three nits. Codex could not answer, so a fresh Claude session reviewed.

Organizer decisions:
- **Review #8 (major, from E2) → E4-W12 step 5:** D5's default view carried a mock plan id, so a fresh `/abonats` on the real core listed nobody. E4-W12 also takes the report's deviations 4, 6, 7, 9 and 10.
- **Review #1 → E4-W12 step 6:** the D7 list always requests `title`.
- **Review #2, accepted:** the product fixes in this task were needed for T-07-32 to pass on the real core, and each has a test that failed before. The contract question goes to E5-T20 (question 1).
- **Review #3 and #7 → E4-W14:** `scripts/e2e-core.sh` keeps the seed container's log tail as evidence, and the E4 core spec asserts the generation body, the Saturday's Torneig cells, «(no dog)» on 03 and the es admin chip.
- **Review #4, accepted.** The image predates E5-T15. E4-W12's real-core run uses the image the kit pulls then.
- **Review #5 and #6, noted.** The week generated through the UI (+3) is not the week validated through the UI (+1). The spec runs only on a fresh seed.
- **Question 1 → E5-T20 (CONVENCIONS_API §4 amended 26-09).** With `fields`, a key that was not requested is left out, as `/members` does; the row id always comes. Each list publishes its accepted keys (`x-fields`).
- **Question 2 → E5-T20 and then the web.** A waiting registration carries `waitlistRank`, its rank among the active waiting entries, and D7 and the app show it. `position` keeps the stored order (S07 amended 26-09).
- **Question 3 → E5-T20:** `appliedFilters` leaves out the path's `activityId`.
- **Question 4, accepted** (see review #4).
- **Question 5 → E5-T20:** a bad `view` on `GET /day-grid` answers `400 VALIDATION_ERROR` with `details.field: view`.
- **Deviations 1, 2, 3, 5 and 8:** data, or rules already in force (8 is question 2).

The E4 gate lines stay open until E4-W12 is verified and the organizer re-checks the E4 screenshots.
