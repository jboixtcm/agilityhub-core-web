---
id: E1-W04
title: E1 integration against the core (staging or local) and gate evidence (front)
stage: E1
repo: agilityhub-core-web
thread: B
order: 40
status: awaiting_verification
depends_on: [E1-W01, E1-W02, E1-W03, E1-W06, E1-W07, external:api:E1-T10, external:api:E1-T14 (published image)]
branch: 
pr: 
updated: 2026-09-09
---

# E1-W04 · E1 integration against the core (staging or local) and gate evidence (front)

## Objective
The front part of the E1 gate: real login by magic link and by password at the Cànic host, 03b with remembered choice, onboarding, handoff to the backoffice, `apps/id` flow.

## Context to load
`roadmap/ROADMAP.md` (gate E1) · the three apps · this file.

## Steps
-1. **Pre-flight (organizer 09-09, relaxed 18:50)**: the image exists (api CI job «publish» of `ci.yml`, tags `main` + `sha-…`, amd64+arm64) and the wrapper that starts your session pulls `ghcr.io/jboixtcm/agilityhub-core-api:main` **outside** the sandbox (the sandbox cannot write to the macOS Keychain, so **never run `docker login`**). Your only check: `docker image inspect ghcr.io/jboixtcm/agilityhub-core-api:main` (or `docker manifest inspect …:main`) succeeds. If it does not, append a `@jordi` entry to `roadmap/MESSAGES.md` with the exact error and set the task `blocked` immediately. Docker Desktop must be running.
0. **OIDC bridge (organizer 06-09, from api E1-T05)**: `apps/id` login continuation is no longer «redirect back to the authorize URL»: `/oauth2/authorize` without a browser session redirects to `id.*/login?flow=<id>` (+ a host-only flow cookie, 5 min); after a fresh password/magic-link login the SPA calls `POST /oauth2/session {flow}` (bearer, same origin) and navigates to the returned `redirectUrl` (the client's callback with the code). Replace the E1-W03 `?continue=` handling by this flow (keep `?continue=` only as a same-origin fallback), MSW handler + e2e.
1. Playwright config with a `CORE_URL` target using the seeded fictional accounts; the MSW mode stays the default for unit runs. **The real core comes from the api's published image** (api E1-T14): `docker compose -f ../agilityhub-core-api/docker-compose.consumer.yml up -d --wait` — or, since the sibling checkout may be absent in your sandbox, a copy of that compose file committed here as `scripts/core-stack/docker-compose.yml` (image `ghcr.io/jboixtcm/agilityhub-core-api:main`, Mongo replica set, seed job; `docker login ghcr.io` with `gh auth token` documented) — plus the Vite proxy of E1-W07 pointing at `http://localhost:8080` with the `Host` header of the club (`app.example.test`).
2. Scenarios: magic link end to end (mailbox = the core's log sender output or the SendGrid test inbox exposed by a small helper endpoint in `local` profile — coordinate with the api E1-T10 smoke), password login, profile choice remembered, onboarding acceptance, handoff to `clubs-admin`, `apps/id` authorize continuation.
3. Screenshots of every screen involved at 375/1280 under `roadmap/evidence/E1-W04/`.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `CORE_URL=… pnpm e2e:core` green (paste); `pnpm turbo run lint typecheck test build` green.

## Definition of Done
Integration suite green against the core; gate items covered.

## Evidence to return
Outputs and screenshots.

---

## Executor report

### Branch / commits

- Working tree only; no branch, commit, or other git-writing command was run, per the sandbox protocol.

### Evidence (full command outputs)

Pre-flight image check:

- Command: `docker image inspect ghcr.io/jboixtcm/agilityhub-core-api:main --format '{{.Architecture}} {{index .Config.Labels "org.opencontainers.image.revision"}}'`
- Exit code: `0`
- Complete output: `roadmap/evidence/E1-W04/00-image-inspect.log`

```text
arm64 2d4cf2891c2c2a266b7406b5712dd606ebe86a9d
```

Real-core browser gate:

- Command: `COREPACK_HOME=/private/tmp/e1-w04-corepack CORE_URL=http://core:8080 pnpm e2e:core`
- Exit code: `0`
- Complete output: `roadmap/evidence/E1-W04/25-e2e-core.log` (129 lines)
- Last 40 lines:

```text
+ eslint 9.39.5
+ eslint-import-resolver-typescript 4.4.5
+ eslint-plugin-import 2.32.0
+ eslint-plugin-jsx-a11y 6.10.2
+ eslint-plugin-react 7.37.5
+ eslint-plugin-react-hooks 7.1.1
+ globals 17.12.0
+ husky 9.1.7
+ i18next-parser 9.4.0
+ lint-staged 17.5.0
+ msw 2.15.0
+ prettier 3.9.6
+ size-limit 13.0.3
+ turbo 2.10.12
+ typescript 5.9.3
+ typescript-eslint 8.69.0

. prepare$ node packages/config/scripts/prepare-husky.mjs
. prepare: .git can't be found
. prepare: Done
Done in 6.8s using pnpm v9.15.9
[WebServer] (node:134) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)


[WebServer] (node:214) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
[WebServer] (node:312) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
[WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)


Running 6 tests using 1 worker

[1/6] e2e/core/e1-core.spec.ts:158:5 › T-01-18 password login reaches the seeded multi-profile account
[2/6] e2e/core/e1-core.spec.ts:174:5 › T-01-21 administrator handoff opens the real backoffice
[3/6] e2e/core/e1-core.spec.ts:215:5 › T-01-20 profile choice is remembered by the core
[4/6] e2e/core/e1-core.spec.ts:253:5 › T-01-19 magic link is delivered and exchanged end to end
[5/6] e2e/core/e1-core.spec.ts:275:5 › T-01-26 first-access onboarding accepts both real policies
[6/6] e2e/core/e1-core.spec.ts:300:5 › T-01-22 apps/id resumes a real authorize flow through oauth2/session
  6 passed (10.3s)
```

Repository gate:

- Command: `COREPACK_HOME=/private/tmp/e1-w04-corepack pnpm turbo run lint typecheck test build`
- Exit code: `0`
- Complete output: `roadmap/evidence/E1-W04/23-turbo.log` (502 lines)
- Last 40 lines:

```text
clubs-admin:test: stderr | src/App.test.tsx > T-01-26 clubs-admin onboarding > shows profile completion as a non-dismissible backoffice modal
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test:
clubs-admin:test: stderr | src/census/CensusRecordPage.test.tsx > T-03-39 D10 member record > maps STALE_VERSION when another administrator edited the member
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test:
clubs-admin:test: stderr | src/App.test.tsx > T-01-26 clubs-admin onboarding > removes the postpone action when the policy limit is exhausted
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test:
clubs-admin:test:  ✓ src/App.test.tsx (8 tests) 400ms
clubs-admin:test: stderr | src/census/CensusListPage.test.tsx > T-03-38 D5 universal member list > toggles visible columns and synchronizes the ordered fields to the URL
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test:
clubs-admin:test: stderr | src/census/CensusRecordPage.test.tsx > T-03-38 dog record > shows the complete dog record and changes level without a booking warning
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test:
clubs-admin:test: stderr | src/census/CensusRecordPage.test.tsx > T-03-38 dog record > deactivates and reactivates a dog without deleting its record
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test:
clubs-admin:test: stderr | src/census/CensusListPage.test.tsx > T-03-42 D15 translated dog list > renders the approved columns and dog record links
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test:
clubs-admin:test:  ✓ src/census/CensusRecordPage.test.tsx (5 tests) 1002ms
clubs-admin:test:  ✓ src/census/CensusListPage.test.tsx (3 tests) 1672ms
clubs-admin:test:    ✓ T-03-38 D5 universal member list (2)
clubs-admin:test:      ✓ renders applied filters and preserves selection when page size changes 532ms
clubs-admin:test:      ✓ toggles visible columns and synchronizes the ordered fields to the URL 325ms
clubs-admin:test:    ✓ T-03-42 D15 translated dog list (1)
clubs-admin:test:      ✓ renders the approved columns and dog record links 812ms
clubs-admin:test:
clubs-admin:test:  Test Files  4 passed (4)
clubs-admin:test:       Tests  18 passed (18)
clubs-admin:test:    Start at  20:01:37
clubs-admin:test:    Duration  2.96s (tests 41%, environment 32%, transform 14%, setup 7%, import 6%)
clubs-admin:test:

 Tasks:    26 successful, 26 total
Cached:    26 cached, 26 total
  Time:    40ms >>> FULL TURBO

```

Additional i18n/catalog gate:

- Command: `COREPACK_HOME=/private/tmp/e1-w04-corepack pnpm i18n:check`
- Exit code: `0`
- Complete output: `roadmap/evidence/E1-W04/24-i18n.log`

```text

> agilityhub-core-web@0.0.0 i18n:check /Users/jordib/dev/agilityhub/agilityhub-core-web
> node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs

i18n check passed
ca: 1405 keys
es: 1405 keys
en: 1405 keys
912 statically used keys, 0 unused
245 codes, 0 missing
```

Earlier failing attempts, each retained as a complete log:

- `01-e2e-core.log`: password flow waited for navigation before isolating the password-grant response; added response-based login synchronization.
- `02-e2e-core.log`: the password request completed but no successful `/me` arrived; added grant-specific token and `/me` diagnostics.
- `03-e2e-core.log`: the real token was accepted by the core but rejected before `/me`; identified and normalized the omitted empty `scope` field.
- `04-e2e-core.log`: `/alumne/` matched both member and instructor descriptions; anchored the accessible-name locator.
- `05-e2e-core.log`: profile screenshot navigation ran before authenticated restoration completed; added authenticated route readiness checks.
- `06-e2e-core.log`: the readiness locator used the wrong shell class; corrected it to `.clubs-shell`.
- `07-e2e-core.log`: direct profile navigation still raced cookie restoration; added URL and shell synchronization.
- `08-e2e-core.log`: profile navigation returned to `/entrar`; added refresh-cookie rotation diagnostics.
- `09-e2e-core.log`: profile navigation again exposed an incomplete restore barrier; coalesced the complete refresh plus `/me` restore operation.
- `10-e2e-core.log`: admin handoff clicked before the chooser was restored; added explicit chooser readiness and navigation-safe clicking.
- `11-e2e-core.log`: the navigation consumed the handoff response body before Playwright read it; intercepted and buffered the body first.
- `12-e2e-core.log`: a repeated profile load exposed missing per-route refresh assertions; added explicit refresh and `/me` assertions.
- `13-e2e-core.log`: Corepack tried to write outside the sandbox; set `COREPACK_HOME=/private/tmp/e1-w04-corepack`.
- `14-e2e-core.log`: the handoff chooser was not ready; reused the authenticated route barrier.
- `15-e2e-core.log`: apps/id did not match the Catalan heading; captured page content before the assertion.
- `16-e2e-core.log`: the diagnostic showed Playwright's desktop locale was English; initialized Catalan before app bootstrap.
- `18-turbo.log`: the new MSW handler made the expected handler count stale; updated the contract test and asserted `/oauth2/session`.
- `20-e2e-core.log`: an avoidable post-login reload reused a rotated refresh token during the handoff scenario; exercised handoff directly from its initial chooser.
- `21-e2e-core.log`: handoff had persisted the admin profile and made the later chooser scenario skip correctly; unchecked “remember” in handoff to keep fixture state independent.

Intermediate green runs `17-e2e-core.log`, `19-turbo.log`, and `22-e2e-core.log` were superseded by the final working-tree runs above.

Screenshots, all visually inspected after waiting for their ready state:

- Mobile 375 px: `roadmap/evidence/E1-W04/01-entrar-core-375.png`, `02-activacio-core-375.png`, `03b-perfil-acces-core-375.png`, `04-inici-core-375.png`, `12-perfil-core-375.png`, `onboarding-core-375.png`.
- Desktop 1280 px: `roadmap/evidence/E1-W04/admin-entrar-core-1280.png`, `admin-handoff-core-1280.png`, `id-login-core-1280.png`, `id-account-core-1280.png`, `id-products-core-1280.png`, `id-oidc-login-core-1280.png`.

### Files changed

- `package.json`; `scripts/e2e-core.sh`; `scripts/core-stack/docker-compose.yml`; `scripts/core-stack/club-canic.yaml` — root command and self-cleaning published-core/Mongo/mailbox/Playwright stack with runtime-generated password.
- `e2e/core/playwright.config.ts`; `e2e/core/tsconfig.json`; `e2e/core/e1-core.spec.ts` — six serial real-core scenarios, host-aware Vite servers, and 375/1280 evidence capture.
- `apps/clubs/vite.config.ts`; `apps/clubs-admin/vite.config.ts`; `apps/id/vite.config.ts` — optional integration-only Host and Origin proxy headers; normal and MSW defaults are unchanged.
- `apps/id/src/App.tsx`; `apps/id/src/App.test.tsx`; `apps/id/e2e/id.spec.ts` — server-side OIDC flow resumption after password/magic login, remembered flow across the email round trip, and safe same-origin continuation fallback.
- `packages/auth/src/auth-client.ts`; `packages/auth/src/auth-client.test.ts` — typed `/oauth2/session` bridge, whole-restore single flight, and real-core empty-scope normalization.
- `packages/api-client/src/mocks/handlers.ts`; `packages/api-client/src/index.test.ts` — `/oauth2/session` MSW handler and contract assertion.
- `CHANGELOG.md`; `roadmap/tasks/E1-W04.md`; `roadmap/evidence/E1-W04/*` — Unreleased note, executor report, logs, and screenshots.
- Pre-existing `docs/specs/S14-tauler-auditoria-exportacions-rgpd.md` modification was preserved and not touched.

### Rules and tests implemented (R-xx-nn / T-xx-nn)

- `T-01-18`: real password grant reaches the seeded multi-profile account.
- `T-01-19`: real magic-link request, local-mailbox delivery, exchange, activation, and authenticated landing.
- `T-01-20`: real profile update rotates the cookie, persists the member choice, and skips the chooser on the next password login.
- `T-01-21`: real clubs-to-clubs-admin handoff code is created, exchanged, and opens `/tauler`.
- `T-01-22`: apps/id account/products flows and a real PKCE authorize flow resume through bearer-authenticated `POST /oauth2/session {flow}` to the callback code.
- `T-01-26`: both real onboarding policy acceptances complete before the authenticated landing.
- `T-01-04`: concurrent restoration stays single-flight through both refresh and `/me`, covered by a controlled unit test.
- Existing same-origin `?continue=` validation remains covered; off-origin continuations remain rejected.

### Assumptions

- Used the locally available published image rather than staging; revision and architecture are recorded above.
- `CORE_URL=http://core:8080` is intentionally the core hostname visible from the Playwright Compose container. Vite receives integration-only `Host`/`Origin` overrides for `app.example.test`, `admin.example.test`, and `id.example.test`.
- The Docker wrapper generates the seed password at runtime, never prints it, and always removes containers, volumes, and the network through its exit trap.
- Cànic literals and literal theme colors exist only in the integration seed fixture, as permitted for fixtures/seeds; application and locale code remains white-label.
- The handoff scenario leaves profile remembrance disabled so the later remembered-choice scenario starts from the documented chooser state without adding an undocumented account.

### Questions / catalog proposals

- Core contract discrepancy: the published core omits `TokenResponse.scope` when the granted scope set is empty, while the verified OpenAPI marks `scope` required. The frontend currently normalizes only this omission to `""` and keeps generated types unchanged. Proposal to `@organizer`/api: preferably serialize `scope: ""` from the core to match the published contract; otherwise update the next OpenAPI snapshot to make it optional.

## Organizer verification
_(pending)_
