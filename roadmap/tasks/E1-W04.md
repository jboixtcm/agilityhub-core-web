---
id: E1-W04
title: E1 integration against the core (staging or local) and gate evidence (front)
stage: E1
repo: agilityhub-core-web
thread: B
order: 40
status: not_open
depends_on: [E1-W01, E1-W02, E1-W03, external:api:E1-T10]
branch: 
pr: 
updated: 2026-09-06
---

# E1-W04 · E1 integration against the core (staging or local) and gate evidence (front)

## Objective
The front part of the E1 gate: real login by magic link and by password at the Cànic host, 03b with remembered choice, onboarding, handoff to the backoffice, `apps/id` flow.

## Context to load
`roadmap/ROADMAP.md` (gate E1) · the three apps · this file.

## Steps
1. Playwright config with a `CORE_URL` target (local core via docker compose, or staging) using the seeded fictional accounts; the MSW mode stays the default for unit runs.
2. Scenarios: magic link end to end (mailbox = the core's log sender output or the SendGrid test inbox exposed by a small helper endpoint in `local` profile — coordinate with the api E1-T10 smoke), password login, profile choice remembered, onboarding acceptance, handoff to `clubs-admin`, `apps/id` authorize continuation.
3. Screenshots of every screen involved at 375/1280 under `roadmap/evidence/E1-W04/`.

## Verification (run all; paste full output in the report)
- `CORE_URL=… pnpm e2e:core` green (paste); `pnpm turbo run lint typecheck test build` green.

## Definition of Done
Integration suite green against the core; gate items covered.

## Evidence to return
Outputs and screenshots.

---

## Executor report
_(not started)_

### Branch / commits
### Evidence (full command outputs)
### Files changed
### Rules and tests implemented (R-xx-nn / T-xx-nn)
### Assumptions
### Questions / catalog proposals

## Organizer verification
_(pending)_
