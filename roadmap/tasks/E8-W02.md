---
id: E8-W02
title: Member app — screen 12 rows and «Rebuts» (/rebuts, no mockup) with receipt detail and PDF, the invalid-card banner and /me/card-setup, the Stripe checkout return state, the real pack balance on 13, screens 14 «Període d'inactivitat» and 15 «Sol·licitar la baixa», and the D10 billing block (recent receipts, upfront payments, pack per dog)
stage: E8
repo: agilityhub-core-web
thread: B
order: 20
status: ready
depends_on: [E5-W01, E3-W01, external:api:E8-T01]
branch:
pr:
updated: 2026-09-19
---

# E8-W02 · Member views of billing (12/rebuts, 13, checkout return), screens 14 and 15, and the D10 billing block

## Objective
Work packages S12 **WP-12-G** and S13 **WP-13-D**. The member sees her own receipts with their frozen lines and downloads the PDF, is told when her card could not be charged and fixes it in two taps, sees the real pack balance of each dog on 13, and requests an inactivity period (14) or her leave (15) with every rule of S13 §2 rendered from the api context. The administrator gets the **read** half of D10's billing: recent receipts, upfront payments with [Registra un pagament], the pack card per dog with [Ajusta], and the «Compte no informat» / «Targeta no vàlida» badges. Mocks-first on the S12 + S13 contract of api E8-T01; pixel-faithful to the V8 mockups at 375 px.

## Spec references
S12 §2 rows **«12 → Rebuts»**, **13 (pack)**, **19 / 06 (Checkout)** and **D10 (bloc)** · R-12-22 (member card: banner, `POST /me/card-setup`, «Targeta no vàlida»), R-12-23 (upfront payments and pack opening; manual registration from D10 with `amountPaid ≤ amountDue`), R-12-24 (pack consumption, expiry, `[Ajusta]` with a new `expiresOn` when reopening an `EXPIRED` balance), R-12-27 (`GET /me/invoices`, family-group receipts flagged, **never an IBAN**), R-12-28 (`BILLING` off → no «Rebuts» row at all; `PACKS` off → no pack block), R-12-30 (frozen descriptions + the member's own locale for the month and the amount) · §5 (`Invoice` statuses) · §6 rows `GET /me/invoices`, `GET /me/invoices/{id}`, `GET /me/invoices/{id}/document`, `GET /me/pack-balances`, `POST /me/card-setup`, `GET /checkout-sessions/{id}`, `GET /upfront-payments`, `POST /upfront-payments`, `GET /pack-balances`, `POST /pack-balances/{id}/adjustments` · §8 N-30, N-35 (the banner's `retry_link` is `/me/card-setup`), N-38 · §10 (namespace `billing`, `enums:invoiceStatus.*`, `enums:upfrontConcept.*`) · §13-11 (no in-app pack purchase at R1), §13-12 (12/rebuts has no mockup) · **T-12-26** and the member half of **T-12-32** (`en` member sees «September 2026 · €60.00» with the frozen `ca` description).
S13 §2 rows **12**, **14** and **15** · R-13-01 (`E(avui)`, `deadlineDay`, everything computed **by the api** — the front only renders `earliestFromMonth`/`proposedFromMonth`), R-13-02 (request rules, `INACTIVITY_NOT_APPLICABLE` for pack plans → the row/button is not offered), R-13-03 (day-25 rule, `INACTIVITY_DEADLINE_PASSED {earliestMonth}`), R-13-04 (member edits and withdrawal, `editable {fromMonth, toMonth, cancel}`), R-13-06 (the yellow booking note), R-13-08 (`fee` card), R-13-09 (leave request, reasons, NPS, comment), R-13-11 (`fullMonthIfLater` help text), R-13-17 (the status subtitles on 12), R-13-18 (a member only ever sees her own period/request) · §6 rows `GET /me/inactivity-periods`, `GET /me/inactivity-periods/preview`, `POST /me/inactivity-periods`, `PATCH /me/inactivity-periods/{id}`, `POST /me/inactivity-periods/{id}/cancellation`, `GET /me/leave-requests`, `POST /me/leave-requests`, `POST /me/leave-requests/{id}/cancellation` + the `GET /me/inactivity-periods` JSON example · §10 (namespaces `inactivity`, `leave`, `enums:inactivityState.*`, `enums:leaveRequestState.*`, the ICU plural `inactivity:form.bookingsInside`, the five reason labels) · §13 assumptions 9 (consultation mode and the 12 subtitles) · tests **T-13-29**, **T-13-30** and the member half of **T-13-32** (ca/es/en snapshots, vocabulary lint) · mockups `docs/pantalles/mobil/12-perfil-i-preferencies.*`, `13-els-meus-gossos.*`, `14-sollicitar-periode-d-inactivitat.*`, `15-sollicitar-la-baixa.*`, `docs/pantalles/escriptori/D10-fitxa-d-abonat.*`.
Decisions: **B11** (unused pack sessions are lost at expiry — no «es recuperaran» wording anywhere), **B12** (pack plans cannot request inactivity), **B10** (`MAINTENANCE_FEE` belongs to the plan; D10 shows the billing mode read-only), **A14** (no in-app payment screen at R1 at the Cànic) · `docs/specs/00-transversal/CATALEG_ERRORS.md`, `CATALEG_NOTIFICACIONS.md`, `MATRIU_PERMISOS.md` (MEMBER: read own invoices, write own inactivity/leave request; INSTRUCTOR: nothing), `CONVENCIONS_I18N.md`.

## Context to load
`docs/specs/S12-facturacio-i-pagaments.md` §2, §6, §10, §11 (front) and `docs/specs/S13-inactivitat-i-baixa.md` §2, §4 (R-13-01…04, 06, 08, 09, 11, 17, 18), §5, §6, §10, §11, §13 · api `roadmap/tasks/E8-T01.md` (schemas `Invoice`, `InvoiceLine`, `PackBalance`, `PackMovement`, `UpfrontPayment`, `InactivityPeriod`, `MeInactivityContext`, `InactivityPreview`, `LeaveRequest`, `MeLeaveContext`, `PlannedLeave`) · `apps/clubs/src/App.tsx` (`MOBILE_ROUTES` — `/perfil`, `/gossos`, and the **already declared** `/inactivitat` and `/baixa`; screen 12's rows were built by E1-W01 with the inactivity/leave rows left as E8 placeholders) · `apps/clubs/src/SelfServicePages.tsx` (`MyDogsPage` — E2-W03 already renders the pack block from `dog.pack`; `census:myDogs.pack`, `available`, the progress track) · `apps/clubs-admin/src/census/CensusRecordPage.tsx` (D10 sections; **E8-W03 adds the drawers to this same file — keep your additions in new components and a single mount point**) · `packages/ui/src/components.tsx` (`Card`, `Badge`, `Chip`, `Select`, `Input`, `Textarea`, `FormField`, `RadioGroup`, `Drawer`, `Modal`, `EmptyState`, `Skeleton`, `useToast`) · `packages/i18n/src/format.ts` (`fmtMoney`, `fmtMonth`, `fmtDate` long/short, `useClubFormats`) · `packages/api-client/src/mocks/fixtures/member-self-service.ts` · `docs/playbooks/screen-from-mockup.md`, `checklist.md` · this file.

## Steps
0. **Contract.** Same step 0 as every E8 web task: if `packages/api-client/openapi/openapi.next.json` is staged → swap byte for byte (`cmp`), `pnpm api:generate`, prune this task's S12/S13 paths from `pending.json`, run the Ajv fixture suite; otherwise declare them in `pending.json` from S12 §6 / S13 §6 with E8-T01's schema names and Ajv-validated fixtures and say so in the report. Add to `DEFAULT_IDEMPOTENT_PATHS`: `/^\/me\/inactivity-periods$/`, `/^\/me\/inactivity-periods\/[^/]+\/cancellation$/`, `/^\/me\/leave-requests$/`, `/^\/me\/leave-requests\/[^/]+\/cancellation$/`, `/^\/me\/card-setup$/`, `/^\/upfront-payments$/`, `/^\/pack-balances\/[^/]+\/adjustments$/`.
1. **Screen 12 rows** (`apps/clubs/src/App.tsx`, profile list — replace the E1-W01 placeholders, keep the mockup order: name card → «Els meus gossos (n)» → «Canvia la contrasenya» → «Canviar de perfil» → avisos → «Idioma» → **«Rebuts»** → «Sol·licitar període d'inactivitat» → «Sol·licitar la baixa» → «Tanca la sessió»):
   - New row **«Rebuts»** → `/rebuts`, rendered only when `BILLING` is in `branding.modules` (S12 R-12-28). The V8 mockup of 12 has no such row (the V2 annotation dropped «Rebuts i packs» from the *notification matrix*); adding it is the S12 §2 contract and an assumption to validate with Josep in staging — say so in the report.
   - **«Sol·licitar període d'inactivitat»** → `/inactivitat`, rendered only when `INACTIVITY` is on **and** `GET /me/inactivity-periods` does not answer `422 INACTIVITY_NOT_APPLICABLE` (pack plan, B12).
   - **«Sol·licitar la baixa»** → `/baixa` (no module gate).
   - Status subtitles (S13 §2, §13-9): «pendent d'aprovació · {month}» (a `REQUESTED` period), «inactivitat {from}–{to}» / «inactivitat des de {from}» (open), «baixa sol·licitada el {date}» (a `PENDING` request), «baixa el {date}» (`plannedLeave`). Keys `inactivity:profileRow.*` and `leave:profileRow.*`; one call each to `GET /me/inactivity-periods` and `GET /me/leave-requests`, both tolerant of `404 MODULE_DISABLED`.
   - The repo route is `/inactivitat` (S13 §2 writes `/perfil/inactivitat`): **keep the repo path**, it round-trips already and E1-W01's tests assert it — record the deviation in the report.
2. **`/rebuts`** (new route in `MOBILE_ROUTES`, MEMBER; **no mockup** → design system, Josep validates in staging): `AppBar` «‹ Rebuts» + list from `GET /me/invoices` (`page`, `size`, infinite «Carrega'n més» button, never an endless scroll). One row per receipt: month (`fmtMonth(period)` in the **member's** locale), the frozen `lines[0].description`, amount (`fmtMoney`), a status `Badge` from `enums:invoiceStatus.*` («pendent» · «cobrat» · «impagat» · «anul·lat» · «reemborsat»; `COLLECTING` reads «remesat»), and the chip `billing:familyGroup` «Grup familiar» when the item carries `familyGroup: true` (R-12-27). Row → `/rebuts/:id` (`GET /me/invoices/{id}`): the frozen line table, the total, the payment method **masked exactly as the api returns it** (never an IBAN), and **[Descarrega el justificant]** → `GET /me/invoices/{id}/document` opened as a PDF in a new tab. Empty state `billing:empty` «Encara no tens cap rebut.»; `404 MODULE_DISABLED` → the route is not reachable because the row is hidden, but handle it with the shared module-off state anyway.
3. **Invalid-card banner** (R-12-22): when `GET /me` (or the me-context E1 already loads) reports `paymentMethod.type = CARD` with `invalid = true`, **or** the last receipt is `FAILED` with a card collection, render a warning banner at the top of `/rebuts` **and** of screen 12: `billing:cardBanner.title` = «No hem pogut cobrar el rebut de {month}» + **[Actualitza la targeta]** → `POST /me/card-setup {successUrl, cancelUrl}` → `201 {checkoutUrl}` → `window.location.assign(checkoutUrl)`. `422 PAYMENT_PROVIDER_NOT_ENABLED` → toast, banner stays. The banner is the UI half of **N-35** (`retry_link = /me/card-setup`); do not duplicate the notification text.
4. **Checkout return** (S12 §2 row «19 / 06»): factor the return handling that E3-W01 (signup) and E5-W01 (single-class payment) already do into one shared component `apps/clubs/src/payments/CheckoutReturn.tsx` — read `checkoutSessionId` from the return URL, poll `GET /checkout-sessions/{id}` every second for at most **10 s**, show `billing:checkout.pending` «Estem confirmant el pagament…» meanwhile and `billing:checkout.paid` «Pagament rebut» only on `status = PAID`; `EXPIRED` → `billing:checkout.expired` with a [Torna-ho a provar] that reopens the flow the caller passed in. **The webhook is the truth; the redirect never is** — never mark anything paid on the front. Reuse it from both callers rather than forking a third copy; if the two existing implementations differ, unify on this one and say what you removed.
5. **Screen 13 — real pack balance** (S12 §2 row «13 (pack)»): `MyDogsPage` already renders the block from `dog.pack` (E2-W03). Point it at `GET /me/pack-balances` (`PACKS` only) keyed by `dogId`, and render the mockup line exactly: «Pack {sessionsTotal}» · «caduca {expiresOn}» (`fmtDate` short) · progress bar · «{consumed} consumides · {remaining} disponibles» (`census:myDogs.*` keys already exist — extend, do not rename). An `EXPIRED` balance shows the `Badge` `census:myDogs.packExpired` «caducat» and keeps the numbers visible; **never** suggest the remaining sessions can still be used or recovered (B11). Two live balances for the same dog → show the one expiring first and «+1 pack» (`census:myDogs.packMore`). `PACKS` off → block absent.
6. **Screen 14 `/inactivitat`** (MEMBER and impersonated): `GET /me/inactivity-periods` paints the context. Literals exactly as the mockup (`inactivity` namespace, `ca` values):
   - title «Període d'inactivitat»; intro `inactivity:intro` = the mockup paragraph with «dia {deadlineDay}» interpolated from the response (never a hard-coded 25).
   - «Mes d'inici (obligatori)» — `Select` of months from `earliestFromMonth` forward (`inactivity.maxStartMonthsAhead` worth of options as the api bounds them), preselected `proposedFromMonth`, labels `fmtMonth`.
   - «Mes de finalització (si el saps)» — first option **«— encara no ho sé»** (value `null`), then months `≥` the chosen start.
   - «Comentaris» — `Textarea` ≤ 500.
   - fee card «Quota del 1r mes · {fee.firstMonth}» / «Mesos següents · {fee.followingMonths}/mes», rendered **only** when the response carries `fee` (`BILLING` on).
   - grey note `inactivity:endNote` «La data de final es pot modificar durant el període d'inactivitat (també fins al dia {deadlineDay}).».
   - yellow note **only** when `preview.bookingsInside.total > 0`, with the ICU plural `inactivity:form.bookingsInside` = `{count, plural, one {Ara tens # reserva dins del període: s'anul·larà si el club aprova la sol·licitud.} other {Ara tens # reserves dins del període: s'anul·laran si el club aprova la sol·licitud.}}`; recomputed on every month change through `GET /me/inactivity-periods/preview?fromMonth=&toMonth=` (debounced 300 ms, previous request aborted).
   - **[ENVIA LA SOL·LICITUD]** → `POST /me/inactivity-periods {fromMonth, toMonth?, comments?}` with `Idempotency-Key` → toast and back to `/perfil`.
   - **Consultation mode** with a live period (`REQUESTED`/`APPROVED`/`ACTIVE`, §13-9): the months come from the period, each field is enabled only per `editable {fromMonth, toMonth, cancel}`, and the buttons become **[MODIFICA]** → `PATCH /me/inactivity-periods/{id} {…, version}` and **[RETIRA LA SOL·LICITUD]** → confirmation → `POST /me/inactivity-periods/{id}/cancellation`.
   - Errors rendered **on the form**, not only as toasts: `422 INACTIVITY_DEADLINE_PASSED` → red note with `details.earliestMonth` (`inactivity:errors.deadlinePassed` «Ja ha passat el dia {deadlineDay}: el primer mes que pots demanar és {month}.»); `409 INACTIVITY_OVERLAP` → «Ja tens un període demanat: modifica'l» with a link to the live period; `422 INACTIVITY_INVALID_RANGE`; `409 MEMBER_NOT_ACTIVE`; `409 LEAVE_ALREADY_SCHEDULED`; `409 STALE_VERSION` → refetch + «El període ha canviat.».
7. **Screen 15 `/baixa`**: `GET /me/leave-requests` paints the context (`leave` namespace):
   - offer block, only when `offerInactivity` — the mockup paragraph with the amounts sentence rendered **only** when `fee` is present — + **[VULL DEMANAR INACTIVITAT]** → `/inactivitat`.
   - «Data en què vols la baixa» — date field preset to `defaultDate` shown as **«Avui, {date}»** (`leave:form.today`, `fmtDate` long: «Avui, 11 d'agost de 2026»), minimum today (club-local, from `/branding` `timeZone`).
   - help `leave:form.fullMonth` «Si tries un dia d'un mes posterior a l'actual, aquell mes es cobrarà íntegrament.» only when `fullMonthIfLater` **and** `BILLING`.
   - «Motiu» — `Select` with placeholder **«Tria un motiu»**, options from `reasons[{key, label}]` (labels come localized from the api; the `ca` set is «Ja he après tot el que volia», «No trobo temps per anar-hi», «No és el que esperava», «Condicionants meus aliens al club», «Altres»). Required.
   - NPS block, only when `npsEnabled`: «De 0 a 10, amb quina probabilitat ens recomanaries?» as eleven selectable chips 0…10, optional; free text «Què podríem millorar perquè el club s'adaptés més a les teves necessitats?» ≤ 2000.
   - **[ENVIA LA SOL·LICITUD]** → `POST /me/leave-requests {requestedDate, reasonKey, nps?, comment?}` with `Idempotency-Key` → toast + footer **«El club la revisarà i et confirmarà la data d'efecte.»** (`leave:form.footer`) + back to `/perfil`.
   - A `PENDING` request → consultation mode with **[RETIRA LA SOL·LICITUD]** → `POST /me/leave-requests/{id}/cancellation`. A `plannedLeave` → read-only text `leave:plannedLeave` «Tens la baixa prevista el {date}. Per canviar-la, contacta amb el club.» and no form.
   - Errors: `409 LEAVE_ALREADY_REQUESTED`, `409 LEAVE_ALREADY_SCHEDULED`, `409 MEMBER_NOT_ACTIVE`, `422 LEAVE_DATE_INVALID` (on the date field), `400 LEAVE_REASON_UNKNOWN` (on the select), `400 VALIDATION_ERROR`/`READ_ONLY` when `nps` is sent with `npsEnabled = false` (should be impossible — the block is absent).
8. **D10 billing block** (`apps/clubs-admin/src/census/MemberBillingBlock.tsx`, mounted once from `CensusRecordPage.tsx`, `BILLING` only): «Rebuts recents» = the 5 latest of `GET /invoices?filter=memberId:eq:{id}&sort=issueDate,desc&size=5` (number, concept, amount, status badge) + link «Tots els rebuts ›» → `/facturacio?filter=memberId:eq:{id}`; «Pagaments a l'acte» = `GET /upfront-payments?memberId={id}` (concept via `enums:upfrontConcept.*`, `amountDue`/`amountPaid`, status) + **[Registra un pagament]** → `Modal` `{dogId?, concept, amountDue, amountPaid, channel, paidAt, reference?, note?}` → `POST /upfront-payments` (`422 AMOUNT_EXCEEDS_DUE` → on the amount field, `422 PLAN_NOT_PACK` → on the concept); per dog with a pack, the card «Pack {sessionsTotal} — {consumed} consumides · {remaining} disponibles · caduca el {expiresOn}» from `GET /pack-balances?memberId={id}` + **[Ajusta]** → `Modal {delta, reason, expiresOn?}` → `POST /pack-balances/{id}/adjustments` (`expiresOn` **required** when the balance is `EXPIRED`, R-12-24; `422 PACK_NEGATIVE` → on the delta field); badges «Compte no informat» (S03, already present) and **«Targeta no vàlida»** (R-12-22) in the record header. Read-only otherwise — the payment-method and plan drawers belong to **E8-W03**: do not add them here.
9. **i18n**: new namespaces **`billing`** (`list.*`, `detail.*`, `cardBanner.*`, `checkout.*`, `familyGroup`, `empty`), **`inactivity`** (`intro`, `form.*`, `profileRow.*`, `errors.*`, `endNote`), **`leave`** (`offer.*`, `form.*`, `profileRow.*`, `plannedLeave`, `errors.*`), plus new `enums` keys `inactivityState.*` («sol·licitat» · «aprovat» · «actiu» · «finalitzat» · «denegat» · «anul·lat»), `leaveRequestState.*` («pendent» · «aprovada» · «denegada» · «anul·lada») and `upfrontConcept.*`, and the `admin-census` additions of step 8; register them in `packages/i18n/src/types.ts` and the three loaders of `resources.ts`. `ca` = the mockup literals quoted above; `es` and `en` in the same task. Months with `fmtMonth`, dates with `fmtDate`, amounts with `fmtMoney` — all in the **member's** locale while the receipt line description stays frozen in the club's `defaultLocale` (R-12-30, the member half of T-12-32). Vocabulary lint: never «parella»/«parelles», no «amigable», no «(paràmetre)», no requirement codes.
10. **MSW**: extend `packages/api-client/src/mocks/fixtures/member-self-service.ts` and add `fixtures/inactivity.ts` — Laura with three `SEPA_DD` receipts (one `PAID`, one `COLLECTING`, one `FAILED`), a family-group receipt flagged `familyGroup: true`, Rock's pack «Pack 10 · caduca 12-11-2026 · 6 consumides · 4 disponibles», the `GET /me/inactivity-periods` JSON of S13 §6 verbatim (`earliestFromMonth` `2026-10`, `deadlineDay` 25, fee 20,00/10,00 €, one `REQUESTED` period «Descans de la Duna» with `editable` all true), a preview with `bookingsInside.total = 1`, and a `MeLeaveContext` with the five reasons, `npsEnabled: true`, `fullMonthIfLater: true`, `defaultDate` = today. Scenario variants: `memberCardInvalid` (banner + `POST /me/card-setup` returning a fake `checkoutUrl`), `memberPackPlan` (`422 INACTIVITY_NOT_APPLICABLE` → no inactivity row on 12), `memberPlannedLeave` (15 in read-only mode, 12 subtitle «baixa el {date}»), `memberDeadlinePassed` (`422 INACTIVITY_DEADLINE_PASSED {earliestMonth: "2026-11"}`), `billingOff` (no «Rebuts» row, no fee card, no pack block). Update the handler-count assertion in `src/index.test.ts`.
11. **Tests and evidence**: Vitest — `apps/clubs/src/billing/InvoicesPage.test.tsx` **T-12-26** (list with the frozen descriptions and the family-group chip; detail with the masked method and the PDF link; the banner appears only with `invalid = true` and [Actualitza la targeta] calls `POST /me/card-setup`; `BILLING` off → no row on 12) · `apps/clubs/src/inactivity/InactivityPage.test.tsx` **T-13-29** (proposed month preselected, «— encara no ho sé» default, yellow note only with `total > 0` and with the right plural, fee card only with `fee`, `INACTIVITY_DEADLINE_PASSED` shows `earliestMonth`, consultation mode with a live period) · `apps/clubs/src/leave/LeavePage.test.tsx` **T-13-30** (offer block and [VULL DEMANAR INACTIVITAT] only with `offerInactivity`, «Avui, {date}» formatted in the club time zone, catalog reasons, NPS 0–10 only with `npsEnabled`, full-month help only with `fullMonthIfLater`, footer literal after sending) · a ca/es/en literal snapshot of 14 and 15 with no missing key (the member half of **T-13-32**) · `MemberBillingBlock.test.tsx` (recent receipts, upfront modal validation, pack adjust requiring `expiresOn` on an expired balance). Playwright `apps/clubs/e2e/billing-inactivity.spec.ts` with screenshots under `roadmap/evidence/E8-W02/`: `12-perfil-rows-375.png`, `rebuts-375.png`, `rebut-detall-375.png`, `rebuts-baner-targeta-375.png`, `13-pack-375.png`, `14-inactivitat-375.png`, `14-inactivitat-consulta-375.png`, `15-baixa-375.png`, and `D10-bloc-facturacio-1280.png`; compare 13, 14 and 15 with `docs/pantalles/mobil/13-*.png`, `14-*.png`, `15-*.png` and list every deviation.

## Fixed conventions
- Design system only; mobile-first at 375 px; the member app never shows an IBAN, a mandate reference beyond what the api masks, or any provider secret.
- Every temporal rule is the api's: `E(avui)`, `earliestFromMonth`, `deadlineDay`, `defaultDate`, `editable` and `lastInvoicedMonth` are rendered, never recomputed. The front formats with the club `timeZone` from `/branding`.
- Closed catalogs. Statuses to code against exactly as `CATALEG_ERRORS.md` fixes them (they win over S12 §6 and S13 §6): **409** `INACTIVITY_OVERLAP`, `INACTIVITY_INVALID_STATE`, `LEAVE_ALREADY_REQUESTED`, `LEAVE_ALREADY_SCHEDULED`, `LEAVE_INVALID_STATE`, `MEMBER_NOT_ACTIVE`, `INVALID_STATE`, `STALE_VERSION`; **422** `INACTIVITY_DEADLINE_PASSED`, `INACTIVITY_INVALID_RANGE`, `INACTIVITY_NOT_APPLICABLE` (new, filed by api E8-T01), `LEAVE_DATE_INVALID`, `AMOUNT_EXCEEDS_DUE`, `PLAN_NOT_PACK`, `PACK_NEGATIVE`, `PAYMENT_PROVIDER_NOT_ENABLED`, `MEMBER_LEAVING`; **400** `LEAVE_REASON_UNKNOWN`, `VALIDATION_ERROR`; **404** `MODULE_DISABLED`, `NOT_FOUND`; **403** `FORBIDDEN`, `READ_ONLY`. Missing item → proposal in the report and `roadmap/MESSAGES.md`, closest existing item shipped meanwhile.
- Assumptions in force (do not ask): 12/rebuts has **no mockup** and the «Rebuts» row on 12 is an S12 §2 addition — design system, Josep validates in staging (§13-12, B24/B25); pack plans cannot request inactivity (B12) and expired pack sessions are **lost** (B11); there is no in-app pack purchase and no payment screen at R1 (A14, §13-11); a member never sees another member's receipt, period or request — **including inside her own family group** (R-13-18) — except the family-group receipt of the holder, which `GET /me/invoices` returns flagged (R-12-27).
- Evidence rule: report = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E8-W02/NN-<name>.log`; task file ≤ 120 KB. Fictional data only (`@example.test`). Never edit other tasks, `ROADMAP.md` or the Organizer verification section. English identifiers; keys never literals; ca/es/en together.
- `CensusRecordPage.tsx` is also touched by **E8-W03**: keep everything of step 8 inside `MemberBillingBlock.tsx` with a single mount line so the two tasks do not collide.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `pnpm api:generate` (after step 0) and `pnpm --filter @agilityhub/api-client test -- src/mocks/fixtures.contract.test.ts`.
- `pnpm turbo run lint typecheck test build`; `pnpm i18n:check`; Vitest T-12-26, T-13-29, T-13-30 and the ca/es/en snapshot; the complete `pnpm e2e` (three Playwright projects) in `mcr.microsoft.com/playwright:v1.63.0-noble`; the nine screenshots.

## Definition of Done
A member sees and downloads her receipts, is warned about an invalid card and fixes it through `/me/card-setup`, sees the real pack balance per dog, requests and withdraws an inactivity period with the day-25 rule and the booking warning rendered from the api, and requests or withdraws her leave with the reasons, NPS and full-month help of the mockup — in ca, es and en, with the module variants (`BILLING` off, `PACKS` off, `INACTIVITY` off, pack plan) behaving as S12 R-12-28 and S13 §9 say. D10 shows the billing block with its two modals and the «Targeta no vàlida» badge.

## Evidence to return
Command outputs; the snapshot adoption proof (or the `pending.json` diff); the nine screenshots with the deviations against the V8 mockups; the key list for `billing`, `inactivity`, `leave` and the new `enums`; the deviations you recorded (the `/inactivitat` route vs `/perfil/inactivitat`, the added «Rebuts» row on 12) with their justification.

---

## Executor report
_(not started)_

### Branch / commits

### Evidence (full command outputs)

### Files changed

### Rules and tests implemented (R-xx-nn / T-xx-nn)

### Assumptions

### Questions / catalog proposals

## Organizer verification
_(pending)_
