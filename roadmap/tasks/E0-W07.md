---
id: E0-W07
title: Front CI: lint, typecheck, tests, i18n check, generated types diff, Playwright, bundle size
stage: E0
repo: agilityhub-core-web
thread: B
order: 70
status: ready
depends_on: [E0-W06]
branch: 
pr: 
updated: 2026-09-05
---

# E0-W07 · Front CI: lint, typecheck, tests, i18n check, generated types diff, Playwright, bundle size

## Objective
Every push to `main` proves the front is green, translated, typed against the contract and within the size budget.

## Context to load
`AGENTS.md` · this file.

## Steps
1. `.github/workflows/ci.yml`: Node 22 + pnpm cache; `pnpm install --frozen-lockfile`; `pnpm turbo run lint typecheck test build`; `pnpm i18n:check`; `pnpm api:generate` followed by `git diff --exit-code packages/api-client/src/generated`; Playwright (chromium) e2e for `clubs` and `clubs-admin` with MSW; upload screenshots as artifacts; `size-limit` on `apps/clubs` (initial JS chunk ≤ 300 kB gzip).
2. `.github/dependabot.yml` (npm weekly). README: branch protection to enable on `main` (Jordi).

## Verification (run all; paste full output in the report)
- Run the same steps locally in order and paste the outputs (`pnpm turbo run lint typecheck test build`, `pnpm i18n:check`, `pnpm api:generate && git diff --exit-code packages/api-client/src/generated`, e2e, `pnpm size-limit`). The CI run happens when the publish script pushes `main`; the organizer checks it.
- Prove the lint gate locally: a throw-away literal color → `pnpm lint` red; revert (paste both).

## Definition of Done
Workflow committed · size budget enforced · both proofs pasted.

## Evidence to return
The local outputs above.

---

## Executor report
_(not started)_

### Branch / commits
### Evidence (full command outputs)
### Files changed
### Rules and tests implemented (R-xx-nn / T-xx-nn)
### Assumptions
### Questions / catalog proposals

## Organizer verification
_(pending)_
