---
id: E8-W01
title: D6 «Facturació» (/facturacio) — month header, simulation with incidents and cash members, KPIs, strong confirmations, generation, card charges, rollback typed «RETROCEDIR», universal invoice list with chips and bulk mark-paid, invoice drawer, manual adjustment receipts, the remittances page (/facturacio/remeses, no mockup) and the accounting export
stage: E8
repo: agilityhub-core-web
thread: B
order: 10
status: ready
depends_on: [E2-W01, E2-W05, external:api:E8-T01]
branch:
pr:
updated: 2026-09-19
---

# E8-W01 · D6 «Facturació» + remittances + accounting export

## Objective
Work packages S12 **WP-12-F** and the front half of **WP-12-H**. The administrator opens `/facturacio?mes=YYYY-MM`, simulates the month and reads the incidents first, sees the cash members with their planned leave date and the four (or five) KPIs, generates the month behind a strong confirmation, charges the cards when the club has Stripe, rolls the whole generation back by typing «RETROCEDIR», filters the receipts with the chips of the mockup, marks a selection paid, opens a receipt drawer and runs every state action on it, issues a manual adjustment receipt, downloads the accounting export, and manages the remittance files on `/facturacio/remeses`. Everything is decided by the api: the front never recomputes a total, a number, a status or the rollbackability of a run. Mocks-first on the S12 contract of api E8-T01; pixel-faithful to the V7 mockup at 1280 px.

## Spec references
S12 §2 rows **D6** and **D6 (remeses)** · R-12-05 (cash: the `SEMESTER` receipt carries `k` monthly lines — the front renders what the api returns), R-12-07 (simulation; incidents never block; `409 SIMULATION_STALE`), R-12-08 (`displayNumber`), R-12-09 (`Money {amountMinor, currency}`), R-12-10 (immutability: no edit UI on an issued receipt), R-12-11 (generation, `Idempotency-Key`), R-12-13 (card charges, `202`), R-12-14 (rollback + `rollbackBlockers[]`), R-12-15 (mark as sent to the bank), R-12-16 (mark paid, single and bulk), R-12-17 (manual unpaid), R-12-18 (card retry, `MAX_ATTEMPTS`), R-12-19 (cancellation and manual adjustment receipt), R-12-20 (refunds, `refundedTotal` → «reemborsat»), R-12-26 (accounting export, `listKey = accounting`), R-12-28 (modules and provider variants: the button-2 label), R-12-29 (idempotency, `version`, tenant), R-12-30 (frozen descriptions, `fmtMoney`, club time zone) · §5 (the `Invoice`, `Collection`, `Remittance` and `BillingRun` state machines — the front renders `status`, never derives it) · §6 **every row this screen calls** (listed in step 0) plus the simulation JSON · §10 (`admin-billing`, `enums:invoiceStatus.*`, `enums:paymentMethodType.*`, `enums:billingIncident.*`) · §13-12, §13-13 · tests **T-12-25** and the D6 half of **T-12-32** · `docs/DECISIONS_PENDENTS.md` **A28** (`billing.sepa.collectionDayOfMonth = 1` = day of the **billed** month, `0` = last day), **A29** (`billing.cashInvoicing = SEMESTER` at the Cànic, `MONTHLY` equally product), **B3** (no VAT at the Cànic, the tax mechanism stays), **B8** (24 migrated months in series `PLAYOFF`), **B10** (`MAINTENANCE_FEE` comes from the plan) · `docs/specs/00-transversal/CONVENCIONS_API.md` §4 (universal list `filter=<field>:<op>:<value>`, `sort`, `q`) and §6 (`ApiError {code, message, details, traceId}`), `CATALEG_ERRORS.md`, `MATRIU_PERMISOS.md` row «Facturació» (MEMBER = own invoices only, **INSTRUCTOR = nothing**, ADMIN = RW + X), `CONVENCIONS_I18N.md` · mockup `docs/pantalles/escriptori/D6-facturacio-simulacio-rebuts-i-remesa-sepa.html|png`.

## Context to load
`docs/specs/S12-facturacio-i-pagaments.md` §2, §5, §6, §9, §10, §11 (front), §13 · api `roadmap/tasks/E8-T01.md` for the schema names this screen consumes (`BillingPeriod`, `BillingSimulation`, `BillingRun`, `Remittance`, `Invoice`, `InvoiceLine`, `InvoiceListItem`, `Collection`, `UpfrontPayment`) · `packages/api-client` (`openapi/openapi.json`, `openapi/pending.json`, `scripts/generate.mjs`, `src/client.ts` → `DEFAULT_IDEMPOTENT_PATHS`, `src/mocks/{handlers,scenarios,fixtures.contract.test}.ts`, `src/mocks/fixtures/`) · `packages/ui/src/universal-list.tsx` and `components.tsx` (`Card`, `Chip`, `Badge`, `Button`, `Modal`, `Drawer`, `Select`, `Input`, `FormField`, `Checkbox`, `DataTable`, `Skeleton`, `EmptyState`, `useToast`) · `apps/clubs-admin/src/App.tsx` (`ADMIN_ROUTES` already declares `/facturacio` and `/facturacio/remeses` as placeholders; sidebar entry `billing` = `shell:nav.billing` «Facturació») · `apps/clubs-admin/src/census/CensusListPage.tsx` (the reference `UniversalList` wiring: filters, saved views, export) and `catalogs/ParameterSettings.tsx` (drawer + confirmation patterns) · `packages/i18n/src/{types.ts,resources.ts,format.ts}` (`fmtMoney`, `fmtDate`, `fmtMonth`, `useClubFormats`) · `docs/playbooks/screen-from-mockup.md`, `checklist.md` · this file.

## Steps
0. **Contract.** Normal path: the organizer staged `packages/api-client/openapi/openapi.next.json` from the api clone → swap it into `openapi.json` byte for byte (`cmp`), `pnpm api:generate`, prune every S12 path and schema of this task from `pending.json`, run the Ajv fixture suite (`packages/api-client/src/mocks/fixtures.contract.test.ts`). If the staged snapshot is **not** there: go mocks-first — declare these S12 §6 routes in `pending.json` with E8-T01's schema names and Ajv-validated fixtures, and say so in the report: `GET /billing/periods/{period}` · `POST /billing/simulations` · `POST /billing/runs` · `GET /billing/runs/{id}` · `POST /billing/runs/{id}/card-charges` · `POST /billing/runs/{id}/rollback` · `GET /remittances` · `GET /remittances/{id}` · `GET /remittances/{id}/file` · `POST /remittances/{id}/submission` · `GET /invoices` · `GET /invoices/{id}` · `POST /invoices` · `POST /invoices/{id}/payment` · `POST /invoices/payments` · `POST /invoices/{id}/failure` · `POST /invoices/{id}/retry` · `POST /invoices/{id}/refund` · `POST /invoices/{id}/cancellation` · `GET /invoices/{id}/document` · `GET /billing/exports`. Either way add to `DEFAULT_IDEMPOTENT_PATHS`: `/^\/billing\/runs$/`, `/^\/billing\/runs\/[^/]+\/(card-charges|rollback)$/`, `/^\/invoices$/`, `/^\/invoices\/payments$/`, `/^\/invoices\/[^/]+\/(payment|failure|retry|refund|cancellation)$/`, `/^\/remittances\/[^/]+\/submission$/`.
1. **Routes and files.** Replace the two placeholders in `apps/clubs-admin/src/App.tsx`: `/facturacio` (query `?mes=YYYY-MM`, default = the club-local current month) and `/facturacio/remeses`, both `roles: ["ADMIN"]` (INSTRUCTOR gets no route and no sidebar entry — `MATRIU_PERMISOS`). New files `apps/clubs-admin/src/billing/BillingPage.tsx`, `InvoiceDrawer.tsx`, `ManualInvoiceModal.tsx`, `RemittancesPage.tsx`, `billing.css`. The whole vertical is hidden when `BILLING` is not in `branding.modules` (the sidebar entry too); a direct hit on the route then renders the shared «mòdul no disponible» state instead of a toast (the api answers `404 MODULE_DISABLED`).
2. **Header** (D6): title `fmtMonth` of the period («Setembre 2026», `es` «Septiembre 2026», `en` «September 2026») with the ‹ / › month steppers of the mockup (`admin-billing:header.previousMonth` / `nextMonth`, accessible names; the visible glyph row is «‹ mes · mes ›»). Changing month rewrites `?mes=` and refetches `GET /billing/periods/{period}`; that single call drives the whole screen (`simulation?`, `run?`, `remittance?`, `counts`). Three action buttons, ADMIN only:
   - **[1 · SIMULA EL MES]** (`admin-billing:actions.simulate`) → `POST /billing/simulations {period}` → refetch. `400` for a period more than three months ahead → field-less toast from `errors:`; `409 BILLING_BUSY` → toast «Hi ha una generació en curs. Torna-ho a provar d'aquí a uns minuts.» (`admin-billing:errors.billingBusy`).
   - **[2 · GENERA REMESA SEPA (XML)]** (`actions.generateSepa`) when the club has `SEPA_XML` among its enabled `paymentProviders`; **«2 · GENERA ELS REBUTS I COBRA LES TARGETES»** (`actions.generateCards`) when only `STRIPE`; **«2 · GENERA ELS REBUTS»** (`actions.generateOnly`) when only `MANUAL` (S12 §2 + §13-13; the providers come from `branding`/`GET /club` as E2 exposes them — reuse that hook, do not add a call). Disabled with a tooltip when there is no simulation for the month or `run` already exists.
   - **[Retrocedeix la remesa]** (`actions.rollback`) rendered **only** when `run.rollbackable === true` (R-12-14).
3. **Strong confirmations** (`Modal`, never a native `confirm`): generation → title `admin-billing:confirm.generateTitle`, body `confirm.generateBody` = «Es generaran {count} rebuts per un total de {total}. La data del proper rebut dels abonats avançarà al dia 1 de {nextMonth}.» with `{count}` and `{total}` from `simulation.kpis` (`fmtMoney`) and `{nextMonth}` = `fmtMonth(period + 1)`; buttons [Genera] / [Cancel·la]. Confirmed → `POST /billing/runs {period, simulationId}` with `Idempotency-Key`; `201` → toast `toasts.generated` «{count} rebuts generats» and refetch. Errors by code: `409 RUN_EXISTS`, `409 SIMULATION_STALE` → modal `errors.simulationStale` «Hi ha hagut canvis des de l'última simulació. Torna a simular el mes.» with a [Simula de nou] button, `409 BILLING_BUSY`, `422 COLLECTION_DATE_TOO_SOON` (render `details.requested` and `details.earliest` as dates), `422 NO_INVOICES`, `422 SEPA_NOT_CONFIGURED` → message pointing at D11. Rollback → modal `confirm.rollbackTitle` + body listing what will be undone + a required `Input` whose value must equal **«RETROCEDIR»** (`confirm.rollbackKeyword`; the button stays disabled otherwise) + a `reason` textarea (≤ 500) → `POST /billing/runs/{id}/rollback {reason, confirmation: "RETROCEDIR"}`; `200` → toast «{cancelledInvoices} rebuts anul·lats»; `409 RUN_NOT_ROLLBACKABLE` → modal listing `details.reasons[]` through `enums:rollbackBlocker.*` (`REMITTANCE_SUBMITTED` «la remesa ja s'ha enviat al banc» · `COLLECTION_SUBMITTED` «hi ha cobraments enviats» · `INVOICE_PAID` «hi ha rebuts cobrats» · `MANUAL_INVOICE_AFTER` «s'ha emès un rebut manual després de la generació»).
4. **Simulation cards** (only when `period.simulation` exists; otherwise `EmptyState` `empty.noSimulation` «Encara no hi ha cap simulació d'aquest mes» + [1 · SIMULA EL MES]): card «Pas 1 — Simulació: incidències primer» (`simulation.incidentsTitle`) with the red count badge and one row per incident «{memberName} · {label} · Obre fitxa›» where `{label}` = `enums:billingIncident.{code}` (`NO_BANK_ACCOUNT` «sense compte bancari informat» · `NO_PRICE` «sense tarifa assignada» · `NO_PLAN` «sense modalitat assignada» · `CARD_INVALID` «targeta no vàlida» · `CURRENCY_MISMATCH` «moneda diferent de la del club» · `PROVIDER_DISABLED` «proveïdor de pagament desactivat») and the link → `/abonats/{memberId}`; zero incidents → the card shows `simulation.noIncidents` «Cap incidència». Card «Actius amb pagament en efectiu» (`simulation.cashTitle`) with rows «{memberName} · data de baixa prevista: {date} · Obre fitxa›» (`simulation.plannedLeave`, the date part omitted when `plannedLeaveDate` is null). Below, the KPI row (`Card` each): «Rebuts del mes» / sub «simulats el {date}» (`fmtDate` short, day/month) · «Import de la remesa» / sub «data de cobrament: {dd/mm}» from `run?.collectionDate ?? simulation.kpis.collectionDate` · «En efectiu» / sub «pendents de marcar cobrat» · «Quota d'inactivitat» / sub «{first} el 1r mes · {following}/mes» from `kpis.inactivityFees` (hidden when `INACTIVITY` is off) · and, only with `STRIPE`, «Amb targeta · {n} · {import}» (`kpis.card`). Incidents **never** block generation — say so in the card footnote `simulation.incidentsFootnote` «Els abonats amb incidència s'ometen de la generació.» and render `run.skipped[]` after a generation as the same row shape.
5. **[COBRA LES TARGETES]** (`actions.chargeCards`, only with `STRIPE` and `run.status ∈ {GENERATED, CHARGING}`) → confirmation modal with the count and total of `run.byProvider.STRIPE` → `POST /billing/runs/{id}/card-charges` → `202` → toast `toasts.chargesSubmitted` «{submitted} cobraments enviats» + `skipped[]` list; poll `GET /billing/runs/{id}` every 5 s while `status = CHARGING` (stop after 2 min with a [Actualitza] button — never an endless poll). `409 INVALID_STATE`, `422 PAYMENT_PROVIDER_NOT_ENABLED` → toast.
6. **Invoice list** (`UniversalList` over `GET /invoices`, `listKey = invoices`): status chips exactly as the mockup, each with its count from `period.counts` — «Tots ({all})», «Pendents ({pending})», «Remesats ({remitted})», «Cobrats», «Impagats ({failed})» — mapped to `filter=status:eq:…` (`Remesats` = `status:eq:COLLECTING`, `Cobrats` = `PAID`, `Impagats` = `FAILED`) on top of the always-present `period:eq:{period}`; the universal column filter and the active-filter indicator come free from the component (never build a specific filter bar). Declared filters `period, status, memberId, paymentMethodType, runId, remittanceId, issueDate, total, kind`, sorts `number, issueDate, total, memberLastName`, free text `q`. Columns: «Núm.» (`displayNumber`) · «Abonat» → link `/abonats/{memberId}` · «Concepte» (the **frozen** `lines[0].description` plus «+{n}» when there is more than one line — never rebuilt on the front, R-12-30) · «Import» (`fmtMoney`) · «Mètode» (`enums:paymentMethodType.SEPA_DD` «Domiciliació» · `CARD` «Targeta» · `MANUAL` «Efectiu») · «Estat» rendered as a `Badge` from `enums:invoiceStatus.*` with the D6 wordings: `PENDING` + `MANUAL` → «pendent · marca cobrat», `PENDING` → «pendent», `COLLECTING` → «remesat», `PAID` → «cobrat» (and «reemborsat» when `refundedTotal` equals `total`), `FAILED` → «impagat (manual)» when the last collection's provider is `SEPA_XML`/`MANUAL` and «impagat (targeta)» when it is `STRIPE`, `CANCELLED` → «anul·lat». Row selection (checkbox) enables **[Marcar cobrat (selecció)]** → modal with `paidAt` (default today, club-local) and `channel` (`enums:paymentChannel.CASH` «Efectiu» · `TRANSFER` «Transferència» · `BIZUM` «Bizum») → `POST /invoices/payments {invoiceIds[], paidAt, channel}`; **[Exporta per a comptabilitat]** → step 8. Also a discreet [＋ Rebut manual] entry (`actions.manualInvoice`) opening `ManualInvoiceModal`: `memberId` picker (reuse the member search of `CensusListPage`), repeatable line rows `{description ≤ 120, base, taxPercent}`, `includeInNextRun` checkbox (only for `SEPA_DD` members), `note` → `POST /invoices` → `201` → toast + refetch; `422 CURRENCY_MISMATCH` → message.
7. **Invoice drawer** (row click → `Drawer`, `GET /invoices/{id}`): header `displayNumber` + status badge + member link; the frozen line table (description, base, tax, total) with the tax column hidden when every `taxPercent` is 0 (B3); the `collections[]` timeline (provider, attempt, status, `providerRef`, failure code) and `refunds[]`; the payment method block (**masked** — the api never returns an IBAN; render `maskedAccount`, `holderName`, `mandateRef`, `last4`). Actions by state, each with its own confirmation and each sending `version` where S12 §6 asks for it: [Marca cobrat] (`PENDING`/`FAILED`) → `POST /invoices/{id}/payment {paidAt, channel, reference?, version}` · [Marca impagat] (`COLLECTING`/`PAID`) → `POST /invoices/{id}/failure {reason, at, version}` · [Reintenta amb targeta] (`FAILED` with `paymentMethod.type = CARD`) → `POST /invoices/{id}/retry {version}` → `202` · [Reemborsa] (`PAID` with a `SUCCEEDED` Stripe collection) → `POST /invoices/{id}/refund {amount?, reason}` → `202`, amount optional = full · [Anul·la el rebut] (`PENDING`/`FAILED` only) → `POST /invoices/{id}/cancellation {reason, version}` · [Descarrega el justificant] → `GET /invoices/{id}/document` (`application/pdf`, open in a new tab, do not parse). No edit affordance anywhere (R-12-10). Error codes → messages: `409 INVALID_STATE`, `409 STALE_VERSION` (→ «El rebut ha canviat. Torna a obrir-lo.» + refetch), `409 MAX_ATTEMPTS` (render `details.attempts`/`details.max`), `409 REFUND_EXCEEDS_PAID`, `422 NO_PAYMENT_METHOD`, `422 PAYMENT_PROVIDER_NOT_ENABLED`.
8. **Accounting export** (`listKey = accounting`, R-12-26): [Exporta per a comptabilitat] opens the shared export modal E2 already built for `ExportJob` (format `csv`/`xlsx`, default `billing.accountingExportFormat`) → `GET /billing/exports?period={period}&format={format}`; `200` downloads with the response filename, `202 {exportJobId}` falls into the existing async export path — **reuse it, do not fork it**. Modal footnote `export.footnote` «Una fila per línia de rebut; format CSV amb separador «;».». Tests assert the request URL, the status and the filename — never the file contents.
9. **Remittances page** `/facturacio/remeses` (**no mockup** — design system, Josep validates in staging, S12 §13-12): the same universal-list pattern over `GET /remittances`, filters `period, status`, columns «Mes» (`fmtMonth`) · «Data de creació» (`fmtDateTime`) · «Rebuts» · «Import» (`fmtMoney`) · «Data de cobrament» (`requestedCollectionDate`) · «Estat» (`enums:remittanceStatus.GENERATED` «generada» · `SUBMITTED` «enviada al banc» · `ROLLED_BACK` «retrocedida»); actions **[Descarrega l'XML]** → `GET /remittances/{id}/file` (follow the signed URL / `Content-Disposition`, never render the XML) and **[Marca com a enviada al banc]** (only on `GENERATED`) → confirmation warning that the rollback becomes impossible (`confirm.submissionBody`) → `POST /remittances/{id}/submission {submittedAt}` (`409 INVALID_STATE` handled). Row → `GET /remittances/{id}` in a drawer with `messageId`, `sequenceBreakdown {FRST, RCUR}`, `xsdValidatedAt` and the creditor block **with the IBAN masked as the api returns it**. Back link to `/facturacio`.
10. **i18n**: new namespace **`admin-billing`** (`header.*`, `actions.*`, `confirm.*`, `simulation.*`, `kpi.*`, `list.*`, `drawer.*`, `remittances.*`, `export.*`, `errors.*`, `toasts.*`) and new keys in **`enums`** (`invoiceStatus.*`, `paymentMethodType.*`, `billingIncident.*`, `paymentChannel.*`, `remittanceStatus.*`, `rollbackBlocker.*`, `upfrontConcept.*`, `invoiceLineOrigin.*`) registered in `packages/i18n/src/types.ts` and the three loaders of `resources.ts`; `ca` = the mockup literals quoted above, `es` and `en` written in the same task. Amounts always `fmtMoney(locale, currency)`, dates always through `useClubFormats` with the club `timeZone`; months with `fmtMonth`. Vocabulary lint: no «parell*», no «amigable», no «(paràmetre)», no requirement codes in screen text.
11. **MSW**: fixtures `packages/api-client/src/mocks/fixtures/billing.ts` reproducing the mockup — period `2026-09`, simulation of **168** receipts / **6.480 €**, `collectionDate` `2026-09-01` (A28), 2 incidents (Joan Vila `NO_BANK_ACCOUNT`, Pau Riera `NO_PRICE`), 2 cash members with planned leave `2026-12-31` / `2027-06-30`, `inactivityFees {count: 2, firstMonth: 2000, following: 1000}`, counts `{all: 168, pending: 4, remitted: 162, paid: 0, failed: 2}`, and the mockup's five rows (`2026-0912` Laura Serra «Quota Abonat 2 gossos — Setembre 2026» 90,00 € Domiciliació remesat · `2026-0913` Marc Prats 60,00 € remesat · `2026-0914` Eva Perez «Quota inactivitat — Setembre 2026» 10,00 € remesat · `2026-0915` Joan Vila 60,00 € Efectiu pendent · `2026-0871` Pere Soler «Quota Abonat — Agost 2026» 60,00 € impagat) — **fictional people only**, `@example.test`, fake `ES00` IBAN fragments. Stateful handlers for simulate / generate / card-charges / rollback / payment / failure / cancellation / manual invoice / submission. Scenario variants in `scenarios.ts`: `billingManualOnly` (`MANUAL` → «2 · GENERA ELS REBUTS», no remittance), `billingStripe` (card KPI, [COBRA LES TARGETES], a `FAILED` card receipt «impagat (targeta)»), `billingRollbackBlocked` (`409 RUN_NOT_ROLLBACKABLE {reasons: ["REMITTANCE_SUBMITTED"]}`), `billingStale` (`409 SIMULATION_STALE`). Update the handler-count assertion in `src/index.test.ts`.
12. **Tests and evidence**: Vitest `apps/clubs-admin/src/billing/BillingPage.test.tsx` covering **T-12-25** — simulate → incidents rendered first with their labels and «Obre fitxa» links; KPI texts; strong confirmation with the exact «Es generaran 168 rebuts…» sentence; [Retrocedeix la remesa] absent while `rollbackable` is false and present when true; the rollback button stays disabled until «RETROCEDIR» is typed; chips filter the list and show the counts; multi-selection enables [Marcar cobrat (selecció)]; the drawer shows the right actions per state (`PENDING`, `COLLECTING`, `PAID`, `FAILED`, `CANCELLED`); button-2 label changes with the providers (the D6 half of **T-12-32**) — plus `RemittancesPage.test.tsx` (download link, submission confirmation, `ROLLED_BACK` row read-only) and a literal test asserting the `ca` strings «Pas 1 — Simulació: incidències primer», «Actius amb pagament en efectiu», «Marcar cobrat (selecció)», «Exporta per a comptabilitat». Playwright `apps/clubs-admin/e2e/billing.spec.ts` (scenarios `admin`, `billingManualOnly`, `billingStripe`) with screenshots under `roadmap/evidence/E8-W01/`: `D6-facturacio-1280.png`, `D6-facturacio-confirmacio-1280.png`, `D6-facturacio-retroces-1280.png`, `D6-facturacio-calaix-rebut-1280.png`, `D6-facturacio-manual-only-1280.png`, `remeses-1280.png`; compare the first against `docs/pantalles/escriptori/D6-facturacio-simulacio-rebuts-i-remesa-sepa.png` and list any deviation.

## Fixed conventions
- Design system only (`@agilityhub/ui`); no literal colour outside `tokens.css`; the universal list and its filters come from `packages/ui/src/universal-list.tsx` — never a hand-rolled filter bar (Josep 28-08).
- Closed catalogs: every error code, parameter key, notification code and audit action must already exist. Statuses to code against, exactly as `CATALEG_ERRORS.md` fixes them (they win over anything S12 §6 writes): **409** `BILLING_BUSY`, `RUN_EXISTS`, `SIMULATION_STALE`, `RUN_NOT_ROLLBACKABLE`, `MAX_ATTEMPTS`, `INVALID_STATE`, `STALE_VERSION`, `IDEMPOTENCY_KEY_REUSED`; **422** `COLLECTION_DATE_TOO_SOON`, `CURRENCY_MISMATCH`, `PAYMENT_PROVIDER_NOT_ENABLED`, `NO_INVOICES`, `SEPA_NOT_CONFIGURED`, `NO_PAYMENT_METHOD`, `AMOUNT_EXCEEDS_DUE`, `REFUND_EXCEEDS_PAID`; **404** `MODULE_DISABLED`, `NOT_FOUND`; **403** `FORBIDDEN`, `IMPERSONATION_DENIED`; **400** `VALIDATION_ERROR`, `INVALID_FILTER`. Anything missing → proposal in the report and in `roadmap/MESSAGES.md`, with the closest existing item shipped meanwhile.
- Assumptions in force (do not ask): the receipts page, the remittances page and 12/rebuts have **no mockup** — design system + Josep validates in staging (S12 §13-12, B24/B25); the Cànic is `SEPA_XML` + `MANUAL`, has **no** in-app payment screen at R1 (A14) and **no VAT** (B3), and the `STRIPE` branch is built and mocked for other clubs; cash is `SEMESTER` at the Cànic with `MONTHLY` equally product and both editable from D11 (A29); the SEPA collection day is day **1 of the billed month** with `0` = last day, both product (A28); an unpaid receipt triggers **no** automatic claim or booking block (BR-08) — the admin decides from D10.
- Amounts are never computed on the front: render `Money` as the api sends it. Descriptions are the frozen `description`; the front never localizes a receipt line.
- Evidence rule: report = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E8-W01/NN-<name>.log`; task file ≤ 120 KB. Fictional data only (`@example.test`, fake IBAN fragments); no secrets in the repo, env vars only. Never edit other tasks, `ROADMAP.md` or the Organizer verification section. English identifiers; i18n keys never literals; ca/es/en always together.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `pnpm api:generate` (after step 0) and the Ajv suite `pnpm --filter @agilityhub/api-client test -- src/mocks/fixtures.contract.test.ts`.
- `pnpm turbo run lint typecheck test build`; `pnpm i18n:check` (parity ca/es/en, no unused key, every error code translated).
- Vitest T-12-25 + the D6 half of T-12-32 + the literal test; the complete `pnpm e2e` (three Playwright projects) in `mcr.microsoft.com/playwright:v1.63.0-noble` via `scripts/e2e-docker.sh`; the six 1280 px screenshots.

## Definition of Done
D6 runs against MSW in ca/es/en for ADMIN with the three provider variants: simulate → incidents and KPIs → strong confirmation → generate → (cards) → rollback behind the typed «RETROCEDIR», chips and universal filters over the receipt list, bulk mark-paid, the receipt drawer with every state action and the PDF, manual adjustment receipts, the accounting export through the shared export path, and the remittances page with XML download and «marca com a enviada al banc». INSTRUCTOR has no route and no sidebar entry. Nothing is recomputed on the front.

## Evidence to return
Command outputs; the snapshot adoption proof (or the `pending.json` diff); the six screenshots with the deviations you saw against `D6-*.png`; the list of `admin-billing` and `enums` keys added; the error codes you mapped with the status you used.

---

## Executor report
_(not started)_

### Branch / commits

### Evidence (full command outputs)

### Files changed

### Rules and tests implemented (R-xx-nn / T-xx-nn)

### Assumptions

### Questions / catalog proposals

## Organizer verification
_(pending)_
