---
id: E4-W04
title: D7 activities (/activitats: universal list, maintenance with rich text + signed uploads, publication with the ring-conflict dialog, cancellation modal, registrants with FIFO waitlist) + activities in the app (block «Activitats» of 04, detail /activitats/:id, rows of 03 and 25)
stage: E4
repo: agilityhub-core-web
thread: B
order: 40
status: awaiting_verification
depends_on: [E2-W01, E2-W03, E3-W01, external:api:E4-T01]
branch:
pr:
updated: 2026-09-24
---

# E4-W04 · D7 activities (/activitats: universal list, maintenance with rich text + signed uploads, publication with the ring-conflict dialog, cancellation modal, registrants with FIFO waitlist) + activities in the app (block «Activitats» of 04, detail /activitats/:id, rows of 03 and 25)

## Objective
Work packages S07 P4 + P5: the administrator maintains activities in D7 (type enum + free label, per-locale texts, restricted rich text, signed uploads, dates/places/linked rings), publishes them (a draft never blocks rings; publishing does, with the conflict dialog that may cancel classes — A21 b), cancels them with the D4c-pattern modal and reads the registrants with FIFO waitlist positions; the student sees open activities in the «Activitats» block of 04, opens the detail, registers per person (no dog), joins the waitlist, cancels until the event start (B20) and sees the rows on 03 and 25. Mocks-first on the S07 contract of api E4-T01; pixel-faithful to D7 (V7) and 04/03/25 (V8).

## Spec references
S07 §2 (rows D7 llistat, D7 manteniment, Inscrits, 04 bloc «Activitats», Detall d'activitat, 03, 25) · R-07-01…R-07-09, R-07-11, R-07-13, R-07-14 · §5 · §6 (club/member rows + forms A, B; `ErrorCode` list) · §10 · §13 assumptions 1, 3, 4, 5, 6, 7, 8, 14 · tests **T-07-29, T-07-30, T-07-31** · `docs/DECISIONS_PENDENTS.md` A21, B20 · `CATALEG_ERRORS.md` (16-09 statuses: `ACTIVITY_FULL`, `DUPLICATE_SLUG`, `SLUG_LOCKED`, `ALREADY_REGISTERED`, `STALE_VERSION`, `INVALID_STATE`, `RING_BLOCK_CONFLICT` 409; `INVALID_TIME_RANGE`, `INVALID_SLOT_GRANULARITY`, `VALIDATION_ERROR`, `FILE_TOO_LARGE`, `FILE_TYPE_NOT_ALLOWED` 400; `MODULE_DISABLED` 404; every other S07 code — `REGISTRATION_CLOSED`, `ACTIVITY_INCOMPLETE`, `ADMIN_TEXT_REQUIRED`, `RING_HAS_BOOKINGS`, `TOO_MANY_DOCUMENTS`… — 422; the front maps by code, never by status) · `CATALEG_PARAMETRES.md` (`activities.cancelDeadline`, `levels.enabled`, `classes.slotMinutes`, `files.maxSizeMb`, `files.allowedTypes`, `club.holidays`) · `MATRIU_PERMISOS.md` · `CONVENCIONS_API.md` (universal lists) · `CONVENCIONS_I18N.md` §3 · mockups `docs/pantalles/escriptori/D7-*.{html,png}`, `docs/pantalles/mobil/04-*`, `03-*`, `25-*`.

## Context to load
S07 complete · api tasks `E4-T01` step 2–3 (the S07 schema names: `Activity`, `ActivityPatchRequest`, `RingConflicts`, `PublicationRequest`, `ActivityCancellationPreview`, `ActivityRegistrationListItem`, `ActivityRegistration`, `MeActivities`/`ActivityRow`, `MemberActivityDetail`…; purposes `ACTIVITY_IMAGE`/`ACTIVITY_DOCUMENT`; registrations export = the existing `GET /activity-registrations/export`) and `E4-T04` (behaviour) · `apps/clubs-admin/src/census/CensusListPage.tsx` (`UniversalList` usage, saved views, exports drawer), `CensusRecordPage.tsx` (signed upload pattern: `POST /attachments/upload-url {fileName, mimeType, purpose, sizeBytes}` → `PUT uploadUrl` with `headers` → attach with `fileKey`) · `apps/clubs-admin/src/catalogs/ClubPagesCard.tsx` (per-locale tabs editor) · `apps/clubs/src/App.tsx` (`MOBILE_ROUTES` `/reservar`, `/inici`, `/historic` placeholders) · `packages/ui/src/{components.tsx,modules.ts}` · `packages/api-client/src/client.ts` (`DEFAULT_IDEMPOTENT_PATHS`) · `packages/i18n/src/{types,resources,format}.ts` · this file.

## Steps
0. **Contract.** Normal path: the organizer staged `packages/api-client/openapi/openapi.next.json` (api E4-T01; its commit message names the api commit) → swap it into `openapi.json` byte for byte, `pnpm api:generate`, prune every S06/S07 path and schema from `pending.json`, run the Ajv suite — skip the swap if a previous E4 task already did it. If nothing is staged: mocks-first — declare every S07 §6 `/activities*`, `/activity-registrations*` and `/me/activities*` route used below in `pending.json` with E4-T01's schema names and Ajv fixtures; say so in the report. Add `/^\/activities\/[^/]+\/(publication|cancellation)$/` and `"/activity-registrations"` to the client's idempotent paths.
1. **Routes, roles, modules**: clubs-admin `/activitats` (ADMIN; INSTRUCTOR read-only), `/activitats/:id` (ADMIN; INSTRUCTOR read-only, no `internalNotes`), `/activitats/:id/inscrits` (ADMIN, INSTRUCTOR); all behind `RequireModule("ACTIVITIES")` (extend `moduleUi.ACTIVITIES.routes`); sidebar entry `activities` roles `["INSTRUCTOR","ADMIN"]`. clubs `/activitats/:id` (MEMBER, IMPERSONATED; module-gated) and the block/rows below. Files: `apps/clubs-admin/src/activities/*` (`ActivitiesPage`, `ActivityPage`, `ActivityRegistrantsPage`, `PublishConflictsDialog`, `CancelActivityModal`), `apps/clubs/src/activities/*` (`ActivitiesBlock`, `ActivityDetailPage`, `ActivityReservationRow`, `ActivityHistoryRow`), `packages/ui/src/{rich-text-editor,safe-html}.tsx`.
2. **D7 list** (`UniversalList` over `GET /activities`, `listKey: "activities"`; `x-filterable: state, type, date, ringId, levelId, deleted, registrationOpen`; `x-sortable: date, title, state, createdAt`; default `date desc` + `deleted:eq:false`; `GET /activities/filter-values?field=`; «Excel · PDF» = `GET /activities/export?format=xlsx|pdf` through the exports drawer, ADMIN only). Title «Activitats», button [＋ Nova activitat]. Columns (mockup literals): «Activitat» = flag icon + **{title}** + « · {typeDisplay}»; «Data» (R-07-13): «{ds 7}» when the month is the current one, else «{ds 12/09}», + « · {18:30}[–{20:30}]» when there are hours (no leading zero) — add `formatActivityDate(date, startTime?, endTime?, locale, timeZone, today)` to `packages/i18n/src/format.ts` (shared with the app); «Pistes» = `allRings` → «totes — bloquejades», one ring → colour dot + name, several → names joined by «, », `!location.atClub` → «— (fora del club)», none → «—»; «Inscripcions» = `PUBLISHED` with `maxPlaces` → «{active}/{max} · fins el {registrationTo}» (date `d/MM`), `PUBLISHED` without max → «obertes · socis», `DRAFT`/`CANCELLED` → «—»; «Estat» chip `enums:activityState.DRAFT` «esborrany» · `PUBLISHED` «publicada» · `FINISHED` «finalitzada» · `CANCELLED` «cancel·lada»; «›» opens `/activitats/:id`. Empty «Encara no hi ha cap activitat» + the button; loading skeleton; error toast by `code` + [Torna-ho a provar]. [＋ Nova activitat] → `Modal` with «Títol» per active locale (`club.locales` tabs, `defaultLocale` required) and «Tipus» (`enums:activityType.SEMINAR` «seminari» · `SOCIAL_LEAGUE` «lliga social» · `COMPETITION` «competició» · `DEMONSTRATION` «demostració» · `COURSE` «curset» · `OTHER` «altres») → `POST /activities {title, type}` → the maintenance.
3. **D7 maintenance** `/activitats/:id` (`GET /activities/{id}`, form A; two cards as the mockup). Card «Manteniment de l'activitat — {title}»: «Títol» (per-locale tabs), type chip «{typeDisplay} ▾» (enum select) + «Etiqueta pròpia» (`typeLabel`, ≤ 30, per locale; proposed literal), «Descripció curta» (≤ 160, per locale), «Descripció llarga (text ric)» = new `RichTextEditor` (`packages/ui`): `contenteditable` + toolbar limited to the R-07-03 allow-list (bold, italic, underline, strike, H3, H4, lists, quote, link `http(s):`/`mailto:` only) and a DOM-walking serializer emitting **only** allow-listed elements/attributes (test: `<script>`, `onclick`, `<img>`, `style`, `javascript:` dropped; `<b>` → `strong`, `<i>` → `em`); no new runtime dependency; image control «{image.name}» + camera icon → file input (`image/*`, ≤ `files.maxSizeMb`) → `POST /attachments/upload-url {purpose: "ACTIVITY_IMAGE", …}` → `PUT` → `PUT /activities/{id}/image {fileKey, name}` (remove → `DELETE`); documents «{name}» → `purpose: "ACTIVITY_DOCUMENT"` → `POST /activities/{id}/documents {fileKey, name}` (name asked on upload; 422 `TOO_MANY_DOCUMENTS`; `DELETE …/documents/{docId}`); 400 `FILE_TOO_LARGE`/`FILE_TYPE_NOT_ALLOWED` messages. Card «Dates, places i pistes»: «{ds 7 d'agost} · {18:30}–{20:30}» = masked date `dd/mm/aaaa` + two time inputs (steps of `classes.slotMinutes`; holiday → warning «dia festiu», `GET /club/holidays`); «Inscripció: de l'{1/07} al {6/08}» = two masked dates; «Places: {40}» (empty = no maximum); chip «Nivells: tots ▾» (multi-select; hidden when `levels.enabled=false`, `GET /parameters/levels.enabled`); chip «Llista d'espera: sí/no» (only with `WAITLIST`); «Pistes vinculades (es bloquegen):» toggle chips of active rings in catalog order, disabled with `location.atClub=false`; location «Al club» switch + «Lloc» + «Adreça» (proposed; `atClub=false` requires a name); state chip + «URL: {publicUrl} · surt a l'API de la web (mai noms)»; «Notes internes» (ADMIN). Buttons: [DESA] → `PATCH /activities/{id}` (diff + `version`): 409 `STALE_VERSION` → refetch + message; `SLUG_LOCKED`/`DUPLICATE_SLUG` on the slug field (editable only while `DRAFT`); 422 `CAPACITY_BELOW_REGISTRATIONS` on «Places»; `INVALID_TIME_RANGE`/`INVALID_SLOT_GRANULARITY`/`OUTSIDE_OPENING_HOURS` on the fields; a `PUBLISHED` patch touching date/hours/rings may answer `RING_BLOCK_CONFLICT`/`RING_HAS_BOOKINGS` → reuse the step-4 dialog and resend with its options. Per the S07 §2 assumption (no mockup): [PUBLICA] (`DRAFT`), [DESPUBLICA] (`PUBLISHED`, `DELETE /activities/{id}/publication`; 422 `ACTIVITY_HAS_REGISTRATIONS`), [CANCEL·LA L'ACTIVITAT] (`PUBLISHED`), [ELIMINA] (`DRAFT` → simple confirm → `POST …/cancellation {reason: "DELETED"}`), link «Inscrits ({counters.active}) ›». `FINISHED`/`CANCELLED`: read-only + «Notes internes». INSTRUCTOR: read-only, no buttons.
4. **Publication and the conflict dialog** (R-07-05): [PUBLICA] → `GET /activities/{id}/ring-conflicts`; with no conflicts → confirm `Modal` («Vols publicar l'activitat? Els abonats admesos rebran un avís.» proposed) with the switch «Avisa també per correu» (`notifyEmail`) → `POST /activities/{id}/publication {notifyEmail}`. Otherwise `PublishConflictsDialog` (literals proposed): title «Conflictes de pista», one row per conflict «{ring} · {label} · {from}–{to}» + «{bookedCount} inscrits», section «Reserves d'entrenament» for `trainingBookings[]`; checkboxes «Anul·la les classes en conflicte i avisa els inscrits» (`cancelClasses`) and «Cancel·la les reserves d'entrenament» (`cancelBookings`); textarea «Text de l'avís» required when a conflicting class has `bookedCount > 0`; [PUBLICA I APLICA] disabled until the required options are set → `POST …/publication {notifyEmail, cancelClasses, cancelBookings, adminText}` (`Idempotency-Key`); 409 `RING_BLOCK_CONFLICT` / 422 `RING_HAS_BOOKINGS` / `ADMIN_TEXT_REQUIRED` → reopen with `details`; 422 `ACTIVITY_INCOMPLETE {fieldErrors[]}` → mark the fields + toast; 422 `ACTIVITY_IN_PAST` message. Success → toast + chip «publicada».
5. **Cancellation modal** (R-07-06, D4c pattern): [CANCEL·LA L'ACTIVITAT] → `GET /activities/{id}/cancellation-preview`; when `activeCount + waitingCount > 0`: title «Cancel·lar l'activitat — {title}», intro «Aquesta activitat té {count, plural, one {# inscrit} other {# inscrits}}. Si la cancel·les, rebran un avís a l'app, per correu i per SMS amb el text que escriguis a sota.», rows «**{memberName}** · {state chip} · {channels}» (`enums:activityRegistrationState.ACTIVE` «inscrita» · `WAITLISTED` «en llista d'espera»; channels «app · correu · SMS ({phoneCount} telèfons)» as in E4-W02), «Text de l'avís» (1–500, required), [Torna enrere] · [CANCEL·LA I AVISA ELS {count} INSCRITS] (disabled without text) → `POST …/cancellation {reason: "CLUB_MANUAL", adminText}` (`Idempotency-Key`; 422 `ADMIN_TEXT_REQUIRED`, 409 `INVALID_STATE`). Without registrants: simple confirm.
6. **Registrants** `/activitats/:id/inscrits`: header «Inscrits — {title}» + back link; `UniversalList` over `GET /activities/{id}/registrations` (`x-filterable: state, origin, registeredAt, memberId`; `x-sortable: registeredAt, position, memberLastName`; default `registeredAt,asc`): «Abonat» = «{member.fullName} · {member.memberNumber}» → `/abonats/{member.id}`; «Estat» = «inscrita» · «en llista d'espera ({position})» · «anul·lada» · «cancel·lada pel club» (`cancelReason = ACTIVITY_CANCELLED`); «Data d'inscripció» (`fmtDateTime`); «Origen» (`enums:activityOrigin.APP` «app» · `BACKOFFICE` «club», proposed); «Contacte» (phones/emails). «Excel · PDF» = the existing `GET /activity-registrations/export?format=&filter=activityId:eq:{id}` through the exports drawer (E4-T01 note).
7. **App — block «Activitats» of 04** (`ActivitiesBlock`, `GET /me/activities` → `bookable[]`; the dog selector and the class list of 04 are S08/E5: mount the block on the existing `/reservar` placeholder under an `AppBar` «Reservar» with the mockup intro «Seleccioneu l'activitat o classe que vulgueu reservar.», state in the report that E5 owns the page and keeps the block, and call without `dogId` — E5 adds the selected dog, §13-6). Section label «Activitats»; row = flag icon + «**{title}** · {ds 12/09} · {9:00}» (`formatActivityDate`) + badge per `rowState`: `OPEN` → `activities:list.freeSeats` «{count, plural, one {# plaça} other {# places}}» or `activities:list.open` «Obertes» when `freeSeats` is null; `FULL_WAITLIST` → `activities:list.fullWaitlist` «Completa · ⏳{waiting}»; `FULL` → `activities:list.full` «Completa», inert (no ›); `NOT_BOOKABLE` → inert (the S08 banner is E5). Tap → `/activitats/{id}`. Absent without `ACTIVITIES` (`RequireModule`) or with an empty `bookable[]`.
8. **App — detail** `/activitats/:id` (`GET /me/activities/{activityId}`; 404 → «No disponible» state): image, title, `typeDisplay` chip, «{dia} · {hores}» (`fmtDate(weekday)` + hours), `placeLabel` («totes les pistes» = `activities:detail.allRings`), short description, long description through `SafeHtml` (`packages/ui`: re-applies the R-07-03 allow-list before `dangerouslySetInnerHTML`; links `rel="noopener" target="_blank"`), documents (signed `url`), «Inscripció fins el {date}», places line («{free} places lliures de {max}» / «Obertes» — proposed). Button by `myRegistration`/`rowState`: none + `OPEN` → [INSCRIU-M'HI] → `POST /activity-registrations {activityId}` (`Idempotency-Key`); none + `FULL_WAITLIST` → [APUNTA'M A LA LLISTA D'ESPERA] → `Modal` «Vols apuntar-te a la llista d'espera de {title}?» → `{activityId, joinWaitlist: true}` → toast «Ets a la llista d'espera»; `ACTIVE` → [ANUL·LA LA INSCRIPCIÓ], `WAITLISTED` → [SURT DE LA LLISTA D'ESPERA] → confirm → `POST /activity-registrations/{id}/cancellation {}`; `now ≥ cancellableUntil` → the text «Per anul·lar la inscripció, posa't en contacte amb el club» instead of the button. Errors: 409 `ACTIVITY_FULL` with `details.waitlistAvailable = true` → the waitlist dialog, `false` → «Completa» message; `REGISTRATION_NOT_CANCELLABLE`, `ALREADY_REGISTERED`, `ACTIVITY_NOT_PUBLISHED`, `REGISTRATION_CLOSED` and the R-07-07 eligibility codes → `errors:` message by code + refetch.
9. **Rows of 03 and 25** (components; the pages are S08/S10): `ActivityReservationRow` (03; `mine[]` items `ACTIVE`/`WAITLISTED` with `endsAtLocal > now`): flag icon + «**{title}**» + badge «inscrita» / «en llista d'espera», line 2 «{Dissabte 7} · {18:30}–{20:30} · {placeLabel}» — never a dog; › → detail. Mount it provisionally on `/inici` in a «LES MEVES RESERVES» section showing **only** activity rows (E5 replaces the page and keeps the component). `ActivityHistoryRow` (25): «{ds 12/07}» + «{title}» + badge «feta» / «anul·lada» / «cancel·lada pel club» (+ quoted «{admin_text}» line for `ACTIVITY_CANCELLED`); its source `/me/history` is S10 (E6): unit-test the row from a fixture typed with form B + `activity.state`; mount it on `/historic` only if the adopted `mine[]` carries finished/cancelled registrations (otherwise leave `/historic` untouched and say so).
10. **i18n**: new namespaces `admin-activities` (`list.*`, `form.*`, `publishDialog.*`, `cancelModal.*`, `registrants.*`) and `activities` (`block.*`, `list.*`, `detail.*`, `rows.*`) registered in `types.ts` + loaders; `enums` additions `activityType.*`, `activityState.*`, `activityRegistrationState.*` (+ «feta» for 25), `activityOrigin.*`; ca = mockup literals and S07 §2 proposals; es/en in the same task; T-07-31: vocabulary lint over the three namespaces (never «parella»: the registration is «per persona»), three locales complete, and a format test: a viewer in `America/Bogota` with club `Europe/Madrid` sees «18:30» for `2026-08-07T16:30:00Z`.
11. **MSW** (`packages/api-client/src/mocks/fixtures/activities.ts`, stateful): the four D7 activities — «Torneig d'Estiu 2026» (`COMPETITION`, 2026-08-07 18:30–20:30, `allRings`, 40 places, registration 01/07–06/08, `counters {22, 0}`, `waitlistEnabled: true`, image «imatge_torneig.jpg», document «normativa.pdf», `PUBLISHED`), «Seminari de handling» (`SEMINAR`, 2026-09-12 9:00–13:00, Central, 12 places, `counters {6, 0}`, until 06/09), «Lliga social — 3a jornada» (`SOCIAL_LEAGUE`, 2026-09-19 9:00, all rings, no max), «Demostració Festa Major» (`DEMONSTRATION`, 2026-10-04, no hours, `atClub: false`, `DRAFT`); `ring-conflicts` for the draft once dated on Central 18:30 (one class «B+C 18:30–19:30» `bookedCount: 3`, one training booking); `cancellation-preview` with 22 fictional rows; registrations with two `WAITLISTED` positions; `me/activities` (`bookable`: the Seminari «6 places» + a `FULL_WAITLIST` row; `mine`: the Torneig `ACTIVE`); member detail fixtures for `OPEN`, `FULL_WAITLIST`, registered, past-deadline. Stateful handlers for create, patch (`version`), image/documents, publication (409 unless `cancelClasses`), cancellation, registration (`ACTIVITY_FULL {waitlistAvailable}`, positions, cancellation promoting position 1). Scenarios: the existing four + `activitiesNoWaitlist`, `activitiesNoLevels`. Update the handler-count assertion.
12. **Tests and evidence**: Vitest `apps/clubs-admin/src/activities/*.test.tsx` **T-07-29** (the four mockup rows with the exact Data/Pistes/Inscripcions/Estat texts; «Nova activitat» creates and opens the maintenance; ring chips disabled with «fora del club»; «Nivells» hidden with `levels.enabled=false`; «Llista d'espera» hidden without `WAITLIST`; [PUBLICA] with conflicts opens the dialog with conflicts + text; [CANCEL·LA L'ACTIVITAT] opens the modal with «CANCEL·LA I AVISA ELS 22 INSCRITS» disabled without text; serializer allow-list), `apps/clubs/src/activities/*.test.tsx` **T-07-30** (exact block badges; detail buttons per `rowState`/`myRegistration`; waitlist dialog → toast «Ets a la llista d'espera»; 03 row «Torneig d'Estiu 2026 · inscrita · Dissabte 7 · 18:30–20:30 · totes les pistes» without dog; 25 row «ds 12/07 · Seminari d'obstacles · feta»), **T-07-31** (step 10). Playwright `apps/clubs-admin/e2e/activities.spec.ts` (1280: `D7-activitats-1280.png` = list + maintenance of «Torneig d'Estiu 2026» as the mockup, `D7-conflictes-1280.png`, `D7-cancellacio-1280.png`, `D7-inscrits-1280.png`) and `apps/clubs/e2e/activities.spec.ts` (375: `04-activitats-375.png`, `activitat-detall-375.png`, `activitat-llista-espera-375.png`, `03-activitat-inscrita-375.png`, `25-activitat-375.png`) under `roadmap/evidence/E4-W04/`; compare with `docs/pantalles/escriptori/D7-*.png`, `docs/pantalles/mobil/04-*.png`, `03-*.png`, `25-*.png`.

## Fixed conventions
- Design system only; keys never literals; ca/es/en together; vocabulary rules (never «parella/parelles», «amigable», «(paràmetre)», requirement codes). Dates through the club `timeZone` formatters (R-07-13); `fmtMoney` unused (no prices in phase 1).
- Api HTML (`longDescriptionHtml`) is rendered only through `SafeHtml`; the editor serializer and `SafeHtml` share one allow-list module; never render `message`/`details` of an `ApiError` as copy.
- Literals of screens without mockup are the S07 §2/§10 proposals; anything else is a proposal in the report + `MESSAGES.md`. Closed catalogs. Mockup fidelity checked with screenshots against `docs/pantalles/…`; expected deviations: the type suffix «· {tipus}» on every D7 row (§13-1), no dog on activity rows (§13-7).
- Evidence rule: command + exit code + last 40 lines; long outputs → `roadmap/evidence/E4-W04/NN-<name>.log`; task file ≤ 120 KB; complete `pnpm e2e` (three projects) in `mcr.microsoft.com/playwright:v1.63.0-noble` before `awaiting_verification`; fictional data only (`@example.test`, tiny generated image/PDF fixtures); never edit other tasks, `ROADMAP.md` or the Organizer verification section.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `pnpm turbo run lint typecheck test build`; `pnpm i18n:check`; Vitest T-07-29, T-07-30, T-07-31 + the serializer/`SafeHtml` tests; Ajv suite; complete `pnpm e2e` in Docker (`scripts/e2e-docker.sh`); the nine screenshots.

## Definition of Done
A fictional administrator creates, completes, publishes (conflict dialog), edits and cancels an activity in D7 against MSW and reads the registrants with waitlist positions; a fictional member sees the block on 04, registers, joins the waitlist, cancels from the detail and sees the 03/25 rows — in ca/es/en, with the `WAITLIST`/`levels.enabled` variants; INSTRUCTOR read-only; `ACTIVITIES` off hides everything.

## Evidence to return
Command outputs, screenshots, the new key lists, the proposed literals, where the 04/03/25 pieces are mounted, mockup deviations with their decision id.

---

## Executor report

**Session 2026-09-24 (claude:claude-opus-5-5).**
- **Status:** D7 (list, maintenance, publication with the conflict dialog, cancellation, registrants), the app pieces (04 block, detail, 03 and 25 rows), i18n and the stateful MSW world are done.
- **Verification:** every Verification command is green. The complete `pnpm e2e` in Docker passed 34 + 27 + 1.
- **Dependencies:** E2-W01, E2-W03, E3-W01 and api E4-T01 are verified.

### Branch / commits
- `main` working tree (trunk-based). No git command that writes was run; the publish script commits after the session.
- **Not mine:** `docs/*`, `roadmap/MESSAGES.md`, `roadmap/tasks/E3-W06.md`, `E3-W07.md`, `E3-W08.md` and `E4-W02.md` changed in the working tree during this session. The organizer wrote them; I did not touch them.

### Evidence (full command outputs)

**Step 0 (contract).** The S07 contract is already in `openapi.json`: all 21 `/activities*`, `/activity-registrations*`, `/me/activities*` and `/public/*/activities*` paths, and every S07 schema. E4-W01 adopted it; I compared against the snapshot in this session.
- The staged `openapi.next.json` (api `f4d6997`) changes **no** activity path or schema, except the public ones. It adds S10 routes (`/me/history`, attendance, tasks). So there was no swap; that adoption belongs to another task.
- `pending.json` had no S06/S07 entries to prune.
- The idempotent-path regex and `"/activity-registrations"` are added to `DEFAULT_IDEMPOTENT_PATHS`.
- The contract makes `Idempotency-Key` a **required** header on `POST …/publication`, `POST …/cancellation` and `POST /activity-registrations`. The screens therefore pass a fresh key per attempt, as `RingBlockDrawer` does.

1. `pnpm api:generate > roadmap/evidence/E4-W04/00-api-generate.log 2>&1` → **exit 0**. The whole output:
```
> agilityhub-core-web@0.0.0 api:generate /Users/jordib/dev/agilityhub/agilityhub-core-web
> pnpm --filter @agilityhub/api-client api:generate
> @agilityhub/api-client@0.0.0 api:generate …/packages/api-client
> node scripts/generate.mjs
Generated …/packages/api-client/src/generated/schema.d.ts from …/openapi/openapi.json merged with …/openapi/pending.json
```
   `schema.d.ts` changes by +15 lines: only the `ActivityListItem` overlay (see Questions 1).

2. `pnpm turbo run lint typecheck test build > roadmap/evidence/E4-W04/01-turbo.log 2>&1` → **exit 0**. Full log: `01-turbo.log`, 1367 lines.
   - This last run replayed Turbo's cache for identical inputs (`34 cached`). The run just before it, at 19:02, and the one at 18:59 after the last code change, executed with 20 of 34 tasks cached; both were green.
   - Last 40 lines:
```
clubs:test: stderr | src/SignupPage.test.tsx > M2 api errors land on their field and step (§2, CATALEG_ERRORS) > PAYMENT_METHOD_NOT_AVAILABLE (422) → undefined
clubs:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs:test:
clubs-admin:test:  Test Files  18 passed (18)
clubs-admin:test:       Tests  121 passed (121)
clubs:test: stderr | src/SignupPage.test.tsx > M2 api errors land on their field and step (§2, CATALEG_ERRORS) > SOMETHING_NEW (422) → undefined
clubs:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs:test:
clubs:test: stderr | src/SignupPage.test.tsx > M2 api errors land on their field and step (§2, CATALEG_ERRORS) > VALIDATION_ERROR (400) → /apuntat-hi
clubs:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs:test:
clubs:test: stderr | src/SignupPage.test.tsx > M2 api errors land on their field and step (§2, CATALEG_ERRORS) > VALIDATION_ERROR (400) → /apuntat-hi/gos
clubs:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs:test:
clubs:test: stderr | src/SignupPage.test.tsx > M19 the consent version sent is the accepted one (R-04-17) > clears an acceptance of other legal texts and sends the version accepted again
clubs-admin:test:    Start at  18:59:49
clubs-admin:test:    Duration  11.54s (environment 40%, tests 30%, transform 15%, setup 9%, import 6%)
clubs:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs:test:
clubs:test: stderr | src/SignupPage.test.tsx > M19 the consent version sent is the accepted one (R-04-17) > a saved acceptance of an older version is not carried into the new texts
clubs-admin:test:
clubs:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs:test:
clubs:test:  ✓ src/SignupPage.test.tsx (53 tests) 5171ms
clubs:test:    ✓ T-04-29 signup person and draft (7)
clubs:test:      ✓ persists a current draft, expires it after 24 hours and maps identity states 995ms
clubs:test:      ✓ masks and validates the browser-independent birth date control 327ms
clubs:test:      ✓ M2 checks the ES postal code and a past birth date from 1900 before any request 374ms
clubs:test:
clubs:test:  Test Files  7 passed (7)
clubs:test:       Tests  109 passed (109)
clubs:test:    Start at  18:59:49
clubs:test:    Duration  9.94s (environment 40%, tests 32%, transform 14%, setup 9%, import 6%)
clubs:test:

 Tasks:    34 successful, 34 total
Cached:    34 cached, 34 total
  Time:    34ms >>> FULL TURBO
```

3. `pnpm i18n:check > roadmap/evidence/E4-W04/02-i18n-check.log 2>&1` → **exit 0**. The whole output:
```
> node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs
i18n check passed
ca: 2253 keys
es: 2253 keys
en: 2253 keys
1679 statically used keys, 0 unused
250 codes, 0 missing
```

4. **Vitest T-07-29, T-07-30, T-07-31, the serializer and `SafeHtml`, and the Ajv suite.** All five exit 0. Full verbose logs are in `03a…03e-*.log`.
   - The tails below are the last lines **without** the repeated `[BrandingProvider] … contrast (3.32:1)` stderr lines. That warning is the known A32 brand question, printed once per test.
   - `pnpm --filter clubs-admin exec vitest run src/activities --reporter=verbose > …/03a-vitest-T-07-29.log 2>&1` → **exit 0**:
```
 ✓ … T-07-29 … > draws the four mockup rows with the R-07-13 dates, rings, registrations and state chips
 ✓ … T-07-29 … > «Nova activitat» creates a draft with title and type and opens its maintenance
 ✓ … T-07-29 … > shows the mockup maintenance of «Torneig d'Estiu 2026» with the ring, level and waitlist chips
 ✓ … T-07-29 … > disables the ring chips of an activity outside the club («— (fora del club)»)
 ✓ … T-07-29 … > R-07-14 hides «Nivells» with levels.enabled=false and «Llista d'espera» without WAITLIST
 ✓ … T-07-29 … > R-07-03 [DESA] sends only the allow-listed rich text of the editor, with the version
 ✓ … T-07-29 … > R-07-05 [PUBLICA] with conflicts opens the dialog with the conflicts and the required notice text
 ✓ … T-07-29 … > R-07-04 a draft without the registration period answers ACTIVITY_INCOMPLETE on the fields
 ✓ … T-07-29 … > R-07-06 [CANCEL·LA L'ACTIVITAT] opens the modal with «CANCEL·LA I AVISA ELS 22 INSCRITS» disabled without text
 ✓ … T-07-29 … > INSTRUCTOR reads D7 without actions, export or internal notes
 ✓ … T-07-29 … > lists the registrants with the FIFO waitlist positions
 Test Files  1 passed (1)
      Tests  11 passed (11)
```
   - `pnpm --filter clubs exec vitest run src/activities --reporter=verbose > …/03b-vitest-T-07-30.log 2>&1` → **exit 0**:
```
 ✓ … T-07-30 … > draws the 04 block with the exact badges; the full row with waitlist stays tappable
 ✓ … T-07-30 … > R-07-14 without WAITLIST the full activity is «Completa» and inert; an open one without maximum is «Obertes»
 ✓ … T-07-30 … > R-07-14 omits the block without ACTIVITIES
 ✓ … T-07-30 … > OPEN: [INSCRIU-M'HI] registers and then offers [ANUL·LA LA INSCRIPCIÓ]
 ✓ … T-07-30 … > FULL_WAITLIST: the waitlist dialog → toast «Ets a la llista d'espera» → [SURT DE LA LLISTA D'ESPERA]
 ✓ … T-07-30 … > R-07-08 an ACTIVITY_FULL answer with waitlistAvailable opens the waitlist dialog
 ✓ … T-07-30 … > registered: [ANUL·LA LA INSCRIPCIÓ] with confirmation; after the start, the contact text
 ✓ … T-07-30 … > an activity that is not visible shows «No disponible»
 ✓ … T-07-30 … > 03: «Torneig d'Estiu 2026 · inscrita · Divendres 7 · 18:30–20:30 · totes les pistes», never a dog
 ✓ … T-07-30 … > 03: a waitlisted registration shows «en llista d'espera»
 ✓ … T-07-30 … > 25: «ds 12/07 · Seminari d'obstacles · feta» and «cancel·lada pel club» with the quoted notice
 Test Files  1 passed (1)
      Tests  11 passed (11)
```
   - `pnpm --filter @agilityhub/i18n exec vitest run src/activities.test.ts src/format.test.ts --reporter=verbose > …/03c-vitest-T-07-31.log 2>&1` → **exit 0** (36 tests). The T-07-31 and R-07-13 lines:
```
 ✓ src/activities.test.ts > T-07-31 … > has the same activities keys in ca, es and en, none empty
 ✓ src/activities.test.ts > T-07-31 … > has the same admin-activities keys in ca, es and en, none empty
 ✓ src/activities.test.ts > T-07-31 … > has the same enums keys in ca, es and en, none empty
 ✓ src/activities.test.ts > T-07-31 … > never uses the forbidden vocabulary (the registration is «per persona»)
 ✓ src/activities.test.ts > T-07-31 … > keeps the mockup and S07 §10 literals in ca
 ✓ src/format.test.ts > … R-07-13 activity dates > lists 2026-08-07 18:30–20:30 as «dv 7 · 18:30–20:30»
 ✓ src/format.test.ts > … R-07-13 activity dates > lists 2026-09-12 09:00–13:00 as «ds 12/09 · 9:00–13:00»
 ✓ src/format.test.ts > … R-07-13 activity dates > lists 2026-09-19 09:00–null as «ds 19/09 · 9:00»
 ✓ src/format.test.ts > … R-07-13 activity dates > lists 2026-10-04 null–null as «dg 4/10»
 ✓ src/format.test.ts > … R-07-13 activity dates > formats the maintenance, 03 and 25 presentations
 ✓ src/format.test.ts > … R-07-13 activity dates > formats the list date in es / in en
 ✓ src/format.test.ts > … R-07-13 activity dates > T-07-31 a viewer in America/Bogota reads 18:30 for 2026-08-07T16:30:00Z (club Europe/Madrid)
 ✓ src/format.test.ts > … R-07-13 activity dates > decides «the current month» in the club zone
 Test Files  2 passed (2)
      Tests  36 passed (36)
```
   - `pnpm --filter @agilityhub/ui exec vitest run src/rich-text.test.tsx --reporter=verbose > …/03d-vitest-rich-text.log 2>&1` → **exit 0**:
```
 ✓ … R-07-03 rich-text allow-list (T-07-02 on the front) > keeps the mockup-safe markup and drops everything else
 ✓ … > drops <script>, style attributes and elements, classes and ids
 ✓ … > maps browser formatting onto the allow-list: <b> → strong, <i> → em, <strike> → s, <div> → p
 ✓ … > keeps http(s) and mailto links with rel/target and unwraps javascript: links
 ✓ … > keeps lists, quotes, h4 and line breaks and escapes text
 ✓ … > serializes a DOM subtree (the editor content) with the same rules
 ✓ … > returns an empty string for empty input
 ✓ … SafeHtml > re-applies the allow-list before rendering and adds rel/target to links
 ✓ … SafeHtml > renders nothing without text
 ✓ … RichTextEditor > offers only the allow-listed tools and emits allow-listed HTML
 ✓ … RichTextEditor > refuses a javascript: link
 ✓ … RichTextEditor > is read-only without a toolbar
 Test Files  1 passed (1)
      Tests  12 passed (12)
```
   - `pnpm --filter @agilityhub/api-client exec vitest run src/mocks/activity-handlers.test.ts src/mocks/fixtures.contract.test.ts src/index.test.ts --reporter=verbose > …/03e-vitest-ajv.log 2>&1` → **exit 0**, 58 tests.
     - The 8 E4-W04 tests validate with Ajv `ListPageActivityListItem` (including the overlay), `Activity`, `ActivityCancellationPreview`, `ListPageActivityRegistrationListItem`, `MeActivities`, `MemberActivityDetail`, `RingConflicts` and `ActivityRegistration`.
     - The rest of the log: every JSON fixture (T-01-25) and the E4-W01/02/03 fixture suites pass. The handler count is now 181.

5. **Complete e2e in Docker:** `pnpm e2e:docker > roadmap/evidence/E4-W04/08-e2e-docker-final.log 2>&1` → **exit 0**. The image is `mcr.microsoft.com/playwright:v1.63.0-noble`, three projects. Last 40 lines:
```
clubs-admin:e2e: Running 34 tests using 9 workers
clubs-admin:e2e: ··································
clubs-admin:e2e:   34 passed (23.3s)
clubs:e2e: cache bypass, force executing f8dc0a91157626e4
clubs:e2e: > clubs@0.0.0 e2e /work/apps/clubs
clubs:e2e: > playwright test
clubs:e2e: Running 27 tests using 7 workers
clubs:e2e: ···························
clubs:e2e:   27 passed (12.4s)
id:e2e: cache bypass, force executing 72c90a5640ebd9db
id:e2e: > id@0.0.0 e2e /work/apps/id
id:e2e: > playwright test
id:e2e: Running 1 test using 1 worker
id:e2e: ·
id:e2e:   1 passed (3.1s)

 Tasks:    8 successful, 8 total
Cached:    0 cached, 8 total
  Time:    50.772s

evidence: E4-W04/03-activitat-inscrita-375.png
evidence: E4-W04/04-activitats-375.png
evidence: E4-W04/activitat-llista-espera-375.png
evidence: E4-W04/D7-activitats-1280.png
evidence: E4-W04/D7-conflictes-1280.png
evidence: E4-W04/D7-inscrits-1280.png
evidence: E4-W04/25-activitat-375.png
evidence: E4-W04/activitat-detall-375.png
evidence: E4-W04/D7-cancellacio-1280.png
```
   The lines above those are WebServer `DEP0169` deprecation warnings from the three dev servers; they are in the log.

   **Earlier attempts (one line each):**
   - `04-e2e-docker-run1.log`: 2 of my new admin tests failed. `getByRole("link", {name: "Activitats"})` also matched «Inactivitats i baixes» (strict mode), and the instructor `goto` was aborted by the login redirect. Fix: `exact: true`, and wait for `/tauler`.
   - `05-e2e-docker-run2.log`: 1 failed. The row text also appeared in the hidden «Obre …» label. Fix: click the row's first link.
   - `06-e2e-docker-run3.log`: 1 failed. `getByLabel("Títol")` also matched the editor's «Títol de secció» and «Subtítol» buttons. Fix: `exact: true`.
   - `07-e2e-docker-run4.log`: green (34 + 1 + 27). After it I fixed two layout defects seen in the screenshots: the document picker overlapped its button, and the time selects wrapped. I also tightened the 04 row text, strengthened one handler test, and re-ran everything as `08`.
   - No product assertion failed in any attempt.

6. **The nine screenshots** are in `roadmap/evidence/E4-W04/`:
   - `D7-activitats-1280.png`: the list, then the «Torneig d'Estiu 2026» maintenance, as the mockup (full page).
   - `D7-conflictes-1280.png`: the dialog with «Central · B+C · 18:30–19:30 · 3 inscrits» and «Muntanya · Pau Soler + Blat · 19:00–19:30», options checked and text written.
   - `D7-cancellacio-1280.png`: the 22 registrants, «app · correu · SMS (2 telèfons)», and [CANCEL·LA I AVISA ELS 22 INSCRITS].
   - `D7-inscrits-1280.png`: «en llista d'espera (1)» and «(2)».
   - `04-activitats-375.png`, `activitat-detall-375.png`, `activitat-llista-espera-375.png`, `03-activitat-inscrita-375.png`, `25-activitat-375.png`.
   - I compared each with `docs/pantalles/escriptori/D7-*.png` and `mobil/04-*`, `03-*`, `25-*`. The deviations are under Assumptions.

### Files changed

**New:**
- `apps/clubs-admin/src/activities/`: `ActivitiesPage.tsx`, `ActivityPage.tsx`, `ActivityRegistrantsPage.tsx`, `PublishConflictsDialog.tsx`, `CancelActivityModal.tsx`, `shared.tsx`, `activities.css`, `ActivitiesPage.test.tsx`.
- `apps/clubs/src/activities/`: `ActivitiesBlock.tsx` (block + `ReserveActivitiesPage`), `ActivityDetailPage.tsx`, `ActivityReservationRow.tsx` (+ `HomeActivityReservations`), `ActivityHistoryRow.tsx`, `HistoryRowsPreview.tsx` (development-only), `shared.ts`, `activities.css`, `activities.test.tsx`.
- `packages/ui/src/`: `rich-text.ts` (the shared allow-list), `rich-text-editor.tsx`, `safe-html.tsx`, `rich-text.css`, `rich-text.test.tsx`.
- `packages/api-client/src/mocks/`: `activity-handlers.ts`, `activity-handlers.test.ts`, `fixtures/activities.ts`.
- `packages/i18n/src/`: `activities.test.ts`, and `locales/{ca,es,en}/activities.json` + `admin-activities.json`.
- e2e: `apps/clubs-admin/e2e/activities.spec.ts`, `apps/clubs/e2e/activities.spec.ts`.
- Evidence: `roadmap/evidence/E4-W04/*` (9 PNG, 11 logs).

**Modified:**
- The two shells: `apps/clubs-admin/src/App.tsx` (routes, sidebar roles, `ActivitiesRoute`) and `main.tsx` (namespace); `apps/clubs/src/App.tsx` (`/activitats/:id`, `/reservar`, `/inici`, the development-only preview).
- `packages/ui/src/`: `modules.ts` (the ACTIVITIES routes), `universal-list.tsx` (`exportable`, `emptyAction`), `index.ts`.
- `packages/i18n/src/`: `format.ts` (`formatActivityDate`, `ClubFormats.formatActivityDate`) + `format.test.ts`; `types.ts` and `resources.ts` (the two namespaces); `locales/*/common.json` (`format.activityDate`); `locales/*/enums.json`.
- `packages/api-client/`: `openapi/pending.json` (the `ActivityListItem` overlay); `src/generated/schema.d.ts` (regenerated); `src/client.ts` (idempotent paths); `src/mocks/handlers.ts` (activity handlers, 2 export jobs, `files.maxSizeMb`, export file name by list key); `src/mocks/scenarios.ts` (`activitiesNoWaitlist`, `activitiesNoLevels`); `src/index.test.ts` (181 handlers).
- `scripts/i18n-check.mjs` (dynamic prefixes for the new enums, `cancelModal.channel.` and `registrants.filters.`), `CHANGELOG.md`.

### Rules and tests implemented (R-xx-nn / T-xx-nn)

**Rules:**
- **R-07-01:** type enum + own label, «{title} · {type}» on every row, create with title + type.
- **R-07-02:** slug field while `DRAFT` and never published; `SLUG_LOCKED` / `DUPLICATE_SLUG` shown on it. The mock generates `-2`, `-3`….
- **R-07-03:** a restricted editor whose toolbar offers only the allow-list, plus a DOM serializer; `SafeHtml` shares the same module (`rich-text.ts`).
- **R-07-04:** [DESA] with a diff + `version`, `STALE_VERSION` → refetch + message; [PUBLICA], [DESPUBLICA] (`ACTIVITY_HAS_REGISTRATIONS`), [ELIMINA] → `reason: DELETED`. `ACTIVITY_INCOMPLETE` marks the fields; `ACTIVITY_IN_PAST` shows its message. `FINISHED` / `CANCELLED` are read-only except the notes.
- **R-07-05:** `ring-conflicts` → the dialog; `cancelClasses` / `cancelBookings` / `adminText`, with the button disabled until they are set; 409 and 422 reopen it with the `details`. A resynchronising `PATCH` reuses the dialog (save mode).
- **R-07-06:** the modal from `cancellation-preview`, with the text required (1–500) and a simple confirm without registrants.
- **R-07-07:** the eligibility codes are shown by `errors:` code, then a refetch.
- **R-07-08:** `ACTIVITY_FULL {waitlistAvailable}` → the waitlist dialog or the «Completa» message; positions; FIFO promotion in the mock.
- **R-07-09:** the button until `cancellableUntil`, then the contact text; the mock answers `REGISTRATION_NOT_CANCELLABLE`.
- **R-07-11:** 04 badges, 03 rows (never a dog), 25 row states, `placeLabel`.
- **R-07-13:** `formatActivityDate` in the club zone (list, long, day and history presentations).
- **R-07-14:** `ACTIVITIES` off → no block, no rows, and the routes show the module-unavailable page; `WAITLIST` off → no chip, «Completa» inert; `levels.enabled=false` → no «Nivells».
- **Roles:** INSTRUCTOR is read-only, with no export and no internal notes.

**Tests:**
- **T-07-29:** `apps/clubs-admin/src/activities/ActivitiesPage.test.tsx`, 11 tests.
- **T-07-30:** `apps/clubs/src/activities/activities.test.tsx`, 11 tests.
- **T-07-31:** `packages/i18n/src/activities.test.ts` + the Bogota test in `format.test.ts`.
- **Front T-07-02:** `packages/ui/src/rich-text.test.tsx`.
- **Handlers:** `packages/api-client/src/mocks/activity-handlers.test.ts`.
- **Playwright:** `activities.spec.ts` in both apps.

### Assumptions
**Mockup deviations:**
1. **7 August 2026 is a Friday.** A correct formatter prints «dv 7 · 18:30–20:30», «dv 7 d’agost» and «Divendres 7» where the mockups and S07 say «ds 7» and «Dissabte 7». The other dates check out: 12/09 and 19/09 are Saturdays, 4/10 is a Sunday. The tests assert the calendar-correct weekday.
   - For the same reason, the 25 fixture uses **2025-07-12** to reproduce «ds 12/07 · Seminari d'obstacles · feta», because 12/07/2026 is a Sunday.
2. **D7 list order:** date desc, per S07 §2 and the api (default sort). The mockup draws the rows ascending. The type suffix is on every row (§13-1).
3. **The list and the maintenance share one page.** `/activitats/:id` renders the list and then the two maintenance cards below it, as the D7 mockup does (its URL is `…/activitats/torneig-estiu-2026`). The universal-list toolbar (search, filter, views, «Excel · PDF») is the D5 pattern.
4. **04 at 375 px:** the rows «Seminari de handling · ds 12/09 · 9:00» + «6 places» wrap onto two lines. The mockup fits them on one line with a very small font; the text wraps instead of being truncated.
5. **Types and styling:**
   - The mockup's «Competició ▾» chip is a styled `<select>`.
   - «Llista d'espera: sí/no» is a toggle chip.
   - «Nivells: tots ▾» is a `<details>` multi-select.
   - The rich-text toolbar glyphs (B/I/U/S/H3/H4/•/1./❝) come from i18n (es: N/K/S/T). The toolbar itself is not in the mockup.

**Fixtures (fictional):**
6. The four mockup activities, as specified, plus a **fifth, «Taller de contactes»**: 22/08 10:00–12:00, Cadells, 10/10 places + 2 waitlisted, waitlist on. The task asks for a `FULL_WAITLIST` row and two waitlisted registrants, and none of the four can hold them: the Torneig is `{22,0}`, the Seminari `{6,0}`, and the Lliga has no maximum.
   - The D7 list therefore shows 5 rows.
   - The Seminari opens registration on 15/07 so it is bookable on the mockup date (early August). The Lliga opens on 01/09, so the 04 block shows only the Taller and the Seminari, as the mockup shows a single activity. In September the Lliga shows «Obertes» (tested).
   - The Demostració is at «Plaça Major».
   - The member (Biel Roca) is registered to the Torneig.
7. **The 03 row's hours come from `RegisteredActivity` `startsAtLocal`/`endsAtLocal`.** The contract has no «without hours» flag, so `T00:00` means «no hours». An end equal to the start, or `T23:59`, means «no end» (see Questions 4).

**Mounting:**
8. **04 block on `/reservar`:** under an `AppBar` «Reservar» with the intro. E5 owns the page and keeps `ActivitiesBlock`; it is called without `dogId` (E5 adds the selected dog, §13-6).
9. **03 rows on `/inici`:** a «Les meves reserves» section with only activity rows (`mine[]` with `endsAtLocal` after the club-local now). Without rows, the existing placeholder stays. E5 replaces the page and keeps `ActivityReservationRow`.
10. **25:** `/historic` is **untouched**. The adopted `mine[]` carries only live registrations (`ACTIVE`/`WAITLISTED`), so finished and cancelled ones will come from `/me/history` (S10/E6). `ActivityHistoryRow` is unit-tested from a fixture typed with form B + `activity.state` (+ `adminText`).
    - The screenshot comes from a **development-only** route, `/_gallery/historic-activitats` (`import.meta.env.DEV`), like the admin `/_gallery`.

**Other:**
11. **`files.maxSizeMb`** defaults to 25 (catalogue) when the parameter is unavailable. The image input accepts `image/*`; documents take any type, and the api decides by `files.allowedTypes`.
12. **Registrants export:** it uses the published `GET /activity-registrations/export?filter=activityId:eq:{id}` (202 → exports drawer). The «Contacte» column is not sent as an export column, because the endpoint's `x-columns` do not list it.

**Proposed literals** (S07 §2/§13 proposals or mine; ca shown, es/en in the JSON):
- «Vols publicar l'activitat? Els abonats admesos rebran un avís.», «Avisa també per correu», «Conflictes de pista», «Reserves d'entrenament», «Anul·la les classes en conflicte i avisa els inscrits», «Cancel·la les reserves d'entrenament», «PUBLICA I APLICA», «DESA I APLICA».
- «Etiqueta pròpia», «Al club», «Lloc», «Adreça», «Identificador de l'adreça», «Buit = sense màxim», «Cal per publicar», «dia festiu».
- «Mostra: Sense les eliminades / Totes», «Inscrits ({n}) ›», «Inscrits — {title}», «Torna a l'activitat», origin «app» / «club».
- «INSCRIU-M'HI», «APUNTA'M A LA LLISTA D'ESPERA», «ANUL·LA LA INSCRIPCIÓ», «SURT DE LA LLISTA D'ESPERA», «Per anul·lar la inscripció, posa't en contacte amb el club», «{n} places lliures de {max}», «Obertes», «Ets a la llista d'espera», «T'hi has inscrit», «Inscripció anul·lada», «Has sortit de la llista d'espera», «Vols anul·lar la inscripció a {title}?».

**New key lists:**
- `activities:` `block.*` (4), `list.*` (4), `detail.*` (29), `rows.*` (3).
- `admin-activities:` `common.*`, `list.*`, `create.*`, `form.*` (incl. `form.editor.*`), `publishDialog.*`, `cancelModal.*`, `registrants.*`.
- `enums:` `activityType.*`, `activityState.*`, `activityRegistrationState.*` (+ `CANCELLED_BY_CLUB`, `DONE`), `activityOrigin.*`.
- `common:format.activityDate.*`.

### Questions / catalog proposals
1. **api: `ActivityListItem` lacks the D7 columns.** It needs `startTime`, `endTime`, `allRings`, `location` (for «fora del club»), `maxPlaces` and `typeDisplay`. Without them, «ds 7 · 18:30–20:30», «totes — bloquejades», «— (fora del club)» and «22/40» cannot be drawn.
   - Meanwhile they are an `x-schema-overlays` entry in `pending.json`. The generator will fail and ask for a prune once the api publishes them.
   - The screen degrades if they are absent: no hours, ring names, and «obertes · socis».
2. **api: `ActivityRegistrationListItem.cancelReason` is typed `["string","null"]`, but its `enum` has no `null`.** Ajv therefore rejects `null`. The mock omits the field until the registration is cancelled. The api should either add `null` to the enum or omit the field.
3. **Mockup and spec dates (Assumptions 1):** 2026-08-07 is a Friday. Should the demo seed and the S07 examples move to Saturday 2026-08-08? Or should the organizer confirm that «dv 7» is expected?
4. **api (optional): `RegisteredActivity` / `ActivityRow` carry no explicit «has hours» flag.** A `startTime`/`endTime` pair, or `null` hours, would remove the `T00:00` convention (Assumptions 7).
5. **Denied commands** (Bash tool permission rules; none blocked the task, and I used the Edit/Write tools instead):
   - inline heredocs whose Python contained `{…"…"}` (e.g. `python3 - <<'EOF' … json.dumps({k:v …}) … EOF`);
   - `npx prettier --write …` (I used `pnpm exec prettier`);
   - a `for l in ca es en; do python3 -c "…" $l; done` loop;
   - `for op in …; do :; done; grep …`;
   - a command combining `git show HEAD:… > …` with a Python heredoc.

   I also restored eight files that a broad `pnpm exec prettier --write` had reformatted without being part of this task, using `git show HEAD:<path> > <path>` (read-only git): `packages/ui/src/components.tsx`, `day-grid.test.tsx`, `api-client` `calendar-handlers.ts`, `fixtures.contract.test.ts`, `branding-canic.json`, `dashboard.json`, `day-grid.ts`, `member-signup-review.json`. I then re-applied my own edits to `handlers.ts` on the restored file.

## Organizer verification
_(pending)_
