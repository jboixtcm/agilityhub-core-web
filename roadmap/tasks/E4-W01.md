---
id: E4-W01
title: D3 + D3b — weekly templates (/plantilles): template tabs, band × day grid, class form with automatic description, inconsistencies, generation card + SETMANES table, coverage table, day view per ring / per instructor
stage: E4
repo: agilityhub-core-web
thread: B
order: 10
status: changes_requested
depends_on: [E2-W01, E2-W04, E2-W08, external:api:E4-T01]
branch:
pr:
updated: 2026-09-24
---

# E4-W01 · D3 + D3b — weekly templates (/plantilles): template tabs, band × day grid, class form with automatic description, inconsistencies, generation card + SETMANES table, coverage table, day view per ring / per instructor

## Objective
Work package S06 P4: the administrator designs the weekly offer in D3 (one Mon–Fri template with the A/B alternation, one Saturday template, both as tabs behind «Plantilles»), creates classes in the band × day grid with the automatic description and a persistent manual override, sees inconsistencies on the cell and in the footer note, generates a week as a draft from the generation card, reads the coverage table with the D3 vocabulary, and opens the day view per ring / per instructor (D3b). The instructor sees everything read-only (decision A22 c). Mocks-first on the S06 contract published by api E4-T01; pixel-faithful to the V7 mockups.

## Spec references
S06 §2 rows D3 and D3b · R-06-01 (bands), R-06-02 (template class), R-06-03 (automatic description, single form «B+C» / «D i sup.» — A22 d), R-06-04 (capacity AUTO/MANUAL), R-06-05 (inconsistencies: cell + footer, `canGenerate=false`), R-06-06 (coverage), R-06-07 (generation: candidates, proposal, confirmation, `Idempotency-Key`), R-06-15 (variants `classes.maxInstructorsPerClass > 1`, `levels.enabled=false`) · §6 rows `/week-templates*`, `/coverage`, `/weeks`, `/weeks/generation-candidates`, `POST /weeks`, `POST /weeks/{id}/generation` + form A, form C, «Altres formes» (`GET /weeks` item) · §10 (`admin-scheduling`, `enums:*`) · §13 assumptions 1, 8, 10 · tests **T-06-26, T-06-27** (+ the D3 part of T-06-30) · `docs/DECISIONS_PENDENTS.md` A22 (b, c, d) · `docs/specs/00-transversal/CATALEG_ERRORS.md` (16-09 statuses), `CATALEG_PARAMETRES.md` (`classes.maxInstructorsPerClass`, `classes.slotMinutes`, `levels.enabled`, `coverage.thresholds`, `coverage.activeDogWeeks`), `MATRIU_PERMISOS.md` («Plantilles setmanals…»: INSTRUCTOR R, ADMIN RW + X), `CONVENCIONS_I18N.md` · mockups `docs/pantalles/escriptori/D3-plantilles-quadre-setmanal-i-creacio-de-classes.*`, `D3b-plantilles-visio-del-dia-per-pista-per-instructor.*`.

## Context to load
S06 §2 (rows D3, D3b), §3 (`WeekTemplate`, `Week`), §4 (R-06-01…07, 15), §6, §10, §11 (front), §13 · api task `E4-T01` (schema names: `WeekTemplateSummary`, `WeekTemplate`, `TimeBand`, `TemplateClass`, `Inconsistency`, `Week`, `WeekListItem`, `GenerationCandidates`, `GenerationRequest`, `GenerationResult`, `Coverage`) — read it from the staged snapshot or, mocks-first, from S06 §6 · `packages/api-client` (`openapi.json`, `pending.json`, `scripts/generate.mjs`, `src/mocks/{handlers,scenarios,fixtures.contract.test}.ts`, `src/client.ts` `DEFAULT_IDEMPOTENT_PATHS`) · `packages/ui/src/{components.tsx,components.css,tokens.css,modules.ts}` (`Card`, `Chip`, `Badge`, `Select`, `Input`, `FormField`, `Modal`, `Drawer`, `Tabs`, `DataTable`, `Skeleton`, `EmptyState`, `useToast`) · `apps/clubs-admin/src/App.tsx` (`ADMIN_ROUTES`, sidebar entry `weekly-template`, `routeContent`) · `apps/clubs-admin/src/catalogs/RingsPage.tsx` (`--catalog-color` custom-property pattern for catalog colours, `GET /rings|/levels|/instructors` usage) · `apps/clubs-admin/src/dashboard/DashboardPage.tsx` (page pattern: client prop, skeleton/empty/error states, toast) · `packages/i18n/src/{types,resources,format}.ts` · `docs/playbooks/screen-from-mockup.md`, `checklist.md` · this file.

## Steps
0. **Contract.** The api thread publishes E4-T01 (contracts S06 + S07) before the web starts E4. Normal path: the organizer has staged `packages/api-client/openapi/openapi.next.json` (its commit message says which api commit) → swap it into `openapi.json` byte for byte (`cmp`), run `pnpm api:generate`, prune every S06/S07 path and schema from `pending.json`, run the Ajv fixture suite (`packages/api-client/src/mocks/fixtures.contract.test.ts`). If the staged snapshot is **not** there when you start: go mocks-first — declare the S06 routes of this task in `pending.json` from S06 §6 (paths `/week-templates`, `/week-templates/{id}`, `/week-templates/{id}/bands`, `…/bands/{bandId}`, `…/classes`, `…/classes/{classId}`, `/coverage`, `/weeks`, `/weeks/generation-candidates`, `/weeks/{id}`, `/weeks/{id}/generation`) with E4-T01's schema names, fixtures validated by Ajv, and say so in the report (the organizer swaps later). Either way, add `/^\/weeks\/[^/]+\/generation$/` to the client's idempotent paths (`Idempotency-Key`, R-06-07).
1. **Routes and roles** (`apps/clubs-admin/src/App.tsx`): `/plantilles` (`?template=<templateId>`) and `/plantilles/:templateId/dia/:dayOfWeek` (`dayOfWeek` = `MONDAY…SATURDAY`, lower-case in the URL is fine as long as it round-trips). Roles `["INSTRUCTOR", "ADMIN"]` on both routes **and** on the sidebar entry `weekly-template` (S06 §2: «ADMIN (INSTRUCTOR: lectura)», §13-10 assumption: visible read-only, no buttons). `readOnly = !roles.includes("ADMIN")`. Files: `apps/clubs-admin/src/planning/TemplatesPage.tsx`, `TemplateDayPage.tsx`, `ClassForm.tsx` (reused by E4-W02), `planning.css`.
2. **Header and tabs** (D3): title «Plantilles»; chip-tabs «dl–dv: «{name}» ▾» (menu = `GET /week-templates?kind=WEEKDAYS&active=true`) and «Dissabtes ▾» (`kind=SATURDAY`); the choice per kind is remembered in `localStorage` `agilityhub.planning.templates.v1` and reflected in `?template=`; the weekday chip is filled/orange when the visible grid is the weekday template, the Saturday chip when it is the Saturday one (one grid at a time). Buttons (ADMIN only): [＋ Nova] → `Modal` with name (≤ 40) + kind radio (`enums:templateKind.WEEKDAYS` «dl–dv» / `SATURDAY` «dissabtes») → `POST /week-templates {name, kind}` (409 `DUPLICATE_NAME` → field error); [Duplica] → same modal prefilled «{name} (còpia)» → `POST /week-templates {name, kind, copyFromId}`; [＋ Franja] → `Drawer` with `startTime`/`endTime` (steps of `classes.slotMinutes`) → `POST /week-templates/{id}/bands` (409 `BAND_OVERLAP`, 400 `INVALID_SLOT_GRANULARITY`, 422 `OUTSIDE_OPENING_HOURS` shown on the field); clicking a row label (e.g. «8:30») opens the same drawer to edit (`PATCH …/bands/{bandId}` with `version`) or delete the band (`DELETE …/bands/{bandId}`; 409 `BAND_NOT_EMPTY` → message); [＋ Crear classe] → the class card (step 4) empty. Empty state (no template at all): card with [＋ Nova]. Loading: `Skeleton` of the grid. Error: toast by `code` + [Torna-ho a provar].
3. **Week grid** — new shared component `packages/ui/src/schedule-grid.tsx` (`ScheduleGrid`, `ScheduleCell`; E4-W02 reuses it for D4 and E4-W03 builds `DayGrid` on it): generic rows × columns with stacked cells. Props: `columns: {id, label, color?, onSelect?}[]`, `rows: {id, label, cells: {columnId, …}[]}[]`, `renderCell`, `selectedCellId`. Layout per mockup: thin lines at the band limits, row label = the band `startTime` string with its leading zero stripped («8:30», «17:40»), day headers as orange outlined buttons «DILLUNS ›» (the weekday name from `Intl.DateTimeFormat(locale, {weekday: "long"})`, upper-cased through CSS, never a literal) → D3b. `ScheduleCell`: line 1 `displayDescription`, line 2 instructor `shortName`s joined by «, » (R-06-15); background/border tinted with the ring colour through a CSS custom property (`--schedule-color`, same pattern as `RingsPage.tsx`, no literal colours), grey «sense pista» tone when `ringId` is null; `inconsistencyIds.length > 0` → warning outline. Footer literal `admin-scheduling:templates.gridFooter` = «Clica el nom d'un dia per veure'l per pista o per instructor · color = pista · gris = sense pista». Below the grid, one yellow note per inconsistency: `templates.inconsistencyNote` = «Incoherència: {message}. Mentre hi hagi incoherències no es poden generar classes d'aquesta plantilla.» (`message` comes localized from the API; never rebuild it). Click on an empty cell (ADMIN) → class card prefilled with band + day; click on a full cell → the card in edit mode (`PATCH …/classes/{classId}` per change, `version`); INSTRUCTOR: cells are not interactive.
4. **Class card** (the mockup's «CREAR CLASSE» card under the grid, not a drawer): fields exactly as the mockup — «Franja» select «18:50–19:50 ▾» (bands), «Dia» chips «dl · dt · dc · dj · dv» (or «ds»; `fmtDate(weekdayShort)` of the template `days`), «Instructor» = one `Select` «Neus ▾» when `classes.maxInstructorsPerClass = 1`, multi-chips when > 1 (R-06-15; read `GET /parameters/classes.maxInstructorsPerClass`), «Pista» chips = active rings in catalog order with their colour dot + «Sense pista», «Nivells» chips = active levels in `order` (hidden when `levels.enabled = false`, `GET /parameters/levels.enabled`), «Descripció» text input (≤ 40) whose placeholder/preview is the **automatic** text and whose value is the manual override; «Límit» number (empty = AUTO, R-06-04) — the mockup omits it in D3, keep it as a small field after «Descripció». Button [CREA LA CLASSE] → `POST /week-templates/{id}/classes {bandId, dayOfWeek, instructorIds[], ringId?, levelIds[], capacity?, description?}`; 422 `TOO_MANY_INSTRUCTORS` / `LEVEL_REQUIRED` / `DESCRIPTION_REQUIRED` mapped to the field. After a save started from an empty cell, ask `Modal` «Vols afegir-ne una altra a la mateixa franja?» (`templates.addAnotherInBand`, key given in S06 §13) with [Sí] / [No] (`templates.addAnotherYes` «Sí» · `templates.addAnotherNo` «No»); «Sí» keeps band + day. Automatic description preview (R-06-03, used only while the class has no API value): pure helper `automaticDescription(levels, levelIds, t)` in `apps/clubs-admin/src/planning/description.ts` — 1 level → «{name}»; a contiguous set that reaches the last active level with ≥ 2 levels → `admin-scheduling:description.andAbove` «{level} i sup.» (es «{level} y sup.», en «{level} and up»); otherwise names joined by `description.join` «+»; unit test with the catalog Cadells·A·B·C·D·E·F·G: {B,C} → «B+C», {D,E,F,G} → «D i sup.», {A,C} → «A+C», {Cadells} → «Cadells», manual «Obed. urbana» wins, cleared → automatic. Cells always render the API `displayDescription` (the mockup's «≥D», «Ob.urbana» variants are not reproduced — A22 d).
5. **Generation card** «Generar classes — per setmanes»: `Select` of `GET /weeks/generation-candidates` items labelled `templates.generation.weekOption` «Setmana del {range}» with `{range}` from a new shared formatter `formatWeekRange(start, end, locale, timeZone)` in `packages/i18n/src/format.ts` → «24 al 30 d'agost» (same month) / «28 de setembre al 4 d'octubre» (different months); the `proposed` candidate preselected (R-06-07). [GENERAR CLASSES] disabled when any visible template has `canGenerate = false` with the badge `templates.generation.blocked` «bloquejat: {count, plural, one {# incoherència} other {# incoherències}} a la plantilla». Click → `Modal` «Es generaran com a esborrany les classes de la setmana del {date}» (`{date}` = `fmtDate(startDate, "short")` → dd/mm/aaaa) → if the candidate has no `weekId`: `POST /weeks {startDate}` → `POST /weeks/{id}/generation {weekdayTemplateId, saturdayTemplateId}` (`saturdayTemplateId` = the template of the «Dissabtes» tab, `null` when none — no extra choice, S06 §2) → toast `templates.generation.done` «{count} classes generades» (+ `templates.generation.skipped` «{count} omeses ({reasons})» with `enums:skipReason.HOLIDAY` «festiu» / `PAST` «dies passats») → refetch table and candidates. Errors by code: 409 `WEEK_ALREADY_GENERATED`, 422 `TEMPLATE_INCONSISTENT` / `TEMPLATE_KIND_MISMATCH` / `WEEK_IN_PAST` → `errors:` message. Table «SETMANES» (`DataTable` over `GET /weeks?filter=startDate:gte:<Monday of the club-local current week>&sort=startDate,asc`): columns «Any · Setmana · Inici · Classes generades · Validades» → `isoYear`, `isoWeek`, `fmtDate(startDate)` as «17/08» (`dayMonth` numeric: use `Intl.DateTimeFormat` with `day: "2-digit", month: "2-digit"` — add presentation `dayMonthNumeric` to `format.ts`), `generatedAt` → «17/08 · 09:12» (`dayMonthNumeric` + « · » + `fmtTime`), otherwise chip «pendent» (`enums:weekState.PENDING`), `validatedAt` idem or «—».
6. **Coverage table** «Cobertura per nivell (places de la setmana)» from `GET /coverage?templateId=<weekday>&saturdayTemplateId=<saturday>` (form C): columns «Nivell · Places màximes · Places prop. · Màxim s/tots · Prop. s/actius*»; `propSeats` with one decimal and the locale separator («70,5»), ratios «233%» (`null` → «—»); status chip `enums:coverageStatus.OK` «bé» (success) · `TIGHT` «ajustat» (warning) · `SHORT` «manca oferta» (danger, muted) · `EXPAND` «cal ampliar» (danger) · `NO_DOGS` «sense gossos» (neutral; literal proposed — flag it). Footnote `coverage.footnote` = «Places prop. = les places de cada classe repartides entre els seus nivells (una classe A+B de 5 places compta 2,5 per A i 2,5 per B) · * sobre els gossos del nivell amb alguna reserva la setmana en curs o l'anterior · avís segons Prop. s/actius: bé > {ok} % · ajustat {tight}–{ok} % · manca oferta {short}–{tight} % · cal ampliar < {short} %» with the `thresholds` of the response. 422 `LEVELS_DISABLED` → table hidden, no error toast. INSTRUCTOR sees it (allowed by the matrix).
7. **D3b** `/plantilles/:templateId/dia/:dayOfWeek`: header chip «Plantilla: «{name}» ({dl–dv | dissabtes})», day selector ‹ «Dilluns ▾» › (days of the template), segmented «Visió per pista» / «Visió per instructor». Derived on the client from form A (no endpoint): rows = template bands; columns per ring = only the rings used that day in catalog order + «Sense» (`templates.day.noRing`) when a class has no ring; per instructor = instructors with a class that day (`shortName`); same `ScheduleGrid`/`ScheduleCell`. [‹ Tornar a la visió setmanal] → `/plantilles?template={id}`. Available to INSTRUCTOR (read-only).
8. **i18n**: namespace `admin-scheduling` (`templates.*`, `description.*`, `coverage.*`, `inconsistency.*`) + new namespace `enums` (`templateKind.*`, `weekState.*`, `coverageStatus.*`, `inconsistencyType.*` — the 7 catalog values —, `skipReason.*`) registered in `packages/i18n/src/types.ts` and the three loaders of `resources.ts`; ca = the mockup literals above, es/en in the same task; vocabulary lint green (T-06-30, D3 part: a Vitest that loads `admin-scheduling` `ca` and asserts the exact literals «Clica el nom d'un dia…», «Mentre hi hagi incoherències…», «Es generaran com a esborrany…», «bloquejat: …»).
9. **MSW**: fixtures `packages/api-client/src/mocks/fixtures/planning.ts` — templates «Setmana A» (clean, `canGenerate: true`, the D3 grid classes with `displayDescription` «A», «A+B», «B+C», «C+D+E», «F+G», «D i sup.», «Cadells», one manual «Obed. urbana» Wednesday 20:00 without ring), «Setmana B» (the D3 inconsistency: Wednesday 20:00, two classes on Central → `RING_DOUBLE_BOOKED`, `message` «dimecres 20:00 — pista Central amb dues classes alhora», `canGenerate: false`), «Dissabtes»; `coverage` = the 8 mockup rows; weeks 2026-W34 (generated + validated) and W35 (pending); candidates with `proposed` = W35. Stateful handlers: create/patch/delete band and class (recompute `displayDescription` with the step-4 helper and `inconsistencyIds` when two classes share day + band + ring), create template, generation (sets `generatedAt`, returns `classCount`). Scenario variants: `instructor` (existing), `planningNoLevels` (`levels.enabled=false`, coverage 422 `LEVELS_DISABLED`), `planningTwoInstructors` (`maxInstructorsPerClass = 2`). Add them to `scenarios.ts`; update the handler-count assertion in `src/index.test.ts`.
10. **Tests and evidence**: Vitest `apps/clubs-admin/src/planning/TemplatesPage.test.tsx` **T-06-26** (empty-cell click prefills band + day; save asks «Vols afegir-ne una altra a la mateixa franja?»; description fills itself on level selection and the manual text stays; cell and footer note mark the inconsistency; [GENERAR CLASSES] disabled with «bloquejat: 1 incoherència a la plantilla»; confirmation «Es generaran com a esborrany les classes de la setmana del …»; INSTRUCTOR: no buttons, cells inert) and `TemplateDayPage.test.tsx` **T-06-27** (columns = used rings + «Sense», instructor switch, back link). Playwright `apps/clubs-admin/e2e/planning-templates.spec.ts` (scenario `admin`, then `instructor`) with screenshots under `roadmap/evidence/E4-W01/`: `D3-plantilles-1280.png` (Setmana B with the note and the blocked badge, class card open on dl 18:50), `D3-plantilles-setmana-a-1280.png`, `D3b-dia-per-pista-1280.png`, `D3b-dia-per-instructor-1280.png`, `D3-plantilles-instructor-1280.png`; compare with `docs/pantalles/escriptori/D3-*.png`, `D3b-*.png`.

## Fixed conventions
- Design system only (`@agilityhub/ui`); no literal colour outside `tokens.css` — ring/level colours come from the catalog through CSS custom properties. Keys never literals; ca/es/en together; no «parell*», «amigable», «(paràmetre)», requirement codes in UI text. Dates/times through `packages/i18n` formatters with the club `timeZone` (`useClubFormats`); `fmtMoney` unused here.
- Every rendered description is the API `displayDescription`; the front helper of step 4 is a preview only.
- Mockup fidelity is checked with screenshots against `docs/pantalles/escriptori/…`; deviations mandated by decisions (A22 d: single description form; A22 c: instructor read-only) are expected and listed in the report.
- Evidence rule: report = command + exit code + last 40 lines; long outputs → `roadmap/evidence/E4-W01/NN-<name>.log`; task file ≤ 120 KB. Complete `pnpm e2e` (three Playwright projects) in `mcr.microsoft.com/playwright:v1.63.0-noble` before `awaiting_verification`. Fictional data only (`@example.test`, fictional instructor names — never the mockup's staff as real people). Never edit other tasks, `ROADMAP.md` or the Organizer verification section. Closed catalogs: proposals (e.g. `coverageStatus.NO_DOGS` literal) in the report + `MESSAGES.md`.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `pnpm api:generate` (after step 0) and the Ajv suite `pnpm --filter @agilityhub/api-client test -- src/mocks/fixtures.contract.test.ts`; `pnpm turbo run lint typecheck test build`; `pnpm i18n:check`; Vitest T-06-26, T-06-27 (+ the description helper and the D3 literal test); complete `pnpm e2e` in the Playwright Docker image (`scripts/e2e-docker.sh`); the five 1280 px screenshots.

## Definition of Done
D3 and D3b work against MSW in ca/es/en for ADMIN (full) and INSTRUCTOR (read-only), with the `levels.enabled=false` and `maxInstructorsPerClass=2` variants; generation goes through candidates → confirmation → `POST /weeks/{id}/generation`; the coverage table shows the D3 vocabulary; `ScheduleGrid` is exported from `packages/ui` for E4-W02/E4-W03.

## Evidence to return
Command outputs, the snapshot adoption proof (or the `pending.json` diff), screenshots, the list of `admin-scheduling`/`enums` keys, deviations from the mockup with their decision id.

---

## Executor report

Session 2026-09-24 (Claude executor). The earlier session had only set `in_progress`; all work below is from this session. Dependencies E2-W01, E2-W04, E2-W08 are `verified`; api E4-T01 contract is in the adopted snapshot.

### Branch / commits
Trunk-based, no git writes by the executor: the working tree on `main` is committed by the publish script after the session.

### Evidence (full command outputs)

**Step 0 — snapshot adoption proof.** The organizer's staged `openapi.next.json` (commit `46d3848`, «stage api snapshot 8117f37») was copied byte for byte into `openapi.json`, then the staged file was removed (as E3-W03 did):

```text
$ cp packages/api-client/openapi/openapi.next.json packages/api-client/openapi/openapi.json && cmp packages/api-client/openapi/openapi.next.json packages/api-client/openapi/openapi.json && echo IDENTICAL
IDENTICAL
$ git show 46d3848:packages/api-client/openapi/openapi.next.json | shasum -a 256
f6a1baaef04667882e4ac5eee0630377694bb591b150228529d1253703f52245  -
$ shasum -a 256 packages/api-client/openapi/openapi.json
f6a1baaef04667882e4ac5eee0630377694bb591b150228529d1253703f52245  packages/api-client/openapi/openapi.json
```
The S06 routes and schemas (`/week-templates*`, `/coverage`, `/weeks*`, `WeekTemplate`, `TimeBand`, `TemplateClass`, `Inconsistency`, `Week`, `WeekListItem`, `GenerationCandidates`, `GenerationRequest`, `GenerationResult`, `Coverage`) were already identical in the previous `openapi.json`; the new snapshot adds 30 E5 paths (bookings, waitlist, training, jobs) and ~95 schemas, and changes only `AuditAction` (enum order). `pending.json` had no S06/S07 entries, so nothing was pruned (it still holds only `/members/{id}/notification-preferences` and its support schemas).

1. `pnpm api:generate` — exit 0 — full output `roadmap/evidence/E4-W01/00-api-generate.log`:
```text
> agilityhub-core-web@0.0.0 api:generate /Users/jordib/dev/agilityhub/agilityhub-core-web
> pnpm --filter @agilityhub/api-client api:generate

> @agilityhub/api-client@0.0.0 api:generate /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client
> node scripts/generate.mjs

Generated /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client/src/generated/schema.d.ts from /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client/openapi/openapi.json merged with /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client/openapi/pending.json
```

2. `pnpm --filter @agilityhub/api-client test -- src/mocks/fixtures.contract.test.ts` — exit 0 — `01-ajv.log` (19 tests: the JSON fixtures plus the new «E4-W01 planning fixtures follow the S06 contract» block validating the three templates as `WeekTemplate`, the coverage fixture as `Coverage`, and the weeks as `WeekListItem`, and ISO week 34/35 for 2026-08-17/24):
```text
> @agilityhub/api-client@0.0.0 test /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client
> vitest run src/mocks/fixtures.contract.test.ts

 RUN  v5.0.0 /Users/jordib/dev/agilityhub/agilityhub-core-web/packages/api-client

 Test Files  1 passed (1)
      Tests  19 passed (19)
   Start at  10:01:08
   Duration  477ms (tests 45%, transform 30%, import 24%)
```

3. `pnpm turbo run lint typecheck test build --force` (the Verification command with `--force` so no task comes from cache) — exit 0 — full output `02-turbo.log` (743 lines). Earlier attempt: `clubs-admin#build` failed on a `Branding` typing in the new `description.test.ts` → fixed by setting `theme.mode` like the other tests (`02a-turbo-failed.log`). Test totals of the final run: ui 41, i18n 18, api-client 31, auth 17, id 9, clubs 29, clubs-admin 53 — all passed. Last 40 lines:
```text
clubs-admin:test:      ✓ renders applied filters and preserves selection when page size changes 587ms
clubs-admin:test:      ✓ toggles visible columns and synchronizes the ordered fields to the URL 308ms
clubs-admin:test:    ✓ T-03-42 D15 translated dog list (1)
clubs-admin:test:      ✓ renders the approved columns and dog record links 840ms
clubs-admin:test: stderr | src/audit/AuditPage.test.tsx > T-14-26 member audit and exports > shows the catalogued EXPORT_LIMIT explanation in the exports drawer
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/TemplatesPage.test.tsx > T-06-26 D3 weekly templates > R-06-15 offers instructor chips with maxInstructorsPerClass = 2
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/planning/TemplatesPage.test.tsx > T-06-26 D3 weekly templates > R-06-15 hides levels and coverage with levels.enabled = false and requires a manual description
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/audit/AuditPage.test.tsx > T-14-26 member audit and exports > routes a queued members list export into the global drawer
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test:  ✓ src/planning/TemplatesPage.test.tsx (8 tests) 1850ms
clubs-admin:test:    ✓ T-06-26 D3 weekly templates (8)
clubs-admin:test:      ✓ prefills band and day from an empty cell, fills the description by levels, keeps the manual text and asks for another class in the band 721ms
clubs-admin:test:      ✓ saves every change of a full cell at once with the template version 460ms
clubs-admin:test: stderr | src/audit/AuditPage.test.tsx > T-14-26 member audit and exports > routes a queued dogs list export into the global drawer
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test: stderr | src/audit/AuditPage.test.tsx > T-14-26 member audit and exports > links LastChange to the entity-scoped audit trail
clubs-admin:test: [BrandingProvider] Club Agility Cànic: primary and onPrimary contrast (3.32:1) is below WCAG AA.
clubs-admin:test: 
clubs-admin:test:  ✓ src/audit/AuditPage.test.tsx (6 tests) 1985ms
clubs-admin:test:    ✓ T-14-26 member audit and exports (6)
clubs-admin:test:      ✓ filters by action and opens the masked before/after change drawer 428ms
clubs-admin:test:      ✓ opens the exports drawer for a queued job and exposes its READY download 1244ms
clubs-admin:test: 
clubs-admin:test:  Test Files  11 passed (11)
clubs-admin:test:       Tests  53 passed (53)
clubs-admin:test:    Start at  10:07:42
clubs-admin:test:    Duration  4.40s (environment 43%, tests 28%, transform 13%, setup 9%, import 6%)
clubs-admin:test: 

 Tasks:    26 successful, 26 total
Cached:    0 cached, 26 total
  Time:    13.486s 
```
(The contrast warning is the pre-existing BrandingProvider notice for the Cànic fixture, printed by every admin page test.)

4. `pnpm i18n:check` — exit 0 — `04-i18n.log`:
```text
> agilityhub-core-web@0.0.0 i18n:check /Users/jordib/dev/agilityhub/agilityhub-core-web
> node scripts/i18n-check.mjs && node scripts/check-error-codes.mjs

i18n check passed
ca: 1858 keys
es: 1858 keys
en: 1858 keys
1312 statically used keys, 0 unused
250 codes, 0 missing
```

5. Vitest T-06-26, T-06-27, description helper and D3 literal test: `pnpm --filter clubs-admin exec vitest run --reporter=verbose src/planning src/App.test.tsx` — exit 0 — `05-vitest-planning.log` (28 tests). Result lines (stderr contrast notices removed from this excerpt only):
```text
 ✓ src/planning/description.test.ts > T-06-02 R-06-03 automatic description preview (D3 class card) > builds «B+C», «D i sup.», «A+C» and «Cadells» from the catalog order
 ✓ src/planning/description.test.ts > T-06-02 R-06-03 automatic description preview (D3 class card) > ignores inactive levels when deciding the last active level
 ✓ src/planning/description.test.ts > T-06-02 R-06-03 automatic description preview (D3 class card) > uses the es form of «i sup.»
 ✓ src/planning/description.test.ts > T-06-02 R-06-03 automatic description preview (D3 class card) > uses the en form of «i sup.»
 ✓ src/planning/description.test.ts > T-06-30 D3 literals and vocabulary (admin-scheduling, ca) > keeps the exact mockup literals
 ✓ src/planning/description.test.ts > T-06-30 D3 literals and vocabulary (admin-scheduling, ca) > uses no forbidden vocabulary or requirement codes in the three locales
 ✓ src/planning/TemplateDayPage.test.tsx > T-06-27 D3b day view per ring / per instructor > shows only the rings used that day in catalog order, plus «Sense» when a class has no ring
 ✓ src/planning/TemplateDayPage.test.tsx > T-06-27 D3b day view per ring / per instructor > drops unused rings and the «Sense» column on a day without them
 ✓ src/planning/TemplateDayPage.test.tsx > T-06-27 D3b day view per ring / per instructor > switches to one column per instructor with a class that day
 ✓ src/planning/TemplateDayPage.test.tsx > T-06-27 D3b day view per ring / per instructor > moves between days and goes back to the weekly view of the same template
 ✓ src/planning/TemplateDayPage.test.tsx > T-06-27 D3b day view per ring / per instructor > shows the Saturday template with its single day
 ✓ src/App.test.tsx > T-02-14 clubs-admin shell > T-06-26 shows «Plantilla setmanal» to instructors (read-only D3, S06 §13-10)
 ✓ src/App.test.tsx > T-02-14 clubs-admin shell > registers every desktop route from the frontend plan
 ✓ src/planning/TemplatesPage.test.tsx > T-06-26 D3 weekly templates > prefills band and day from an empty cell, fills the description by levels, keeps the manual text and asks for another class in the band
 ✓ src/planning/TemplatesPage.test.tsx > T-06-26 D3 weekly templates > saves every change of a full cell at once with the template version
 ✓ src/planning/TemplatesPage.test.tsx > T-06-26 D3 weekly templates > marks the inconsistency on the cells and in the footer note and blocks the generation
 ✓ src/planning/TemplatesPage.test.tsx > T-06-26 D3 weekly templates > confirms «Es generaran com a esborrany les classes de la setmana del …» and generates the proposed week
 ✓ src/planning/TemplatesPage.test.tsx > T-06-26 D3 weekly templates > hides every action from INSTRUCTOR and keeps the cells inert (A22 c)
 ✓ src/planning/TemplatesPage.test.tsx > T-06-26 D3 weekly templates > shows the coverage table with the D3 vocabulary and one-decimal places
 ✓ src/planning/TemplatesPage.test.tsx > T-06-26 D3 weekly templates > R-06-15 offers instructor chips with maxInstructorsPerClass = 2
 ✓ src/planning/TemplatesPage.test.tsx > T-06-26 D3 weekly templates > R-06-15 hides levels and coverage with levels.enabled = false and requires a manual description
 Test Files  4 passed (4)
      Tests  28 passed (28)
```
(Also covered in the turbo run: `packages/ui/src/schedule-grid.test.tsx`, the `formatWeekRange`/`dayMonthNumeric` tests in `packages/i18n/src/format.test.ts`, and «R-06-07 sends an Idempotency-Key on POST /weeks/{id}/generation by default» in `packages/api-client/src/index.test.ts`.)

6. Complete Playwright suite in `mcr.microsoft.com/playwright:v1.63.0-noble`: `pnpm e2e:docker > test-results/e4-w01-e2e-docker.log 2>&1` — exit 0 — copied to `03-e2e-docker.log` (343 lines). clubs-admin **27 passed** (includes the 2 new `planning-templates.spec.ts` tests), clubs **21 passed**, id **1 passed**. Note on earlier runs: the first three runs (two exit 0 with the log redirected into `roadmap/evidence/E4-W01/`, one through `tee`) produced logs of only 5 lines because `scripts/e2e-docker.sh` ends with `cp -R /work/roadmap/evidence/. /evidence/`, which overwrites any log written into the evidence folder with the container's copy; the `tee` run did show 27/21/1 passed on screen. The final run writes to the git-ignored `test-results/` and copies the log in afterwards. A CSS tweak (SETMANES column widths) and a Prettier pass on the new files came after earlier green runs; the log above is from the final run on the final tree. Last 40 lines:
```text
clubs-admin:build: [INEFFECTIVE_DYNAMIC_IMPORT] ../../packages/i18n/src/locales/es/common.json is dynamically imported by ../../packages/i18n/src/resources.ts but also statically imported by ../../packages/i18n/src/catalog.ts, dynamic import will not move module into another chunk.
clubs-admin:build: 
clubs-admin:build: [INEFFECTIVE_DYNAMIC_IMPORT] ../../packages/i18n/src/locales/es/shell.json is dynamically imported by ../../packages/i18n/src/resources.ts but also statically imported by ../../packages/i18n/src/catalog.ts, dynamic import will not move module into another chunk.
clubs-admin:build: 
clubs-admin:build: ✓ built in 197ms
id:e2e: cache bypass, force executing e2026623cfa67ccc
id:e2e: 
id:e2e: > id@0.0.0 e2e /work/apps/id
id:e2e: > playwright test
id:e2e: 
id:e2e: 
id:e2e: Running 1 test using 1 worker
id:e2e: ·
id:e2e:   1 passed (2.9s)
clubs:e2e: cache bypass, force executing 8759df9381a5f86b
clubs:e2e: 
clubs:e2e: > clubs@0.0.0 e2e /work/apps/clubs
clubs:e2e: > playwright test
clubs:e2e: 
clubs:e2e: [WebServer] (node:841) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs:e2e: 
clubs:e2e: Running 21 tests using 5 workers
clubs:e2e: ·····················
clubs:e2e:   21 passed (10.9s)
clubs-admin:e2e: cache bypass, force executing 2a21cc336cd6406f
clubs-admin:e2e: 
clubs-admin:e2e: > clubs-admin@0.0.0 e2e /work/apps/clubs-admin
clubs-admin:e2e: > playwright test
clubs-admin:e2e: 
clubs-admin:e2e: [WebServer] (node:1950) [DEP0169] DeprecationWarning: `url.parse()` behavior is not standardized and prone to errors that have security implications. Use the WHATWG URL API instead. CVEs are not issued for `url.parse()` vulnerabilities.
clubs-admin:e2e: [WebServer] (Use `node --trace-deprecation ...` to show where the warning was created)
clubs-admin:e2e: 
clubs-admin:e2e: Running 27 tests using 9 workers
clubs-admin:e2e: ···························
clubs-admin:e2e:   27 passed (15.2s)

 Tasks:    6 successful, 6 total
Cached:    0 cached, 6 total
  Time:    38.006s 
```
The complete suite also re-captured other tasks' evidence PNGs; those 47 files were restored byte for byte from `HEAD` (`git show HEAD:<path>`), so only `roadmap/evidence/E4-W01/` changes.

7. Screenshots (1280 px, `page.clock.setFixedTime(2026-08-19T10:00+02:00)` so the SETMANES table shows the mockup's W34/W35), compared with `docs/pantalles/escriptori/D3-*.png` / `D3b-*.png`:
- `roadmap/evidence/E4-W01/D3-plantilles-1280.png` — Setmana B, yellow outlines on dc 20:00 (two classes on Central), footer hint + yellow note, «bloquejat: 1 incoherència a la plantilla», disabled [GENERAR CLASSES], class card on 18:50–19:50 · dl · Anna · Central · B+C (placeholder «B+C»).
- `D3-plantilles-setmana-a-1280.png` — clean Setmana A, SETMANES «2026 34 17/08 17/08 · 09:12 18/08 · 10:02» and «2026 35 24/08 pendent —», coverage table with the 8 mockup rows.
- `D3b-dia-per-pista-1280.png` — dilluns per ring: Muntanya · Central · Carretera · Cadells · Petita · Sense («Obed. urbana»).
- `D3b-dia-per-instructor-1280.png` — dilluns per instructor: Laura · Marc · Anna · Sergio.
- `D3-plantilles-instructor-1280.png` — INSTRUCTOR: no buttons, no class card, no generation select/button; SETMANES and coverage read-only; sidebar shows «Plantilla setmanal».

Deviations from the mockup: (a) A22 d — descriptions use the single form «D i sup.» / «C i sup.» / «E i sup.» instead of «≥D», «≥C», «E+F+G»; the manual text is «Obed. urbana» (mockup cell «Ob.urbana»). (b) A22 c — INSTRUCTOR read-only. (c) Instructor names are the fictional catalog fixture (Laura, Marc, Anna, Sergio) instead of the mockup staff. (d) Ring order follows the fixture catalog (Muntanya, Central, Carretera, Cadells, Petita), so D3b columns differ in order from the mockup (Muntanya, Carretera, Central, Petita, Cadells). (e) The level chips include «Teràpia» because it is an active level in the S05 seed/fixture (the mockup draws Cadells…G only). (f) «Límit» is shown after «Descripció» (the mockup omits it, as the task asks). (g) Coverage status header «Avís» (the mockup header is empty; the word comes from the footnote). (h) Dates from `Intl` use the typographic apostrophe («24 al 30 d’agost») like the D1 dashboard; the mockup uses «d'agost».

### Files changed
- Contract: `packages/api-client/openapi/openapi.json` (snapshot `8117f37`), `openapi.next.json` (removed after adoption), `src/generated/schema.d.ts` (regenerated).
- Client: `packages/api-client/src/client.ts` (`/^\/weeks\/[^/]+\/generation$/` in the default idempotent paths), `src/index.test.ts` (idempotency test, handler count 126 → 142).
- MSW: `src/mocks/fixtures/planning.ts` (new: Setmana A clean, Setmana B with the D3 inconsistency, Dissabtes; coverage 8 rows; weeks relative to the club-local current week; mock description/inconsistency resolvers), `src/mocks/planning-handlers.ts` (new, 16 stateful handlers), `src/mocks/handlers.ts` (spreads them, `resetPlanningState`, parameters `classes.maxInstructorsPerClass`, `classes.slotMinutes`, scenario-driven `levels.enabled`), `src/mocks/scenarios.ts` (`planningNoLevels`, `planningTwoInstructors`), `src/mocks/fixtures/catalogs.ts` (level names A…G as in the S05 seed), `src/mocks/fixtures.contract.test.ts` (Ajv block for planning fixtures).
- UI: `packages/ui/src/schedule-grid.tsx`, `schedule-grid.css`, `schedule-grid.test.tsx` (new `ScheduleGrid`, `ScheduleCell`), `src/index.ts`.
- i18n: `packages/i18n/src/format.ts` (+`formatWeekRange`/`fmtWeekRange`, presentations `dayMonthNumeric` and `weekdayLong`), `format.test.ts`, `types.ts`, `resources.ts` (namespaces `admin-scheduling`, `enums`), `locales/{ca,es,en}/admin-scheduling.json`, `locales/{ca,es,en}/enums.json`, `locales/{ca,es,en}/common.json` (`format.weekRange.*`).
- App: `apps/clubs-admin/src/planning/{TemplatesPage.tsx, TemplateDayPage.tsx, ClassForm.tsx, description.ts, shared.ts, planning.css}` + tests `{TemplatesPage.test.tsx, TemplateDayPage.test.tsx, description.test.ts}`; `src/App.tsx` (routes + roles + sidebar), `src/App.test.tsx`, `src/main.tsx` (initial namespaces); `e2e/planning-templates.spec.ts`.
- Tooling/docs: `scripts/i18n-check.mjs` (dynamic `enums:` prefixes), `CHANGELOG.md`, `roadmap/MESSAGES.md` (proposal entry), `roadmap/evidence/E4-W01/*`.

**Keys added** — `admin-scheduling`: `common.{error,loading,retry,close,cancel,save,saving,none}`; `templates.{title,weekdayTab,weekdayTabEmpty,saturdayTab,new,duplicate,band,createClass,gridLabel,gridFooter,inconsistencyNote,cellLabel,emptyCell,bandLabel,bandRange,addAnotherInBand,addAnotherYes,addAnotherNo}`, `templates.empty.{title,description}`, `templates.form.{newTitle,duplicateTitle,name,kind,copyName}`, `templates.bandForm.{newTitle,editTitle,start,end,remove}`, `templates.generation.{title,week,weekOption,submit,blocked,confirm,done,skipped}`, `templates.day.{header,previous,next,select,viewLabel,byRing,byInstructor,noRing,back,gridLabel}`; `classCard.{title,editTitle,band,day,instructor,instructors,ring,noRing,levels,description,capacity,submit,remove,close}`; `weeks.{caption,year,week,start,generated,validated,dateTime,empty}`; `coverage.{title,level,maxSeats,propSeats,maxRatio,propRatio,status,percent,footnote}`; `description.{andAbove,join}`. `enums`: `templateKind.{WEEKDAYS,SATURDAY}`, `weekState.{PENDING,GENERATED,VALIDATED}`, `coverageStatus.{OK,TIGHT,SHORT,EXPAND,NO_DOGS}`, `inconsistencyType.*` (7 values), `skipReason.{HOLIDAY,PAST}`. `common`: `format.weekRange.{sameMonth,differentMonth}`.

### Rules and tests implemented (R-xx-nn / T-xx-nn)
- R-06-01 bands: [＋ Franja] drawer (step = `classes.slotMinutes`), row label → edit/delete; `BAND_OVERLAP`, `INVALID_SLOT_GRANULARITY`, `INVALID_TIME_RANGE`, `OUTSIDE_OPENING_HOURS` on the field, `BAND_NOT_EMPTY`/`STALE_VERSION` as a message (mocked with the same statuses as the catalog).
- R-06-02 class card: band select, one day, single instructor select (or chips up to the max), ring chips + «Sense pista», level chips; create from [＋ Crear classe] or an empty cell (band + day prefilled); full cell → edit mode with one `PATCH` per change carrying the template `version`; «Treu de la plantilla» (`DELETE`).
- R-06-03 automatic description preview (`description.ts`, placeholder) + manual override (value); cells always show the API `displayDescription`.
- R-06-04 «Límit» empty = AUTO (`capacity: null`), placeholder = min level capacity.
- R-06-05 inconsistencies: warning outline + inconsistency type in the cell's accessible name, yellow footer note per API `message`, generation blocked by `inconsistencyCount` of the visible weekday + Saturday templates.
- R-06-06 coverage table (form C), `propSeats` one decimal with the locale separator, ratios «233%», `null` → «—», 422 `LEVELS_DISABLED` hides the card.
- R-06-07 generation: candidates select with `proposed` preselected, confirmation, `POST /weeks` when no `weekId`, `POST /weeks/{id}/generation {weekdayTemplateId, saturdayTemplateId}` with one `Idempotency-Key` per confirmation (+ default idempotent path in the client), toast «{count} classes generades» (+ skipped with reasons), errors by code.
- R-06-15 variants: `planningTwoInstructors` (chips, max 2), `planningNoLevels` (no level chips, manual description required → 422 `DESCRIPTION_REQUIRED` on the field, coverage hidden).
- S06 §2 D3/D3b, §13-10 / A22 c: routes and sidebar entry for `["INSTRUCTOR","ADMIN"]`, `readOnly = !roles.includes("ADMIN")`.
- Tests: **T-06-26** (`TemplatesPage.test.tsx` ×8, `App.test.tsx`, `e2e/planning-templates.spec.ts` ×2), **T-06-27** (`TemplateDayPage.test.tsx` ×5, e2e), **T-06-30** D3 part (`description.test.ts` literal + vocabulary tests, `pnpm i18n:check`), T-06-02 front preview (`description.test.ts`).

### Assumptions
1. Step 0: the staged `openapi.next.json` (api `8117f37`) was adopted even though it also carries E5 contracts (ROADMAP: «each screen task's step 0 adopts it»); S06 was already identical in the previous snapshot.
2. Mock level names now follow the S05 seed («A»…«G», «Cadells»), so chips and descriptions match the mockup; D11's derived row «Cadells 5 · A–D 5 · E–G 4 · Teràpia 1» is unchanged (checked by the settings e2e).
3. «Obed. urbana» (manual, no ring) sits on **Monday** 20:00 as in the D3/D3b mockups (the task text says Wednesday); Monday's D3b then shows the «Sense» column like the mockup.
4. Mock weeks are relative to the club-local current week (current = generated + validated like W34, next = pending like W35), so D3 stays meaningful on any date; the e2e fixes the clock to 2026-08-19 to reproduce W34/W35.
5. The `version` sent with band/class `PATCH` is the template `version` (the contract gives `TemplateClass`/`TimeBand` no own version).
6. The class card is always visible for ADMIN under the grid (create mode by default), as in the mockup; default instructor = first active one, default ring = «Sense pista».
7. Proposed literals without mockup source (ca/es/en): empty state «Encara no hi ha cap plantilla» / «Crea la primera plantilla per dissenyar l'oferta setmanal de classes.», modal titles «Nova plantilla» / «Duplica la plantilla», band drawer «Nova franja» / «Esborra la franja», edit title «Classe seleccionada» (D4 wording), «Treu de la plantilla» (S06 §6 wording), accessibility labels («Dia anterior», «Dia següent», «Crear classe: {day} {time}»), coverage header «Avís», and the `enums:inconsistencyType.*` labels.

### Questions / catalog proposals
1. **`enums:coverageStatus.NO_DOGS` = «sense gossos»** (es «sin perros», en «no dogs») — literal proposed, as the task anticipated; also written to `roadmap/MESSAGES.md`.
2. **R-06-03 «i sup.» and «Teràpia»**: the S05 seed keeps «Teràpia» (order 80) as the last active level, so read literally R-06-03 turns {D,E,F,G} into «D+E+F+G», never «D i sup.». The front helper and the MSW resolver follow the literal rule (the unit test uses the Cadells…G catalog from the spec example); the static fixture strings use «D i sup.» as the task lists. Should the rule use the last active level *excluding* levels such as Teràpia (a flag on `Level`?), or should the api keep the literal rule? Assumption meanwhile: literal rule. Also in `MESSAGES.md`.
3. `scripts/e2e-docker.sh` copies the container's `roadmap/evidence` back over the host one at the end, which overwrites logs written into the evidence folder and re-captures every other task's screenshots. Proposal: copy back only PNGs of the current task, or tell executors to log to `test-results/` (what this session did).

## Organizer verification
Result: changes_requested · 2026-09-24 · organizer (Claude)

D3 and D3b match the mockups. The organizer compared `D3-plantilles-1280.png` with the D3 mockup: the grid, the class card, the generation card and the coverage card all match. The only differences are the A22 d description form and the fictional instructors. The contract is adopted from the staged snapshot, T-06-26/27 pass in Vitest and Playwright, and CI is green at `2dfc476`.

The independent review `roadmap/reviews/E4-W01-20260924-1012-claude.md` found two blocking problems. The organizer confirmed both:
- **#1: the evidence logs are not committed.** The seven logs were never committed, because `*.log` is ignored. The organizer added `!roadmap/evidence/E4-*/*.log` … `E12-*` to `.gitignore`, so they will go in with your next publish.
- **#2: stale version in edit mode.** Each chip click sends a `PATCH` with the `version` of the last render. With two quick clicks, the second sends a stale version and gets `409 STALE_VERSION`, while the chip stays selected.

Round 2 — fix exactly these points:
1. **Evidence.** Check that the logs of `roadmap/evidence/E4-W01/` are sanitized; they are now tracked.
2. **Edit mode (review #2).**
   - Serialize the PATCHes: queue them, or disable the card while one runs.
   - Send the latest `version`, from a ref that `applyTemplate` updates.
   - On `STALE_VERSION`, reload the template and remount the card.
   - Test two quick clicks without waiting between them.
3. **Failed PATCH (review #5).** A failed PATCH reverts the form to the saved class.
4. **Failed loads (review #4).** A failed load of `coverage`, `weeks` or `candidates` shows the toast with [Torna-ho a provar]. Nothing stays loading forever.
5. **Mock consistency (review #3, ruling E29).**
   - Declare `Level.progression` (boolean, default `true`) in `pending.json`, and set it to `false` for Teràpia in the mock catalog.
   - The MSW resolver and `description.ts` compute «i sup.» over the progression levels only, so the fixtures keep «D i sup.» after an edit.
   - The es form is «y sup.» and the en form is «and up».
6. **`scripts/e2e-docker.sh`** copies back only the current task's evidence (your Question 3).
7. **Nits.**
   - #7: band errors show on the right field.
   - #8: an unknown `:dayOfWeek` redirects to a valid day, so the URL round-trips.
   - #10: run Prettier on `App.tsx`.

**Your questions and the other findings:**
- (1) «sense gossos» is accepted.
- (2) Ruling E29: `Level.progression` (S05, and S06 R-06-03 amended). The D11 switch is the new task E4-W06.
- (3) Accepted → point 6.
- Review #6 is accepted: «Obed. urbana» stays on Monday, as in the mockups.
