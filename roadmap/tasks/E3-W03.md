---
id: E3-W03
title: E3 integration (front): adopt the api E3 snapshot and run the fictional signup end to end against the real core (public form → D1 → D2 → welcome mail → screen 03; add-dog → N-37) — gate E3
stage: E3
repo: agilityhub-core-web
thread: B
order: 30
status: ready
depends_on: [E3-W01, E3-W02, E1-W04, external:api:E3-T05]
branch:
pr:
updated: 2026-09-09
---

# E3-W03 · E3 integration (front): adopt the api E3 snapshot and run the fictional signup end to end against the real core (public form → D1 → D2 → welcome mail → screen 03; add-dog → N-37) — gate E3

## Objective
Work package WP-04-6 (front half) + S04 T-04-34: the whole E3 scenario runs in the browser against the published core image, and the S04 contract is adopted from the real snapshot (no `pending.json` left for S04).

## Spec references
S04 T-04-34 · S14 T-14-28 (the D1 part: validate from D1 → back with «2 · Preinscripcions pendents») · gate E3 in `backlog` (organizer) · E1-W04 (`scripts/core-stack`, `scripts/e2e-core.sh`, host-aware Vite proxies, local mailbox access) · api E3-T05 (`bin/e3-smoke`, seed with the D1 pending signups).

## Context to load
`e2e/core/*` and `scripts/core-stack/*` of E1-W04 · E3-W01/E3-W02 · the staged `openapi.next.json` (organizer: api E3-T05 snapshot) · this file.

## Steps
0. **Contract adoption**: swap the staged snapshot (organizer: api E3-T05 snapshot, which already includes the E3-T03 add-dog option fields — `additionalDogOption`, `upfront.additionalDogOptions[]`, `upfront.additionalDog`, `AddDogSignupResult.checkout`) byte for byte, prune `pending.json` of every S04/S14 operation now published, regenerate, align fixtures (Ajv) and consumers.
1. `e2e/core/e3-signup.spec.ts` (serial, real core + seed + mailbox): public signup with family group `FOUND` (holder from the seed), SEPA without IBAN → D1 shows the row with «Compte no informat» and the counter «Preinscripcions n» → D2 → inform the IBAN (edit) → [VALIDA L'ALTA] (level, next invoice date, amount) → N-02 captured from the mailbox → welcome link → screen 03 as the new member; second scenario: signup without family group and a rejection from D2 (N-03 captured); third: as an active member «＋ AFEGEIX UN GOS» (13 → 17 → 19) → D1 «(nou gos)» → validation → N-37 in the app inbox; fourth: `signup.enabled=false` (parameter set through D11) → 16 shows only `closedText`.
2. Screenshots at 375 (16–19, enviada, 03) and 1280 (D1 with real data, D2) under `roadmap/evidence/E3-W03/`.
3. `ROADMAP.md`: gate E3 checklist (front) ticked with the run evidence; vocabulary lint over `signup` and `admin-census` in ca/es (T-04-34).

## Fixed conventions
- Same Docker wrapper as E1-W04 (`pnpm e2e:core`); no secrets printed; fictional data only.

## Verification (run all; evidence = command + exit code + last 40 lines; long outputs → `roadmap/evidence/<ID>/*.log`)
- `pnpm e2e:core` green (the E1 scenarios + the E3 ones); `pnpm turbo run lint typecheck test build` + `pnpm i18n:check` green; CI green after publication.

## Definition of Done
Gate E3 demonstrated through the browser against the real core; S04 contract adopted.

## Evidence to return
e2e tails, screenshots, ROADMAP checklist.

---

## Executor report
_(not started)_

## Organizer verification
_(pending)_
