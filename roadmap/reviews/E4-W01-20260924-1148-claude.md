<!-- independent review · task E4-W01 · reviewer claude:claude-opus-5-5 · executor claude · commit b12013e · 2026-09-24 11:48 -->

# Review E4-W01: minor issues

This is round 2. The review covers commit `b12013e` against the seven points in the Organizer verification. All seven are fixed. Nothing blocks verification.

## Findings
1. **minor** `roadmap/MESSAGES.md` (2026-09-24 entry, 3rd bullet) and `roadmap/tasks/E4-W01.md` (round-2 Questions §2): the executor says the `clubs` PWA "builds its client the same way" with `navigator.language` and "probably needs the same fix". That is wrong. `apps/clubs/src/main.tsx:55-63` already passes `getLocale: () => i18n.resolvedLanguage ?? branding.defaultLocale` to both `createAuthenticatedApiClient` and `createApiClient`. Only clubs-admin had the gap, and this task fixes it (`apps/clubs-admin/src/App.tsx:674-682`). **Fix:** the organizer should not open a follow-up for `clubs` from this note. Correct or remove the bullet.
2. **minor** Round-2 evidence in `roadmap/tasks/E4-W01.md` (lines 306-330) does not follow the AGENTS.md evidence rule, which asks for the last 40 lines of every Verification command:
   - `api:generate`, the Ajv suite, `i18n:check` and the Vitest planning run get a one-line paraphrase each.
   - turbo gets about 17 lines, e2e about 25.

   The logs exist and back the claims:
   - `11` has 24/24.
   - `13` has 26/26 tasks, 0 cached, clubs-admin 75.
   - `15` has clubs 21, id 1, clubs-admin 29.
   - `16` has 27.

   So this is a formatting gap, not missing proof. **Fix:** paste the tails, or accept the log references.
3. **minor** `apps/clubs-admin/src/planning/TemplatesPage.tsx:1150-1159` and `:1192-1199`: `removeClass` and `removeBand` bypass the new `patchQueue`. If the admin clicks «Treu de la plantilla» while a level PATCH is still queued, the DELETE runs first. The queued PATCH then gets 404 `NOT_FOUND` against a card that has already been remounted in create mode. The error is not shown and the data is not corrupted, but it is the same ordering problem that review point 2 addressed. **Fix:** optional. Chain the two DELETEs on `patchQueue.current`, or bump `patchGeneration` before the DELETE.
4. **nit** `TemplatesPage.tsx` `templateOpening` (`?? { close: "22:00", open: "07:00" }`, `?? "07:00"`, `?? "22:00"`): hard-coded opening hours are the fallback when `GET /club/opening-hours` has no entry or fails. They only decide which field shows `OUTSIDE_OPENING_HOURS`, so the effect is cosmetic. Still, they are invented values that are not in `CATALEG_PARAMETRES`. Placing the error on "start"/"end" by comparing the sent times with each other, without assumed hours, would avoid them.
5. **nit** `apps/clubs-admin/src/planning/TemplateDayPage.tsx:89-96`: `history.replaceState` rewrites the URL, but App's `location` state (`App.tsx` `useState(location)`) still holds `/dia/sunday` until the next navigation. It does no harm today because the page derives `day` itself. Calling `onNavigate` in replace mode, if one is added later, would keep the router and the URL in sync.
6. **nit** `scripts/e2e-docker.sh:15-21`: by default, evidence is copied back only for tasks whose status is `in_progress`. When the organizer or a reviewer runs the script while no task is `in_progress`, nothing is copied back. The script only prints a line saying no task was selected, so re-captured PNGs are lost unless `E2E_EVIDENCE_TASK` is set. Consider documenting the override in AGENTS.md or the playbook.
7. **nit** `ClassForm.tsx:208-219`: a failed PATCH reverts all `values` to the saved class with `initialValues(...)`. That also clears any not-yet-committed text in «Descripció» or «Límit». It only matters when a chip change fails while the admin is still typing.

Round-2 fixes, checked and correct:
- **PATCH queue:** each PATCH reads `versionRef` when it is sent. After a failure, `patchGeneration` drops the changes queued behind it. On `STALE_VERSION` the page reloads the template and remounts the card. The test really fires two clicks without waiting and asserts `version = first + 1`.
- **Failed PATCH:** the form reverts through `savedItem`, and its test asserts the chip, the placeholder and the cell.
- **Failed loads:** a failed coverage, weeks or candidates load shows the toast with [Torna-ho a provar], and the weeks table gets `[]` on error.
- **`x-schema-overlays`:** the generator fails when the snapshot publishes an overlaid property, shown in `12-round2-overlay-guard.log`, and the Ajv suite merges the overlay the same way.
- **Progression:** `description.ts` and the mock resolver both apply E29, and a new test checks that fixture descriptions equal the resolver's output.
- **Nits #7, #8, #10:** each has a test (#7, #8) or has been reformatted with Prettier (#10).

## Not checked
- I did not re-run anything: lint, typecheck, Vitest, Ajv, `i18n:check`, `api:generate` and the Docker Playwright suite. I checked the counts against the committed logs `10…16-round2-*.log` only.
- I did not open the re-captured PNGs. The claim that the Catalan UI shows «D i sup.» and «F i sup.» with no «and up» is covered only by the new e2e assertions.
- I did not check es/en rendering of the D3 page in a browser.
- The {F,G} → «F i sup.» conflict with E4-W02's `fixtures/calendar.ts` and `planning-calendar.spec.ts:157` is not verified here. The executor flagged it in MESSAGES, and it belongs to E4-W02 or E4-W06. The full e2e run (29 passed) suggests the spec does not reach the PATCH path today.
- I did not check whether `default: true` on the overlay makes `progression` required in the generated `Level` response type (the executor says it does) while the real api does not send it yet. The runtime code treats a missing value as `true` (`!== false`), so this is only a type/contract gap until E4-W06.
