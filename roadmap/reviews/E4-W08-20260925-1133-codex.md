<!-- independent review · task E4-W08 · reviewer codex:gpt-6-astra · executor claude · commit 52c2af6 · 2026-09-25 11:33 -->

# Review E4-W08 — minor issues

## Findings

1. **minor** `apps/clubs/src/activities/ActivityDetailPage.tsx:100` — Reason validation does not recognize the actual API response. The adjacent core’s `CancellationDeadline.java:14` throws `VALIDATION_ERROR` with `details: {field: "reason"}`; its exception handler preserves those details and returns 400. This helper accepts only `fieldErrors[]`, so the error closes the confirmation instead of appearing on «Motiu». An in-memory check confirmed the mismatch. The new mock at `packages/api-client/src/mocks/activity-handlers.ts:947` and both status variants of the UI test reproduce a different shape · Step 1, R-07-09, mocks-follow-API DoD · Support both field representations, test the actual payload, and reconcile the task’s 422 instruction with the backend/catalog.

2. **minor** `apps/clubs-admin/src/activities/ActivityRegistrantsPage.tsx:51` — Member choices silently exclude registrations beyond the first 1,000 rows because the loader ignores `totalPages`. S07 §3 permits activities without a capacity limit, and cancelled registrations also accumulate; there is no 1,000-registration invariant. An in-memory run with 1,001 distinct members returned 1,000 choices and never requested page 1 · Step 8, S07 §6, CONVENCIONS_API §4 · Fetch subsequent pages or implement member search, with a regression test selecting a member beyond the first page.

3. **nit** `apps/clubs-admin/src/activities/ActivitiesPage.test.tsx:862` — The new regression suites use `E4-W08`/`R-07-*` names without the applicable acceptance-test IDs. The report’s explanation that T-07-06/T-07-23 are backend tests overlooks frontend IDs T-07-29 and T-07-30 · AGENTS rule 5 and the task objective · Add T-07-29 to the D7 suite and T-07-30 to the app suite while retaining individual rule references.

## Not checked

- Full lint/typecheck/build, Vitest and Docker Playwright were not rerun under the read-only restriction; committed logs and screenshots were inspected.
- Live backend integration, tenant isolation, transactional concurrency, audit/outbox effects and Spanish/English browser flows were not exercised. Backend comparison was source-only.