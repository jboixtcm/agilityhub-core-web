<!-- independent review · task E4-W12 · reviewer claude:claude-opus-5-5 · executor claude · commit c64aefe · 2026-09-26 03:33 -->

# Review E4-W12 — minor issues

## Findings
1. **minor** `packages/i18n/src/locales/en/admin-census.json:251` + `apps/clubs-admin/src/dashboard/SignupReviewPage.test.tsx` (step 2 `it.each`, row `["en","NIE",…,"ID document"]`). Step 2 says a DNI or NIE reads «DNI/NIE» in ca, es **and en**. In en, D2 shows "ID document" for DNI/NIE, and the new test locks that in. It is also inconsistent with en screen 16 (`signup.json` `idDniNie`: "DNI / NIE"). With "ID document" the en row names no type, while PASSPORT reads "Passport". **Fix:** en `signupReview.fields.idDocument` → "DNI/NIE" (first check that no other D2 or readmission label reuses this key with a generic meaning), and update the test row.

2. **minor** `apps/clubs/src/SignupPage.tsx` (DogStep, `config.texts.therapyIntro` just above the new add-dog branch). In add-dog mode the modality block is now read-only (step 4). The intro paragraph still says «…selecciona l'opció d'Abonat o Pack; si d'entrada no faràs classes en grup, selecciona l'opció Teràpia». It is visible in `roadmap/evidence/E4-W12/17-add-dog-375.png`, directly above a line the member cannot change. **Fix:** hide `therapyIntro` when `addDog` is set, and add a Vitest assertion in the step 4 test.

3. **minor** `packages/ui/src/day-grid.css:63-71`. At 375 px both 10 and 23 now show «manteni…» (`10-avui-cells-375.png`, `23-visio-global-core-375.png`). Mockups 10 (`10-avui-classes-del-dia-quadre.html:193`) and 23 (`23-instructor-visio-global-quadre-del-dia.html:190`) show «manteniment» whole. The step does allow an ellipsis, but the task's own spec reference (AGENTS rule 6, «nothing clipped at the design widths») and the pixel-fidelity goal point the other way. **Organizer's call:** accept it, or ask for a subtitle size, letter-spacing or padding that fits «manteniment» in the 375 px cell.

4. **minor** `apps/clubs/src/SignupPage.tsx:1302` (`planPrice`). The comment cites S05 R-05-19, but every non-PACK plan is formatted as «{price}/mes». R-05-19 says `SINGLE_CLASS` → «{preu}/classe». The bug was already in the cards; the new add-dog line now reuses it, so a SINGLE_CLASS member would read «… · 25,00 €/mes». **Fix:** add a `signup:dog.singleClassPrice` key (ca/es/en) plus a Vitest case, or drop the R-05-19 claim and log it as a follow-up.

5. **minor** `apps/clubs/src/SignupPage.tsx:~2613` (Assumption 3). In add-dog mode, a member without `planId` now gets no modality block and sends no `planIdRequested`. That case is real: S05 B34 says instructors are migrated without a modality. Such a member has no way to pick a plan on 17, and nothing shows the api accepts `POST /me/dogs/signup` without `planIdRequested`. No Vitest or core test covers this path. **Fix:** confirm the api contract (organizer or api task), or show the cards when `member.planId` is absent, and add a test either way.

6. **minor (tracking)** Steps 3 and 4 are still not visible on the real core:
   - `17-dog-core-375.png`: the Teràpia price slot is now empty. Before this task it showed the conditions text.
   - `17-add-dog-core-375.png`: there is no modality line at all.

   The staged `packages/api-client/openapi/openapi.next.json` (api `bdc5e66`) still has no `SignupPlan.priceLabel` (its `SignupPlan` adds only `billingMode`). The pending adoption will not close Questions 1 and 2, and the `pending.json` overlay will survive it. The organizer needs to open the api tasks for Q1 (`priceLabel` on `GET /signup`) and Q2 (the member's own plan in `plans`, or `member.plan`).

7. **nit** `apps/clubs-admin/src/planning/CalendarPage.tsx:~577`. The accessible name `${title} · ${place}` is built by string concatenation, not from an ICU key, so the word order is fixed for every locale (AGENTS rule 1). The subtitle `${place} · …` already did this before the task. **Suggestion:** one key, `calendar.cell.activityAllRings` «Activitat · {title} · totes les pistes», in ca/es/en.

8. **nit** `packages/api-client/openapi/pending.json:55`. The overlay description embeds the Cànic seed text «condicions i cost segons cada cas» inside `packages/*` (AGENTS rule 2: club literals only in branding and fixtures). It does no harm, but the description should be generic.

9. **nit (evidence)** Step 9's `--repeat-each=20` ran only the fixed test, with 9 parallel workers (`26-e2e-onboarding-repeat-20.log`: «Running 20 tests using 9 workers … 20 passed»). The old version was never reproduced, which the report discloses. The run meets the DoD but does not prove the fix. Separately, `pnpm e2e:docker` (log 29) ran before the last `format.ts` comment re-wrap, which is also disclosed and has no runtime effect.

Checked and consistent with the report:
- The "fails before the fix" logs `01`–`05`, `09` and `10` show the expected `×` failures, and the only runs that "fail before the fix" by design are the guard tests.
- `docs/specs/*` changes in `c64aefe` are organizer verification notes, not executor edits.
- `scripts/e2e-docker.sh` still runs the complete suite when called without Playwright arguments.
- `x-schema-overlays` is a mechanism that `generate.mjs` already supported.

## Not checked
- I ran no command: this session was read-only and Bash was partly denied. Turbo (log `31`: 34/34), `i18n:check`, the real-core e2e (12 + 5 + 7) and the Docker e2e (31 + 44 + 1) are taken from the committed logs.
- I did not check whether the real core accepts `title` in `fields` when the «Activitat» column is hidden. No core e2e hides that column, so the always-`title` request was only exercised against MSW.
- I did not check that `POST /me/dogs/signup` accepts an omitted `planIdRequested` (finding 5), or a plan outside `GET /signup.plans`. The second case passed the existing core add-dog flow before and after this task, so it is probably fine.
- I only glanced at the captures: 17 (MSW), 17 add-dog, 10/23, D7 registrants at 1280 and D4 all-rings. I did not compare the D2 or D5 captures against the mockups pixel by pixel.
