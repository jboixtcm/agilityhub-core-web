<!-- independent review · task E3-W11 · reviewer claude:claude-opus-5-5 · executor claude · commit 2461212 · 2026-09-25 22:36 -->

# Review E3-W11 — minor issues

Steps 0–2 are implemented as asked:
- The adopted `openapi.json` is byte-identical to the staged `openapi.next.json`, and `pending.json` has no matching entry to prune.
- Both review findings are fixed.
- Every new D2 test is shown failing before the fix (`01`: 10 failed; `02`: 6/6 step-2 tests failed), and the new mock tests fail on the HEAD mocks (`08`).

The findings below are about evidence and small edge cases. None of them blocks the task.

## Findings

1. **minor** `roadmap/tasks/E3-W11.md:61`, `roadmap/evidence/E3-W11/09-e2e-core.log` — The claim "the four Verification commands pass on the final tree" does not hold for the committed tree.
   - Commit 2461212 also carries E3-W12, including new real-core tests in the same shared files (`e2e/core/e1-core.spec.ts`, `e2-core.spec.ts`, `e3-signup.spec.ts`).
   - E3-W11's run `09` covers 10 + 4 tests. The committed tree has 12 + 5.
   - The only run on the committed tree is `roadmap/evidence/E3-W12/08-e2e-core.log`. It exits 1 on the same local image: 3 E3-W12 tests fail on `legalAddress` / `documentTypes`, which that core does not send.
   - So `pnpm e2e:core E3-W11` on the published tree is red today. The failures come from E3-W12's tests, not E3-W11's code.
   - The turbo evidence also predates E3-W12's edits (api-client 165 and clubs 162 here, against 172 and 166 in `E3-W12/10-turbo-final.log` at 22:29). That later run is green.
   - Fix: the organizer re-runs `pnpm e2e:core E3-W11` on an image with api E3-T16 (or accepts E3-W12's re-run for the shared files), and the report says which tree its runs cover.

2. **minor** `roadmap/tasks/E3-W11.md:204`, `packages/api-client/src/mocks/fixtures/branding-canic.json` — The report says the core image is revision `c9eff8f`, "the adopted snapshot's".
   - That snapshot marks `ClubSummary.legalAddress` and `MeDogs.documentTypes` as **required**.
   - E3-W12's run on the same local image shows `/branding` without `legalAddress` and `/me/dogs` without `documentTypes` (`E3-W12/branding-club-core.json`, `me-dogs-core.json`).
   - So either the image is not `c9eff8f`, or the snapshot is ahead of its own implementation. Either way, the mocks and the fixture contract test now require fields the core used for evidence does not return.
   - The report's "the real core answers exactly what the mocks answer" is only shown for the D2 probes (`d2-*-core.json`).
   - Fix: confirm the image's revision label, and send the mismatch to the api if the snapshot is ahead.

3. **nit** `apps/clubs-admin/src/dashboard/SignupReviewPage.tsx:323` — `proposedDate` is not gated by `viewCurrent`.
   - Without a plan change, «Data del proper rebut» keeps the stale view's `proposals.nextInvoiceDate` while a reload is pending or after it failed.
   - After a plan change, the same field goes blank, because `quoteResult` is undefined.
   - The proposal is part of the quote (R-04-15, dry run `nextInvoiceDate`). Step 1 says "the current quote is dropped", so the two cases should behave the same.
   - There is no harm, because VALIDA is disabled. Fix: `const proposedDate = !viewCurrent ? undefined : …`.

4. **nit** `SignupReviewPage.tsx:741` and `:745–746` — After `STALE_VERSION`, and until the admin clicks [Torna a carregar], the old upfront block stays visible and [VALIDA L'ALTA] stays enabled.
   - Also, during a pending or failed reload, [EDITA LES DADES] and [REBUTJA] stay enabled and act on the stale view (old `paymentMethods`, old `version` → another 409).
   - This matches the step's wording ("when a reload starts"), and the api's optimistic locking protects the data.
   - Optional fix: treat a `stale` `decisionError` as not `viewCurrent`, and disable edit/reject while `loadState !== "ready"`.

5. **nit** `e2e/core/e3-signup.spec.ts` (the add-dog probe, `expect(addDogMethodPatch.status).toBe(400)`) — The test only asserts status 400.
   - The step → test table claims "the add-dog method → 400 `READ_ONLY`".
   - The captured JSON does show `fieldErrors[{field: paymentMethod, code: READ_ONLY}]`, but the test does not check it.
   - Fix: `toMatchObject({ status: 400, body: { code: "VALIDATION_ERROR", details: { fieldErrors: [{ field: "paymentMethod", code: "READ_ONLY" }] } } })`, as the CARD probe already does.

6. **nit** `scripts/e2e-core.sh:48–56` — `run_core_suite || status=$?` turns off `set -e` inside the function.
   - A failed `docker compose up -d --wait` no longer stops the stage: `run playwright` still runs, and only its exit code is kept.
   - The run fails anyway in practice, but the log becomes harder to read.
   - Fix: `docker compose … up -d --wait mongo seed core || return $?` inside `run_core_suite`.

7. **nit** `apps/clubs-admin/src/dashboard/SignupReviewPage.test.tsx:858`, `:966` — The new tests are named «E3-W11 step 1/2 · …», not after spec ids.
   - AGENTS rule 5 asks for names like T-04-33, T-04-14 or T-04-20.
   - The describes do cite R-04-15, R-04-10 and R-04-19, which keeps them traceable. Consider adding the T-ids for the E3-W09 traceability pass.

8. **nit** `packages/api-client/src/mocks/scenarios.ts:53` — `(brandingMinim as NullableBranding) as Branding` hides the fixture's `legalAddress: null` from the type system.
   - Code that dereferences `branding.club.legalAddress` typechecks but would crash on the minimal club.
   - This is acceptable as a stopgap while report Q1 (nullable `$ref` in the snapshot) is open. It should go away with Q1's fix.

## Not checked

- I did not re-run any command (read-only review). Vitest, turbo, `i18n:check`, `e2e:docker` and the real-core e2e were judged from the committed logs.
- `pnpm e2e:core` on an image that includes api E3-T16. The revision of the image used for `09` cannot be checked from the repo.
- The screenshots `D2-signup-payment-method-core-1280.png` and `D2-signup-add-dog-method-core-1280.png` against the D2 mockup.
- The E3-W12 and E3-W08 parts of the same commit (footer, screen 13, `/parameters` mocks, `NOT_ALLOWED`), except where they affect E3-W11's evidence.
