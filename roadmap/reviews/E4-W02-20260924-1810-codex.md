<!-- independent review · task E4-W02 · reviewer codex:gpt-6-astra · executor claude · commit 1a82922 · 2026-09-24 18:10 -->

# Review E4-W02 — needs changes

## Findings

1. **major** `apps/clubs-admin/src/planning/CreateClassDrawer.tsx:209` — A delayed `RING_HAS_BOOKINGS` response can restore an obsolete confirmation. Submit for ring A, change to ring B while the request is pending, then receive A’s conflict: `setBookings()` restores A’s list, but confirmation calls `create(true)` with B’s current fields. This can cancel bookings the administrator never reviewed, violating R-06-05 and round-3 correction 1. Reproduced using the component source in an in-memory harness. Disable placement fields while pending, or associate responses and confirmation with an immutable request. Add a delayed-response test; the current test changes fields only after the conflict arrives.

2. **major** `apps/clubs-admin/src/planning/calendar-shared.tsx:99` — Sampling offsets at wall-time-as-UTC ±12 hours does not always capture both sides of a transition. Executing the production helper with `("2026-04-05", "02:30", "Pacific/Auckland")` returns `2026-04-04T14:30:00Z`, the **second** occurrence; R-06-14 requires `2026-04-04T13:30:00Z`. Both offset samples fall after the transition. Ring blocks can therefore be submitted an hour late for a supported club timezone. Use transition-safe offset discovery or a timezone resolver with explicit disambiguation, fix the duplicated mock helper, and add this regression case.

3. **minor** `roadmap/tasks/E4-W02.md:562` — The final Ajv evidence includes selected lines rather than the literal last 40; the Vitest evidence at line 573 substitutes a prose description and four summary lines. This still violates AGENTS.md session-protocol step 4 and the organizer’s explicit evidence instruction. Paste the actual tails from `22-r3-ajv.log` and `23-r3-vitest-calendar.log`.

## Not checked

- Full Vitest, build and Docker Playwright suites were not rerun under the read-only constraint; their committed logs were inspected.
- No fresh screenshot comparison or interactive browser verification.
- Real-core tenant isolation, authorization, transactions, audit/outbox and notification delivery were not exercised; those remain integration checks for E4-W05.