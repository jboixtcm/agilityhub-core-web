<!-- independent review · task E3-W09 · reviewer claude:claude-opus-5-5 · executor claude · commit 6cf867e · 2026-09-26 01:18 -->

# Review E3-W09: minor issues

The four runs are real and green, and the logs match the report:
- `01-e2e-core.log`: stages of 12, 5 and 7 tests, and N-37 `SENT`.
- `oauth-token-calls.log`: 43 non-2xx calls, all `REFRESH_EXPIRED`, and one of them sent with the cookie.
- The Vitest JSON reports: 167, 268 and 181 tests, `success: true`.
- The `file:line` references in `trace-tests.md` point at the right tests.

The problems are in how `screens.md` classifies and explains some differences, plus a few process gaps.

## Findings
1. **minor** `roadmap/evidence/E3-W09/screens.md` (D2 row (d), the summary's «still open: the full DNI») and the report's Questions: the full DNI/NIE on D2 is marked **new (no masking decision)**, and the organizer is asked about it again.
   - A decision already exists: the organizer's ruling of 24-09 at the E3-W05 verification (`roadmap/tasks/E3-W05.md:357`, `roadmap/tasks/E3-W07.md:111`, the S04 changelog line 379 «D2 mostra el DNI/NIE sencer, com D10»). The D2 test cites it (`SignupReviewPage.test.tsx:161`).
   - Row 16 (a), the typed DNI in full, is covered by S03 R-03-27, which masks `idDocument` only on screen 28.
   - Step 5 says **accepted** needs a decision, and AGENTS says not to re-decide.
   - Fix: mark both **accepted** (organizer 24-09; R-03-27) and drop the question.
2. **minor** `screens.md` D2 `NOT_FOUND_PENDING` (c): «1 adjunt» beside empty notes is left as «to check», but the code shows it is a defect.
   - `apps/clubs-admin/src/dashboard/SignupReviewPage.tsx:664,675` puts a badge on «Notes als instructors» that counts `dog.documents[].files`. So the vaccination card is counted again as a notes attachment.
   - S04 §3 (l.81) and §13 #1 say the signup notes have no attachments.
   - The mockup shows 3 documents and «1 adjunt». The T-04-33 test #56 asserts «3 adjunts» for the 3 documents (`SignupReviewPage.test.tsx:171`), so the test locks in the wrong behaviour.
   - Report it as **new** with a fix-task proposal, and name the test.
3. **minor** `screens.md` 19 (d): the missing «how to pay» paragraph under the total is left as «cause not established», but the cause can be traced.
   - That paragraph is `paymentMethods[type=MANUAL].instructions` from `GET /signup` (`apps/clubs/src/SignupPage.tsx:1970,2399`; MSW `signup-config-canic.json:74`).
   - The contract has the field (`SignupPaymentMethod.instructions`), so the real core or its seed sends «Efectiu» without it.
   - This is an api/seed data gap, not a web defect, and it answers the executor's first question. Committing `GET /signup`'s `paymentMethods` as JSON evidence would prove it.
4. **minor** Setup, and AGENTS «Do exactly one roadmap task per session»: the gate re-run ran on `a999f15` plus the uncommitted E4-W05 changes, which are still awaiting verification.
   - Those changes touch shared code: `packages/i18n/src/format.ts`, `packages/ui/src/day-grid.tsx`, and the `openapi.json` snapshot swap with `schema.d.ts`.
   - So the audited tree is the published `6cf867e`, not `a999f15` and not a verified tree.
   - The organizer should record `6cf867e` as the audited commit, and re-run if E4-W05 or E4-W11 changes those shared files before the gate closes.
5. **minor** Step 5 coverage gaps:
   - «19 before day 16» is E3-W08's MSW capture, with the old fixture footer `G00000000`, not a real-core capture. The reason given (no test clock in the core) is plausible, but the organizer has to accept this substitution explicitly.
   - `13-document-types-core-375.png` shows the «Tipus» select closed, with only «Cartilla de vacunes». The seed's document types are not visible in the capture; the claim rests on the e2e assertion and `me-dogs-core.json`. An opened select, or listing the types in `screens.md`, would close this.
6. **nit** `screens.md` misses some visible differences (step 5 asks for every one):
   - Back office (D1, D2, D11): a top bar with the brand, a native «Català ▾» select and the export and user icons; the mockups have no top bar. S3 and S5 cover only the member shell and the sidebar.
   - D2 and 17: the chip is shown in full; the mockup has «941·······». This is accepted under R-03-27 / S03 §13, but not listed.
   - D2 `NOT_FOUND_PENDING`: «Domiciliació · — · titular: la mateixa» with a «—» where the account would be, next to the red «Compte no informat» box.
   - D2: the image-consent warning puts its icon above the text; the mockup has it inline.
   - D2 add-dog: «tarifa familiar en validar» and a required «Data del proper rebut» for a member already validated into the group. Worth checking against R-04-25.
   - 19 add-dog: the heading «tria quan vols començar:» sits over a single option.
   - 16: the «DNI / NIE» input is misaligned with the passport input.
   - 13 dialog: the backdrop covers only the first 844 px of the full-page capture.
7. **nit** `03-home-new-member-core-375.png` has a blank content area: the capture fires once `.clubs-shell` is visible but before the route renders. The same test in E4-W05's run captured «Aviat · Aquesta pantalla estarà disponible aviat.». The description «empty member shell» is accurate, but the capture depends on timing.
8. **nit** `trace-tests.md` §2, T-14-25 is called covered somewhat generously:
   - Only three risk states are asserted: #37 has «anul·lada · avisada», «en risc · avisat» and «1 inscrit», and #48 adds «s'anul·larà».
   - «Compte no informat only on Núria» is proven only by `getByText` finding a single match, not by checking which row it is on.
   - Tighten the wording, or mark the clause «partial».
9. **nit** There is no `CHANGELOG.md` entry for E3-W09; E3-W05 had one (`CHANGELOG.md:69`). This is step 5 of the AGENTS session protocol.

## Not checked
- I re-ran no command; the review is read-only and Bash was mostly denied. The logs and JSON reports were checked for internal consistency only.
- I spot-checked about 13 of the captures (19, both D1s, D2 normal / add-dog / `NOT_FOUND_PENDING`, 13 pending and dialog, 16, 17, 17 and 19 add-dog, 18, 01, 03). I did not check D2 readmission, D11, «enviada», 16 closed or the 17 document-required state.
- Whether image `1308743` really contains every api E3 fix; I relied on MESSAGES 25-09.
- CI on `main` after the publish.
- The E4-W05 product changes in the same commit, which are out of this task's scope.
- Whether the consolidated list's «family-plan add-dog» and «validation with the family fare» have asserting web tests; the task's step 6 list leaves them out.
