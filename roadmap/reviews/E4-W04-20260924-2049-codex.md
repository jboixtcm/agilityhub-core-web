<!-- independent review · task E4-W04 · reviewer codex:gpt-6-astra · executor claude · commit d95b199 · 2026-09-24 20:49 -->

# Review E4-W04 — needs changes

## Findings

1. **major** `apps/clubs-admin/src/activities/ActivitiesPage.tsx:506` — Synchronous exports fail. Both this handler and `ActivityRegistrantsPage.tsx:270` assume JSON, but the contract permits `200` PDF/XLSX responses. An in-memory reproduction with the installed client throws `SyntaxError` on `%PDF-1.7`. MSW returns only `202`, hiding the failure · CONVENCIONS_API §4, steps 2/6 · Handle binary downloads separately from queued jobs and test both response types.

2. **major** `apps/clubs/src/activities/ActivityDetailPage.tsx:193` — Impersonated cancellation always sends `{}` without collecting a reason. R-07-09 requires a reason for impersonation, and the referenced backend’s `CancellationDeadline.check()` rejects its absence. MSW instead accepts this and records a MEMBER cancellation · R-07-09/10, step 1 · Add the impersonated reason flow and matching tests; reconcile step 8’s `{}` instruction with the backend requirement.

3. **major** `apps/clubs-admin/src/activities/ActivityPage.tsx:992` — Rich text remains editable during saving because `readOnly` ignores `busy`; disabling the surrounding fieldset does not disable a contenteditable div. Text entered while PATCH is pending is subsequently discarded by `applySaved()` resetting the form · Step 3, editing correctness · Make the editor read-only while busy, or preserve subsequent edits. Test with a deferred PATCH response.

4. **minor** `apps/clubs-admin/src/activities/ActivityRegistrantsPage.tsx:272` — Export requests omit `current.q`, although the displayed list applies it. Searching for one member and exporting therefore includes other members · CONVENCIONS_API §4 requires the same list parameters · Forward the search query and assert it in an export test.

5. **minor** `apps/clubs/src/activities/ActivityDetailPage.tsx:134` — Cancellation eligibility compares the deadline with the fixed load timestamp. A page opened before the deadline continues offering cancellation afterward, including when opening the confirmation dialog · R-07-09, step 8 explicitly requires `now ≥ cancellableUntil` handling · Update eligibility when the deadline passes and recheck before confirmation; test without remounting the page.

6. **minor** `apps/clubs-admin/src/activities/shared.tsx:139` — An INSTRUCTOR cannot read `/parameters/{key}`. The swallowed `403` becomes `levelsEnabled: true`, so a club with levels disabled still shows “Nivells” in instructor maintenance · R-07-14, MATRIU_PERMISOS, step 3 · Use an instructor-readable capability or an approved response-derived fallback; test the disabled-level variant with parameter access denied.

7. **minor** `apps/clubs-admin/src/activities/ActivityPage.tsx:838` — Time choices always use club opening hours, including activities outside the club with no linked rings. An offsite activity at 06:00 cannot be entered when the club opens at 07:00 · R-07-05 restricts ring-block windows, not offsite activities · Offer the full day when no ring blocking applies and test this case.

8. **minor** `apps/clubs/src/App.tsx:1244` — The new member-detail route has authentication and module guards but no MEMBER role guard. An instructor-only session can enter it and request the member-only API, ending in an authorization error · AGENTS rule 3, step 1, S07 §6 · Add the appropriate MEMBER/IMPERSONATED guard and a negative role test.

9. **minor** `apps/clubs-admin/src/activities/CancelActivityModal.tsx:39` — If the preview contains zero registrants and someone registers before confirmation, `ADMIN_TEXT_REQUIRED` leaves the modal permanently in its simple-confirm branch. It cannot collect text, and every retry sends `undefined` · R-07-06, step 5 · Refresh the preview and display the required notice field after this response.

10. **minor** `apps/clubs-admin/src/activities/ActivityRegistrantsPage.tsx:335` — The filter menu exposes only state, origin and registration date; the required `memberId` filter is missing · Step 6 · Add a member filter with localized labels and verify that it survives saved views and exports.

## Not checked

- Full lint/typecheck/build, Vitest and Docker Playwright were not rerun under the read-only restriction; committed logs and screenshots were inspected.
- Live backend integration, tenant isolation, transactional concurrency, audit/outbox effects and browser flows in Spanish/English were not independently exercised.