<!-- independent review · task E3-W12 · reviewer claude:claude-opus-5-5 · executor claude · commit 2461212 · 2026-09-25 22:40 -->

# Review E3-W12: minor issues

## Findings
1. **major (evidence)**: Verification 3 (`pnpm e2e:core E3-W12`) exits 1 (`roadmap/evidence/E3-W12/08-e2e-core.log`). The task was still set to `awaiting_verification`.
   - The step 5 check «real-core screenshots of 01 and 16 show «Cabrera de Mar» and the office line» is not met. The local core's `/branding` has `taxId: null` and no `legalAddress` (`branding-club-core.json`).
   - The step 4 check «lists the seed's document types» is not met either: `me-dogs-core.json` has `documentTypes: "absent"`.
   - The executor says this is an environment limit: the local image has no E3-T16, and `docker pull` was denied. That is plausible, but I cannot confirm it.
   - Oddity: the local image is labelled revision `c9eff8f`, the same revision as the adopted snapshot, and that snapshot *declares* `legalAddress` and `documentTypes` as required.
   - Fix: the organizer re-runs `pnpm e2e:core E3-W12` on the published `5b158ae` image before verifying. The three new real-core tests must pass with no soft failures.
2. **minor**: `e2e/core/e3-signup.spec.ts` (last test, «E3-W12 step 5 · the signup's public footer…»): the 16 capture is not screen 16.
   - The test runs after the «signup closed» test in the same serial file, on a seed where signup stays disabled.
   - So `16-footer-core-375.png` is the closed-signup page. It is byte-identical in size (14669 B) to `16-signup-closed-core-375.png`, and I opened it: it shows «Les inscripcions estan tancades…».
   - Step 5 asks for the real-core screenshot of screen 16.
   - Fix: re-open signup (or run this check before the closing test), then capture `/apuntat-hi` step 1 with the form visible.
3. **minor**: `packages/api-client/src/mocks/fixtures/branding-canic.json:7-12`: step 5 says «The mocks carry the Cànic's values», but the fixture keeps a made-up tax id and office.
   - It has `taxId: "G00000000"` and a made-up office «Carrer de la Riera, 1 · 08392 …».
   - S02's 25-09 change log and MESSAGES:625 give the real values: G63189617 and «Carrer Sant Pere, 10 · 08392 Sant Andreu de Llavaneres». R-02-10 now says the Cànic seed carries its real legal identity.
   - The task's own «Fixed conventions: fictional data only» contradicts step 5. The executor picked fictional data silently: the report says «a fictional office» but does not flag the conflict with step 5.
   - Fix: either align the fixture with the seed (club identity is not personal data; AGENTS rule 7 covers people), or raise the conflict as a question. The mock e2e (`apps/clubs/e2e/access.spec.ts`), the `App.test.tsx` T-01-18 assertion and `01-footer-375.png` would change with it.
4. **minor**: `packages/api-client/src/mocks/handlers.ts` `parametersForbidden`: an impersonation gets `403 FORBIDDEN`. That code is assumed, not verified (report Assumption 1).
   - The snapshot says for `/parameters/*`: «ADMIN endpoints reject impersonation».
   - Elsewhere the snapshot spells out impersonation → `IMPERSONATION_DENIED` (for example the attachments and attendances operations). MATRIU rule 3 says the token is «prohibit» on ADMIN endpoints.
   - An admin's impersonation token probably still carries the ADMIN role, so the real guard may well answer `IMPERSONATION_DENIED`.
   - Low impact now that the PWA never reads `/parameters`, but «mocks behave like the api» applies.
   - Fix: check `SettingsController`'s guard in the api (or add the impersonated case to the real-core e2e), then align the mock and the `impersonated` row of the step 3 test.
5. **minor (process)**: E3-W11 and E3-W12 were done in the same session, against AGENTS «Do exactly one roadmap task per session».
   - They share one commit and several files: `handlers.ts`, `signup-handlers.test.ts`, `scripts/e2e-core.sh`, `fixtures.contract.test.ts` and the snapshot adoption. The step 0 adoption is credited to E3-W11.
   - Each task can no longer be verified or reverted on its own. For example, `clubPaymentProviders` `MANUAL.enabled: true` (E3-W11) changes mock behaviour that E3-W12's tests also run on.
   - Also, `roadmap/evidence/E3-W12/` duplicates about 70 E3-W11 real-core screenshots and JSONs (same byte sizes), because `e2e:core` writes the full run into each task dir.
6. **nit**: `apps/clubs/src/SignupPage.test.tsx:1902`: the rewritten assertion is always true.
   - It reads `expect(getByText(`${name} · ${city}`).textContent).not.toMatch(/·.*·/u)`. An exact `getByText` of «name · city» already guarantees a single «·».
   - The old check (the whole footer has no tax-id separator) is now implied only by the exact match. Drop the line, or assert on `footer.children[0]`.
7. **nit**: the new Vitest and e2e cases are named «E3-W12 step N …» rather than after S03 test ids (AGENTS rule 5). The existing T-03-40 own-dogs cases are the natural home for them. Consider prefixing them with T-03-40 (or the T-02-07 footer id).
8. **nit**: `packages/ui/src/branding.tsx`: `legalAddress` is typed by hand, because openapi-typescript drops the `null` of a nullable `$ref`.
   - The same workaround appears in `scenarios.ts` (`NullableBranding`) and `fixtures.contract.test.ts` (`nullableReferences`).
   - It is reasonable and was reported (Question 2). The api-side fix (E3-T16 round 2) should be tracked so all three workarounds can be removed.

## Not checked
- The real-core behaviour with api E3-T16 (`5b158ae`): `documentTypes` from the seed, the `legalAddress` line on 01 and 16, and the api's error code for an impersonation on `/parameters`. The only core that ran lacked E3-T16.
- I did not re-run any command (read-only). Verifications 1, 2 and 4, and the before-fix logs `01` and `03`, were checked only by reading the committed logs:
  - `10-turbo-final.log`: 34/34 tasks, clubs 166, api-client 172.
  - `11-i18n-check-final.log`: 2328 keys ×3.
  - `09-e2e-docker.log`: 1 + 39 + 29 passed.
  - `01`: 5 failures before the fix. `03`: 9 failures before the fix.
- That the report's audit of `apps/clubs` calls against the MATRIU MEMBER column is complete. My grep of the call sites found no remaining `/parameters` read in `apps/clubs` or shared packages.
- 1280 px layouts: they don't apply (all touched screens are mobile).
