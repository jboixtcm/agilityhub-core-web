<!-- independent review · task E3-W05 · reviewer codex:gpt-6-astra · executor claude · commit e16797f · 2026-09-24 17:02 -->

# Review E3-W05 — needs changes

## Findings

1. **major** `roadmap/evidence/E3-W05/trace-tests.md:55` — “none found” overstates T-04-33 coverage. Neither mapped test changes the plan or asserts `dryRun` recalculation; the current selector contains only one option (`SignupReviewPage.tsx:479`). Likewise, T-04-29’s “none found” at line 51 overlooks that the invalid-document test exercises local validation, not an HTTP 400 with `fieldErrors`. These are explicit S04 §11 requirements. Mark these behaviors as untested/partial and reconcile the table with the consolidated audit’s findings. Correct the evidence without changing product code or assertions.

2. **minor** `roadmap/evidence/E3-W05/trace-tests.md:31` — The inventory collapses 12 D2 cases into one row and six audit cases into another, omitting their individual names and locations. The reported “25 tests” counts a mixture of suites and test cases; the mapped files contain 41 cases, including parameterized cases. Step 6 requires every matching test. Expand these rows into fully qualified test names, locations and results.

3. **minor** `roadmap/evidence/E3-W05/screens.md:41` — Several descriptions do not match the committed captures. D1 shows **15** alerts, including four “anul·lada” rows, and **0/58** places—not 16 alerts, all awaiting cancellation, and 0/75. Line 48 also claims `{deadlineDay}` appears on add-dog screen 19, but that capture has no payment-day text. This undermines the final-run comparison required by step 5. Recheck the final PNGs and update both `screens.md` and the corresponding executor-report claims.

4. **minor** `roadmap/evidence/E3-W05/screens.md:44` — Raw D11 translation keys are classified as “accepted” because E4-W06 will fix them; line 41 similarly labels TER/PENDENT chart columns “accepted as known,” although E35 explicitly excludes them. A scheduled correction is not an approved visual exception. Under step 5, classify these as **new**, noting the existing follow-up task, and reserve **accepted** for deviations actually authorized by a decision.

## Not checked

- Did not rerun Docker, builds or test suites under the read-only review constraint; assessed committed logs, assertions and captures.
- Did not independently verify CI, the core image revision, or the backend cache-invalidation implementation.