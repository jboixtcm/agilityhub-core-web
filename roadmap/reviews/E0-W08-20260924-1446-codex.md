<!-- independent review · task E0-W08 · reviewer codex:gpt-6-astra · executor claude · commit e8b18ee · 2026-09-24 14:46 -->

# Review E0-W08 — needs changes

## Findings

1. **major** `packages/course-core/package.json:10` — Exporting raw TypeScript exposes the recovered implementation to consumers’ compiler settings. Both recovered packages disable `exactOptionalPropertyTypes`, but the apps inherit `true`. Reproduction: `pnpm --filter @agilityhub/shared-types exec tsc --noEmit --exactOptionalPropertyTypes true` fails with TS2379 at `marker-svg.ts:344` and `:360`. Importing either package into an app therefore introduces typecheck failures · E0-W08 step 1 requires monorepo tooling compatibility · Make the internal optional-property types compatible without changing runtime behavior, and check a consumer using the shared strict configuration.

2. **minor** `packages/course-core/README.md:70` — JSON Schema publication for `CourseData` and `BuildSessionExportV1` is explicitly deferred, although S16 §14.5 includes it in WP-16-A′, the work package named by this task. The narrower task Steps/DoD omit it, leaving scope unresolved · Publish the schemas, or have the organizer explicitly assign this requirement to a follow-up task before treating WP-16-A′ as complete.

3. **minor** `packages/config/eslint.config.mjs:205` — The color-rule exemption covers the entire marker-page renderer, including decorative orange, yellow and grey constants at `marker-svg.ts:222`, not just the black/white fiducial. Detection requirements do not justify bypassing theming for the footer · AGENTS.md hard rule 2 · Source decorative colors from the shared tokens and narrow the exception to the fiducial, or document an explicit organizer-approved exception for this recovery.

4. **minor** `docs/specs/S06-planificacio-setmanes-i-calendari.md:94` — The commit also changes scheduling coverage, migration rules, catalog seeds and release decisions across five documentation files, but the executor report does not identify these changes. They are outside E0-W08 and alter other tasks’ acceptance criteria · AGENTS.md’s single-task scope and reporting requirements · Attribute these changes if they came from concurrent organizer synchronization; otherwise separate them for independent review.

## Not checked

- Independent Vitest execution: both runs stopped before collecting tests because the read-only sandbox denied creation of temporary SSR directories (`EPERM`), even with `--configLoader runner --no-cache`.
- Full build and Docker Playwright execution were not rerun; their committed logs were inspected.