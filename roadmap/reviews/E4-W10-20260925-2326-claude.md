<!-- independent review · task E4-W10 · reviewer claude:claude-opus-5-5 · executor claude · commit a2bc0f6 · 2026-09-25 23:26 -->

# Review E4-W10 — minor issues

## Findings
1. **minor** `apps/clubs-admin/e2e/activities.spec.ts:158`, `apps/clubs/e2e/activities.spec.ts:84`, `apps/clubs/src/App.test.tsx:435`: Step 3 is only partly done.
   - **What's wrong:** Step 3 asks for T-07-29 on the D7 suites and T-07-30 on the app suites of E4-W08 (plural). Only the two Vitest describes were renamed (`ActivitiesPage.test.tsx:861`, `activities.test.tsx:492`). Four places still have no T-id:
     - the D7 e2e describe «E4-W08 D7 follow-ups»;
     - the app e2e test «E4-W08 R-07-09/10 impersonated…»;
     - the app Vitest describe «E4-W08 S07 §6 /activitats/:id is for MEMBER…»;
     - the new «E4-W10 D7 on a day the club is closed» e2e describe (`activities.spec.ts:221`).
   - **Why:** AGENTS rule 5; this is the same finding as E4-W08 review #3.
   - **Fix:** add T-07-29 / T-07-30 to those names and keep their R-07-* references.

2. **minor** `packages/api-client/src/mocks/activity-handlers.ts` (no `OUTSIDE_OPENING_HOURS` or `openingHours` anywhere in the file) together with `apps/clubs-admin/src/activities/ActivityPage.tsx:871-886`: D7 on a closed day gets only half of «D4's treatment».
   - **What happens:** the note shows and no new times are offered. But `withValue` keeps the saved 18:30/20:30, and [DESA]/[PUBLICA] stay enabled (assumption 2).
   - **Mock gap:** the activity mock never checks the opening hours. In mock mode, publishing the Torneig on a closed Friday succeeds, but the api answers `422 OUTSIDE_OPENING_HOURS`.
   - **Why:** S07 §6 lists `POST …/publication` → 422 `OUTSIDE_OPENING_HOURS`; R-07-05 says blocks must be «dins `club.openingHours`» and T-07-04 says «06:00 amb obertura 07:00 → `OUTSIDE_OPENING_HOURS`». The task's fixed convention is «Mocks behave like the api». D4 blocks submission on a closed day ([Crear classe] date error, block drawer, card [ACCEPTA]); D7 does not.
   - **Fix:** make publication and the PATCH re-sync in the activity mock check the ring-block window against `club.openingHours`, with a mock test. Either block [PUBLICA] on a closed day with rings, or route that decision explicitly. The executor flagged this (assumptions 2 and 4); the organizer should decide.

3. **nit** `apps/clubs-admin/src/planning/CalendarPage.test.tsx` («E4-W10 D4 follow-ups of the E4-W09 review»), `TemplatesPage.test.tsx` («E4-W10 D3 closed days…»), and the E4-W10 describes in `e2e/planning-calendar.spec.ts` and `e2e/planning-templates.spec.ts`: the new D4/D3 suites cite only R-ids, no T-id.
   - **Why:** each file already has its T-06-28 (D4) and T-06-26 (D3) suites; AGENTS rule 5.
   - **Fix:** prefix the new describes with T-06-28 / T-06-26.

4. **nit** `apps/clubs-admin/src/planning/TemplatesPage.test.tsx` («S06 §3 a failed GET /club/opening-hours … instead of assuming 07:00–22:00»): the test only checks that [Torna-ho a provar] appears and then disappears.
   - It asserts neither the error text nor anything about «not assuming 07:00–22:00», so the name promises more than it checks.
   - It still fails before the fix (log `08`), so it is valid as a regression test.
   - **Fix:** assert the toast message, and that after the retry a closed day again shows the band-drawer note.

5. **nit** Step 5 «read-only sessions never pass `{}`»: no test covers it (the report says «no observable behaviour»).
   - A cheap guard: record requests and assert that an INSTRUCTOR on D4/D3 never calls `GET /club/opening-hours`.
   - `ActivityPage.tsx:414` still reads the opening hours for non-editable views (INSTRUCTOR, FINISHED). It is harmless because the alert is gated by `editable`, but it is inconsistent with D4/D3.

6. **nit** `apps/clubs-admin/src/planning/SelectedClassCard.tsx:276-279`: on a closed day the chip editors stay enabled, but [ACCEPTA] silently disables after any non-notes change.
   - The note says only «El club està tancat aquest dia». Nothing tells the admin that only «Notes» can be saved there.
   - This is the executor's assumption 1. Either disable the chips, or extend the note (a new literal, needs organizer approval).

7. **nit** `packages/i18n/src/locales/ca/admin-scheduling.json` `templates.bandForm.closedDays`: «El club està tancat {days}: …» renders as «El club està tancat dilluns».
   - Idiomatic Catalan is «tancat els dilluns». With several days it reads «tancat dilluns, dimarts».
   - The es literal uses a different structure («cerrado: {days}.»).
   - There is no mockup for this string and the executor flagged it. The organizer should confirm the three locales.

8. **nit** Out of scope, needs routing: `details.field` is now accepted only by `ActivityDetailPage.isFieldError`.
   - The same commit changes `CONVENCIONS_API.md` so that `details.field` is a general single-field shape.
   - These helpers still read only `fieldErrors[]`, so a single-field `VALIDATION_ERROR` from the core would land as a general error there:
     - `apps/clubs-admin/src/activities/shared.tsx:76` (D7; e.g. R-07-01 `ringIds`);
     - `apps/clubs-admin/src/planning/shared.ts:106`;
     - `apps/clubs/src/SelfServicePages.tsx:138`;
     - `apps/clubs/src/SignupPage.tsx:506`.
   - **Suggestion:** one shared helper in `packages/api-client` in a follow-up task.

9. **nit** Evidence:
   - `14-e2e-docker.log` ran before E3-W12 round 2 changed shared D7 files. For the final tree, the report points to `roadmap/evidence/E3-W12/22-e2e-docker.log`: 29 + 42 + 1 passed, «8 successful», but that log has no `exit` line.
   - `E3-W12/23-turbo.log` has `exit 0` (clubs-admin 266, clubs 166).
   - Everything I checked matches the report:
     - `01`: 1 failed / 5 passed.
     - `02`: 2 of 2 failed.
     - `03`: 7 failed / 1 passed, on the expected assertions («Unable to find … Soci Prova0994 · 5993 (1)», «Unable to find a label … Notes»).
     - `07`: 12 passed.
     - `15`: 34/34, 0 cached, exit 0.
     - `11`: i18n check complete, exit 0.

## Not checked
- I did not run the Verification commands (read-only session); I judged them from the committed logs.
- The api's real behaviour, because I did not open `agilityhub-core-api`:
  - the `CancellationDeadline.check` shape (400, `details.field`);
  - whether `PATCH /class-sessions/{id}` accepts a notes-only change on a closed day;
  - whether `WeekTemplateRules` refuses a band PATCH with unchanged times on a closed day, as the mock now does.
- Whether the api's `memberLastName,asc` + `registeredAt,asc` sort is a total order, so that 1000-row pages never overlap. Rows added or cancelled between page reads could shift the offsets; this is rare and I did not flag it.
- Who wrote the `docs/specs/00-transversal/CONVENCIONS_API.md` edit in this batch commit. The text credits «organitzador 25-09», but the docs are supposed to be synced, not edited by the executor.
- Screenshots: I looked at D7 and D4; neither has a mockup. I did not open D3.
