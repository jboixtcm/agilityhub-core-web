<!-- independent review · task E4-W14 · reviewer claude:claude-opus-5-5 (a separate session the organizer started: the automatic review did not run) · executor claude · commits d98a0a8 and 8192323 · 2026-09-26 15:50 -->

# Review E4-W14 — needs changes

## Findings
1. **major** `packages/api-client/src/mocks/activity-handlers.ts:497-501` (the preview uses it at `:841`), `activity-handlers.test.ts:800`, `apps/clubs-admin/src/activities/ActivityPage.tsx:535,697`: the mock `ring-conflicts` preview still answers 200 where the api refuses the window, and the new step-1 test asserts that 200.
   - **The api.** `ActivityContext.blockRequest` calls `RingBlockWindow.of`, which answers 400 `INVALID_TIME_RANGE` in two cases the mock misses (`agilityhub-core-api/.../activities/domain/RingBlockWindow.java:11-12`, unchanged since E4-T04):
     - an activity with rings has no date, no `startTime` or no `endTime`;
     - its `ringBlockWindow` does not contain `[startTime, endTime]`.
   - **The spec and contract say the same.** T-07-04: «finestra que no conté l'activitat → 400». The contract staged in `67a4797` (before the second session re-checked the diff) and adopted in `8192323` says: «400 INVALID_TIME_RANGE without a date or hours».
   - **The mock.** `ringBlockWindowProblem` returns `undefined` when `from` or `to` is `null` (`:501`) and never checks that the window contains the activity. The test sets `{10:00, 10:30}` over a 10:00–12:00 activity and expects `{conflicts: [], trainingBookings: []}` (`:800`); the api answers 400 there.
   - **What users see on the api.** A new draft starts at the club with no times (the api's create leaves the date empty too).
     - An admin picks rings, leaves «Hora d'inici» empty and clicks [PUBLICA].
     - The preview's 400 has no field, so `saveErrorTargets` (`fields[0] ?? "endTime"`) puts «L'interval horari no és vàlid.» under «Hora de final».
     - R-07-04's `ACTIVITY_INCOMPLETE` is never reached: no «Falten dades…», no «Cal per publicar» on each empty field (the registration period included).
     - MSW shows the confirmation and the `ACTIVITY_INCOMPLETE` path instead, so no test sees the api's behaviour.
   - **Why it matters.** Step 1 («the mock answers like the api»), the fixed convention «Mocks behave like the api», and the E4-W11 organizer verification («The mock answers the same»).
   - **Fix.**
     - Mirror `RingBlockWindow.of` in the mock: with rings, a `null` date, start or end → 400; `from > startTime || to < endTime` → 400.
     - Make the `:800` case expect 400.
     - In `startPublish`, when the draft has rings but no date or hours, skip the preview and send `POST …/publication`, so `ACTIVITY_INCOMPLETE` lands on the empty fields. Add a Vitest for it.
     - Same helper, older gap: the mock saves a PATCH that clears a time of a PUBLISHED activity with rings. The api refuses it with 422 `ACTIVITY_INCOMPLETE`, because it validates a published activity as a publication (`ActivityService.java:77`).

2. **minor** `apps/clubs-admin/src/planning/TemplatesPage.tsx:1101,1182,1222` with `ClassForm.tsx:231`: a single page-wide generation counter drops every change queued behind a successful class DELETE, including another class's change, and nothing tells the admin.
   - **How it happens.**
     - The grid stays clickable while «Treu de la plantilla» of class A waits in the queue (`onCell` → `setCardMode`, `:1565`, no guard).
     - The admin opens class B and clicks a chip. The PATCH takes the old ticket.
     - After A's DELETE succeeds, the counter is bumped and B's change is dropped. `DroppedChangeError` puts B's chip back, and `ClassForm` shows no message for a drop.
     - The admin sees the chip flip back with no reason.
     - The same happens to B's change queued behind A's failed PATCH: A's message was in A's form, which is gone.
   - **Why it matters.** The organizer asked for «the removal queue that drops no send silently» (E4-W06 verification). Dropping is justified only for changes of the removed class.
   - **Fix.** Drop only changes of the removed or refused class (a ticket per class id). Before sending the others, read the template version again, as `removeBand` does now. Or show a message when a dropped change has no error on screen.

3. **minor** `e2e/core/e4-core.spec.ts:1108-1113`: the es check that the row leaves 03 after the member's own cancellation can pass before 03 renders.
   - `useMeActivities` (`apps/clubs/src/activities/shared.ts:26`) has no cache, so 03 renders its fallback until it has parsed the answer.
   - `toHaveCount(0)` right after `waitForResponse` is satisfied on its first poll, whatever the answer contains. The ca check at `:963-966` has the same weakness.
   - **Why it matters.** Step 9 asks the spec to assert that the row goes; a check that cannot fail proves nothing.
   - **Fix.** Assert on the answer: `mine[]` has no ACTIVE or WAITLISTED entry for `seminarId`. Or wait for something 03 renders only once the list is ready, then count 0.

4. **nit** `apps/clubs-admin/src/catalogs/SettingsPage.test.tsx:273`: «R-06-06 S05 §12 B32 the mock levels include «Pendent»…» checks D11's switch and code, which is the S05 §12 seed. It does not check R-06-06 (coverage), which `TemplatesPage.test.tsx` covers. Remove R-06-06 from the name (AGENTS rule 5).

5. **nit** `apps/clubs/e2e/activities.spec.ts:148`: the title promises «the start alone without an end», but both asserted rows have an end (9:00–13:00, 10:00–12:00). Only Vitest covers a row without an end. Rename the test, or add a mock activity without an end and assert «· 9:00».

6. **nit** `apps/clubs-admin/src/planning/TemplatesPage.test.tsx:757`: the comment says «the DELETE's error in the card», but no line asserts it. Add a `findByText` for the error `ClassForm.remove` shows.

7. **nit** `apps/clubs-admin/src/styles.css:798`: `.catalog-page .ah-button__content` is the fourth per-page copy of the same inline-icon rule:
   - `activities/activities.css:30`;
   - `planning/planning.css:34`;
   - `styles.css:163`.

   `.ah-button__content` has no layout rule in `packages/ui/src/components.css`, so other icon buttons can still wrap. Move the rule into the design system.

8. **nit** `packages/api-client/src/mocks/planning-handlers.ts:624-633` (report assumption 1): the mock `/coverage` leaves out an active progression level that has no fixture figures. R-06-06 gives every active progression level a row, and T-06-05 turns a zero denominator into `NO_DOGS`. Keep the api's shape with a zero row (`status: "NO_DOGS"`) or with figures computed from the mock's dogs and classes.

9. **nit** Step 9 evidence: the `seed-e4.log` line «week start 2026-09-21» does not prove on its own that the option was used (E4-W05 review #3).
   - That line prints the seed's effective week start, and the date passed is also the seed's default anchor (previous-or-same Monday, `DemoSeedCommand.java:48-49`). An ignored `--week-start` would print the same line.
   - The actual proof is in the api code: `seed:demo` rejects unknown options (`DemoSeedCommand.java:29`).
   - To prove it from the log, run once with `E4_WEEK_START` set to another Monday.

## Not checked
- **No commands run.** I ran no Vitest, turbo, `i18n:check`, Playwright or Docker. I judged the runs from the committed files: logs `01` and `14`–`17`, the `seed-*.log` files and `e4-core-run.json`. They match the report:
  - before the fix: 2/73, 1/5, 6/37 and 15/116 failing;
  - turbo: 34/34 tasks, 0 cached;
  - `i18n:check`: 2368 keys in each of the three locales;
  - real core: 12 + 5 + 7 passed;
  - Docker: 33 + 47 + 1 passed.

  Exit codes appear only in the report, not in the logs.
- **Screenshots.** I did not open them; I checked only their sizes. They are 375 or 1280 px wide, except `D11-nivells-1280.png`, a 960 px capture of the card. The e2e tests that took them assert what they show.
- **Api side.** I read only the preview and re-sync path (`RingBlockWindow`, `ActivityContext.blockRequest`, `ActivitiesController.ringConflicts`, `ActivityService.create/patch`) and `DemoSeedCommand`. I did not check myself that the api accepts the risk exemption on a closed day; that rests on the E4-W11 review and the organizer.
- **E4-W13 and the working tree.** I did not review E4-W13's code in `8192323` or the uncommitted tree. In `8192323`, the es chip check of `e4-core.spec.ts` already uses `waitlistRank`, but `ringBlockWindowProblem` is unchanged (`:500`).
