<!-- independent review · task E3-W04 · reviewer codex:gpt-6-astra · executor claude · commit a7c5c00 · 2026-09-24 16:22 -->

# Review E3-W04 — needs changes

## Findings

1. **major** `apps/clubs-admin/src/dashboard/SignupReviewPage.tsx:362` — A `FOUND` family claim whose holder returns `familyGroupId: null` now becomes `undefined` and hits the early return at line 368. Validation never reaches the API, leaving this signup blocked. S04 R-04-13 and T-04-16 explicitly allow a holder without a group: validation creates it. Step 3 requires omitting `null`, not rejecting this case. Allow validation with `familyGroupId` omitted and let the API resolve the stored claim. Update `SignupReviewPage.test.tsx:109`, which currently asserts the incorrect rejection, to assert a successful POST without that field.

2. **nit** `roadmap/tasks/E3-W04.md:171` — The report substitutes shortened, selectively filtered excerpts for the required last 40 output lines of turbo, real-core e2e, and complete Playwright verification. AGENTS.md’s evidence rule explicitly requires those tails. Paste the actual tails from the committed logs.

## Not checked

- Did not rerun builds, Vitest, Docker, or Playwright in this read-only review; execution results were assessed from committed evidence.
- Did not independently exercise backend tenant isolation, transactions, audit/outbox, or group creation.
- Inspected D2 and D10 screenshots; did not perform a full visual comparison of all captured screens or verify post-publication CI.