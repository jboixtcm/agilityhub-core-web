<!-- independent review · task E4-W03 · reviewer codex:gpt-6-astra · executor claude · commit efa2941 · 2026-09-24 15:06 -->

# Review E4-W03 — needs changes

## Findings

1. **major** `apps/clubs/src/today/useDayGrid.ts:28` — Invalid date parameters crash rendering. Both `?date=2026-13-01` and `?date=2026-08-32` pass the regex, then `toISOString()` throws `RangeError: Invalid time value` inside the initial state calculation. This bypasses the screen’s error handling · Task step 3 (`?date=` navigation and error states) · Check that the parsed timestamp is finite before calling `toISOString()`, fall back to club-local today, and add invalid-date tests.

2. **major** `apps/clubs/src/today/shared.tsx:35` — Formatting a business date through noon UTC shifts it forward in clubs at UTC+12 or later. Confirmed: requested `2026-08-04` displays Wednesday, August 5 in `Pacific/Auckland` and `Pacific/Kiritimati`. Week chips also combine the shifted weekday with the original day number · R-06-14 · Use a date-only formatter or construct the instant in the club’s zone; test the header, chips and requested date together in these zones.

3. **minor** `packages/api-client/src/mocks/day-grid-handlers.ts:49` — The new detail and cancellation handlers ignore the caller’s role, impersonation and tenant. Member scenarios receive staff class counters and block notes/authors, and can cancel fixture blocks. The `/day-grid` permission check does not protect these handlers · S06 §6 and `MATRIU_PERMISOS.md` · Apply role-specific projections and mutation authorization, scope fixtures by tenant, and add negative member/impersonation/cross-tenant tests. This concerns mock fidelity and test coverage; it does not establish a production API vulnerability.

4. **minor** `roadmap/tasks/E4-W03.md:76` — Verification excerpts do not satisfy the required evidence format: Turbo has five summary lines, Vitest has a reconstructed selection, and Docker omits intervening output instead of preserving the last 40 lines. The Vitest commands are also described rather than provided as exact invocations · AGENTS session protocol §4 · Paste each exact command, exit code and actual final 40 lines, retaining the existing full-log references.

## Not checked

Full Vitest/build and Docker Playwright suites were not rerun under the read-only constraint. Backend authorization, tenant isolation, concurrency, transactions and audit/outbox behavior were not exercised. Unrelated organizer changes bundled into the commit were outside this review.