<!-- independent review · task E3-W10 · reviewer codex:gpt-6-astra · executor claude · commit 3e0c28f · 2026-09-24 21:25 -->

# Review E3-W10 — needs changes

## Findings

1. **major** `packages/api-client/src/mocks/mock-worlds.budget.test.ts:41` — The budget misses startup-only construction. Fixtures are imported before timing begins, and resets reuse precomputed data: `initialWeekTemplates` runs `withInconsistencies` during import, while `resetPlanningState` only clones its result. An in-memory probe added 100 ms to each of those three initializer calls; all 13 test bodies still passed despite 300 ms of additional startup work. Equality of the resulting worlds does not prove that initialization work was timed, contrary to the report. **E3-W10 Step 1 requires coverage of initial fixture construction.** Add a budget for fresh initialization, retain the reset measurements, and correct the report’s coverage claim.

2. **minor** `packages/api-client/src/mocks/mock-worlds.budget.test.ts:79` — The parity assertion introduces a clock-dependent failure. `startupWorlds` captures planning data at import, but resets calculate the current club week again. Crossing Monday midnight between those operations changes the expected dates. An in-memory probe advancing the clock by 200 ms across that boundary produced 12 passes and a parity failure. This undermines the task’s stability objective. Freeze `Date` before importing the fixtures, keep `performance.now()` real for timing, and restore the clock afterward.

## Not checked

- Did not independently rerun Turbo, Vitest, Docker Playwright, or hosted CI.
- The targeted probes executed transpiled test bodies with assertion shims in memory, not the Vitest runner; no files were changed.